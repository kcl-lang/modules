"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema ExternalMariaDB:
    r"""
    ExternalMariaDB is the Schema for the external MariaDBs API. It is used to define external MariaDB server.

    Attributes
    ----------
    apiVersion : str, default is "k8s.mariadb.com/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ExternalMariaDB", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : K8sMariadbComV1alpha1ExternalMariaDBSpec, default is Undefined, required
        spec
    status : K8sMariadbComV1alpha1ExternalMariaDBStatus, default is Undefined, optional
        status
    """


    apiVersion: "k8s.mariadb.com/v1alpha1" = "k8s.mariadb.com/v1alpha1"

    kind: "ExternalMariaDB" = "ExternalMariaDB"

    metadata?: v1.ObjectMeta

    spec: K8sMariadbComV1alpha1ExternalMariaDBSpec

    status?: K8sMariadbComV1alpha1ExternalMariaDBStatus


schema K8sMariadbComV1alpha1ExternalMariaDBSpec:
    r"""
    ExternalMariaDBSpec defines the desired state of an External MariaDB

    Attributes
    ----------
    connection : K8sMariadbComV1alpha1ExternalMariaDBSpecConnection, default is Undefined, optional
        connection
    host : str, default is Undefined, required
        Hostname of the external MariaDB.
    image : str, default is Undefined, optional
        Image name to be used to perform operations on the external MariaDB, for example, for taking backups.
        The supported format is `<image>:<tag>`. Only MariaDB official images are supported.
        If not provided, the MariaDB image version be inferred by the operator in runtime. The default MariaDB image will be used in this case,
    imagePullPolicy : str, default is Undefined, optional
        ImagePullPolicy is the image pull policy. One of `Always`, `Never` or `IfNotPresent`. If not defined, it defaults to `IfNotPresent`.
    imagePullSecrets : [K8sMariadbComV1alpha1ExternalMariaDBSpecImagePullSecretsItems0], default is Undefined, optional
        ImagePullSecrets is the list of pull Secrets to be used to pull the image.
    inheritMetadata : K8sMariadbComV1alpha1ExternalMariaDBSpecInheritMetadata, default is Undefined, optional
        inherit metadata
    passwordSecretKeyRef : K8sMariadbComV1alpha1ExternalMariaDBSpecPasswordSecretKeyRef, default is Undefined, optional
        password secret key ref
    port : int, default is 3306, optional
        Port of the external MariaDB.
    tls : K8sMariadbComV1alpha1ExternalMariaDBSpecTLS, default is Undefined, optional
        tls
    username : str, default is Undefined, required
        Username is the username to connect to the external MariaDB.
    """


    connection?: K8sMariadbComV1alpha1ExternalMariaDBSpecConnection

    host: str

    image?: str

    imagePullPolicy?: "Always" | "Never" | "IfNotPresent"

    imagePullSecrets?: [K8sMariadbComV1alpha1ExternalMariaDBSpecImagePullSecretsItems0]

    inheritMetadata?: K8sMariadbComV1alpha1ExternalMariaDBSpecInheritMetadata

    passwordSecretKeyRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecPasswordSecretKeyRef

    port?: int = 3306

    tls?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLS

    username: str


schema K8sMariadbComV1alpha1ExternalMariaDBSpecConnection:
    r"""
    Connection defines a template to configure a Connection for the external MariaDB.

    Attributes
    ----------
    healthCheck : K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionHealthCheck, default is Undefined, optional
        health check
    params : {str:str}, default is Undefined, optional
        Params to be used in the Connection.
    port : int, default is Undefined, optional
        Port to connect to. If not provided, it defaults to the MariaDB port or to the first MaxScale listener.
    secretName : str, default is Undefined, optional
        SecretName to be used in the Connection.
    secretTemplate : K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionSecretTemplate, default is Undefined, optional
        secret template
    serviceName : str, default is Undefined, optional
        ServiceName to be used in the Connection.
    """


    healthCheck?: K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionHealthCheck

    params?: {str:str}

    port?: int

    secretName?: str

    secretTemplate?: K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionSecretTemplate

    serviceName?: str


schema K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionHealthCheck:
    r"""
    HealthCheck to be used in the Connection.

    Attributes
    ----------
    interval : str, default is Undefined, optional
        Interval used to perform health checks.
    retryInterval : str, default is Undefined, optional
        RetryInterval is the interval used to perform health check retries.
    """


    interval?: str

    retryInterval?: str


schema K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionSecretTemplate:
    r"""
    SecretTemplate to be used in the Connection.

    Attributes
    ----------
    databaseKey : str, default is Undefined, optional
        DatabaseKey to be used in the Secret.
    format : str, default is Undefined, optional
        Format to be used in the Secret.
    hostKey : str, default is Undefined, optional
        HostKey to be used in the Secret.
    key : str, default is Undefined, optional
        Key to be used in the Secret.
    metadata : K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionSecretTemplateMetadata, default is Undefined, optional
        metadata
    passwordKey : str, default is Undefined, optional
        PasswordKey to be used in the Secret.
    portKey : str, default is Undefined, optional
        PortKey to be used in the Secret.
    usernameKey : str, default is Undefined, optional
        UsernameKey to be used in the Secret.
    """


    databaseKey?: str

    format?: str

    hostKey?: str

    key?: str

    metadata?: K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionSecretTemplateMetadata

    passwordKey?: str

    portKey?: str

    usernameKey?: str


schema K8sMariadbComV1alpha1ExternalMariaDBSpecConnectionSecretTemplateMetadata:
    r"""
    Metadata to be added to the Secret object.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations to be added to children resources.
    labels : {str:str}, default is Undefined, optional
        Labels to be added to children resources.
    """


    annotations?: {str:str}

    labels?: {str:str}


schema K8sMariadbComV1alpha1ExternalMariaDBSpecImagePullSecretsItems0:
    r"""
    Refer to the Kubernetes docs: https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.36/#localobjectreference-v1-core.

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    """


    name?: str = ""


schema K8sMariadbComV1alpha1ExternalMariaDBSpecInheritMetadata:
    r"""
    InheritMetadata defines the metadata to be inherited by children resources.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations to be added to children resources.
    labels : {str:str}, default is Undefined, optional
        Labels to be added to children resources.
    """


    annotations?: {str:str}

    labels?: {str:str}


schema K8sMariadbComV1alpha1ExternalMariaDBSpecPasswordSecretKeyRef:
    r"""
    PasswordSecretKeyRef is a reference to the password to connect to the external MariaDB.

    Attributes
    ----------
    key : str, default is Undefined, required
        key
    name : str, default is Undefined, optional
        name
    """


    key: str

    name?: str = ""


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLS:
    r"""
    TLS defines the PKI to be used with the external MariaDB.

    Attributes
    ----------
    clientCASecretRef : K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCASecretRef, default is Undefined, optional
        client c a secret ref
    clientCertIssuerRef : K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCertIssuerRef, default is Undefined, optional
        client cert issuer ref
    clientCertSecretRef : K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCertSecretRef, default is Undefined, optional
        client cert secret ref
    enabled : bool, default is Undefined, optional
        Enabled indicates whether TLS is enabled, determining if certificates should be issued and mounted to the MariaDB instance.
        It is enabled by default.
    galeraSSTEnabled : bool, default is Undefined, optional
        GaleraSSTEnabled determines whether Galera SST connections should use TLS.
        It disabled by default.
    mutual : bool, default is Undefined, optional
        Mutual specifies whether TLS must be mutual between server and client for external connections.
        When set to false, the client certificate will not be sent during the TLS handshake.
        It is enabled by default.
    required : bool, default is Undefined, optional
        Required specifies whether TLS must be enforced for all connections.
        User TLS requirements take precedence over this.
        It disabled by default.
    serverCASecretRef : K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCASecretRef, default is Undefined, optional
        server c a secret ref
    serverCertAdditionalNames : [str], default is Undefined, optional
        ServerCertAdditionalNames is a list of additional certificate common names
    serverCertIssuerRef : K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCertIssuerRef, default is Undefined, optional
        server cert issuer ref
    serverCertSecretRef : K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCertSecretRef, default is Undefined, optional
        server cert secret ref
    """


    clientCASecretRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCASecretRef

    clientCertIssuerRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCertIssuerRef

    clientCertSecretRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCertSecretRef

    enabled?: bool

    galeraSSTEnabled?: bool

    mutual?: bool

    required?: bool

    serverCASecretRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCASecretRef

    serverCertAdditionalNames?: [str]

    serverCertIssuerRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCertIssuerRef

    serverCertSecretRef?: K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCertSecretRef


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCASecretRef:
    r"""
    ClientCASecretRef is a reference to a Secret containing the client certificate authority keypair. It is used to establish trust and issue client certificates.
    One of:
    - Secret containing both the 'ca.crt' and 'ca.key' keys. This allows you to bring your own CA to Kubernetes to issue certificates.
    - Secret containing only the 'ca.crt' in order to establish trust. In this case, either clientCertSecretRef or clientCertIssuerRef fields must be provided.
    If not provided, a self-signed CA will be provisioned to issue the client certificate.

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    """


    name?: str = ""


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCertIssuerRef:
    r"""
    ClientCertIssuerRef is a reference to a cert-manager issuer object used to issue the client certificate. cert-manager must be installed previously in the cluster.
    It is mutually exclusive with clientCertSecretRef.
    By default, the Secret field 'ca.crt' provisioned by cert-manager will be added to the trust chain. A custom trust bundle may be specified via clientCASecretRef.

    Attributes
    ----------
    group : str, default is Undefined, optional
        Group of the issuer being referred to.
        Defaults to 'cert-manager.io'.
    kind : str, default is Undefined, optional
        Kind of the issuer being referred to.
        Defaults to 'Issuer'.
    name : str, default is Undefined, required
        Name of the issuer being referred to.
    """


    group?: str

    kind?: str

    name: str


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLSClientCertSecretRef:
    r"""
    ClientCertSecretRef is a reference to a TLS Secret containing the client certificate.
    It is mutually exclusive with clientCertIssuerRef.

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    """


    name?: str = ""


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCASecretRef:
    r"""
    ServerCASecretRef is a reference to a Secret containing the server certificate authority keypair. It is used to establish trust and issue server certificates.
    One of:
    - Secret containing both the 'ca.crt' and 'ca.key' keys. This allows you to bring your own CA to Kubernetes to issue certificates.
    - Secret containing only the 'ca.crt' in order to establish trust. In this case, either serverCertSecretRef or serverCertIssuerRef must be provided.
    If not provided, a self-signed CA will be provisioned to issue the server certificate.

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    """


    name?: str = ""


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCertIssuerRef:
    r"""
    ServerCertIssuerRef is a reference to a cert-manager issuer object used to issue the server certificate. cert-manager must be installed previously in the cluster.
    It is mutually exclusive with serverCertSecretRef.
    By default, the Secret field 'ca.crt' provisioned by cert-manager will be added to the trust chain. A custom trust bundle may be specified via serverCASecretRef.

    Attributes
    ----------
    group : str, default is Undefined, optional
        Group of the issuer being referred to.
        Defaults to 'cert-manager.io'.
    kind : str, default is Undefined, optional
        Kind of the issuer being referred to.
        Defaults to 'Issuer'.
    name : str, default is Undefined, required
        Name of the issuer being referred to.
    """


    group?: str

    kind?: str

    name: str


schema K8sMariadbComV1alpha1ExternalMariaDBSpecTLSServerCertSecretRef:
    r"""
    ServerCertSecretRef is a reference to a TLS Secret containing the server certificate.
    It is mutually exclusive with serverCertIssuerRef.

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    """


    name?: str = ""


schema K8sMariadbComV1alpha1ExternalMariaDBStatus:
    r"""
    ExternalMariaDBStatus defines the observed state of MariaDB

    Attributes
    ----------
    conditions : [K8sMariadbComV1alpha1ExternalMariaDBStatusConditionsItems0], default is Undefined, optional
        Conditions for the ExternalMariadb object.
    isGaleraEnabled : bool, default is Undefined, optional
        IsGaleraEnabled indicates that the external MariaDb has Galera enabled.
    version : str, default is Undefined, optional
        Version of the external MariaDB server.
    """


    conditions?: [K8sMariadbComV1alpha1ExternalMariaDBStatusConditionsItems0]

    isGaleraEnabled?: bool

    version?: str


schema K8sMariadbComV1alpha1ExternalMariaDBStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """


    lastTransitionTime: str

    message: str

    observedGeneration?: int

    reason: str

    status: "True" | "False" | "Unknown"

    $type: str


    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


