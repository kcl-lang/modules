"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Note:
    """
    Note is the Schema for the Notes API. A Container Analysis note is a high-level piece of metadata that describes a type of analysis that can be done for a resource.

    Attributes
    ----------
    apiVersion : str, default is "containeranalysis.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Note", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ContaineranalysisGcpUpboundIoV1beta1NoteSpec, default is Undefined, required
        spec
    status : ContaineranalysisGcpUpboundIoV1beta1NoteStatus, default is Undefined, optional
        status
    """


    apiVersion: "containeranalysis.gcp.upbound.io/v1beta1" = "containeranalysis.gcp.upbound.io/v1beta1"

    kind: "Note" = "Note"

    metadata?: v1.ObjectMeta

    spec: ContaineranalysisGcpUpboundIoV1beta1NoteSpec

    status?: ContaineranalysisGcpUpboundIoV1beta1NoteStatus


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpec:
    """
    NoteSpec defines the desired state of Note

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProvider, default is Undefined, required
        for provider
    initProvider : ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : ContaineranalysisGcpUpboundIoV1beta1NoteSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : ContaineranalysisGcpUpboundIoV1beta1NoteSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProvider

    initProvider?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecProviderConfigRef

    publishConnectionDetailsTo?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecWriteConnectionSecretToRef


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProvider:
    """
    containeranalysis gcp upbound io v1beta1 note spec for provider

    Attributes
    ----------
    attestationAuthority : [ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderAttestationAuthorityItems0], default is Undefined, optional
        Note kind that represents a logical attestation "role" or "authority".
        For example, an organization might have one AttestationAuthority for
        "QA" and one for "build". This Note is intended to act strictly as a
        grouping mechanism for the attached Occurrences (Attestations). This
        grouping mechanism also provides a security boundary, since IAM ACLs
        gate the ability for a principle to attach an Occurrence to a given
        Note. It also provides a single point of lookup to find all attached
        Attestation Occurrences, even if they don't all live in the same
        project.
        Structure is documented below.
    expirationTime : str, default is Undefined, optional
        Time of expiration for this note. Leave empty if note does not expire.
    longDescription : str, default is Undefined, optional
        A detailed description of the note
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    relatedNoteNames : [str], default is Undefined, optional
        Names of other notes related to this note.
    relatedUrl : [ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderRelatedURLItems0], default is Undefined, optional
        URLs associated with this note and related metadata.
        Structure is documented below.
    shortDescription : str, default is Undefined, optional
        A one sentence description of the note.
    """


    attestationAuthority?: [ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderAttestationAuthorityItems0]

    expirationTime?: str

    longDescription?: str

    project?: str

    relatedNoteNames?: [str]

    relatedUrl?: [ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderRelatedURLItems0]

    shortDescription?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderAttestationAuthorityItems0:
    """
    containeranalysis gcp upbound io v1beta1 note spec for provider attestation authority items0

    Attributes
    ----------
    hint : [ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderAttestationAuthorityItems0HintItems0], default is Undefined, optional
        This submessage provides human-readable hints about the purpose of
        the AttestationAuthority. Because the name of a Note acts as its
        resource reference, it is important to disambiguate the canonical
        name of the Note (which might be a UUID for security purposes)
        from "readable" names more suitable for debug output. Note that
        these hints should NOT be used to look up AttestationAuthorities
        in security sensitive contexts, such as when looking up
        Attestations to verify.
        Structure is documented below.
    """


    hint?: [ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderAttestationAuthorityItems0HintItems0]


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderAttestationAuthorityItems0HintItems0:
    """
    containeranalysis gcp upbound io v1beta1 note spec for provider attestation authority items0 hint items0

    Attributes
    ----------
    humanReadableName : str, default is Undefined, optional
        The human readable name of this Attestation Authority, for
        example "qa".
    """


    humanReadableName?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecForProviderRelatedURLItems0:
    """
    containeranalysis gcp upbound io v1beta1 note spec for provider related URL items0

    Attributes
    ----------
    label : str, default is Undefined, optional
        Label to describe usage of the URL
    url : str, default is Undefined, optional
        Specific URL associated with the resource.
    """


    label?: str

    url?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    attestationAuthority : [ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderAttestationAuthorityItems0], default is Undefined, optional
        Note kind that represents a logical attestation "role" or "authority".
        For example, an organization might have one AttestationAuthority for
        "QA" and one for "build". This Note is intended to act strictly as a
        grouping mechanism for the attached Occurrences (Attestations). This
        grouping mechanism also provides a security boundary, since IAM ACLs
        gate the ability for a principle to attach an Occurrence to a given
        Note. It also provides a single point of lookup to find all attached
        Attestation Occurrences, even if they don't all live in the same
        project.
        Structure is documented below.
    expirationTime : str, default is Undefined, optional
        Time of expiration for this note. Leave empty if note does not expire.
    longDescription : str, default is Undefined, optional
        A detailed description of the note
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    relatedNoteNames : [str], default is Undefined, optional
        Names of other notes related to this note.
    relatedUrl : [ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderRelatedURLItems0], default is Undefined, optional
        URLs associated with this note and related metadata.
        Structure is documented below.
    shortDescription : str, default is Undefined, optional
        A one sentence description of the note.
    """


    attestationAuthority?: [ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderAttestationAuthorityItems0]

    expirationTime?: str

    longDescription?: str

    project?: str

    relatedNoteNames?: [str]

    relatedUrl?: [ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderRelatedURLItems0]

    shortDescription?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderAttestationAuthorityItems0:
    """
    containeranalysis gcp upbound io v1beta1 note spec init provider attestation authority items0

    Attributes
    ----------
    hint : [ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderAttestationAuthorityItems0HintItems0], default is Undefined, optional
        This submessage provides human-readable hints about the purpose of
        the AttestationAuthority. Because the name of a Note acts as its
        resource reference, it is important to disambiguate the canonical
        name of the Note (which might be a UUID for security purposes)
        from "readable" names more suitable for debug output. Note that
        these hints should NOT be used to look up AttestationAuthorities
        in security sensitive contexts, such as when looking up
        Attestations to verify.
        Structure is documented below.
    """


    hint?: [ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderAttestationAuthorityItems0HintItems0]


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderAttestationAuthorityItems0HintItems0:
    """
    containeranalysis gcp upbound io v1beta1 note spec init provider attestation authority items0 hint items0

    Attributes
    ----------
    humanReadableName : str, default is Undefined, optional
        The human readable name of this Attestation Authority, for
        example "qa".
    """


    humanReadableName?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecInitProviderRelatedURLItems0:
    """
    containeranalysis gcp upbound io v1beta1 note spec init provider related URL items0

    Attributes
    ----------
    label : str, default is Undefined, optional
        Label to describe usage of the URL
    url : str, default is Undefined, optional
        Specific URL associated with the resource.
    """


    label?: str

    url?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : ContaineranalysisGcpUpboundIoV1beta1NoteSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecProviderConfigRefPolicy


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToConfigRef

    metadata?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToMetadata

    name: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToConfigRefPolicy


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteStatus:
    """
    NoteStatus defines the observed state of Note.

    Attributes
    ----------
    atProvider : ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [ContaineranalysisGcpUpboundIoV1beta1NoteStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProvider

    conditions?: [ContaineranalysisGcpUpboundIoV1beta1NoteStatusConditionsItems0]


schema ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProvider:
    """
    containeranalysis gcp upbound io v1beta1 note status at provider

    Attributes
    ----------
    attestationAuthority : [ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderAttestationAuthorityItems0], default is Undefined, optional
        Note kind that represents a logical attestation "role" or "authority".
        For example, an organization might have one AttestationAuthority for
        "QA" and one for "build". This Note is intended to act strictly as a
        grouping mechanism for the attached Occurrences (Attestations). This
        grouping mechanism also provides a security boundary, since IAM ACLs
        gate the ability for a principle to attach an Occurrence to a given
        Note. It also provides a single point of lookup to find all attached
        Attestation Occurrences, even if they don't all live in the same
        project.
        Structure is documented below.
    createTime : str, default is Undefined, optional
        The time this note was created.
    expirationTime : str, default is Undefined, optional
        Time of expiration for this note. Leave empty if note does not expire.
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/notes/{{name}}
    kind : str, default is Undefined, optional
        The type of analysis this note describes
    longDescription : str, default is Undefined, optional
        A detailed description of the note
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    relatedNoteNames : [str], default is Undefined, optional
        Names of other notes related to this note.
    relatedUrl : [ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderRelatedURLItems0], default is Undefined, optional
        URLs associated with this note and related metadata.
        Structure is documented below.
    shortDescription : str, default is Undefined, optional
        A one sentence description of the note.
    updateTime : str, default is Undefined, optional
        The time this note was last updated.
    """


    attestationAuthority?: [ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderAttestationAuthorityItems0]

    createTime?: str

    expirationTime?: str

    id?: str

    kind?: str

    longDescription?: str

    project?: str

    relatedNoteNames?: [str]

    relatedUrl?: [ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderRelatedURLItems0]

    shortDescription?: str

    updateTime?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderAttestationAuthorityItems0:
    """
    containeranalysis gcp upbound io v1beta1 note status at provider attestation authority items0

    Attributes
    ----------
    hint : [ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderAttestationAuthorityItems0HintItems0], default is Undefined, optional
        This submessage provides human-readable hints about the purpose of
        the AttestationAuthority. Because the name of a Note acts as its
        resource reference, it is important to disambiguate the canonical
        name of the Note (which might be a UUID for security purposes)
        from "readable" names more suitable for debug output. Note that
        these hints should NOT be used to look up AttestationAuthorities
        in security sensitive contexts, such as when looking up
        Attestations to verify.
        Structure is documented below.
    """


    hint?: [ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderAttestationAuthorityItems0HintItems0]


schema ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderAttestationAuthorityItems0HintItems0:
    """
    containeranalysis gcp upbound io v1beta1 note status at provider attestation authority items0 hint items0

    Attributes
    ----------
    humanReadableName : str, default is Undefined, optional
        The human readable name of this Attestation Authority, for
        example "qa".
    """


    humanReadableName?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteStatusAtProviderRelatedURLItems0:
    """
    containeranalysis gcp upbound io v1beta1 note status at provider related URL items0

    Attributes
    ----------
    label : str, default is Undefined, optional
        Label to describe usage of the URL
    url : str, default is Undefined, optional
        Specific URL associated with the resource.
    """


    label?: str

    url?: str


schema ContaineranalysisGcpUpboundIoV1beta1NoteStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


