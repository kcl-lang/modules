"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema StackV1:
    r"""
    StackV1 is the Schema for the StackV1s API. Manages a V1 stack resource within OpenStack.

    Attributes
    ----------
    apiVersion : str, default is "orchestration.openstack.m.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "StackV1", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1Spec, default is Undefined, required
        spec
    status : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1Status, default is Undefined, optional
        status
    """


    apiVersion: "orchestration.openstack.m.crossplane.io/v1alpha1" = "orchestration.openstack.m.crossplane.io/v1alpha1"

    kind: "StackV1" = "StackV1"

    metadata?: v1.ObjectMeta

    spec: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1Spec

    status?: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1Status


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1Spec:
    r"""
    StackV1Spec defines the desired state of StackV1

    Attributes
    ----------
    forProvider : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecForProvider, default is Undefined, required
        for provider
    initProvider : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecForProvider

    initProvider?: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecProviderConfigRef

    writeConnectionSecretToRef?: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecWriteConnectionSecretToRef


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecForProvider:
    r"""
    orchestration openstackm crossplane io v1alpha1 stack v1 spec for provider

    Attributes
    ----------
    capabilities : [str], default is Undefined, optional
        List of stack capabilities for stack.
    creationTime : str, default is Undefined, optional
        The date and time when the resource was created. The date
        and time stamp format is ISO 8601: CCYY-MM-DDThh:mm:ss±hh:mm
        For example, 2015-08-27T09:49:58-05:00. The ±hh:mm value, if included,
        is the time zone as an offset from UTC.
    description : str, default is Undefined, optional
        The description of the stack resource.
    disableRollback : bool, default is Undefined, optional
        Enables or disables deletion of all stack
        resources when a stack creation fails. Default is true, meaning all
        resources are not deleted when stack creation fails.
    environmentOpts : {str:str}, default is Undefined, optional
        Environment key/value pairs to associate with
        the stack which contains details for the environment of the stack.
        Allowed keys: Bin, URL, Files. Changing this updates the existing stack
        Environment Opts.
    name : str, default is Undefined, optional
        A unique name for the stack. It must start with an
        alphabetic character. Changing this updates the stack's name.
    notificationTopics : [str], default is Undefined, optional
        List of notification topics for stack.
    outputs : [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecForProviderOutputsItems0], default is Undefined, optional
        A list of stack outputs.
    parameters : {str:str}, default is Undefined, optional
        User-defined key/value pairs as parameters to pass
        to the template. Changing this updates the existing stack parameters.
    region : str, default is Undefined, optional
        The region in which to create the stack. If
        omitted, the region argument of the provider is used. Changing this
        creates a new stack.
    status : str, default is Undefined, optional
        The status of the stack.
    statusReason : str, default is Undefined, optional
        The reason for the current status of the stack.
    tags : [str], default is Undefined, optional
        A list of tags to assosciate with the Stack
    templateDescription : str, default is Undefined, optional
        The description of the stack template.
    templateOpts : {str:str}, default is Undefined, optional
        Template key/value pairs to associate with the
        stack which contains either the template file or url.
        Allowed keys: Bin, URL, Files. Changing this updates the existing stack
        Template Opts.
    timeout : float, default is Undefined, optional
        The timeout for stack action in minutes.
    updatedTime : str, default is Undefined, optional
        The date and time when the resource was updated. The date
        and time stamp format is ISO 8601: CCYY-MM-DDThh:mm:ss±hh:mm
        For example, 2015-08-27T09:49:58-05:00. The ±hh:mm value, if included,
        is the time zone as an offset from UTC.
    """


    capabilities?: [str]

    creationTime?: str

    description?: str

    disableRollback?: bool

    environmentOpts?: {str:str}

    name?: str

    notificationTopics?: [str]

    outputs?: [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecForProviderOutputsItems0]

    parameters?: {str:str}

    region?: str

    status?: str

    statusReason?: str

    tags?: [str]

    templateDescription?: str

    templateOpts?: {str:str}

    timeout?: float

    updatedTime?: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecForProviderOutputsItems0:
    r"""
    orchestration openstackm crossplane io v1alpha1 stack v1 spec for provider outputs items0

    Attributes
    ----------
    description : str, default is Undefined, optional
        The description of the stack resource.
    outputKey : str, default is Undefined, optional
        output key
    outputValue : str, default is Undefined, optional
        output value
    """


    description?: str

    outputKey?: str

    outputValue?: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    capabilities : [str], default is Undefined, optional
        List of stack capabilities for stack.
    creationTime : str, default is Undefined, optional
        The date and time when the resource was created. The date
        and time stamp format is ISO 8601: CCYY-MM-DDThh:mm:ss±hh:mm
        For example, 2015-08-27T09:49:58-05:00. The ±hh:mm value, if included,
        is the time zone as an offset from UTC.
    description : str, default is Undefined, optional
        The description of the stack resource.
    disableRollback : bool, default is Undefined, optional
        Enables or disables deletion of all stack
        resources when a stack creation fails. Default is true, meaning all
        resources are not deleted when stack creation fails.
    environmentOpts : {str:str}, default is Undefined, optional
        Environment key/value pairs to associate with
        the stack which contains details for the environment of the stack.
        Allowed keys: Bin, URL, Files. Changing this updates the existing stack
        Environment Opts.
    name : str, default is Undefined, optional
        A unique name for the stack. It must start with an
        alphabetic character. Changing this updates the stack's name.
    notificationTopics : [str], default is Undefined, optional
        List of notification topics for stack.
    outputs : [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecInitProviderOutputsItems0], default is Undefined, optional
        A list of stack outputs.
    parameters : {str:str}, default is Undefined, optional
        User-defined key/value pairs as parameters to pass
        to the template. Changing this updates the existing stack parameters.
    region : str, default is Undefined, optional
        The region in which to create the stack. If
        omitted, the region argument of the provider is used. Changing this
        creates a new stack.
    status : str, default is Undefined, optional
        The status of the stack.
    statusReason : str, default is Undefined, optional
        The reason for the current status of the stack.
    tags : [str], default is Undefined, optional
        A list of tags to assosciate with the Stack
    templateDescription : str, default is Undefined, optional
        The description of the stack template.
    templateOpts : {str:str}, default is Undefined, optional
        Template key/value pairs to associate with the
        stack which contains either the template file or url.
        Allowed keys: Bin, URL, Files. Changing this updates the existing stack
        Template Opts.
    timeout : float, default is Undefined, optional
        The timeout for stack action in minutes.
    updatedTime : str, default is Undefined, optional
        The date and time when the resource was updated. The date
        and time stamp format is ISO 8601: CCYY-MM-DDThh:mm:ss±hh:mm
        For example, 2015-08-27T09:49:58-05:00. The ±hh:mm value, if included,
        is the time zone as an offset from UTC.
    """


    capabilities?: [str]

    creationTime?: str

    description?: str

    disableRollback?: bool

    environmentOpts?: {str:str}

    name?: str

    notificationTopics?: [str]

    outputs?: [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecInitProviderOutputsItems0]

    parameters?: {str:str}

    region?: str

    status?: str

    statusReason?: str

    tags?: [str]

    templateDescription?: str

    templateOpts?: {str:str}

    timeout?: float

    updatedTime?: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecInitProviderOutputsItems0:
    r"""
    orchestration openstackm crossplane io v1alpha1 stack v1 spec init provider outputs items0

    Attributes
    ----------
    description : str, default is Undefined, optional
        The description of the stack resource.
    outputKey : str, default is Undefined, optional
        output key
    outputValue : str, default is Undefined, optional
        output value
    """


    description?: str

    outputKey?: str

    outputValue?: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1SpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1Status:
    r"""
    StackV1Status defines the observed state of StackV1.

    Attributes
    ----------
    atProvider : OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusAtProvider, default is Undefined, optional
        at provider
    conditions : [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusAtProvider

    conditions?: [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusConditionsItems0]

    observedGeneration?: int


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusAtProvider:
    r"""
    orchestration openstackm crossplane io v1alpha1 stack v1 status at provider

    Attributes
    ----------
    capabilities : [str], default is Undefined, optional
        List of stack capabilities for stack.
    creationTime : str, default is Undefined, optional
        The date and time when the resource was created. The date
        and time stamp format is ISO 8601: CCYY-MM-DDThh:mm:ss±hh:mm
        For example, 2015-08-27T09:49:58-05:00. The ±hh:mm value, if included,
        is the time zone as an offset from UTC.
    description : str, default is Undefined, optional
        The description of the stack resource.
    disableRollback : bool, default is Undefined, optional
        Enables or disables deletion of all stack
        resources when a stack creation fails. Default is true, meaning all
        resources are not deleted when stack creation fails.
    environmentOpts : {str:str}, default is Undefined, optional
        Environment key/value pairs to associate with
        the stack which contains details for the environment of the stack.
        Allowed keys: Bin, URL, Files. Changing this updates the existing stack
        Environment Opts.
    id : str, default is Undefined, optional
        id
    name : str, default is Undefined, optional
        A unique name for the stack. It must start with an
        alphabetic character. Changing this updates the stack's name.
    notificationTopics : [str], default is Undefined, optional
        List of notification topics for stack.
    outputs : [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusAtProviderOutputsItems0], default is Undefined, optional
        A list of stack outputs.
    parameters : {str:str}, default is Undefined, optional
        User-defined key/value pairs as parameters to pass
        to the template. Changing this updates the existing stack parameters.
    region : str, default is Undefined, optional
        The region in which to create the stack. If
        omitted, the region argument of the provider is used. Changing this
        creates a new stack.
    status : str, default is Undefined, optional
        The status of the stack.
    statusReason : str, default is Undefined, optional
        The reason for the current status of the stack.
    tags : [str], default is Undefined, optional
        A list of tags to assosciate with the Stack
    templateDescription : str, default is Undefined, optional
        The description of the stack template.
    templateOpts : {str:str}, default is Undefined, optional
        Template key/value pairs to associate with the
        stack which contains either the template file or url.
        Allowed keys: Bin, URL, Files. Changing this updates the existing stack
        Template Opts.
    timeout : float, default is Undefined, optional
        The timeout for stack action in minutes.
    updatedTime : str, default is Undefined, optional
        The date and time when the resource was updated. The date
        and time stamp format is ISO 8601: CCYY-MM-DDThh:mm:ss±hh:mm
        For example, 2015-08-27T09:49:58-05:00. The ±hh:mm value, if included,
        is the time zone as an offset from UTC.
    """


    capabilities?: [str]

    creationTime?: str

    description?: str

    disableRollback?: bool

    environmentOpts?: {str:str}

    id?: str

    name?: str

    notificationTopics?: [str]

    outputs?: [OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusAtProviderOutputsItems0]

    parameters?: {str:str}

    region?: str

    status?: str

    statusReason?: str

    tags?: [str]

    templateDescription?: str

    templateOpts?: {str:str}

    timeout?: float

    updatedTime?: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusAtProviderOutputsItems0:
    r"""
    orchestration openstackm crossplane io v1alpha1 stack v1 status at provider outputs items0

    Attributes
    ----------
    description : str, default is Undefined, optional
        The description of the stack resource.
    outputKey : str, default is Undefined, optional
        output key
    outputValue : str, default is Undefined, optional
        output value
    """


    description?: str

    outputKey?: str

    outputValue?: str


schema OrchestrationOpenstackmCrossplaneIoV1alpha1StackV1StatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


