"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema MembersV2:
    r"""
    MembersV2 is the Schema for the MembersV2s API. Manages a V2 members resource within OpenStack (batch members update).

    Attributes
    ----------
    apiVersion : str, default is "lb.openstack.m.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "MembersV2", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : LbOpenstackmCrossplaneIoV1alpha1MembersV2Spec, default is Undefined, required
        spec
    status : LbOpenstackmCrossplaneIoV1alpha1MembersV2Status, default is Undefined, optional
        status
    """


    apiVersion: "lb.openstack.m.crossplane.io/v1alpha1" = "lb.openstack.m.crossplane.io/v1alpha1"

    kind: "MembersV2" = "MembersV2"

    metadata?: v1.ObjectMeta

    spec: LbOpenstackmCrossplaneIoV1alpha1MembersV2Spec

    status?: LbOpenstackmCrossplaneIoV1alpha1MembersV2Status


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2Spec:
    r"""
    MembersV2Spec defines the desired state of MembersV2

    Attributes
    ----------
    forProvider : LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecForProvider, default is Undefined, required
        for provider
    initProvider : LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecForProvider

    initProvider?: LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecProviderConfigRef

    writeConnectionSecretToRef?: LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecWriteConnectionSecretToRef


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecForProvider:
    r"""
    lb openstackm crossplane io v1alpha1 members v2 spec for provider

    Attributes
    ----------
    member : [LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecForProviderMemberItems0], default is Undefined, optional
        A set of dictionaries containing member parameters. The
        structure is described below.
    poolId : str, default is Undefined, optional
        The id of the pool that members will be assigned to.
        Changing this creates a new members resource.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create pool members. If omitted, the
        region argument of the provider is used. Changing this creates a new
        members resource.
    """


    member?: [LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecForProviderMemberItems0]

    poolId?: str

    region?: str


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecForProviderMemberItems0:
    r"""
    lb openstackm crossplane io v1alpha1 members v2 spec for provider member items0

    Attributes
    ----------
    address : str, default is Undefined, optional
        The IP address of the members to receive traffic from
        the load balancer.
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the member.
        A valid value is true (UP) or false (DOWN). Defaults to true.
    backup : bool, default is Undefined, optional
        A bool that indicates whether the member is
        backup. Requires octavia minor version 2.1 or later.
    monitorAddress : str, default is Undefined, optional
        An alternate IP address used for health
        monitoring a backend member.
    monitorPort : float, default is Undefined, optional
        An alternate protocol port used for health
        monitoring a backend member.
    name : str, default is Undefined, optional
        Human-readable name for the member.
    protocolPort : float, default is Undefined, optional
        The port on which to listen for client traffic.
    subnetId : str, default is Undefined, optional
        The subnet in which to access the member.
    weight : float, default is Undefined, optional
        A positive integer value that indicates the relative
        portion of traffic that this members should receive from the pool. For
        example, a member with a weight of 10 receives five times as much traffic
        as a member with a weight of 2. Defaults to 1.
    """


    address?: str

    adminStateUp?: bool

    backup?: bool

    monitorAddress?: str

    monitorPort?: float

    name?: str

    protocolPort?: float

    subnetId?: str

    weight?: float


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    member : [LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecInitProviderMemberItems0], default is Undefined, optional
        A set of dictionaries containing member parameters. The
        structure is described below.
    poolId : str, default is Undefined, optional
        The id of the pool that members will be assigned to.
        Changing this creates a new members resource.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create pool members. If omitted, the
        region argument of the provider is used. Changing this creates a new
        members resource.
    """


    member?: [LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecInitProviderMemberItems0]

    poolId?: str

    region?: str


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecInitProviderMemberItems0:
    r"""
    lb openstackm crossplane io v1alpha1 members v2 spec init provider member items0

    Attributes
    ----------
    address : str, default is Undefined, optional
        The IP address of the members to receive traffic from
        the load balancer.
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the member.
        A valid value is true (UP) or false (DOWN). Defaults to true.
    backup : bool, default is Undefined, optional
        A bool that indicates whether the member is
        backup. Requires octavia minor version 2.1 or later.
    monitorAddress : str, default is Undefined, optional
        An alternate IP address used for health
        monitoring a backend member.
    monitorPort : float, default is Undefined, optional
        An alternate protocol port used for health
        monitoring a backend member.
    name : str, default is Undefined, optional
        Human-readable name for the member.
    protocolPort : float, default is Undefined, optional
        The port on which to listen for client traffic.
    subnetId : str, default is Undefined, optional
        The subnet in which to access the member.
    weight : float, default is Undefined, optional
        A positive integer value that indicates the relative
        portion of traffic that this members should receive from the pool. For
        example, a member with a weight of 10 receives five times as much traffic
        as a member with a weight of 2. Defaults to 1.
    """


    address?: str

    adminStateUp?: bool

    backup?: bool

    monitorAddress?: str

    monitorPort?: float

    name?: str

    protocolPort?: float

    subnetId?: str

    weight?: float


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2SpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2Status:
    r"""
    MembersV2Status defines the observed state of MembersV2.

    Attributes
    ----------
    atProvider : LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusAtProvider, default is Undefined, optional
        at provider
    conditions : [LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusAtProvider

    conditions?: [LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusConditionsItems0]

    observedGeneration?: int


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusAtProvider:
    r"""
    lb openstackm crossplane io v1alpha1 members v2 status at provider

    Attributes
    ----------
    id : str, default is Undefined, optional
        The unique ID for the members.
    member : [LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusAtProviderMemberItems0], default is Undefined, optional
        A set of dictionaries containing member parameters. The
        structure is described below.
    poolId : str, default is Undefined, optional
        The id of the pool that members will be assigned to.
        Changing this creates a new members resource.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create pool members. If omitted, the
        region argument of the provider is used. Changing this creates a new
        members resource.
    """


    id?: str

    member?: [LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusAtProviderMemberItems0]

    poolId?: str

    region?: str


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusAtProviderMemberItems0:
    r"""
    lb openstackm crossplane io v1alpha1 members v2 status at provider member items0

    Attributes
    ----------
    address : str, default is Undefined, optional
        The IP address of the members to receive traffic from
        the load balancer.
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the member.
        A valid value is true (UP) or false (DOWN). Defaults to true.
    backup : bool, default is Undefined, optional
        A bool that indicates whether the member is
        backup. Requires octavia minor version 2.1 or later.
    id : str, default is Undefined, optional
        The unique ID for the members.
    monitorAddress : str, default is Undefined, optional
        An alternate IP address used for health
        monitoring a backend member.
    monitorPort : float, default is Undefined, optional
        An alternate protocol port used for health
        monitoring a backend member.
    name : str, default is Undefined, optional
        Human-readable name for the member.
    protocolPort : float, default is Undefined, optional
        The port on which to listen for client traffic.
    subnetId : str, default is Undefined, optional
        The subnet in which to access the member.
    weight : float, default is Undefined, optional
        A positive integer value that indicates the relative
        portion of traffic that this members should receive from the pool. For
        example, a member with a weight of 10 receives five times as much traffic
        as a member with a weight of 2. Defaults to 1.
    """


    address?: str

    adminStateUp?: bool

    backup?: bool

    id?: str

    monitorAddress?: str

    monitorPort?: float

    name?: str

    protocolPort?: float

    subnetId?: str

    weight?: float


schema LbOpenstackmCrossplaneIoV1alpha1MembersV2StatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


