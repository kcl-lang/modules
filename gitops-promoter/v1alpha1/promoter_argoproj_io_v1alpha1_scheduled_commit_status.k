"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema ScheduledCommitStatus:
    r"""
    ScheduledCommitStatus is the Schema for the scheduledcommitstatuses API.

    Attributes
    ----------
    apiVersion : str, default is "promoter.argoproj.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ScheduledCommitStatus", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpec, default is Undefined, required
        spec
    status : PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatus, default is Undefined, optional
        status
    """


    apiVersion: "promoter.argoproj.io/v1alpha1" = "promoter.argoproj.io/v1alpha1"

    kind: "ScheduledCommitStatus" = "ScheduledCommitStatus"

    metadata?: v1.ObjectMeta

    spec: PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpec

    status?: PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatus


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpec:
    r"""
    spec defines the desired state of ScheduledCommitStatus

    Attributes
    ----------
    allow : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecAllowItems0], default is Undefined, optional
        Allow defines global allow windows applied to all listed environments in addition to
        per-environment allow windows (OR semantics across all).
    environments : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0], default is Undefined, required
        Environments defines the list of environments to gate. Only listed environments are gated;
        unlisted environments default to success (24/7 open). Each environment inherits global
        allow/exclude windows and may add its own. An environment with no per-environment windows
        is valid when global windows are defined.
    exclude : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecExcludeItems0], default is Undefined, optional
        Exclude defines global exclusion windows applied to all listed environments in addition to
        per-environment exclusions. Exclusions take precedence over allow windows.
    key : str, default is Undefined, required
        Key is the gate name referenced in the PromotionStrategy's proposedCommitStatuses.
        Must be lowercase alphanumeric with hyphens, 1–63 characters (pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$).
    promotionStrategyRef : PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecPromotionStrategyRef, default is Undefined, required
        promotion strategy ref
    timezone : str, default is "UTC", optional
        Timezone is the default IANA timezone name used for evaluating cron expressions across all windows.
        Individual windows may override this with their own timezone field.
        Defaults to UTC.
    """


    allow?: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecAllowItems0]

    environments: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0]

    exclude?: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecExcludeItems0]

    key: str

    promotionStrategyRef: PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecPromotionStrategyRef

    timezone?: str = "UTC"


    check:
        len(allow) <= 20 if allow
        len(environments) >= 1
        len(environments) <= 1000
        len(exclude) <= 20 if exclude
        len(key) <= 63
        len(key) >= 1
        _regex_match(str(key), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?$")
        len(timezone) <= 64 if timezone
        _regex_match(str(timezone), r"^[a-zA-Z0-9_/+-]*$") if timezone


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecAllowItems0:
    r"""
    CronWindow defines a recurring time window using a cron expression and a duration.

    Attributes
    ----------
    cron : str, default is Undefined, required
        Cron is a standard 5-field cron expression defining the start of the window
        (minute hour day-of-month month day-of-week). For example "0 9 * * 1-5" means
        Monday–Friday at 09:00 in the configured timezone.
    description : str, default is Undefined, optional
        Description is an optional human-readable explanation of this window
        (e.g. "US East business hours", "Holiday deployment freeze").
    duration : str, default is Undefined, required
        Duration is how long the window remains open after the cron trigger.
        The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "8h", "30m", "2h30m".
    timezone : str, default is Undefined, optional
        Timezone overrides the spec-level default timezone for this specific window.
        If not set, the spec-level timezone (or UTC if that is also not set) is used.
    """


    cron: str

    description?: str

    duration: str

    timezone?: str


    check:
        len(cron) <= 256
        len(cron) >= 9
        len(description) <= 256 if description
        len(timezone) <= 64 if timezone
        _regex_match(str(timezone), r"^[a-zA-Z0-9_/+-]*$") if timezone


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0:
    r"""
    ScheduledEnvironment defines the window configuration for a single environment.

    Attributes
    ----------
    allow : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0AllowItems0], default is Undefined, optional
        Allow defines the allowed promotion windows. Promotions are allowed when the current time
        falls inside any of these windows (OR semantics). These are combined with any global
        spec.allow windows. If no allow windows are defined (neither here nor globally), the
        environment operates in exclusion-only mode.
    branch : str, default is Undefined, required
        Branch is the name of the branch/environment you want to gate with time windows.
        Must not start with '-', contain ':', or contain '..'.
    exclude : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0ExcludeItems0], default is Undefined, optional
        Exclude defines blackout periods during which promotions are blocked. Exclusions take
        precedence over allow windows — if the current time falls inside any exclusion, promotions
        are blocked regardless of allow windows. These are combined with any global spec.exclude
        windows. If empty, no blackout periods are applied.
    """


    allow?: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0AllowItems0]

    branch: str

    exclude?: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0ExcludeItems0]


    check:
        len(allow) <= 20 if allow
        len(branch) <= 100
        len(branch) >= 1
        len(exclude) <= 20 if exclude


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0AllowItems0:
    r"""
    CronWindow defines a recurring time window using a cron expression and a duration.

    Attributes
    ----------
    cron : str, default is Undefined, required
        Cron is a standard 5-field cron expression defining the start of the window
        (minute hour day-of-month month day-of-week). For example "0 9 * * 1-5" means
        Monday–Friday at 09:00 in the configured timezone.
    description : str, default is Undefined, optional
        Description is an optional human-readable explanation of this window
        (e.g. "US East business hours", "Holiday deployment freeze").
    duration : str, default is Undefined, required
        Duration is how long the window remains open after the cron trigger.
        The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "8h", "30m", "2h30m".
    timezone : str, default is Undefined, optional
        Timezone overrides the spec-level default timezone for this specific window.
        If not set, the spec-level timezone (or UTC if that is also not set) is used.
    """


    cron: str

    description?: str

    duration: str

    timezone?: str


    check:
        len(cron) <= 256
        len(cron) >= 9
        len(description) <= 256 if description
        len(timezone) <= 64 if timezone
        _regex_match(str(timezone), r"^[a-zA-Z0-9_/+-]*$") if timezone


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecEnvironmentsItems0ExcludeItems0:
    r"""
    CronWindow defines a recurring time window using a cron expression and a duration.

    Attributes
    ----------
    cron : str, default is Undefined, required
        Cron is a standard 5-field cron expression defining the start of the window
        (minute hour day-of-month month day-of-week). For example "0 9 * * 1-5" means
        Monday–Friday at 09:00 in the configured timezone.
    description : str, default is Undefined, optional
        Description is an optional human-readable explanation of this window
        (e.g. "US East business hours", "Holiday deployment freeze").
    duration : str, default is Undefined, required
        Duration is how long the window remains open after the cron trigger.
        The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "8h", "30m", "2h30m".
    timezone : str, default is Undefined, optional
        Timezone overrides the spec-level default timezone for this specific window.
        If not set, the spec-level timezone (or UTC if that is also not set) is used.
    """


    cron: str

    description?: str

    duration: str

    timezone?: str


    check:
        len(cron) <= 256
        len(cron) >= 9
        len(description) <= 256 if description
        len(timezone) <= 64 if timezone
        _regex_match(str(timezone), r"^[a-zA-Z0-9_/+-]*$") if timezone


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecExcludeItems0:
    r"""
    CronWindow defines a recurring time window using a cron expression and a duration.

    Attributes
    ----------
    cron : str, default is Undefined, required
        Cron is a standard 5-field cron expression defining the start of the window
        (minute hour day-of-month month day-of-week). For example "0 9 * * 1-5" means
        Monday–Friday at 09:00 in the configured timezone.
    description : str, default is Undefined, optional
        Description is an optional human-readable explanation of this window
        (e.g. "US East business hours", "Holiday deployment freeze").
    duration : str, default is Undefined, required
        Duration is how long the window remains open after the cron trigger.
        The duration should be in a format accepted by Go's time.ParseDuration function, e.g., "8h", "30m", "2h30m".
    timezone : str, default is Undefined, optional
        Timezone overrides the spec-level default timezone for this specific window.
        If not set, the spec-level timezone (or UTC if that is also not set) is used.
    """


    cron: str

    description?: str

    duration: str

    timezone?: str


    check:
        len(cron) <= 256
        len(cron) >= 9
        len(description) <= 256 if description
        len(timezone) <= 64 if timezone
        _regex_match(str(timezone), r"^[a-zA-Z0-9_/+-]*$") if timezone


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusSpecPromotionStrategyRef:
    r"""
    PromotionStrategyRef is a reference to the promotion strategy that this scheduled commit status applies to.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name is the name of the object to refer to.
    """


    name: str


    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$")


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatus:
    r"""
    status defines the observed state of ScheduledCommitStatus

    Attributes
    ----------
    conditions : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusConditionsItems0], default is Undefined, optional
        Conditions represent the latest available observations of an object's state
    environments : [PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0], default is Undefined, optional
        Environments holds the status of each environment being tracked.
    instanceID : str, default is Undefined, optional
        InstanceID mirrors metadata.labels[promoter.argoproj.io/instance-id] stamped on each
        reconcile attempt by this install's controller, including when Ready=False; omitted
        when the resource has no instance-id label (default install).
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the .metadata.generation that this status was reconciled from.
        Because status is written via Server-Side Apply with ForceOwnership (which has no
        optimistic-concurrency check), this field is the canonical way to detect stale
        status writes: compare status.observedGeneration with metadata.generation.
    """


    conditions?: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusConditionsItems0]

    environments?: [PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0]

    instanceID?: str

    observedGeneration?: int


    check:
        len(instanceID) <= 63 if instanceID
        len(instanceID) >= 1 if instanceID
        _regex_match(str(instanceID), r"^[a-zA-Z0-9]([a-zA-Z0-9._-]*[a-zA-Z0-9])?$") if instanceID


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """


    lastTransitionTime: str

    message: str

    observedGeneration?: int

    reason: str

    status: "True" | "False" | "Unknown"

    $type: str


    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0:
    r"""
    ScheduledEnvironmentStatus defines the observed window state for a specific environment.

    Attributes
    ----------
    active : PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0Active, default is Undefined, optional
        active
    branch : str, default is Undefined, required
        Branch is the name of the branch/environment.
    next : PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0Next, default is Undefined, optional
        next
    phase : str, default is Undefined, required
        Phase represents the current phase of the scheduled gate.
    sha : str, default is Undefined, required
        Sha is the proposed commit SHA being tracked for this environment.
        Supports both SHA-1 (40 chars) and SHA-256 (64 chars) Git hash formats.
    """


    active?: PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0Active

    branch: str

    next?: PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0Next

    phase: "pending" | "success"

    sha: str


    check:
        len(branch) >= 1
        len(sha) <= 64
        _regex_match(str(sha), r"^([a-f0-9]{40}|[a-f0-9]{64})$")


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0Active:
    r"""
    Active holds details of the currently active window driving the phase.
    Nil when no specific window is active (e.g. exclusion-only mode with no active exclusion).

    Attributes
    ----------
    allow : str, default is Undefined, optional
        Allow is the cron expression of the allow window, if applicable.
    exclude : str, default is Undefined, optional
        Exclude is the cron expression of the exclusion window, if applicable.
    transition : str, default is Undefined, optional
        Transition is when this window state is expected to change
        (e.g. window closing for active, window opening for next).
    """


    allow?: str

    exclude?: str

    transition?: str


schema PromoterArgoprojIoV1alpha1ScheduledCommitStatusStatusEnvironmentsItems0Next:
    r"""
    Next holds details of the next expected window transition.
    Used by UIs for countdown timers and by the controller for precise requeuing.

    Attributes
    ----------
    allow : str, default is Undefined, optional
        Allow is the cron expression of the allow window, if applicable.
    exclude : str, default is Undefined, optional
        Exclude is the cron expression of the exclusion window, if applicable.
    transition : str, default is Undefined, optional
        Transition is when this window state is expected to change
        (e.g. window closing for active, window opening for next).
    """


    allow?: str

    exclude?: str

    transition?: str


