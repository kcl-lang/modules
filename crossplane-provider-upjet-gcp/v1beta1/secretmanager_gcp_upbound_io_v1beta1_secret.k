"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Secret:
    """
    Secret is the Schema for the Secrets API. A Secret is a logical secret whose value and versions can be accessed.

    Attributes
    ----------
    apiVersion : str, default is "secretmanager.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Secret", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SecretmanagerGcpUpboundIoV1beta1SecretSpec, default is Undefined, required
        spec
    status : SecretmanagerGcpUpboundIoV1beta1SecretStatus, default is Undefined, optional
        status
    """


    apiVersion: "secretmanager.gcp.upbound.io/v1beta1" = "secretmanager.gcp.upbound.io/v1beta1"

    kind: "Secret" = "Secret"

    metadata?: v1.ObjectMeta

    spec: SecretmanagerGcpUpboundIoV1beta1SecretSpec

    status?: SecretmanagerGcpUpboundIoV1beta1SecretStatus


schema SecretmanagerGcpUpboundIoV1beta1SecretSpec:
    """
    SecretSpec defines the desired state of Secret

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : SecretmanagerGcpUpboundIoV1beta1SecretSpecForProvider, default is Undefined, required
        for provider
    initProvider : SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : SecretmanagerGcpUpboundIoV1beta1SecretSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : SecretmanagerGcpUpboundIoV1beta1SecretSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: SecretmanagerGcpUpboundIoV1beta1SecretSpecForProvider

    initProvider?: SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: SecretmanagerGcpUpboundIoV1beta1SecretSpecProviderConfigRef

    publishConnectionDetailsTo?: SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: SecretmanagerGcpUpboundIoV1beta1SecretSpecWriteConnectionSecretToRef


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProvider:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Custom metadata about the secret.
        Annotations are distinct from various forms of labels. Annotations exist to allow
        client tools to store their own state information without requiring a database.
        Annotation keys must be between 1 and 63 characters long, have a UTF-8 encoding of
        maximum 128 bytes, begin and end with an alphanumeric character ([a-z0-9A-Z]), and
        may have dashes (-), underscores (_), dots (.), and alphanumerics in between these
        symbols.
        The total size of annotation keys and values must be less than 16KiB.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    expireTime : str, default is Undefined, optional
        Timestamp in UTC when the Secret is scheduled to expire. This is always provided on output, regardless of what was sent on input.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
        Only one of expire_time or ttl can be provided.
    labels : {str:str}, default is Undefined, optional
        The labels assigned to this Secret.
        Label keys must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes,
        and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}][\p{Ll}\p{Lo}\p{N}-]{0,62}
        Label values must be between 0 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes,
        and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}\p{N}-]{0,63}
        No more than 64 labels can be assigned to a given resource.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    replication : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0], default is Undefined, optional
        The replication policy of the secret data attached to the Secret. It cannot be changed
        after the Secret has been created.
        Structure is documented below.
    rotation : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderRotationItems0], default is Undefined, optional
        The rotation time and period for a Secret. At next_rotation_time, Secret Manager will send a Pub/Sub notification to the topics configured on the Secret. topics must be set to configure rotation.
        Structure is documented below.
    topics : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderTopicsItems0], default is Undefined, optional
        A list of up to 10 Pub/Sub topics to which messages are published when control plane operations are called on the secret or its versions.
        Structure is documented below.
    ttl : str, default is Undefined, optional
        The TTL for the Secret.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
        Only one of ttl or expire_time can be provided.
    versionAliases : {str:str}, default is Undefined, optional
        Mapping from version alias to version name.
        A version alias is a string with a maximum length of 63 characters and can contain
        uppercase and lowercase letters, numerals, and the hyphen (-) and underscore ('_')
        characters. An alias string must start with a letter and cannot be the string
        'latest' or 'NEW'. No more than 50 aliases can be assigned to a given secret.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    """


    annotations?: {str:str}

    expireTime?: str

    labels?: {str:str}

    project?: str

    replication?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0]

    rotation?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderRotationItems0]

    topics?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderTopicsItems0]

    ttl?: str

    versionAliases?: {str:str}


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider replication items0

    Attributes
    ----------
    auto : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0AutoItems0], default is Undefined, optional
        The Secret will automatically be replicated without any restrictions.
        Structure is documented below.
    userManaged : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0], default is Undefined, optional
        The Secret will be replicated to the regions specified by the user.
        Structure is documented below.
    """


    auto?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0AutoItems0]

    userManaged?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0AutoItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider replication items0 auto items0

    Attributes
    ----------
    customerManagedEncryption : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0], default is Undefined, optional
        Customer Managed Encryption for the secret.
        Structure is documented below.
    """


    customerManagedEncryption?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider replication items0 auto items0 customer managed encryption items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination secret.
    """


    kmsKeyName?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider replication items0 user managed items0

    Attributes
    ----------
    replicas : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0ReplicasItems0], default is Undefined, optional
        The list of Replicas for this Secret. Cannot be empty.
        Structure is documented below.
    """


    replicas?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0ReplicasItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0ReplicasItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider replication items0 user managed items0 replicas items0

    Attributes
    ----------
    customerManagedEncryption : [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0], default is Undefined, optional
        Customer Managed Encryption for the secret.
        Structure is documented below.
    location : str, default is Undefined, optional
        The canonical IDs of the location to replicate data. For example: "us-east1".
    """


    customerManagedEncryption?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0]

    location?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider replication items0 user managed items0 replicas items0 customer managed encryption items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination secret.
    """


    kmsKeyName?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderRotationItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider rotation items0

    Attributes
    ----------
    nextRotationTime : str, default is Undefined, optional
        Timestamp in UTC at which the Secret is scheduled to rotate.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
    rotationPeriod : str, default is Undefined, optional
        The Duration between rotation notifications. Must be in seconds and at least 3600s (1h) and at most 3153600000s (100 years).
        If rotationPeriod is set, next_rotation_time must be set. next_rotation_time will be advanced by this period when the service automatically sends rotation notifications.
    """


    nextRotationTime?: str

    rotationPeriod?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecForProviderTopicsItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec for provider topics items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        The resource name of the Pub/Sub topic that will be published to, in the following format: projects//topics/.
        For publication to succeed, the Secret Manager Service Agent service account must have pubsub.publisher permissions on the topic.
    """


    name?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Custom metadata about the secret.
        Annotations are distinct from various forms of labels. Annotations exist to allow
        client tools to store their own state information without requiring a database.
        Annotation keys must be between 1 and 63 characters long, have a UTF-8 encoding of
        maximum 128 bytes, begin and end with an alphanumeric character ([a-z0-9A-Z]), and
        may have dashes (-), underscores (_), dots (.), and alphanumerics in between these
        symbols.
        The total size of annotation keys and values must be less than 16KiB.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    expireTime : str, default is Undefined, optional
        Timestamp in UTC when the Secret is scheduled to expire. This is always provided on output, regardless of what was sent on input.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
        Only one of expire_time or ttl can be provided.
    labels : {str:str}, default is Undefined, optional
        The labels assigned to this Secret.
        Label keys must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes,
        and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}][\p{Ll}\p{Lo}\p{N}-]{0,62}
        Label values must be between 0 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes,
        and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}\p{N}-]{0,63}
        No more than 64 labels can be assigned to a given resource.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    replication : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0], default is Undefined, optional
        The replication policy of the secret data attached to the Secret. It cannot be changed
        after the Secret has been created.
        Structure is documented below.
    rotation : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderRotationItems0], default is Undefined, optional
        The rotation time and period for a Secret. At next_rotation_time, Secret Manager will send a Pub/Sub notification to the topics configured on the Secret. topics must be set to configure rotation.
        Structure is documented below.
    topics : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderTopicsItems0], default is Undefined, optional
        A list of up to 10 Pub/Sub topics to which messages are published when control plane operations are called on the secret or its versions.
        Structure is documented below.
    ttl : str, default is Undefined, optional
        The TTL for the Secret.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
        Only one of ttl or expire_time can be provided.
    versionAliases : {str:str}, default is Undefined, optional
        Mapping from version alias to version name.
        A version alias is a string with a maximum length of 63 characters and can contain
        uppercase and lowercase letters, numerals, and the hyphen (-) and underscore ('_')
        characters. An alias string must start with a letter and cannot be the string
        'latest' or 'NEW'. No more than 50 aliases can be assigned to a given secret.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    """


    annotations?: {str:str}

    expireTime?: str

    labels?: {str:str}

    project?: str

    replication?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0]

    rotation?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderRotationItems0]

    topics?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderTopicsItems0]

    ttl?: str

    versionAliases?: {str:str}


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider replication items0

    Attributes
    ----------
    auto : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0AutoItems0], default is Undefined, optional
        The Secret will automatically be replicated without any restrictions.
        Structure is documented below.
    userManaged : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0], default is Undefined, optional
        The Secret will be replicated to the regions specified by the user.
        Structure is documented below.
    """


    auto?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0AutoItems0]

    userManaged?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0AutoItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider replication items0 auto items0

    Attributes
    ----------
    customerManagedEncryption : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0], default is Undefined, optional
        Customer Managed Encryption for the secret.
        Structure is documented below.
    """


    customerManagedEncryption?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider replication items0 auto items0 customer managed encryption items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination secret.
    """


    kmsKeyName?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider replication items0 user managed items0

    Attributes
    ----------
    replicas : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0ReplicasItems0], default is Undefined, optional
        The list of Replicas for this Secret. Cannot be empty.
        Structure is documented below.
    """


    replicas?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0ReplicasItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0ReplicasItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider replication items0 user managed items0 replicas items0

    Attributes
    ----------
    customerManagedEncryption : [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0], default is Undefined, optional
        Customer Managed Encryption for the secret.
        Structure is documented below.
    location : str, default is Undefined, optional
        The canonical IDs of the location to replicate data. For example: "us-east1".
    """


    customerManagedEncryption?: [SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0]

    location?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider replication items0 user managed items0 replicas items0 customer managed encryption items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination secret.
    """


    kmsKeyName?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderRotationItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider rotation items0

    Attributes
    ----------
    nextRotationTime : str, default is Undefined, optional
        Timestamp in UTC at which the Secret is scheduled to rotate.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
    rotationPeriod : str, default is Undefined, optional
        The Duration between rotation notifications. Must be in seconds and at least 3600s (1h) and at most 3153600000s (100 years).
        If rotationPeriod is set, next_rotation_time must be set. next_rotation_time will be advanced by this period when the service automatically sends rotation notifications.
    """


    nextRotationTime?: str

    rotationPeriod?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecInitProviderTopicsItems0:
    """
    secretmanager gcp upbound io v1beta1 secret spec init provider topics items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        The resource name of the Pub/Sub topic that will be published to, in the following format: projects//topics/.
        For publication to succeed, the Secret Manager Service Agent service account must have pubsub.publisher permissions on the topic.
    """


    name?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SecretmanagerGcpUpboundIoV1beta1SecretSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SecretmanagerGcpUpboundIoV1beta1SecretSpecProviderConfigRefPolicy


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToConfigRef

    metadata?: SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToMetadata

    name: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToConfigRefPolicy


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema SecretmanagerGcpUpboundIoV1beta1SecretStatus:
    """
    SecretStatus defines the observed state of Secret.

    Attributes
    ----------
    atProvider : SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [SecretmanagerGcpUpboundIoV1beta1SecretStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProvider

    conditions?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusConditionsItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProvider:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Custom metadata about the secret.
        Annotations are distinct from various forms of labels. Annotations exist to allow
        client tools to store their own state information without requiring a database.
        Annotation keys must be between 1 and 63 characters long, have a UTF-8 encoding of
        maximum 128 bytes, begin and end with an alphanumeric character ([a-z0-9A-Z]), and
        may have dashes (-), underscores (_), dots (.), and alphanumerics in between these
        symbols.
        The total size of annotation keys and values must be less than 16KiB.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    createTime : str, default is Undefined, optional
        The time at which the Secret was created.
    effectiveAnnotations : {str:str}, default is Undefined, optional
        effective annotations
    effectiveLabels : {str:str}, default is Undefined, optional
        effective labels
    expireTime : str, default is Undefined, optional
        Timestamp in UTC when the Secret is scheduled to expire. This is always provided on output, regardless of what was sent on input.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
        Only one of expire_time or ttl can be provided.
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/secrets/{{secret_id}}
    labels : {str:str}, default is Undefined, optional
        The labels assigned to this Secret.
        Label keys must be between 1 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes,
        and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}][\p{Ll}\p{Lo}\p{N}-]{0,62}
        Label values must be between 0 and 63 characters long, have a UTF-8 encoding of maximum 128 bytes,
        and must conform to the following PCRE regular expression: [\p{Ll}\p{Lo}\p{N}-]{0,63}
        No more than 64 labels can be assigned to a given resource.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    name : str, default is Undefined, optional
        The resource name of the Secret. Format:
        projects/{{project}}/secrets/{{secret_id}}
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    replication : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0], default is Undefined, optional
        The replication policy of the secret data attached to the Secret. It cannot be changed
        after the Secret has been created.
        Structure is documented below.
    rotation : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderRotationItems0], default is Undefined, optional
        The rotation time and period for a Secret. At next_rotation_time, Secret Manager will send a Pub/Sub notification to the topics configured on the Secret. topics must be set to configure rotation.
        Structure is documented below.
    terraformLabels : {str:str}, default is Undefined, optional
        The combination of labels configured directly on the resource
        and default labels configured on the provider.
    topics : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderTopicsItems0], default is Undefined, optional
        A list of up to 10 Pub/Sub topics to which messages are published when control plane operations are called on the secret or its versions.
        Structure is documented below.
    ttl : str, default is Undefined, optional
        The TTL for the Secret.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
        Only one of ttl or expire_time can be provided.
    versionAliases : {str:str}, default is Undefined, optional
        Mapping from version alias to version name.
        A version alias is a string with a maximum length of 63 characters and can contain
        uppercase and lowercase letters, numerals, and the hyphen (-) and underscore ('_')
        characters. An alias string must start with a letter and cannot be the string
        'latest' or 'NEW'. No more than 50 aliases can be assigned to a given secret.
        An object containing a list of "key": value pairs. Example:
        { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    """


    annotations?: {str:str}

    createTime?: str

    effectiveAnnotations?: {str:str}

    effectiveLabels?: {str:str}

    expireTime?: str

    id?: str

    labels?: {str:str}

    name?: str

    project?: str

    replication?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0]

    rotation?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderRotationItems0]

    terraformLabels?: {str:str}

    topics?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderTopicsItems0]

    ttl?: str

    versionAliases?: {str:str}


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider replication items0

    Attributes
    ----------
    auto : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0AutoItems0], default is Undefined, optional
        The Secret will automatically be replicated without any restrictions.
        Structure is documented below.
    userManaged : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0], default is Undefined, optional
        The Secret will be replicated to the regions specified by the user.
        Structure is documented below.
    """


    auto?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0AutoItems0]

    userManaged?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0AutoItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider replication items0 auto items0

    Attributes
    ----------
    customerManagedEncryption : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0], default is Undefined, optional
        Customer Managed Encryption for the secret.
        Structure is documented below.
    """


    customerManagedEncryption?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0AutoItems0CustomerManagedEncryptionItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider replication items0 auto items0 customer managed encryption items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination secret.
    """


    kmsKeyName?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider replication items0 user managed items0

    Attributes
    ----------
    replicas : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0ReplicasItems0], default is Undefined, optional
        The list of Replicas for this Secret. Cannot be empty.
        Structure is documented below.
    """


    replicas?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0ReplicasItems0]


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0ReplicasItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider replication items0 user managed items0 replicas items0

    Attributes
    ----------
    customerManagedEncryption : [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0], default is Undefined, optional
        Customer Managed Encryption for the secret.
        Structure is documented below.
    location : str, default is Undefined, optional
        The canonical IDs of the location to replicate data. For example: "us-east1".
    """


    customerManagedEncryption?: [SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0]

    location?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderReplicationItems0UserManagedItems0ReplicasItems0CustomerManagedEncryptionItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider replication items0 user managed items0 replicas items0 customer managed encryption items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination secret.
    """


    kmsKeyName?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderRotationItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider rotation items0

    Attributes
    ----------
    nextRotationTime : str, default is Undefined, optional
        Timestamp in UTC at which the Secret is scheduled to rotate.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
    rotationPeriod : str, default is Undefined, optional
        The Duration between rotation notifications. Must be in seconds and at least 3600s (1h) and at most 3153600000s (100 years).
        If rotationPeriod is set, next_rotation_time must be set. next_rotation_time will be advanced by this period when the service automatically sends rotation notifications.
    """


    nextRotationTime?: str

    rotationPeriod?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusAtProviderTopicsItems0:
    """
    secretmanager gcp upbound io v1beta1 secret status at provider topics items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        The resource name of the Pub/Sub topic that will be published to, in the following format: projects//topics/.
        For publication to succeed, the Secret Manager Service Agent service account must have pubsub.publisher permissions on the topic.
    """


    name?: str


schema SecretmanagerGcpUpboundIoV1beta1SecretStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


