"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema AccessControlPolicy:
    """
    AccessControlPolicy defines an access control policy.

    Attributes
    ----------
    apiVersion : str, default is "hub.traefik.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "AccessControlPolicy", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : HubTraefikIoV1alpha1AccessControlPolicySpec, default is Undefined, optional
        spec
    status : HubTraefikIoV1alpha1AccessControlPolicyStatus, default is Undefined, optional
        status
    """


    apiVersion: "hub.traefik.io/v1alpha1" = "hub.traefik.io/v1alpha1"

    kind: "AccessControlPolicy" = "AccessControlPolicy"

    metadata?: v1.ObjectMeta

    spec?: HubTraefikIoV1alpha1AccessControlPolicySpec

    status?: HubTraefikIoV1alpha1AccessControlPolicyStatus


schema HubTraefikIoV1alpha1AccessControlPolicySpec:
    """
    AccessControlPolicySpec configures an access control policy.

    Attributes
    ----------
    apiKey : HubTraefikIoV1alpha1AccessControlPolicySpecAPIKey, default is Undefined, optional
        api key
    basicAuth : HubTraefikIoV1alpha1AccessControlPolicySpecBasicAuth, default is Undefined, optional
        basic auth
    jwt : HubTraefikIoV1alpha1AccessControlPolicySpecJwt, default is Undefined, optional
        jwt
    oAuthIntro : HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntro, default is Undefined, optional
        o auth intro
    oidc : HubTraefikIoV1alpha1AccessControlPolicySpecOidc, default is Undefined, optional
        oidc
    oidcGoogle : HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogle, default is Undefined, optional
        oidc google
    """


    apiKey?: HubTraefikIoV1alpha1AccessControlPolicySpecAPIKey

    basicAuth?: HubTraefikIoV1alpha1AccessControlPolicySpecBasicAuth

    jwt?: HubTraefikIoV1alpha1AccessControlPolicySpecJwt

    oAuthIntro?: HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntro

    oidc?: HubTraefikIoV1alpha1AccessControlPolicySpecOidc

    oidcGoogle?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogle


schema HubTraefikIoV1alpha1AccessControlPolicySpecAPIKey:
    """
    AccessControlPolicyAPIKey configure an APIKey control policy.

    Attributes
    ----------
    forwardHeaders : {str:str}, default is Undefined, optional
        ForwardHeaders instructs the middleware to forward key metadata as header values upon successful authentication.
    keySource : HubTraefikIoV1alpha1AccessControlPolicySpecAPIKeyKeySource, default is Undefined, required
        key source
    keys : [HubTraefikIoV1alpha1AccessControlPolicySpecAPIKeyKeysItems0], default is Undefined, optional
        Keys define the set of authorized keys to access a protected resource.
    """


    forwardHeaders?: {str:str}

    keySource: HubTraefikIoV1alpha1AccessControlPolicySpecAPIKeyKeySource

    keys?: [HubTraefikIoV1alpha1AccessControlPolicySpecAPIKeyKeysItems0]


schema HubTraefikIoV1alpha1AccessControlPolicySpecAPIKeyKeySource:
    """
    KeySource defines how to extract API keys from requests.

    Attributes
    ----------
    cookie : str, default is Undefined, optional
        Cookie is the name of a cookie.
    header : str, default is Undefined, optional
        Header is the name of a header.
    headerAuthScheme : str, default is Undefined, optional
        HeaderAuthScheme sets an optional auth scheme when Header is set to "Authorization".
        If set, this scheme is removed from the token, and all requests not including it are dropped.
    query : str, default is Undefined, optional
        Query is the name of a query parameter.
    """


    cookie?: str

    header?: str

    headerAuthScheme?: str

    query?: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecAPIKeyKeysItems0:
    """
    AccessControlPolicyAPIKeyKey defines an API key.

    Attributes
    ----------
    id : str, default is Undefined, required
        ID is the unique identifier of the key.
    metadata : {str:str}, default is Undefined, optional
        Metadata holds arbitrary metadata for this key, can be used by ForwardHeaders.
    value : str, default is Undefined, required
        Value is the SHAKE-256 hash (using 64 bytes) of the API key.
    """


    id: str

    metadata?: {str:str}

    value: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecBasicAuth:
    """
    AccessControlPolicyBasicAuth holds the HTTP basic authentication configuration.

    Attributes
    ----------
    forwardUsernameHeader : str, default is Undefined, optional
        forward username header
    realm : str, default is Undefined, optional
        realm
    stripAuthorizationHeader : bool, default is Undefined, optional
        strip authorization header
    users : [str], default is Undefined, optional
        users
    """


    forwardUsernameHeader?: str

    realm?: str

    stripAuthorizationHeader?: bool

    users?: [str]


schema HubTraefikIoV1alpha1AccessControlPolicySpecJwt:
    """
    AccessControlPolicyJWT configures a JWT access control policy.

    Attributes
    ----------
    claims : str, default is Undefined, optional
        claims
    forwardHeaders : {str:str}, default is Undefined, optional
        forward headers
    jwksFile : str, default is Undefined, optional
        jwks file
    jwksUrl : str, default is Undefined, optional
        jwks Url
    publicKey : str, default is Undefined, optional
        public key
    signingSecret : str, default is Undefined, optional
        signing secret
    signingSecretBase64Encoded : bool, default is Undefined, optional
        signing secret base64 encoded
    stripAuthorizationHeader : bool, default is Undefined, optional
        strip authorization header
    tokenQueryKey : str, default is Undefined, optional
        token query key
    """


    claims?: str

    forwardHeaders?: {str:str}

    jwksFile?: str

    jwksUrl?: str

    publicKey?: str

    signingSecret?: str

    signingSecretBase64Encoded?: bool

    stripAuthorizationHeader?: bool

    tokenQueryKey?: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntro:
    """
    AccessControlOAuthIntro configures an OAuth 2.0 Token Introspection access control policy.

    Attributes
    ----------
    claims : str, default is Undefined, optional
        claims
    clientConfig : HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroClientConfig, default is Undefined, required
        client config
    forwardHeaders : {str:str}, default is Undefined, optional
        forward headers
    tokenSource : HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroTokenSource, default is Undefined, required
        token source
    """


    claims?: str

    clientConfig: HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroClientConfig

    forwardHeaders?: {str:str}

    tokenSource: HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroTokenSource


schema HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroClientConfig:
    """
    AccessControlOAuthIntroClientConfig configures the OAuth 2.0 client for issuing token introspection requests.

    Attributes
    ----------
    headers : {str:str}, default is Undefined, optional
        Headers to set when sending requests to the Authorization Server.
    maxRetries : int, default is 3, optional
        MaxRetries defines the number of retries for introspection requests.
    timeoutSeconds : int, default is 5, optional
        TimeoutSeconds configures the maximum amount of seconds to wait before giving up on requests.
    tls : HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroClientConfigTLS, default is Undefined, optional
        tls
    tokenTypeHint : str, default is Undefined, optional
        TokenTypeHint is a hint to pass to the Authorization Server.
        See https://tools.ietf.org/html/rfc7662#section-2.1 for more information.
    url : str, default is Undefined, required
        URL of the Authorization Server.
    """


    headers?: {str:str}

    maxRetries?: int = 3

    timeoutSeconds?: int = 5

    tls?: HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroClientConfigTLS

    tokenTypeHint?: str

    url: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroClientConfigTLS:
    """
    TLS configures TLS communication with the Authorization Server.

    Attributes
    ----------
    ca : str, default is Undefined, optional
        CA sets the CA bundle used to sign the Authorization Server certificate.
    insecureSkipVerify : bool, default is Undefined, optional
        InsecureSkipVerify skips the Authorization Server certificate validation.
        For testing purposes only, do not use in production.
    """


    ca?: str

    insecureSkipVerify?: bool


schema HubTraefikIoV1alpha1AccessControlPolicySpecOAuthIntroTokenSource:
    """
    TokenSource describes how to extract tokens from HTTP requests.
    If multiple sources are set, the order is the following: header > query > cookie.

    Attributes
    ----------
    cookie : str, default is Undefined, optional
        Cookie is the name of a cookie.
    header : str, default is Undefined, optional
        Header is the name of a header.
    headerAuthScheme : str, default is Undefined, optional
        HeaderAuthScheme sets an optional auth scheme when Header is set to "Authorization".
        If set, this scheme is removed from the token, and all requests not including it are dropped.
    query : str, default is Undefined, optional
        Query is the name of a query parameter.
    """


    cookie?: str

    header?: str

    headerAuthScheme?: str

    query?: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidc:
    """
    AccessControlPolicyOIDC holds the OIDC authentication configuration.

    Attributes
    ----------
    authParams : {str:str}, default is Undefined, optional
        auth params
    claims : str, default is Undefined, optional
        claims
    clientId : str, default is Undefined, optional
        client Id
    disableAuthRedirectionPaths : [str], default is Undefined, optional
        disable auth redirection paths
    forwardHeaders : {str:str}, default is Undefined, optional
        forward headers
    issuer : str, default is Undefined, optional
        issuer
    logoutUrl : str, default is Undefined, optional
        logout Url
    redirectUrl : str, default is Undefined, optional
        redirect Url
    scopes : [str], default is Undefined, optional
        scopes
    secret : HubTraefikIoV1alpha1AccessControlPolicySpecOidcSecret, default is Undefined, optional
        secret
    session : HubTraefikIoV1alpha1AccessControlPolicySpecOidcSession, default is Undefined, optional
        session
    stateCookie : HubTraefikIoV1alpha1AccessControlPolicySpecOidcStateCookie, default is Undefined, optional
        state cookie
    """


    authParams?: {str:str}

    claims?: str

    clientId?: str

    disableAuthRedirectionPaths?: [str]

    forwardHeaders?: {str:str}

    issuer?: str

    logoutUrl?: str

    redirectUrl?: str

    scopes?: [str]

    secret?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcSecret

    session?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcSession

    stateCookie?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcStateCookie


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogle:
    """
    AccessControlPolicyOIDCGoogle holds the Google OIDC authentication configuration.

    Attributes
    ----------
    authParams : {str:str}, default is Undefined, optional
        auth params
    clientId : str, default is Undefined, optional
        client Id
    emails : [str], default is Undefined, optional
        Emails are the allowed emails to connect.
    forwardHeaders : {str:str}, default is Undefined, optional
        forward headers
    logoutUrl : str, default is Undefined, optional
        logout Url
    redirectUrl : str, default is Undefined, optional
        redirect Url
    secret : HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleSecret, default is Undefined, optional
        secret
    session : HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleSession, default is Undefined, optional
        session
    stateCookie : HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleStateCookie, default is Undefined, optional
        state cookie
    """


    authParams?: {str:str}

    clientId?: str

    emails?: [str]

    forwardHeaders?: {str:str}

    logoutUrl?: str

    redirectUrl?: str

    secret?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleSecret

    session?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleSession

    stateCookie?: HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleStateCookie


    check:
        len(emails) >= 1


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleSecret:
    """
    SecretReference represents a Secret Reference. It has enough information to retrieve secret
    in any namespace

    Attributes
    ----------
    name : str, default is Undefined, optional
        name is unique within a namespace to reference a secret resource.
    namespace : str, default is Undefined, optional
        namespace defines the space within which the secret name must be unique.
    """


    name?: str

    namespace?: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleSession:
    """
    Session holds session configuration.

    Attributes
    ----------
    domain : str, default is Undefined, optional
        domain
    path : str, default is Undefined, optional
        path
    refresh : bool, default is Undefined, optional
        refresh
    sameSite : str, default is Undefined, optional
        same site
    secure : bool, default is Undefined, optional
        secure
    """


    domain?: str

    path?: str

    refresh?: bool

    sameSite?: str

    secure?: bool


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcGoogleStateCookie:
    """
    StateCookie holds state cookie configuration.

    Attributes
    ----------
    domain : str, default is Undefined, optional
        domain
    path : str, default is Undefined, optional
        path
    sameSite : str, default is Undefined, optional
        same site
    secure : bool, default is Undefined, optional
        secure
    """


    domain?: str

    path?: str

    sameSite?: str

    secure?: bool


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcSecret:
    """
    SecretReference represents a Secret Reference. It has enough information to retrieve secret
    in any namespace

    Attributes
    ----------
    name : str, default is Undefined, optional
        name is unique within a namespace to reference a secret resource.
    namespace : str, default is Undefined, optional
        namespace defines the space within which the secret name must be unique.
    """


    name?: str

    namespace?: str


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcSession:
    """
    Session holds session configuration.

    Attributes
    ----------
    domain : str, default is Undefined, optional
        domain
    path : str, default is Undefined, optional
        path
    refresh : bool, default is Undefined, optional
        refresh
    sameSite : str, default is Undefined, optional
        same site
    secure : bool, default is Undefined, optional
        secure
    """


    domain?: str

    path?: str

    refresh?: bool

    sameSite?: str

    secure?: bool


schema HubTraefikIoV1alpha1AccessControlPolicySpecOidcStateCookie:
    """
    StateCookie holds state cookie configuration.

    Attributes
    ----------
    domain : str, default is Undefined, optional
        domain
    path : str, default is Undefined, optional
        path
    sameSite : str, default is Undefined, optional
        same site
    secure : bool, default is Undefined, optional
        secure
    """


    domain?: str

    path?: str

    sameSite?: str

    secure?: bool


schema HubTraefikIoV1alpha1AccessControlPolicyStatus:
    """
    The current status of this access control policy.

    Attributes
    ----------
    specHash : str, default is Undefined, optional
        spec hash
    syncedAt : str, default is Undefined, optional
        synced at
    version : str, default is Undefined, optional
        version
    """


    specHash?: str

    syncedAt?: str

    version?: str


