"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Entry:
    """
    Entry is the Schema for the Entrys API. Entry Metadata.

    Attributes
    ----------
    apiVersion : str, default is "datacatalog.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Entry", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : DatacatalogGcpUpboundIoV1beta1EntrySpec, default is Undefined, required
        spec
    status : DatacatalogGcpUpboundIoV1beta1EntryStatus, default is Undefined, optional
        status
    """


    apiVersion: "datacatalog.gcp.upbound.io/v1beta1" = "datacatalog.gcp.upbound.io/v1beta1"

    kind: "Entry" = "Entry"

    metadata?: v1.ObjectMeta

    spec: DatacatalogGcpUpboundIoV1beta1EntrySpec

    status?: DatacatalogGcpUpboundIoV1beta1EntryStatus


schema DatacatalogGcpUpboundIoV1beta1EntrySpec:
    """
    EntrySpec defines the desired state of Entry

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : DatacatalogGcpUpboundIoV1beta1EntrySpecForProvider, default is Undefined, required
        for provider
    initProvider : DatacatalogGcpUpboundIoV1beta1EntrySpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : DatacatalogGcpUpboundIoV1beta1EntrySpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : DatacatalogGcpUpboundIoV1beta1EntrySpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: DatacatalogGcpUpboundIoV1beta1EntrySpecForProvider

    initProvider?: DatacatalogGcpUpboundIoV1beta1EntrySpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: DatacatalogGcpUpboundIoV1beta1EntrySpecProviderConfigRef

    publishConnectionDetailsTo?: DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: DatacatalogGcpUpboundIoV1beta1EntrySpecWriteConnectionSecretToRef


schema DatacatalogGcpUpboundIoV1beta1EntrySpecForProvider:
    """
    datacatalog gcp upbound io v1beta1 entry spec for provider

    Attributes
    ----------
    description : str, default is Undefined, optional
        Entry description, which can consist of several sentences or paragraphs that describe entry contents.
    displayName : str, default is Undefined, optional
        Display information such as title and description. A short name to identify the entry,
        for example, "Analytics Data - Jan 2011".
    entryGroup : str, default is Undefined, optional
        The name of the entry group this entry is in.
    entryGroupRef : DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupRef, default is Undefined, optional
        entry group ref
    entryGroupSelector : DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupSelector, default is Undefined, optional
        entry group selector
    entryId : str, default is Undefined, optional
        The id of the entry to create.
    gcsFilesetSpec : [DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderGcsFilesetSpecItems0], default is Undefined, optional
        Specification that applies to a Cloud Storage fileset. This is only valid on entries of type FILESET.
        Structure is documented below.
    linkedResource : str, default is Undefined, optional
        The resource this metadata entry refers to.
        For Google Cloud Platform resources, linkedResource is the full name of the resource.
        For example, the linkedResource for a table resource from BigQuery is:
        //bigquery.googleapis.com/projects/projectId/datasets/datasetId/tables/tableId
        Output only when Entry is of type in the EntryType enum. For entries with userSpecifiedType,
        this field is optional and defaults to an empty string.
    $schema : str, default is Undefined, optional
        Schema of the entry (e.g. BigQuery, GoogleSQL, Avro schema), as a json string. An entry might not have any schema
        attached to it. See
        https://cloud.google.com/data-catalog/docs/reference/rest/v1/projects.locations.entryGroups.entries#schema
        for what fields this schema can contain.
    $type : str, default is Undefined, optional
        The type of the entry. Only used for Entries with types in the EntryType enum.
        Currently, only FILESET enum value is allowed. All other entries created through Data Catalog must use userSpecifiedType.
        Possible values are: FILESET.
    userSpecifiedSystem : str, default is Undefined, optional
        This field indicates the entry's source system that Data Catalog does not integrate with.
        userSpecifiedSystem strings must begin with a letter or underscore and can only contain letters, numbers,
        and underscores; are case insensitive; must be at least 1 character and at most 64 characters long.
    userSpecifiedType : str, default is Undefined, optional
        Entry type if it does not fit any of the input-allowed values listed in EntryType enum above.
        When creating an entry, users should check the enum values first, if nothing matches the entry
        to be created, then provide a custom value, for example "my_special_type".
        userSpecifiedType strings must begin with a letter or underscore and can only contain letters,
        numbers, and underscores; are case insensitive; must be at least 1 character and at most 64 characters long.
    """


    description?: str

    displayName?: str

    entryGroup?: str

    entryGroupRef?: DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupRef

    entryGroupSelector?: DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupSelector

    entryId?: str

    gcsFilesetSpec?: [DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderGcsFilesetSpecItems0]

    linkedResource?: str

    $schema?: str

    $type?: str

    userSpecifiedSystem?: str

    userSpecifiedType?: str


schema DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupRef:
    """
    Reference to a EntryGroup in datacatalog to populate entryGroup.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupRefPolicy


schema DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupSelector:
    """
    Selector for a EntryGroup in datacatalog to populate entryGroup.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupSelectorPolicy


schema DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderEntryGroupSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema DatacatalogGcpUpboundIoV1beta1EntrySpecForProviderGcsFilesetSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry spec for provider gcs fileset spec items0

    Attributes
    ----------
    filePatterns : [str], default is Undefined, optional
        Patterns to identify a set of files in Google Cloud Storage.
        See Cloud Storage documentation
        for more information. Note that bucket wildcards are currently not supported. Examples of valid filePatterns:
    """


    filePatterns?: [str]


schema DatacatalogGcpUpboundIoV1beta1EntrySpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    description : str, default is Undefined, optional
        Entry description, which can consist of several sentences or paragraphs that describe entry contents.
    displayName : str, default is Undefined, optional
        Display information such as title and description. A short name to identify the entry,
        for example, "Analytics Data - Jan 2011".
    entryGroup : str, default is Undefined, optional
        The name of the entry group this entry is in.
    entryGroupRef : DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupRef, default is Undefined, optional
        entry group ref
    entryGroupSelector : DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupSelector, default is Undefined, optional
        entry group selector
    entryId : str, default is Undefined, optional
        The id of the entry to create.
    gcsFilesetSpec : [DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderGcsFilesetSpecItems0], default is Undefined, optional
        Specification that applies to a Cloud Storage fileset. This is only valid on entries of type FILESET.
        Structure is documented below.
    linkedResource : str, default is Undefined, optional
        The resource this metadata entry refers to.
        For Google Cloud Platform resources, linkedResource is the full name of the resource.
        For example, the linkedResource for a table resource from BigQuery is:
        //bigquery.googleapis.com/projects/projectId/datasets/datasetId/tables/tableId
        Output only when Entry is of type in the EntryType enum. For entries with userSpecifiedType,
        this field is optional and defaults to an empty string.
    $schema : str, default is Undefined, optional
        Schema of the entry (e.g. BigQuery, GoogleSQL, Avro schema), as a json string. An entry might not have any schema
        attached to it. See
        https://cloud.google.com/data-catalog/docs/reference/rest/v1/projects.locations.entryGroups.entries#schema
        for what fields this schema can contain.
    $type : str, default is Undefined, optional
        The type of the entry. Only used for Entries with types in the EntryType enum.
        Currently, only FILESET enum value is allowed. All other entries created through Data Catalog must use userSpecifiedType.
        Possible values are: FILESET.
    userSpecifiedSystem : str, default is Undefined, optional
        This field indicates the entry's source system that Data Catalog does not integrate with.
        userSpecifiedSystem strings must begin with a letter or underscore and can only contain letters, numbers,
        and underscores; are case insensitive; must be at least 1 character and at most 64 characters long.
    userSpecifiedType : str, default is Undefined, optional
        Entry type if it does not fit any of the input-allowed values listed in EntryType enum above.
        When creating an entry, users should check the enum values first, if nothing matches the entry
        to be created, then provide a custom value, for example "my_special_type".
        userSpecifiedType strings must begin with a letter or underscore and can only contain letters,
        numbers, and underscores; are case insensitive; must be at least 1 character and at most 64 characters long.
    """


    description?: str

    displayName?: str

    entryGroup?: str

    entryGroupRef?: DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupRef

    entryGroupSelector?: DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupSelector

    entryId?: str

    gcsFilesetSpec?: [DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderGcsFilesetSpecItems0]

    linkedResource?: str

    $schema?: str

    $type?: str

    userSpecifiedSystem?: str

    userSpecifiedType?: str


schema DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupRef:
    """
    Reference to a EntryGroup in datacatalog to populate entryGroup.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupRefPolicy


schema DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupSelector:
    """
    Selector for a EntryGroup in datacatalog to populate entryGroup.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupSelectorPolicy


schema DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderEntryGroupSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema DatacatalogGcpUpboundIoV1beta1EntrySpecInitProviderGcsFilesetSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry spec init provider gcs fileset spec items0

    Attributes
    ----------
    filePatterns : [str], default is Undefined, optional
        Patterns to identify a set of files in Google Cloud Storage.
        See Cloud Storage documentation
        for more information. Note that bucket wildcards are currently not supported. Examples of valid filePatterns:
    """


    filePatterns?: [str]


schema DatacatalogGcpUpboundIoV1beta1EntrySpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : DatacatalogGcpUpboundIoV1beta1EntrySpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: DatacatalogGcpUpboundIoV1beta1EntrySpecProviderConfigRefPolicy


schema DatacatalogGcpUpboundIoV1beta1EntrySpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToConfigRef

    metadata?: DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToMetadata

    name: str


schema DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToConfigRefPolicy


schema DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema DatacatalogGcpUpboundIoV1beta1EntrySpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema DatacatalogGcpUpboundIoV1beta1EntrySpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema DatacatalogGcpUpboundIoV1beta1EntryStatus:
    """
    EntryStatus defines the observed state of Entry.

    Attributes
    ----------
    atProvider : DatacatalogGcpUpboundIoV1beta1EntryStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [DatacatalogGcpUpboundIoV1beta1EntryStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: DatacatalogGcpUpboundIoV1beta1EntryStatusAtProvider

    conditions?: [DatacatalogGcpUpboundIoV1beta1EntryStatusConditionsItems0]


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProvider:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider

    Attributes
    ----------
    bigqueryDateShardedSpec : [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryDateShardedSpecItems0], default is Undefined, optional
        Specification for a group of BigQuery tables with name pattern [prefix]YYYYMMDD.
        Context: https://cloud.google.com/bigquery/docs/partitioned-tables#partitioning_versus_sharding.
        Structure is documented below.
    bigqueryTableSpec : [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0], default is Undefined, optional
        Specification that applies to a BigQuery table. This is only valid on entries of type TABLE.
        Structure is documented below.
    description : str, default is Undefined, optional
        Entry description, which can consist of several sentences or paragraphs that describe entry contents.
    displayName : str, default is Undefined, optional
        Display information such as title and description. A short name to identify the entry,
        for example, "Analytics Data - Jan 2011".
    entryGroup : str, default is Undefined, optional
        The name of the entry group this entry is in.
    entryId : str, default is Undefined, optional
        The id of the entry to create.
    gcsFilesetSpec : [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderGcsFilesetSpecItems0], default is Undefined, optional
        Specification that applies to a Cloud Storage fileset. This is only valid on entries of type FILESET.
        Structure is documented below.
    id : str, default is Undefined, optional
        an identifier for the resource with format {{name}}
    integratedSystem : str, default is Undefined, optional
        This field indicates the entry's source system that Data Catalog integrates with, such as BigQuery or Pub/Sub.
    linkedResource : str, default is Undefined, optional
        The resource this metadata entry refers to.
        For Google Cloud Platform resources, linkedResource is the full name of the resource.
        For example, the linkedResource for a table resource from BigQuery is:
        //bigquery.googleapis.com/projects/projectId/datasets/datasetId/tables/tableId
        Output only when Entry is of type in the EntryType enum. For entries with userSpecifiedType,
        this field is optional and defaults to an empty string.
    name : str, default is Undefined, optional
        The Data Catalog resource name of the entry in URL format.
        Example: projects/{project_id}/locations/{location}/entryGroups/{entryGroupId}/entries/{entryId}.
        Note that this Entry and its child resources may not actually be stored in the location in this name.
    $schema : str, default is Undefined, optional
        Schema of the entry (e.g. BigQuery, GoogleSQL, Avro schema), as a json string. An entry might not have any schema
        attached to it. See
        https://cloud.google.com/data-catalog/docs/reference/rest/v1/projects.locations.entryGroups.entries#schema
        for what fields this schema can contain.
    $type : str, default is Undefined, optional
        The type of the entry. Only used for Entries with types in the EntryType enum.
        Currently, only FILESET enum value is allowed. All other entries created through Data Catalog must use userSpecifiedType.
        Possible values are: FILESET.
    userSpecifiedSystem : str, default is Undefined, optional
        This field indicates the entry's source system that Data Catalog does not integrate with.
        userSpecifiedSystem strings must begin with a letter or underscore and can only contain letters, numbers,
        and underscores; are case insensitive; must be at least 1 character and at most 64 characters long.
    userSpecifiedType : str, default is Undefined, optional
        Entry type if it does not fit any of the input-allowed values listed in EntryType enum above.
        When creating an entry, users should check the enum values first, if nothing matches the entry
        to be created, then provide a custom value, for example "my_special_type".
        userSpecifiedType strings must begin with a letter or underscore and can only contain letters,
        numbers, and underscores; are case insensitive; must be at least 1 character and at most 64 characters long.
    """


    bigqueryDateShardedSpec?: [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryDateShardedSpecItems0]

    bigqueryTableSpec?: [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0]

    description?: str

    displayName?: str

    entryGroup?: str

    entryId?: str

    gcsFilesetSpec?: [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderGcsFilesetSpecItems0]

    id?: str

    integratedSystem?: str

    linkedResource?: str

    name?: str

    $schema?: str

    $type?: str

    userSpecifiedSystem?: str

    userSpecifiedType?: str


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryDateShardedSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider bigquery date sharded spec items0

    Attributes
    ----------
    dataset : str, default is Undefined, optional
        (Output)
        The Data Catalog resource name of the dataset entry the current table belongs to, for example,
        projects/{project_id}/locations/{location}/entrygroups/{entryGroupId}/entries/{entryId}
    shardCount : float, default is Undefined, optional
        (Output)
        Total number of shards.
    tablePrefix : str, default is Undefined, optional
        (Output)
        The table name prefix of the shards. The name of any given shard is [tablePrefix]YYYYMMDD,
        for example, for shard MyTable20180101, the tablePrefix is MyTable.
    """


    dataset?: str

    shardCount?: float

    tablePrefix?: str


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider bigquery table spec items0

    Attributes
    ----------
    tableSourceType : str, default is Undefined, optional
        (Output)
        The table source type.
    tableSpec : [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0TableSpecItems0], default is Undefined, optional
        (Output)
        Spec of a BigQuery table. This field should only be populated if tableSourceType is BIGQUERY_TABLE.
        Structure is documented below.
    viewSpec : [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0ViewSpecItems0], default is Undefined, optional
        (Output)
        Table view specification. This field should only be populated if tableSourceType is BIGQUERY_VIEW.
        Structure is documented below.
    """


    tableSourceType?: str

    tableSpec?: [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0TableSpecItems0]

    viewSpec?: [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0ViewSpecItems0]


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0TableSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider bigquery table spec items0 table spec items0

    Attributes
    ----------
    groupedEntry : str, default is Undefined, optional
        (Output)
        If the table is a dated shard, i.e., with name pattern [prefix]YYYYMMDD, groupedEntry is the
        Data Catalog resource name of the date sharded grouped entry, for example,
        projects/{project_id}/locations/{location}/entrygroups/{entryGroupId}/entries/{entryId}.
        Otherwise, groupedEntry is empty.
    """


    groupedEntry?: str


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderBigqueryTableSpecItems0ViewSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider bigquery table spec items0 view spec items0

    Attributes
    ----------
    viewQuery : str, default is Undefined, optional
        (Output)
        The query that defines the table view.
    """


    viewQuery?: str


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderGcsFilesetSpecItems0:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider gcs fileset spec items0

    Attributes
    ----------
    filePatterns : [str], default is Undefined, optional
        Patterns to identify a set of files in Google Cloud Storage.
        See Cloud Storage documentation
        for more information. Note that bucket wildcards are currently not supported. Examples of valid filePatterns:
    sampleGcsFileSpecs : [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderGcsFilesetSpecItems0SampleGcsFileSpecsItems0], default is Undefined, optional
        (Output)
        Sample files contained in this fileset, not all files contained in this fileset are represented here.
        Structure is documented below.
    """


    filePatterns?: [str]

    sampleGcsFileSpecs?: [DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderGcsFilesetSpecItems0SampleGcsFileSpecsItems0]


schema DatacatalogGcpUpboundIoV1beta1EntryStatusAtProviderGcsFilesetSpecItems0SampleGcsFileSpecsItems0:
    """
    datacatalog gcp upbound io v1beta1 entry status at provider gcs fileset spec items0 sample gcs file specs items0

    Attributes
    ----------
    filePath : str, default is Undefined, optional
        (Output)
        The full file path
    sizeBytes : float, default is Undefined, optional
        (Output)
        The size of the file, in bytes.
    """


    filePath?: str

    sizeBytes?: float


schema DatacatalogGcpUpboundIoV1beta1EntryStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


