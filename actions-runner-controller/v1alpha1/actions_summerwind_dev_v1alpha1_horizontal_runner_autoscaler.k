"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema HorizontalRunnerAutoscaler:
    """
    HorizontalRunnerAutoscaler is the Schema for the horizontalrunnerautoscaler API

    Attributes
    ----------
    apiVersion : str, default is "actions.summerwind.dev/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "HorizontalRunnerAutoscaler", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpec, default is Undefined, optional
        spec
    status : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerStatus, default is Undefined, optional
        status
    """


    apiVersion: "actions.summerwind.dev/v1alpha1" = "actions.summerwind.dev/v1alpha1"

    kind: "HorizontalRunnerAutoscaler" = "HorizontalRunnerAutoscaler"

    metadata?: v1.ObjectMeta

    spec?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpec

    status?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerStatus


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpec:
    """
    HorizontalRunnerAutoscalerSpec defines the desired state of HorizontalRunnerAutoscaler

    Attributes
    ----------
    capacityReservations : [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecCapacityReservationsItems0], default is Undefined, optional
        capacity reservations
    githubAPICredentialsFrom : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecGithubAPICredentialsFrom, default is Undefined, optional
        github API credentials from
    maxReplicas : int, default is Undefined, optional
        MaxReplicas is the maximum number of replicas the deployment is allowed to scale
    metrics : [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecMetricsItems0], default is Undefined, optional
        Metrics is the collection of various metric targets to calculate desired number of runners
    minReplicas : int, default is Undefined, optional
        MinReplicas is the minimum number of replicas the deployment is allowed to scale
    scaleDownDelaySecondsAfterScaleOut : int, default is Undefined, optional
        ScaleDownDelaySecondsAfterScaleUp is the approximate delay for a scale down followed by a scale up Used to prevent flapping (down->up->down->... loop)
    scaleTargetRef : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleTargetRef, default is Undefined, optional
        scale target ref
    scaleUpTriggers : [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0], default is Undefined, optional
        ScaleUpTriggers is an experimental feature to increase the desired replicas by 1 on each webhook requested received by the webhookBasedAutoscaler. 
         This feature requires you to also enable and deploy the webhookBasedAutoscaler onto your cluster. 
         Note that the added runners remain until the next sync period at least, and they may or may not be used by GitHub Actions depending on the timing. They are intended to be used to gain "resource slack" immediately after you receive a webhook from GitHub, so that you can loosely expect MinReplicas runners to be always available.
    scheduledOverrides : [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScheduledOverridesItems0], default is Undefined, optional
        ScheduledOverrides is the list of ScheduledOverride. It can be used to override a few fields of HorizontalRunnerAutoscalerSpec on schedule. The earlier a scheduled override is, the higher it is prioritized.
    """


    capacityReservations?: [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecCapacityReservationsItems0]

    githubAPICredentialsFrom?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecGithubAPICredentialsFrom

    maxReplicas?: int

    metrics?: [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecMetricsItems0]

    minReplicas?: int

    scaleDownDelaySecondsAfterScaleOut?: int

    scaleTargetRef?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleTargetRef

    scaleUpTriggers?: [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0]

    scheduledOverrides?: [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScheduledOverridesItems0]


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecCapacityReservationsItems0:
    """
    CapacityReservation specifies the number of replicas temporarily added to the scale target until ExpirationTime.

    Attributes
    ----------
    effectiveTime : str, default is Undefined, optional
        effective time
    expirationTime : str, default is Undefined, optional
        expiration time
    name : str, default is Undefined, optional
        name
    replicas : int, default is Undefined, optional
        replicas
    """


    effectiveTime?: str

    expirationTime?: str

    name?: str

    replicas?: int


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecGithubAPICredentialsFrom:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler spec github API credentials from

    Attributes
    ----------
    secretRef : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecGithubAPICredentialsFromSecretRef, default is Undefined, optional
        secret ref
    """


    secretRef?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecGithubAPICredentialsFromSecretRef


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecGithubAPICredentialsFromSecretRef:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler spec github API credentials from secret ref

    Attributes
    ----------
    name : str, default is Undefined, required
        name
    """


    name: str


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecMetricsItems0:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler spec metrics items0

    Attributes
    ----------
    repositoryNames : [str], default is Undefined, optional
        RepositoryNames is the list of repository names to be used for calculating the metric. For example, a repository name is the REPO part of `github.com/USER/REPO`.
    scaleDownAdjustment : int, default is Undefined, optional
        ScaleDownAdjustment is the number of runners removed on scale-down. You can only specify either ScaleDownFactor or ScaleDownAdjustment.
    scaleDownFactor : str, default is Undefined, optional
        ScaleDownFactor is the multiplicative factor applied to the current number of runners used to determine how many pods should be removed.
    scaleDownThreshold : str, default is Undefined, optional
        ScaleDownThreshold is the percentage of busy runners less than which will trigger the hpa to scale the runners down.
    scaleUpAdjustment : int, default is Undefined, optional
        ScaleUpAdjustment is the number of runners added on scale-up. You can only specify either ScaleUpFactor or ScaleUpAdjustment.
    scaleUpFactor : str, default is Undefined, optional
        ScaleUpFactor is the multiplicative factor applied to the current number of runners used to determine how many pods should be added.
    scaleUpThreshold : str, default is Undefined, optional
        ScaleUpThreshold is the percentage of busy runners greater than which will trigger the hpa to scale runners up.
    $type : str, default is Undefined, optional
        Type is the type of metric to be used for autoscaling. It can be TotalNumberOfQueuedAndInProgressWorkflowRuns or PercentageRunnersBusy.
    """


    repositoryNames?: [str]

    scaleDownAdjustment?: int

    scaleDownFactor?: str

    scaleDownThreshold?: str

    scaleUpAdjustment?: int

    scaleUpFactor?: str

    scaleUpThreshold?: str

    $type?: str


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleTargetRef:
    """
    ScaleTargetRef is the reference to scaled resource like RunnerDeployment

    Attributes
    ----------
    kind : str, default is Undefined, optional
        Kind is the type of resource being referenced
    name : str, default is Undefined, optional
        Name is the name of resource being referenced
    """


    kind?: "RunnerDeployment" | "RunnerSet"

    name?: str


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler spec scale up triggers items0

    Attributes
    ----------
    amount : int, default is Undefined, optional
        amount
    duration : str, default is Undefined, optional
        duration
    githubEvent : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEvent, default is Undefined, optional
        github event
    """


    amount?: int

    duration?: str

    githubEvent?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEvent


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEvent:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler spec scale up triggers items0 github event

    Attributes
    ----------
    checkRun : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEventCheckRun, default is Undefined, optional
        check run
    pullRequest : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEventPullRequest, default is Undefined, optional
        pull request
    push : any, default is Undefined, optional
        PushSpec is the condition for triggering scale-up on push event Also see https://docs.github.com/en/actions/reference/events-that-trigger-workflows#push
    workflowJob : any, default is Undefined, optional
        https://docs.github.com/en/developers/webhooks-and-events/webhooks/webhook-events-and-payloads#workflow_job
    """


    checkRun?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEventCheckRun

    pullRequest?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEventPullRequest

    push?: any

    workflowJob?: any


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEventCheckRun:
    """
    https://docs.github.com/en/actions/reference/events-that-trigger-workflows#check_run

    Attributes
    ----------
    names : [str], default is Undefined, optional
        Names is a list of GitHub Actions glob patterns. Any check_run event whose name matches one of patterns in the list can trigger autoscaling. Note that check_run name seem to equal to the job name you've defined in your actions workflow yaml file. So it is very likely that you can utilize this to trigger depending on the job.
    repositories : [str], default is Undefined, optional
        Repositories is a list of GitHub repositories. Any check_run event whose repository matches one of repositories in the list can trigger autoscaling.
    status : str, default is Undefined, optional
        status
    types : [str], default is Undefined, optional
        One of: created, rerequested, or completed
    """


    names?: [str]

    repositories?: [str]

    status?: str

    types?: [str]


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScaleUpTriggersItems0GithubEventPullRequest:
    """
    https://docs.github.com/en/actions/reference/events-that-trigger-workflows#pull_request

    Attributes
    ----------
    branches : [str], default is Undefined, optional
        branches
    types : [str], default is Undefined, optional
        types
    """


    branches?: [str]

    types?: [str]


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScheduledOverridesItems0:
    """
    ScheduledOverride can be used to override a few fields of HorizontalRunnerAutoscalerSpec on schedule. A schedule can optionally be recurring, so that the corresponding override happens every day, week, month, or year.

    Attributes
    ----------
    endTime : str, default is Undefined, required
        EndTime is the time at which the first override ends.
    minReplicas : int, default is Undefined, optional
        MinReplicas is the number of runners while overriding. If omitted, it doesn't override minReplicas.
    recurrenceRule : ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScheduledOverridesItems0RecurrenceRule, default is Undefined, optional
        recurrence rule
    startTime : str, default is Undefined, required
        StartTime is the time at which the first override starts.
    """


    endTime: str

    minReplicas?: int

    recurrenceRule?: ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScheduledOverridesItems0RecurrenceRule

    startTime: str


    check:
        minReplicas >= 0


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerSpecScheduledOverridesItems0RecurrenceRule:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler spec scheduled overrides items0 recurrence rule

    Attributes
    ----------
    frequency : str, default is Undefined, optional
        Frequency is the name of a predefined interval of each recurrence. The valid values are "Daily", "Weekly", "Monthly", and "Yearly". If empty, the corresponding override happens only once.
    untilTime : str, default is Undefined, optional
        UntilTime is the time of the final recurrence. If empty, the schedule recurs forever.
    """


    frequency?: "Daily" | "Weekly" | "Monthly" | "Yearly"

    untilTime?: str


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerStatus:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler status

    Attributes
    ----------
    cacheEntries : [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerStatusCacheEntriesItems0], default is Undefined, optional
        cache entries
    desiredReplicas : int, default is Undefined, optional
        DesiredReplicas is the total number of desired, non-terminated and latest pods to be set for the primary RunnerSet This doesn't include outdated pods while upgrading the deployment and replacing the runnerset.
    lastSuccessfulScaleOutTime : str, default is Undefined, optional
        last successful scale out time
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the most recent generation observed for the target. It corresponds to e.g. RunnerDeployment's generation, which is updated on mutation by the API Server.
    scheduledOverridesSummary : str, default is Undefined, optional
        ScheduledOverridesSummary is the summary of active and upcoming scheduled overrides to be shown in e.g. a column of a `kubectl get hra` output for observability.
    """


    cacheEntries?: [ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerStatusCacheEntriesItems0]

    desiredReplicas?: int

    lastSuccessfulScaleOutTime?: str

    observedGeneration?: int

    scheduledOverridesSummary?: str


schema ActionsSummerwindDevV1alpha1HorizontalRunnerAutoscalerStatusCacheEntriesItems0:
    """
    actions summerwind dev v1alpha1 horizontal runner autoscaler status cache entries items0

    Attributes
    ----------
    expirationTime : str, default is Undefined, optional
        expiration time
    key : str, default is Undefined, optional
        key
    value : int, default is Undefined, optional
        value
    """


    expirationTime?: str

    key?: str

    value?: int


