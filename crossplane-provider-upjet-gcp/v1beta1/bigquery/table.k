"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Table:
    """
    Table is the Schema for the Tables API. Creates a table resource in a dataset for Google BigQuery.

    Attributes
    ----------
    apiVersion : str, default is "bigquery.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Table", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : BigqueryGcpUpboundIoV1beta1TableSpec, default is Undefined, required
        spec
    status : BigqueryGcpUpboundIoV1beta1TableStatus, default is Undefined, optional
        status
    """


    apiVersion: "bigquery.gcp.upbound.io/v1beta1" = "bigquery.gcp.upbound.io/v1beta1"

    kind: "Table" = "Table"

    metadata?: v1.ObjectMeta

    spec: BigqueryGcpUpboundIoV1beta1TableSpec

    status?: BigqueryGcpUpboundIoV1beta1TableStatus


schema BigqueryGcpUpboundIoV1beta1TableSpec:
    """
    TableSpec defines the desired state of Table

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : BigqueryGcpUpboundIoV1beta1TableSpecForProvider, default is Undefined, required
        for provider
    initProvider : BigqueryGcpUpboundIoV1beta1TableSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : BigqueryGcpUpboundIoV1beta1TableSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : BigqueryGcpUpboundIoV1beta1TableSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: BigqueryGcpUpboundIoV1beta1TableSpecForProvider

    initProvider?: BigqueryGcpUpboundIoV1beta1TableSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: BigqueryGcpUpboundIoV1beta1TableSpecProviderConfigRef

    publishConnectionDetailsTo?: BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: BigqueryGcpUpboundIoV1beta1TableSpecWriteConnectionSecretToRef


schema BigqueryGcpUpboundIoV1beta1TableSpecForProvider:
    """
    bigquery gcp upbound io v1beta1 table spec for provider

    Attributes
    ----------
    clustering : [str], default is Undefined, optional
        Specifies column names to use for data clustering.
        Up to four top-level columns are allowed, and should be specified in
        descending priority order.
    datasetId : str, default is Undefined, optional
        The dataset ID to create the table in.
        Changing this forces a new resource to be created.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDSelector, default is Undefined, optional
        dataset Id selector
    deletionProtection : bool, default is Undefined, optional
        deletion protection
    description : str, default is Undefined, optional
        The field description.
    encryptionConfiguration : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderEncryptionConfigurationItems0], default is Undefined, optional
        Specifies how the table should be encrypted.
        If left blank, the table will be encrypted with a Google-managed key; that process
        is transparent to the user.  Structure is documented below.
    expirationTime : float, default is Undefined, optional
        The time when this table expires, in
        milliseconds since the epoch. If not present, the table will persist
        indefinitely. Expired tables will be deleted and their storage
        reclaimed.
    externalDataConfiguration : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0], default is Undefined, optional
        Describes the data format,
        location, and other properties of a table stored outside of BigQuery.
        By defining these properties, the data source can then be queried as
        if it were a standard BigQuery table. Structure is documented below.
    friendlyName : str, default is Undefined, optional
        A descriptive name for the table.
    labels : {str:str}, default is Undefined, optional
        A mapping of labels to assign to the resource.
    materializedView : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderMaterializedViewItems0], default is Undefined, optional
        If specified, configures this table as a materialized view.
        Structure is documented below.
    maxStaleness : str, default is Undefined, optional
        :  The maximum staleness of data that could be
        returned when the table (or stale MV) is queried. Staleness encoded as a
        string encoding of SQL IntervalValue
        type.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs. If it
        is not provided, the provider project is used.
    rangePartitioning : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderRangePartitioningItems0], default is Undefined, optional
        If specified, configures range-based
        partitioning for this table. Structure is documented below.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified.
    $schema : str, default is Undefined, optional
        A JSON schema for the table.
    tableConstraints : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0], default is Undefined, optional
        Defines the primary key and foreign keys.
        Structure is documented below.
    tableReplicationInfo : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableReplicationInfoItems0], default is Undefined, optional
        table replication info
    timePartitioning : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTimePartitioningItems0], default is Undefined, optional
        If specified, configures time-based
        partitioning for this table. Structure is documented below.
    view : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderViewItems0], default is Undefined, optional
        If specified, configures this table as a view.
        Structure is documented below.
    """


    clustering?: [str]

    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDSelector

    deletionProtection?: bool

    description?: str

    encryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderEncryptionConfigurationItems0]

    expirationTime?: float

    externalDataConfiguration?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0]

    friendlyName?: str

    labels?: {str:str}

    materializedView?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderMaterializedViewItems0]

    maxStaleness?: str

    project?: str

    rangePartitioning?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderRangePartitioningItems0]

    requirePartitionFilter?: bool

    $schema?: str

    tableConstraints?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0]

    tableReplicationInfo?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableReplicationInfoItems0]

    timePartitioning?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTimePartitioningItems0]

    view?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderViewItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderDatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        The self link or full name of a key which should be used to
        encrypt this table.  Note that the default bigquery service account will need to have
        encrypt/decrypt permissions on this key - you may want to see the
        google_bigquery_default_service_account datasource and the
        google_kms_crypto_key_iam_binding resource.
    """


    kmsKeyName?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0

    Attributes
    ----------
    autodetect : bool, default is Undefined, optional
        - Let BigQuery try to autodetect the schema
        and format of the table.
    avroOptions : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0AvroOptionsItems0], default is Undefined, optional
        Additional options if source_format is set to
        "AVRO".  Structure is documented below.
    compression : str, default is Undefined, optional
        The compression type of the data source.
        Valid values are "NONE" or "GZIP".
    connectionId : str, default is Undefined, optional
        The connection specifying the credentials to be used to read
        external storage, such as Azure Blob, Cloud Storage, or S3. The connection_id can have
        the form {{project}}.{{location}}.{{connection_id}}
        or projects/{{project}}/locations/{{location}}/connections/{{connection_id}}.
    csvOptions : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0CsvOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "CSV". Structure is documented below.
    fileSetSpecType : str, default is Undefined, optional
        Specifies how source URIs are interpreted for constructing the file set to load.
        By default source URIs are expanded against the underlying storage.
        Other options include specifying manifest files. Only applicable to object storage systems. Docs
    googleSheetsOptions : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0], default is Undefined, optional
        Additional options if
        source_format is set to "GOOGLE_SHEETS". Structure is
        documented below.
    hivePartitioningOptions : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0], default is Undefined, optional
        When set, configures hive partitioning
        support. Not all storage formats support hive partitioning -- requesting hive
        partitioning on an unsupported format will lead to an error, as will providing
        an invalid specification. Structure is documented below.
    ignoreUnknownValues : bool, default is Undefined, optional
        Indicates if BigQuery should
        allow extra values that are not represented in the table schema.
        If true, the extra values are ignored. If false, records with
        extra columns are treated as bad records, and if there are too
        many bad records, an invalid error is returned in the job result.
        The default value is false.
    jsonOptions : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0JSONOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "JSON". Structure is documented below.
    maxBadRecords : float, default is Undefined, optional
        The maximum number of bad records that
        BigQuery can ignore when reading data.
    metadataCacheMode : str, default is Undefined, optional
        Metadata Cache Mode for the table. Set this to enable caching of metadata from external data source. Valid values are AUTOMATIC and MANUAL.
    objectMetadata : str, default is Undefined, optional
        Object Metadata is used to create Object Tables. Object Tables contain a listing of objects (with their metadata) found at the sourceUris. If object_metadata is set, source_format should be omitted.
    parquetOptions : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0ParquetOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "PARQUET". Structure is documented below.
    referenceFileSchemaUri : str, default is Undefined, optional
        When creating an external table, the user can provide a reference file with the table schema. This is enabled for the following formats: AVRO, PARQUET, ORC.
    $schema : str, default is Undefined, optional
        A JSON schema for the external table. Schema is required
        for CSV and JSON formats if autodetect is not on. Schema is disallowed
        for Google Cloud Bigtable, Cloud Datastore backups, Avro, Iceberg, ORC and Parquet formats.
        ~>NOTE: Because this field expects a JSON string, any changes to the
        string will create a diff, even if the JSON itself hasn't changed.
        Furthermore drift for this field cannot not be detected because BigQuery
        only uses this schema to compute the effective schema for the table, therefore
        any changes on the configured value will force the table to be recreated.
        This schema is effectively only applied when creating a table from an external
        datasource, after creation the computed schema will be stored in
        google_bigquery_table.schema
    sourceFormat : str, default is Undefined, optional
        The data format. Please see sourceFormat under
        ExternalDataConfiguration
        in Bigquery's public API documentation for supported formats. To use "GOOGLE_SHEETS"
        the scopes must include "https://www.googleapis.com/auth/drive.readonly".
    sourceUris : [str], default is Undefined, optional
        A list of the fully-qualified URIs that point to
        your data in Google Cloud.
    """


    autodetect?: bool

    avroOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0AvroOptionsItems0]

    compression?: str

    connectionId?: str

    csvOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0CsvOptionsItems0]

    fileSetSpecType?: str

    googleSheetsOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0]

    hivePartitioningOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0]

    ignoreUnknownValues?: bool

    jsonOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0JSONOptionsItems0]

    maxBadRecords?: float

    metadataCacheMode?: str

    objectMetadata?: str

    parquetOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0ParquetOptionsItems0]

    referenceFileSchemaUri?: str

    $schema?: str

    sourceFormat?: str

    sourceUris?: [str]


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0AvroOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0 avro options items0

    Attributes
    ----------
    useAvroLogicalTypes : bool, default is Undefined, optional
        If is set to true, indicates whether
        to interpret logical types as the corresponding BigQuery data type
        (for example, TIMESTAMP), instead of using the raw type (for example, INTEGER).
    """


    useAvroLogicalTypes?: bool


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0CsvOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0 csv options items0

    Attributes
    ----------
    allowJaggedRows : bool, default is Undefined, optional
        Indicates if BigQuery should accept rows
        that are missing trailing optional columns.
    allowQuotedNewlines : bool, default is Undefined, optional
        Indicates if BigQuery should allow
        quoted data sections that contain newline characters in a CSV file.
        The default value is false.
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8.
    fieldDelimiter : str, default is Undefined, optional
        The separator for fields in a CSV file.
    quote : str, default is Undefined, optional
        The value that is used to quote data sections in a
        CSV file. If your data does not contain quoted sections, set the
        property value to an empty string. If your data contains quoted newline
        characters, you must also set the allow_quoted_newlines property to true.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of the sheet
        that BigQuery will skip when reading the data. At least one of range or
        skip_leading_rows must be set.
    """


    allowJaggedRows?: bool

    allowQuotedNewlines?: bool

    encoding?: str

    fieldDelimiter?: str

    quote?: str

    skipLeadingRows?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0 google sheets options items0

    Attributes
    ----------
    range : str, default is Undefined, optional
        Information required to partition based on ranges.
        Structure is documented below.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of the sheet
        that BigQuery will skip when reading the data. At least one of range or
        skip_leading_rows must be set.
    """


    range?: str

    skipLeadingRows?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0 hive partitioning options items0

    Attributes
    ----------
    mode : str, default is Undefined, optional
        When set, what mode of hive partitioning to use when
        reading data. The following modes are supported.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified. require_partition_filter is deprecated and will be removed in
        a future major release. Use the top level field with the same name instead.
    sourceUriPrefix : str, default is Undefined, optional
        When hive partition detection is requested,
        a common for all source uris must be required. The prefix must end immediately
        before the partition key encoding begins. For example, consider files following
        this data layout. gs://bucket/path_to_table/dt=2019-06-01/country=USA/id=7/file.avro
        gs://bucket/path_to_table/dt=2019-05-31/country=CA/id=3/file.avro When hive
        partitioning is requested with either AUTO or STRINGS detection, the common prefix
        can be either of gs://bucket/path_to_table or gs://bucket/path_to_table/.
        Note that when mode is set to CUSTOM, you must encode the partition key schema within the source_uri_prefix by setting source_uri_prefix to gs://bucket/path_to_table/{key1:TYPE1}/{key2:TYPE2}/{key3:TYPE3}.
    """


    mode?: str

    requirePartitionFilter?: bool

    sourceUriPrefix?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0JSONOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0 JSON options items0

    Attributes
    ----------
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8.
    """


    encoding?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderExternalDataConfigurationItems0ParquetOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider external data configuration items0 parquet options items0

    Attributes
    ----------
    enableListInference : bool, default is Undefined, optional
        Indicates whether to use schema inference specifically for Parquet LIST logical type.
    enumAsString : bool, default is Undefined, optional
        Indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default.
    """


    enableListInference?: bool

    enumAsString?: bool


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderMaterializedViewItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider materialized view items0

    Attributes
    ----------
    allowNonIncrementalDefinition : bool, default is Undefined, optional
        Allow non incremental materialized view definition.
        The default value is false.
    enableRefresh : bool, default is Undefined, optional
        Specifies whether to use BigQuery's automatic refresh for this materialized view when the base table is updated.
        The default value is true.
    query : str, default is Undefined, optional
        A query whose result is persisted.
    refreshIntervalMs : float, default is Undefined, optional
        The maximum frequency at which this materialized view will be refreshed.
        The default value is 1800000
    """


    allowNonIncrementalDefinition?: bool

    enableRefresh?: bool

    query?: str

    refreshIntervalMs?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderRangePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider range partitioning items0

    Attributes
    ----------
    field : str, default is Undefined, optional
        The field used to determine how to create a range-based
        partition.
    range : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderRangePartitioningItems0RangeItems0], default is Undefined, optional
        Information required to partition based on ranges.
        Structure is documented below.
    """


    field?: str

    range?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderRangePartitioningItems0RangeItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderRangePartitioningItems0RangeItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider range partitioning items0 range items0

    Attributes
    ----------
    end : float, default is Undefined, optional
        End of the range partitioning, exclusive.
    interval : float, default is Undefined, optional
        The width of each range within the partition.
    start : float, default is Undefined, optional
        Start of the range partitioning, inclusive.
    """


    end?: float

    interval?: float

    start?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider table constraints items0

    Attributes
    ----------
    foreignKeys : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0], default is Undefined, optional
        Present only if the table has a foreign key.
        The foreign key is not enforced.
        Structure is documented below.
    primaryKey : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0PrimaryKeyItems0], default is Undefined, optional
        Represents the primary key constraint
        on a table's columns. Present only if the table has a primary key.
        The primary key is not enforced.
        Structure is documented below.
    """


    foreignKeys?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0]

    primaryKey?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0PrimaryKeyItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider table constraints items0 foreign keys items0

    Attributes
    ----------
    columnReferences : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0], default is Undefined, optional
        :  The pair of the foreign key column and primary key column.
        Structure is documented below.
    name : str, default is Undefined, optional
        :  Set only if the foreign key constraint is named.
    referencedTable : [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0], default is Undefined, optional
        :  The table that holds the primary key
        and is referenced by this foreign key.
        Structure is documented below.
    """


    columnReferences?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0]

    name?: str

    referencedTable?: [BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider table constraints items0 foreign keys items0 column references items0

    Attributes
    ----------
    referencedColumn : str, default is Undefined, optional
        :  The column in the primary key that are
        referenced by the referencingColumn
    referencingColumn : str, default is Undefined, optional
        :  The column that composes the foreign key.
    """


    referencedColumn?: str

    referencingColumn?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider table constraints items0 foreign keys items0 referenced table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, required
        :  The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        :  The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        A unique ID for the resource.
        Changing this forces a new resource to be created.
    """


    datasetId: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableConstraintsItems0PrimaryKeyItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider table constraints items0 primary key items0

    Attributes
    ----------
    columns : [str], default is Undefined, optional
        :  The columns that are composed of the primary key constraint.
    """


    columns?: [str]


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTableReplicationInfoItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider table replication info items0

    Attributes
    ----------
    replicationIntervalMs : float, default is Undefined, optional
        replication interval ms
    sourceDatasetId : str, default is Undefined, optional
        The dataset ID to create the table in.
        Changing this forces a new resource to be created.
    sourceProjectId : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/datasets/{{dataset}}/tables/{{name}}
    sourceTableId : str, default is Undefined, optional
        A unique ID for the resource.
        Changing this forces a new resource to be created.
    """


    replicationIntervalMs?: float

    sourceDatasetId?: str

    sourceProjectId?: str

    sourceTableId?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderTimePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider time partitioning items0

    Attributes
    ----------
    expirationMs : float, default is Undefined, optional
        Number of milliseconds for which to keep the
        storage for a partition.
    field : str, default is Undefined, optional
        The field used to determine how to create a time-based
        partition. If time-based partitioning is enabled without this value, the
        table is partitioned based on the load time.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified. require_partition_filter is deprecated and will be removed in
        a future major release. Use the top level field with the same name instead.
    $type : str, default is Undefined, optional
        The supported types are DAY, HOUR, MONTH, and YEAR,
        which will generate one partition per day, hour, month, and year, respectively.
    """


    expirationMs?: float

    field?: str

    requirePartitionFilter?: bool

    $type?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecForProviderViewItems0:
    """
    bigquery gcp upbound io v1beta1 table spec for provider view items0

    Attributes
    ----------
    query : str, default is Undefined, optional
        A query that BigQuery executes when the view is referenced.
    useLegacySql : bool, default is Undefined, optional
        Specifies whether to use BigQuery's legacy SQL for this view.
        The default value is true. If set to false, the view will use BigQuery's standard SQL.
    """


    query?: str

    useLegacySql?: bool


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    clustering : [str], default is Undefined, optional
        Specifies column names to use for data clustering.
        Up to four top-level columns are allowed, and should be specified in
        descending priority order.
    deletionProtection : bool, default is Undefined, optional
        deletion protection
    description : str, default is Undefined, optional
        The field description.
    encryptionConfiguration : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderEncryptionConfigurationItems0], default is Undefined, optional
        Specifies how the table should be encrypted.
        If left blank, the table will be encrypted with a Google-managed key; that process
        is transparent to the user.  Structure is documented below.
    expirationTime : float, default is Undefined, optional
        The time when this table expires, in
        milliseconds since the epoch. If not present, the table will persist
        indefinitely. Expired tables will be deleted and their storage
        reclaimed.
    externalDataConfiguration : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0], default is Undefined, optional
        Describes the data format,
        location, and other properties of a table stored outside of BigQuery.
        By defining these properties, the data source can then be queried as
        if it were a standard BigQuery table. Structure is documented below.
    friendlyName : str, default is Undefined, optional
        A descriptive name for the table.
    labels : {str:str}, default is Undefined, optional
        A mapping of labels to assign to the resource.
    materializedView : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderMaterializedViewItems0], default is Undefined, optional
        If specified, configures this table as a materialized view.
        Structure is documented below.
    maxStaleness : str, default is Undefined, optional
        :  The maximum staleness of data that could be
        returned when the table (or stale MV) is queried. Staleness encoded as a
        string encoding of SQL IntervalValue
        type.
    rangePartitioning : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderRangePartitioningItems0], default is Undefined, optional
        If specified, configures range-based
        partitioning for this table. Structure is documented below.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified.
    $schema : str, default is Undefined, optional
        A JSON schema for the table.
    tableConstraints : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0], default is Undefined, optional
        Defines the primary key and foreign keys.
        Structure is documented below.
    tableReplicationInfo : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableReplicationInfoItems0], default is Undefined, optional
        table replication info
    timePartitioning : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTimePartitioningItems0], default is Undefined, optional
        If specified, configures time-based
        partitioning for this table. Structure is documented below.
    view : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderViewItems0], default is Undefined, optional
        If specified, configures this table as a view.
        Structure is documented below.
    """


    clustering?: [str]

    deletionProtection?: bool

    description?: str

    encryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderEncryptionConfigurationItems0]

    expirationTime?: float

    externalDataConfiguration?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0]

    friendlyName?: str

    labels?: {str:str}

    materializedView?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderMaterializedViewItems0]

    maxStaleness?: str

    rangePartitioning?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderRangePartitioningItems0]

    requirePartitionFilter?: bool

    $schema?: str

    tableConstraints?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0]

    tableReplicationInfo?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableReplicationInfoItems0]

    timePartitioning?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTimePartitioningItems0]

    view?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderViewItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        The self link or full name of a key which should be used to
        encrypt this table.  Note that the default bigquery service account will need to have
        encrypt/decrypt permissions on this key - you may want to see the
        google_bigquery_default_service_account datasource and the
        google_kms_crypto_key_iam_binding resource.
    """


    kmsKeyName?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0

    Attributes
    ----------
    autodetect : bool, default is Undefined, optional
        - Let BigQuery try to autodetect the schema
        and format of the table.
    avroOptions : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0AvroOptionsItems0], default is Undefined, optional
        Additional options if source_format is set to
        "AVRO".  Structure is documented below.
    compression : str, default is Undefined, optional
        The compression type of the data source.
        Valid values are "NONE" or "GZIP".
    connectionId : str, default is Undefined, optional
        The connection specifying the credentials to be used to read
        external storage, such as Azure Blob, Cloud Storage, or S3. The connection_id can have
        the form {{project}}.{{location}}.{{connection_id}}
        or projects/{{project}}/locations/{{location}}/connections/{{connection_id}}.
    csvOptions : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0CsvOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "CSV". Structure is documented below.
    fileSetSpecType : str, default is Undefined, optional
        Specifies how source URIs are interpreted for constructing the file set to load.
        By default source URIs are expanded against the underlying storage.
        Other options include specifying manifest files. Only applicable to object storage systems. Docs
    googleSheetsOptions : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0], default is Undefined, optional
        Additional options if
        source_format is set to "GOOGLE_SHEETS". Structure is
        documented below.
    hivePartitioningOptions : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0], default is Undefined, optional
        When set, configures hive partitioning
        support. Not all storage formats support hive partitioning -- requesting hive
        partitioning on an unsupported format will lead to an error, as will providing
        an invalid specification. Structure is documented below.
    ignoreUnknownValues : bool, default is Undefined, optional
        Indicates if BigQuery should
        allow extra values that are not represented in the table schema.
        If true, the extra values are ignored. If false, records with
        extra columns are treated as bad records, and if there are too
        many bad records, an invalid error is returned in the job result.
        The default value is false.
    jsonOptions : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0JSONOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "JSON". Structure is documented below.
    maxBadRecords : float, default is Undefined, optional
        The maximum number of bad records that
        BigQuery can ignore when reading data.
    metadataCacheMode : str, default is Undefined, optional
        Metadata Cache Mode for the table. Set this to enable caching of metadata from external data source. Valid values are AUTOMATIC and MANUAL.
    objectMetadata : str, default is Undefined, optional
        Object Metadata is used to create Object Tables. Object Tables contain a listing of objects (with their metadata) found at the sourceUris. If object_metadata is set, source_format should be omitted.
    parquetOptions : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0ParquetOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "PARQUET". Structure is documented below.
    referenceFileSchemaUri : str, default is Undefined, optional
        When creating an external table, the user can provide a reference file with the table schema. This is enabled for the following formats: AVRO, PARQUET, ORC.
    $schema : str, default is Undefined, optional
        A JSON schema for the external table. Schema is required
        for CSV and JSON formats if autodetect is not on. Schema is disallowed
        for Google Cloud Bigtable, Cloud Datastore backups, Avro, Iceberg, ORC and Parquet formats.
        ~>NOTE: Because this field expects a JSON string, any changes to the
        string will create a diff, even if the JSON itself hasn't changed.
        Furthermore drift for this field cannot not be detected because BigQuery
        only uses this schema to compute the effective schema for the table, therefore
        any changes on the configured value will force the table to be recreated.
        This schema is effectively only applied when creating a table from an external
        datasource, after creation the computed schema will be stored in
        google_bigquery_table.schema
    sourceFormat : str, default is Undefined, optional
        The data format. Please see sourceFormat under
        ExternalDataConfiguration
        in Bigquery's public API documentation for supported formats. To use "GOOGLE_SHEETS"
        the scopes must include "https://www.googleapis.com/auth/drive.readonly".
    sourceUris : [str], default is Undefined, optional
        A list of the fully-qualified URIs that point to
        your data in Google Cloud.
    """


    autodetect?: bool

    avroOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0AvroOptionsItems0]

    compression?: str

    connectionId?: str

    csvOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0CsvOptionsItems0]

    fileSetSpecType?: str

    googleSheetsOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0]

    hivePartitioningOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0]

    ignoreUnknownValues?: bool

    jsonOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0JSONOptionsItems0]

    maxBadRecords?: float

    metadataCacheMode?: str

    objectMetadata?: str

    parquetOptions?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0ParquetOptionsItems0]

    referenceFileSchemaUri?: str

    $schema?: str

    sourceFormat?: str

    sourceUris?: [str]


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0AvroOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0 avro options items0

    Attributes
    ----------
    useAvroLogicalTypes : bool, default is Undefined, optional
        If is set to true, indicates whether
        to interpret logical types as the corresponding BigQuery data type
        (for example, TIMESTAMP), instead of using the raw type (for example, INTEGER).
    """


    useAvroLogicalTypes?: bool


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0CsvOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0 csv options items0

    Attributes
    ----------
    allowJaggedRows : bool, default is Undefined, optional
        Indicates if BigQuery should accept rows
        that are missing trailing optional columns.
    allowQuotedNewlines : bool, default is Undefined, optional
        Indicates if BigQuery should allow
        quoted data sections that contain newline characters in a CSV file.
        The default value is false.
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8.
    fieldDelimiter : str, default is Undefined, optional
        The separator for fields in a CSV file.
    quote : str, default is Undefined, optional
        The value that is used to quote data sections in a
        CSV file. If your data does not contain quoted sections, set the
        property value to an empty string. If your data contains quoted newline
        characters, you must also set the allow_quoted_newlines property to true.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of the sheet
        that BigQuery will skip when reading the data. At least one of range or
        skip_leading_rows must be set.
    """


    allowJaggedRows?: bool

    allowQuotedNewlines?: bool

    encoding?: str

    fieldDelimiter?: str

    quote?: str

    skipLeadingRows?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0 google sheets options items0

    Attributes
    ----------
    range : str, default is Undefined, optional
        Information required to partition based on ranges.
        Structure is documented below.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of the sheet
        that BigQuery will skip when reading the data. At least one of range or
        skip_leading_rows must be set.
    """


    range?: str

    skipLeadingRows?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0 hive partitioning options items0

    Attributes
    ----------
    mode : str, default is Undefined, optional
        When set, what mode of hive partitioning to use when
        reading data. The following modes are supported.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified. require_partition_filter is deprecated and will be removed in
        a future major release. Use the top level field with the same name instead.
    sourceUriPrefix : str, default is Undefined, optional
        When hive partition detection is requested,
        a common for all source uris must be required. The prefix must end immediately
        before the partition key encoding begins. For example, consider files following
        this data layout. gs://bucket/path_to_table/dt=2019-06-01/country=USA/id=7/file.avro
        gs://bucket/path_to_table/dt=2019-05-31/country=CA/id=3/file.avro When hive
        partitioning is requested with either AUTO or STRINGS detection, the common prefix
        can be either of gs://bucket/path_to_table or gs://bucket/path_to_table/.
        Note that when mode is set to CUSTOM, you must encode the partition key schema within the source_uri_prefix by setting source_uri_prefix to gs://bucket/path_to_table/{key1:TYPE1}/{key2:TYPE2}/{key3:TYPE3}.
    """


    mode?: str

    requirePartitionFilter?: bool

    sourceUriPrefix?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0JSONOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0 JSON options items0

    Attributes
    ----------
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8.
    """


    encoding?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderExternalDataConfigurationItems0ParquetOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider external data configuration items0 parquet options items0

    Attributes
    ----------
    enableListInference : bool, default is Undefined, optional
        Indicates whether to use schema inference specifically for Parquet LIST logical type.
    enumAsString : bool, default is Undefined, optional
        Indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default.
    """


    enableListInference?: bool

    enumAsString?: bool


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderMaterializedViewItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider materialized view items0

    Attributes
    ----------
    allowNonIncrementalDefinition : bool, default is Undefined, optional
        Allow non incremental materialized view definition.
        The default value is false.
    enableRefresh : bool, default is Undefined, optional
        Specifies whether to use BigQuery's automatic refresh for this materialized view when the base table is updated.
        The default value is true.
    query : str, default is Undefined, optional
        A query whose result is persisted.
    refreshIntervalMs : float, default is Undefined, optional
        The maximum frequency at which this materialized view will be refreshed.
        The default value is 1800000
    """


    allowNonIncrementalDefinition?: bool

    enableRefresh?: bool

    query?: str

    refreshIntervalMs?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderRangePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider range partitioning items0

    Attributes
    ----------
    field : str, default is Undefined, optional
        The field used to determine how to create a range-based
        partition.
    range : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderRangePartitioningItems0RangeItems0], default is Undefined, optional
        Information required to partition based on ranges.
        Structure is documented below.
    """


    field?: str

    range?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderRangePartitioningItems0RangeItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderRangePartitioningItems0RangeItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider range partitioning items0 range items0

    Attributes
    ----------
    end : float, default is Undefined, optional
        End of the range partitioning, exclusive.
    interval : float, default is Undefined, optional
        The width of each range within the partition.
    start : float, default is Undefined, optional
        Start of the range partitioning, inclusive.
    """


    end?: float

    interval?: float

    start?: float


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider table constraints items0

    Attributes
    ----------
    foreignKeys : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0], default is Undefined, optional
        Present only if the table has a foreign key.
        The foreign key is not enforced.
        Structure is documented below.
    primaryKey : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0PrimaryKeyItems0], default is Undefined, optional
        Represents the primary key constraint
        on a table's columns. Present only if the table has a primary key.
        The primary key is not enforced.
        Structure is documented below.
    """


    foreignKeys?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0]

    primaryKey?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0PrimaryKeyItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider table constraints items0 foreign keys items0

    Attributes
    ----------
    columnReferences : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0], default is Undefined, optional
        :  The pair of the foreign key column and primary key column.
        Structure is documented below.
    name : str, default is Undefined, optional
        :  Set only if the foreign key constraint is named.
    referencedTable : [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0], default is Undefined, optional
        :  The table that holds the primary key
        and is referenced by this foreign key.
        Structure is documented below.
    """


    columnReferences?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0]

    name?: str

    referencedTable?: [BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0]


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider table constraints items0 foreign keys items0 column references items0

    Attributes
    ----------
    referencedColumn : str, default is Undefined, optional
        :  The column in the primary key that are
        referenced by the referencingColumn
    referencingColumn : str, default is Undefined, optional
        :  The column that composes the foreign key.
    """


    referencedColumn?: str

    referencingColumn?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider table constraints items0 foreign keys items0 referenced table items0

    Attributes
    ----------
    projectId : str, default is Undefined, optional
        :  The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        A unique ID for the resource.
        Changing this forces a new resource to be created.
    """


    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableConstraintsItems0PrimaryKeyItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider table constraints items0 primary key items0

    Attributes
    ----------
    columns : [str], default is Undefined, optional
        :  The columns that are composed of the primary key constraint.
    """


    columns?: [str]


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTableReplicationInfoItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider table replication info items0

    Attributes
    ----------
    replicationIntervalMs : float, default is Undefined, optional
        replication interval ms
    sourceDatasetId : str, default is Undefined, optional
        The dataset ID to create the table in.
        Changing this forces a new resource to be created.
    sourceProjectId : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/datasets/{{dataset}}/tables/{{name}}
    sourceTableId : str, default is Undefined, optional
        A unique ID for the resource.
        Changing this forces a new resource to be created.
    """


    replicationIntervalMs?: float

    sourceDatasetId?: str

    sourceProjectId?: str

    sourceTableId?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderTimePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider time partitioning items0

    Attributes
    ----------
    expirationMs : float, default is Undefined, optional
        Number of milliseconds for which to keep the
        storage for a partition.
    field : str, default is Undefined, optional
        The field used to determine how to create a time-based
        partition. If time-based partitioning is enabled without this value, the
        table is partitioned based on the load time.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified. require_partition_filter is deprecated and will be removed in
        a future major release. Use the top level field with the same name instead.
    $type : str, default is Undefined, optional
        The supported types are DAY, HOUR, MONTH, and YEAR,
        which will generate one partition per day, hour, month, and year, respectively.
    """


    expirationMs?: float

    field?: str

    requirePartitionFilter?: bool

    $type?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecInitProviderViewItems0:
    """
    bigquery gcp upbound io v1beta1 table spec init provider view items0

    Attributes
    ----------
    query : str, default is Undefined, optional
        A query that BigQuery executes when the view is referenced.
    useLegacySql : bool, default is Undefined, optional
        Specifies whether to use BigQuery's legacy SQL for this view.
        The default value is true. If set to false, the view will use BigQuery's standard SQL.
    """


    query?: str

    useLegacySql?: bool


schema BigqueryGcpUpboundIoV1beta1TableSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1TableSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1TableSpecProviderConfigRefPolicy


schema BigqueryGcpUpboundIoV1beta1TableSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToConfigRef

    metadata?: BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToMetadata

    name: str


schema BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToConfigRefPolicy


schema BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1TableSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema BigqueryGcpUpboundIoV1beta1TableSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema BigqueryGcpUpboundIoV1beta1TableStatus:
    """
    TableStatus defines the observed state of Table.

    Attributes
    ----------
    atProvider : BigqueryGcpUpboundIoV1beta1TableStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [BigqueryGcpUpboundIoV1beta1TableStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: BigqueryGcpUpboundIoV1beta1TableStatusAtProvider

    conditions?: [BigqueryGcpUpboundIoV1beta1TableStatusConditionsItems0]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProvider:
    """
    bigquery gcp upbound io v1beta1 table status at provider

    Attributes
    ----------
    clustering : [str], default is Undefined, optional
        Specifies column names to use for data clustering.
        Up to four top-level columns are allowed, and should be specified in
        descending priority order.
    creationTime : float, default is Undefined, optional
        The time when this table was created, in milliseconds since the epoch.
    datasetId : str, default is Undefined, optional
        The dataset ID to create the table in.
        Changing this forces a new resource to be created.
    deletionProtection : bool, default is Undefined, optional
        deletion protection
    description : str, default is Undefined, optional
        The field description.
    effectiveLabels : {str:str}, default is Undefined, optional
        effective labels
    encryptionConfiguration : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderEncryptionConfigurationItems0], default is Undefined, optional
        Specifies how the table should be encrypted.
        If left blank, the table will be encrypted with a Google-managed key; that process
        is transparent to the user.  Structure is documented below.
    etag : str, default is Undefined, optional
        A hash of the resource.
    expirationTime : float, default is Undefined, optional
        The time when this table expires, in
        milliseconds since the epoch. If not present, the table will persist
        indefinitely. Expired tables will be deleted and their storage
        reclaimed.
    externalDataConfiguration : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0], default is Undefined, optional
        Describes the data format,
        location, and other properties of a table stored outside of BigQuery.
        By defining these properties, the data source can then be queried as
        if it were a standard BigQuery table. Structure is documented below.
    friendlyName : str, default is Undefined, optional
        A descriptive name for the table.
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/datasets/{{dataset}}/tables/{{name}}
    labels : {str:str}, default is Undefined, optional
        A mapping of labels to assign to the resource.
    lastModifiedTime : float, default is Undefined, optional
        The time when this table was last modified, in milliseconds since the epoch.
    location : str, default is Undefined, optional
        The geographic location where the table resides. This value is inherited from the dataset.
    materializedView : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderMaterializedViewItems0], default is Undefined, optional
        If specified, configures this table as a materialized view.
        Structure is documented below.
    maxStaleness : str, default is Undefined, optional
        :  The maximum staleness of data that could be
        returned when the table (or stale MV) is queried. Staleness encoded as a
        string encoding of SQL IntervalValue
        type.
    numBytes : float, default is Undefined, optional
        The size of this table in bytes, excluding any data in the streaming buffer.
    numLongTermBytes : float, default is Undefined, optional
        The number of bytes in the table that are considered "long-term storage".
    numRows : float, default is Undefined, optional
        The number of rows of data in this table, excluding any data in the streaming buffer.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs. If it
        is not provided, the provider project is used.
    rangePartitioning : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderRangePartitioningItems0], default is Undefined, optional
        If specified, configures range-based
        partitioning for this table. Structure is documented below.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified.
    $schema : str, default is Undefined, optional
        A JSON schema for the table.
    selfLink : str, default is Undefined, optional
        The URI of the created resource.
    tableConstraints : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0], default is Undefined, optional
        Defines the primary key and foreign keys.
        Structure is documented below.
    tableReplicationInfo : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableReplicationInfoItems0], default is Undefined, optional
        table replication info
    terraformLabels : {str:str}, default is Undefined, optional
        The combination of labels configured directly on the resource and default labels configured on the provider.
    timePartitioning : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTimePartitioningItems0], default is Undefined, optional
        If specified, configures time-based
        partitioning for this table. Structure is documented below.
    $type : str, default is Undefined, optional
        Describes the table type.
    view : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderViewItems0], default is Undefined, optional
        If specified, configures this table as a view.
        Structure is documented below.
    """


    clustering?: [str]

    creationTime?: float

    datasetId?: str

    deletionProtection?: bool

    description?: str

    effectiveLabels?: {str:str}

    encryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderEncryptionConfigurationItems0]

    etag?: str

    expirationTime?: float

    externalDataConfiguration?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0]

    friendlyName?: str

    id?: str

    labels?: {str:str}

    lastModifiedTime?: float

    location?: str

    materializedView?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderMaterializedViewItems0]

    maxStaleness?: str

    numBytes?: float

    numLongTermBytes?: float

    numRows?: float

    project?: str

    rangePartitioning?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderRangePartitioningItems0]

    requirePartitionFilter?: bool

    $schema?: str

    selfLink?: str

    tableConstraints?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0]

    tableReplicationInfo?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableReplicationInfoItems0]

    terraformLabels?: {str:str}

    timePartitioning?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTimePartitioningItems0]

    $type?: str

    view?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderViewItems0]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        The self link or full name of a key which should be used to
        encrypt this table.  Note that the default bigquery service account will need to have
        encrypt/decrypt permissions on this key - you may want to see the
        google_bigquery_default_service_account datasource and the
        google_kms_crypto_key_iam_binding resource.
    kmsKeyVersion : str, default is Undefined, optional
        The self link or full name of the kms key version used to encrypt this table.
    """


    kmsKeyName?: str

    kmsKeyVersion?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0

    Attributes
    ----------
    autodetect : bool, default is Undefined, optional
        - Let BigQuery try to autodetect the schema
        and format of the table.
    avroOptions : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0AvroOptionsItems0], default is Undefined, optional
        Additional options if source_format is set to
        "AVRO".  Structure is documented below.
    compression : str, default is Undefined, optional
        The compression type of the data source.
        Valid values are "NONE" or "GZIP".
    connectionId : str, default is Undefined, optional
        The connection specifying the credentials to be used to read
        external storage, such as Azure Blob, Cloud Storage, or S3. The connection_id can have
        the form {{project}}.{{location}}.{{connection_id}}
        or projects/{{project}}/locations/{{location}}/connections/{{connection_id}}.
    csvOptions : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0CsvOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "CSV". Structure is documented below.
    fileSetSpecType : str, default is Undefined, optional
        Specifies how source URIs are interpreted for constructing the file set to load.
        By default source URIs are expanded against the underlying storage.
        Other options include specifying manifest files. Only applicable to object storage systems. Docs
    googleSheetsOptions : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0], default is Undefined, optional
        Additional options if
        source_format is set to "GOOGLE_SHEETS". Structure is
        documented below.
    hivePartitioningOptions : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0], default is Undefined, optional
        When set, configures hive partitioning
        support. Not all storage formats support hive partitioning -- requesting hive
        partitioning on an unsupported format will lead to an error, as will providing
        an invalid specification. Structure is documented below.
    ignoreUnknownValues : bool, default is Undefined, optional
        Indicates if BigQuery should
        allow extra values that are not represented in the table schema.
        If true, the extra values are ignored. If false, records with
        extra columns are treated as bad records, and if there are too
        many bad records, an invalid error is returned in the job result.
        The default value is false.
    jsonOptions : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0JSONOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "JSON". Structure is documented below.
    maxBadRecords : float, default is Undefined, optional
        The maximum number of bad records that
        BigQuery can ignore when reading data.
    metadataCacheMode : str, default is Undefined, optional
        Metadata Cache Mode for the table. Set this to enable caching of metadata from external data source. Valid values are AUTOMATIC and MANUAL.
    objectMetadata : str, default is Undefined, optional
        Object Metadata is used to create Object Tables. Object Tables contain a listing of objects (with their metadata) found at the sourceUris. If object_metadata is set, source_format should be omitted.
    parquetOptions : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0ParquetOptionsItems0], default is Undefined, optional
        Additional properties to set if
        source_format is set to "PARQUET". Structure is documented below.
    referenceFileSchemaUri : str, default is Undefined, optional
        When creating an external table, the user can provide a reference file with the table schema. This is enabled for the following formats: AVRO, PARQUET, ORC.
    $schema : str, default is Undefined, optional
        A JSON schema for the external table. Schema is required
        for CSV and JSON formats if autodetect is not on. Schema is disallowed
        for Google Cloud Bigtable, Cloud Datastore backups, Avro, Iceberg, ORC and Parquet formats.
        ~>NOTE: Because this field expects a JSON string, any changes to the
        string will create a diff, even if the JSON itself hasn't changed.
        Furthermore drift for this field cannot not be detected because BigQuery
        only uses this schema to compute the effective schema for the table, therefore
        any changes on the configured value will force the table to be recreated.
        This schema is effectively only applied when creating a table from an external
        datasource, after creation the computed schema will be stored in
        google_bigquery_table.schema
    sourceFormat : str, default is Undefined, optional
        The data format. Please see sourceFormat under
        ExternalDataConfiguration
        in Bigquery's public API documentation for supported formats. To use "GOOGLE_SHEETS"
        the scopes must include "https://www.googleapis.com/auth/drive.readonly".
    sourceUris : [str], default is Undefined, optional
        A list of the fully-qualified URIs that point to
        your data in Google Cloud.
    """


    autodetect?: bool

    avroOptions?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0AvroOptionsItems0]

    compression?: str

    connectionId?: str

    csvOptions?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0CsvOptionsItems0]

    fileSetSpecType?: str

    googleSheetsOptions?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0]

    hivePartitioningOptions?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0]

    ignoreUnknownValues?: bool

    jsonOptions?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0JSONOptionsItems0]

    maxBadRecords?: float

    metadataCacheMode?: str

    objectMetadata?: str

    parquetOptions?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0ParquetOptionsItems0]

    referenceFileSchemaUri?: str

    $schema?: str

    sourceFormat?: str

    sourceUris?: [str]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0AvroOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0 avro options items0

    Attributes
    ----------
    useAvroLogicalTypes : bool, default is Undefined, optional
        If is set to true, indicates whether
        to interpret logical types as the corresponding BigQuery data type
        (for example, TIMESTAMP), instead of using the raw type (for example, INTEGER).
    """


    useAvroLogicalTypes?: bool


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0CsvOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0 csv options items0

    Attributes
    ----------
    allowJaggedRows : bool, default is Undefined, optional
        Indicates if BigQuery should accept rows
        that are missing trailing optional columns.
    allowQuotedNewlines : bool, default is Undefined, optional
        Indicates if BigQuery should allow
        quoted data sections that contain newline characters in a CSV file.
        The default value is false.
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8.
    fieldDelimiter : str, default is Undefined, optional
        The separator for fields in a CSV file.
    quote : str, default is Undefined, optional
        The value that is used to quote data sections in a
        CSV file. If your data does not contain quoted sections, set the
        property value to an empty string. If your data contains quoted newline
        characters, you must also set the allow_quoted_newlines property to true.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of the sheet
        that BigQuery will skip when reading the data. At least one of range or
        skip_leading_rows must be set.
    """


    allowJaggedRows?: bool

    allowQuotedNewlines?: bool

    encoding?: str

    fieldDelimiter?: str

    quote?: str

    skipLeadingRows?: float


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0GoogleSheetsOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0 google sheets options items0

    Attributes
    ----------
    range : str, default is Undefined, optional
        Information required to partition based on ranges.
        Structure is documented below.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of the sheet
        that BigQuery will skip when reading the data. At least one of range or
        skip_leading_rows must be set.
    """


    range?: str

    skipLeadingRows?: float


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0HivePartitioningOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0 hive partitioning options items0

    Attributes
    ----------
    mode : str, default is Undefined, optional
        When set, what mode of hive partitioning to use when
        reading data. The following modes are supported.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified. require_partition_filter is deprecated and will be removed in
        a future major release. Use the top level field with the same name instead.
    sourceUriPrefix : str, default is Undefined, optional
        When hive partition detection is requested,
        a common for all source uris must be required. The prefix must end immediately
        before the partition key encoding begins. For example, consider files following
        this data layout. gs://bucket/path_to_table/dt=2019-06-01/country=USA/id=7/file.avro
        gs://bucket/path_to_table/dt=2019-05-31/country=CA/id=3/file.avro When hive
        partitioning is requested with either AUTO or STRINGS detection, the common prefix
        can be either of gs://bucket/path_to_table or gs://bucket/path_to_table/.
        Note that when mode is set to CUSTOM, you must encode the partition key schema within the source_uri_prefix by setting source_uri_prefix to gs://bucket/path_to_table/{key1:TYPE1}/{key2:TYPE2}/{key3:TYPE3}.
    """


    mode?: str

    requirePartitionFilter?: bool

    sourceUriPrefix?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0JSONOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0 JSON options items0

    Attributes
    ----------
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8, UTF-16BE, UTF-16LE, UTF-32BE, and UTF-32LE. The default value is UTF-8.
    """


    encoding?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderExternalDataConfigurationItems0ParquetOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider external data configuration items0 parquet options items0

    Attributes
    ----------
    enableListInference : bool, default is Undefined, optional
        Indicates whether to use schema inference specifically for Parquet LIST logical type.
    enumAsString : bool, default is Undefined, optional
        Indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default.
    """


    enableListInference?: bool

    enumAsString?: bool


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderMaterializedViewItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider materialized view items0

    Attributes
    ----------
    allowNonIncrementalDefinition : bool, default is Undefined, optional
        Allow non incremental materialized view definition.
        The default value is false.
    enableRefresh : bool, default is Undefined, optional
        Specifies whether to use BigQuery's automatic refresh for this materialized view when the base table is updated.
        The default value is true.
    query : str, default is Undefined, optional
        A query whose result is persisted.
    refreshIntervalMs : float, default is Undefined, optional
        The maximum frequency at which this materialized view will be refreshed.
        The default value is 1800000
    """


    allowNonIncrementalDefinition?: bool

    enableRefresh?: bool

    query?: str

    refreshIntervalMs?: float


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderRangePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider range partitioning items0

    Attributes
    ----------
    field : str, default is Undefined, optional
        The field used to determine how to create a range-based
        partition.
    range : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderRangePartitioningItems0RangeItems0], default is Undefined, optional
        Information required to partition based on ranges.
        Structure is documented below.
    """


    field?: str

    range?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderRangePartitioningItems0RangeItems0]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderRangePartitioningItems0RangeItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider range partitioning items0 range items0

    Attributes
    ----------
    end : float, default is Undefined, optional
        End of the range partitioning, exclusive.
    interval : float, default is Undefined, optional
        The width of each range within the partition.
    start : float, default is Undefined, optional
        Start of the range partitioning, inclusive.
    """


    end?: float

    interval?: float

    start?: float


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider table constraints items0

    Attributes
    ----------
    foreignKeys : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0], default is Undefined, optional
        Present only if the table has a foreign key.
        The foreign key is not enforced.
        Structure is documented below.
    primaryKey : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0PrimaryKeyItems0], default is Undefined, optional
        Represents the primary key constraint
        on a table's columns. Present only if the table has a primary key.
        The primary key is not enforced.
        Structure is documented below.
    """


    foreignKeys?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0]

    primaryKey?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0PrimaryKeyItems0]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider table constraints items0 foreign keys items0

    Attributes
    ----------
    columnReferences : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0], default is Undefined, optional
        :  The pair of the foreign key column and primary key column.
        Structure is documented below.
    name : str, default is Undefined, optional
        :  Set only if the foreign key constraint is named.
    referencedTable : [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0], default is Undefined, optional
        :  The table that holds the primary key
        and is referenced by this foreign key.
        Structure is documented below.
    """


    columnReferences?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0]

    name?: str

    referencedTable?: [BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0ColumnReferencesItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider table constraints items0 foreign keys items0 column references items0

    Attributes
    ----------
    referencedColumn : str, default is Undefined, optional
        :  The column in the primary key that are
        referenced by the referencingColumn
    referencingColumn : str, default is Undefined, optional
        :  The column that composes the foreign key.
    """


    referencedColumn?: str

    referencingColumn?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0ForeignKeysItems0ReferencedTableItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider table constraints items0 foreign keys items0 referenced table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        :  The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        :  The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        A unique ID for the resource.
        Changing this forces a new resource to be created.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableConstraintsItems0PrimaryKeyItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider table constraints items0 primary key items0

    Attributes
    ----------
    columns : [str], default is Undefined, optional
        :  The columns that are composed of the primary key constraint.
    """


    columns?: [str]


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTableReplicationInfoItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider table replication info items0

    Attributes
    ----------
    replicationIntervalMs : float, default is Undefined, optional
        replication interval ms
    sourceDatasetId : str, default is Undefined, optional
        The dataset ID to create the table in.
        Changing this forces a new resource to be created.
    sourceProjectId : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/datasets/{{dataset}}/tables/{{name}}
    sourceTableId : str, default is Undefined, optional
        A unique ID for the resource.
        Changing this forces a new resource to be created.
    """


    replicationIntervalMs?: float

    sourceDatasetId?: str

    sourceProjectId?: str

    sourceTableId?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderTimePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider time partitioning items0

    Attributes
    ----------
    expirationMs : float, default is Undefined, optional
        Number of milliseconds for which to keep the
        storage for a partition.
    field : str, default is Undefined, optional
        The field used to determine how to create a time-based
        partition. If time-based partitioning is enabled without this value, the
        table is partitioned based on the load time.
    requirePartitionFilter : bool, default is Undefined, optional
        If set to true, queries over this table
        require a partition filter that can be used for partition elimination to be
        specified. require_partition_filter is deprecated and will be removed in
        a future major release. Use the top level field with the same name instead.
    $type : str, default is Undefined, optional
        The supported types are DAY, HOUR, MONTH, and YEAR,
        which will generate one partition per day, hour, month, and year, respectively.
    """


    expirationMs?: float

    field?: str

    requirePartitionFilter?: bool

    $type?: str


schema BigqueryGcpUpboundIoV1beta1TableStatusAtProviderViewItems0:
    """
    bigquery gcp upbound io v1beta1 table status at provider view items0

    Attributes
    ----------
    query : str, default is Undefined, optional
        A query that BigQuery executes when the view is referenced.
    useLegacySql : bool, default is Undefined, optional
        Specifies whether to use BigQuery's legacy SQL for this view.
        The default value is true. If set to false, the view will use BigQuery's standard SQL.
    """


    query?: str

    useLegacySql?: bool


schema BigqueryGcpUpboundIoV1beta1TableStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


