"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema SpannerBackupSchedule:
    r"""
    SpannerBackupSchedule is the Schema for the SpannerBackupSchedule API

    Attributes
    ----------
    apiVersion : str, default is "spanner.cnrm.cloud.google.com/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "SpannerBackupSchedule", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpec, default is Undefined, required
        spec
    status : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatus, default is Undefined, optional
        status
    """


    apiVersion: "spanner.cnrm.cloud.google.com/v1alpha1" = "spanner.cnrm.cloud.google.com/v1alpha1"

    kind: "SpannerBackupSchedule" = "SpannerBackupSchedule"

    metadata?: v1.ObjectMeta

    spec: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpec

    status?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatus


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpec:
    r"""
    SpannerBackupScheduleSpec defines the desired state of SpannerBackupSchedule

    Attributes
    ----------
    encryptionConfig : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfig, default is Undefined, optional
        encryption config
    fullBackupSpec : any, default is Undefined, optional
        The schedule creates only full backups.
    incrementalBackupSpec : any, default is Undefined, optional
        The schedule creates incremental backup chains.
    resourceID : str, default is Undefined, optional
        The SpannerBackupSchedule name. If not given, the metadata.name will be used.
    retentionDuration : str, default is Undefined, optional
        Optional. The retention duration of a backup that must be at least 6 hours and at most 366 days. The backup is eligible to be automatically deleted once the retention period has elapsed.
    spannerDatabaseRef : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpannerDatabaseRef, default is Undefined, optional
        spanner database ref
    spec : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpec, default is Undefined, optional
        spec
    """


    encryptionConfig?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfig

    fullBackupSpec?: any

    incrementalBackupSpec?: any

    resourceID?: str

    retentionDuration?: str

    spannerDatabaseRef?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpannerDatabaseRef

    spec?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpec


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfig:
    r"""
    Optional. The encryption configuration that will be used to encrypt the backup. If this field is not specified, the backup will use the same encryption configuration as the database.

    Attributes
    ----------
    encryptionType : str, default is Undefined, optional
        Required. The encryption type of the backup.
    kmsKeyRef : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfigKmsKeyRef, default is Undefined, optional
        kms key ref
    kmsKeyRefs : [SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfigKmsKeyRefsItems0], default is Undefined, optional
        Optional. Specifies the KMS configuration for the one or more keys used to
         protect the backup. Values are of the form
         `projects/<project>/locations/<location>/keyRings/<key_ring>/cryptoKeys/<kms_key_name>`.

         The keys referenced by kms_key_names must fully cover all
         regions of the backup's instance configuration. Some examples:
         * For single region instance configs, specify a single regional
         location KMS key.
         * For multi-regional instance configs of type GOOGLE_MANAGED,
         either specify a multi-regional location KMS key or multiple regional
         location KMS keys that cover all regions in the instance config.
         * For an instance config of type USER_MANAGED, please specify only
         regional location KMS keys to cover each region in the instance config.
         Multi-regional location KMS keys are not supported for USER_MANAGED
         instance configs.
    """


    encryptionType?: str

    kmsKeyRef?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfigKmsKeyRef

    kmsKeyRefs?: [SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfigKmsKeyRefsItems0]


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfigKmsKeyRef:
    r"""
    Optional. The Cloud KMS key that will be used to protect the backup. This field should be set only when [encryption_type][google.spanner.admin.database.v1.CreateBackupEncryptionConfig.encryption_type] is `CUSTOMER_MANAGED_ENCRYPTION`. Values are of the form `projects/<project>/locations/<location>/keyRings/<key_ring>/cryptoKeys/<kms_key_name>`.

    Attributes
    ----------
    external : str, default is Undefined, optional
        A reference to an externally managed KMSCryptoKey. Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
    name : str, default is Undefined, optional
        The `name` of a `KMSCryptoKey` resource.
    namespace : str, default is Undefined, optional
        The `namespace` of a `KMSCryptoKey` resource.
    """


    external?: str

    name?: str

    namespace?: str


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecEncryptionConfigKmsKeyRefsItems0:
    r"""
    spanner cnrm cloud google com v1alpha1 spanner backup schedule spec encryption config kms key refs items0

    Attributes
    ----------
    external : str, default is Undefined, optional
        A reference to an externally managed KMSCryptoKey. Should be in the format `projects/[kms_project_id]/locations/[region]/keyRings/[key_ring_id]/cryptoKeys/[key]`.
    name : str, default is Undefined, optional
        The `name` of a `KMSCryptoKey` resource.
    namespace : str, default is Undefined, optional
        The `namespace` of a `KMSCryptoKey` resource.
    """


    external?: str

    name?: str

    namespace?: str


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpannerDatabaseRef:
    r"""
    Required. The Spanner Database that this backup applies to.

    Attributes
    ----------
    external : str, default is Undefined, optional
        A reference to an externally managed SpannerDatabase resource. Should be in the format "projects/{{projectID}}/instances/{{instanceID}}/databases/{{databaseID}}".
    name : str, default is Undefined, optional
        The name of a SpannerDatabase resource.
    namespace : str, default is Undefined, optional
        The namespace of a SpannerDatabase resource.
    """


    external?: str

    name?: str

    namespace?: str


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpec:
    r"""
    Optional. The schedule specification based on which the backup creations are triggered.

    Attributes
    ----------
    cronSpec : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpecCronSpec, default is Undefined, optional
        cron spec
    """


    cronSpec?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpecCronSpec


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleSpecSpecCronSpec:
    r"""
    Cron style schedule specification.

    Attributes
    ----------
    text : str, default is Undefined, optional
        Required. Textual representation of the crontab. User can customize the
         backup frequency and the backup version time using the cron
         expression. The version time must be in UTC timzeone.

         The backup will contain an externally consistent copy of the
         database at the version time. Allowed frequencies are 12 hour, 1 day,
         1 week and 1 month. Examples of valid cron specifications:
           * `0 2/12 * * * ` : every 12 hours at (2, 14) hours past midnight in UTC.
           * `0 2,14 * * * ` : every 12 hours at (2,14) hours past midnight in UTC.
           * `0 2 * * * `    : once a day at 2 past midnight in UTC.
           * `0 2 * * 0 `    : once a week every Sunday at 2 past midnight in UTC.
           * `0 2 8 * * `    : once a month on 8th day at 2 past midnight in UTC.
    """


    text?: str


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatus:
    r"""
    SpannerBackupScheduleStatus defines the config connector machine state of SpannerBackupSchedule

    Attributes
    ----------
    conditions : [SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusConditionsItems0], default is Undefined, optional
        Conditions represent the latest available observations of the object's current state.
    externalRef : str, default is Undefined, optional
        A unique specifier for the SpannerBackupSchedule resource in GCP.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
    observedState : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusObservedState, default is Undefined, optional
        observed state
    """


    conditions?: [SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusConditionsItems0]

    externalRef?: str

    observedGeneration?: int

    observedState?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusObservedState


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusConditionsItems0:
    r"""
    spanner cnrm cloud google com v1alpha1 spanner backup schedule status conditions items0

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        Last time the condition transitioned from one status to another.
    message : str, default is Undefined, optional
        Human-readable message indicating details about last transition.
    reason : str, default is Undefined, optional
        Unique, one-word, CamelCase reason for the condition's last transition.
    status : str, default is Undefined, optional
        Status is the status of the condition. Can be True, False, Unknown.
    $type : str, default is Undefined, optional
        Type is the type of the condition.
    """


    lastTransitionTime?: str

    message?: str

    reason?: str

    status?: str

    $type?: str


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusObservedState:
    r"""
    ObservedState is the state of the resource as most recently observed in GCP.

    Attributes
    ----------
    cronSpec : SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusObservedStateCronSpec, default is Undefined, optional
        cron spec
    updateTime : str, default is Undefined, optional
        Output only. The timestamp at which the schedule was last updated. If the schedule has never been updated, this field contains the timestamp when the schedule was first created.
    """


    cronSpec?: SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusObservedStateCronSpec

    updateTime?: str


schema SpannerCnrmCloudGoogleComV1alpha1SpannerBackupScheduleStatusObservedStateCronSpec:
    r"""
    Cron style schedule specification.

    Attributes
    ----------
    creationWindow : str, default is Undefined, optional
        Output only. Schedule backups will contain an externally consistent copy of the database at the version time specified in `schedule_spec.cron_spec`. However, Spanner may not initiate the creation of the scheduled backups at that version time. Spanner will initiate the creation of scheduled backups within the time window bounded by the version_time specified in `schedule_spec.cron_spec` and version_time + `creation_window`.
    timeZone : str, default is Undefined, optional
        Output only. The time zone of the times in `CrontabSpec.text`. Currently only UTC is supported.
    """


    creationWindow?: str

    timeZone?: str


