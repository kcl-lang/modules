"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Cluster:
    """
    kops k8s io v1alpha2 cluster

    Attributes
    ----------
    apiVersion : str, default is "kops.k8s.io/v1alpha2", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Cluster", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : KopsK8sIoV1alpha2ClusterSpec, default is Undefined, optional
        spec
    """


    apiVersion: "kops.k8s.io/v1alpha2" = "kops.k8s.io/v1alpha2"

    kind: "Cluster" = "Cluster"

    metadata?: v1.ObjectMeta

    spec?: KopsK8sIoV1alpha2ClusterSpec


    check:


schema KopsK8sIoV1alpha2ClusterSpec:
    """
    ClusterSpec defines the configuration for a cluster

    Attributes
    ----------
    DisableSubnetTags : bool, default is Undefined, optional
        DisableSubnetTags controls if subnets are tagged in AWS
    additionalNetworkCIDRs : [str], default is Undefined, optional
        AdditionalNetworkCIDRs is a list of additional CIDR used for the AWS VPC or otherwise allocated to k8s. This is a real CIDR, not the internal k8s network On AWS, it maps to any additional CIDRs added to a VPC.
    additionalPolicies : {str:str}, default is Undefined, optional
        Additional policies to add for roles
    additionalSans : [str], default is Undefined, optional
        AdditionalSANs adds additional Subject Alternate Names to apiserver cert that kops generates
    addons : [KopsK8sIoV1alpha2ClusterSpecAddonsItems0], default is Undefined, optional
        Additional addons that should be installed on the cluster
    api : KopsK8sIoV1alpha2ClusterSpecAPI, default is Undefined, optional
        api
    assets : KopsK8sIoV1alpha2ClusterSpecAssets, default is Undefined, optional
        assets
    authentication : KopsK8sIoV1alpha2ClusterSpecAuthentication, default is Undefined, optional
        authentication
    authorization : KopsK8sIoV1alpha2ClusterSpecAuthorization, default is Undefined, optional
        authorization
    awsLoadBalancerController : KopsK8sIoV1alpha2ClusterSpecAwsLoadBalancerController, default is Undefined, optional
        aws load balancer controller
    certManager : KopsK8sIoV1alpha2ClusterSpecCertManager, default is Undefined, optional
        cert manager
    channel : str, default is Undefined, optional
        The Channel we are following
    cloudConfig : KopsK8sIoV1alpha2ClusterSpecCloudConfig, default is Undefined, optional
        cloud config
    cloudControllerManager : KopsK8sIoV1alpha2ClusterSpecCloudControllerManager, default is Undefined, optional
        cloud controller manager
    cloudLabels : {str:str}, default is Undefined, optional
        CloudLabels defines additional tags or labels on cloud provider resources
    cloudProvider : str, default is Undefined, optional
        The CloudProvider to use (aws or gce)
    clusterAutoscaler : KopsK8sIoV1alpha2ClusterSpecClusterAutoscaler, default is Undefined, optional
        cluster autoscaler
    clusterDNSDomain : str, default is Undefined, optional
        ClusterDNSDomain is the suffix we use for internal DNS names (normally cluster.local)
    configBase : str, default is Undefined, optional
        ConfigBase is the path where we store configuration for the cluster This might be different that the location when the cluster spec itself is stored, both because this must be accessible to the cluster, and because it might be on a different cloud or storage system (etcd vs S3)
    configStore : str, default is Undefined, optional
        ConfigStore is unused.
    containerRuntime : str, default is Undefined, optional
        ContainerRuntime was removed.
    containerd : KopsK8sIoV1alpha2ClusterSpecContainerd, default is Undefined, optional
        containerd
    dnsControllerGossipConfig : KopsK8sIoV1alpha2ClusterSpecDNSControllerGossipConfig, default is Undefined, optional
        dns controller gossip config
    dnsZone : str, default is Undefined, optional
        DNSZone is the DNS zone we should use when configuring DNS This is because some clouds let us define a managed zone foo.bar, and then have kubernetes.dev.foo.bar, without needing to define dev.foo.bar as a hosted zone. DNSZone will probably be a suffix of the MasterPublicName. Note that DNSZone can either by the host name of the zone (containing dots), or can be an identifier for the zone.
    docker : KopsK8sIoV1alpha2ClusterSpecDocker, default is Undefined, optional
        docker
    egressProxy : KopsK8sIoV1alpha2ClusterSpecEgressProxy, default is Undefined, optional
        egress proxy
    encryptionConfig : bool, default is Undefined, optional
        EncryptionConfig holds the encryption config
    etcdClusters : [KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0], default is Undefined, optional
        EtcdClusters stores the configuration for each cluster
    externalDns : KopsK8sIoV1alpha2ClusterSpecExternalDNS, default is Undefined, optional
        external Dns
    externalPolicies : {str:[str]}, default is Undefined, optional
        ExternalPolicies allows the insertion of pre-existing managed policies on IG Roles
    fileAssets : [KopsK8sIoV1alpha2ClusterSpecFileAssetsItems0], default is Undefined, optional
        A collection of files assets for deployed cluster wide
    gossipConfig : KopsK8sIoV1alpha2ClusterSpecGossipConfig, default is Undefined, optional
        gossip config
    hooks : [KopsK8sIoV1alpha2ClusterSpecHooksItems0], default is Undefined, optional
        Hooks for custom actions e.g. on first installation
    iam : KopsK8sIoV1alpha2ClusterSpecIam, default is Undefined, optional
        iam
    isolateMasters : bool, default is Undefined, optional
        IsolateMasters determines whether we should lock down masters so that they are not on the pod network. true is the kube-up behaviour, but it is very surprising: it means that daemonsets only work on the master if they have hostNetwork=true. false is now the default, and it will: * give the master a normal PodCIDR * run kube-proxy on the master * enable debugging handlers on the master, so kubectl logs works
    karpenter : KopsK8sIoV1alpha2ClusterSpecKarpenter, default is Undefined, optional
        karpenter
    keyStore : str, default is Undefined, optional
        KeyStore is the VFS path to where SSL keys and certificates are stored
    kubeAPIServer : KopsK8sIoV1alpha2ClusterSpecKubeAPIServer, default is Undefined, optional
        kube API server
    kubeControllerManager : KopsK8sIoV1alpha2ClusterSpecKubeControllerManager, default is Undefined, optional
        kube controller manager
    kubeDNS : KopsK8sIoV1alpha2ClusterSpecKubeDNS, default is Undefined, optional
        kube DNS
    kubeProxy : KopsK8sIoV1alpha2ClusterSpecKubeProxy, default is Undefined, optional
        kube proxy
    kubeScheduler : KopsK8sIoV1alpha2ClusterSpecKubeScheduler, default is Undefined, optional
        kube scheduler
    kubelet : KopsK8sIoV1alpha2ClusterSpecKubelet, default is Undefined, optional
        kubelet
    kubernetesApiAccess : [str], default is Undefined, optional
        KubernetesAPIAccess determines the permitted access to the API endpoints (master HTTPS) Currently only a single CIDR is supported (though a richer grammar could be added in future)
    kubernetesVersion : str, default is Undefined, optional
        The version of kubernetes to install (optional, and can be a "spec" like stable)
    masterInternalName : str, default is Undefined, optional
        MasterInternalName is unused.
    masterKubelet : KopsK8sIoV1alpha2ClusterSpecMasterKubelet, default is Undefined, optional
        master kubelet
    masterPublicName : str, default is Undefined, optional
        MasterPublicName is the external DNS name for the master nodes
    metricsServer : KopsK8sIoV1alpha2ClusterSpecMetricsServer, default is Undefined, optional
        metrics server
    networkCIDR : str, default is Undefined, optional
        NetworkCIDR is the CIDR used for the AWS VPC / GCE Network, or otherwise allocated to k8s This is a real CIDR, not the internal k8s network On AWS, it maps to the VPC CIDR.  It is not required on GCE.
    networkID : str, default is Undefined, optional
        NetworkID is an identifier of a network, if we want to reuse/share an existing network (e.g. an AWS VPC)
    networking : KopsK8sIoV1alpha2ClusterSpecNetworking, default is Undefined, optional
        networking
    nodeAuthorization : KopsK8sIoV1alpha2ClusterSpecNodeAuthorization, default is Undefined, optional
        node authorization
    nodePortAccess : [str], default is Undefined, optional
        NodePortAccess is a list of the CIDRs that can access the node ports range (30000-32767).
    nodeProblemDetector : KopsK8sIoV1alpha2ClusterSpecNodeProblemDetector, default is Undefined, optional
        node problem detector
    nodeTerminationHandler : KopsK8sIoV1alpha2ClusterSpecNodeTerminationHandler, default is Undefined, optional
        node termination handler
    nonMasqueradeCIDR : str, default is Undefined, optional
        MasterIPRange                 string `json:",omitempty"` NonMasqueradeCIDR is the CIDR for the internal k8s network (for pod IPs) It cannot overlap ServiceClusterIPRange
    ntp : KopsK8sIoV1alpha2ClusterSpecNtp, default is Undefined, optional
        ntp
    packages : [str], default is Undefined, optional
        Packages specifies additional packages to be installed.
    podCIDR : str, default is Undefined, optional
        PodCIDR is the CIDR from which we allocate IPs for pods
    podIdentityWebhook : KopsK8sIoV1alpha2ClusterSpecPodIdentityWebhook, default is Undefined, optional
        pod identity webhook
    project : str, default is Undefined, optional
        Project is the cloud project we should use, required on GCE
    rollingUpdate : KopsK8sIoV1alpha2ClusterSpecRollingUpdate, default is Undefined, optional
        rolling update
    secretStore : str, default is Undefined, optional
        SecretStore is the VFS path to where secrets are stored
    serviceAccountIssuerDiscovery : KopsK8sIoV1alpha2ClusterSpecServiceAccountIssuerDiscovery, default is Undefined, optional
        service account issuer discovery
    serviceClusterIPRange : str, default is Undefined, optional
        ServiceClusterIPRange is the CIDR, from the internal network, where we allocate IPs for services
    snapshotController : KopsK8sIoV1alpha2ClusterSpecSnapshotController, default is Undefined, optional
        snapshot controller
    sshAccess : [str], default is Undefined, optional
        SSHAccess determines the permitted access to SSH Currently only a single CIDR is supported (though a richer grammar could be added in future)
    sshKeyName : str, default is Undefined, optional
        SSHKeyName specifies a preexisting SSH key to use
    subnets : [KopsK8sIoV1alpha2ClusterSpecSubnetsItems0], default is Undefined, optional
        Configuration of subnets we are targeting
    sysctlParameters : [str], default is Undefined, optional
        SysctlParameters will configure kernel parameters using sysctl(8). When specified, each parameter must follow the form variable=value, the way it would appear in sysctl.conf.
    target : KopsK8sIoV1alpha2ClusterSpecTarget, default is Undefined, optional
        target
    topology : KopsK8sIoV1alpha2ClusterSpecTopology, default is Undefined, optional
        topology
    updatePolicy : str, default is Undefined, optional
        UpdatePolicy determines the policy for applying upgrades automatically. Valid values: 'automatic' (default): apply updates automatically (apply OS security upgrades, avoiding rebooting when possible) 'external': do not apply updates automatically; they are applied manually or by an external system
    useHostCertificates : bool, default is Undefined, optional
        UseHostCertificates will mount /etc/ssl/certs to inside needed containers. This is needed if some APIs do have self-signed certs
    warmPool : KopsK8sIoV1alpha2ClusterSpecWarmPool, default is Undefined, optional
        warm pool
    """


    DisableSubnetTags?: bool

    additionalNetworkCIDRs?: [str]

    additionalPolicies?: {str:str}

    additionalSans?: [str]

    addons?: [KopsK8sIoV1alpha2ClusterSpecAddonsItems0]

    api?: KopsK8sIoV1alpha2ClusterSpecAPI

    assets?: KopsK8sIoV1alpha2ClusterSpecAssets

    authentication?: KopsK8sIoV1alpha2ClusterSpecAuthentication

    authorization?: KopsK8sIoV1alpha2ClusterSpecAuthorization

    awsLoadBalancerController?: KopsK8sIoV1alpha2ClusterSpecAwsLoadBalancerController

    certManager?: KopsK8sIoV1alpha2ClusterSpecCertManager

    channel?: str

    cloudConfig?: KopsK8sIoV1alpha2ClusterSpecCloudConfig

    cloudControllerManager?: KopsK8sIoV1alpha2ClusterSpecCloudControllerManager

    cloudLabels?: {str:str}

    cloudProvider?: str

    clusterAutoscaler?: KopsK8sIoV1alpha2ClusterSpecClusterAutoscaler

    clusterDNSDomain?: str

    configBase?: str

    configStore?: str

    containerRuntime?: str

    containerd?: KopsK8sIoV1alpha2ClusterSpecContainerd

    dnsControllerGossipConfig?: KopsK8sIoV1alpha2ClusterSpecDNSControllerGossipConfig

    dnsZone?: str

    docker?: KopsK8sIoV1alpha2ClusterSpecDocker

    egressProxy?: KopsK8sIoV1alpha2ClusterSpecEgressProxy

    encryptionConfig?: bool

    etcdClusters?: [KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0]

    externalDns?: KopsK8sIoV1alpha2ClusterSpecExternalDNS

    externalPolicies?: {str:[str]}

    fileAssets?: [KopsK8sIoV1alpha2ClusterSpecFileAssetsItems0]

    gossipConfig?: KopsK8sIoV1alpha2ClusterSpecGossipConfig

    hooks?: [KopsK8sIoV1alpha2ClusterSpecHooksItems0]

    iam?: KopsK8sIoV1alpha2ClusterSpecIam

    isolateMasters?: bool

    karpenter?: KopsK8sIoV1alpha2ClusterSpecKarpenter

    keyStore?: str

    kubeAPIServer?: KopsK8sIoV1alpha2ClusterSpecKubeAPIServer

    kubeControllerManager?: KopsK8sIoV1alpha2ClusterSpecKubeControllerManager

    kubeDNS?: KopsK8sIoV1alpha2ClusterSpecKubeDNS

    kubeProxy?: KopsK8sIoV1alpha2ClusterSpecKubeProxy

    kubeScheduler?: KopsK8sIoV1alpha2ClusterSpecKubeScheduler

    kubelet?: KopsK8sIoV1alpha2ClusterSpecKubelet

    kubernetesApiAccess?: [str]

    kubernetesVersion?: str

    masterInternalName?: str

    masterKubelet?: KopsK8sIoV1alpha2ClusterSpecMasterKubelet

    masterPublicName?: str

    metricsServer?: KopsK8sIoV1alpha2ClusterSpecMetricsServer

    networkCIDR?: str

    networkID?: str

    networking?: KopsK8sIoV1alpha2ClusterSpecNetworking

    nodeAuthorization?: KopsK8sIoV1alpha2ClusterSpecNodeAuthorization

    nodePortAccess?: [str]

    nodeProblemDetector?: KopsK8sIoV1alpha2ClusterSpecNodeProblemDetector

    nodeTerminationHandler?: KopsK8sIoV1alpha2ClusterSpecNodeTerminationHandler

    nonMasqueradeCIDR?: str

    ntp?: KopsK8sIoV1alpha2ClusterSpecNtp

    packages?: [str]

    podCIDR?: str

    podIdentityWebhook?: KopsK8sIoV1alpha2ClusterSpecPodIdentityWebhook

    project?: str

    rollingUpdate?: KopsK8sIoV1alpha2ClusterSpecRollingUpdate

    secretStore?: str

    serviceAccountIssuerDiscovery?: KopsK8sIoV1alpha2ClusterSpecServiceAccountIssuerDiscovery

    serviceClusterIPRange?: str

    snapshotController?: KopsK8sIoV1alpha2ClusterSpecSnapshotController

    sshAccess?: [str]

    sshKeyName?: str

    subnets?: [KopsK8sIoV1alpha2ClusterSpecSubnetsItems0]

    sysctlParameters?: [str]

    target?: KopsK8sIoV1alpha2ClusterSpecTarget

    topology?: KopsK8sIoV1alpha2ClusterSpecTopology

    updatePolicy?: str

    useHostCertificates?: bool

    warmPool?: KopsK8sIoV1alpha2ClusterSpecWarmPool


    check:


schema KopsK8sIoV1alpha2ClusterSpecAPI:
    """
    API field controls how the API is exposed outside the cluster

    Attributes
    ----------
    dns : any, default is Undefined, optional
        DNS will be used to provide config on kube-apiserver ELB DNS
    loadBalancer : KopsK8sIoV1alpha2ClusterSpecAPILoadBalancer, default is Undefined, optional
        load balancer
    """


    dns?: any

    loadBalancer?: KopsK8sIoV1alpha2ClusterSpecAPILoadBalancer


schema KopsK8sIoV1alpha2ClusterSpecAPILoadBalancer:
    """
    LoadBalancer is the configuration for the kube-apiserver ELB

    Attributes
    ----------
    accessLog : KopsK8sIoV1alpha2ClusterSpecAPILoadBalancerAccessLog, default is Undefined, optional
        access log
    additionalSecurityGroups : [str], default is Undefined, optional
        AdditionalSecurityGroups attaches additional security groups (e.g. sg-123456).
    class : str, default is Undefined, optional
        LoadBalancerClass specifies the class of load balancer to create: Classic, Network
    crossZoneLoadBalancing : bool, default is Undefined, optional
        CrossZoneLoadBalancing allows you to enable the cross zone load balancing
    idleTimeoutSeconds : int, default is Undefined, optional
        IdleTimeoutSeconds sets the timeout of the api loadbalancer.
    securityGroupOverride : str, default is Undefined, optional
        SecurityGroupOverride overrides the default Kops created SG for the load balancer.
    sslCertificate : str, default is Undefined, optional
        SSLCertificate allows you to specify the ACM cert to be used the LB
    sslPolicy : str, default is Undefined, optional
        SSLPolicy allows you to overwrite the LB listener's Security Policy
    subnets : [KopsK8sIoV1alpha2ClusterSpecAPILoadBalancerSubnetsItems0], default is Undefined, optional
        Subnets allows you to specify the subnets that must be used for the load balancer
    $type : str, default is Undefined, optional
        Type of load balancer to create may Public or Internal.
    useForInternalApi : bool, default is Undefined, optional
        UseForInternalAPI indicates whether the LB should be used by the kubelet
    """


    accessLog?: KopsK8sIoV1alpha2ClusterSpecAPILoadBalancerAccessLog

    additionalSecurityGroups?: [str]

    class?: str

    crossZoneLoadBalancing?: bool

    idleTimeoutSeconds?: int

    securityGroupOverride?: str

    sslCertificate?: str

    sslPolicy?: str

    subnets?: [KopsK8sIoV1alpha2ClusterSpecAPILoadBalancerSubnetsItems0]

    $type?: str

    useForInternalApi?: bool


schema KopsK8sIoV1alpha2ClusterSpecAPILoadBalancerAccessLog:
    """
    AccessLog is the configuration of access logs

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket is S3 bucket name to store the logs in
    bucketPrefix : str, default is Undefined, optional
        BucketPrefix is S3 bucket prefix. Logs are stored in the root if not configured.
    interval : int, default is Undefined, optional
        Interval is publishing interval in minutes. This parameter is only used with classic load balancer.
    """


    bucket?: str

    bucketPrefix?: str

    interval?: int


schema KopsK8sIoV1alpha2ClusterSpecAPILoadBalancerSubnetsItems0:
    """
    LoadBalancerSubnetSpec provides configuration for subnets used for a load balancer

    Attributes
    ----------
    allocationId : str, default is Undefined, optional
        AllocationID specifies the Elastic IP Allocation ID for use by a NLB
    name : str, default is Undefined, optional
        Name specifies the name of the cluster subnet
    privateIPv4Address : str, default is Undefined, optional
        PrivateIPv4Address specifies the private IPv4 address to use for a NLB
    """


    allocationId?: str

    name?: str

    privateIPv4Address?: str


schema KopsK8sIoV1alpha2ClusterSpecAddonsItems0:
    """
    AddonSpec defines an addon that we want to install in the cluster

    Attributes
    ----------
    manifest : str, default is Undefined, optional
        Manifest is a path to the manifest that defines the addon
    """


    manifest?: str


schema KopsK8sIoV1alpha2ClusterSpecAssets:
    """
    Alternative locations for files and containers

    Attributes
    ----------
    containerProxy : str, default is Undefined, optional
        ContainerProxy is a url for a pull-through proxy of a docker registry
    containerRegistry : str, default is Undefined, optional
        ContainerRegistry is a url for to a docker registry
    fileRepository : str, default is Undefined, optional
        FileRepository is the url for a private file serving repository
    """


    containerProxy?: str

    containerRegistry?: str

    fileRepository?: str


schema KopsK8sIoV1alpha2ClusterSpecAuthentication:
    """
    Authentication field controls how the cluster is configured for authentication

    Attributes
    ----------
    aws : KopsK8sIoV1alpha2ClusterSpecAuthenticationAws, default is Undefined, optional
        aws
    kopeio : any, default is Undefined, optional
        kopeio
    """


    aws?: KopsK8sIoV1alpha2ClusterSpecAuthenticationAws

    kopeio?: any


    check:


schema KopsK8sIoV1alpha2ClusterSpecAuthenticationAws:
    """
    kops k8s io v1alpha2 cluster spec authentication aws

    Attributes
    ----------
    backendMode : str, default is Undefined, optional
        BackendMode is the AWS IAM Authenticator backend to use. Default MountedFile
    clusterID : str, default is Undefined, optional
        ClusterID identifies the cluster performing authentication to prevent certain replay attacks. Default master public DNS name
    cpuLimit : int | str, default is Undefined, optional
        CPULimit CPU limit of AWS IAM Authenticator container. Default 10m
    cpuRequest : int | str, default is Undefined, optional
        CPURequest CPU request of AWS IAM Authenticator container. Default 10m
    identityMappings : [KopsK8sIoV1alpha2ClusterSpecAuthenticationAwsIdentityMappingsItems0], default is Undefined, optional
        IdentityMappings maps IAM Identities to Kubernetes users/groups
    image : str, default is Undefined, optional
        Image is the AWS IAM Authenticator container image to use.
    memoryLimit : int | str, default is Undefined, optional
        MemoryLimit memory limit of AWS IAM Authenticator container. Default 20Mi
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest memory request of AWS IAM Authenticator container. Default 20Mi
    """


    backendMode?: str

    clusterID?: str

    cpuLimit?: int | str

    cpuRequest?: int | str

    identityMappings?: [KopsK8sIoV1alpha2ClusterSpecAuthenticationAwsIdentityMappingsItems0]

    image?: str

    memoryLimit?: int | str

    memoryRequest?: int | str


    check:
        matchPattern(cpuLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecAuthenticationAwsIdentityMappingsItems0:
    """
    kops k8s io v1alpha2 cluster spec authentication aws identity mappings items0

    Attributes
    ----------
    arn : str, default is Undefined, optional
        Arn of the IAM User or IAM Role to be allowed to authenticate
    groups : [str], default is Undefined, optional
        Groups to be attached to your users/roles
    username : str, default is Undefined, optional
        Username that Kubernetes will see the user as
    """


    arn?: str

    groups?: [str]

    username?: str


schema KopsK8sIoV1alpha2ClusterSpecAuthorization:
    """
    Authorization field controls how the cluster is configured for authorization

    Attributes
    ----------
    alwaysAllow : any, default is Undefined, optional
        always allow
    rbac : any, default is Undefined, optional
        rbac
    """


    alwaysAllow?: any

    rbac?: any


schema KopsK8sIoV1alpha2ClusterSpecAwsLoadBalancerController:
    """
    AWSLoadbalancerControllerConfig determines the AWS LB controller configuration.

    Attributes
    ----------
    enableShield : bool, default is Undefined, optional
        EnableShield specifies whether the controller can enable Shield Advanced. Default: false
    enableWAF : bool, default is Undefined, optional
        EnableWAF specifies whether the controller can use WAFs (Classic Regional). Default: false
    enableWAFv2 : bool, default is Undefined, optional
        EnableWAFv2 specifies whether the controller can use WAFs (V2). Default: false
    enabled : bool, default is Undefined, optional
        Enabled enables the loadbalancer controller. Default: false
    version : str, default is Undefined, optional
        Version is the container image tag used.
    """


    enableShield?: bool

    enableWAF?: bool

    enableWAFv2?: bool

    enabled?: bool

    version?: str


schema KopsK8sIoV1alpha2ClusterSpecCertManager:
    """
    CertManager determines the metrics server configuration.

    Attributes
    ----------
    defaultIssuer : str, default is Undefined, optional
        defaultIssuer sets a default clusterIssuer Default: none
    enabled : bool, default is Undefined, optional
        Enabled enables the cert manager. Default: false
    hostedZoneIDs : [str], default is Undefined, optional
        HostedZoneIDs is a list of route53 hostedzone IDs that cert-manager will be allowed to do dns-01 validation for
    image : str, default is Undefined, optional
        Image is the container image used. Default: the latest supported image for the specified kubernetes version.
    managed : bool, default is Undefined, optional
        Managed controls if cert-manager is manged and deployed by kOps. The deployment of cert-manager is skipped if this is set to false.
    nameservers : [str], default is Undefined, optional
        nameservers is a list of nameserver IP addresses to use instead of the pod defaults. Default: none
    """


    defaultIssuer?: str

    enabled?: bool

    hostedZoneIDs?: [str]

    image?: str

    managed?: bool

    nameservers?: [str]


schema KopsK8sIoV1alpha2ClusterSpecCloudConfig:
    """
    CloudConfiguration defines the cloud provider configuration

    Attributes
    ----------
    awsEBSCSIDriver : KopsK8sIoV1alpha2ClusterSpecCloudConfigAwsEBSCSIDriver, default is Undefined, optional
        aws e b s c s i driver
    azure : KopsK8sIoV1alpha2ClusterSpecCloudConfigAzure, default is Undefined, optional
        azure
    disableSecurityGroupIngress : bool, default is Undefined, optional
        DisableSecurityGroupIngress disables the Cloud Controller Manager's creation of an AWS Security Group for each load balancer provisioned for a Service (AWS only).
    elbSecurityGroup : str, default is Undefined, optional
        ElbSecurityGroup specifies an existing AWS Security group for the Cloud Controller Manager to assign to each ELB provisioned for a Service, instead of creating one per ELB (AWS only).
    gceServiceAccount : str, default is Undefined, optional
        GCEServiceAccount specifies the service account with which the GCE VM runs
    gcpPDCSIDriver : KopsK8sIoV1alpha2ClusterSpecCloudConfigGcpPDCSIDriver, default is Undefined, optional
        gcp p d c s i driver
    manageStorageClasses : bool, default is Undefined, optional
        ManageStorageClasses specifies whether kOps should create and maintain a set of StorageClasses, one of which it nominates as the default class for the cluster.
    multizone : bool, default is Undefined, optional
        GCE cloud-config options
    nodeIPFamilies : [str], default is Undefined, optional
        NodeIPFamilies controls the IP families reported for each node (AWS only).
    nodeInstancePrefix : str, default is Undefined, optional
        node instance prefix
    nodeTags : str, default is Undefined, optional
        node tags
    openstack : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstack, default is Undefined, optional
        openstack
    spotinstOrientation : str, default is Undefined, optional
        spotinst orientation
    spotinstProduct : str, default is Undefined, optional
        Spotinst cloud-config specs
    vSphereCoreDNSServer : str, default is Undefined, optional
        VSphereCoreDNSServer is unused.
    vSphereDatacenter : str, default is Undefined, optional
        VShpereDatacenter is unused.
    vSphereDatastore : str, default is Undefined, optional
        VSphereDatastore is unused.
    vSpherePassword : str, default is Undefined, optional
        VSpherePassword is unused.
    vSphereResourcePool : str, default is Undefined, optional
        VSphereResourcePool is unused.
    vSphereServer : str, default is Undefined, optional
        VSphereServer is unused.
    vSphereUsername : str, default is Undefined, optional
        VSphereUsername is unused.
    """


    awsEBSCSIDriver?: KopsK8sIoV1alpha2ClusterSpecCloudConfigAwsEBSCSIDriver

    azure?: KopsK8sIoV1alpha2ClusterSpecCloudConfigAzure

    disableSecurityGroupIngress?: bool

    elbSecurityGroup?: str

    gceServiceAccount?: str

    gcpPDCSIDriver?: KopsK8sIoV1alpha2ClusterSpecCloudConfigGcpPDCSIDriver

    manageStorageClasses?: bool

    multizone?: bool

    nodeIPFamilies?: [str]

    nodeInstancePrefix?: str

    nodeTags?: str

    openstack?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstack

    spotinstOrientation?: str

    spotinstProduct?: str

    vSphereCoreDNSServer?: str

    vSphereDatacenter?: str

    vSphereDatastore?: str

    vSpherePassword?: str

    vSphereResourcePool?: str

    vSphereServer?: str

    vSphereUsername?: str


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigAwsEBSCSIDriver:
    """
    AWSEBSCSIDriver is the config for the AWS EBS CSI driver

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enabled enables the AWS EBS CSI driver. Can only be set to true. Default: true
    managed : bool, default is Undefined, optional
        Managed controls if aws-ebs-csi-driver is manged and deployed by kOps. The deployment of aws-ebs-csi-driver is skipped if this is set to false.
    podAnnotations : {str:str}, default is Undefined, optional
        PodAnnotations are the annotations added to AWS EBS CSI node and controller Pods. Default: none
    version : str, default is Undefined, optional
        Version is the container image tag used. Default: The latest stable release which is compatible with your Kubernetes version
    volumeAttachLimit : int, default is Undefined, optional
        VolumeAttachLimit is the maximum number of volumes attachable per node. If specified, the limit applies to all nodes. If not specified, the value is approximated from the instance type. Default: -
    """


    enabled?: bool

    managed?: bool

    podAnnotations?: {str:str}

    version?: str

    volumeAttachLimit?: int


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigAzure:
    """
    Azure cloud-config options

    Attributes
    ----------
    adminUser : str, default is Undefined, optional
        AdminUser specifies the admin user of VMs.
    resourceGroupName : str, default is Undefined, optional
        ResourceGroupName specifies the name of the resource group where the cluster is built. If this is empty, kops will create a new resource group whose name is same as the cluster name. If this is not empty, kops will not create a new resource group, and it will just reuse the existing resource group of the name. This follows the model that kops takes for AWS VPC.
    routeTableName : str, default is Undefined, optional
        RouteTableName is the name of the route table attached to the subnet that the cluster is deployed in.
    subscriptionId : str, default is Undefined, optional
        SubscriptionID specifies the subscription used for the cluster installation.
    tenantId : str, default is Undefined, required
        TenantID is the ID of the tenant that the cluster is deployed in.
    """


    adminUser?: str

    resourceGroupName?: str

    routeTableName?: str

    subscriptionId?: str

    tenantId: str


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigGcpPDCSIDriver:
    """
    GCPPDCSIDriver is the config for the GCP PD CSI driver

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enabled enables the GCP PD CSI driver
    """


    enabled?: bool


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstack:
    """
    Openstack cloud-config options

    Attributes
    ----------
    blockStorage : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackBlockStorage, default is Undefined, optional
        block storage
    insecureSkipVerify : bool, default is Undefined, optional
        insecure skip verify
    loadbalancer : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackLoadbalancer, default is Undefined, optional
        loadbalancer
    metadata : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackMetadata, default is Undefined, optional
        metadata
    monitor : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackMonitor, default is Undefined, optional
        monitor
    network : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackNetwork, default is Undefined, optional
        network
    router : KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackRouter, default is Undefined, optional
        router
    """


    blockStorage?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackBlockStorage

    insecureSkipVerify?: bool

    loadbalancer?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackLoadbalancer

    metadata?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackMetadata

    monitor?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackMonitor

    network?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackNetwork

    router?: KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackRouter


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackBlockStorage:
    """
    kops k8s io v1alpha2 cluster spec cloud config openstack block storage

    Attributes
    ----------
    bs_version : str, default is Undefined, optional
        bs version
    clusterName : str, default is Undefined, optional
        ClusterName sets the --cluster flag for the cinder-csi-plugin to the provided name
    createStorageClass : bool, default is Undefined, optional
        CreateStorageClass provisions a default class for the Cinder plugin
    csiPluginImage : str, default is Undefined, optional
        csi plugin image
    csiTopologySupport : bool, default is Undefined, optional
        csi topology support
    ignore_volume_az : bool, default is Undefined, optional
        ignore volume az
    ignore_volume_microversion : bool, default is Undefined, optional
        ignore volume microversion
    metricsEnabled : bool, default is Undefined, optional
        metrics enabled
    override_volume_az : str, default is Undefined, optional
        override volume az
    """


    bs_version?: str

    clusterName?: str

    createStorageClass?: bool

    csiPluginImage?: str

    csiTopologySupport?: bool

    ignore_volume_az?: bool

    ignore_volume_microversion?: bool

    metricsEnabled?: bool

    override_volume_az?: str


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackLoadbalancer:
    """
    OpenstackLoadbalancerConfig defines the config for a neutron loadbalancer

    Attributes
    ----------
    enableIngressHostname : bool, default is Undefined, optional
        enable ingress hostname
    flavorID : str, default is Undefined, optional
        flavor ID
    floatingNetwork : str, default is Undefined, optional
        floating network
    floatingNetworkID : str, default is Undefined, optional
        floating network ID
    floatingSubnet : str, default is Undefined, optional
        floating subnet
    ingressHostnameSuffix : str, default is Undefined, optional
        ingress hostname suffix
    manageSecurityGroups : bool, default is Undefined, optional
        manage security groups
    method : str, default is Undefined, optional
        method
    provider : str, default is Undefined, optional
        provider
    subnetID : str, default is Undefined, optional
        subnet ID
    useOctavia : bool, default is Undefined, optional
        use octavia
    """


    enableIngressHostname?: bool

    flavorID?: str

    floatingNetwork?: str

    floatingNetworkID?: str

    floatingSubnet?: str

    ingressHostnameSuffix?: str

    manageSecurityGroups?: bool

    method?: str

    provider?: str

    subnetID?: str

    useOctavia?: bool


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackMetadata:
    """
    OpenstackMetadata defines config for metadata service related settings

    Attributes
    ----------
    configDrive : bool, default is Undefined, optional
        ConfigDrive specifies to use config drive for retrieving user data instead of the metadata service when launching instances
    """


    configDrive?: bool


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackMonitor:
    """
    OpenstackMonitor defines the config for a health monitor

    Attributes
    ----------
    delay : str, default is Undefined, optional
        delay
    maxRetries : int, default is Undefined, optional
        max retries
    timeout : str, default is Undefined, optional
        timeout
    """


    delay?: str

    maxRetries?: int

    timeout?: str


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackNetwork:
    """
    OpenstackNetwork defines the config for a network

    Attributes
    ----------
    addressSortOrder : str, default is Undefined, optional
        address sort order
    availabilityZoneHints : [str], default is Undefined, optional
        availability zone hints
    internalNetworkNames : [str], default is Undefined, optional
        internal network names
    ipv6SupportDisabled : bool, default is Undefined, optional
        ipv6 support disabled
    publicNetworkNames : [str], default is Undefined, optional
        public network names
    """


    addressSortOrder?: str

    availabilityZoneHints?: [str]

    internalNetworkNames?: [str]

    ipv6SupportDisabled?: bool

    publicNetworkNames?: [str]


schema KopsK8sIoV1alpha2ClusterSpecCloudConfigOpenstackRouter:
    """
    OpenstackRouter defines the config for a router

    Attributes
    ----------
    availabilityZoneHints : [str], default is Undefined, optional
        availability zone hints
    dnsServers : str, default is Undefined, optional
        dns servers
    externalNetwork : str, default is Undefined, optional
        external network
    externalSubnet : str, default is Undefined, optional
        external subnet
    """


    availabilityZoneHints?: [str]

    dnsServers?: str

    externalNetwork?: str

    externalSubnet?: str


schema KopsK8sIoV1alpha2ClusterSpecCloudControllerManager:
    """
    CloudControllerManagerConfig is the configuration of the cloud controller

    Attributes
    ----------
    allocateNodeCIDRs : bool, default is Undefined, optional
        AllocateNodeCIDRs enables CIDRs for Pods to be allocated and, if ConfigureCloudRoutes is true, to be set on the cloud provider.
    allowUntaggedCloud : bool, default is Undefined, optional
        Allow the cluster to run without the cluster-id on cloud instances
    cidrAllocatorType : str, default is Undefined, optional
        CIDRAllocatorType specifies the type of CIDR allocator to use.
    cloudProvider : str, default is Undefined, optional
        CloudProvider is the provider for cloud services.
    clusterCIDR : str, default is Undefined, optional
        ClusterCIDR is CIDR Range for Pods in cluster.
    clusterName : str, default is Undefined, optional
        ClusterName is the instance prefix for the cluster.
    configureCloudRoutes : bool, default is Undefined, optional
        ConfigureCloudRoutes enables CIDRs allocated with to be configured on the cloud provider.
    controllers : [str], default is Undefined, optional
        Controllers is a list of controllers to enable on the controller-manager
    cpuRequest : int | str, default is Undefined, optional
        CPURequest of CloudControllerManager container. Default: 200m
    enableLeaderMigration : bool, default is Undefined, optional
        EnableLeaderMigration enables controller leader migration.
    image : str, default is Undefined, optional
        Image is the OCI image of the cloud controller manager.
    leaderElection : KopsK8sIoV1alpha2ClusterSpecCloudControllerManagerLeaderElection, default is Undefined, optional
        leader election
    logLevel : int, default is Undefined, optional
        LogLevel is the verbosity of the logs.
    master : str, default is Undefined, optional
        Master is the url for the kube api master.
    nodeStatusUpdateFrequency : str, default is Undefined, optional
        NodeStatusUpdateFrequency is the duration between node status updates. (default: 5m)
    useServiceAccountCredentials : bool, default is Undefined, optional
        UseServiceAccountCredentials controls whether we use individual service account credentials for each controller.
    """


    allocateNodeCIDRs?: bool

    allowUntaggedCloud?: bool

    cidrAllocatorType?: str

    cloudProvider?: str

    clusterCIDR?: str

    clusterName?: str

    configureCloudRoutes?: bool

    controllers?: [str]

    cpuRequest?: int | str

    enableLeaderMigration?: bool

    image?: str

    leaderElection?: KopsK8sIoV1alpha2ClusterSpecCloudControllerManagerLeaderElection

    logLevel?: int

    master?: str

    nodeStatusUpdateFrequency?: str

    useServiceAccountCredentials?: bool


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecCloudControllerManagerLeaderElection:
    """
    LeaderElection defines the configuration of leader election client.

    Attributes
    ----------
    leaderElect : bool, default is Undefined, optional
        leaderElect enables a leader election client to gain leadership before executing the main loop. Enable this when running replicated components for high availability.
    leaderElectLeaseDuration : str, default is Undefined, optional
        leaderElectLeaseDuration is the length in time non-leader candidates will wait after observing a leadership renewal until attempting to acquire leadership of a led but unrenewed leader slot. This is effectively the maximum duration that a leader can be stopped before it is replaced by another candidate
    leaderElectRenewDeadlineDuration : str, default is Undefined, optional
        LeaderElectRenewDeadlineDuration is the interval between attempts by the acting master to renew a leadership slot before it stops leading. This must be less than or equal to the lease duration.
    leaderElectResourceLock : str, default is Undefined, optional
        LeaderElectResourceLock is the type of resource object that is used for locking during leader election. Supported options are endpoints (default) and `configmaps`.
    leaderElectResourceName : str, default is Undefined, optional
        LeaderElectResourceName is the name of resource object that is used for locking during leader election.
    leaderElectResourceNamespace : str, default is Undefined, optional
        LeaderElectResourceNamespace is the namespace of resource object that is used for locking during leader election.
    leaderElectRetryPeriod : str, default is Undefined, optional
        LeaderElectRetryPeriod is The duration the clients should wait between attempting acquisition and renewal of a leadership. This is only applicable if leader election is enabled.
    """


    leaderElect?: bool

    leaderElectLeaseDuration?: str

    leaderElectRenewDeadlineDuration?: str

    leaderElectResourceLock?: str

    leaderElectResourceName?: str

    leaderElectResourceNamespace?: str

    leaderElectRetryPeriod?: str


schema KopsK8sIoV1alpha2ClusterSpecClusterAutoscaler:
    """
    ClusterAutoscaler defines the cluster autoscaler configuration.

    Attributes
    ----------
    awsUseStaticInstanceList : bool, default is Undefined, optional
        AWSUseStaticInstanceList makes the cluster autoscaler to use statically defined set of AWS EC2 Instance List. Default: false
    balanceSimilarNodeGroups : bool, default is Undefined, optional
        BalanceSimilarNodeGroups makes the cluster autoscaler treat similar node groups as one. Default: false
    cordonNodeBeforeTerminating : bool, default is Undefined, optional
        CordonNodeBeforeTerminating should CA cordon nodes before terminating during downscale process Default: false
    cpuRequest : int | str, default is Undefined, optional
        CPURequest of cluster autoscaler container. Default: 100m
    createPriorityExpanderConfig : bool, default is Undefined, optional
        CreatePriorityExpenderConfig makes kOps create the priority-expander ConfigMap Default: true
    customPriorityExpanderConfig : {str:[str]}, default is Undefined, optional
        CustomPriorityExpanderConfig overides the priority-expander ConfigMap with the provided configuration. Any InstanceGroup configuration will be ignored if this is set. This could be useful in order to use regex on priorities configuration
    enabled : bool, default is Undefined, optional
        Enabled enables the cluster autoscaler. Default: false
    expander : str, default is Undefined, optional
        Expander determines the strategy for which instance group gets expanded. Supported values: least-waste, most-pods, random, price, priority. The price expander is only supported on GCE. By default, kOps will generate the priority expander ConfigMap based on the `autoscale` and `autoscalePriority` fields in the InstanceGroup specs. Default: least-waste
    ignoreDaemonSetsUtilization : bool, default is Undefined, optional
        IgnoreDaemonSetsUtilization causes the cluster autoscaler to ignore DaemonSet-managed pods when calculating resource utilization for scaling down. Default: false
    image : str, default is Undefined, optional
        Image is the container image used. Default: the latest supported image for the specified kubernetes version.
    maxNodeProvisionTime : str, default is Undefined, optional
        MaxNodeProvisionTime determines how long CAS will wait for a node to join the cluster.
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest of cluster autoscaler container. Default: 300Mi
    newPodScaleUpDelay : str, default is Undefined, optional
        NewPodScaleUpDelay causes the cluster autoscaler to ignore unschedulable pods until they are a certain "age", regardless of the scan-interval Default: 0s
    podAnnotations : {str:str}, default is Undefined, optional
        PodAnnotations are the annotations added to cluster autoscaler pods when they are created. Default: none
    scaleDownDelayAfterAdd : str, default is Undefined, optional
        ScaleDownDelayAfterAdd determines the time after scale up that scale down evaluation resumes Default: 10m0s
    scaleDownUnneededTime : str, default is Undefined, optional
        scaleDownUnneededTime determines the time a node should be unneeded before it is eligible for scale down Default: 10m0s
    scaleDownUnreadyTime : str, default is Undefined, optional
        ScaleDownUnreadyTime determines the time an unready node should be unneeded before it is eligible for scale down Default: 20m0s
    scaleDownUtilizationThreshold : str, default is Undefined, optional
        ScaleDownUtilizationThreshold determines the utilization threshold for node scale-down. Default: 0.5
    skipNodesWithCustomControllerPods : bool, default is Undefined, optional
        SkipNodesWithCustomControllerPods makes the cluster autoscaler skip scale-down of nodes with pods owned by custom controllers. Default: true
    skipNodesWithLocalStorage : bool, default is Undefined, optional
        SkipNodesWithLocalStorage makes the cluster autoscaler skip scale-down of nodes with local storage. Default: true
    skipNodesWithSystemPods : bool, default is Undefined, optional
        SkipNodesWithSystemPods makes the cluster autoscaler skip scale-down of nodes with non-DaemonSet pods in the kube-system namespace. Default: true
    """


    awsUseStaticInstanceList?: bool

    balanceSimilarNodeGroups?: bool

    cordonNodeBeforeTerminating?: bool

    cpuRequest?: int | str

    createPriorityExpanderConfig?: bool

    customPriorityExpanderConfig?: {str:[str]}

    enabled?: bool

    expander?: str

    ignoreDaemonSetsUtilization?: bool

    image?: str

    maxNodeProvisionTime?: str

    memoryRequest?: int | str

    newPodScaleUpDelay?: str

    podAnnotations?: {str:str}

    scaleDownDelayAfterAdd?: str

    scaleDownUnneededTime?: str

    scaleDownUnreadyTime?: str

    scaleDownUtilizationThreshold?: str

    skipNodesWithCustomControllerPods?: bool

    skipNodesWithLocalStorage?: bool

    skipNodesWithSystemPods?: bool


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecContainerd:
    """
    Component configurations

    Attributes
    ----------
    address : str, default is Undefined, optional
        Address of containerd's GRPC server (default "/run/containerd/containerd.sock").
    configAdditions : {str:int | str}, default is Undefined, optional
        ConfigAdditions adds additional config entries to the generated config file.
    configOverride : str, default is Undefined, optional
        ConfigOverride is the complete containerd config file provided by the user.
    logLevel : str, default is Undefined, optional
        LogLevel controls the logging details [trace, debug, info, warn, error, fatal, panic] (default "info").
    nri : KopsK8sIoV1alpha2ClusterSpecContainerdNri, default is Undefined, optional
        nri
    nvidiaGPU : KopsK8sIoV1alpha2ClusterSpecContainerdNvidiaGPU, default is Undefined, optional
        nvidia g p u
    packages : KopsK8sIoV1alpha2ClusterSpecContainerdPackages, default is Undefined, optional
        packages
    registryMirrors : {str:[str]}, default is Undefined, optional
        RegistryMirrors is list of image registries
    root : str, default is Undefined, optional
        Root directory for persistent data (default "/var/lib/containerd").
    runc : KopsK8sIoV1alpha2ClusterSpecContainerdRunc, default is Undefined, optional
        runc
    selinuxEnabled : bool, default is Undefined, optional
        SelinuxEnabled enables SELinux support
    skipInstall : bool, default is Undefined, optional
        SkipInstall prevents kOps from installing and modifying containerd in any way (default "false").
    state : str, default is Undefined, optional
        State directory for execution state files (default "/run/containerd").
    version : str, default is Undefined, optional
        Version used to pick the containerd package.
    """


    address?: str

    configAdditions?: {str:int | str}

    configOverride?: str

    logLevel?: str

    nri?: KopsK8sIoV1alpha2ClusterSpecContainerdNri

    nvidiaGPU?: KopsK8sIoV1alpha2ClusterSpecContainerdNvidiaGPU

    packages?: KopsK8sIoV1alpha2ClusterSpecContainerdPackages

    registryMirrors?: {str:[str]}

    root?: str

    runc?: KopsK8sIoV1alpha2ClusterSpecContainerdRunc

    selinuxEnabled?: bool

    skipInstall?: bool

    state?: str

    version?: str


schema KopsK8sIoV1alpha2ClusterSpecContainerdNri:
    """
    NRI configures the Node Resource Interface.

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enable NRI support in containerd
    pluginRegistrationTimeout : str, default is Undefined, optional
        PluginRegistrationTimeout is the timeout for plugin registration
    pluginRequestTimeout : str, default is Undefined, optional
        PluginRequestTimeout is the timeout for a plugin to handle a request
    """


    enabled?: bool

    pluginRegistrationTimeout?: str

    pluginRequestTimeout?: str


schema KopsK8sIoV1alpha2ClusterSpecContainerdNvidiaGPU:
    """
    NvidiaGPU configures the Nvidia GPU runtime.

    Attributes
    ----------
    dcgmExporter : KopsK8sIoV1alpha2ClusterSpecContainerdNvidiaGPUDcgmExporter, default is Undefined, optional
        dcgm exporter
    enabled : bool, default is Undefined, optional
        Enabled determines if kOps will install the Nvidia GPU runtime and drivers. They will only be installed on intances that has an Nvidia GPU.
    package : str, default is Undefined, optional
        Package is the name of the nvidia driver package that will be installed. Default is "nvidia-headless-460-server".
    """


    dcgmExporter?: KopsK8sIoV1alpha2ClusterSpecContainerdNvidiaGPUDcgmExporter

    enabled?: bool

    package?: str


schema KopsK8sIoV1alpha2ClusterSpecContainerdNvidiaGPUDcgmExporter:
    """
    DCGMExporterConfig configures the DCGM exporter

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enabled determines if kOps will install the DCGM exporter
    """


    enabled?: bool


schema KopsK8sIoV1alpha2ClusterSpecContainerdPackages:
    """
    Packages overrides the URL and hash for the packages.

    Attributes
    ----------
    hashAmd64 : str, default is Undefined, optional
        HashAmd64 overrides the hash for the AMD64 package.
    hashArm64 : str, default is Undefined, optional
        HashArm64 overrides the hash for the ARM64 package.
    urlAmd64 : str, default is Undefined, optional
        UrlAmd64 overrides the URL for the AMD64 package.
    urlArm64 : str, default is Undefined, optional
        UrlArm64 overrides the URL for the ARM64 package.
    """


    hashAmd64?: str

    hashArm64?: str

    urlAmd64?: str

    urlArm64?: str


schema KopsK8sIoV1alpha2ClusterSpecContainerdRunc:
    """
    Runc configures the runc runtime.

    Attributes
    ----------
    packages : KopsK8sIoV1alpha2ClusterSpecContainerdRuncPackages, default is Undefined, optional
        packages
    version : str, default is Undefined, optional
        Version used to pick the runc package.
    """


    packages?: KopsK8sIoV1alpha2ClusterSpecContainerdRuncPackages

    version?: str


schema KopsK8sIoV1alpha2ClusterSpecContainerdRuncPackages:
    """
    Packages overrides the URL and hash for the packages.

    Attributes
    ----------
    hashAmd64 : str, default is Undefined, optional
        HashAmd64 overrides the hash for the AMD64 package.
    hashArm64 : str, default is Undefined, optional
        HashArm64 overrides the hash for the ARM64 package.
    urlAmd64 : str, default is Undefined, optional
        UrlAmd64 overrides the URL for the AMD64 package.
    urlArm64 : str, default is Undefined, optional
        UrlArm64 overrides the URL for the ARM64 package.
    """


    hashAmd64?: str

    hashArm64?: str

    urlAmd64?: str

    urlArm64?: str


schema KopsK8sIoV1alpha2ClusterSpecDNSControllerGossipConfig:
    """
    DNSControllerGossipConfig for the cluster assuming the use of gossip DNS

    Attributes
    ----------
    listen : str, default is Undefined, optional
        listen
    $protocol : str, default is Undefined, optional
        protocol
    secondary : KopsK8sIoV1alpha2ClusterSpecDNSControllerGossipConfigSecondary, default is Undefined, optional
        secondary
    secret : str, default is Undefined, optional
        secret
    seed : str, default is Undefined, optional
        seed
    """


    listen?: str

    $protocol?: str

    secondary?: KopsK8sIoV1alpha2ClusterSpecDNSControllerGossipConfigSecondary

    secret?: str

    seed?: str


schema KopsK8sIoV1alpha2ClusterSpecDNSControllerGossipConfigSecondary:
    """
    kops k8s io v1alpha2 cluster spec DNS controller gossip config secondary

    Attributes
    ----------
    listen : str, default is Undefined, optional
        listen
    $protocol : str, default is Undefined, optional
        protocol
    secret : str, default is Undefined, optional
        secret
    seed : str, default is Undefined, optional
        seed
    """


    listen?: str

    $protocol?: str

    secret?: str

    seed?: str


schema KopsK8sIoV1alpha2ClusterSpecDocker:
    """
    Docker was removed.

    Attributes
    ----------
    authorizationPlugins : [str], default is Undefined, optional
        AuthorizationPlugins is a list of authorization plugins
    bridge : str, default is Undefined, optional
        Bridge is the network interface containers should bind onto
    bridgeIP : str, default is Undefined, optional
        BridgeIP is a specific IP address and netmask for the docker0 bridge, using standard CIDR notation
    dataRoot : str, default is Undefined, optional
        DataRoot is the root directory of persistent docker state (default "/var/lib/docker")
    defaultRuntime : str, default is Undefined, optional
        DefaultRuntime is the default OCI runtime for containers (default "runc")
    defaultUlimit : [str], default is Undefined, optional
        DefaultUlimit is the ulimits for containers
    dns : [str], default is Undefined, optional
        DNS is the IP address of the DNS server
    execOpt : [str], default is Undefined, optional
        ExecOpt is a series of options passed to the runtime
    execRoot : str, default is Undefined, optional
        ExecRoot is the root directory for execution state files (default "/var/run/docker")
    experimental : bool, default is Undefined, optional
        Experimental features permits enabling new features such as dockerd metrics
    healthCheck : bool, default is Undefined, optional
        HealthCheck enables the periodic health-check service
    hosts : [str], default is Undefined, optional
        Hosts enables you to configure the endpoints the docker daemon listens on i.e. tcp://0.0.0.0.2375 or unix:///var/run/docker.sock etc
    insecureRegistries : [str], default is Undefined, optional
        InsecureRegistries enables multiple insecure docker registry communications
    insecureRegistry : str, default is Undefined, optional
        InsecureRegistry enable insecure registry communication @question according to dockers this a list??
    ipMasq : bool, default is Undefined, optional
        IPMasq enables ip masquerading for containers
    ipTables : bool, default is Undefined, optional
        IPtables enables addition of iptables rules
    liveRestore : bool, default is Undefined, optional
        LiveRestore enables live restore of docker when containers are still running
    logDriver : str, default is Undefined, optional
        LogDriver is the default driver for container logs (default "json-file")
    logLevel : str, default is Undefined, optional
        LogLevel is the logging level ("debug", "info", "warn", "error", "fatal") (default "info")
    logOpt : [str], default is Undefined, optional
        Logopt is a series of options given to the log driver options for containers
    maxConcurrentDownloads : int, default is Undefined, optional
        MaxConcurrentDownloads sets the max concurrent downloads for each pull
    maxConcurrentUploads : int, default is Undefined, optional
        MaxConcurrentUploads sets the max concurrent uploads for each push
    maxDownloadAttempts : int, default is Undefined, optional
        MaxDownloadAttempts sets the max download attempts for each pull
    metricsAddress : str, default is Undefined, optional
        Metrics address is the endpoint to serve with Prometheus format metrics
    mtu : int, default is Undefined, optional
        MTU is the containers network MTU
    packages : KopsK8sIoV1alpha2ClusterSpecDockerPackages, default is Undefined, optional
        packages
    registryMirrors : [str], default is Undefined, optional
        RegistryMirrors is a referred list of docker registry mirror
    runtimes : [str], default is Undefined, optional
        Runtimes registers an additional OCI compatible runtime (default [])
    selinuxEnabled : bool, default is Undefined, optional
        SelinuxEnabled enables SELinux support
    skipInstall : bool, default is Undefined, optional
        SkipInstall when set to true will prevent kops from installing and modifying Docker in any way
    storage : str, default is Undefined, optional
        Storage is the docker storage driver to use
    storageOpts : [str], default is Undefined, optional
        StorageOpts is a series of options passed to the storage driver
    userNamespaceRemap : str, default is Undefined, optional
        UserNamespaceRemap sets the user namespace remapping option for the docker daemon
    version : str, default is Undefined, optional
        Version is consumed by the nodeup and used to pick the docker version
    """


    authorizationPlugins?: [str]

    bridge?: str

    bridgeIP?: str

    dataRoot?: str

    defaultRuntime?: str

    defaultUlimit?: [str]

    dns?: [str]

    execOpt?: [str]

    execRoot?: str

    experimental?: bool

    healthCheck?: bool

    hosts?: [str]

    insecureRegistries?: [str]

    insecureRegistry?: str

    ipMasq?: bool

    ipTables?: bool

    liveRestore?: bool

    logDriver?: str

    logLevel?: str

    logOpt?: [str]

    maxConcurrentDownloads?: int

    maxConcurrentUploads?: int

    maxDownloadAttempts?: int

    metricsAddress?: str

    mtu?: int

    packages?: KopsK8sIoV1alpha2ClusterSpecDockerPackages

    registryMirrors?: [str]

    runtimes?: [str]

    selinuxEnabled?: bool

    skipInstall?: bool

    storage?: str

    storageOpts?: [str]

    userNamespaceRemap?: str

    version?: str


schema KopsK8sIoV1alpha2ClusterSpecDockerPackages:
    """
    Packages overrides the URL and hash for the packages.

    Attributes
    ----------
    hashAmd64 : str, default is Undefined, optional
        HashAmd64 overrides the hash for the AMD64 package.
    hashArm64 : str, default is Undefined, optional
        HashArm64 overrides the hash for the ARM64 package.
    urlAmd64 : str, default is Undefined, optional
        UrlAmd64 overrides the URL for the AMD64 package.
    urlArm64 : str, default is Undefined, optional
        UrlArm64 overrides the URL for the ARM64 package.
    """


    hashAmd64?: str

    hashArm64?: str

    urlAmd64?: str

    urlArm64?: str


schema KopsK8sIoV1alpha2ClusterSpecEgressProxy:
    """
    HTTPProxy defines connection information to support use of a private cluster behind an forward HTTP Proxy

    Attributes
    ----------
    excludes : str, default is Undefined, optional
        excludes
    httpProxy : KopsK8sIoV1alpha2ClusterSpecEgressProxyHTTPProxy, default is Undefined, optional
        http proxy
    """


    excludes?: str

    httpProxy?: KopsK8sIoV1alpha2ClusterSpecEgressProxyHTTPProxy


schema KopsK8sIoV1alpha2ClusterSpecEgressProxyHTTPProxy:
    """
    kops k8s io v1alpha2 cluster spec egress proxy HTTP proxy

    Attributes
    ----------
    host : str, default is Undefined, optional
        host
    port : int, default is Undefined, optional
        port
    """


    host?: str

    port?: int


schema KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0:
    """
    EtcdClusterSpec is the etcd cluster specification

    Attributes
    ----------
    backups : KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0Backups, default is Undefined, optional
        backups
    cpuRequest : int | str, default is Undefined, optional
        CPURequest specifies the cpu requests of each etcd container in the cluster.
    enableEtcdTLS : bool, default is Undefined, optional
        EnableEtcdTLS is unused.
    enableTLSAuth : bool, default is Undefined, optional
        EnableTLSAuth is unused.
    etcdMembers : [KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0EtcdMembersItems0], default is Undefined, optional
        Members stores the configurations for each member of the cluster (including the data volume)
    heartbeatInterval : str, default is Undefined, optional
        HeartbeatInterval is the time (in milliseconds) for an etcd heartbeat interval
    image : str, default is Undefined, optional
        Image is the etcd docker image to use. Setting this will ignore the Version specified.
    leaderElectionTimeout : str, default is Undefined, optional
        LeaderElectionTimeout is the time (in milliseconds) for an etcd leader election timeout
    manager : KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0Manager, default is Undefined, optional
        manager
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest specifies the memory requests of each etcd container in the cluster.
    name : str, default is Undefined, optional
        Name is the name of the etcd cluster (main, events etc)
    provider : str, default is Undefined, optional
        Provider is the provider used to run etcd: Manager, Legacy. Defaults to Manager.
    version : str, default is Undefined, optional
        Version is the version of etcd to run.
    """


    backups?: KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0Backups

    cpuRequest?: int | str

    enableEtcdTLS?: bool

    enableTLSAuth?: bool

    etcdMembers?: [KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0EtcdMembersItems0]

    heartbeatInterval?: str

    image?: str

    leaderElectionTimeout?: str

    manager?: KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0Manager

    memoryRequest?: int | str

    name?: str

    provider?: str

    version?: str


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0Backups:
    """
    Backups describes how we do backups of etcd

    Attributes
    ----------
    backupStore : str, default is Undefined, optional
        BackupStore is the VFS path where we will read/write backup data
    image : str, default is Undefined, optional
        Image is the etcd backup manager image to use.  Setting this will create a sidecar container in the etcd pod with the specified image.
    """


    backupStore?: str

    image?: str


schema KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0EtcdMembersItems0:
    """
    EtcdMemberSpec is a specification for a etcd member

    Attributes
    ----------
    encryptedVolume : bool, default is Undefined, optional
        EncryptedVolume indicates you want to encrypt the volume
    instanceGroup : str, default is Undefined, optional
        InstanceGroup is the instanceGroup this volume is associated
    kmsKeyId : str, default is Undefined, optional
        KmsKeyID is a AWS KMS ID used to encrypt the volume
    name : str, default is Undefined, optional
        Name is the name of the member within the etcd cluster
    volumeIops : int, default is Undefined, optional
        If volume type is io1, then we need to specify the number of IOPS.
    volumeSize : int, default is Undefined, optional
        VolumeSize is the underlying cloud volume size
    volumeThroughput : int, default is Undefined, optional
        Parameter for disks that support provisioned throughput
    volumeType : str, default is Undefined, optional
        VolumeType is the underlying cloud storage class
    """


    encryptedVolume?: bool

    instanceGroup?: str

    kmsKeyId?: str

    name?: str

    volumeIops?: int

    volumeSize?: int

    volumeThroughput?: int

    volumeType?: str


schema KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0Manager:
    """
    Manager describes the manager configuration

    Attributes
    ----------
    backupInterval : str, default is Undefined, optional
        BackupInterval which is used for backups. The default is 15 minutes.
    backupRetentionDays : int, default is Undefined, optional
        BackupRetentionDays which is used for backups. The default is 90 days.
    discoveryPollInterval : str, default is Undefined, optional
        DiscoveryPollInterval which is used for discovering other cluster members. The default is 60 seconds.
    env : [KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0ManagerEnvItems0], default is Undefined, optional
        Env allows users to pass in env variables to the etcd-manager container. Variables starting with ETCD_ will be further passed down to the etcd process. This allows etcd setting to be configured/overwriten. No config validation is done. A list of etcd config ENV vars can be found at https://github.com/etcd-io/etcd/blob/master/Documentation/op-guide/configuration.md
    image : str, default is Undefined, optional
        Image is the etcd manager image to use.
    listenMetricsURLs : [str], default is Undefined, optional
        ListenMetricsURLs is the list of URLs to listen on that will respond to both the /metrics and /health endpoints
    logLevel : int, default is Undefined, optional
        LogLevel allows the klog library verbose log level to be set for etcd-manager. The default is 6. https://github.com/google/glog#verbose-logging
    """


    backupInterval?: str

    backupRetentionDays?: int

    discoveryPollInterval?: str

    env?: [KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0ManagerEnvItems0]

    image?: str

    listenMetricsURLs?: [str]

    logLevel?: int


schema KopsK8sIoV1alpha2ClusterSpecEtcdClustersItems0ManagerEnvItems0:
    """
    EnvVar represents an environment variable present in a Container.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the environment variable. Must be a C_IDENTIFIER.
    value : str, default is Undefined, optional
        Variable references $(VAR_NAME) are expanded using the previous defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. The $(VAR_NAME) syntax can be escaped with a double $$, ie: $$(VAR_NAME). Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
    """


    name: str

    value?: str


schema KopsK8sIoV1alpha2ClusterSpecExternalDNS:
    """
    ExternalDNSConfig are options of the dns-controller

    Attributes
    ----------
    disable : bool, default is Undefined, optional
        Disable indicates we do not wish to run the dns-controller addon
    provider : str, default is Undefined, optional
        Provider determines which implementation of ExternalDNS to use. 'dns-controller' will use kOps DNS Controller. 'external-dns' will use kubernetes-sigs/external-dns.
    watchIngress : bool, default is Undefined, optional
        WatchIngress indicates you want the dns-controller to watch and create dns entries for ingress resources. Default: true if provider is 'external-dns', false otherwise.
    watchNamespace : str, default is Undefined, optional
        WatchNamespace is namespace to watch, defaults to all (use to control whom can creates dns entries)
    """


    disable?: bool

    provider?: str

    watchIngress?: bool

    watchNamespace?: str


schema KopsK8sIoV1alpha2ClusterSpecFileAssetsItems0:
    """
    FileAssetSpec defines the structure for a file asset

    Attributes
    ----------
    content : str, default is Undefined, optional
        Content is the contents of the file
    isBase64 : bool, default is Undefined, optional
        IsBase64 indicates the contents is base64 encoded
    mode : str, default is Undefined, optional
        Mode is this file's mode and permission bits
    name : str, default is Undefined, optional
        Name is a shortened reference to the asset
    path : str, default is Undefined, optional
        Path is the location this file should reside
    roles : [str], default is Undefined, optional
        Roles is a list of roles the file asset should be applied, defaults to all
    """


    content?: str

    isBase64?: bool

    mode?: str

    name?: str

    path?: str

    roles?: [str]


schema KopsK8sIoV1alpha2ClusterSpecGossipConfig:
    """
    GossipConfig for the cluster assuming the use of gossip DNS

    Attributes
    ----------
    listen : str, default is Undefined, optional
        listen
    $protocol : str, default is Undefined, optional
        protocol
    secondary : KopsK8sIoV1alpha2ClusterSpecGossipConfigSecondary, default is Undefined, optional
        secondary
    secret : str, default is Undefined, optional
        secret
    """


    listen?: str

    $protocol?: str

    secondary?: KopsK8sIoV1alpha2ClusterSpecGossipConfigSecondary

    secret?: str


schema KopsK8sIoV1alpha2ClusterSpecGossipConfigSecondary:
    """
    kops k8s io v1alpha2 cluster spec gossip config secondary

    Attributes
    ----------
    listen : str, default is Undefined, optional
        listen
    $protocol : str, default is Undefined, optional
        protocol
    secret : str, default is Undefined, optional
        secret
    """


    listen?: str

    $protocol?: str

    secret?: str


schema KopsK8sIoV1alpha2ClusterSpecHooksItems0:
    """
    HookSpec is a definition hook

    Attributes
    ----------
    before : [str], default is Undefined, optional
        Before is a series of systemd units which this hook must run before
    disabled : bool, default is Undefined, optional
        Disabled indicates if you want the unit switched off
    execContainer : KopsK8sIoV1alpha2ClusterSpecHooksItems0ExecContainer, default is Undefined, optional
        exec container
    manifest : str, default is Undefined, optional
        Manifest is a raw systemd unit file
    name : str, default is Undefined, optional
        Name is an optional name for the hook, otherwise the name is kops-hook-<index>
    requires : [str], default is Undefined, optional
        Requires is a series of systemd units the action requires
    roles : [str], default is Undefined, optional
        Roles is an optional list of roles the hook should be rolled out to, defaults to all
    useRawManifest : bool, default is Undefined, optional
        UseRawManifest indicates that the contents of Manifest should be used as the contents of the systemd unit, unmodified. Before and Requires are ignored when used together with this value (and validation shouldn't allow them to be set)
    """


    before?: [str]

    disabled?: bool

    execContainer?: KopsK8sIoV1alpha2ClusterSpecHooksItems0ExecContainer

    manifest?: str

    name?: str

    requires?: [str]

    roles?: [str]

    useRawManifest?: bool


schema KopsK8sIoV1alpha2ClusterSpecHooksItems0ExecContainer:
    """
    ExecContainer is the image itself

    Attributes
    ----------
    command : [str], default is Undefined, optional
        Command is the command supplied to the above image
    environment : {str:str}, default is Undefined, optional
        Environment is a map of environment variables added to the hook
    image : str, default is Undefined, optional
        Image is the docker image
    """


    command?: [str]

    environment?: {str:str}

    image?: str


schema KopsK8sIoV1alpha2ClusterSpecIam:
    """
    IAM field adds control over the IAM security policies applied to resources

    Attributes
    ----------
    allowContainerRegistry : bool, default is Undefined, optional
        allow container registry
    legacy : bool, default is Undefined, required
        legacy
    permissionsBoundary : str, default is Undefined, optional
        permissions boundary
    serviceAccountExternalPermissions : [KopsK8sIoV1alpha2ClusterSpecIamServiceAccountExternalPermissionsItems0], default is Undefined, optional
        ServiceAccountExternalPermissions defines the relationship between Kubernetes ServiceAccounts and permissions with external resources.
    useServiceAccountExternalPermissions : bool, default is Undefined, optional
        UseServiceAccountExternalPermissions determines if managed ServiceAccounts will use external permissions directly. If this is set to false, ServiceAccounts will assume external permissions from the instances they run on.
    """


    allowContainerRegistry?: bool

    legacy: bool

    permissionsBoundary?: str

    serviceAccountExternalPermissions?: [KopsK8sIoV1alpha2ClusterSpecIamServiceAccountExternalPermissionsItems0]

    useServiceAccountExternalPermissions?: bool


schema KopsK8sIoV1alpha2ClusterSpecIamServiceAccountExternalPermissionsItems0:
    """
    ServiceAccountExternalPermissions grants a ServiceAccount permissions to external resources.

    Attributes
    ----------
    aws : KopsK8sIoV1alpha2ClusterSpecIamServiceAccountExternalPermissionsItems0Aws, default is Undefined, optional
        aws
    name : str, default is Undefined, required
        Name is the name of the Kubernetes ServiceAccount.
    namespace : str, default is Undefined, required
        Namespace is the namespace of the Kubernetes ServiceAccount.
    """


    aws?: KopsK8sIoV1alpha2ClusterSpecIamServiceAccountExternalPermissionsItems0Aws

    name: str

    namespace: str


schema KopsK8sIoV1alpha2ClusterSpecIamServiceAccountExternalPermissionsItems0Aws:
    """
    AWS grants permissions to AWS resources.

    Attributes
    ----------
    inlinePolicy : str, default is Undefined, optional
        InlinePolicy is an IAM Policy that will be attached inline to the IAM Role.
    policyARNs : [str], default is Undefined, optional
        PolicyARNs is a list of existing IAM Policies.
    """


    inlinePolicy?: str

    policyARNs?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKarpenter:
    """
    Karpenter defines the Karpenter configuration.

    Attributes
    ----------
    cpuRequest : int | str, default is Undefined, optional
        cpu request
    enabled : bool, default is Undefined, optional
        enabled
    image : str, default is Undefined, optional
        image
    logEncoding : str, default is Undefined, optional
        log encoding
    logLevel : str, default is Undefined, optional
        log level
    memoryLimit : int | str, default is Undefined, optional
        memory limit
    memoryRequest : int | str, default is Undefined, optional
        memory request
    """


    cpuRequest?: int | str

    enabled?: bool

    image?: str

    logEncoding?: str

    logLevel?: str

    memoryLimit?: int | str

    memoryRequest?: int | str


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeAPIServer:
    """
    KubeAPIServerConfig defines the configuration for the kube api

    Attributes
    ----------
    address : str, default is Undefined, optional
        Address is the binding address for the kube api: Deprecated - use insecure-bind-address and bind-address
    admissionControl : [str], default is Undefined, optional
        AdmissionControl is a list of admission controllers to use: Deprecated - use enable-admission-plugins instead
    admissionControlConfigFile : str, default is Undefined, optional
        AdmissionControlConfigFile is the location of the admission-control-config-file
    advertiseAddress : str, default is Undefined, optional
        AdvertiseAddress is the IP address on which to advertise the apiserver to members of the cluster.
    allowPrivileged : bool, default is Undefined, optional
        AllowPrivileged indicates if we can run privileged containers
    anonymousAuth : bool, default is Undefined, optional
        AnonymousAuth indicates if anonymous authentication is permitted
    apiAudiences : [str], default is Undefined, optional
        Identifiers of the API. The service account token authenticator will validate that tokens used against the API are bound to at least one of these audiences. If the --service-account-issuer flag is configured and this flag is not, this field defaults to a single element list containing the issuer URL.
    apiServerCount : int, default is Undefined, optional
        APIServerCount is the number of api servers
    appendAdmissionPlugins : [str], default is Undefined, optional
        AppendAdmissionPlugins appends list of enabled admission plugins
    auditDynamicConfiguration : bool, default is Undefined, optional
        AuditDynamicConfiguration enables dynamic audit configuration via AuditSinks
    auditLogFormat : str, default is Undefined, optional
        AuditLogFormat flag specifies the format type for audit log files.
    auditLogMaxAge : int, default is Undefined, optional
        The maximum number of days to retain old audit log files based on the timestamp encoded in their filename.
    auditLogMaxBackups : int, default is Undefined, optional
        The maximum number of old audit log files to retain.
    auditLogMaxSize : int, default is Undefined, optional
        The maximum size in megabytes of the audit log file before it gets rotated. Defaults to 100MB.
    auditLogPath : str, default is Undefined, optional
        If set, all requests coming to the apiserver will be logged to this file.
    auditPolicyFile : str, default is Undefined, optional
        AuditPolicyFile is the full path to a advanced audit configuration file e.g. /srv/kubernetes/audit.conf
    auditWebhookBatchBufferSize : int, default is Undefined, optional
        AuditWebhookBatchBufferSize is The size of the buffer to store events before batching and writing. Only used in batch mode. (default 10000)
    auditWebhookBatchMaxSize : int, default is Undefined, optional
        AuditWebhookBatchMaxSize is The maximum size of a batch. Only used in batch mode. (default 400)
    auditWebhookBatchMaxWait : str, default is Undefined, optional
        AuditWebhookBatchMaxWait is The amount of time to wait before force writing the batch that hadn't reached the max size. Only used in batch mode. (default 30s)
    auditWebhookBatchThrottleBurst : int, default is Undefined, optional
        AuditWebhookBatchThrottleBurst is Maximum number of requests sent at the same moment if ThrottleQPS was not utilized before. Only used in batch mode. (default 15)
    auditWebhookBatchThrottleEnable : bool, default is Undefined, optional
        AuditWebhookBatchThrottleEnable is Whether batching throttling is enabled. Only used in batch mode. (default true)
    auditWebhookBatchThrottleQps : int | str, default is Undefined, optional
        AuditWebhookBatchThrottleQps is Maximum average number of batches per second. Only used in batch mode. (default 10)
    auditWebhookConfigFile : str, default is Undefined, optional
        AuditWebhookConfigFile is Path to a kubeconfig formatted file that defines the audit webhook configuration. Requires the 'AdvancedAuditing' feature gate.
    auditWebhookInitialBackoff : str, default is Undefined, optional
        AuditWebhookInitialBackoff is The amount of time to wait before retrying the first failed request. (default 10s)
    auditWebhookMode : str, default is Undefined, optional
        AuditWebhookMode is Strategy for sending audit events. Blocking indicates sending events should block server responses. Batch causes the backend to buffer and write events asynchronously. Known modes are batch,blocking. (default "batch")
    authenticationTokenWebhookCacheTtl : str, default is Undefined, optional
        The duration to cache responses from the webhook token authenticator. Default is 2m. (default 2m0s)
    authenticationTokenWebhookConfigFile : str, default is Undefined, optional
        File with webhook configuration for token authentication in kubeconfig format. The API server will query the remote service to determine authentication for bearer tokens.
    authorizationMode : str, default is Undefined, optional
        AuthorizationMode is the authorization mode the kubeapi is running in
    authorizationRbacSuperUser : str, default is Undefined, optional
        AuthorizationRBACSuperUser is the name of the superuser for default rbac
    authorizationWebhookCacheAuthorizedTtl : str, default is Undefined, optional
        The duration to cache authorized responses from the webhook token authorizer. Default is 5m. (default 5m0s)
    authorizationWebhookCacheUnauthorizedTtl : str, default is Undefined, optional
        The duration to cache authorized responses from the webhook token authorizer. Default is 30s. (default 30s)
    authorizationWebhookConfigFile : str, default is Undefined, optional
        File with webhook configuration for authorization in kubeconfig format. The API server will query the remote service to determine whether to authorize the request.
    basicAuthFile : str, default is Undefined, optional
        TODO: Remove unused BasicAuthFile
    bindAddress : str, default is Undefined, optional
        BindAddress is the binding address for the secure kubernetes API
    clientCAFile : str, default is Undefined, optional
        ClientCAFile is the file used by apisever that contains the client CA
    cloudProvider : str, default is Undefined, optional
        CloudProvider is the name of the cloudProvider we are using, aws, gce etcd
    corsAllowedOrigins : [str], default is Undefined, optional
        CorsAllowedOrigins is a list of origins for CORS. An allowed origin can be a regular expression to support subdomain matching. If this list is empty CORS will not be enabled.
    cpuLimit : int | str, default is Undefined, optional
        CPULimit, cpu limit compute resource for api server e.g. "500m"
    cpuRequest : int | str, default is Undefined, optional
        CPURequest, cpu request compute resource for api server. Defaults to "150m"
    defaultNotReadyTolerationSeconds : int, default is Undefined, optional
        DefaultNotReadyTolerationSeconds
    defaultUnreachableTolerationSeconds : int, default is Undefined, optional
        DefaultUnreachableTolerationSeconds
    disableAdmissionPlugins : [str], default is Undefined, optional
        DisableAdmissionPlugins is a list of disabled admission plugins
    disableBasicAuth : bool, default is Undefined, optional
        DisableBasicAuth removes the --basic-auth-file flag
    enableAdmissionPlugins : [str], default is Undefined, optional
        EnableAdmissionPlugins is a list of enabled admission plugins
    enableAggregatorRouting : bool, default is Undefined, optional
        EnableAggregatorRouting enables aggregator routing requests to endpoints IP rather than cluster IP
    enableBootstrapTokenAuth : bool, default is Undefined, optional
        EnableBootstrapAuthToken enables 'bootstrap.kubernetes.io/token' in the 'kube-system' namespace to be used for TLS bootstrapping authentication
    enableContentionProfiling : bool, default is Undefined, optional
        EnableContentionProfiling enables block profiling, if profiling is enabled
    enableProfiling : bool, default is Undefined, optional
        EnableProfiling enables profiling via web interface host:port/debug/pprof/
    encryptionProviderConfig : str, default is Undefined, optional
        EncryptionProviderConfig enables encryption at rest for secrets.
    etcdCaFile : str, default is Undefined, optional
        EtcdCAFile is the path to a ca certificate
    etcdCertFile : str, default is Undefined, optional
        EtcdCertFile is the path to a certificate
    etcdKeyFile : str, default is Undefined, optional
        EtcdKeyFile is the path to a private key
    etcdQuorumRead : bool, default is Undefined, optional
        EtcdQuorumRead configures the etcd-quorum-read flag, which forces consistent reads from etcd
    etcdServers : [str], default is Undefined, optional
        EtcdServers is a list of the etcd service to connect
    etcdServersOverrides : [str], default is Undefined, optional
        EtcdServersOverrides is per-resource etcd servers overrides, comma separated. The individual override format: group/resource#servers, where servers are http://ip:port, semicolon separated
    eventTTL : str, default is Undefined, optional
        Amount of time to retain Kubernetes events
    experimentalEncryptionProviderConfig : str, default is Undefined, optional
        ExperimentalEncryptionProviderConfig enables encryption at rest for secrets.
    featureGates : {str:str}, default is Undefined, optional
        FeatureGates is set of key=value pairs that describe feature gates for alpha/experimental features.
    http2MaxStreamsPerConnection : int, default is Undefined, optional
        HTTP2MaxStreamsPerConnection sets the limit that the server gives to clients for the maximum number of streams in an HTTP/2 connection. Zero means to use golang's default.
    image : str, default is Undefined, optional
        Image is the container image used.
    insecureBindAddress : str, default is Undefined, optional
        InsecureBindAddress is the binding address for the InsecurePort for the insecure kubernetes API
    insecurePort : int, default is Undefined, optional
        InsecurePort is the port the insecure api runs
    kubeletCertificateAuthority : str, default is Undefined, optional
        KubeletCertificateAuthority is the path of a certificate authority for secure communication between api and kubelet.
    kubeletClientCertificate : str, default is Undefined, optional
        KubeletClientCertificate is the path of a certificate for secure communication between api and kubelet
    kubeletClientKey : str, default is Undefined, optional
        KubeletClientKey is the path of a private to secure communication between api and kubelet
    kubeletPreferredAddressTypes : [str], default is Undefined, optional
        KubeletPreferredAddressTypes is a list of the preferred NodeAddressTypes to use for kubelet connections
    logFormat : str, default is Undefined, optional
        LogFormat is the logging format of the api. Supported values: text, json. Default: text
    logLevel : int, default is Undefined, optional
        LogLevel is the logging level of the api
    maxMutatingRequestsInflight : int, default is Undefined, optional
        MaxMutatingRequestsInflight The maximum number of mutating requests in flight at a given time. Defaults to 200
    maxRequestsInflight : int, default is Undefined, optional
        MaxRequestsInflight The maximum number of non-mutating requests in flight at a given time.
    memoryLimit : int | str, default is Undefined, optional
        MemoryLimit, memory limit compute resource for api server e.g. "30Mi"
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest, memory request compute resource for api server e.g. "30Mi"
    minRequestTimeout : int, default is Undefined, optional
        MinRequestTimeout configures the minimum number of seconds a handler must keep a request open before timing it out. Currently only honored by the watch request handler
    oidcCAFile : str, default is Undefined, optional
        OIDCCAFile if set, the OpenID server's certificate will be verified by one of the authorities in the oidc-ca-file
    oidcClientID : str, default is Undefined, optional
        OIDCClientID is the client ID for the OpenID Connect client, must be set if oidc-issuer-url is set.
    oidcGroupsClaim : str, default is Undefined, optional
        OIDCGroupsClaim if provided, the name of a custom OpenID Connect claim for specifying user groups. The claim value is expected to be a string or array of strings.
    oidcGroupsPrefix : str, default is Undefined, optional
        OIDCGroupsPrefix is the prefix prepended to group claims to prevent clashes with existing names (such as 'system:' groups)
    oidcIssuerURL : str, default is Undefined, optional
        OIDCIssuerURL is the URL of the OpenID issuer, only HTTPS scheme will be accepted. If set, it will be used to verify the OIDC JSON Web Token (JWT).
    oidcRequiredClaim : [str], default is Undefined, optional
        A key=value pair that describes a required claim in the ID Token. If set, the claim is verified to be present in the ID Token with a matching value. Repeat this flag to specify multiple claims.
    oidcUsernameClaim : str, default is Undefined, optional
        OIDCUsernameClaim is the OpenID claim to use as the user name. Note that claims other than the default ('sub') is not guaranteed to be unique and immutable.
    oidcUsernamePrefix : str, default is Undefined, optional
        OIDCUsernamePrefix is the prefix prepended to username claims to prevent clashes with existing names (such as 'system:' users).
    proxyClientCertFile : str, default is Undefined, optional
        The apiserver's client certificate used for outbound requests.
    proxyClientKeyFile : str, default is Undefined, optional
        The apiserver's client key used for outbound requests.
    requestTimeout : str, default is Undefined, optional
        RequestTimeout configures the duration a handler must keep a request open before timing it out. (default 1m0s)
    requestheaderAllowedNames : [str], default is Undefined, optional
        List of client certificate common names to allow to provide usernames in headers specified by --requestheader-username-headers. If empty, any client certificate validated by the authorities in --requestheader-client-ca-file is allowed.
    requestheaderClientCAFile : str, default is Undefined, optional
        Root certificate bundle to use to verify client certificates on incoming requests before trusting usernames in headers specified by --requestheader-username-headers
    requestheaderExtraHeaderPrefixes : [str], default is Undefined, optional
        List of request header prefixes to inspect. X-Remote-Extra- is suggested.
    requestheaderGroupHeaders : [str], default is Undefined, optional
        List of request headers to inspect for groups. X-Remote-Group is suggested.
    requestheaderUsernameHeaders : [str], default is Undefined, optional
        List of request headers to inspect for usernames. X-Remote-User is common.
    runtimeConfig : {str:str}, default is Undefined, optional
        RuntimeConfig is a series of keys/values are parsed into the `--runtime-config` parameters
    securePort : int, default is Undefined, optional
        SecurePort is the port the kube runs on
    serviceAccountIssuer : str, default is Undefined, optional
        Identifier of the service account token issuer. The issuer will assert this identifier in "iss" claim of issued tokens. This value is a string or URI.
    serviceAccountJWKSURI : str, default is Undefined, optional
        ServiceAccountJWKSURI overrides the path for the jwks document; this is useful when we are republishing the service account discovery information elsewhere.
    serviceAccountKeyFile : [str], default is Undefined, optional
        File containing PEM-encoded x509 RSA or ECDSA private or public keys, used to verify ServiceAccount tokens. The specified file can contain multiple keys, and the flag can be specified multiple times with different files. If unspecified, --tls-private-key-file is used.
    serviceAccountSigningKeyFile : str, default is Undefined, optional
        Path to the file that contains the current private key of the service account token issuer. The issuer will sign issued ID tokens with this private key. (Requires the 'TokenRequest' feature gate.)
    serviceClusterIPRange : str, default is Undefined, optional
        ServiceClusterIPRange is the service address range
    serviceNodePortRange : str, default is Undefined, optional
        Passed as --service-node-port-range to kube-apiserver. Expects 'startPort-endPort' format e.g. 30000-33000
    storageBackend : str, default is Undefined, optional
        StorageBackend is the backend storage
    targetRamMb : int, default is Undefined, optional
        Memory limit for apiserver in MB (used to configure sizes of caches, etc.)
    tlsCertFile : str, default is Undefined, optional
        TODO: Remove unused TLSCertFile
    tlsCipherSuites : [str], default is Undefined, optional
        TLSCipherSuites indicates the allowed TLS cipher suite
    tlsMinVersion : str, default is Undefined, optional
        TLSMinVersion indicates the minimum TLS version allowed
    tlsPrivateKeyFile : str, default is Undefined, optional
        TODO: Remove unused TLSPrivateKeyFile
    tokenAuthFile : str, default is Undefined, optional
        TODO: Remove unused TokenAuthFile
    """


    address?: str

    admissionControl?: [str]

    admissionControlConfigFile?: str

    advertiseAddress?: str

    allowPrivileged?: bool

    anonymousAuth?: bool

    apiAudiences?: [str]

    apiServerCount?: int

    appendAdmissionPlugins?: [str]

    auditDynamicConfiguration?: bool

    auditLogFormat?: str

    auditLogMaxAge?: int

    auditLogMaxBackups?: int

    auditLogMaxSize?: int

    auditLogPath?: str

    auditPolicyFile?: str

    auditWebhookBatchBufferSize?: int

    auditWebhookBatchMaxSize?: int

    auditWebhookBatchMaxWait?: str

    auditWebhookBatchThrottleBurst?: int

    auditWebhookBatchThrottleEnable?: bool

    auditWebhookBatchThrottleQps?: int | str

    auditWebhookConfigFile?: str

    auditWebhookInitialBackoff?: str

    auditWebhookMode?: str

    authenticationTokenWebhookCacheTtl?: str

    authenticationTokenWebhookConfigFile?: str

    authorizationMode?: str

    authorizationRbacSuperUser?: str

    authorizationWebhookCacheAuthorizedTtl?: str

    authorizationWebhookCacheUnauthorizedTtl?: str

    authorizationWebhookConfigFile?: str

    basicAuthFile?: str

    bindAddress?: str

    clientCAFile?: str

    cloudProvider?: str

    corsAllowedOrigins?: [str]

    cpuLimit?: int | str

    cpuRequest?: int | str

    defaultNotReadyTolerationSeconds?: int

    defaultUnreachableTolerationSeconds?: int

    disableAdmissionPlugins?: [str]

    disableBasicAuth?: bool

    enableAdmissionPlugins?: [str]

    enableAggregatorRouting?: bool

    enableBootstrapTokenAuth?: bool

    enableContentionProfiling?: bool

    enableProfiling?: bool

    encryptionProviderConfig?: str

    etcdCaFile?: str

    etcdCertFile?: str

    etcdKeyFile?: str

    etcdQuorumRead?: bool

    etcdServers?: [str]

    etcdServersOverrides?: [str]

    eventTTL?: str

    experimentalEncryptionProviderConfig?: str

    featureGates?: {str:str}

    http2MaxStreamsPerConnection?: int

    image?: str

    insecureBindAddress?: str

    insecurePort?: int

    kubeletCertificateAuthority?: str

    kubeletClientCertificate?: str

    kubeletClientKey?: str

    kubeletPreferredAddressTypes?: [str]

    logFormat?: str

    logLevel?: int

    maxMutatingRequestsInflight?: int

    maxRequestsInflight?: int

    memoryLimit?: int | str

    memoryRequest?: int | str

    minRequestTimeout?: int

    oidcCAFile?: str

    oidcClientID?: str

    oidcGroupsClaim?: str

    oidcGroupsPrefix?: str

    oidcIssuerURL?: str

    oidcRequiredClaim?: [str]

    oidcUsernameClaim?: str

    oidcUsernamePrefix?: str

    proxyClientCertFile?: str

    proxyClientKeyFile?: str

    requestTimeout?: str

    requestheaderAllowedNames?: [str]

    requestheaderClientCAFile?: str

    requestheaderExtraHeaderPrefixes?: [str]

    requestheaderGroupHeaders?: [str]

    requestheaderUsernameHeaders?: [str]

    runtimeConfig?: {str:str}

    securePort?: int

    serviceAccountIssuer?: str

    serviceAccountJWKSURI?: str

    serviceAccountKeyFile?: [str]

    serviceAccountSigningKeyFile?: str

    serviceClusterIPRange?: str

    serviceNodePortRange?: str

    storageBackend?: str

    targetRamMb?: int

    tlsCertFile?: str

    tlsCipherSuites?: [str]

    tlsMinVersion?: str

    tlsPrivateKeyFile?: str

    tokenAuthFile?: str


    check:
        matchPattern(auditWebhookBatchThrottleQps, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(cpuLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeControllerManager:
    """
    KubeControllerManagerConfig is the configuration for the controller

    Attributes
    ----------
    ClusterSigningDuration : str, default is Undefined, optional
        ClusterSigningDuration is the max length of duration that the signed certificates will be given. (default 365*24h)
    allocateNodeCIDRs : bool, default is Undefined, optional
        AllocateNodeCIDRs enables CIDRs for Pods to be allocated and, if ConfigureCloudRoutes is true, to be set on the cloud provider.
    attachDetachReconcileSyncPeriod : str, default is Undefined, optional
        ReconcilerSyncLoopPeriod is the amount of time the reconciler sync states loop wait between successive executions. Is set to 1 min by kops by default
    authenticationKubeconfig : str, default is Undefined, optional
        AuthenticationKubeconfig is the path to an Authentication Kubeconfig
    authorizationAlwaysAllowPaths : [str], default is Undefined, optional
        AuthorizationAlwaysAllowPaths is the list of HTTP paths to skip during authorization
    authorizationKubeconfig : str, default is Undefined, optional
        AuthorizationKubeconfig is the path to an Authorization Kubeconfig
    cidrAllocatorType : str, default is Undefined, optional
        CIDRAllocatorType specifies the type of CIDR allocator to use.
    cloudProvider : str, default is Undefined, optional
        CloudProvider is the provider for cloud services.
    clusterCIDR : str, default is Undefined, optional
        ClusterCIDR is CIDR Range for Pods in cluster.
    clusterName : str, default is Undefined, optional
        ClusterName is the instance prefix for the cluster.
    concurrentDeploymentSyncs : int, default is Undefined, optional
        The number of deployment objects that are allowed to sync concurrently.
    concurrentEndpointSyncs : int, default is Undefined, optional
        The number of endpoint objects that are allowed to sync concurrently.
    concurrentNamespaceSyncs : int, default is Undefined, optional
        The number of namespace objects that are allowed to sync concurrently.
    concurrentRcSyncs : int, default is Undefined, optional
        The number of replicationcontroller objects that are allowed to sync concurrently. This only works on kubernetes >= 1.14
    concurrentReplicasetSyncs : int, default is Undefined, optional
        The number of replicaset objects that are allowed to sync concurrently.
    concurrentResourceQuotaSyncs : int, default is Undefined, optional
        The number of resourcequota objects that are allowed to sync concurrently.
    concurrentServiceSyncs : int, default is Undefined, optional
        The number of service objects that are allowed to sync concurrently.
    concurrentServiceaccountTokenSyncs : int, default is Undefined, optional
        The number of serviceaccount objects that are allowed to sync concurrently to create tokens.
    configureCloudRoutes : bool, default is Undefined, optional
        ConfigureCloudRoutes enables CIDRs allocated with to be configured on the cloud provider.
    controllers : [str], default is Undefined, optional
        Controllers is a list of controllers to enable on the controller-manager
    disableAttachDetachReconcileSync : bool, default is Undefined, optional
        DisableAttachDetachReconcileSync disables the reconcile sync loop in the attach-detach controller. This can cause volumes to become mismatched with pods
    enableContentionProfiling : bool, default is Undefined, optional
        EnableContentionProfiling enables block profiling, if profiling is enabled
    enableLeaderMigration : bool, default is Undefined, optional
        EnableLeaderMigration enables controller leader migration.
    enableProfiling : bool, default is Undefined, optional
        EnableProfiling enables profiling via web interface host:port/debug/pprof/
    endpointSliceUpdatesBatchPeriod : str, default is Undefined, optional
        The length of endpoint slice updates batching period. Processing of pod changes will be delayed by this duration to join them with potential upcoming updates and reduce the overall number of endpoints updates. Larger number = higher endpoint programming latency, but lower number of endpoints revision generated.
    endpointUpdatesBatchPeriod : str, default is Undefined, optional
        The length of endpoint updates batching period. Processing of pod changes will be delayed by this duration to join them with potential upcoming updates and reduce the overall number of endpoints updates. Larger number = higher endpoint programming latency, but lower number of endpoints revision generated
    experimentalClusterSigningDuration : str, default is Undefined, optional
        ExperimentalClusterSigningDuration is the max length of duration that the signed certificates will be given. (default 365*24h) Deprecated - use cluster-signing-duration instead
    externalCloudVolumePlugin : str, default is Undefined, optional
        ExternalCloudVolumePlugin is a fallback mechanism that allows a legacy, in-tree cloudprovider to be used for volume plugins even when an external cloud controller manager is being used.  This can be used instead of installing CSI.  The value should be the same as is used for the --cloud-provider flag, i.e. "aws".
    featureGates : {str:str}, default is Undefined, optional
        FeatureGates is set of key=value pairs that describe feature gates for alpha/experimental features.
    horizontalPodAutoscalerCpuInitializationPeriod : str, default is Undefined, optional
        HorizontalPodAutoscalerCPUInitializationPeriod is the period after pod start when CPU samples might be skipped. (default 5m)
    horizontalPodAutoscalerDownscaleDelay : str, default is Undefined, optional
        HorizontalPodAutoscalerDownscaleDelay is a duration that specifies how long the autoscaler has to wait before another downscale operation can be performed after the current one has completed.
    horizontalPodAutoscalerDownscaleStabilization : str, default is Undefined, optional
        HorizontalPodAutoscalerDownscaleStabilization is the period for which autoscaler will look backwards and not scale down below any recommendation it made during that period.
    horizontalPodAutoscalerInitialReadinessDelay : str, default is Undefined, optional
        HorizontalPodAutoscalerInitialReadinessDelay is the period after pod start during which readiness changes will be treated as initial readiness. (default 30s)
    horizontalPodAutoscalerSyncPeriod : str, default is Undefined, optional
        HorizontalPodAutoscalerSyncPeriod is the amount of time between syncs During each period, the controller manager queries the resource utilization against the metrics specified in each HorizontalPodAutoscaler definition.
    horizontalPodAutoscalerTolerance : int | str, default is Undefined, optional
        HorizontalPodAutoscalerTolerance is the minimum change (from 1.0) in the desired-to-actual metrics ratio for the horizontal pod autoscaler to consider scaling.
    horizontalPodAutoscalerUpscaleDelay : str, default is Undefined, optional
        HorizontalPodAutoscalerUpscaleDelay is a duration that specifies how long the autoscaler has to wait before another upscale operation can be performed after the current one has completed.
    horizontalPodAutoscalerUseRestClients : bool, default is Undefined, optional
        HorizontalPodAutoscalerUseRestClients determines if the new-style clients should be used if support for custom metrics is enabled.
    image : str, default is Undefined, optional
        Image is the container image to use.
    kubeAPIBurst : int, default is Undefined, optional
        KubeAPIBurst Burst to use while talking with kubernetes apiserver. (default 30)
    kubeAPIQPS : int | str, default is Undefined, optional
        KubeAPIQPS QPS to use while talking with kubernetes apiserver. (default 20)
    leaderElection : KopsK8sIoV1alpha2ClusterSpecKubeControllerManagerLeaderElection, default is Undefined, optional
        leader election
    logFormat : str, default is Undefined, optional
        LogFormat is the logging format of the controler manager. Supported values: text, json. Default: text
    logLevel : int, default is Undefined, optional
        LogLevel is the defined logLevel
    master : str, default is Undefined, optional
        Master is the url for the kube api master
    minResyncPeriod : str, default is Undefined, optional
        MinResyncPeriod indicates the resync period in reflectors. The resync period will be random between MinResyncPeriod and 2*MinResyncPeriod. (default 12h0m0s)
    nodeCIDRMaskSize : int, default is Undefined, optional
        NodeCIDRMaskSize set the size for the mask of the nodes.
    nodeMonitorGracePeriod : str, default is Undefined, optional
        NodeMonitorGracePeriod is the amount of time which we allow running Node to be unresponsive before marking it unhealthy. (default 40s) Must be N-1 times more than kubelet's nodeStatusUpdateFrequency, where N means number of retries allowed for kubelet to post node status.
    nodeMonitorPeriod : str, default is Undefined, optional
        NodeMonitorPeriod is the period for syncing NodeStatus in NodeController. (default 5s)
    podEvictionTimeout : str, default is Undefined, optional
        PodEvictionTimeout is the grace period for deleting pods on failed nodes. (default 5m0s)
    rootCAFile : str, default is Undefined, optional
        rootCAFile is the root certificate authority will be included in service account's token secret. This must be a valid PEM-encoded CA bundle.
    serviceAccountPrivateKeyFile : str, default is Undefined, optional
        ServiceAccountPrivateKeyFile is the location of the private key for service account token signing.
    terminatedPodGCThreshold : int, default is Undefined, optional
        TerminatedPodGCThreshold is the number of terminated pods that can exist before the terminated pod garbage collector starts deleting terminated pods. If <= 0, the terminated pod garbage collector is disabled.
    tlsCertFile : str, default is Undefined, optional
        TLSCertFile is the file containing the TLS server certificate.
    tlsCipherSuites : [str], default is Undefined, optional
        TLSCipherSuites indicates the allowed TLS cipher suite
    tlsMinVersion : str, default is Undefined, optional
        TLSMinVersion indicates the minimum TLS version allowed
    tlsPrivateKeyFile : str, default is Undefined, optional
        TLSPrivateKeyFile is the file containing the private key for the TLS server certificate.
    useServiceAccountCredentials : bool, default is Undefined, optional
        UseServiceAccountCredentials controls whether we use individual service account credentials for each controller.
    """


    ClusterSigningDuration?: str

    allocateNodeCIDRs?: bool

    attachDetachReconcileSyncPeriod?: str

    authenticationKubeconfig?: str

    authorizationAlwaysAllowPaths?: [str]

    authorizationKubeconfig?: str

    cidrAllocatorType?: str

    cloudProvider?: str

    clusterCIDR?: str

    clusterName?: str

    concurrentDeploymentSyncs?: int

    concurrentEndpointSyncs?: int

    concurrentNamespaceSyncs?: int

    concurrentRcSyncs?: int

    concurrentReplicasetSyncs?: int

    concurrentResourceQuotaSyncs?: int

    concurrentServiceSyncs?: int

    concurrentServiceaccountTokenSyncs?: int

    configureCloudRoutes?: bool

    controllers?: [str]

    disableAttachDetachReconcileSync?: bool

    enableContentionProfiling?: bool

    enableLeaderMigration?: bool

    enableProfiling?: bool

    endpointSliceUpdatesBatchPeriod?: str

    endpointUpdatesBatchPeriod?: str

    experimentalClusterSigningDuration?: str

    externalCloudVolumePlugin?: str

    featureGates?: {str:str}

    horizontalPodAutoscalerCpuInitializationPeriod?: str

    horizontalPodAutoscalerDownscaleDelay?: str

    horizontalPodAutoscalerDownscaleStabilization?: str

    horizontalPodAutoscalerInitialReadinessDelay?: str

    horizontalPodAutoscalerSyncPeriod?: str

    horizontalPodAutoscalerTolerance?: int | str

    horizontalPodAutoscalerUpscaleDelay?: str

    horizontalPodAutoscalerUseRestClients?: bool

    image?: str

    kubeAPIBurst?: int

    kubeAPIQPS?: int | str

    leaderElection?: KopsK8sIoV1alpha2ClusterSpecKubeControllerManagerLeaderElection

    logFormat?: str

    logLevel?: int

    master?: str

    minResyncPeriod?: str

    nodeCIDRMaskSize?: int

    nodeMonitorGracePeriod?: str

    nodeMonitorPeriod?: str

    podEvictionTimeout?: str

    rootCAFile?: str

    serviceAccountPrivateKeyFile?: str

    terminatedPodGCThreshold?: int

    tlsCertFile?: str

    tlsCipherSuites?: [str]

    tlsMinVersion?: str

    tlsPrivateKeyFile?: str

    useServiceAccountCredentials?: bool


    check:
        matchPattern(horizontalPodAutoscalerTolerance, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(kubeAPIQPS, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeControllerManagerLeaderElection:
    """
    LeaderElection defines the configuration of leader election client.

    Attributes
    ----------
    leaderElect : bool, default is Undefined, optional
        leaderElect enables a leader election client to gain leadership before executing the main loop. Enable this when running replicated components for high availability.
    leaderElectLeaseDuration : str, default is Undefined, optional
        leaderElectLeaseDuration is the length in time non-leader candidates will wait after observing a leadership renewal until attempting to acquire leadership of a led but unrenewed leader slot. This is effectively the maximum duration that a leader can be stopped before it is replaced by another candidate
    leaderElectRenewDeadlineDuration : str, default is Undefined, optional
        LeaderElectRenewDeadlineDuration is the interval between attempts by the acting master to renew a leadership slot before it stops leading. This must be less than or equal to the lease duration.
    leaderElectResourceLock : str, default is Undefined, optional
        LeaderElectResourceLock is the type of resource object that is used for locking during leader election. Supported options are endpoints (default) and `configmaps`.
    leaderElectResourceName : str, default is Undefined, optional
        LeaderElectResourceName is the name of resource object that is used for locking during leader election.
    leaderElectResourceNamespace : str, default is Undefined, optional
        LeaderElectResourceNamespace is the namespace of resource object that is used for locking during leader election.
    leaderElectRetryPeriod : str, default is Undefined, optional
        LeaderElectRetryPeriod is The duration the clients should wait between attempting acquisition and renewal of a leadership. This is only applicable if leader election is enabled.
    """


    leaderElect?: bool

    leaderElectLeaseDuration?: str

    leaderElectRenewDeadlineDuration?: str

    leaderElectResourceLock?: str

    leaderElectResourceName?: str

    leaderElectResourceNamespace?: str

    leaderElectRetryPeriod?: str


schema KopsK8sIoV1alpha2ClusterSpecKubeDNS:
    """
    KubeDNSConfig defines the kube dns configuration

    Attributes
    ----------
    affinity : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinity, default is Undefined, optional
        affinity
    cacheMaxConcurrent : int, default is Undefined, optional
        CacheMaxConcurrent is the maximum number of concurrent queries for dnsmasq
    cacheMaxSize : int, default is Undefined, optional
        CacheMaxSize is the maximum entries to keep in dnsmasq
    coreDNSImage : str, default is Undefined, optional
        CoreDNSImage is used to override the default image used for CoreDNS
    cpaImage : str, default is Undefined, optional
        CPAImage is used to override the default image used for Cluster Proportional Autoscaler
    cpuRequest : int | str, default is Undefined, optional
        CPURequest specifies the cpu requests of each dns container in the cluster. Default 100m.
    domain : str, default is Undefined, optional
        Domain is the dns domain
    externalCoreFile : str, default is Undefined, optional
        ExternalCoreFile is used to provide a complete CoreDNS CoreFile by the user - ignores other provided flags which modify the CoreFile.
    image : str, default is Undefined, optional
        Image is unused.
    memoryLimit : int | str, default is Undefined, optional
        MemoryLimit specifies the memory limit of each dns container in the cluster. Default 170m.
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest specifies the memory requests of each dns container in the cluster. Default 70m.
    nodeLocalDNS : KopsK8sIoV1alpha2ClusterSpecKubeDNSNodeLocalDNS, default is Undefined, optional
        node local DNS
    provider : str, default is Undefined, optional
        Provider indicates whether CoreDNS or kube-dns will be the default service discovery.
    replicas : int, default is Undefined, optional
        Replicas is unused.
    serverIP : str, default is Undefined, optional
        ServerIP is the server ip
    stubDomains : {str:[str]}, default is Undefined, optional
        StubDomains redirects a domains to another DNS service
    tolerations : [KopsK8sIoV1alpha2ClusterSpecKubeDNSTolerationsItems0], default is Undefined, optional
        Tolerations	are tolerations to apply to the kube-dns deployment
    upstreamNameservers : [str], default is Undefined, optional
        UpstreamNameservers sets the upstream nameservers for queries not on the cluster domain
    """


    affinity?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinity

    cacheMaxConcurrent?: int

    cacheMaxSize?: int

    coreDNSImage?: str

    cpaImage?: str

    cpuRequest?: int | str

    domain?: str

    externalCoreFile?: str

    image?: str

    memoryLimit?: int | str

    memoryRequest?: int | str

    nodeLocalDNS?: KopsK8sIoV1alpha2ClusterSpecKubeDNSNodeLocalDNS

    provider?: str

    replicas?: int

    serverIP?: str

    stubDomains?: {str:[str]}

    tolerations?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSTolerationsItems0]

    upstreamNameservers?: [str]


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinity:
    """
    Affinity is the kube-dns affinity, uses the same syntax as kubectl's affinity

    Attributes
    ----------
    nodeAffinity : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinity, default is Undefined, optional
        node affinity
    podAffinity : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinity, default is Undefined, optional
        pod affinity
    podAntiAffinity : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinity, default is Undefined, optional
        pod anti affinity
    """


    nodeAffinity?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinity

    podAffinity?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinity

    podAntiAffinity?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinity


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinity:
    """
    Describes node affinity scheduling rules for the pod.

    Attributes
    ----------
    preferredDuringSchedulingIgnoredDuringExecution : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0], default is Undefined, optional
        The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node matches the corresponding matchExpressions; the node(s) with the highest sum are the most preferred.
    requiredDuringSchedulingIgnoredDuringExecution : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution, default is Undefined, optional
        required during scheduling ignored during execution
    """


    preferredDuringSchedulingIgnoredDuringExecution?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0]

    requiredDuringSchedulingIgnoredDuringExecution?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0:
    """
    An empty preferred scheduling term matches all objects with implicit weight 0 (i.e. it's a no-op). A null preferred scheduling term matches no objects (i.e. is also a no-op).

    Attributes
    ----------
    preference : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0Preference, default is Undefined, required
        preference
    weight : int, default is Undefined, required
        Weight associated with matching the corresponding nodeSelectorTerm, in the range 1-100.
    """


    preference: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0Preference

    weight: int


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0Preference:
    """
    A node selector term, associated with the corresponding weight.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PreferenceMatchExpressionsItems0], default is Undefined, optional
        A list of node selector requirements by node's labels.
    matchFields : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PreferenceMatchFieldsItems0], default is Undefined, optional
        A list of node selector requirements by node's fields.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PreferenceMatchExpressionsItems0]

    matchFields?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PreferenceMatchFieldsItems0]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PreferenceMatchExpressionsItems0:
    """
    A node selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        The label key that the selector applies to.
    operator : str, default is Undefined, required
        Represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
    values : [str], default is Undefined, optional
        An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PreferenceMatchFieldsItems0:
    """
    A node selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        The label key that the selector applies to.
    operator : str, default is Undefined, required
        Represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
    values : [str], default is Undefined, optional
        An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecution:
    """
    If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to an update), the system may or may not try to eventually evict the pod from its node.

    Attributes
    ----------
    nodeSelectorTerms : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0], default is Undefined, required
        Required. A list of node selector terms. The terms are ORed.
    """


    nodeSelectorTerms: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0:
    """
    A null or empty node selector term matches no objects. The requirements of them are ANDed. The TopologySelectorTerm type implements a subset of the NodeSelectorTerm.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0MatchExpressionsItems0], default is Undefined, optional
        A list of node selector requirements by node's labels.
    matchFields : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0MatchFieldsItems0], default is Undefined, optional
        A list of node selector requirements by node's fields.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0MatchExpressionsItems0]

    matchFields?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0MatchFieldsItems0]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0MatchExpressionsItems0:
    """
    A node selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        The label key that the selector applies to.
    operator : str, default is Undefined, required
        Represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
    values : [str], default is Undefined, optional
        An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityNodeAffinityRequiredDuringSchedulingIgnoredDuringExecutionNodeSelectorTermsItems0MatchFieldsItems0:
    """
    A node selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        The label key that the selector applies to.
    operator : str, default is Undefined, required
        Represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
    values : [str], default is Undefined, optional
        An array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. If the operator is Gt or Lt, the values array must have a single element, which will be interpreted as an integer. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinity:
    """
    Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s)).

    Attributes
    ----------
    preferredDuringSchedulingIgnoredDuringExecution : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0], default is Undefined, optional
        The scheduler will prefer to schedule pods to nodes that satisfy the affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred.
    requiredDuringSchedulingIgnoredDuringExecution : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0], default is Undefined, optional
        If the affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied.
    """


    preferredDuringSchedulingIgnoredDuringExecution?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0]

    requiredDuringSchedulingIgnoredDuringExecution?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0:
    """
    The weights of all of the matched WeightedPodAffinityTerm fields are added per-node to find the most preferred node(s)

    Attributes
    ----------
    podAffinityTerm : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTerm, default is Undefined, required
        pod affinity term
    weight : int, default is Undefined, required
        weight associated with matching the corresponding podAffinityTerm, in the range 1-100.
    """


    podAffinityTerm: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTerm

    weight: int


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTerm:
    """
    Required. A pod affinity term, associated with the corresponding weight.

    Attributes
    ----------
    labelSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelector, default is Undefined, optional
        label selector
    namespaceSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelector, default is Undefined, optional
        namespace selector
    namespaces : [str], default is Undefined, optional
        namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod's namespace".
    topologyKey : str, default is Undefined, required
        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed.
    """


    labelSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelector

    namespaceSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelector

    namespaces?: [str]

    topologyKey: str


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelector:
    """
    A label query over a set of resources, in this case pods.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelector:
    """
    A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod's namespace". An empty selector ({}) matches all namespaces.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0:
    """
    Defines a set of pods (namely those matching the labelSelector relative to the given namespace(s)) that this pod should be co-located (affinity) or not co-located (anti-affinity) with, where co-located is defined as running on a node whose value of the label with key <topologyKey> matches that of any node on which a pod of the set of pods is running

    Attributes
    ----------
    labelSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelector, default is Undefined, optional
        label selector
    namespaceSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelector, default is Undefined, optional
        namespace selector
    namespaces : [str], default is Undefined, optional
        namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod's namespace".
    topologyKey : str, default is Undefined, required
        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed.
    """


    labelSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelector

    namespaceSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelector

    namespaces?: [str]

    topologyKey: str


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelector:
    """
    A label query over a set of resources, in this case pods.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelector:
    """
    A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod's namespace". An empty selector ({}) matches all namespaces.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinity:
    """
    Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s)).

    Attributes
    ----------
    preferredDuringSchedulingIgnoredDuringExecution : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0], default is Undefined, optional
        The scheduler will prefer to schedule pods to nodes that satisfy the anti-affinity expressions specified by this field, but it may choose a node that violates one or more of the expressions. The node that is most preferred is the one with the greatest sum of weights, i.e. for each node that meets all of the scheduling requirements (resource request, requiredDuringScheduling anti-affinity expressions, etc.), compute a sum by iterating through the elements of this field and adding "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the node(s) with the highest sum are the most preferred.
    requiredDuringSchedulingIgnoredDuringExecution : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0], default is Undefined, optional
        If the anti-affinity requirements specified by this field are not met at scheduling time, the pod will not be scheduled onto the node. If the anti-affinity requirements specified by this field cease to be met at some point during pod execution (e.g. due to a pod label update), the system may or may not try to eventually evict the pod from its node. When there are multiple elements, the lists of nodes corresponding to each podAffinityTerm are intersected, i.e. all terms must be satisfied.
    """


    preferredDuringSchedulingIgnoredDuringExecution?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0]

    requiredDuringSchedulingIgnoredDuringExecution?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0:
    """
    The weights of all of the matched WeightedPodAffinityTerm fields are added per-node to find the most preferred node(s)

    Attributes
    ----------
    podAffinityTerm : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTerm, default is Undefined, required
        pod affinity term
    weight : int, default is Undefined, required
        weight associated with matching the corresponding podAffinityTerm, in the range 1-100.
    """


    podAffinityTerm: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTerm

    weight: int


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTerm:
    """
    Required. A pod affinity term, associated with the corresponding weight.

    Attributes
    ----------
    labelSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelector, default is Undefined, optional
        label selector
    namespaceSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelector, default is Undefined, optional
        namespace selector
    namespaces : [str], default is Undefined, optional
        namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod's namespace".
    topologyKey : str, default is Undefined, required
        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed.
    """


    labelSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelector

    namespaceSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelector

    namespaces?: [str]

    topologyKey: str


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelector:
    """
    A label query over a set of resources, in this case pods.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermLabelSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelector:
    """
    A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod's namespace". An empty selector ({}) matches all namespaces.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityPreferredDuringSchedulingIgnoredDuringExecutionItems0PodAffinityTermNamespaceSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0:
    """
    Defines a set of pods (namely those matching the labelSelector relative to the given namespace(s)) that this pod should be co-located (affinity) or not co-located (anti-affinity) with, where co-located is defined as running on a node whose value of the label with key <topologyKey> matches that of any node on which a pod of the set of pods is running

    Attributes
    ----------
    labelSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelector, default is Undefined, optional
        label selector
    namespaceSelector : KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelector, default is Undefined, optional
        namespace selector
    namespaces : [str], default is Undefined, optional
        namespaces specifies a static list of namespace names that the term applies to. The term is applied to the union of the namespaces listed in this field and the ones selected by namespaceSelector. null or empty namespaces list and null namespaceSelector means "this pod's namespace".
    topologyKey : str, default is Undefined, required
        This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching the labelSelector in the specified namespaces, where co-located is defined as running on a node whose value of the label with key topologyKey matches that of any node on which any of the selected pods is running. Empty topologyKey is not allowed.
    """


    labelSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelector

    namespaceSelector?: KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelector

    namespaces?: [str]

    topologyKey: str


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelector:
    """
    A label query over a set of resources, in this case pods.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0LabelSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelector:
    """
    A label query over the set of namespaces that the term applies to. The term is applied to the union of the namespaces selected by this field and the ones listed in the namespaces field. null selector and null or empty namespaces list means "this pod's namespace". An empty selector ({}) matches all namespaces.

    Attributes
    ----------
    matchExpressions : [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelectorMatchExpressionsItems0], default is Undefined, optional
        matchExpressions is a list of label selector requirements. The requirements are ANDed.
    matchLabels : {str:str}, default is Undefined, optional
        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels map is equivalent to an element of matchExpressions, whose key field is "key", the operator is "In", and the values array contains only "value". The requirements are ANDed.
    """


    matchExpressions?: [KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelectorMatchExpressionsItems0]

    matchLabels?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSAffinityPodAntiAffinityRequiredDuringSchedulingIgnoredDuringExecutionItems0NamespaceSelectorMatchExpressionsItems0:
    """
    A label selector requirement is a selector that contains values, a key, and an operator that relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values. Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn, the values array must be non-empty. If the operator is Exists or DoesNotExist, the values array must be empty. This array is replaced during a strategic merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSNodeLocalDNS:
    """
    NodeLocalDNS specifies the configuration for the node-local-dns addon

    Attributes
    ----------
    additionalConfig : str, default is Undefined, optional
        AdditionalConfig is used to provide additional config for node local dns by the user - it will include the original CoreFile made by kOps.
    cpuRequest : int | str, default is Undefined, optional
        CPURequest specifies the cpu requests of each node-local-dns container in the daemonset. Default 25m.
    enabled : bool, default is Undefined, optional
        Enabled activates the node-local-dns addon.
    externalCoreFile : str, default is Undefined, optional
        ExternalCoreFile is used to provide a complete NodeLocalDNS CoreFile by the user - ignores other provided flags which modify the CoreFile.
    forwardToKubeDNS : bool, default is Undefined, optional
        If enabled, nodelocal dns will use kubedns as a default upstream
    image : str, default is Undefined, optional
        Image overrides the default docker image used for node-local-dns addon.
    localIP : str, default is Undefined, optional
        Local listen IP address. It can be any IP in the 169.254.20.0/16 space or any other IP address that can be guaranteed to not collide with any existing IP.
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest specifies the memory requests of each node-local-dns container in the daemonset. Default 5Mi.
    podAnnotations : {str:str}, default is Undefined, optional
        PodAnnotations makes possible to add additional annotations to node-local-dns. Default: none
    """


    additionalConfig?: str

    cpuRequest?: int | str

    enabled?: bool

    externalCoreFile?: str

    forwardToKubeDNS?: bool

    image?: str

    localIP?: str

    memoryRequest?: int | str

    podAnnotations?: {str:str}


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeDNSTolerationsItems0:
    """
    The pod this Toleration is attached to tolerates any taint that matches the triple <key,value,effect> using the matching operator <operator>.

    Attributes
    ----------
    effect : str, default is Undefined, optional
        Effect indicates the taint effect to match. Empty means match all taint effects. When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
    key : str, default is Undefined, optional
        Key is the taint key that the toleration applies to. Empty means match all taint keys. If the key is empty, operator must be Exists; this combination means to match all values and all keys.
    operator : str, default is Undefined, optional
        Operator represents a key's relationship to the value. Valid operators are Exists and Equal. Defaults to Equal. Exists is equivalent to wildcard for value, so that a pod can tolerate all taints of a particular category.
    tolerationSeconds : int, default is Undefined, optional
        TolerationSeconds represents the period of time the toleration (which must be of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default, it is not set, which means tolerate the taint forever (do not evict). Zero and negative values will be treated as 0 (evict immediately) by the system.
    value : str, default is Undefined, optional
        Value is the taint value the toleration matches to. If the operator is Exists, the value should be empty, otherwise just a regular string.
    """


    effect?: str

    key?: str

    operator?: str

    tolerationSeconds?: int

    value?: str


schema KopsK8sIoV1alpha2ClusterSpecKubeProxy:
    """
    KubeProxyConfig defines the configuration for a proxy

    Attributes
    ----------
    bindAddress : str, default is Undefined, optional
        BindAddress is IP address for the proxy server to serve on
    clusterCIDR : str, default is Undefined, optional
        ClusterCIDR is the CIDR range of the pods in the cluster
    conntrackMaxPerCore : int, default is Undefined, optional
        Maximum number of NAT connections to track per CPU core (default: 131072)
    conntrackMin : int, default is Undefined, optional
        Minimum number of conntrack entries to allocate, regardless of conntrack-max-per-core
    cpuLimit : int | str, default is Undefined, optional
        CPULimit, cpu limit compute resource for kube proxy e.g. "30m"
    cpuRequest : int | str, default is Undefined, optional
        CPURequest, cpu request compute resource for kube proxy e.g. "20m"
    enabled : bool, default is Undefined, optional
        Enabled allows enabling or disabling kube-proxy
    featureGates : {str:str}, default is Undefined, optional
        FeatureGates is a series of key pairs used to switch on features for the proxy
    hostnameOverride : str, default is Undefined, optional
        HostnameOverride, if non-empty, will be used as the identity instead of the actual hostname.
    image : str, default is Undefined, optional
        image
    ipvsExcludeCidrs : [str], default is Undefined, optional
        IPVSExcludeCIDRs is comma-separated list of CIDR's which the ipvs proxier should not touch when cleaning up IPVS rules
    ipvsMinSyncPeriod : str, default is Undefined, optional
        IPVSMinSyncPeriod is the minimum interval of how often the ipvs rules can be refreshed as endpoints and services change (e.g. '5s', '1m', '2h22m')
    ipvsScheduler : str, default is Undefined, optional
        IPVSScheduler is the ipvs scheduler type when proxy mode is ipvs
    ipvsSyncPeriod : str, default is Undefined, optional
        IPVSSyncPeriod duration is the maximum interval of how often ipvs rules are refreshed
    logLevel : int, default is Undefined, optional
        LogLevel is the logging level of the proxy
    master : str, default is Undefined, optional
        Master is the address of the Kubernetes API server (overrides any value in kubeconfig)
    memoryLimit : int | str, default is Undefined, optional
        MemoryLimit, memory limit compute resource for kube proxy e.g. "30Mi"
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest, memory request compute resource for kube proxy e.g. "30Mi"
    metricsBindAddress : str, default is Undefined, optional
        MetricsBindAddress is the IP address for the metrics server to serve on
    proxyMode : str, default is Undefined, optional
        Which proxy mode to use: (userspace, iptables, ipvs)
    """


    bindAddress?: str

    clusterCIDR?: str

    conntrackMaxPerCore?: int

    conntrackMin?: int

    cpuLimit?: int | str

    cpuRequest?: int | str

    enabled?: bool

    featureGates?: {str:str}

    hostnameOverride?: str

    image?: str

    ipvsExcludeCidrs?: [str]

    ipvsMinSyncPeriod?: str

    ipvsScheduler?: str

    ipvsSyncPeriod?: str

    logLevel?: int

    master?: str

    memoryLimit?: int | str

    memoryRequest?: int | str

    metricsBindAddress?: str

    proxyMode?: str


    check:
        matchPattern(cpuLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeScheduler:
    """
    KubeSchedulerConfig is the configuration for the kube-scheduler

    Attributes
    ----------
    authenticationKubeconfig : str, default is Undefined, optional
        AuthenticationKubeconfig is the path to an Authentication Kubeconfig
    authorizationAlwaysAllowPaths : [str], default is Undefined, optional
        AuthorizationAlwaysAllowPaths is the list of HTTP paths to skip during authorization
    authorizationKubeconfig : str, default is Undefined, optional
        AuthorizationKubeconfig is the path to an Authorization Kubeconfig
    burst : int, default is Undefined, optional
        Burst sets the maximum qps to send to apiserver after the burst quota is exhausted
    enableContentionProfiling : bool, default is Undefined, optional
        EnableContentionProfiling enables block profiling, if profiling is enabled
    enableProfiling : bool, default is Undefined, optional
        EnableProfiling enables profiling via web interface host:port/debug/pprof/
    featureGates : {str:str}, default is Undefined, optional
        FeatureGates is set of key=value pairs that describe feature gates for alpha/experimental features.
    image : str, default is Undefined, optional
        Image is the container image to use.
    kubeAPIBurst : int, default is Undefined, optional
        KubeAPIBurst Burst to use while talking with kubernetes apiserver. (default 30)
    kubeAPIQPS : int | str, default is Undefined, optional
        KubeAPIQPS QPS to use while talking with kubernetes apiserver. (default 20)
    leaderElection : KopsK8sIoV1alpha2ClusterSpecKubeSchedulerLeaderElection, default is Undefined, optional
        leader election
    logFormat : str, default is Undefined, optional
        LogFormat is the logging format of the scheduler. Supported values: text, json. Default: text
    logLevel : int, default is Undefined, optional
        LogLevel is the logging level
    master : str, default is Undefined, optional
        Master is a url to the kube master
    maxPersistentVolumes : int, default is Undefined, optional
        MaxPersistentVolumes changes the maximum number of persistent volumes the scheduler will scheduler onto the same node. Only takes effect if value is positive. This corresponds to the KUBE_MAX_PD_VOLS environment variable. The default depends on the version and the cloud provider as outlined: https://kubernetes.io/docs/concepts/storage/storage-limits/
    qps : int | str, default is Undefined, optional
        Qps sets the maximum qps to send to apiserver after the burst quota is exhausted
    tlsCertFile : str, default is Undefined, optional
        TLSCertFile is the file containing the TLS server certificate.
    tlsPrivateKeyFile : str, default is Undefined, optional
        TLSPrivateKeyFile is the file containing the private key for the TLS server certificate.
    usePolicyConfigMap : bool, default is Undefined, optional
        UsePolicyConfigMap enable setting the scheduler policy from a configmap
    """


    authenticationKubeconfig?: str

    authorizationAlwaysAllowPaths?: [str]

    authorizationKubeconfig?: str

    burst?: int

    enableContentionProfiling?: bool

    enableProfiling?: bool

    featureGates?: {str:str}

    image?: str

    kubeAPIBurst?: int

    kubeAPIQPS?: int | str

    leaderElection?: KopsK8sIoV1alpha2ClusterSpecKubeSchedulerLeaderElection

    logFormat?: str

    logLevel?: int

    master?: str

    maxPersistentVolumes?: int

    qps?: int | str

    tlsCertFile?: str

    tlsPrivateKeyFile?: str

    usePolicyConfigMap?: bool


    check:
        matchPattern(kubeAPIQPS, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(qps, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecKubeSchedulerLeaderElection:
    """
    LeaderElection defines the configuration of leader election client.

    Attributes
    ----------
    leaderElect : bool, default is Undefined, optional
        leaderElect enables a leader election client to gain leadership before executing the main loop. Enable this when running replicated components for high availability.
    leaderElectLeaseDuration : str, default is Undefined, optional
        leaderElectLeaseDuration is the length in time non-leader candidates will wait after observing a leadership renewal until attempting to acquire leadership of a led but unrenewed leader slot. This is effectively the maximum duration that a leader can be stopped before it is replaced by another candidate
    leaderElectRenewDeadlineDuration : str, default is Undefined, optional
        LeaderElectRenewDeadlineDuration is the interval between attempts by the acting master to renew a leadership slot before it stops leading. This must be less than or equal to the lease duration.
    leaderElectResourceLock : str, default is Undefined, optional
        LeaderElectResourceLock is the type of resource object that is used for locking during leader election. Supported options are endpoints (default) and `configmaps`.
    leaderElectResourceName : str, default is Undefined, optional
        LeaderElectResourceName is the name of resource object that is used for locking during leader election.
    leaderElectResourceNamespace : str, default is Undefined, optional
        LeaderElectResourceNamespace is the namespace of resource object that is used for locking during leader election.
    leaderElectRetryPeriod : str, default is Undefined, optional
        LeaderElectRetryPeriod is The duration the clients should wait between attempting acquisition and renewal of a leadership. This is only applicable if leader election is enabled.
    """


    leaderElect?: bool

    leaderElectLeaseDuration?: str

    leaderElectRenewDeadlineDuration?: str

    leaderElectResourceLock?: str

    leaderElectResourceName?: str

    leaderElectResourceNamespace?: str

    leaderElectRetryPeriod?: str


schema KopsK8sIoV1alpha2ClusterSpecKubelet:
    """
    Kubelet is the kubelet configuration for nodes not belonging to the control plane. It can be overridden by the kubelet configuration specified in the instance group.

    Attributes
    ----------
    allowPrivileged : bool, default is Undefined, optional
        AllowPrivileged enables containers to request privileged mode (defaults to false)
    allowedUnsafeSysctls : [str], default is Undefined, optional
        AllowedUnsafeSysctls are passed to the kubelet config to whitelist allowable sysctls
    anonymousAuth : bool, default is Undefined, optional
        AnonymousAuth permits you to control auth to the kubelet api
    apiServers : str, default is Undefined, optional
        APIServers is not used for clusters version 1.6 and later - flag removed
    authenticationTokenWebhook : bool, default is Undefined, optional
        AuthenticationTokenWebhook uses the TokenReview API to determine authentication for bearer tokens.
    authenticationTokenWebhookCacheTtl : str, default is Undefined, optional
        AuthenticationTokenWebhook sets the duration to cache responses from the webhook token authenticator. Default is 2m. (default 2m0s)
    authorizationMode : str, default is Undefined, optional
        AuthorizationMode is the authorization mode the kubelet is running in
    babysitDaemons : bool, default is Undefined, optional
        The node has babysitter process monitoring docker and kubelet. Removed as of 1.7
    bootstrapKubeconfig : str, default is Undefined, optional
        BootstrapKubeconfig is the path to a kubeconfig file that will be used to get client certificate for kubelet
    cgroupDriver : str, default is Undefined, optional
        CgroupDriver allows the explicit setting of the kubelet cgroup driver. If omitted, defaults to cgroupfs.
    cgroupRoot : str, default is Undefined, optional
        cgroupRoot is the root cgroup to use for pods. This is handled by the container runtime on a best effort basis.
    clientCaFile : str, default is Undefined, optional
        ClientCAFile is the path to a CA certificate
    cloudProvider : str, default is Undefined, optional
        CloudProvider is the provider for cloud services.
    clusterDNS : str, default is Undefined, optional
        ClusterDNS is the IP address for a cluster DNS server
    clusterDomain : str, default is Undefined, optional
        ClusterDomain is the DNS domain for this cluster
    configureCbr0 : bool, default is Undefined, optional
        configureCBR0 enables the kubelet to configure cbr0 based on Node.Spec.PodCIDR.
    containerLogMaxFiles : int, default is Undefined, optional
        ContainerLogMaxFiles is the maximum number of container log files that can be present for a container. The number must be >= 2.
    containerLogMaxSize : str, default is Undefined, optional
        ContainerLogMaxSize is the maximum size (e.g. 10Mi) of container log file before it is rotated.
    cpuCFSQuota : bool, default is Undefined, optional
        CPUCFSQuota enables CPU CFS quota enforcement for containers that specify CPU limits
    cpuCFSQuotaPeriod : str, default is Undefined, optional
        CPUCFSQuotaPeriod sets CPU CFS quota period value, cpu.cfs_period_us, defaults to Linux Kernel default
    cpuManagerPolicy : str, default is Undefined, optional
        CpuManagerPolicy allows for changing the default policy of None to static
    dockerDisableSharedPID : bool, default is Undefined, optional
        DockerDisableSharedPID was removed.
    enableCadvisorJsonEndpoints : bool, default is Undefined, optional
        EnableCadvisorJsonEndpoints enables cAdvisor json `/spec` and `/stats/*` endpoints. Defaults to False.
    enableCustomMetrics : bool, default is Undefined, optional
        Enable gathering custom metrics.
    enableDebuggingHandlers : bool, default is Undefined, optional
        EnableDebuggingHandlers enables server endpoints for log collection and local running of containers and commands
    enforceNodeAllocatable : str, default is Undefined, optional
        Enforce Allocatable across pods whenever the overall usage across all pods exceeds Allocatable.
    eventBurst : int, default is Undefined, optional
        EventBurst temporarily allows event records to burst to this number, while still not exceeding EventQPS. Only used if EventQPS > 0.
    eventQPS : int, default is Undefined, optional
        EventQPS if > 0, limit event creations per second to this value.  If 0, unlimited.
    evictionHard : str, default is Undefined, optional
        Comma-delimited list of hard eviction expressions.  For example, 'memory.available<300Mi'.
    evictionMaxPodGracePeriod : int, default is Undefined, optional
        Maximum allowed grace period (in seconds) to use when terminating pods in response to a soft eviction threshold being met.
    evictionMinimumReclaim : str, default is Undefined, optional
        Comma-delimited list of minimum reclaims (e.g. imagefs.available=2Gi) that describes the minimum amount of resource the kubelet will reclaim when performing a pod eviction if that resource is under pressure.
    evictionPressureTransitionPeriod : str, default is Undefined, optional
        Duration for which the kubelet has to wait before transitioning out of an eviction pressure condition.
    evictionSoft : str, default is Undefined, optional
        Comma-delimited list of soft eviction expressions.  For example, 'memory.available<300Mi'.
    evictionSoftGracePeriod : str, default is Undefined, optional
        Comma-delimited list of grace periods for each soft eviction signal.  For example, 'memory.available=30s'.
    experimentalAllocatableIgnoreEviction : bool, default is Undefined, optional
        ExperimentalAllocatableIgnoreEviction enables ignoring Hard Eviction Thresholds while calculating Node Allocatable
    experimentalAllowedUnsafeSysctls : [str], default is Undefined, optional
        ExperimentalAllowedUnsafeSysctls are passed to the kubelet config to whitelist allowable sysctls Was promoted to beta and renamed. https://github.com/kubernetes/kubernetes/pull/63717
    failSwapOn : bool, default is Undefined, optional
        Tells the Kubelet to fail to start if swap is enabled on the node.
    featureGates : {str:str}, default is Undefined, optional
        FeatureGates is set of key=value pairs that describe feature gates for alpha/experimental features.
    hairpinMode : str, default is Undefined, optional
        How should the kubelet configure the container bridge for hairpin packets. Setting this flag allows endpoints in a Service to loadbalance back to themselves if they should try to access their own Service. Values: "promiscuous-bridge": make the container bridge promiscuous. "hairpin-veth":       set the hairpin flag on container veth interfaces. "none":               do nothing. Setting --configure-cbr0 to false implies that to achieve hairpin NAT one must set --hairpin-mode=veth-flag, because bridge assumes the existence of a container bridge named cbr0.
    hostnameOverride : str, default is Undefined, optional
        HostnameOverride is the hostname used to identify the kubelet instead of the actual hostname.
    housekeepingInterval : str, default is Undefined, optional
        HousekeepingInterval allows to specify interval between container housekeepings.
    imageGCHighThresholdPercent : int, default is Undefined, optional
        ImageGCHighThresholdPercent is the percent of disk usage after which image garbage collection is always run.
    imageGCLowThresholdPercent : int, default is Undefined, optional
        ImageGCLowThresholdPercent is the percent of disk usage before which image garbage collection is never run. Lowest disk usage to garbage collect to.
    imagePullProgressDeadline : str, default is Undefined, optional
        ImagePullProgressDeadline is the timeout for image pulls If no pulling progress is made before this deadline, the image pulling will be cancelled. (default 1m0s)
    kernelMemcgNotification : bool, default is Undefined, optional
        Integrate with the kernel memcg notification to determine if memory eviction thresholds are crossed rather than polling.
    kubeReserved : {str:str}, default is Undefined, optional
        Resource reservation for kubernetes system daemons like the kubelet, container runtime, node problem detector, etc.
    kubeReservedCgroup : str, default is Undefined, optional
        Control group for kube daemons.
    kubeconfigPath : str, default is Undefined, optional
        KubeconfigPath is the path of kubeconfig for the kubelet
    kubeletCgroups : str, default is Undefined, optional
        KubeletCgroups is the absolute name of cgroups to isolate the kubelet in.
    logFormat : str, default is Undefined, optional
        LogFormat is the logging format of the kubelet. Supported values: text, json. Default: text
    logLevel : int, default is Undefined, optional
        LogLevel is the logging level of the kubelet
    maxPods : int, default is Undefined, optional
        MaxPods is the number of pods that can run on this Kubelet.
    memorySwapBehavior : str, default is Undefined, optional
        MemorySwapBehavior defines how swap is used by container workloads. Supported values: LimitedSwap, "UnlimitedSwap.
    networkPluginMTU : int, default is Undefined, optional
        NetworkPluginMTU is the MTU to be passed to the network plugin, and overrides the default MTU for cases where it cannot be automatically computed (such as IPSEC).
    networkPluginName : str, default is Undefined, optional
        NetworkPluginName is the name of the network plugin to be invoked for various events in kubelet/pod lifecycle
    nodeLabels : {str:str}, default is Undefined, optional
        NodeLabels to add when registering the node in the cluster.
    nodeStatusUpdateFrequency : str, default is Undefined, optional
        NodeStatusUpdateFrequency Specifies how often kubelet posts node status to master (default 10s) must work with nodeMonitorGracePeriod in KubeControllerManagerConfig.
    nonMasqueradeCIDR : str, default is Undefined, optional
        NonMasqueradeCIDR configures masquerading: traffic to IPs outside this range will use IP masquerade.
    nvidiaGPUs : int, default is Undefined, optional
        NvidiaGPUs is the number of NVIDIA GPU devices on this node.
    podCIDR : str, default is Undefined, optional
        PodCIDR is the CIDR to use for pod IP addresses, only used in standalone mode. In cluster mode, this is obtained from the master.
    podInfraContainerImage : str, default is Undefined, optional
        PodInfraContainerImage is the image whose network/ipc containers in each pod will use.
    podManifestPath : str, default is Undefined, optional
        config is the path to the config file or directory of files
    podPidsLimit : int, default is Undefined, optional
        PodPidsLimit is the maximum number of pids in any pod.
    protectKernelDefaults : bool, default is Undefined, optional
        Default kubelet behaviour for kernel tuning. If set, kubelet errors if any of kernel tunables is different than kubelet defaults. (DEPRECATED: This parameter should be set via the config file specified by the Kubelet's --config flag.
    readOnlyPort : int, default is Undefined, optional
        ReadOnlyPort is the port used by the kubelet api for read-only access (default 10255)
    reconcileCIDR : bool, default is Undefined, optional
        ReconcileCIDR is Reconcile node CIDR with the CIDR specified by the API server. No-op if register-node or configure-cbr0 is false.
    registerNode : bool, default is Undefined, optional
        RegisterNode enables automatic registration with the apiserver.
    registerSchedulable : bool, default is Undefined, optional
        registerSchedulable tells the kubelet to register the node as schedulable. No-op if register-node is false.
    registryBurst : int, default is Undefined, optional
        RegistryBurst Maximum size of a bursty pulls, temporarily allows pulls to burst to this number, while still not exceeding registry-qps. Only used if --registry-qps > 0 (default 10)
    registryPullQPS : int, default is Undefined, optional
        RegistryPullQPS if > 0, limit registry pull QPS to this value.  If 0, unlimited. (default 5)
    requireKubeconfig : bool, default is Undefined, optional
        RequireKubeconfig indicates a kubeconfig is required
    resolvConf : str, default is Undefined, optional
        ResolverConfig is the resolver configuration file used as the basis for the container DNS resolution configuration."), []
    rootDir : str, default is Undefined, optional
        RootDir is the directory path for managing kubelet files (volume mounts,etc)
    rotateCertificates : bool, default is Undefined, optional
        rotateCertificates enables client certificate rotation.
    runtimeCgroups : str, default is Undefined, optional
        Cgroups that container runtime is expected to be isolated in.
    runtimeRequestTimeout : str, default is Undefined, optional
        RuntimeRequestTimeout is timeout for runtime requests on - pull, logs, exec and attach
    seccompDefault : bool, default is Undefined, optional
        SeccompDefault enables the use of `RuntimeDefault` as the default seccomp profile for all workloads.
    seccompProfileRoot : str, default is Undefined, optional
        SeccompProfileRoot is the directory path for seccomp profiles.
    serializeImagePulls : bool, default is Undefined, optional
        SerializeImagePulls when enabled, tells the Kubelet to pull images one at a time.
    shutdownGracePeriod : str, default is Undefined, optional
        ShutdownGracePeriod specifies the total duration that the node should delay the shutdown by. Default: 30s
    shutdownGracePeriodCriticalPods : str, default is Undefined, optional
        ShutdownGracePeriodCriticalPods specifies the duration used to terminate critical pods during a node shutdown. Default: 10s
    streamingConnectionIdleTimeout : str, default is Undefined, optional
        StreamingConnectionIdleTimeout is the maximum time a streaming connection can be idle before the connection is automatically closed
    systemCgroups : str, default is Undefined, optional
        SystemCgroups is absolute name of cgroups in which to place all non-kernel processes that are not already in a container. Empty for no container. Rolling back the flag requires a reboot.
    systemReserved : {str:str}, default is Undefined, optional
        Capture resource reservation for OS system daemons like sshd, udev, etc.
    systemReservedCgroup : str, default is Undefined, optional
        Parent control group for OS system daemons.
    taints : [str], default is Undefined, optional
        Taints to add when registering a node in the cluster
    tlsCertFile : str, default is Undefined, optional
        TODO: Remove unused TLSCertFile
    tlsCipherSuites : [str], default is Undefined, optional
        TLSCipherSuites indicates the allowed TLS cipher suite
    tlsMinVersion : str, default is Undefined, optional
        TLSMinVersion indicates the minimum TLS version allowed
    tlsPrivateKeyFile : str, default is Undefined, optional
        TODO: Remove unused TLSPrivateKeyFile
    topologyManagerPolicy : str, default is Undefined, optional
        TopologyManagerPolicy determines the allocation policy for the topology manager.
    volumePluginDirectory : str, default is Undefined, optional
        The full path of the directory in which to search for additional third party volume plugins (this path must be writeable, dependent on your choice of OS)
    volumeStatsAggPeriod : str, default is Undefined, optional
        VolumeStatsAggPeriod is the interval for kubelet to calculate and cache the volume disk usage for all pods and volumes
    """


    allowPrivileged?: bool

    allowedUnsafeSysctls?: [str]

    anonymousAuth?: bool

    apiServers?: str

    authenticationTokenWebhook?: bool

    authenticationTokenWebhookCacheTtl?: str

    authorizationMode?: str

    babysitDaemons?: bool

    bootstrapKubeconfig?: str

    cgroupDriver?: str

    cgroupRoot?: str

    clientCaFile?: str

    cloudProvider?: str

    clusterDNS?: str

    clusterDomain?: str

    configureCbr0?: bool

    containerLogMaxFiles?: int

    containerLogMaxSize?: str

    cpuCFSQuota?: bool

    cpuCFSQuotaPeriod?: str

    cpuManagerPolicy?: str

    dockerDisableSharedPID?: bool

    enableCadvisorJsonEndpoints?: bool

    enableCustomMetrics?: bool

    enableDebuggingHandlers?: bool

    enforceNodeAllocatable?: str

    eventBurst?: int

    eventQPS?: int

    evictionHard?: str

    evictionMaxPodGracePeriod?: int

    evictionMinimumReclaim?: str

    evictionPressureTransitionPeriod?: str

    evictionSoft?: str

    evictionSoftGracePeriod?: str

    experimentalAllocatableIgnoreEviction?: bool

    experimentalAllowedUnsafeSysctls?: [str]

    failSwapOn?: bool

    featureGates?: {str:str}

    hairpinMode?: str

    hostnameOverride?: str

    housekeepingInterval?: str

    imageGCHighThresholdPercent?: int

    imageGCLowThresholdPercent?: int

    imagePullProgressDeadline?: str

    kernelMemcgNotification?: bool

    kubeReserved?: {str:str}

    kubeReservedCgroup?: str

    kubeconfigPath?: str

    kubeletCgroups?: str

    logFormat?: str

    logLevel?: int

    maxPods?: int

    memorySwapBehavior?: str

    networkPluginMTU?: int

    networkPluginName?: str

    nodeLabels?: {str:str}

    nodeStatusUpdateFrequency?: str

    nonMasqueradeCIDR?: str

    nvidiaGPUs?: int

    podCIDR?: str

    podInfraContainerImage?: str

    podManifestPath?: str

    podPidsLimit?: int

    protectKernelDefaults?: bool

    readOnlyPort?: int

    reconcileCIDR?: bool

    registerNode?: bool

    registerSchedulable?: bool

    registryBurst?: int

    registryPullQPS?: int

    requireKubeconfig?: bool

    resolvConf?: str

    rootDir?: str

    rotateCertificates?: bool

    runtimeCgroups?: str

    runtimeRequestTimeout?: str

    seccompDefault?: bool

    seccompProfileRoot?: str

    serializeImagePulls?: bool

    shutdownGracePeriod?: str

    shutdownGracePeriodCriticalPods?: str

    streamingConnectionIdleTimeout?: str

    systemCgroups?: str

    systemReserved?: {str:str}

    systemReservedCgroup?: str

    taints?: [str]

    tlsCertFile?: str

    tlsCipherSuites?: [str]

    tlsMinVersion?: str

    tlsPrivateKeyFile?: str

    topologyManagerPolicy?: str

    volumePluginDirectory?: str

    volumeStatsAggPeriod?: str


schema KopsK8sIoV1alpha2ClusterSpecMasterKubelet:
    """
    MasterKubelet is the kubelet configuration for nodes belonging to the control plane It can be overridden by the kubelet configuration specified in the instance group.

    Attributes
    ----------
    allowPrivileged : bool, default is Undefined, optional
        AllowPrivileged enables containers to request privileged mode (defaults to false)
    allowedUnsafeSysctls : [str], default is Undefined, optional
        AllowedUnsafeSysctls are passed to the kubelet config to whitelist allowable sysctls
    anonymousAuth : bool, default is Undefined, optional
        AnonymousAuth permits you to control auth to the kubelet api
    apiServers : str, default is Undefined, optional
        APIServers is not used for clusters version 1.6 and later - flag removed
    authenticationTokenWebhook : bool, default is Undefined, optional
        AuthenticationTokenWebhook uses the TokenReview API to determine authentication for bearer tokens.
    authenticationTokenWebhookCacheTtl : str, default is Undefined, optional
        AuthenticationTokenWebhook sets the duration to cache responses from the webhook token authenticator. Default is 2m. (default 2m0s)
    authorizationMode : str, default is Undefined, optional
        AuthorizationMode is the authorization mode the kubelet is running in
    babysitDaemons : bool, default is Undefined, optional
        The node has babysitter process monitoring docker and kubelet. Removed as of 1.7
    bootstrapKubeconfig : str, default is Undefined, optional
        BootstrapKubeconfig is the path to a kubeconfig file that will be used to get client certificate for kubelet
    cgroupDriver : str, default is Undefined, optional
        CgroupDriver allows the explicit setting of the kubelet cgroup driver. If omitted, defaults to cgroupfs.
    cgroupRoot : str, default is Undefined, optional
        cgroupRoot is the root cgroup to use for pods. This is handled by the container runtime on a best effort basis.
    clientCaFile : str, default is Undefined, optional
        ClientCAFile is the path to a CA certificate
    cloudProvider : str, default is Undefined, optional
        CloudProvider is the provider for cloud services.
    clusterDNS : str, default is Undefined, optional
        ClusterDNS is the IP address for a cluster DNS server
    clusterDomain : str, default is Undefined, optional
        ClusterDomain is the DNS domain for this cluster
    configureCbr0 : bool, default is Undefined, optional
        configureCBR0 enables the kubelet to configure cbr0 based on Node.Spec.PodCIDR.
    containerLogMaxFiles : int, default is Undefined, optional
        ContainerLogMaxFiles is the maximum number of container log files that can be present for a container. The number must be >= 2.
    containerLogMaxSize : str, default is Undefined, optional
        ContainerLogMaxSize is the maximum size (e.g. 10Mi) of container log file before it is rotated.
    cpuCFSQuota : bool, default is Undefined, optional
        CPUCFSQuota enables CPU CFS quota enforcement for containers that specify CPU limits
    cpuCFSQuotaPeriod : str, default is Undefined, optional
        CPUCFSQuotaPeriod sets CPU CFS quota period value, cpu.cfs_period_us, defaults to Linux Kernel default
    cpuManagerPolicy : str, default is Undefined, optional
        CpuManagerPolicy allows for changing the default policy of None to static
    dockerDisableSharedPID : bool, default is Undefined, optional
        DockerDisableSharedPID was removed.
    enableCadvisorJsonEndpoints : bool, default is Undefined, optional
        EnableCadvisorJsonEndpoints enables cAdvisor json `/spec` and `/stats/*` endpoints. Defaults to False.
    enableCustomMetrics : bool, default is Undefined, optional
        Enable gathering custom metrics.
    enableDebuggingHandlers : bool, default is Undefined, optional
        EnableDebuggingHandlers enables server endpoints for log collection and local running of containers and commands
    enforceNodeAllocatable : str, default is Undefined, optional
        Enforce Allocatable across pods whenever the overall usage across all pods exceeds Allocatable.
    eventBurst : int, default is Undefined, optional
        EventBurst temporarily allows event records to burst to this number, while still not exceeding EventQPS. Only used if EventQPS > 0.
    eventQPS : int, default is Undefined, optional
        EventQPS if > 0, limit event creations per second to this value.  If 0, unlimited.
    evictionHard : str, default is Undefined, optional
        Comma-delimited list of hard eviction expressions.  For example, 'memory.available<300Mi'.
    evictionMaxPodGracePeriod : int, default is Undefined, optional
        Maximum allowed grace period (in seconds) to use when terminating pods in response to a soft eviction threshold being met.
    evictionMinimumReclaim : str, default is Undefined, optional
        Comma-delimited list of minimum reclaims (e.g. imagefs.available=2Gi) that describes the minimum amount of resource the kubelet will reclaim when performing a pod eviction if that resource is under pressure.
    evictionPressureTransitionPeriod : str, default is Undefined, optional
        Duration for which the kubelet has to wait before transitioning out of an eviction pressure condition.
    evictionSoft : str, default is Undefined, optional
        Comma-delimited list of soft eviction expressions.  For example, 'memory.available<300Mi'.
    evictionSoftGracePeriod : str, default is Undefined, optional
        Comma-delimited list of grace periods for each soft eviction signal.  For example, 'memory.available=30s'.
    experimentalAllocatableIgnoreEviction : bool, default is Undefined, optional
        ExperimentalAllocatableIgnoreEviction enables ignoring Hard Eviction Thresholds while calculating Node Allocatable
    experimentalAllowedUnsafeSysctls : [str], default is Undefined, optional
        ExperimentalAllowedUnsafeSysctls are passed to the kubelet config to whitelist allowable sysctls Was promoted to beta and renamed. https://github.com/kubernetes/kubernetes/pull/63717
    failSwapOn : bool, default is Undefined, optional
        Tells the Kubelet to fail to start if swap is enabled on the node.
    featureGates : {str:str}, default is Undefined, optional
        FeatureGates is set of key=value pairs that describe feature gates for alpha/experimental features.
    hairpinMode : str, default is Undefined, optional
        How should the kubelet configure the container bridge for hairpin packets. Setting this flag allows endpoints in a Service to loadbalance back to themselves if they should try to access their own Service. Values: "promiscuous-bridge": make the container bridge promiscuous. "hairpin-veth":       set the hairpin flag on container veth interfaces. "none":               do nothing. Setting --configure-cbr0 to false implies that to achieve hairpin NAT one must set --hairpin-mode=veth-flag, because bridge assumes the existence of a container bridge named cbr0.
    hostnameOverride : str, default is Undefined, optional
        HostnameOverride is the hostname used to identify the kubelet instead of the actual hostname.
    housekeepingInterval : str, default is Undefined, optional
        HousekeepingInterval allows to specify interval between container housekeepings.
    imageGCHighThresholdPercent : int, default is Undefined, optional
        ImageGCHighThresholdPercent is the percent of disk usage after which image garbage collection is always run.
    imageGCLowThresholdPercent : int, default is Undefined, optional
        ImageGCLowThresholdPercent is the percent of disk usage before which image garbage collection is never run. Lowest disk usage to garbage collect to.
    imagePullProgressDeadline : str, default is Undefined, optional
        ImagePullProgressDeadline is the timeout for image pulls If no pulling progress is made before this deadline, the image pulling will be cancelled. (default 1m0s)
    kernelMemcgNotification : bool, default is Undefined, optional
        Integrate with the kernel memcg notification to determine if memory eviction thresholds are crossed rather than polling.
    kubeReserved : {str:str}, default is Undefined, optional
        Resource reservation for kubernetes system daemons like the kubelet, container runtime, node problem detector, etc.
    kubeReservedCgroup : str, default is Undefined, optional
        Control group for kube daemons.
    kubeconfigPath : str, default is Undefined, optional
        KubeconfigPath is the path of kubeconfig for the kubelet
    kubeletCgroups : str, default is Undefined, optional
        KubeletCgroups is the absolute name of cgroups to isolate the kubelet in.
    logFormat : str, default is Undefined, optional
        LogFormat is the logging format of the kubelet. Supported values: text, json. Default: text
    logLevel : int, default is Undefined, optional
        LogLevel is the logging level of the kubelet
    maxPods : int, default is Undefined, optional
        MaxPods is the number of pods that can run on this Kubelet.
    memorySwapBehavior : str, default is Undefined, optional
        MemorySwapBehavior defines how swap is used by container workloads. Supported values: LimitedSwap, "UnlimitedSwap.
    networkPluginMTU : int, default is Undefined, optional
        NetworkPluginMTU is the MTU to be passed to the network plugin, and overrides the default MTU for cases where it cannot be automatically computed (such as IPSEC).
    networkPluginName : str, default is Undefined, optional
        NetworkPluginName is the name of the network plugin to be invoked for various events in kubelet/pod lifecycle
    nodeLabels : {str:str}, default is Undefined, optional
        NodeLabels to add when registering the node in the cluster.
    nodeStatusUpdateFrequency : str, default is Undefined, optional
        NodeStatusUpdateFrequency Specifies how often kubelet posts node status to master (default 10s) must work with nodeMonitorGracePeriod in KubeControllerManagerConfig.
    nonMasqueradeCIDR : str, default is Undefined, optional
        NonMasqueradeCIDR configures masquerading: traffic to IPs outside this range will use IP masquerade.
    nvidiaGPUs : int, default is Undefined, optional
        NvidiaGPUs is the number of NVIDIA GPU devices on this node.
    podCIDR : str, default is Undefined, optional
        PodCIDR is the CIDR to use for pod IP addresses, only used in standalone mode. In cluster mode, this is obtained from the master.
    podInfraContainerImage : str, default is Undefined, optional
        PodInfraContainerImage is the image whose network/ipc containers in each pod will use.
    podManifestPath : str, default is Undefined, optional
        config is the path to the config file or directory of files
    podPidsLimit : int, default is Undefined, optional
        PodPidsLimit is the maximum number of pids in any pod.
    protectKernelDefaults : bool, default is Undefined, optional
        Default kubelet behaviour for kernel tuning. If set, kubelet errors if any of kernel tunables is different than kubelet defaults. (DEPRECATED: This parameter should be set via the config file specified by the Kubelet's --config flag.
    readOnlyPort : int, default is Undefined, optional
        ReadOnlyPort is the port used by the kubelet api for read-only access (default 10255)
    reconcileCIDR : bool, default is Undefined, optional
        ReconcileCIDR is Reconcile node CIDR with the CIDR specified by the API server. No-op if register-node or configure-cbr0 is false.
    registerNode : bool, default is Undefined, optional
        RegisterNode enables automatic registration with the apiserver.
    registerSchedulable : bool, default is Undefined, optional
        registerSchedulable tells the kubelet to register the node as schedulable. No-op if register-node is false.
    registryBurst : int, default is Undefined, optional
        RegistryBurst Maximum size of a bursty pulls, temporarily allows pulls to burst to this number, while still not exceeding registry-qps. Only used if --registry-qps > 0 (default 10)
    registryPullQPS : int, default is Undefined, optional
        RegistryPullQPS if > 0, limit registry pull QPS to this value.  If 0, unlimited. (default 5)
    requireKubeconfig : bool, default is Undefined, optional
        RequireKubeconfig indicates a kubeconfig is required
    resolvConf : str, default is Undefined, optional
        ResolverConfig is the resolver configuration file used as the basis for the container DNS resolution configuration."), []
    rootDir : str, default is Undefined, optional
        RootDir is the directory path for managing kubelet files (volume mounts,etc)
    rotateCertificates : bool, default is Undefined, optional
        rotateCertificates enables client certificate rotation.
    runtimeCgroups : str, default is Undefined, optional
        Cgroups that container runtime is expected to be isolated in.
    runtimeRequestTimeout : str, default is Undefined, optional
        RuntimeRequestTimeout is timeout for runtime requests on - pull, logs, exec and attach
    seccompDefault : bool, default is Undefined, optional
        SeccompDefault enables the use of `RuntimeDefault` as the default seccomp profile for all workloads.
    seccompProfileRoot : str, default is Undefined, optional
        SeccompProfileRoot is the directory path for seccomp profiles.
    serializeImagePulls : bool, default is Undefined, optional
        SerializeImagePulls when enabled, tells the Kubelet to pull images one at a time.
    shutdownGracePeriod : str, default is Undefined, optional
        ShutdownGracePeriod specifies the total duration that the node should delay the shutdown by. Default: 30s
    shutdownGracePeriodCriticalPods : str, default is Undefined, optional
        ShutdownGracePeriodCriticalPods specifies the duration used to terminate critical pods during a node shutdown. Default: 10s
    streamingConnectionIdleTimeout : str, default is Undefined, optional
        StreamingConnectionIdleTimeout is the maximum time a streaming connection can be idle before the connection is automatically closed
    systemCgroups : str, default is Undefined, optional
        SystemCgroups is absolute name of cgroups in which to place all non-kernel processes that are not already in a container. Empty for no container. Rolling back the flag requires a reboot.
    systemReserved : {str:str}, default is Undefined, optional
        Capture resource reservation for OS system daemons like sshd, udev, etc.
    systemReservedCgroup : str, default is Undefined, optional
        Parent control group for OS system daemons.
    taints : [str], default is Undefined, optional
        Taints to add when registering a node in the cluster
    tlsCertFile : str, default is Undefined, optional
        TODO: Remove unused TLSCertFile
    tlsCipherSuites : [str], default is Undefined, optional
        TLSCipherSuites indicates the allowed TLS cipher suite
    tlsMinVersion : str, default is Undefined, optional
        TLSMinVersion indicates the minimum TLS version allowed
    tlsPrivateKeyFile : str, default is Undefined, optional
        TODO: Remove unused TLSPrivateKeyFile
    topologyManagerPolicy : str, default is Undefined, optional
        TopologyManagerPolicy determines the allocation policy for the topology manager.
    volumePluginDirectory : str, default is Undefined, optional
        The full path of the directory in which to search for additional third party volume plugins (this path must be writeable, dependent on your choice of OS)
    volumeStatsAggPeriod : str, default is Undefined, optional
        VolumeStatsAggPeriod is the interval for kubelet to calculate and cache the volume disk usage for all pods and volumes
    """


    allowPrivileged?: bool

    allowedUnsafeSysctls?: [str]

    anonymousAuth?: bool

    apiServers?: str

    authenticationTokenWebhook?: bool

    authenticationTokenWebhookCacheTtl?: str

    authorizationMode?: str

    babysitDaemons?: bool

    bootstrapKubeconfig?: str

    cgroupDriver?: str

    cgroupRoot?: str

    clientCaFile?: str

    cloudProvider?: str

    clusterDNS?: str

    clusterDomain?: str

    configureCbr0?: bool

    containerLogMaxFiles?: int

    containerLogMaxSize?: str

    cpuCFSQuota?: bool

    cpuCFSQuotaPeriod?: str

    cpuManagerPolicy?: str

    dockerDisableSharedPID?: bool

    enableCadvisorJsonEndpoints?: bool

    enableCustomMetrics?: bool

    enableDebuggingHandlers?: bool

    enforceNodeAllocatable?: str

    eventBurst?: int

    eventQPS?: int

    evictionHard?: str

    evictionMaxPodGracePeriod?: int

    evictionMinimumReclaim?: str

    evictionPressureTransitionPeriod?: str

    evictionSoft?: str

    evictionSoftGracePeriod?: str

    experimentalAllocatableIgnoreEviction?: bool

    experimentalAllowedUnsafeSysctls?: [str]

    failSwapOn?: bool

    featureGates?: {str:str}

    hairpinMode?: str

    hostnameOverride?: str

    housekeepingInterval?: str

    imageGCHighThresholdPercent?: int

    imageGCLowThresholdPercent?: int

    imagePullProgressDeadline?: str

    kernelMemcgNotification?: bool

    kubeReserved?: {str:str}

    kubeReservedCgroup?: str

    kubeconfigPath?: str

    kubeletCgroups?: str

    logFormat?: str

    logLevel?: int

    maxPods?: int

    memorySwapBehavior?: str

    networkPluginMTU?: int

    networkPluginName?: str

    nodeLabels?: {str:str}

    nodeStatusUpdateFrequency?: str

    nonMasqueradeCIDR?: str

    nvidiaGPUs?: int

    podCIDR?: str

    podInfraContainerImage?: str

    podManifestPath?: str

    podPidsLimit?: int

    protectKernelDefaults?: bool

    readOnlyPort?: int

    reconcileCIDR?: bool

    registerNode?: bool

    registerSchedulable?: bool

    registryBurst?: int

    registryPullQPS?: int

    requireKubeconfig?: bool

    resolvConf?: str

    rootDir?: str

    rotateCertificates?: bool

    runtimeCgroups?: str

    runtimeRequestTimeout?: str

    seccompDefault?: bool

    seccompProfileRoot?: str

    serializeImagePulls?: bool

    shutdownGracePeriod?: str

    shutdownGracePeriodCriticalPods?: str

    streamingConnectionIdleTimeout?: str

    systemCgroups?: str

    systemReserved?: {str:str}

    systemReservedCgroup?: str

    taints?: [str]

    tlsCertFile?: str

    tlsCipherSuites?: [str]

    tlsMinVersion?: str

    tlsPrivateKeyFile?: str

    topologyManagerPolicy?: str

    volumePluginDirectory?: str

    volumeStatsAggPeriod?: str


schema KopsK8sIoV1alpha2ClusterSpecMetricsServer:
    """
    MetricsServer determines the metrics server configuration.

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enabled enables the metrics server. Default: false
    image : str, default is Undefined, optional
        Image is the container image used. Default: the latest supported image for the specified kubernetes version.
    insecure : bool, default is Undefined, optional
        Insecure determines if API server will validate metrics server TLS cert. Default: true
    """


    enabled?: bool

    image?: str

    insecure?: bool


schema KopsK8sIoV1alpha2ClusterSpecNetworking:
    """
    Networking configuration

    Attributes
    ----------
    amazonvpc : KopsK8sIoV1alpha2ClusterSpecNetworkingAmazonvpc, default is Undefined, optional
        amazonvpc
    calico : KopsK8sIoV1alpha2ClusterSpecNetworkingCalico, default is Undefined, optional
        calico
    canal : KopsK8sIoV1alpha2ClusterSpecNetworkingCanal, default is Undefined, optional
        canal
    cilium : KopsK8sIoV1alpha2ClusterSpecNetworkingCilium, default is Undefined, optional
        cilium
    classic : any, default is Undefined, optional
        ClassicNetworkingSpec is the specification of classic networking mode, integrated into kubernetes. Support been removed since Kubernetes 1.4.
    cni : KopsK8sIoV1alpha2ClusterSpecNetworkingCni, default is Undefined, optional
        cni
    external : any, default is Undefined, optional
        ExternalNetworkingSpec is the specification for networking that is implemented by a user-provided Daemonset that uses the Kubenet kubelet networking plugin.
    flannel : KopsK8sIoV1alpha2ClusterSpecNetworkingFlannel, default is Undefined, optional
        flannel
    gce : any, default is Undefined, optional
        GCPNetworkingSpec is the specification of GCP's native networking mode, using IP aliases.
    kopeio : any, default is Undefined, optional
        KopeioNetworkingSpec declares that we want Kopeio networking
    kubenet : any, default is Undefined, optional
        KubenetNetworkingSpec is the specification for kubenet networking, largely integrated but intended to replace classic
    kuberouter : any, default is Undefined, optional
        KuberouterNetworkingSpec declares that we want Kube-router networking
    lyftvpc : KopsK8sIoV1alpha2ClusterSpecNetworkingLyftvpc, default is Undefined, optional
        lyftvpc
    romana : KopsK8sIoV1alpha2ClusterSpecNetworkingRomana, default is Undefined, optional
        romana
    weave : KopsK8sIoV1alpha2ClusterSpecNetworkingWeave, default is Undefined, optional
        weave
    """


    amazonvpc?: KopsK8sIoV1alpha2ClusterSpecNetworkingAmazonvpc

    calico?: KopsK8sIoV1alpha2ClusterSpecNetworkingCalico

    canal?: KopsK8sIoV1alpha2ClusterSpecNetworkingCanal

    cilium?: KopsK8sIoV1alpha2ClusterSpecNetworkingCilium

    classic?: any

    cni?: KopsK8sIoV1alpha2ClusterSpecNetworkingCni

    external?: any

    flannel?: KopsK8sIoV1alpha2ClusterSpecNetworkingFlannel

    gce?: any

    kopeio?: any

    kubenet?: any

    kuberouter?: any

    lyftvpc?: KopsK8sIoV1alpha2ClusterSpecNetworkingLyftvpc

    romana?: KopsK8sIoV1alpha2ClusterSpecNetworkingRomana

    weave?: KopsK8sIoV1alpha2ClusterSpecNetworkingWeave


    check:


schema KopsK8sIoV1alpha2ClusterSpecNetworkingAmazonvpc:
    """
    AmazonVPCNetworkingSpec declares that we want Amazon VPC CNI networking

    Attributes
    ----------
    env : [KopsK8sIoV1alpha2ClusterSpecNetworkingAmazonvpcEnvItems0], default is Undefined, optional
        Env is a list of environment variables to set in the container.
    imageName : str, default is Undefined, optional
        ImageName is the container image name to use.
    initImageName : str, default is Undefined, optional
        InitImageName is the init container image name to use.
    """


    env?: [KopsK8sIoV1alpha2ClusterSpecNetworkingAmazonvpcEnvItems0]

    imageName?: str

    initImageName?: str


schema KopsK8sIoV1alpha2ClusterSpecNetworkingAmazonvpcEnvItems0:
    """
    EnvVar represents an environment variable present in a Container.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the environment variable. Must be a C_IDENTIFIER.
    value : str, default is Undefined, optional
        Variable references $(VAR_NAME) are expanded using the previous defined environment variables in the container and any service environment variables. If a variable cannot be resolved, the reference in the input string will be unchanged. The $(VAR_NAME) syntax can be escaped with a double $$, ie: $$(VAR_NAME). Escaped references will never be expanded, regardless of whether the variable exists or not. Defaults to "".
    """


    name: str

    value?: str


schema KopsK8sIoV1alpha2ClusterSpecNetworkingCalico:
    """
    CalicoNetworkingSpec declares that we want Calico networking

    Attributes
    ----------
    allowIPForwarding : bool, default is Undefined, optional
        AllowIPForwarding enable ip_forwarding setting within the container namespace. (default: false)
    awsSrcDstCheck : str, default is Undefined, optional
        AWSSrcDstCheck enables/disables ENI source/destination checks (AWS IPv4 only) Options: Disable (default for IPv4), Enable, or DoNothing
    bpfEnabled : bool, default is Undefined, optional
        BPFEnabled enables the eBPF dataplane mode.
    bpfExternalServiceMode : str, default is Undefined, optional
        BPFExternalServiceMode controls how traffic from outside the cluster to NodePorts and ClusterIPs is handled. In Tunnel mode, packet is tunneled from the ingress host to the host with the backing pod and back again. In DSR mode, traffic is tunneled to the host with the backing pod and then returned directly; this requires a network that allows direct return. Default: Tunnel (other options: DSR)
    bpfKubeProxyIptablesCleanupEnabled : bool, default is Undefined, optional
        BPFKubeProxyIptablesCleanupEnabled controls whether Felix will clean up the iptables rules created by the Kubernetes kube-proxy; should only be enabled if kube-proxy is not running.
    bpfLogLevel : str, default is Undefined, optional
        BPFLogLevel controls the log level used by the BPF programs. The logs are emitted to the BPF trace pipe, accessible with the command tc exec BPF debug. Default: Off (other options: Info, Debug)
    chainInsertMode : str, default is Undefined, optional
        ChainInsertMode controls whether Felix inserts rules to the top of iptables chains, or appends to the bottom. Leaving the default option is safest to prevent accidentally breaking connectivity. Default: 'insert' (other options: 'append')
    cpuRequest : int | str, default is Undefined, optional
        CPURequest CPU request of Calico container. Default: 100m
    crossSubnet : bool, default is Undefined, optional
        CrossSubnet is deprecated as of kOps 1.22 and has no effect
    encapsulationMode : str, default is Undefined, optional
        EncapsulationMode specifies the network packet encapsulation protocol for Calico to use, employing such encapsulation at the necessary scope per the related CrossSubnet field. In "ipip" mode, Calico will use IP-in-IP encapsulation as needed. In "vxlan" mode, Calico will encapsulate packets as needed using the VXLAN scheme. Options: ipip (default) or vxlan
    ipipMode : str, default is Undefined, optional
        IPIPMode determines when to use IP-in-IP encapsulation for the default Calico IPv4 pool. It is conveyed to the "calico-node" daemon container via the CALICO_IPV4POOL_IPIP environment variable. EncapsulationMode must be set to "ipip". Options: "CrossSubnet", "Always", or "Never". Default: "CrossSubnet" if EncapsulationMode is "ipip", "Never" otherwise.
    iptablesBackend : str, default is Undefined, optional
        IptablesBackend controls which variant of iptables binary Felix uses Default: Auto (other options: Legacy, NFT)
    ipv4AutoDetectionMethod : str, default is Undefined, optional
        IPv4AutoDetectionMethod configures how Calico chooses the IP address used to route between nodes.  This should be set when the host has multiple interfaces and it is important to select the interface used. Options: "first-found" (default), "can-reach=DESTINATION", "interface=INTERFACE-REGEX", or "skip-interface=INTERFACE-REGEX"
    ipv6AutoDetectionMethod : str, default is Undefined, optional
        IPv6AutoDetectionMethod configures how Calico chooses the IP address used to route between nodes.  This should be set when the host has multiple interfaces and it is important to select the interface used. Options: "first-found" (default), "can-reach=DESTINATION", "interface=INTERFACE-REGEX", or "skip-interface=INTERFACE-REGEX"
    logSeverityScreen : str, default is Undefined, optional
        LogSeverityScreen lets us set the desired log level. (Default: info)
    majorVersion : str, default is Undefined, optional
        MajorVersion is unused.
    mtu : int, default is Undefined, optional
        MTU to be set in the cni-network-config for calico.
    prometheusGoMetricsEnabled : bool, default is Undefined, optional
        PrometheusGoMetricsEnabled enables Prometheus Go runtime metrics collection
    prometheusMetricsEnabled : bool, default is Undefined, optional
        PrometheusMetricsEnabled can be set to enable the experimental Prometheus metrics server (default: false)
    prometheusMetricsPort : int, default is Undefined, optional
        PrometheusMetricsPort is the TCP port that the experimental Prometheus metrics server should bind to (default: 9091)
    prometheusProcessMetricsEnabled : bool, default is Undefined, optional
        PrometheusProcessMetricsEnabled enables Prometheus process metrics collection
    registry : str, default is Undefined, optional
        Registry overrides the Calico container image registry.
    typhaPrometheusMetricsEnabled : bool, default is Undefined, optional
        TyphaPrometheusMetricsEnabled enables Prometheus metrics collection from Typha (default: false)
    typhaPrometheusMetricsPort : int, default is Undefined, optional
        TyphaPrometheusMetricsPort is the TCP port the typha Prometheus metrics server should bind to (default: 9093)
    typhaReplicas : int, default is Undefined, optional
        TyphaReplicas is the number of replicas of Typha to deploy
    version : str, default is Undefined, optional
        Version overrides the Calico container image tag.
    vxlanMode : str, default is Undefined, optional
        VXLANMode determines when to use VXLAN encapsulation for the default Calico IPv4 pool. It is conveyed to the "calico-node" daemon container via the CALICO_IPV4POOL_VXLAN environment variable. EncapsulationMode must be set to "vxlan". Options: "CrossSubnet", "Always", or "Never". Default: "CrossSubnet" if EncapsulationMode is "vxlan", "Never" otherwise.
    wireguardEnabled : bool, default is Undefined, optional
        WireguardEnabled enables WireGuard encryption for all on-the-wire pod-to-pod traffic (default: false)
    """


    allowIPForwarding?: bool

    awsSrcDstCheck?: str

    bpfEnabled?: bool

    bpfExternalServiceMode?: str

    bpfKubeProxyIptablesCleanupEnabled?: bool

    bpfLogLevel?: str

    chainInsertMode?: str

    cpuRequest?: int | str

    crossSubnet?: bool

    encapsulationMode?: str

    ipipMode?: str

    iptablesBackend?: str

    ipv4AutoDetectionMethod?: str

    ipv6AutoDetectionMethod?: str

    logSeverityScreen?: str

    majorVersion?: str

    mtu?: int

    prometheusGoMetricsEnabled?: bool

    prometheusMetricsEnabled?: bool

    prometheusMetricsPort?: int

    prometheusProcessMetricsEnabled?: bool

    registry?: str

    typhaPrometheusMetricsEnabled?: bool

    typhaPrometheusMetricsPort?: int

    typhaReplicas?: int

    version?: str

    vxlanMode?: str

    wireguardEnabled?: bool


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecNetworkingCanal:
    """
    CanalNetworkingSpec declares that we want Canal networking

    Attributes
    ----------
    chainInsertMode : str, default is Undefined, optional
        ChainInsertMode controls whether Felix inserts rules to the top of iptables chains, or appends to the bottom. Leaving the default option is safest to prevent accidentally breaking connectivity. Default: 'insert' (other options: 'append')
    cpuRequest : int | str, default is Undefined, optional
        CPURequest CPU request of Canal container. Default: 100m
    defaultEndpointToHostAction : str, default is Undefined, optional
        DefaultEndpointToHostAction allows users to configure the default behaviour for traffic between pod to host after calico rules have been processed. Default: ACCEPT (other options: DROP, RETURN)
    disableFlannelForwardRules : bool, default is Undefined, optional
        DisableFlannelForwardRules configures Flannel to NOT add the default ACCEPT traffic rules to the iptables FORWARD chain
    disableTxChecksumOffloading : bool, default is Undefined, optional
        DisableTxChecksumOffloading is unused.
    iptablesBackend : str, default is Undefined, optional
        IptablesBackend controls which variant of iptables binary Felix uses Default: Auto (other options: Legacy, NFT)
    logSeveritySys : str, default is Undefined, optional
        LogSeveritySys the severity to set for logs which are sent to syslog Default: INFO (other options: DEBUG, WARNING, ERROR, CRITICAL, NONE)
    mtu : int, default is Undefined, optional
        MTU to be set in the cni-network-config (default: 1500)
    prometheusGoMetricsEnabled : bool, default is Undefined, optional
        PrometheusGoMetricsEnabled enables Prometheus Go runtime metrics collection
    prometheusMetricsEnabled : bool, default is Undefined, optional
        PrometheusMetricsEnabled can be set to enable the experimental Prometheus metrics server (default: false)
    prometheusMetricsPort : int, default is Undefined, optional
        PrometheusMetricsPort is the TCP port that the experimental Prometheus metrics server should bind to (default: 9091)
    prometheusProcessMetricsEnabled : bool, default is Undefined, optional
        PrometheusProcessMetricsEnabled enables Prometheus process metrics collection
    typhaPrometheusMetricsEnabled : bool, default is Undefined, optional
        TyphaPrometheusMetricsEnabled enables Prometheus metrics collection from Typha (default: false)
    typhaPrometheusMetricsPort : int, default is Undefined, optional
        TyphaPrometheusMetricsPort is the TCP port the typha Prometheus metrics server should bind to (default: 9093)
    typhaReplicas : int, default is Undefined, optional
        TyphaReplicas is the number of replicas of Typha to deploy
    """


    chainInsertMode?: str

    cpuRequest?: int | str

    defaultEndpointToHostAction?: str

    disableFlannelForwardRules?: bool

    disableTxChecksumOffloading?: bool

    iptablesBackend?: str

    logSeveritySys?: str

    mtu?: int

    prometheusGoMetricsEnabled?: bool

    prometheusMetricsEnabled?: bool

    prometheusMetricsPort?: int

    prometheusProcessMetricsEnabled?: bool

    typhaPrometheusMetricsEnabled?: bool

    typhaPrometheusMetricsPort?: int

    typhaReplicas?: int


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecNetworkingCilium:
    """
    CiliumNetworkingSpec declares that we want Cilium networking

    Attributes
    ----------
    IPTablesRulesNoinstall : bool, default is Undefined, optional
        IPTablesRulesNoinstall disables installing the base IPTables rules used for masquerading and kube-proxy. Default: false
    accessLog : str, default is Undefined, optional
        AccessLog is unused.
    agentLabels : [str], default is Undefined, optional
        AgentLabels is unused.
    agentPodAnnotations : {str:str}, default is Undefined, optional
        AgentPodAnnotations makes possible to add additional annotations to the cilium agent. Default: none
    agentPrometheusPort : int, default is Undefined, optional
        AgentPrometheusPort is the port to listen to for Prometheus metrics. Defaults to 9090.
    allowLocalhost : str, default is Undefined, optional
        AllowLocalhost is unused.
    autoDirectNodeRoutes : bool, default is Undefined, optional
        AutoDirectNodeRoutes adds automatic L2 routing between nodes. Default: false
    autoIpv6NodeRoutes : bool, default is Undefined, optional
        AutoIpv6NodeRoutes is unused.
    bpfCTGlobalAnyMax : int, default is Undefined, optional
        BPFCTGlobalAnyMax is the maximum number of entries in the non-TCP CT table. Default: 262144
    bpfCTGlobalTCPMax : int, default is Undefined, optional
        BPFCTGlobalTCPMax is the maximum number of entries in the TCP CT table. Default: 524288
    bpfLBAlgorithm : str, default is Undefined, optional
        BPFLBAlgorithm is the load balancing algorithm ("random", "maglev"). Default: random
    bpfLBMaglevTableSize : str, default is Undefined, optional
        BPFLBMaglevTableSize is the per service backend table size when going with Maglev (parameter M). Default: 16381
    bpfLBMapMax : int, default is Undefined, optional
        BPFLBMapMax is the maximum number of entries in bpf lb service, backend and affinity maps. Default: 65536
    bpfLBSockHostNSOnly : bool, default is Undefined, optional
        BPFLBSockHostNSOnly enables skipping socket LB for services when inside a pod namespace, in favor of service LB at the pod interface. Socket LB is still used when in the host namespace. Required by service mesh (e.g., Istio, Linkerd). Default: false
    bpfNATGlobalMax : int, default is Undefined, optional
        BPFNATGlobalMax is the the maximum number of entries in the BPF NAT table. Default: 524288
    bpfNeighGlobalMax : int, default is Undefined, optional
        BPFNeighGlobalMax is the the maximum number of entries in the BPF Neighbor table. Default: 524288
    bpfPolicyMapMax : int, default is Undefined, optional
        BPFPolicyMapMax is the maximum number of entries in endpoint policy map. Default: 16384
    bpfRoot : str, default is Undefined, optional
        BPFRoot is unused.
    chainingMode : str, default is Undefined, optional
        ChainingMode allows using Cilium in combination with other CNI plugins. With Cilium CNI chaining, the base network connectivity and IP address management is managed by the non-Cilium CNI plugin, but Cilium attaches eBPF programs to the network devices created by the non-Cilium plugin to provide L3/L4 network visibility, policy enforcement and other advanced features. Default: none
    clusterID : int, default is Undefined, optional
        ClusterID is the ID of the cluster. It is only relevant when building a mesh of clusters. Must be a number between 1 and 255.
    clusterName : str, default is Undefined, optional
        ClusterName is the name of the cluster. It is only relevant when building a mesh of clusters.
    cniBinPath : str, default is Undefined, optional
        CniBinPath is unused.
    containerRuntime : [str], default is Undefined, optional
        ContainerRuntime is unused.
    containerRuntimeEndpoint : {str:str}, default is Undefined, optional
        ContainerRuntimeEndpoint is unused.
    containerRuntimeLabels : str, default is Undefined, optional
        ContainerRuntimeLabels is unused.
    cpuRequest : int | str, default is Undefined, optional
        CPURequest CPU request of Cilium agent + operator container. (default: 25m)
    debug : bool, default is Undefined, optional
        Debug runs Cilium in debug mode.
    debugVerbose : [str], default is Undefined, optional
        DebugVerbose is unused.
    device : str, default is Undefined, optional
        Device is unused.
    disableCNPStatusUpdates : bool, default is Undefined, optional
        DisableCNPStatusUpdates determines if CNP NodeStatus updates will be sent to the Kubernetes api-server.
    disableConntrack : bool, default is Undefined, optional
        DisableConntrack is unused.
    disableEndpointCRD : bool, default is Undefined, optional
        DisableEndpointCRD disables usage of CiliumEndpoint CRD. Default: false
    disableIpv4 : bool, default is Undefined, optional
        DisableIpv4 is unused.
    disableK8sServices : bool, default is Undefined, optional
        DisableK8sServices is unused.
    disableMasquerade : bool, default is Undefined, optional
        DisableMasquerade disables masquerading traffic to external destinations behind the node IP.
    enableBPFMasquerade : bool, default is Undefined, optional
        EnableBPFMasquerade enables masquerading packets from endpoints leaving the host with BPF instead of iptables. Default: false
    enableEncryption : bool, default is Undefined, optional
        EnableEncryption enables Cilium Encryption. Default: false
    enableEndpointHealthChecking : bool, default is Undefined, optional
        EnableEndpointHealthChecking enables connectivity health checking between virtual endpoints. Default: true
    enableHostReachableServices : bool, default is Undefined, optional
        EnableHostReachableServices configures Cilium to enable services to be reached from the host namespace in addition to pod namespaces. https://docs.cilium.io/en/v1.9/gettingstarted/host-services/ Default: false
    enableL7Proxy : bool, default is Undefined, optional
        EnableL7Proxy enables L7 proxy for L7 policy enforcement. Default: true
    enableNodePort : bool, default is Undefined, optional
        EnableNodePort replaces kube-proxy with Cilium's BPF implementation. Requires spec.kubeProxy.enabled be set to false. Default: false
    enablePolicy : str, default is Undefined, optional
        EnablePolicy specifies the policy enforcement mode. "default": Follows Kubernetes policy enforcement. "always": Cilium restricts all traffic if no policy is in place. "never": Cilium allows all traffic regardless of policies in place. If unspecified, "default" policy mode will be used.
    enablePrometheusMetrics : bool, default is Undefined, optional
        EnablePrometheusMetrics enables the Cilium "/metrics" endpoint for both the agent and the operator.
    enableRemoteNodeIdentity : bool, default is Undefined, optional
        EnableRemoteNodeIdentity enables the remote-node-identity. Default: true
    enableServiceTopology : bool, default is Undefined, optional
        EnableServiceTopology determine if cilium should use topology aware hints.
    enableTracing : bool, default is Undefined, optional
        EnableTracing is unused.
    enableUnreachableRoutes : bool, default is Undefined, optional
        EnableUnreachableRoutes enables unreachable routes on pod deletion. Default: false
    enableipv4 : bool, default is Undefined, optional
        EnableIpv4 is unused.
    enableipv6 : bool, default is Undefined, optional
        EnableIpv6 is unused.
    encryptionType : str, default is Undefined, optional
        EncryptionType specifies Cilium Encryption method ("ipsec", "wireguard"). Default: ipsec
    envoyLog : str, default is Undefined, optional
        EnvoyLog is unused.
    etcdManaged : bool, default is Undefined, optional
        EtcdManagd installs an additional etcd cluster that is used for Cilium state change. The cluster is operated by cilium-etcd-operator. Default: false
    hubble : KopsK8sIoV1alpha2ClusterSpecNetworkingCiliumHubble, default is Undefined, optional
        hubble
    identityAllocationMode : str, default is Undefined, optional
        IdentityAllocationMode specifies in which backend identities are stored ("crd", "kvstore"). Default: crd
    identityChangeGracePeriod : str, default is Undefined, optional
        IdentityChangeGracePeriod specifies the duration to wait before using a changed identity. Default: 5s
    ingress : KopsK8sIoV1alpha2ClusterSpecNetworkingCiliumIngress, default is Undefined, optional
        ingress
    ipam : str, default is Undefined, optional
        IPAM specifies the IP address allocation mode to use. Possible values are "crd" and "eni". "eni" will use AWS native networking for pods. Eni requires masquerade to be set to false. "crd" will use CRDs for controlling IP address management. "hostscope" will use hostscope IPAM mode. "kubernetes" will use addersing based on node pod CIDR. Default: "kubernetes".
    ipv4ClusterCidrMaskSize : int, default is Undefined, optional
        Ipv4ClusterCIDRMaskSize is unused.
    ipv4Node : str, default is Undefined, optional
        Ipv4Node is unused.
    ipv4Range : str, default is Undefined, optional
        Ipv4Range is unused.
    ipv4ServiceRange : str, default is Undefined, optional
        Ipv4ServiceRange is unused.
    ipv6ClusterAllocCidr : str, default is Undefined, optional
        Ipv6ClusterAllocCidr is unused.
    ipv6Node : str, default is Undefined, optional
        Ipv6Node is unused.
    ipv6Range : str, default is Undefined, optional
        Ipv6Range is unused.
    ipv6ServiceRange : str, default is Undefined, optional
        Ipv6ServiceRange is unused.
    k8sApiServer : str, default is Undefined, optional
        K8sAPIServer is unused.
    k8sKubeconfigPath : str, default is Undefined, optional
        K8sKubeconfigPath is unused.
    keepBpfTemplates : bool, default is Undefined, optional
        KeepBPFTemplates is unused.
    keepConfig : bool, default is Undefined, optional
        KeepConfig is unused.
    labelPrefixFile : str, default is Undefined, optional
        LabelPrefixFile is unused.
    labels : [str], default is Undefined, optional
        Labels is unused.
    lb : str, default is Undefined, optional
        LB is unused.
    libDir : str, default is Undefined, optional
        LibDir is unused.
    logDriver : [str], default is Undefined, optional
        LogDrivers is unused.
    logOpt : {str:str}, default is Undefined, optional
        LogOpt is unused.
    logstash : bool, default is Undefined, optional
        Logstash is unused.
    logstashAgent : str, default is Undefined, optional
        LogstashAgent is unused.
    logstashProbeTimer : int, default is Undefined, optional
        LogstashProbeTimer is unused.
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest memory request of Cilium agent + operator container. (default: 128Mi)
    metrics : [str], default is Undefined, optional
        Metrics is a list of metrics to add or remove from the default list of metrics the agent exposes.
    monitorAggregation : str, default is Undefined, optional
        MonitorAggregation sets the level of packet monitoring. Possible values are "low", "medium", or "maximum". Default: medium
    nat46Range : str, default is Undefined, optional
        Nat46Range is unused.
    nodeEncryption : bool, default is Undefined, optional
        NodeEncryption enables encryption for pure node to node traffic. Default: false
    nodeInitBootstrapFile : str, default is Undefined, optional
        NodeInitBootstrapFile is unused.
    operatorPodAnnotations : {str:str}, default is Undefined, optional
        OperatorPodAnnotations makes possible to add additional annotations to cilium operator. Default: none
    pprof : bool, default is Undefined, optional
        Pprof is unused.
    preallocateBPFMaps : bool, default is Undefined, optional
        PreallocateBPFMaps reduces the per-packet latency at the expense of up-front memory allocation. Default: true
    prefilterDevice : str, default is Undefined, optional
        PrefilterDevice is unused.
    prometheusServeAddr : str, default is Undefined, optional
        PrometheusServeAddr is unused.
    reconfigureKubelet : bool, default is Undefined, optional
        ReconfigureKubelet is unused.
    registry : str, default is Undefined, optional
        Registry overrides the default Cilium container registry (quay.io)
    removeCbrBridge : bool, default is Undefined, optional
        RemoveCbrBridge is unused.
    restartPods : bool, default is Undefined, optional
        RestartPods is unused.
    restore : bool, default is Undefined, optional
        Restore is unused.
    sidecarIstioProxyImage : str, default is Undefined, optional
        SidecarIstioProxyImage is the regular expression matching compatible Istio sidecar istio-proxy container image names. Default: cilium/istio_proxy
    singleClusterRoute : bool, default is Undefined, optional
        SingleClusterRoute is unused.
    socketPath : str, default is Undefined, optional
        SocketPath is unused.
    stateDir : str, default is Undefined, optional
        StateDir is unused.
    toFqdnsDnsRejectResponseCode : str, default is Undefined, optional
        ToFQDNsDNSRejectResponseCode sets the DNS response code for rejecting DNS requests. Possible values are "nameError" or "refused". Default: refused
    toFqdnsEnablePoller : bool, default is Undefined, optional
        ToFQDNsEnablePoller replaces the DNS proxy-based implementation of FQDN policies with the less powerful legacy implementation. Default: false
    tracePayloadlen : int, default is Undefined, optional
        TracePayloadLen is unused.
    tunnel : str, default is Undefined, optional
        Tunnel specifies the Cilium tunnelling mode. Possible values are "vxlan", "geneve", or "disabled". Default: vxlan
    version : str, default is Undefined, optional
        Version is the version of the Cilium agent and the Cilium Operator.
    """


    IPTablesRulesNoinstall?: bool

    accessLog?: str

    agentLabels?: [str]

    agentPodAnnotations?: {str:str}

    agentPrometheusPort?: int

    allowLocalhost?: str

    autoDirectNodeRoutes?: bool

    autoIpv6NodeRoutes?: bool

    bpfCTGlobalAnyMax?: int

    bpfCTGlobalTCPMax?: int

    bpfLBAlgorithm?: str

    bpfLBMaglevTableSize?: str

    bpfLBMapMax?: int

    bpfLBSockHostNSOnly?: bool

    bpfNATGlobalMax?: int

    bpfNeighGlobalMax?: int

    bpfPolicyMapMax?: int

    bpfRoot?: str

    chainingMode?: str

    clusterID?: int

    clusterName?: str

    cniBinPath?: str

    containerRuntime?: [str]

    containerRuntimeEndpoint?: {str:str}

    containerRuntimeLabels?: str

    cpuRequest?: int | str

    debug?: bool

    debugVerbose?: [str]

    device?: str

    disableCNPStatusUpdates?: bool

    disableConntrack?: bool

    disableEndpointCRD?: bool

    disableIpv4?: bool

    disableK8sServices?: bool

    disableMasquerade?: bool

    enableBPFMasquerade?: bool

    enableEncryption?: bool

    enableEndpointHealthChecking?: bool

    enableHostReachableServices?: bool

    enableL7Proxy?: bool

    enableNodePort?: bool

    enablePolicy?: str

    enablePrometheusMetrics?: bool

    enableRemoteNodeIdentity?: bool

    enableServiceTopology?: bool

    enableTracing?: bool

    enableUnreachableRoutes?: bool

    enableipv4?: bool

    enableipv6?: bool

    encryptionType?: str

    envoyLog?: str

    etcdManaged?: bool

    hubble?: KopsK8sIoV1alpha2ClusterSpecNetworkingCiliumHubble

    identityAllocationMode?: str

    identityChangeGracePeriod?: str

    ingress?: KopsK8sIoV1alpha2ClusterSpecNetworkingCiliumIngress

    ipam?: str

    ipv4ClusterCidrMaskSize?: int

    ipv4Node?: str

    ipv4Range?: str

    ipv4ServiceRange?: str

    ipv6ClusterAllocCidr?: str

    ipv6Node?: str

    ipv6Range?: str

    ipv6ServiceRange?: str

    k8sApiServer?: str

    k8sKubeconfigPath?: str

    keepBpfTemplates?: bool

    keepConfig?: bool

    labelPrefixFile?: str

    labels?: [str]

    lb?: str

    libDir?: str

    logDriver?: [str]

    logOpt?: {str:str}

    logstash?: bool

    logstashAgent?: str

    logstashProbeTimer?: int

    memoryRequest?: int | str

    metrics?: [str]

    monitorAggregation?: str

    nat46Range?: str

    nodeEncryption?: bool

    nodeInitBootstrapFile?: str

    operatorPodAnnotations?: {str:str}

    pprof?: bool

    preallocateBPFMaps?: bool

    prefilterDevice?: str

    prometheusServeAddr?: str

    reconfigureKubelet?: bool

    registry?: str

    removeCbrBridge?: bool

    restartPods?: bool

    restore?: bool

    sidecarIstioProxyImage?: str

    singleClusterRoute?: bool

    socketPath?: str

    stateDir?: str

    toFqdnsDnsRejectResponseCode?: str

    toFqdnsEnablePoller?: bool

    tracePayloadlen?: int

    tunnel?: str

    version?: str


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecNetworkingCiliumHubble:
    """
    Hubble configures the Hubble service on the Cilium agent.

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enabled decides if Hubble is enabled on the agent or not
    metrics : [str], default is Undefined, optional
        Metrics is a list of metrics to collect. If empty or null, metrics are disabled. See https://docs.cilium.io/en/stable/configuration/metrics/#hubble-exported-metrics
    """


    enabled?: bool

    metrics?: [str]


schema KopsK8sIoV1alpha2ClusterSpecNetworkingCiliumIngress:
    """
    Ingress specifies the configuration for Cilium Ingress settings.

    Attributes
    ----------
    defaultLoadBalancerMode : str, default is Undefined, optional
        DefaultLoadBalancerMode specifies the default load balancer mode. Possible values: 'shared' or 'dedicated' Default: dedicated
    enableSecretsSync : bool, default is Undefined, optional
        EnableSecretsSync specifies whether synchronization of secrets is enabled. Default: true
    enabled : bool, default is Undefined, optional
        Enabled specifies whether Cilium Ingress is enabled.
    enforceHttps : bool, default is Undefined, optional
        EnforceHttps specifies whether HTTPS enforcement is enabled for Ingress traffic. Default: true
    loadBalancerAnnotationPrefixes : str, default is Undefined, optional
        LoadBalancerAnnotationPrefixes specifies annotation prefixes for Load Balancer configuration. Default: "service.beta.kubernetes.io service.kubernetes.io cloud.google.com"
    sharedLoadBalancerServiceName : str, default is Undefined, optional
        SharedLoadBalancerServiceName specifies the name of the shared load balancer service. Default: cilium-ingress
    """


    defaultLoadBalancerMode?: str

    enableSecretsSync?: bool

    enabled?: bool

    enforceHttps?: bool

    loadBalancerAnnotationPrefixes?: str

    sharedLoadBalancerServiceName?: str


schema KopsK8sIoV1alpha2ClusterSpecNetworkingCni:
    """
    CNINetworkingSpec is the specification for networking that is implemented by a user-provided Daemonset, which uses the CNI kubelet networking plugin.

    Attributes
    ----------
    usesSecondaryIP : bool, default is Undefined, optional
        uses secondary IP
    """


    usesSecondaryIP?: bool


schema KopsK8sIoV1alpha2ClusterSpecNetworkingFlannel:
    """
    FlannelNetworkingSpec declares that we want Flannel networking

    Attributes
    ----------
    backend : str, default is Undefined, optional
        Backend is the backend overlay type we want to use (vxlan or udp)
    disableTxChecksumOffloading : bool, default is Undefined, optional
        DisableTxChecksumOffloading is unused.
    iptablesResyncSeconds : int, default is Undefined, optional
        IptablesResyncSeconds sets resync period for iptables rules, in seconds
    """


    backend?: str

    disableTxChecksumOffloading?: bool

    iptablesResyncSeconds?: int


schema KopsK8sIoV1alpha2ClusterSpecNetworkingLyftvpc:
    """
    LyftVPCNetworkingSpec declares that we want to use the cni-ipvlan-vpc-k8s CNI networking. Lyft VPC is deprecated as of kOps 1.22 and removed as of kOps 1.23.

    Attributes
    ----------
    subnetTags : {str:str}, default is Undefined, optional
        subnet tags
    """


    subnetTags?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecNetworkingRomana:
    """
    RomanaNetworkingSpec declares that we want Romana networking Romana is deprecated as of kOps 1.18 and removed as of kOps 1.19.

    Attributes
    ----------
    daemonServiceIP : str, default is Undefined, optional
        DaemonServiceIP is the Kubernetes Service IP for the romana-daemon pod
    etcdServiceIP : str, default is Undefined, optional
        EtcdServiceIP is the Kubernetes Service IP for the etcd backend used by Romana
    """


    daemonServiceIP?: str

    etcdServiceIP?: str


schema KopsK8sIoV1alpha2ClusterSpecNetworkingWeave:
    """
    WeaveNetworkingSpec declares that we want Weave networking

    Attributes
    ----------
    connLimit : int, default is Undefined, optional
        conn limit
    cpuLimit : int | str, default is Undefined, optional
        CPULimit CPU limit of weave container.
    cpuRequest : int | str, default is Undefined, optional
        CPURequest CPU request of weave container. Default 50m
    memoryLimit : int | str, default is Undefined, optional
        MemoryLimit memory limit of weave container. Default 200Mi
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest memory request of weave container. Default 200Mi
    mtu : int, default is Undefined, optional
        mtu
    netExtraArgs : str, default is Undefined, optional
        NetExtraArgs are extra arguments that are passed to weave-kube.
    noMasqLocal : int, default is Undefined, optional
        no masq local
    npcCPULimit : int | str, default is Undefined, optional
        NPCCPULimit CPU limit of weave npc container
    npcCPURequest : int | str, default is Undefined, optional
        NPCCPURequest CPU request of weave npc container. Default 50m
    npcExtraArgs : str, default is Undefined, optional
        NPCExtraArgs are extra arguments that are passed to weave-npc.
    npcMemoryLimit : int | str, default is Undefined, optional
        NPCMemoryLimit memory limit of weave npc container. Default 200Mi
    npcMemoryRequest : int | str, default is Undefined, optional
        NPCMemoryRequest memory request of weave npc container. Default 200Mi
    version : str, default is Undefined, optional
        Version specifies the Weave container image tag. The default depends on the kOps version.
    """


    connLimit?: int

    cpuLimit?: int | str

    cpuRequest?: int | str

    memoryLimit?: int | str

    memoryRequest?: int | str

    mtu?: int

    netExtraArgs?: str

    noMasqLocal?: int

    npcCPULimit?: int | str

    npcCPURequest?: int | str

    npcExtraArgs?: str

    npcMemoryLimit?: int | str

    npcMemoryRequest?: int | str

    version?: str


    check:
        matchPattern(cpuLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(npcCPULimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(npcCPURequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(npcMemoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(npcMemoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecNodeAuthorization:
    """
    NodeAuthorization defined the custom node authorization configuration

    Attributes
    ----------
    nodeAuthorizer : KopsK8sIoV1alpha2ClusterSpecNodeAuthorizationNodeAuthorizer, default is Undefined, optional
        node authorizer
    """


    nodeAuthorizer?: KopsK8sIoV1alpha2ClusterSpecNodeAuthorizationNodeAuthorizer


schema KopsK8sIoV1alpha2ClusterSpecNodeAuthorizationNodeAuthorizer:
    """
    NodeAuthorizer defined the configuration for the node authorizer

    Attributes
    ----------
    authorizer : str, default is Undefined, optional
        Authorizer is the authorizer to use
    features : [str], default is Undefined, optional
        Features is a series of authorizer features to enable or disable
    image : str, default is Undefined, optional
        Image is the location of container
    interval : str, default is Undefined, optional
        Interval the time between retires for authorization request
    nodeURL : str, default is Undefined, optional
        NodeURL is the node authorization service url
    port : int, default is Undefined, optional
        Port is the port the service is running on the master
    timeout : str, default is Undefined, optional
        Timeout the max time for authorization request
    tokenTTL : str, default is Undefined, optional
        TokenTTL is the max ttl for an issued token
    """


    authorizer?: str

    features?: [str]

    image?: str

    interval?: str

    nodeURL?: str

    port?: int

    timeout?: str

    tokenTTL?: str


schema KopsK8sIoV1alpha2ClusterSpecNodeProblemDetector:
    """
    NodeProblemDetector determines the node problem detector configuration.

    Attributes
    ----------
    cpuLimit : int | str, default is Undefined, optional
        CPULimit of NodeProblemDetector container. Default: 10m
    cpuRequest : int | str, default is Undefined, optional
        CPURequest of NodeProblemDetector container. Default: 10m
    enabled : bool, default is Undefined, optional
        Enabled enables the NodeProblemDetector. Default: false
    image : str, default is Undefined, optional
        Image is the NodeProblemDetector container image used.
    memoryLimit : int | str, default is Undefined, optional
        MemoryLimit of NodeProblemDetector container. Default: 80Mi
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest of NodeProblemDetector container. Default: 80Mi
    """


    cpuLimit?: int | str

    cpuRequest?: int | str

    enabled?: bool

    image?: str

    memoryLimit?: int | str

    memoryRequest?: int | str


    check:
        matchPattern(cpuLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryLimit, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecNodeTerminationHandler:
    """
    NodeTerminationHandler determines the cluster autoscaler configuration.

    Attributes
    ----------
    cpuRequest : int | str, default is Undefined, optional
        CPURequest of NodeTerminationHandler container. Default: 50m
    enableRebalanceDraining : bool, default is Undefined, optional
        EnableRebalanceDraining makes node termination handler drain nodes when the rebalance recommendation notice is received. Default: false
    enableRebalanceMonitoring : bool, default is Undefined, optional
        EnableRebalanceMonitoring makes node termination handler cordon nodes when the rebalance recommendation notice is received. In queue-processor mode, cannot be enabled without rebalance draining. Default: false
    enableSQSTerminationDraining : bool, default is Undefined, optional
        EnableSQSTerminationDraining enables queue-processor mode which drains nodes when an SQS termination event is received. Default: true
    enableScheduledEventDraining : bool, default is Undefined, optional
        EnableScheduledEventDraining makes node termination handler drain nodes before the maintenance window starts for an EC2 instance scheduled event. Cannot be disabled in queue-processor mode. Default: true
    enableSpotInterruptionDraining : bool, default is Undefined, optional
        EnableSpotInterruptionDraining makes node termination handler drain nodes when spot interruption termination notice is received. Cannot be disabled in queue-processor mode. Default: true
    enabled : bool, default is Undefined, optional
        Enabled enables the node termination handler. Default: true
    excludeFromLoadBalancers : bool, default is Undefined, optional
        ExcludeFromLoadBalancers makes node termination handler will mark for exclusion from load balancers before node are cordoned. Default: true
    managedASGTag : str, default is Undefined, optional
        ManagedASGTag is the tag used to determine which nodes NTH can take action on This field has kept its name even though it now maps to the --managed-tag flag due to keeping the API stable. Node termination handler does no longer check the ASG for this tag, but the actual EC2 instances.
    memoryRequest : int | str, default is Undefined, optional
        MemoryRequest of NodeTerminationHandler container. Default: 64Mi
    prometheusEnable : bool, default is Undefined, optional
        EnablePrometheusMetrics enables the "/metrics" endpoint. Default: false
    version : str, default is Undefined, optional
        Version is the container image tag used.
    """


    cpuRequest?: int | str

    enableRebalanceDraining?: bool

    enableRebalanceMonitoring?: bool

    enableSQSTerminationDraining?: bool

    enableScheduledEventDraining?: bool

    enableSpotInterruptionDraining?: bool

    enabled?: bool

    excludeFromLoadBalancers?: bool

    managedASGTag?: str

    memoryRequest?: int | str

    prometheusEnable?: bool

    version?: str


    check:
        matchPattern(cpuRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)
        matchPattern(memoryRequest, ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$)


schema KopsK8sIoV1alpha2ClusterSpecNtp:
    """
    NTPConfig is the configuration for NTP.

    Attributes
    ----------
    managed : bool, default is Undefined, optional
        Managed controls if the NTP configuration is managed by kOps. The NTP configuration task is skipped if this is set to false.
    """


    managed?: bool


schema KopsK8sIoV1alpha2ClusterSpecPodIdentityWebhook:
    """
    PodIdentityWebhook determines the EKS Pod Identity Webhook configuration.

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        enabled
    replicas : int, default is Undefined, optional
        replicas
    """


    enabled?: bool

    replicas?: int


schema KopsK8sIoV1alpha2ClusterSpecRollingUpdate:
    """
    RollingUpdate defines the default rolling-update settings for instance groups

    Attributes
    ----------
    drainAndTerminate : bool, default is Undefined, optional
        DrainAndTerminate enables draining and terminating nodes during rolling updates. Defaults to true.
    maxSurge : int | str, default is Undefined, optional
        MaxSurge is the maximum number of extra nodes that can be created during the update. The value can be an absolute number (for example 5) or a percentage of desired machines (for example 10%). The absolute number is calculated from a percentage by rounding up. Has no effect on instance groups with role "Master". Defaults to 1 on AWS, 0 otherwise. Example: when this is set to 30%, the InstanceGroup can be scaled up immediately when the rolling update starts, such that the total number of old and new nodes do not exceed 130% of desired nodes.
    maxUnavailable : int | str, default is Undefined, optional
        MaxUnavailable is the maximum number of nodes that can be unavailable during the update. The value can be an absolute number (for example 5) or a percentage of desired nodes (for example 10%). The absolute number is calculated from a percentage by rounding down. Defaults to 1 if MaxSurge is 0, otherwise defaults to 0. Example: when this is set to 30%, the InstanceGroup can be scaled down to 70% of desired nodes immediately when the rolling update starts. Once new nodes are ready, more old nodes can be drained, ensuring that the total number of nodes available at all times during the update is at least 70% of desired nodes.
    """


    drainAndTerminate?: bool

    maxSurge?: int | str

    maxUnavailable?: int | str


schema KopsK8sIoV1alpha2ClusterSpecServiceAccountIssuerDiscovery:
    """
    ServiceAccountIssuerDiscovery configures the OIDC Issuer for ServiceAccounts.

    Attributes
    ----------
    additionalAudiences : [str], default is Undefined, optional
        AdditionalAudiences adds user defined audiences to the provisioned AWS OIDC provider
    discoveryStore : str, default is Undefined, optional
        DiscoveryStore is the VFS path to where OIDC Issuer Discovery metadata is stored.
    enableAWSOIDCProvider : bool, default is Undefined, optional
        EnableAWSOIDCProvider will provision an AWS OIDC provider that trusts the ServiceAccount Issuer
    """


    additionalAudiences?: [str]

    discoveryStore?: str

    enableAWSOIDCProvider?: bool


schema KopsK8sIoV1alpha2ClusterSpecSnapshotController:
    """
    SnapshotController defines the CSI Snapshot Controller configuration.

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        Enabled enables the CSI Snapshot Controller
    installDefaultClass : bool, default is Undefined, optional
        InstallDefaultClass will install the default VolumeSnapshotClass
    """


    enabled?: bool

    installDefaultClass?: bool


schema KopsK8sIoV1alpha2ClusterSpecSubnetsItems0:
    """
    kops k8s io v1alpha2 cluster spec subnets items0

    Attributes
    ----------
    additionalRoutes : [KopsK8sIoV1alpha2ClusterSpecSubnetsItems0AdditionalRoutesItems0], default is Undefined, optional
        AdditionalRoutes to attach to the subnet's route table
    cidr : str, default is Undefined, optional
        CIDR is the IPv4 CIDR block assigned to the subnet.
    egress : str, default is Undefined, optional
        Egress defines the method of traffic egress for this subnet
    id : str, default is Undefined, optional
        ID is the cloud provider ID for the objects associated with the zone (the subnet on AWS).
    ipv6CIDR : str, default is Undefined, optional
        IPv6CIDR is the IPv6 CIDR block assigned to the subnet.
    name : str, default is Undefined, optional
        name
    publicIP : str, default is Undefined, optional
        PublicIP to attach to NatGateway
    region : str, default is Undefined, optional
        Region is the region the subnet is in, set for subnets that are regionally scoped
    $type : str, default is Undefined, optional
        SubnetType string describes subnet types (public, private, utility)
    zone : str, default is Undefined, optional
        Zone is the zone the subnet is in, set for subnets that are zonally scoped
    """


    additionalRoutes?: [KopsK8sIoV1alpha2ClusterSpecSubnetsItems0AdditionalRoutesItems0]

    cidr?: str

    egress?: str

    id?: str

    ipv6CIDR?: str

    name?: str

    publicIP?: str

    region?: str

    $type?: str

    zone?: str


schema KopsK8sIoV1alpha2ClusterSpecSubnetsItems0AdditionalRoutesItems0:
    """
    kops k8s io v1alpha2 cluster spec subnets items0 additional routes items0

    Attributes
    ----------
    cidr : str, default is Undefined, optional
        CIDR destination of the route
    target : str, default is Undefined, optional
        Target of the route
    """


    cidr?: str

    target?: str


schema KopsK8sIoV1alpha2ClusterSpecTarget:
    """
    Target allows for us to nest extra config for targets such as terraform

    Attributes
    ----------
    terraform : KopsK8sIoV1alpha2ClusterSpecTargetTerraform, default is Undefined, optional
        terraform
    """


    terraform?: KopsK8sIoV1alpha2ClusterSpecTargetTerraform


schema KopsK8sIoV1alpha2ClusterSpecTargetTerraform:
    """
    TerraformSpec allows us to specify terraform config in an extensible way

    Attributes
    ----------
    filesProviderExtraConfig : {str:str}, default is Undefined, optional
        FilesProviderExtraConfig contains key/value pairs to add to the terraform provider block used for managed files
    providerExtraConfig : {str:str}, default is Undefined, optional
        ProviderExtraConfig contains key/value pairs to add to the main terraform provider block
    """


    filesProviderExtraConfig?: {str:str}

    providerExtraConfig?: {str:str}


schema KopsK8sIoV1alpha2ClusterSpecTopology:
    """
    Topology defines the type of network topology to use on the cluster - default public This is heavily weighted towards AWS for the time being, but should also be agnostic enough to port out to GCE later if needed

    Attributes
    ----------
    bastion : KopsK8sIoV1alpha2ClusterSpecTopologyBastion, default is Undefined, optional
        bastion
    dns : KopsK8sIoV1alpha2ClusterSpecTopologyDNS, default is Undefined, optional
        dns
    masters : str, default is Undefined, optional
        Masters is not used.
    nodes : str, default is Undefined, optional
        Nodes is not used.
    """


    bastion?: KopsK8sIoV1alpha2ClusterSpecTopologyBastion

    dns?: KopsK8sIoV1alpha2ClusterSpecTopologyDNS

    masters?: str

    nodes?: str


schema KopsK8sIoV1alpha2ClusterSpecTopologyBastion:
    """
    Bastion provide an external facing point of entry into a network containing private network instances. This host can provide a single point of fortification or audit and can be started and stopped to enable or disable inbound SSH communication from the Internet, some call bastion as the "jump server".

    Attributes
    ----------
    bastionPublicName : str, default is Undefined, optional
        bastion public name
    idleTimeoutSeconds : int, default is Undefined, optional
        IdleTimeoutSeconds is unused
    loadBalancer : KopsK8sIoV1alpha2ClusterSpecTopologyBastionLoadBalancer, default is Undefined, optional
        load balancer
    """


    bastionPublicName?: str

    idleTimeoutSeconds?: int

    loadBalancer?: KopsK8sIoV1alpha2ClusterSpecTopologyBastionLoadBalancer


schema KopsK8sIoV1alpha2ClusterSpecTopologyBastionLoadBalancer:
    """
    kops k8s io v1alpha2 cluster spec topology bastion load balancer

    Attributes
    ----------
    additionalSecurityGroups : [str], default is Undefined, optional
        AdditionalSecurityGroups is unused
    $type : str, default is Undefined, optional
        Type of load balancer to create, it can be Public or Internal.
    """


    additionalSecurityGroups?: [str]

    $type?: str


schema KopsK8sIoV1alpha2ClusterSpecTopologyDNS:
    """
    DNS configures options relating to DNS, in particular whether we use a public or a private hosted zone

    Attributes
    ----------
    $type : str, default is Undefined, optional
        type
    """


    $type?: str


schema KopsK8sIoV1alpha2ClusterSpecWarmPool:
    """
    WarmPool defines the default warm pool settings for instance groups (AWS only).

    Attributes
    ----------
    enableLifecycleHook : bool, default is Undefined, optional
        EnableLifecycleHook determines if an ASG lifecycle hook will be added ensuring that nodeup runs to completion. Note that the metadata API must be protected from arbitrary Pods when this is enabled.
    maxSize : int, default is Undefined, optional
        MaxSize is the maximum size of the warm pool. The desired size of the instance group is subtracted from this number to determine the desired size of the warm pool (unless the resulting number is smaller than MinSize). The default is the instance group's MaxSize.
    minSize : int, default is Undefined, optional
        MinSize is the minimum size of the pool
    """


    enableLifecycleHook?: bool

    maxSize?: int

    minSize?: int


