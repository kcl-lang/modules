"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema DataprocJob:
    r"""
    DataprocJob is the Schema for the DataprocJob API

    Attributes
    ----------
    apiVersion : str, default is "dataproc.cnrm.cloud.google.com/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "DataprocJob", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpec, default is Undefined, required
        spec
    status : DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatus, default is Undefined, optional
        status
    """


    apiVersion: "dataproc.cnrm.cloud.google.com/v1alpha1" = "dataproc.cnrm.cloud.google.com/v1alpha1"

    kind: "DataprocJob" = "DataprocJob"

    metadata?: v1.ObjectMeta

    spec: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpec

    status?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatus


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpec:
    r"""
    DataprocJobSpec defines the desired state of DataprocJob

    Attributes
    ----------
    driverSchedulingConfig : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecDriverSchedulingConfig, default is Undefined, optional
        driver scheduling config
    flinkJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecFlinkJob, default is Undefined, optional
        flink job
    hadoopJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHadoopJob, default is Undefined, optional
        hadoop job
    hiveJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHiveJob, default is Undefined, optional
        hive job
    labels : {str:str}, default is Undefined, optional
        Optional. The labels to associate with this job. Label **keys** must contain 1 to 63 characters, and must conform to [RFC 1035](https://www.ietf.org/rfc/rfc1035.txt). Label **values** can be empty, but, if present, must contain 1 to 63 characters, and must conform to [RFC 1035](https://www.ietf.org/rfc/rfc1035.txt). No more than 32 labels can be associated with a job.
    parent : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecParent, default is Undefined, optional
        parent
    pigJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPigJob, default is Undefined, optional
        pig job
    placement : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPlacement, default is Undefined, optional
        placement
    prestoJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPrestoJob, default is Undefined, optional
        presto job
    pysparkJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPysparkJob, default is Undefined, optional
        pyspark job
    reference : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecReference, default is Undefined, optional
        reference
    resourceID : str, default is Undefined, optional
        The DataprocJob name. If not given, the metadata.name will be used.
    scheduling : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecScheduling, default is Undefined, optional
        scheduling
    sparkJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkJob, default is Undefined, optional
        spark job
    sparkRJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkRJob, default is Undefined, optional
        spark r job
    sparkSQLJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkSQLJob, default is Undefined, optional
        spark SQL job
    trinoJob : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecTrinoJob, default is Undefined, optional
        trino job
    """


    driverSchedulingConfig?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecDriverSchedulingConfig

    flinkJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecFlinkJob

    hadoopJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHadoopJob

    hiveJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHiveJob

    labels?: {str:str}

    parent?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecParent

    pigJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPigJob

    placement?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPlacement

    prestoJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPrestoJob

    pysparkJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPysparkJob

    reference?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecReference

    resourceID?: str

    scheduling?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecScheduling

    sparkJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkJob

    sparkRJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkRJob

    sparkSQLJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkSQLJob

    trinoJob?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecTrinoJob


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecDriverSchedulingConfig:
    r"""
    Optional. Driver scheduling configuration.

    Attributes
    ----------
    memoryMb : int, default is Undefined, optional
        Required. The amount of memory in MB the driver is requesting.
    vcores : int, default is Undefined, optional
        Required. The number of vCPUs the driver is requesting.
    """


    memoryMb?: int

    vcores?: int


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecFlinkJob:
    r"""
    Optional. Job is a Flink job.

    Attributes
    ----------
    args : [str], default is Undefined, optional
        Optional. The arguments to pass to the driver. Do not include arguments, such as `--conf`, that can be set as job properties, since a collision might occur that causes an incorrect job submission.
    jarFileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of jar files to add to the CLASSPATHs of the Flink driver and tasks.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    mainClass : str, default is Undefined, optional
        The name of the driver's main class. The jar file that contains the class must be in the default CLASSPATH or specified in [jarFileUris][google.cloud.dataproc.v1.FlinkJob.jar_file_uris].
    mainJarFileURI : str, default is Undefined, optional
        The HCFS URI of the jar file that contains the main class.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure Flink. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in `/etc/flink/conf/flink-defaults.conf` and classes in user code.
    savepointURI : str, default is Undefined, optional
        Optional. HCFS URI of the savepoint, which contains the last saved progress for starting the current job.
    """


    args?: [str]

    jarFileURIs?: [str]

    loggingConfig?: any

    mainClass?: str

    mainJarFileURI?: str

    properties?: {str:str}

    savepointURI?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHadoopJob:
    r"""
    Optional. Job is a Hadoop job.

    Attributes
    ----------
    archiveURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of archives to be extracted in the working directory of Hadoop drivers and tasks. Supported file types: .jar, .tar, .tar.gz, .tgz, or .zip.
    args : [str], default is Undefined, optional
        Optional. The arguments to pass to the driver. Do not include arguments, such as `-libjars` or `-Dfoo=bar`, that can be set as job properties, since a collision might occur that causes an incorrect job submission.
    fileURIs : [str], default is Undefined, optional
        Optional. HCFS (Hadoop Compatible Filesystem) URIs of files to be copied to the working directory of Hadoop drivers and distributed tasks. Useful for naively parallel tasks.
    jarFileURIs : [str], default is Undefined, optional
        Optional. Jar file URIs to add to the CLASSPATHs of the Hadoop driver and tasks.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    mainClass : str, default is Undefined, optional
        The name of the driver's main class. The jar file containing the class must be in the default CLASSPATH or specified in `jar_file_uris`.
    mainJarFileURI : str, default is Undefined, optional
        The HCFS URI of the jar file containing the main class. Examples: 'gs://foo-bucket/analytics-binaries/extract-useful-metrics-mr.jar' 'hdfs:/tmp/test-samples/custom-wordcount.jar' 'file:///home/usr/lib/hadoop-mapreduce/hadoop-mapreduce-examples.jar'
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure Hadoop. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in `/etc/hadoop/conf/*-site` and classes in user code.
    """


    archiveURIs?: [str]

    args?: [str]

    fileURIs?: [str]

    jarFileURIs?: [str]

    loggingConfig?: any

    mainClass?: str

    mainJarFileURI?: str

    properties?: {str:str}


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHiveJob:
    r"""
    Optional. Job is a Hive job.

    Attributes
    ----------
    continueOnFailure : bool, default is Undefined, optional
        Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
    jarFileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of jar files to add to the CLASSPATH of the Hive server and Hadoop MapReduce (MR) tasks. Can contain Hive SerDes and UDFs.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names and values, used to configure Hive. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in `/etc/hadoop/conf/*-site.xml`, /etc/hive/conf/hive-site.xml, and classes in user code.
    queryFileURI : str, default is Undefined, optional
        The HCFS URI of the script that contains Hive queries.
    queryList : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHiveJobQueryList, default is Undefined, optional
        query list
    scriptVariables : {str:str}, default is Undefined, optional
        Optional. Mapping of query variable names to values (equivalent to the Hive command: `SET name="value";`).
    """


    continueOnFailure?: bool

    jarFileURIs?: [str]

    properties?: {str:str}

    queryFileURI?: str

    queryList?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHiveJobQueryList

    scriptVariables?: {str:str}


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecHiveJobQueryList:
    r"""
    A list of queries.

    Attributes
    ----------
    queries : [str], default is Undefined, optional
        Required. The queries to execute. You do not need to end a query expression
         with a semicolon. Multiple queries can be specified in one
         string by separating each with a semicolon. Here is an example of a
         Dataproc API snippet that uses a QueryList to specify a HiveJob:

             "hiveJob": {
               "queryList": {
                 "queries": [
                   "query1",
                   "query2",
                   "query3;query4",
                 ]
               }
             }
    """


    queries?: [str]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecParent:
    r"""
    Required.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Required.
    projectRef : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecParentProjectRef, default is Undefined, optional
        project ref
    """


    location?: str

    projectRef?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecParentProjectRef


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecParentProjectRef:
    r"""
    Required.

    Attributes
    ----------
    external : str, default is Undefined, optional
        The `projectID` field of a project, when not managed by Config Connector.
    kind : str, default is Undefined, optional
        The kind of the Project resource; optional but must be `Project` if provided.
    name : str, default is Undefined, optional
        The `name` field of a `Project` resource.
    namespace : str, default is Undefined, optional
        The `namespace` field of a `Project` resource.
    """


    external?: str

    kind?: str

    name?: str

    namespace?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPigJob:
    r"""
    Optional. Job is a Pig job.

    Attributes
    ----------
    continueOnFailure : bool, default is Undefined, optional
        Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
    jarFileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of jar files to add to the CLASSPATH of the Pig Client and Hadoop MapReduce (MR) tasks. Can contain Pig UDFs.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure Pig. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in `/etc/hadoop/conf/*-site.xml`, /etc/pig/conf/pig.properties, and classes in user code.
    queryFileURI : str, default is Undefined, optional
        The HCFS URI of the script that contains the Pig queries.
    queryList : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPigJobQueryList, default is Undefined, optional
        query list
    scriptVariables : {str:str}, default is Undefined, optional
        Optional. Mapping of query variable names to values (equivalent to the Pig command: `name=[value]`).
    """


    continueOnFailure?: bool

    jarFileURIs?: [str]

    loggingConfig?: any

    properties?: {str:str}

    queryFileURI?: str

    queryList?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPigJobQueryList

    scriptVariables?: {str:str}


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPigJobQueryList:
    r"""
    A list of queries.

    Attributes
    ----------
    queries : [str], default is Undefined, optional
        Required. The queries to execute. You do not need to end a query expression
         with a semicolon. Multiple queries can be specified in one
         string by separating each with a semicolon. Here is an example of a
         Dataproc API snippet that uses a QueryList to specify a HiveJob:

             "hiveJob": {
               "queryList": {
                 "queries": [
                   "query1",
                   "query2",
                   "query3;query4",
                 ]
               }
             }
    """


    queries?: [str]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPlacement:
    r"""
    Required. Job information, including how, when, and where to run the job.

    Attributes
    ----------
    clusterLabels : {str:str}, default is Undefined, optional
        Optional. Cluster labels to identify a cluster where the job will be submitted.
    clusterName : str, default is Undefined, optional
        Required. The name of the cluster where the job will be submitted.
    """


    clusterLabels?: {str:str}

    clusterName?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPrestoJob:
    r"""
    Optional. Job is a Presto job.

    Attributes
    ----------
    clientTags : [str], default is Undefined, optional
        Optional. Presto client tags to attach to this query
    continueOnFailure : bool, default is Undefined, optional
        Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    outputFormat : str, default is Undefined, optional
        Optional. The format in which query output will be displayed. See the Presto documentation for supported output formats
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values. Used to set Presto [session properties](https://prestodb.io/docs/current/sql/set-session.html) Equivalent to using the --session flag in the Presto CLI
    queryFileURI : str, default is Undefined, optional
        The HCFS URI of the script that contains SQL queries.
    queryList : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPrestoJobQueryList, default is Undefined, optional
        query list
    """


    clientTags?: [str]

    continueOnFailure?: bool

    loggingConfig?: any

    outputFormat?: str

    properties?: {str:str}

    queryFileURI?: str

    queryList?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPrestoJobQueryList


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPrestoJobQueryList:
    r"""
    A list of queries.

    Attributes
    ----------
    queries : [str], default is Undefined, optional
        Required. The queries to execute. You do not need to end a query expression
         with a semicolon. Multiple queries can be specified in one
         string by separating each with a semicolon. Here is an example of a
         Dataproc API snippet that uses a QueryList to specify a HiveJob:

             "hiveJob": {
               "queryList": {
                 "queries": [
                   "query1",
                   "query2",
                   "query3;query4",
                 ]
               }
             }
    """


    queries?: [str]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecPysparkJob:
    r"""
    Optional. Job is a PySpark job.

    Attributes
    ----------
    archiveURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of archives to be extracted into the working directory of each executor. Supported file types: .jar, .tar, .tar.gz, .tgz, and .zip.
    args : [str], default is Undefined, optional
        Optional. The arguments to pass to the driver.  Do not include arguments, such as `--conf`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
    fileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of files to be placed in the working directory of each executor. Useful for naively parallel tasks.
    jarFileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of jar files to add to the CLASSPATHs of the Python driver and tasks.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    mainPythonFileURI : str, default is Undefined, optional
        Required. The HCFS URI of the main Python file to use as the driver. Must be a .py file.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure PySpark. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in /etc/spark/conf/spark-defaults.conf and classes in user code.
    pythonFileURIs : [str], default is Undefined, optional
        Optional. HCFS file URIs of Python files to pass to the PySpark framework. Supported file types: .py, .egg, and .zip.
    """


    archiveURIs?: [str]

    args?: [str]

    fileURIs?: [str]

    jarFileURIs?: [str]

    loggingConfig?: any

    mainPythonFileURI?: str

    properties?: {str:str}

    pythonFileURIs?: [str]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecReference:
    r"""
    Optional. The fully qualified reference to the job, which can be used to obtain the equivalent REST path of the job resource. If this property is not specified when a job is created, the server generates a <code>job_id</code>.

    Attributes
    ----------
    jobID : str, default is Undefined, optional
        Optional. The job ID, which must be unique within the project.

         The ID must contain only letters (a-z, A-Z), numbers (0-9),
         underscores (_), or hyphens (-). The maximum length is 100 characters.

         If not specified by the caller, the job ID will be provided by the server.
    projectID : str, default is Undefined, optional
        Optional. The ID of the Google Cloud Platform project that the job belongs to. If specified, must match the request project ID.
    """


    jobID?: str

    projectID?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecScheduling:
    r"""
    Optional. Job scheduling configuration.

    Attributes
    ----------
    maxFailuresPerHour : int, default is Undefined, optional
        Optional. Maximum number of times per hour a driver can be restarted as
         a result of driver exiting with non-zero code before job is
         reported failed.

         A job might be reported as thrashing if the driver exits with a non-zero
         code four times within a 10-minute window.

         Maximum value is 10.

         **Note:** This restartable job option is not supported in Dataproc
         [workflow templates]
         (https://cloud.google.com/dataproc/docs/concepts/workflows/using-workflows#adding_jobs_to_a_template).
    maxFailuresTotal : int, default is Undefined, optional
        Optional. Maximum total number of times a driver can be restarted as a
         result of the driver exiting with a non-zero code. After the maximum number
         is reached, the job will be reported as failed.

         Maximum value is 240.

         **Note:** Currently, this restartable job option is
         not supported in Dataproc
         [workflow
         templates](https://cloud.google.com/dataproc/docs/concepts/workflows/using-workflows#adding_jobs_to_a_template).
    """


    maxFailuresPerHour?: int

    maxFailuresTotal?: int


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkJob:
    r"""
    Optional. Job is a Spark job.

    Attributes
    ----------
    archiveURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of archives to be extracted into the working directory of each executor. Supported file types: .jar, .tar, .tar.gz, .tgz, and .zip.
    args : [str], default is Undefined, optional
        Optional. The arguments to pass to the driver. Do not include arguments, such as `--conf`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
    fileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of files to be placed in the working directory of each executor. Useful for naively parallel tasks.
    jarFileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of jar files to add to the CLASSPATHs of the Spark driver and tasks.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    mainClass : str, default is Undefined, optional
        The name of the driver's main class. The jar file that contains the class must be in the default CLASSPATH or specified in SparkJob.jar_file_uris.
    mainJarFileURI : str, default is Undefined, optional
        The HCFS URI of the jar file that contains the main class.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure Spark. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in /etc/spark/conf/spark-defaults.conf and classes in user code.
    """


    archiveURIs?: [str]

    args?: [str]

    fileURIs?: [str]

    jarFileURIs?: [str]

    loggingConfig?: any

    mainClass?: str

    mainJarFileURI?: str

    properties?: {str:str}


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkRJob:
    r"""
    Optional. Job is a SparkR job.

    Attributes
    ----------
    archiveURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of archives to be extracted into the working directory of each executor. Supported file types: .jar, .tar, .tar.gz, .tgz, and .zip.
    args : [str], default is Undefined, optional
        Optional. The arguments to pass to the driver.  Do not include arguments, such as `--conf`, that can be set as job properties, since a collision may occur that causes an incorrect job submission.
    fileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of files to be placed in the working directory of each executor. Useful for naively parallel tasks.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    mainRFileURI : str, default is Undefined, optional
        Required. The HCFS URI of the main R file to use as the driver. Must be a .R file.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure SparkR. Properties that conflict with values set by the Dataproc API might be overwritten. Can include properties set in /etc/spark/conf/spark-defaults.conf and classes in user code.
    """


    archiveURIs?: [str]

    args?: [str]

    fileURIs?: [str]

    loggingConfig?: any

    mainRFileURI?: str

    properties?: {str:str}


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkSQLJob:
    r"""
    Optional. Job is a SparkSql job.

    Attributes
    ----------
    jarFileURIs : [str], default is Undefined, optional
        Optional. HCFS URIs of jar files to be added to the Spark CLASSPATH.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values, used to configure Spark SQL's SparkConf. Properties that conflict with values set by the Dataproc API might be overwritten.
    queryFileURI : str, default is Undefined, optional
        The HCFS URI of the script that contains SQL queries.
    queryList : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkSQLJobQueryList, default is Undefined, optional
        query list
    scriptVariables : {str:str}, default is Undefined, optional
        Optional. Mapping of query variable names to values (equivalent to the Spark SQL command: SET `name="value";`).
    """


    jarFileURIs?: [str]

    loggingConfig?: any

    properties?: {str:str}

    queryFileURI?: str

    queryList?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkSQLJobQueryList

    scriptVariables?: {str:str}


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecSparkSQLJobQueryList:
    r"""
    A list of queries.

    Attributes
    ----------
    queries : [str], default is Undefined, optional
        Required. The queries to execute. You do not need to end a query expression
         with a semicolon. Multiple queries can be specified in one
         string by separating each with a semicolon. Here is an example of a
         Dataproc API snippet that uses a QueryList to specify a HiveJob:

             "hiveJob": {
               "queryList": {
                 "queries": [
                   "query1",
                   "query2",
                   "query3;query4",
                 ]
               }
             }
    """


    queries?: [str]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecTrinoJob:
    r"""
    Optional. Job is a Trino job.

    Attributes
    ----------
    clientTags : [str], default is Undefined, optional
        Optional. Trino client tags to attach to this query
    continueOnFailure : bool, default is Undefined, optional
        Optional. Whether to continue executing queries if a query fails. The default value is `false`. Setting to `true` can be useful when executing independent parallel queries.
    loggingConfig : any, default is Undefined, optional
        Optional. The runtime log config for job execution.
    outputFormat : str, default is Undefined, optional
        Optional. The format in which query output will be displayed. See the Trino documentation for supported output formats
    properties : {str:str}, default is Undefined, optional
        Optional. A mapping of property names to values. Used to set Trino [session properties](https://trino.io/docs/current/sql/set-session.html) Equivalent to using the --session flag in the Trino CLI
    queryFileURI : str, default is Undefined, optional
        The HCFS URI of the script that contains SQL queries.
    queryList : DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecTrinoJobQueryList, default is Undefined, optional
        query list
    """


    clientTags?: [str]

    continueOnFailure?: bool

    loggingConfig?: any

    outputFormat?: str

    properties?: {str:str}

    queryFileURI?: str

    queryList?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecTrinoJobQueryList


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobSpecTrinoJobQueryList:
    r"""
    A list of queries.

    Attributes
    ----------
    queries : [str], default is Undefined, optional
        Required. The queries to execute. You do not need to end a query expression
         with a semicolon. Multiple queries can be specified in one
         string by separating each with a semicolon. Here is an example of a
         Dataproc API snippet that uses a QueryList to specify a HiveJob:

             "hiveJob": {
               "queryList": {
                 "queries": [
                   "query1",
                   "query2",
                   "query3;query4",
                 ]
               }
             }
    """


    queries?: [str]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatus:
    r"""
    DataprocJobStatus defines the config connector machine state of DataprocJob

    Attributes
    ----------
    conditions : [DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusConditionsItems0], default is Undefined, optional
        Conditions represent the latest available observations of the object's current state.
    externalRef : str, default is Undefined, optional
        A unique specifier for the DataprocJob resource in GCP.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
    observedState : DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedState, default is Undefined, optional
        observed state
    """


    conditions?: [DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusConditionsItems0]

    externalRef?: str

    observedGeneration?: int

    observedState?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedState


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusConditionsItems0:
    r"""
    dataproc cnrm cloud google com v1alpha1 dataproc job status conditions items0

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        Last time the condition transitioned from one status to another.
    message : str, default is Undefined, optional
        Human-readable message indicating details about last transition.
    reason : str, default is Undefined, optional
        Unique, one-word, CamelCase reason for the condition's last transition.
    status : str, default is Undefined, optional
        Status is the status of the condition. Can be True, False, Unknown.
    $type : str, default is Undefined, optional
        Type is the type of the condition.
    """


    lastTransitionTime?: str

    message?: str

    reason?: str

    status?: str

    $type?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedState:
    r"""
    ObservedState is the state of the resource as most recently observed in GCP.

    Attributes
    ----------
    done : bool, default is Undefined, optional
        Output only. Indicates whether the job is completed. If the value is `false`, the job is still in progress. If `true`, the job is completed, and `status.state` field will indicate if it was successful, failed, or cancelled.
    driverControlFilesURI : str, default is Undefined, optional
        Output only. If present, the location of miscellaneous control files which can be used as part of job setup and handling. If not present, control files might be placed in the same location as `driver_output_uri`.
    driverOutputResourceURI : str, default is Undefined, optional
        Output only. A URI pointing to the location of the stdout of the job's driver program.
    jobUUid : str, default is Undefined, optional
        Output only. A UUID that uniquely identifies a job within the project over time. This is in contrast to a user-settable reference.job_id that might be reused over time.
    placement : DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStatePlacement, default is Undefined, optional
        placement
    status : DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateStatus, default is Undefined, optional
        status
    statusHistory : [DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateStatusHistoryItems0], default is Undefined, optional
        Output only. The previous job status.
    yarnApplications : [DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateYarnApplicationsItems0], default is Undefined, optional
        Output only. The collection of YARN applications spun up by this job.

         **Beta** Feature: This report is available for testing purposes only. It
         might be changed before final release.
    """


    done?: bool

    driverControlFilesURI?: str

    driverOutputResourceURI?: str

    jobUUid?: str

    placement?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStatePlacement

    status?: DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateStatus

    statusHistory?: [DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateStatusHistoryItems0]

    yarnApplications?: [DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateYarnApplicationsItems0]


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStatePlacement:
    r"""
    Required. Job information, including how, when, and where to run the job.

    Attributes
    ----------
    clusterUuid : str, default is Undefined, optional
        Output only. A cluster UUID generated by the Dataproc service when the job is submitted.
    """


    clusterUuid?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateStatus:
    r"""
    Output only. The job status. Additional application-specific status information might be contained in the <code>type_job</code> and <code>yarn_applications</code> fields.

    Attributes
    ----------
    details : str, default is Undefined, optional
        Optional. Output only. Job state details, such as an error description if the state is `ERROR`.
    state : str, default is Undefined, optional
        Output only. A state message specifying the overall job state.
    stateStartTime : str, default is Undefined, optional
        Output only. The time when this state was entered.
    substate : str, default is Undefined, optional
        Output only. Additional state information, which includes status reported by the agent.
    """


    details?: str

    state?: str

    stateStartTime?: str

    substate?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateStatusHistoryItems0:
    r"""
    dataproc cnrm cloud google com v1alpha1 dataproc job status observed state status history items0

    Attributes
    ----------
    details : str, default is Undefined, optional
        Optional. Output only. Job state details, such as an error description if the state is `ERROR`.
    state : str, default is Undefined, optional
        Output only. A state message specifying the overall job state.
    stateStartTime : str, default is Undefined, optional
        Output only. The time when this state was entered.
    substate : str, default is Undefined, optional
        Output only. Additional state information, which includes status reported by the agent.
    """


    details?: str

    state?: str

    stateStartTime?: str

    substate?: str


schema DataprocCnrmCloudGoogleComV1alpha1DataprocJobStatusObservedStateYarnApplicationsItems0:
    r"""
    dataproc cnrm cloud google com v1alpha1 dataproc job status observed state yarn applications items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Required. The application name.
    progress : float, default is Undefined, optional
        Required. The numerical progress of the application, from 1 to 100.
    state : str, default is Undefined, optional
        Required. The application state.
    trackingURL : str, default is Undefined, optional
        Optional. The HTTP URL of the ApplicationMaster, HistoryServer, or TimelineServer that provides application-specific information. The URL uses the internal hostname, and requires a proxy server for resolution and, possibly, access.
    """


    name?: str

    progress?: float

    state?: str

    trackingURL?: str


