"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema ComputeHealthCheck:
    r"""
    compute cnrm cloud google com v1beta1 compute health check

    Attributes
    ----------
    apiVersion : str, default is "compute.cnrm.cloud.google.com/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ComputeHealthCheck", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpec, default is Undefined, required
        spec
    status : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckStatus, default is Undefined, optional
        status
    """


    apiVersion: "compute.cnrm.cloud.google.com/v1beta1" = "compute.cnrm.cloud.google.com/v1beta1"

    kind: "ComputeHealthCheck" = "ComputeHealthCheck"

    metadata?: v1.ObjectMeta

    spec: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpec

    status?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckStatus


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpec:
    r"""
    compute cnrm cloud google com v1beta1 compute health check spec

    Attributes
    ----------
    checkIntervalSec : int, default is Undefined, optional
        How often (in seconds) to send a health check. The default value is 5
        seconds.
    description : str, default is Undefined, optional
        An optional description of this resource. Provide this property when
        you create the resource.
    grpcHealthCheck : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecGrpcHealthCheck, default is Undefined, optional
        grpc health check
    healthyThreshold : int, default is Undefined, optional
        A so-far unhealthy instance will be marked healthy after this many
        consecutive successes. The default value is 2.
    http2HealthCheck : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHttp2HealthCheck, default is Undefined, optional
        http2 health check
    httpHealthCheck : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHTTPHealthCheck, default is Undefined, optional
        http health check
    httpsHealthCheck : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHTTPSHealthCheck, default is Undefined, optional
        https health check
    location : str, default is Undefined, required
        Location represents the geographical location of the ComputeHealthCheck. Specify a region name or "global" for global resources. Reference: GCP definition of regions/zones (https://cloud.google.com/compute/docs/regions-zones/)
    logConfig : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecLogConfig, default is Undefined, optional
        log config
    resourceID : str, default is Undefined, optional
        Immutable. Optional. The name of the resource. Used for creation and acquisition. When unset, the value of `metadata.name` is used as the default.
    sslHealthCheck : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecSslHealthCheck, default is Undefined, optional
        ssl health check
    tcpHealthCheck : ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecTCPHealthCheck, default is Undefined, optional
        tcp health check
    timeoutSec : int, default is Undefined, optional
        How long (in seconds) to wait before claiming failure.
        The default value is 5 seconds.  It is invalid for timeoutSec to have
        greater value than checkIntervalSec.
    unhealthyThreshold : int, default is Undefined, optional
        A so-far healthy instance will be marked unhealthy after this many
        consecutive failures. The default value is 2.
    """


    checkIntervalSec?: int

    description?: str

    grpcHealthCheck?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecGrpcHealthCheck

    healthyThreshold?: int

    http2HealthCheck?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHttp2HealthCheck

    httpHealthCheck?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHTTPHealthCheck

    httpsHealthCheck?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHTTPSHealthCheck

    location: str

    logConfig?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecLogConfig

    resourceID?: str

    sslHealthCheck?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecSslHealthCheck

    tcpHealthCheck?: ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecTCPHealthCheck

    timeoutSec?: int

    unhealthyThreshold?: int


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecGrpcHealthCheck:
    r"""
    A nested object resource.

    Attributes
    ----------
    grpcServiceName : str, default is Undefined, optional
        The gRPC service name for the health check.
        The value of grpcServiceName has the following meanings by convention:
          - Empty serviceName means the overall status of all services at the backend.
          - Non-empty serviceName means the health of that gRPC service, as defined by the owner of the service.
        The grpcServiceName can only be ASCII.
    port : int, default is Undefined, optional
        The port number for the health check request.
        Must be specified if portName and portSpecification are not set
        or if port_specification is USE_FIXED_PORT. Valid values are 1 through 65535.
    portName : str, default is Undefined, optional
        Port name as defined in InstanceGroup#NamedPort#name. If both port and
        port_name are defined, port takes precedence.
    portSpecification : str, default is Undefined, optional
        Specifies how port is selected for health checking, can be one of the
        following values:

          * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.

          * 'USE_NAMED_PORT': The 'portName' is used for health checking.

          * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
          network endpoint is used for health checking. For other backends, the
          port or named port specified in the Backend Service is used for health
          checking.

        If not specified, gRPC health check follows behavior specified in 'port' and
        'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"].
    """


    grpcServiceName?: str

    port?: int

    portName?: str

    portSpecification?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHTTPHealthCheck:
    r"""
    A nested object resource.

    Attributes
    ----------
    host : str, default is Undefined, optional
        The value of the host header in the HTTP health check request.
        If left empty (default value), the public IP on behalf of which this health
        check is performed will be used.
    port : int, default is Undefined, optional
        The TCP port number for the HTTP health check request.
        The default value is 80.
    portName : str, default is Undefined, optional
        Port name as defined in InstanceGroup#NamedPort#name. If both port and
        port_name are defined, port takes precedence.
    portSpecification : str, default is Undefined, optional
        Specifies how port is selected for health checking, can be one of the
        following values:

          * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.

          * 'USE_NAMED_PORT': The 'portName' is used for health checking.

          * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
          network endpoint is used for health checking. For other backends, the
          port or named port specified in the Backend Service is used for health
          checking.

        If not specified, HTTP health check follows behavior specified in 'port' and
        'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"].
    proxyHeader : str, default is Undefined, optional
        Specifies the type of proxy header to append before sending data to the
        backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"].
    requestPath : str, default is Undefined, optional
        The request path of the HTTP health check request.
        The default value is /.
    response : str, default is Undefined, optional
        The bytes to match against the beginning of the response data. If left empty
        (the default value), any response will indicate health. The response data
        can only be ASCII.
    """


    host?: str

    port?: int

    portName?: str

    portSpecification?: str

    proxyHeader?: str

    requestPath?: str

    response?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHTTPSHealthCheck:
    r"""
    A nested object resource.

    Attributes
    ----------
    host : str, default is Undefined, optional
        The value of the host header in the HTTPS health check request.
        If left empty (default value), the public IP on behalf of which this health
        check is performed will be used.
    port : int, default is Undefined, optional
        The TCP port number for the HTTPS health check request.
        The default value is 443.
    portName : str, default is Undefined, optional
        Port name as defined in InstanceGroup#NamedPort#name. If both port and
        port_name are defined, port takes precedence.
    portSpecification : str, default is Undefined, optional
        Specifies how port is selected for health checking, can be one of the
        following values:

          * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.

          * 'USE_NAMED_PORT': The 'portName' is used for health checking.

          * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
          network endpoint is used for health checking. For other backends, the
          port or named port specified in the Backend Service is used for health
          checking.

        If not specified, HTTPS health check follows behavior specified in 'port' and
        'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"].
    proxyHeader : str, default is Undefined, optional
        Specifies the type of proxy header to append before sending data to the
        backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"].
    requestPath : str, default is Undefined, optional
        The request path of the HTTPS health check request.
        The default value is /.
    response : str, default is Undefined, optional
        The bytes to match against the beginning of the response data. If left empty
        (the default value), any response will indicate health. The response data
        can only be ASCII.
    """


    host?: str

    port?: int

    portName?: str

    portSpecification?: str

    proxyHeader?: str

    requestPath?: str

    response?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecHttp2HealthCheck:
    r"""
    A nested object resource.

    Attributes
    ----------
    host : str, default is Undefined, optional
        The value of the host header in the HTTP2 health check request.
        If left empty (default value), the public IP on behalf of which this health
        check is performed will be used.
    port : int, default is Undefined, optional
        The TCP port number for the HTTP2 health check request.
        The default value is 443.
    portName : str, default is Undefined, optional
        Port name as defined in InstanceGroup#NamedPort#name. If both port and
        port_name are defined, port takes precedence.
    portSpecification : str, default is Undefined, optional
        Specifies how port is selected for health checking, can be one of the
        following values:

          * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.

          * 'USE_NAMED_PORT': The 'portName' is used for health checking.

          * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
          network endpoint is used for health checking. For other backends, the
          port or named port specified in the Backend Service is used for health
          checking.

        If not specified, HTTP2 health check follows behavior specified in 'port' and
        'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"].
    proxyHeader : str, default is Undefined, optional
        Specifies the type of proxy header to append before sending data to the
        backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"].
    requestPath : str, default is Undefined, optional
        The request path of the HTTP2 health check request.
        The default value is /.
    response : str, default is Undefined, optional
        The bytes to match against the beginning of the response data. If left empty
        (the default value), any response will indicate health. The response data
        can only be ASCII.
    """


    host?: str

    port?: int

    portName?: str

    portSpecification?: str

    proxyHeader?: str

    requestPath?: str

    response?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecLogConfig:
    r"""
    Configure logging on this health check.

    Attributes
    ----------
    enable : bool, default is Undefined, optional
        Indicates whether or not to export logs. This is false by default,
        which means no health check logging will be done.
    """


    enable?: bool


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecSslHealthCheck:
    r"""
    A nested object resource.

    Attributes
    ----------
    port : int, default is Undefined, optional
        The TCP port number for the SSL health check request.
        The default value is 443.
    portName : str, default is Undefined, optional
        Port name as defined in InstanceGroup#NamedPort#name. If both port and
        port_name are defined, port takes precedence.
    portSpecification : str, default is Undefined, optional
        Specifies how port is selected for health checking, can be one of the
        following values:

          * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.

          * 'USE_NAMED_PORT': The 'portName' is used for health checking.

          * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
          network endpoint is used for health checking. For other backends, the
          port or named port specified in the Backend Service is used for health
          checking.

        If not specified, SSL health check follows behavior specified in 'port' and
        'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"].
    proxyHeader : str, default is Undefined, optional
        Specifies the type of proxy header to append before sending data to the
        backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"].
    request : str, default is Undefined, optional
        The application data to send once the SSL connection has been
        established (default value is empty). If both request and response are
        empty, the connection establishment alone will indicate health. The request
        data can only be ASCII.
    response : str, default is Undefined, optional
        The bytes to match against the beginning of the response data. If left empty
        (the default value), any response will indicate health. The response data
        can only be ASCII.
    """


    port?: int

    portName?: str

    portSpecification?: str

    proxyHeader?: str

    request?: str

    response?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckSpecTCPHealthCheck:
    r"""
    A nested object resource.

    Attributes
    ----------
    port : int, default is Undefined, optional
        The TCP port number for the TCP health check request.
        The default value is 443.
    portName : str, default is Undefined, optional
        Port name as defined in InstanceGroup#NamedPort#name. If both port and
        port_name are defined, port takes precedence.
    portSpecification : str, default is Undefined, optional
        Specifies how port is selected for health checking, can be one of the
        following values:

          * 'USE_FIXED_PORT': The port number in 'port' is used for health checking.

          * 'USE_NAMED_PORT': The 'portName' is used for health checking.

          * 'USE_SERVING_PORT': For NetworkEndpointGroup, the port specified for each
          network endpoint is used for health checking. For other backends, the
          port or named port specified in the Backend Service is used for health
          checking.

        If not specified, TCP health check follows behavior specified in 'port' and
        'portName' fields. Possible values: ["USE_FIXED_PORT", "USE_NAMED_PORT", "USE_SERVING_PORT"].
    proxyHeader : str, default is Undefined, optional
        Specifies the type of proxy header to append before sending data to the
        backend. Default value: "NONE" Possible values: ["NONE", "PROXY_V1"].
    request : str, default is Undefined, optional
        The application data to send once the TCP connection has been
        established (default value is empty). If both request and response are
        empty, the connection establishment alone will indicate health. The request
        data can only be ASCII.
    response : str, default is Undefined, optional
        The bytes to match against the beginning of the response data. If left empty
        (the default value), any response will indicate health. The response data
        can only be ASCII.
    """


    port?: int

    portName?: str

    portSpecification?: str

    proxyHeader?: str

    request?: str

    response?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckStatus:
    r"""
    compute cnrm cloud google com v1beta1 compute health check status

    Attributes
    ----------
    conditions : [ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckStatusConditionsItems0], default is Undefined, optional
        Conditions represent the latest available observation of the resource's current state.
    creationTimestamp : str, default is Undefined, optional
        Creation timestamp in RFC3339 text format.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
    selfLink : str, default is Undefined, optional
        self link
    $type : str, default is Undefined, optional
        The type of the health check. One of HTTP, HTTPS, TCP, or SSL.
    """


    conditions?: [ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckStatusConditionsItems0]

    creationTimestamp?: str

    observedGeneration?: int

    selfLink?: str

    $type?: str


schema ComputeCnrmCloudGoogleComV1beta1ComputeHealthCheckStatusConditionsItems0:
    r"""
    compute cnrm cloud google com v1beta1 compute health check status conditions items0

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        Last time the condition transitioned from one status to another.
    message : str, default is Undefined, optional
        Human-readable message indicating details about last transition.
    reason : str, default is Undefined, optional
        Unique, one-word, CamelCase reason for the condition's last transition.
    status : str, default is Undefined, optional
        Status is the status of the condition. Can be True, False, Unknown.
    $type : str, default is Undefined, optional
        Type is the type of the condition.
    """


    lastTransitionTime?: str

    message?: str

    reason?: str

    status?: str

    $type?: str


