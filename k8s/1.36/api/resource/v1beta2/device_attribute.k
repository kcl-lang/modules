"""
This is the device_attribute module in k8s.api.resource.v1beta2 package.
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""


schema DeviceAttribute:
    r"""
    DeviceAttribute must have exactly one field set.

    Attributes
    ----------
    bool : bool, default is Undefined, optional
        BoolValue is a true/false value.
    bools : [bool], default is Undefined, optional
        BoolValues is a non-empty list of true/false values.
    int : int, default is Undefined, optional
        IntValue is a number.
    ints : [int], default is Undefined, optional
        IntValues is a non-empty list of numbers.

        This is an alpha field and requires enabling the DRAListTypeAttributes feature gate.
    string : str, default is Undefined, optional
        StringValue is a string. Must not be longer than 64 characters.
    strings : [str], default is Undefined, optional
        StringValues is a non-empty list of strings. Each string must not be longer than 64 characters.

        This is an alpha field and requires enabling the DRAListTypeAttributes feature gate.
    version : str, default is Undefined, optional
        VersionValue is a semantic version according to semver.org spec 2.0.0. Must not be longer than 64 characters.
    versions : [str], default is Undefined, optional
        VersionValues is a non-empty list of semantic versions according to semver.org spec 2.0.0. Each version string must not be longer than 64 characters.

        This is an alpha field and requires enabling the DRAListTypeAttributes feature gate.
    """


    bool?: bool

    bools?: [bool]

    int?: int

    ints?: [int]

    string?: str

    strings?: [str]

    version?: str

    versions?: [str]


