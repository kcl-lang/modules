"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema ClusterPortForward:
    """
    ClusterPortForward provides cluster-wide port forward configuration.

    Attributes
    ----------
    apiVersion : str, default is "kwok.x-k8s.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ClusterPortForward", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, required
        metadata
    spec : KwokxK8sIoV1alpha1ClusterPortForwardSpec, default is Undefined, required
        spec
    status : KwokxK8sIoV1alpha1ClusterPortForwardStatus, default is Undefined, optional
        status
    """


    apiVersion: "kwok.x-k8s.io/v1alpha1" = "kwok.x-k8s.io/v1alpha1"

    kind: "ClusterPortForward" = "ClusterPortForward"

    metadata: v1.ObjectMeta

    spec: KwokxK8sIoV1alpha1ClusterPortForwardSpec

    status?: KwokxK8sIoV1alpha1ClusterPortForwardStatus


schema KwokxK8sIoV1alpha1ClusterPortForwardSpec:
    """
    Spec holds spec for cluster port forward.

    Attributes
    ----------
    forwards : [KwokxK8sIoV1alpha1ClusterPortForwardSpecForwardsItems0], default is Undefined, required
        Forwards is a list of forwards to configure.
    selector : KwokxK8sIoV1alpha1ClusterPortForwardSpecSelector, default is Undefined, optional
        selector
    """


    forwards: [KwokxK8sIoV1alpha1ClusterPortForwardSpecForwardsItems0]

    selector?: KwokxK8sIoV1alpha1ClusterPortForwardSpecSelector


schema KwokxK8sIoV1alpha1ClusterPortForwardSpecForwardsItems0:
    """
    Forward holds information how to forward based on ports.

    Attributes
    ----------
    command : [str], default is Undefined, optional
        Command is the command to run to forward with stdin/stdout.
        if set, Target will be ignored.
    ports : [int], default is Undefined, optional
        Ports is a list of ports to forward.
        if not set, all ports will be forwarded.
    target : KwokxK8sIoV1alpha1ClusterPortForwardSpecForwardsItems0Target, default is Undefined, optional
        target
    """


    command?: [str]

    ports?: [int]

    target?: KwokxK8sIoV1alpha1ClusterPortForwardSpecForwardsItems0Target


schema KwokxK8sIoV1alpha1ClusterPortForwardSpecForwardsItems0Target:
    """
    Target is the target to forward to.

    Attributes
    ----------
    address : str, default is Undefined, required
        Address is the address to forward to.
    port : int, default is Undefined, required
        Port is the port to forward to.
    """


    address: str

    port: int


    check:
        len(address) >= 1
        port <= 65535
        port >= 0


schema KwokxK8sIoV1alpha1ClusterPortForwardSpecSelector:
    """
    Selector is a selector to filter pods to configure.

    Attributes
    ----------
    matchNames : [str], default is Undefined, optional
        MatchNames is a list of names to match.
        if not set, all names will be matched.
    matchNamespaces : [str], default is Undefined, optional
        MatchNamespaces is a list of namespaces to match.
        if not set, all namespaces will be matched.
    """


    matchNames?: [str]

    matchNamespaces?: [str]


schema KwokxK8sIoV1alpha1ClusterPortForwardStatus:
    """
    Status holds status for cluster port forward

    Attributes
    ----------
    conditions : [KwokxK8sIoV1alpha1ClusterPortForwardStatusConditionsItems0], default is Undefined, optional
        Conditions holds conditions for cluster port forward.
    """


    conditions?: [KwokxK8sIoV1alpha1ClusterPortForwardStatusConditionsItems0]


schema KwokxK8sIoV1alpha1ClusterPortForwardStatusConditionsItems0:
    """
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        Message is a human readable message indicating details about the transition.
        This may be an empty string.
    reason : str, default is Undefined, required
        Reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        Status of the condition
    $type : str, default is Undefined, required
        Type of condition in CamelCase or in foo.example.com/CamelCase.
        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
        useful (see .node.status.conditions), the ability to deconflict is important.
        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
    """


    lastTransitionTime: str

    message: str

    reason: str

    status: str

    $type: str


    check:
        len(message) <= 32768
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


