"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Job:
    """
    Job is the Schema for the Jobs API. Jobs are actions that BigQuery runs on your behalf to load data, export data, query data, or copy data.

    Attributes
    ----------
    apiVersion : str, default is "bigquery.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Job", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : BigqueryGcpUpboundIoV1beta1JobSpec, default is Undefined, required
        spec
    status : BigqueryGcpUpboundIoV1beta1JobStatus, default is Undefined, optional
        status
    """


    apiVersion: "bigquery.gcp.upbound.io/v1beta1" = "bigquery.gcp.upbound.io/v1beta1"

    kind: "Job" = "Job"

    metadata?: v1.ObjectMeta

    spec: BigqueryGcpUpboundIoV1beta1JobSpec

    status?: BigqueryGcpUpboundIoV1beta1JobStatus


schema BigqueryGcpUpboundIoV1beta1JobSpec:
    """
    JobSpec defines the desired state of Job

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : BigqueryGcpUpboundIoV1beta1JobSpecForProvider, default is Undefined, required
        for provider
    initProvider : BigqueryGcpUpboundIoV1beta1JobSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : BigqueryGcpUpboundIoV1beta1JobSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : BigqueryGcpUpboundIoV1beta1JobSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: BigqueryGcpUpboundIoV1beta1JobSpecForProvider

    initProvider?: BigqueryGcpUpboundIoV1beta1JobSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: BigqueryGcpUpboundIoV1beta1JobSpecProviderConfigRef

    publishConnectionDetailsTo?: BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: BigqueryGcpUpboundIoV1beta1JobSpecWriteConnectionSecretToRef


schema BigqueryGcpUpboundIoV1beta1JobSpecForProvider:
    """
    bigquery gcp upbound io v1beta1 job spec for provider

    Attributes
    ----------
    copy : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0], default is Undefined, optional
        Copies a table.
        Structure is documented below.
    extract : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0], default is Undefined, optional
        Configures an extract job.
        Structure is documented below.
    jobId : str, default is Undefined, optional
        The ID of the job. The ID must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). The maximum length is 1,024 characters.
    jobTimeoutMs : str, default is Undefined, optional
        Job timeout in milliseconds. If this time limit is exceeded, BigQuery may attempt to terminate the job.
    labels : {str:str}, default is Undefined, optional
        The labels associated with this job. You can use these to organize and group your jobs.
    load : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0], default is Undefined, optional
        Configures a load job.
        Structure is documented below.
    location : str, default is Undefined, optional
        The geographic location of the job. The default value is US.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    query : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0], default is Undefined, optional
        Configures a query job.
        Structure is documented below.
    """


    copy?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0]

    extract?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0]

    jobId?: str

    jobTimeoutMs?: str

    labels?: {str:str}

    load?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0]

    location?: str

    project?: str

    query?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0]


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider copy items0

    Attributes
    ----------
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0], default is Undefined, optional
        The destination table.
        Structure is documented below.
    sourceTables : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0SourceTablesItems0], default is Undefined, optional
        Source tables to copy.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    createDisposition?: str

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0]

    sourceTables?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0SourceTablesItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider copy items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    kmsKeyNameRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRef, default is Undefined, optional
        kms key name ref
    kmsKeyNameSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelector, default is Undefined, optional
        kms key name selector
    """


    kmsKeyName?: str

    kmsKeyNameRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRef

    kmsKeyNameSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRef:
    """
    Reference to a CryptoKey in kms to populate kmsKeyName.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelector:
    """
    Selector for a CryptoKey in kms to populate kmsKeyName.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider copy items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0DestinationTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderCopyItems0SourceTablesItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider copy items0 source tables items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider extract items0

    Attributes
    ----------
    compression : str, default is Undefined, optional
        The compression type to use for exported files. Possible values include GZIP, DEFLATE, SNAPPY, and NONE.
        The default value is NONE. DEFLATE and SNAPPY are only supported for Avro.
    destinationFormat : str, default is Undefined, optional
        The exported file format. Possible values include CSV, NEWLINE_DELIMITED_JSON and AVRO for tables and SAVED_MODEL for models.
        The default value for tables is CSV. Tables with nested or repeated fields cannot be exported as CSV.
        The default value for models is SAVED_MODEL.
    destinationUris : [str], default is Undefined, optional
        A list of fully-qualified Google Cloud Storage URIs where the extracted table should be written.
    fieldDelimiter : str, default is Undefined, optional
        When extracting data in CSV format, this defines the delimiter to use between fields in the exported data.
        Default is ','
    printHeader : bool, default is Undefined, optional
        Whether to print out a header row in the results. Default is true.
    sourceModel : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceModelItems0], default is Undefined, optional
        A reference to the model being exported.
        Structure is documented below.
    sourceTable : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0], default is Undefined, optional
        A reference to the table being exported.
        Structure is documented below.
    useAvroLogicalTypes : bool, default is Undefined, optional
        Whether to use logical types when extracting to AVRO format.
    """


    compression?: str

    destinationFormat?: str

    destinationUris?: [str]

    fieldDelimiter?: str

    printHeader?: bool

    sourceModel?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceModelItems0]

    sourceTable?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0]

    useAvroLogicalTypes?: bool


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceModelItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider extract items0 source model items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    modelId : str, default is Undefined, optional
        The ID of the model.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    """


    datasetId?: str

    modelId?: str

    projectId?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider extract items0 source table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderExtractItems0SourceTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider load items0

    Attributes
    ----------
    allowJaggedRows : bool, default is Undefined, optional
        Accept rows that are missing trailing optional columns. The missing values are treated as nulls.
        If false, records with missing trailing columns are treated as bad records, and if there are too many bad records,
        an invalid error is returned in the job result. The default value is false. Only applicable to CSV, ignored for other formats.
    allowQuotedNewlines : bool, default is Undefined, optional
        Indicates if BigQuery should allow quoted data sections that contain newline characters in a CSV file.
        The default value is false.
    autodetect : bool, default is Undefined, optional
        Indicates if we should automatically infer the options and schema for CSV and JSON sources.
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0], default is Undefined, optional
        The destination table to load the data into.
        Structure is documented below.
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8 or ISO-8859-1.
        The default value is UTF-8. BigQuery decodes the data after the raw, binary data
        has been split using the values of the quote and fieldDelimiter properties.
    fieldDelimiter : str, default is Undefined, optional
        The separator for fields in a CSV file. The separator can be any ISO-8859-1 single-byte character.
        To use a character in the range 128-255, you must encode the character as UTF8. BigQuery converts
        the string to ISO-8859-1 encoding, and then uses the first byte of the encoded string to split the
        data in its raw, binary state. BigQuery also supports the escape sequence "\t" to specify a tab separator.
        The default value is a comma (',').
    ignoreUnknownValues : bool, default is Undefined, optional
        Indicates if BigQuery should allow extra values that are not represented in the table schema.
        If true, the extra values are ignored. If false, records with extra columns are treated as bad records,
        and if there are too many bad records, an invalid error is returned in the job result.
        The default value is false. The sourceFormat property determines what BigQuery treats as an extra value:
        CSV: Trailing columns
        JSON: Named values that don't match any column names
    jsonExtension : str, default is Undefined, optional
        If sourceFormat is set to newline-delimited JSON, indicates whether it should be processed as a JSON variant such as GeoJSON.
        For a sourceFormat other than JSON, omit this field. If the sourceFormat is newline-delimited JSON: - for newline-delimited
        GeoJSON: set to GEOJSON.
    maxBadRecords : float, default is Undefined, optional
        The maximum number of bad records that BigQuery can ignore when running the job. If the number of bad records exceeds this value,
        an invalid error is returned in the job result. The default value is 0, which requires that all records are valid.
    nullMarker : str, default is Undefined, optional
        Specifies a string that represents a null value in a CSV file. For example, if you specify "\N", BigQuery interprets "\N" as a null value
        when loading a CSV file. The default value is the empty string. If you set this property to a custom value, BigQuery throws an error if an
        empty string is present for all data types except for STRING and BYTE. For STRING and BYTE columns, BigQuery interprets the empty string as
        an empty value.
    parquetOptions : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0ParquetOptionsItems0], default is Undefined, optional
        Parquet Options for load and make external tables.
        Structure is documented below.
    projectionFields : [str], default is Undefined, optional
        If sourceFormat is set to "DATASTORE_BACKUP", indicates which entity properties to load into BigQuery from a Cloud Datastore backup.
        Property names are case sensitive and must be top-level properties. If no properties are specified, BigQuery loads all properties.
        If any named property isn't found in the Cloud Datastore backup, an invalid error is returned in the job result.
    quote : str, default is Undefined, optional
        The value that is used to quote data sections in a CSV file. BigQuery converts the string to ISO-8859-1 encoding,
        and then uses the first byte of the encoded string to split the data in its raw, binary state.
        The default value is a double-quote ('"'). If your data does not contain quoted sections, set the property value to an empty string.
        If your data contains quoted newline characters, you must also set the allowQuotedNewlines property to true.
    schemaUpdateOptions : [str], default is Undefined, optional
        Allows the schema of the destination table to be updated as a side effect of the load job if a schema is autodetected or
        supplied in the job configuration. Schema update options are supported in two cases: when writeDisposition is WRITE_APPEND;
        when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table, specified by partition decorators.
        For normal tables, WRITE_TRUNCATE will always overwrite the schema. One or more of the following values are specified:
        ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema.
        ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of a CSV file that BigQuery will skip when loading the data.
        The default value is 0. This property is useful if you have header rows in the file that should be skipped.
        When autodetect is on, the behavior is the following:
        skipLeadingRows unspecified - Autodetect tries to detect headers in the first row. If they are not detected,
        the row is read as data. Otherwise data is read starting from the second row.
        skipLeadingRows is 0 - Instructs autodetect that there are no headers and data should be read starting from the first row.
        skipLeadingRows = N > 0 - Autodetect skips N-1 rows and tries to detect headers in row N. If headers are not detected,
        row N is just skipped. Otherwise row N is used to extract column names for the detected schema.
    sourceFormat : str, default is Undefined, optional
        The format of the data files. For CSV files, specify "CSV". For datastore backups, specify "DATASTORE_BACKUP".
        For newline-delimited JSON, specify "NEWLINE_DELIMITED_JSON". For Avro, specify "AVRO". For parquet, specify "PARQUET".
        For orc, specify "ORC". [Beta] For Bigtable, specify "BIGTABLE".
        The default value is CSV.
    sourceUris : [str], default is Undefined, optional
        The fully-qualified URIs that point to your data in Google Cloud.
        For Google Cloud Storage URIs: Each URI can contain one '*' wildcard character
        and it must come after the 'bucket' name. Size limits related to load jobs apply
        to external data sources. For Google Cloud Bigtable URIs: Exactly one URI can be
        specified and it has be a fully specified and valid HTTPS URL for a Google Cloud Bigtable table.
        For Google Cloud Datastore backups: Exactly one URI can be specified. Also, the '*' wildcard character is not allowed.
    timePartitioning : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0TimePartitioningItems0], default is Undefined, optional
        Time-based partitioning specification for the destination table.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    allowJaggedRows?: bool

    allowQuotedNewlines?: bool

    autodetect?: bool

    createDisposition?: str

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0]

    encoding?: str

    fieldDelimiter?: str

    ignoreUnknownValues?: bool

    jsonExtension?: str

    maxBadRecords?: float

    nullMarker?: str

    parquetOptions?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0ParquetOptionsItems0]

    projectionFields?: [str]

    quote?: str

    schemaUpdateOptions?: [str]

    skipLeadingRows?: float

    sourceFormat?: str

    sourceUris?: [str]

    timePartitioning?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0TimePartitioningItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider load items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    """


    kmsKeyName?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider load items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0DestinationTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0ParquetOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider load items0 parquet options items0

    Attributes
    ----------
    enableListInference : bool, default is Undefined, optional
        If sourceFormat is set to PARQUET, indicates whether to use schema inference specifically for Parquet LIST logical type.
    enumAsString : bool, default is Undefined, optional
        If sourceFormat is set to PARQUET, indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default.
    """


    enableListInference?: bool

    enumAsString?: bool


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderLoadItems0TimePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider load items0 time partitioning items0

    Attributes
    ----------
    expirationMs : str, default is Undefined, optional
        Number of milliseconds for which to keep the storage for a partition. A wrapper is used here because 0 is an invalid value.
    field : str, default is Undefined, optional
        If not set, the table is partitioned by pseudo column '_PARTITIONTIME'; if set, the table is partitioned by this field.
        The field must be a top-level TIMESTAMP or DATE field. Its mode must be NULLABLE or REQUIRED.
        A wrapper is used here because an empty string is an invalid value.
    $type : str, default is Undefined, optional
        The only type supported is DAY, which will generate one partition per day. Providing an empty string used to cause an error,
        but in OnePlatform the field will be treated as unset.
    """


    expirationMs?: str

    field?: str

    $type?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider query items0

    Attributes
    ----------
    allowLargeResults : bool, default is Undefined, optional
        If true and query uses legacy SQL dialect, allows the query to produce arbitrarily large result tables at a slight cost in performance.
        Requires destinationTable to be set. For standard SQL queries, this flag is ignored and large results are always allowed.
        However, you must still set destinationTable when result size exceeds the allowed maximum response size.
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    defaultDataset : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0], default is Undefined, optional
        Specifies the default dataset to use for unqualified table names in the query. Note that this does not alter behavior of unqualified dataset names.
        Structure is documented below.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0], default is Undefined, optional
        Describes the table where the query results should be stored.
        This property must be set for large results that exceed the maximum response size.
        For queries that produce anonymous (cached) results, this field will be populated by BigQuery.
        Structure is documented below.
    flattenResults : bool, default is Undefined, optional
        If true and query uses legacy SQL dialect, flattens all nested and repeated fields in the query results.
        allowLargeResults must be true if this is set to false. For standard SQL queries, this flag is ignored and results are never flattened.
    maximumBillingTier : float, default is Undefined, optional
        Limits the billing tier for this job. Queries that have resource usage beyond this tier will fail (without incurring a charge).
        If unspecified, this will be set to your project default.
    maximumBytesBilled : str, default is Undefined, optional
        Limits the bytes billed for this job. Queries that will have bytes billed beyond this limit will fail (without incurring a charge).
        If unspecified, this will be set to your project default.
    parameterMode : str, default is Undefined, optional
        Standard SQL only. Set to POSITIONAL to use positional (?) query parameters or to NAMED to use named (@myparam) query parameters in this query.
    priority : str, default is Undefined, optional
        Specifies a priority for the query.
        Default value is INTERACTIVE.
        Possible values are: INTERACTIVE, BATCH.
    query : str, default is Undefined, optional
        SQL query text to execute. The useLegacySql field can be used to indicate whether the query uses legacy SQL or standard SQL.
        NOTE: queries containing DML language
        (DELETE, UPDATE, MERGE, INSERT) must specify create_disposition = "" and write_disposition = "".
    schemaUpdateOptions : [str], default is Undefined, optional
        Allows the schema of the destination table to be updated as a side effect of the query job.
        Schema update options are supported in two cases: when writeDisposition is WRITE_APPEND;
        when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table,
        specified by partition decorators. For normal tables, WRITE_TRUNCATE will always overwrite the schema.
        One or more of the following values are specified:
        ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema.
        ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable.
    scriptOptions : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0ScriptOptionsItems0], default is Undefined, optional
        Options controlling the execution of scripts.
        Structure is documented below.
    useLegacySql : bool, default is Undefined, optional
        Specifies whether to use BigQuery's legacy SQL dialect for this query. The default value is true.
        If set to false, the query will use BigQuery's standard SQL.
    useQueryCache : bool, default is Undefined, optional
        Whether to look for the result in the query cache. The query cache is a best-effort cache that will be flushed whenever
        tables in the query are modified. Moreover, the query cache is only available when a query does not have a destination table specified.
        The default value is true.
    userDefinedFunctionResources : [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0UserDefinedFunctionResourcesItems0], default is Undefined, optional
        Describes user-defined function resources used in the query.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    allowLargeResults?: bool

    createDisposition?: str

    defaultDataset?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0]

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0]

    flattenResults?: bool

    maximumBillingTier?: float

    maximumBytesBilled?: str

    parameterMode?: str

    priority?: str

    query?: str

    schemaUpdateOptions?: [str]

    scriptOptions?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0ScriptOptionsItems0]

    useLegacySql?: bool

    useQueryCache?: bool

    userDefinedFunctionResources?: [BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0UserDefinedFunctionResourcesItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider query items0 default dataset items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDSelector

    projectId?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DefaultDatasetItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider query items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    """


    kmsKeyName?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider query items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0DestinationTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0ScriptOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider query items0 script options items0

    Attributes
    ----------
    keyResultStatement : str, default is Undefined, optional
        Determines which statement in the script represents the "key result",
        used to populate the schema and query results of the script job.
        Possible values are: LAST, FIRST_SELECT.
    statementByteBudget : str, default is Undefined, optional
        Limit on the number of bytes billed per statement. Exceeding this budget results in an error.
    statementTimeoutMs : str, default is Undefined, optional
        Timeout period for each statement in a script.
    """


    keyResultStatement?: str

    statementByteBudget?: str

    statementTimeoutMs?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecForProviderQueryItems0UserDefinedFunctionResourcesItems0:
    """
    bigquery gcp upbound io v1beta1 job spec for provider query items0 user defined function resources items0

    Attributes
    ----------
    inlineCode : str, default is Undefined, optional
        An inline resource that contains code for a user-defined function (UDF).
        Providing a inline code resource is equivalent to providing a URI for a file containing the same code.
    resourceUri : str, default is Undefined, optional
        A code resource to load from a Google Cloud Storage URI (gs://bucket/path).
    """


    inlineCode?: str

    resourceUri?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    copy : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0], default is Undefined, optional
        Copies a table.
        Structure is documented below.
    extract : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0], default is Undefined, optional
        Configures an extract job.
        Structure is documented below.
    jobId : str, default is Undefined, optional
        The ID of the job. The ID must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). The maximum length is 1,024 characters.
    jobTimeoutMs : str, default is Undefined, optional
        Job timeout in milliseconds. If this time limit is exceeded, BigQuery may attempt to terminate the job.
    labels : {str:str}, default is Undefined, optional
        The labels associated with this job. You can use these to organize and group your jobs.
    load : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0], default is Undefined, optional
        Configures a load job.
        Structure is documented below.
    location : str, default is Undefined, optional
        The geographic location of the job. The default value is US.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    query : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0], default is Undefined, optional
        Configures a query job.
        Structure is documented below.
    """


    copy?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0]

    extract?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0]

    jobId?: str

    jobTimeoutMs?: str

    labels?: {str:str}

    load?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0]

    location?: str

    project?: str

    query?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0]


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider copy items0

    Attributes
    ----------
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0], default is Undefined, optional
        The destination table.
        Structure is documented below.
    sourceTables : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0SourceTablesItems0], default is Undefined, optional
        Source tables to copy.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    createDisposition?: str

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0]

    sourceTables?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0SourceTablesItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider copy items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    kmsKeyNameRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRef, default is Undefined, optional
        kms key name ref
    kmsKeyNameSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelector, default is Undefined, optional
        kms key name selector
    """


    kmsKeyName?: str

    kmsKeyNameRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRef

    kmsKeyNameSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRef:
    """
    Reference to a CryptoKey in kms to populate kmsKeyName.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelector:
    """
    Selector for a CryptoKey in kms to populate kmsKeyName.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationEncryptionConfigurationItems0KmsKeyNameSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider copy items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0DestinationTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderCopyItems0SourceTablesItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider copy items0 source tables items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider extract items0

    Attributes
    ----------
    compression : str, default is Undefined, optional
        The compression type to use for exported files. Possible values include GZIP, DEFLATE, SNAPPY, and NONE.
        The default value is NONE. DEFLATE and SNAPPY are only supported for Avro.
    destinationFormat : str, default is Undefined, optional
        The exported file format. Possible values include CSV, NEWLINE_DELIMITED_JSON and AVRO for tables and SAVED_MODEL for models.
        The default value for tables is CSV. Tables with nested or repeated fields cannot be exported as CSV.
        The default value for models is SAVED_MODEL.
    destinationUris : [str], default is Undefined, optional
        A list of fully-qualified Google Cloud Storage URIs where the extracted table should be written.
    fieldDelimiter : str, default is Undefined, optional
        When extracting data in CSV format, this defines the delimiter to use between fields in the exported data.
        Default is ','
    printHeader : bool, default is Undefined, optional
        Whether to print out a header row in the results. Default is true.
    sourceModel : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceModelItems0], default is Undefined, optional
        A reference to the model being exported.
        Structure is documented below.
    sourceTable : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0], default is Undefined, optional
        A reference to the table being exported.
        Structure is documented below.
    useAvroLogicalTypes : bool, default is Undefined, optional
        Whether to use logical types when extracting to AVRO format.
    """


    compression?: str

    destinationFormat?: str

    destinationUris?: [str]

    fieldDelimiter?: str

    printHeader?: bool

    sourceModel?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceModelItems0]

    sourceTable?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0]

    useAvroLogicalTypes?: bool


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceModelItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider extract items0 source model items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    modelId : str, default is Undefined, optional
        The ID of the model.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    """


    datasetId?: str

    modelId?: str

    projectId?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider extract items0 source table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderExtractItems0SourceTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider load items0

    Attributes
    ----------
    allowJaggedRows : bool, default is Undefined, optional
        Accept rows that are missing trailing optional columns. The missing values are treated as nulls.
        If false, records with missing trailing columns are treated as bad records, and if there are too many bad records,
        an invalid error is returned in the job result. The default value is false. Only applicable to CSV, ignored for other formats.
    allowQuotedNewlines : bool, default is Undefined, optional
        Indicates if BigQuery should allow quoted data sections that contain newline characters in a CSV file.
        The default value is false.
    autodetect : bool, default is Undefined, optional
        Indicates if we should automatically infer the options and schema for CSV and JSON sources.
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0], default is Undefined, optional
        The destination table to load the data into.
        Structure is documented below.
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8 or ISO-8859-1.
        The default value is UTF-8. BigQuery decodes the data after the raw, binary data
        has been split using the values of the quote and fieldDelimiter properties.
    fieldDelimiter : str, default is Undefined, optional
        The separator for fields in a CSV file. The separator can be any ISO-8859-1 single-byte character.
        To use a character in the range 128-255, you must encode the character as UTF8. BigQuery converts
        the string to ISO-8859-1 encoding, and then uses the first byte of the encoded string to split the
        data in its raw, binary state. BigQuery also supports the escape sequence "\t" to specify a tab separator.
        The default value is a comma (',').
    ignoreUnknownValues : bool, default is Undefined, optional
        Indicates if BigQuery should allow extra values that are not represented in the table schema.
        If true, the extra values are ignored. If false, records with extra columns are treated as bad records,
        and if there are too many bad records, an invalid error is returned in the job result.
        The default value is false. The sourceFormat property determines what BigQuery treats as an extra value:
        CSV: Trailing columns
        JSON: Named values that don't match any column names
    jsonExtension : str, default is Undefined, optional
        If sourceFormat is set to newline-delimited JSON, indicates whether it should be processed as a JSON variant such as GeoJSON.
        For a sourceFormat other than JSON, omit this field. If the sourceFormat is newline-delimited JSON: - for newline-delimited
        GeoJSON: set to GEOJSON.
    maxBadRecords : float, default is Undefined, optional
        The maximum number of bad records that BigQuery can ignore when running the job. If the number of bad records exceeds this value,
        an invalid error is returned in the job result. The default value is 0, which requires that all records are valid.
    nullMarker : str, default is Undefined, optional
        Specifies a string that represents a null value in a CSV file. For example, if you specify "\N", BigQuery interprets "\N" as a null value
        when loading a CSV file. The default value is the empty string. If you set this property to a custom value, BigQuery throws an error if an
        empty string is present for all data types except for STRING and BYTE. For STRING and BYTE columns, BigQuery interprets the empty string as
        an empty value.
    parquetOptions : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0ParquetOptionsItems0], default is Undefined, optional
        Parquet Options for load and make external tables.
        Structure is documented below.
    projectionFields : [str], default is Undefined, optional
        If sourceFormat is set to "DATASTORE_BACKUP", indicates which entity properties to load into BigQuery from a Cloud Datastore backup.
        Property names are case sensitive and must be top-level properties. If no properties are specified, BigQuery loads all properties.
        If any named property isn't found in the Cloud Datastore backup, an invalid error is returned in the job result.
    quote : str, default is Undefined, optional
        The value that is used to quote data sections in a CSV file. BigQuery converts the string to ISO-8859-1 encoding,
        and then uses the first byte of the encoded string to split the data in its raw, binary state.
        The default value is a double-quote ('"'). If your data does not contain quoted sections, set the property value to an empty string.
        If your data contains quoted newline characters, you must also set the allowQuotedNewlines property to true.
    schemaUpdateOptions : [str], default is Undefined, optional
        Allows the schema of the destination table to be updated as a side effect of the load job if a schema is autodetected or
        supplied in the job configuration. Schema update options are supported in two cases: when writeDisposition is WRITE_APPEND;
        when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table, specified by partition decorators.
        For normal tables, WRITE_TRUNCATE will always overwrite the schema. One or more of the following values are specified:
        ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema.
        ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of a CSV file that BigQuery will skip when loading the data.
        The default value is 0. This property is useful if you have header rows in the file that should be skipped.
        When autodetect is on, the behavior is the following:
        skipLeadingRows unspecified - Autodetect tries to detect headers in the first row. If they are not detected,
        the row is read as data. Otherwise data is read starting from the second row.
        skipLeadingRows is 0 - Instructs autodetect that there are no headers and data should be read starting from the first row.
        skipLeadingRows = N > 0 - Autodetect skips N-1 rows and tries to detect headers in row N. If headers are not detected,
        row N is just skipped. Otherwise row N is used to extract column names for the detected schema.
    sourceFormat : str, default is Undefined, optional
        The format of the data files. For CSV files, specify "CSV". For datastore backups, specify "DATASTORE_BACKUP".
        For newline-delimited JSON, specify "NEWLINE_DELIMITED_JSON". For Avro, specify "AVRO". For parquet, specify "PARQUET".
        For orc, specify "ORC". [Beta] For Bigtable, specify "BIGTABLE".
        The default value is CSV.
    sourceUris : [str], default is Undefined, optional
        The fully-qualified URIs that point to your data in Google Cloud.
        For Google Cloud Storage URIs: Each URI can contain one '*' wildcard character
        and it must come after the 'bucket' name. Size limits related to load jobs apply
        to external data sources. For Google Cloud Bigtable URIs: Exactly one URI can be
        specified and it has be a fully specified and valid HTTPS URL for a Google Cloud Bigtable table.
        For Google Cloud Datastore backups: Exactly one URI can be specified. Also, the '*' wildcard character is not allowed.
    timePartitioning : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0TimePartitioningItems0], default is Undefined, optional
        Time-based partitioning specification for the destination table.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    allowJaggedRows?: bool

    allowQuotedNewlines?: bool

    autodetect?: bool

    createDisposition?: str

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0]

    encoding?: str

    fieldDelimiter?: str

    ignoreUnknownValues?: bool

    jsonExtension?: str

    maxBadRecords?: float

    nullMarker?: str

    parquetOptions?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0ParquetOptionsItems0]

    projectionFields?: [str]

    quote?: str

    schemaUpdateOptions?: [str]

    skipLeadingRows?: float

    sourceFormat?: str

    sourceUris?: [str]

    timePartitioning?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0TimePartitioningItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider load items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    """


    kmsKeyName?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider load items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0DestinationTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0ParquetOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider load items0 parquet options items0

    Attributes
    ----------
    enableListInference : bool, default is Undefined, optional
        If sourceFormat is set to PARQUET, indicates whether to use schema inference specifically for Parquet LIST logical type.
    enumAsString : bool, default is Undefined, optional
        If sourceFormat is set to PARQUET, indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default.
    """


    enableListInference?: bool

    enumAsString?: bool


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderLoadItems0TimePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider load items0 time partitioning items0

    Attributes
    ----------
    expirationMs : str, default is Undefined, optional
        Number of milliseconds for which to keep the storage for a partition. A wrapper is used here because 0 is an invalid value.
    field : str, default is Undefined, optional
        If not set, the table is partitioned by pseudo column '_PARTITIONTIME'; if set, the table is partitioned by this field.
        The field must be a top-level TIMESTAMP or DATE field. Its mode must be NULLABLE or REQUIRED.
        A wrapper is used here because an empty string is an invalid value.
    $type : str, default is Undefined, optional
        The only type supported is DAY, which will generate one partition per day. Providing an empty string used to cause an error,
        but in OnePlatform the field will be treated as unset.
    """


    expirationMs?: str

    field?: str

    $type?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider query items0

    Attributes
    ----------
    allowLargeResults : bool, default is Undefined, optional
        If true and query uses legacy SQL dialect, allows the query to produce arbitrarily large result tables at a slight cost in performance.
        Requires destinationTable to be set. For standard SQL queries, this flag is ignored and large results are always allowed.
        However, you must still set destinationTable when result size exceeds the allowed maximum response size.
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    defaultDataset : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0], default is Undefined, optional
        Specifies the default dataset to use for unqualified table names in the query. Note that this does not alter behavior of unqualified dataset names.
        Structure is documented below.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0], default is Undefined, optional
        Describes the table where the query results should be stored.
        This property must be set for large results that exceed the maximum response size.
        For queries that produce anonymous (cached) results, this field will be populated by BigQuery.
        Structure is documented below.
    flattenResults : bool, default is Undefined, optional
        If true and query uses legacy SQL dialect, flattens all nested and repeated fields in the query results.
        allowLargeResults must be true if this is set to false. For standard SQL queries, this flag is ignored and results are never flattened.
    maximumBillingTier : float, default is Undefined, optional
        Limits the billing tier for this job. Queries that have resource usage beyond this tier will fail (without incurring a charge).
        If unspecified, this will be set to your project default.
    maximumBytesBilled : str, default is Undefined, optional
        Limits the bytes billed for this job. Queries that will have bytes billed beyond this limit will fail (without incurring a charge).
        If unspecified, this will be set to your project default.
    parameterMode : str, default is Undefined, optional
        Standard SQL only. Set to POSITIONAL to use positional (?) query parameters or to NAMED to use named (@myparam) query parameters in this query.
    priority : str, default is Undefined, optional
        Specifies a priority for the query.
        Default value is INTERACTIVE.
        Possible values are: INTERACTIVE, BATCH.
    query : str, default is Undefined, optional
        SQL query text to execute. The useLegacySql field can be used to indicate whether the query uses legacy SQL or standard SQL.
        NOTE: queries containing DML language
        (DELETE, UPDATE, MERGE, INSERT) must specify create_disposition = "" and write_disposition = "".
    schemaUpdateOptions : [str], default is Undefined, optional
        Allows the schema of the destination table to be updated as a side effect of the query job.
        Schema update options are supported in two cases: when writeDisposition is WRITE_APPEND;
        when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table,
        specified by partition decorators. For normal tables, WRITE_TRUNCATE will always overwrite the schema.
        One or more of the following values are specified:
        ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema.
        ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable.
    scriptOptions : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0ScriptOptionsItems0], default is Undefined, optional
        Options controlling the execution of scripts.
        Structure is documented below.
    useLegacySql : bool, default is Undefined, optional
        Specifies whether to use BigQuery's legacy SQL dialect for this query. The default value is true.
        If set to false, the query will use BigQuery's standard SQL.
    useQueryCache : bool, default is Undefined, optional
        Whether to look for the result in the query cache. The query cache is a best-effort cache that will be flushed whenever
        tables in the query are modified. Moreover, the query cache is only available when a query does not have a destination table specified.
        The default value is true.
    userDefinedFunctionResources : [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0UserDefinedFunctionResourcesItems0], default is Undefined, optional
        Describes user-defined function resources used in the query.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    allowLargeResults?: bool

    createDisposition?: str

    defaultDataset?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0]

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0]

    flattenResults?: bool

    maximumBillingTier?: float

    maximumBytesBilled?: str

    parameterMode?: str

    priority?: str

    query?: str

    schemaUpdateOptions?: [str]

    scriptOptions?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0ScriptOptionsItems0]

    useLegacySql?: bool

    useQueryCache?: bool

    userDefinedFunctionResources?: [BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0UserDefinedFunctionResourcesItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider query items0 default dataset items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDSelector

    projectId?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DefaultDatasetItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider query items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    """


    kmsKeyName?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider query items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    datasetIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDRef, default is Undefined, optional
        dataset Id ref
    datasetIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDSelector, default is Undefined, optional
        dataset Id selector
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    tableIdRef : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDRef, default is Undefined, optional
        table Id ref
    tableIdSelector : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDSelector, default is Undefined, optional
        table Id selector
    """


    datasetId?: str

    datasetIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDRef

    datasetIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDSelector

    projectId?: str

    tableId?: str

    tableIdRef?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDRef

    tableIdSelector?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDSelector


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDRef:
    """
    Reference to a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDSelector:
    """
    Selector for a Dataset in bigquery to populate datasetId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0DatasetIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDRef:
    """
    Reference to a Table in bigquery to populate tableId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDSelector:
    """
    Selector for a Table in bigquery to populate tableId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDSelectorPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0DestinationTableItems0TableIDSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0ScriptOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider query items0 script options items0

    Attributes
    ----------
    keyResultStatement : str, default is Undefined, optional
        Determines which statement in the script represents the "key result",
        used to populate the schema and query results of the script job.
        Possible values are: LAST, FIRST_SELECT.
    statementByteBudget : str, default is Undefined, optional
        Limit on the number of bytes billed per statement. Exceeding this budget results in an error.
    statementTimeoutMs : str, default is Undefined, optional
        Timeout period for each statement in a script.
    """


    keyResultStatement?: str

    statementByteBudget?: str

    statementTimeoutMs?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecInitProviderQueryItems0UserDefinedFunctionResourcesItems0:
    """
    bigquery gcp upbound io v1beta1 job spec init provider query items0 user defined function resources items0

    Attributes
    ----------
    inlineCode : str, default is Undefined, optional
        An inline resource that contains code for a user-defined function (UDF).
        Providing a inline code resource is equivalent to providing a URI for a file containing the same code.
    resourceUri : str, default is Undefined, optional
        A code resource to load from a Google Cloud Storage URI (gs://bucket/path).
    """


    inlineCode?: str

    resourceUri?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecProviderConfigRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRef

    metadata?: BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToMetadata

    name: str


schema BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRefPolicy


schema BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema BigqueryGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema BigqueryGcpUpboundIoV1beta1JobSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema BigqueryGcpUpboundIoV1beta1JobStatus:
    """
    JobStatus defines the observed state of Job.

    Attributes
    ----------
    atProvider : BigqueryGcpUpboundIoV1beta1JobStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [BigqueryGcpUpboundIoV1beta1JobStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: BigqueryGcpUpboundIoV1beta1JobStatusAtProvider

    conditions?: [BigqueryGcpUpboundIoV1beta1JobStatusConditionsItems0]


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProvider:
    """
    bigquery gcp upbound io v1beta1 job status at provider

    Attributes
    ----------
    copy : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0], default is Undefined, optional
        Copies a table.
        Structure is documented below.
    effectiveLabels : {str:str}, default is Undefined, optional
        effective labels
    extract : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0], default is Undefined, optional
        Configures an extract job.
        Structure is documented below.
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/jobs/{{job_id}}
    jobId : str, default is Undefined, optional
        The ID of the job. The ID must contain only letters (a-z, A-Z), numbers (0-9), underscores (_), or dashes (-). The maximum length is 1,024 characters.
    jobTimeoutMs : str, default is Undefined, optional
        Job timeout in milliseconds. If this time limit is exceeded, BigQuery may attempt to terminate the job.
    jobType : str, default is Undefined, optional
        (Output)
        The type of the job.
    labels : {str:str}, default is Undefined, optional
        The labels associated with this job. You can use these to organize and group your jobs.
    load : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0], default is Undefined, optional
        Configures a load job.
        Structure is documented below.
    location : str, default is Undefined, optional
        The geographic location of the job. The default value is US.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    query : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0], default is Undefined, optional
        Configures a query job.
        Structure is documented below.
    status : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0], default is Undefined, optional
        The status of this job. Examine this value when polling an asynchronous job to see if the job is complete.
        Structure is documented below.
    terraformLabels : {str:str}, default is Undefined, optional
        (Output)
        The combination of labels configured directly on the resource
        and default labels configured on the provider.
    userEmail : str, default is Undefined, optional
        Email address of the user who ran the job.
    """


    copy?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0]

    effectiveLabels?: {str:str}

    extract?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0]

    id?: str

    jobId?: str

    jobTimeoutMs?: str

    jobType?: str

    labels?: {str:str}

    load?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0]

    location?: str

    project?: str

    query?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0]

    status?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0]

    terraformLabels?: {str:str}

    userEmail?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider copy items0

    Attributes
    ----------
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0DestinationTableItems0], default is Undefined, optional
        The destination table.
        Structure is documented below.
    sourceTables : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0SourceTablesItems0], default is Undefined, optional
        Source tables to copy.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    createDisposition?: str

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0DestinationTableItems0]

    sourceTables?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0SourceTablesItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider copy items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    kmsKeyVersion : str, default is Undefined, optional
        (Output)
        Describes the Cloud KMS encryption key version used to protect destination BigQuery table.
    """


    kmsKeyName?: str

    kmsKeyVersion?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider copy items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderCopyItems0SourceTablesItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider copy items0 source tables items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider extract items0

    Attributes
    ----------
    compression : str, default is Undefined, optional
        The compression type to use for exported files. Possible values include GZIP, DEFLATE, SNAPPY, and NONE.
        The default value is NONE. DEFLATE and SNAPPY are only supported for Avro.
    destinationFormat : str, default is Undefined, optional
        The exported file format. Possible values include CSV, NEWLINE_DELIMITED_JSON and AVRO for tables and SAVED_MODEL for models.
        The default value for tables is CSV. Tables with nested or repeated fields cannot be exported as CSV.
        The default value for models is SAVED_MODEL.
    destinationUris : [str], default is Undefined, optional
        A list of fully-qualified Google Cloud Storage URIs where the extracted table should be written.
    fieldDelimiter : str, default is Undefined, optional
        When extracting data in CSV format, this defines the delimiter to use between fields in the exported data.
        Default is ','
    printHeader : bool, default is Undefined, optional
        Whether to print out a header row in the results. Default is true.
    sourceModel : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0SourceModelItems0], default is Undefined, optional
        A reference to the model being exported.
        Structure is documented below.
    sourceTable : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0SourceTableItems0], default is Undefined, optional
        A reference to the table being exported.
        Structure is documented below.
    useAvroLogicalTypes : bool, default is Undefined, optional
        Whether to use logical types when extracting to AVRO format.
    """


    compression?: str

    destinationFormat?: str

    destinationUris?: [str]

    fieldDelimiter?: str

    printHeader?: bool

    sourceModel?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0SourceModelItems0]

    sourceTable?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0SourceTableItems0]

    useAvroLogicalTypes?: bool


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0SourceModelItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider extract items0 source model items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    modelId : str, default is Undefined, optional
        The ID of the model.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    """


    datasetId?: str

    modelId?: str

    projectId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderExtractItems0SourceTableItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider extract items0 source table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider load items0

    Attributes
    ----------
    allowJaggedRows : bool, default is Undefined, optional
        Accept rows that are missing trailing optional columns. The missing values are treated as nulls.
        If false, records with missing trailing columns are treated as bad records, and if there are too many bad records,
        an invalid error is returned in the job result. The default value is false. Only applicable to CSV, ignored for other formats.
    allowQuotedNewlines : bool, default is Undefined, optional
        Indicates if BigQuery should allow quoted data sections that contain newline characters in a CSV file.
        The default value is false.
    autodetect : bool, default is Undefined, optional
        Indicates if we should automatically infer the options and schema for CSV and JSON sources.
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0DestinationTableItems0], default is Undefined, optional
        The destination table to load the data into.
        Structure is documented below.
    encoding : str, default is Undefined, optional
        The character encoding of the data. The supported values are UTF-8 or ISO-8859-1.
        The default value is UTF-8. BigQuery decodes the data after the raw, binary data
        has been split using the values of the quote and fieldDelimiter properties.
    fieldDelimiter : str, default is Undefined, optional
        The separator for fields in a CSV file. The separator can be any ISO-8859-1 single-byte character.
        To use a character in the range 128-255, you must encode the character as UTF8. BigQuery converts
        the string to ISO-8859-1 encoding, and then uses the first byte of the encoded string to split the
        data in its raw, binary state. BigQuery also supports the escape sequence "\t" to specify a tab separator.
        The default value is a comma (',').
    ignoreUnknownValues : bool, default is Undefined, optional
        Indicates if BigQuery should allow extra values that are not represented in the table schema.
        If true, the extra values are ignored. If false, records with extra columns are treated as bad records,
        and if there are too many bad records, an invalid error is returned in the job result.
        The default value is false. The sourceFormat property determines what BigQuery treats as an extra value:
        CSV: Trailing columns
        JSON: Named values that don't match any column names
    jsonExtension : str, default is Undefined, optional
        If sourceFormat is set to newline-delimited JSON, indicates whether it should be processed as a JSON variant such as GeoJSON.
        For a sourceFormat other than JSON, omit this field. If the sourceFormat is newline-delimited JSON: - for newline-delimited
        GeoJSON: set to GEOJSON.
    maxBadRecords : float, default is Undefined, optional
        The maximum number of bad records that BigQuery can ignore when running the job. If the number of bad records exceeds this value,
        an invalid error is returned in the job result. The default value is 0, which requires that all records are valid.
    nullMarker : str, default is Undefined, optional
        Specifies a string that represents a null value in a CSV file. For example, if you specify "\N", BigQuery interprets "\N" as a null value
        when loading a CSV file. The default value is the empty string. If you set this property to a custom value, BigQuery throws an error if an
        empty string is present for all data types except for STRING and BYTE. For STRING and BYTE columns, BigQuery interprets the empty string as
        an empty value.
    parquetOptions : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0ParquetOptionsItems0], default is Undefined, optional
        Parquet Options for load and make external tables.
        Structure is documented below.
    projectionFields : [str], default is Undefined, optional
        If sourceFormat is set to "DATASTORE_BACKUP", indicates which entity properties to load into BigQuery from a Cloud Datastore backup.
        Property names are case sensitive and must be top-level properties. If no properties are specified, BigQuery loads all properties.
        If any named property isn't found in the Cloud Datastore backup, an invalid error is returned in the job result.
    quote : str, default is Undefined, optional
        The value that is used to quote data sections in a CSV file. BigQuery converts the string to ISO-8859-1 encoding,
        and then uses the first byte of the encoded string to split the data in its raw, binary state.
        The default value is a double-quote ('"'). If your data does not contain quoted sections, set the property value to an empty string.
        If your data contains quoted newline characters, you must also set the allowQuotedNewlines property to true.
    schemaUpdateOptions : [str], default is Undefined, optional
        Allows the schema of the destination table to be updated as a side effect of the load job if a schema is autodetected or
        supplied in the job configuration. Schema update options are supported in two cases: when writeDisposition is WRITE_APPEND;
        when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table, specified by partition decorators.
        For normal tables, WRITE_TRUNCATE will always overwrite the schema. One or more of the following values are specified:
        ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema.
        ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable.
    skipLeadingRows : float, default is Undefined, optional
        The number of rows at the top of a CSV file that BigQuery will skip when loading the data.
        The default value is 0. This property is useful if you have header rows in the file that should be skipped.
        When autodetect is on, the behavior is the following:
        skipLeadingRows unspecified - Autodetect tries to detect headers in the first row. If they are not detected,
        the row is read as data. Otherwise data is read starting from the second row.
        skipLeadingRows is 0 - Instructs autodetect that there are no headers and data should be read starting from the first row.
        skipLeadingRows = N > 0 - Autodetect skips N-1 rows and tries to detect headers in row N. If headers are not detected,
        row N is just skipped. Otherwise row N is used to extract column names for the detected schema.
    sourceFormat : str, default is Undefined, optional
        The format of the data files. For CSV files, specify "CSV". For datastore backups, specify "DATASTORE_BACKUP".
        For newline-delimited JSON, specify "NEWLINE_DELIMITED_JSON". For Avro, specify "AVRO". For parquet, specify "PARQUET".
        For orc, specify "ORC". [Beta] For Bigtable, specify "BIGTABLE".
        The default value is CSV.
    sourceUris : [str], default is Undefined, optional
        The fully-qualified URIs that point to your data in Google Cloud.
        For Google Cloud Storage URIs: Each URI can contain one '*' wildcard character
        and it must come after the 'bucket' name. Size limits related to load jobs apply
        to external data sources. For Google Cloud Bigtable URIs: Exactly one URI can be
        specified and it has be a fully specified and valid HTTPS URL for a Google Cloud Bigtable table.
        For Google Cloud Datastore backups: Exactly one URI can be specified. Also, the '*' wildcard character is not allowed.
    timePartitioning : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0TimePartitioningItems0], default is Undefined, optional
        Time-based partitioning specification for the destination table.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    allowJaggedRows?: bool

    allowQuotedNewlines?: bool

    autodetect?: bool

    createDisposition?: str

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0DestinationTableItems0]

    encoding?: str

    fieldDelimiter?: str

    ignoreUnknownValues?: bool

    jsonExtension?: str

    maxBadRecords?: float

    nullMarker?: str

    parquetOptions?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0ParquetOptionsItems0]

    projectionFields?: [str]

    quote?: str

    schemaUpdateOptions?: [str]

    skipLeadingRows?: float

    sourceFormat?: str

    sourceUris?: [str]

    timePartitioning?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0TimePartitioningItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider load items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    kmsKeyVersion : str, default is Undefined, optional
        (Output)
        Describes the Cloud KMS encryption key version used to protect destination BigQuery table.
    """


    kmsKeyName?: str

    kmsKeyVersion?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider load items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0ParquetOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider load items0 parquet options items0

    Attributes
    ----------
    enableListInference : bool, default is Undefined, optional
        If sourceFormat is set to PARQUET, indicates whether to use schema inference specifically for Parquet LIST logical type.
    enumAsString : bool, default is Undefined, optional
        If sourceFormat is set to PARQUET, indicates whether to infer Parquet ENUM logical type as STRING instead of BYTES by default.
    """


    enableListInference?: bool

    enumAsString?: bool


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderLoadItems0TimePartitioningItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider load items0 time partitioning items0

    Attributes
    ----------
    expirationMs : str, default is Undefined, optional
        Number of milliseconds for which to keep the storage for a partition. A wrapper is used here because 0 is an invalid value.
    field : str, default is Undefined, optional
        If not set, the table is partitioned by pseudo column '_PARTITIONTIME'; if set, the table is partitioned by this field.
        The field must be a top-level TIMESTAMP or DATE field. Its mode must be NULLABLE or REQUIRED.
        A wrapper is used here because an empty string is an invalid value.
    $type : str, default is Undefined, optional
        The only type supported is DAY, which will generate one partition per day. Providing an empty string used to cause an error,
        but in OnePlatform the field will be treated as unset.
    """


    expirationMs?: str

    field?: str

    $type?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider query items0

    Attributes
    ----------
    allowLargeResults : bool, default is Undefined, optional
        If true and query uses legacy SQL dialect, allows the query to produce arbitrarily large result tables at a slight cost in performance.
        Requires destinationTable to be set. For standard SQL queries, this flag is ignored and large results are always allowed.
        However, you must still set destinationTable when result size exceeds the allowed maximum response size.
    createDisposition : str, default is Undefined, optional
        Specifies whether the job is allowed to create new tables. The following values are supported:
        CREATE_IF_NEEDED: If the table does not exist, BigQuery creates the table.
        CREATE_NEVER: The table must already exist. If it does not, a 'notFound' error is returned in the job result.
        Creation, truncation and append actions occur as one atomic update upon job completion
        Default value is CREATE_IF_NEEDED.
        Possible values are: CREATE_IF_NEEDED, CREATE_NEVER.
    defaultDataset : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DefaultDatasetItems0], default is Undefined, optional
        Specifies the default dataset to use for unqualified table names in the query. Note that this does not alter behavior of unqualified dataset names.
        Structure is documented below.
    destinationEncryptionConfiguration : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DestinationEncryptionConfigurationItems0], default is Undefined, optional
        Custom encryption configuration (e.g., Cloud KMS keys)
        Structure is documented below.
    destinationTable : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DestinationTableItems0], default is Undefined, optional
        Describes the table where the query results should be stored.
        This property must be set for large results that exceed the maximum response size.
        For queries that produce anonymous (cached) results, this field will be populated by BigQuery.
        Structure is documented below.
    flattenResults : bool, default is Undefined, optional
        If true and query uses legacy SQL dialect, flattens all nested and repeated fields in the query results.
        allowLargeResults must be true if this is set to false. For standard SQL queries, this flag is ignored and results are never flattened.
    maximumBillingTier : float, default is Undefined, optional
        Limits the billing tier for this job. Queries that have resource usage beyond this tier will fail (without incurring a charge).
        If unspecified, this will be set to your project default.
    maximumBytesBilled : str, default is Undefined, optional
        Limits the bytes billed for this job. Queries that will have bytes billed beyond this limit will fail (without incurring a charge).
        If unspecified, this will be set to your project default.
    parameterMode : str, default is Undefined, optional
        Standard SQL only. Set to POSITIONAL to use positional (?) query parameters or to NAMED to use named (@myparam) query parameters in this query.
    priority : str, default is Undefined, optional
        Specifies a priority for the query.
        Default value is INTERACTIVE.
        Possible values are: INTERACTIVE, BATCH.
    query : str, default is Undefined, optional
        SQL query text to execute. The useLegacySql field can be used to indicate whether the query uses legacy SQL or standard SQL.
        NOTE: queries containing DML language
        (DELETE, UPDATE, MERGE, INSERT) must specify create_disposition = "" and write_disposition = "".
    schemaUpdateOptions : [str], default is Undefined, optional
        Allows the schema of the destination table to be updated as a side effect of the query job.
        Schema update options are supported in two cases: when writeDisposition is WRITE_APPEND;
        when writeDisposition is WRITE_TRUNCATE and the destination table is a partition of a table,
        specified by partition decorators. For normal tables, WRITE_TRUNCATE will always overwrite the schema.
        One or more of the following values are specified:
        ALLOW_FIELD_ADDITION: allow adding a nullable field to the schema.
        ALLOW_FIELD_RELAXATION: allow relaxing a required field in the original schema to nullable.
    scriptOptions : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0ScriptOptionsItems0], default is Undefined, optional
        Options controlling the execution of scripts.
        Structure is documented below.
    useLegacySql : bool, default is Undefined, optional
        Specifies whether to use BigQuery's legacy SQL dialect for this query. The default value is true.
        If set to false, the query will use BigQuery's standard SQL.
    useQueryCache : bool, default is Undefined, optional
        Whether to look for the result in the query cache. The query cache is a best-effort cache that will be flushed whenever
        tables in the query are modified. Moreover, the query cache is only available when a query does not have a destination table specified.
        The default value is true.
    userDefinedFunctionResources : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0UserDefinedFunctionResourcesItems0], default is Undefined, optional
        Describes user-defined function resources used in the query.
        Structure is documented below.
    writeDisposition : str, default is Undefined, optional
        Specifies the action that occurs if the destination table already exists. The following values are supported:
        WRITE_TRUNCATE: If the table already exists, BigQuery overwrites the table data and uses the schema from the query result.
        WRITE_APPEND: If the table already exists, BigQuery appends the data to the table.
        WRITE_EMPTY: If the table already exists and contains data, a 'duplicate' error is returned in the job result.
        Each action is atomic and only occurs if BigQuery is able to complete the job successfully.
        Creation, truncation and append actions occur as one atomic update upon job completion.
        Default value is WRITE_EMPTY.
        Possible values are: WRITE_TRUNCATE, WRITE_APPEND, WRITE_EMPTY.
    """


    allowLargeResults?: bool

    createDisposition?: str

    defaultDataset?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DefaultDatasetItems0]

    destinationEncryptionConfiguration?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DestinationEncryptionConfigurationItems0]

    destinationTable?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DestinationTableItems0]

    flattenResults?: bool

    maximumBillingTier?: float

    maximumBytesBilled?: str

    parameterMode?: str

    priority?: str

    query?: str

    schemaUpdateOptions?: [str]

    scriptOptions?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0ScriptOptionsItems0]

    useLegacySql?: bool

    useQueryCache?: bool

    userDefinedFunctionResources?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0UserDefinedFunctionResourcesItems0]

    writeDisposition?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DefaultDatasetItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider query items0 default dataset items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    """


    datasetId?: str

    projectId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DestinationEncryptionConfigurationItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider query items0 destination encryption configuration items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Describes the Cloud KMS encryption key that will be used to protect destination BigQuery table.
        The BigQuery Service Account associated with your project requires access to this encryption key.
    kmsKeyVersion : str, default is Undefined, optional
        (Output)
        Describes the Cloud KMS encryption key version used to protect destination BigQuery table.
    """


    kmsKeyName?: str

    kmsKeyVersion?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0DestinationTableItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider query items0 destination table items0

    Attributes
    ----------
    datasetId : str, default is Undefined, optional
        The ID of the dataset containing this table.
    projectId : str, default is Undefined, optional
        The ID of the project containing this table.
    tableId : str, default is Undefined, optional
        The table. Can be specified {{table_id}} if project_id and dataset_id are also set,
        or of the form projects/{{project}}/datasets/{{dataset_id}}/tables/{{table_id}} if not.
    """


    datasetId?: str

    projectId?: str

    tableId?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0ScriptOptionsItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider query items0 script options items0

    Attributes
    ----------
    keyResultStatement : str, default is Undefined, optional
        Determines which statement in the script represents the "key result",
        used to populate the schema and query results of the script job.
        Possible values are: LAST, FIRST_SELECT.
    statementByteBudget : str, default is Undefined, optional
        Limit on the number of bytes billed per statement. Exceeding this budget results in an error.
    statementTimeoutMs : str, default is Undefined, optional
        Timeout period for each statement in a script.
    """


    keyResultStatement?: str

    statementByteBudget?: str

    statementTimeoutMs?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderQueryItems0UserDefinedFunctionResourcesItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider query items0 user defined function resources items0

    Attributes
    ----------
    inlineCode : str, default is Undefined, optional
        An inline resource that contains code for a user-defined function (UDF).
        Providing a inline code resource is equivalent to providing a URI for a file containing the same code.
    resourceUri : str, default is Undefined, optional
        A code resource to load from a Google Cloud Storage URI (gs://bucket/path).
    """


    inlineCode?: str

    resourceUri?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider status items0

    Attributes
    ----------
    errorResult : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0ErrorResultItems0], default is Undefined, optional
        (Output)
        Final error result of the job. If present, indicates that the job has completed and was unsuccessful.
        Structure is documented below.
    errors : [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0ErrorsItems0], default is Undefined, optional
        (Output)
        The first errors encountered during the running of the job. The final message
        includes the number of errors that caused the process to stop. Errors here do
        not necessarily mean that the job has not completed or was unsuccessful.
        Structure is documented below.
    state : str, default is Undefined, optional
        (Output)
        Running state of the job. Valid states include 'PENDING', 'RUNNING', and 'DONE'.
    """


    errorResult?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0ErrorResultItems0]

    errors?: [BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0ErrorsItems0]

    state?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0ErrorResultItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider status items0 error result items0

    Attributes
    ----------
    location : str, default is Undefined, optional
        The geographic location of the job. The default value is US.
    message : str, default is Undefined, optional
        A human-readable description of the error.
    reason : str, default is Undefined, optional
        A short error code that summarizes the error.
    """


    location?: str

    message?: str

    reason?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusAtProviderStatusItems0ErrorsItems0:
    """
    bigquery gcp upbound io v1beta1 job status at provider status items0 errors items0

    Attributes
    ----------
    location : str, default is Undefined, optional
        The geographic location of the job. The default value is US.
    message : str, default is Undefined, optional
        A human-readable description of the error.
    reason : str, default is Undefined, optional
        A short error code that summarizes the error.
    """


    location?: str

    message?: str

    reason?: str


schema BigqueryGcpUpboundIoV1beta1JobStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


