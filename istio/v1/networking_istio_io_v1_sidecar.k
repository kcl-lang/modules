"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Sidecar:
    """
    networking istio io v1 sidecar

    Attributes
    ----------
    apiVersion : str, default is "networking.istio.io/v1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Sidecar", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingIstioIoV1SidecarSpec, default is Undefined, optional
        spec
    status : any, default is Undefined, optional
        status
    """


    apiVersion: "networking.istio.io/v1" = "networking.istio.io/v1"

    kind: "Sidecar" = "Sidecar"

    metadata?: v1.ObjectMeta

    spec?: NetworkingIstioIoV1SidecarSpec

    status?: any


schema NetworkingIstioIoV1SidecarSpec:
    """
    Configuration affecting network reachability of a sidecar. See more details at: https://istio.io/docs/reference/config/networking/sidecar.html

    Attributes
    ----------
    egress : [NetworkingIstioIoV1SidecarSpecEgressItems0], default is Undefined, optional
        Egress specifies the configuration of the sidecar for processing outbound traffic from the attached workload instance to other services in the mesh.
    inboundConnectionPool : NetworkingIstioIoV1SidecarSpecInboundConnectionPool, default is Undefined, optional
        inbound connection pool
    ingress : [NetworkingIstioIoV1SidecarSpecIngressItems0], default is Undefined, optional
        Ingress specifies the configuration of the sidecar for processing inbound traffic to the attached workload instance.
    outboundTrafficPolicy : NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicy, default is Undefined, optional
        outbound traffic policy
    workloadSelector : NetworkingIstioIoV1SidecarSpecWorkloadSelector, default is Undefined, optional
        workload selector
    """


    egress?: [NetworkingIstioIoV1SidecarSpecEgressItems0]

    inboundConnectionPool?: NetworkingIstioIoV1SidecarSpecInboundConnectionPool

    ingress?: [NetworkingIstioIoV1SidecarSpecIngressItems0]

    outboundTrafficPolicy?: NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicy

    workloadSelector?: NetworkingIstioIoV1SidecarSpecWorkloadSelector


schema NetworkingIstioIoV1SidecarSpecEgressItems0:
    """
    networking istio io v1 sidecar spec egress items0

    Attributes
    ----------
    bind : str, default is Undefined, optional
        The IP(IPv4 or IPv6) or the Unix domain socket to which the listener should be bound to.
    captureMode : str, default is Undefined, optional
        When the bind address is an IP, the captureMode option dictates how traffic to the listener is expected to be captured (or not).

        Valid Options: DEFAULT, IPTABLES, NONE
    hosts : [str], default is Undefined, required
        One or more service hosts exposed by the listener in `namespace/dnsName` format.
    port : NetworkingIstioIoV1SidecarSpecEgressItems0Port, default is Undefined, optional
        port
    """


    bind?: str

    captureMode?: "DEFAULT" | "IPTABLES" | "NONE"

    hosts: [str]

    port?: NetworkingIstioIoV1SidecarSpecEgressItems0Port


schema NetworkingIstioIoV1SidecarSpecEgressItems0Port:
    """
    The port associated with the listener.

    Attributes
    ----------
    name : str, default is Undefined, optional
        Label assigned to the port.
    number : int, default is Undefined, optional
        A valid non-negative integer port number.
    $protocol : str, default is Undefined, optional
        The protocol exposed on the port.
    targetPort : int, default is Undefined, optional
        target port
    """


    name?: str

    number?: int

    $protocol?: str

    targetPort?: int


    check:
        number <= 4.294967295e+09
        number >= 0
        targetPort <= 4.294967295e+09
        targetPort >= 0


schema NetworkingIstioIoV1SidecarSpecInboundConnectionPool:
    """
    Settings controlling the volume of connections Envoy will accept from the network.

    Attributes
    ----------
    http : NetworkingIstioIoV1SidecarSpecInboundConnectionPoolHTTP, default is Undefined, optional
        http
    tcp : NetworkingIstioIoV1SidecarSpecInboundConnectionPoolTCP, default is Undefined, optional
        tcp
    """


    http?: NetworkingIstioIoV1SidecarSpecInboundConnectionPoolHTTP

    tcp?: NetworkingIstioIoV1SidecarSpecInboundConnectionPoolTCP


schema NetworkingIstioIoV1SidecarSpecInboundConnectionPoolHTTP:
    """
    HTTP connection pool settings.

    Attributes
    ----------
    h2UpgradePolicy : str, default is Undefined, optional
        Specify if http1.1 connection should be upgraded to http2 for the associated destination.

        Valid Options: DEFAULT, DO_NOT_UPGRADE, UPGRADE
    http1MaxPendingRequests : int, default is Undefined, optional
        Maximum number of requests that will be queued while waiting for a ready connection pool connection.
    http2MaxRequests : int, default is Undefined, optional
        Maximum number of active requests to a destination.
    idleTimeout : str, default is Undefined, optional
        The idle timeout for upstream connection pool connections.
    maxConcurrentStreams : int, default is Undefined, optional
        The maximum number of concurrent streams allowed for a peer on one HTTP/2 connection.
    maxRequestsPerConnection : int, default is Undefined, optional
        Maximum number of requests per connection to a backend.
    maxRetries : int, default is Undefined, optional
        Maximum number of retries that can be outstanding to all hosts in a cluster at a given time.
    useClientProtocol : bool, default is Undefined, optional
        If set to true, client protocol will be preserved while initiating connection to backend.
    """


    h2UpgradePolicy?: "DEFAULT" | "DO_NOT_UPGRADE" | "UPGRADE"

    http1MaxPendingRequests?: int

    http2MaxRequests?: int

    idleTimeout?: str

    maxConcurrentStreams?: int

    maxRequestsPerConnection?: int

    maxRetries?: int

    useClientProtocol?: bool


schema NetworkingIstioIoV1SidecarSpecInboundConnectionPoolTCP:
    """
    Settings common to both HTTP and TCP upstream connections.

    Attributes
    ----------
    connectTimeout : str, default is Undefined, optional
        TCP connection timeout.
    idleTimeout : str, default is Undefined, optional
        The idle timeout for TCP connections.
    maxConnectionDuration : str, default is Undefined, optional
        The maximum duration of a connection.
    maxConnections : int, default is Undefined, optional
        Maximum number of HTTP1 /TCP connections to a destination host.
    tcpKeepalive : NetworkingIstioIoV1SidecarSpecInboundConnectionPoolTCPTCPKeepalive, default is Undefined, optional
        tcp keepalive
    """


    connectTimeout?: str

    idleTimeout?: str

    maxConnectionDuration?: str

    maxConnections?: int

    tcpKeepalive?: NetworkingIstioIoV1SidecarSpecInboundConnectionPoolTCPTCPKeepalive


schema NetworkingIstioIoV1SidecarSpecInboundConnectionPoolTCPTCPKeepalive:
    """
    If set then set SO_KEEPALIVE on the socket to enable TCP Keepalives.

    Attributes
    ----------
    interval : str, default is Undefined, optional
        The time duration between keep-alive probes.
    probes : int, default is Undefined, optional
        Maximum number of keepalive probes to send without response before deciding the connection is dead.
    time : str, default is Undefined, optional
        The time duration a connection needs to be idle before keep-alive probes start being sent.
    """


    interval?: str

    probes?: int

    time?: str


    check:
        probes <= 4.294967295e+09
        probes >= 0


schema NetworkingIstioIoV1SidecarSpecIngressItems0:
    """
    networking istio io v1 sidecar spec ingress items0

    Attributes
    ----------
    bind : str, default is Undefined, optional
        The IP(IPv4 or IPv6) to which the listener should be bound.
    captureMode : str, default is Undefined, optional
        The captureMode option dictates how traffic to the listener is expected to be captured (or not).

        Valid Options: DEFAULT, IPTABLES, NONE
    connectionPool : NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPool, default is Undefined, optional
        connection pool
    defaultEndpoint : str, default is Undefined, optional
        The IP endpoint or Unix domain socket to which traffic should be forwarded to.
    port : NetworkingIstioIoV1SidecarSpecIngressItems0Port, default is Undefined, required
        port
    tls : NetworkingIstioIoV1SidecarSpecIngressItems0TLS, default is Undefined, optional
        tls
    """


    bind?: str

    captureMode?: "DEFAULT" | "IPTABLES" | "NONE"

    connectionPool?: NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPool

    defaultEndpoint?: str

    port: NetworkingIstioIoV1SidecarSpecIngressItems0Port

    tls?: NetworkingIstioIoV1SidecarSpecIngressItems0TLS


schema NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPool:
    """
    Settings controlling the volume of connections Envoy will accept from the network.

    Attributes
    ----------
    http : NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolHTTP, default is Undefined, optional
        http
    tcp : NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolTCP, default is Undefined, optional
        tcp
    """


    http?: NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolHTTP

    tcp?: NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolTCP


schema NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolHTTP:
    """
    HTTP connection pool settings.

    Attributes
    ----------
    h2UpgradePolicy : str, default is Undefined, optional
        Specify if http1.1 connection should be upgraded to http2 for the associated destination.

        Valid Options: DEFAULT, DO_NOT_UPGRADE, UPGRADE
    http1MaxPendingRequests : int, default is Undefined, optional
        Maximum number of requests that will be queued while waiting for a ready connection pool connection.
    http2MaxRequests : int, default is Undefined, optional
        Maximum number of active requests to a destination.
    idleTimeout : str, default is Undefined, optional
        The idle timeout for upstream connection pool connections.
    maxConcurrentStreams : int, default is Undefined, optional
        The maximum number of concurrent streams allowed for a peer on one HTTP/2 connection.
    maxRequestsPerConnection : int, default is Undefined, optional
        Maximum number of requests per connection to a backend.
    maxRetries : int, default is Undefined, optional
        Maximum number of retries that can be outstanding to all hosts in a cluster at a given time.
    useClientProtocol : bool, default is Undefined, optional
        If set to true, client protocol will be preserved while initiating connection to backend.
    """


    h2UpgradePolicy?: "DEFAULT" | "DO_NOT_UPGRADE" | "UPGRADE"

    http1MaxPendingRequests?: int

    http2MaxRequests?: int

    idleTimeout?: str

    maxConcurrentStreams?: int

    maxRequestsPerConnection?: int

    maxRetries?: int

    useClientProtocol?: bool


schema NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolTCP:
    """
    Settings common to both HTTP and TCP upstream connections.

    Attributes
    ----------
    connectTimeout : str, default is Undefined, optional
        TCP connection timeout.
    idleTimeout : str, default is Undefined, optional
        The idle timeout for TCP connections.
    maxConnectionDuration : str, default is Undefined, optional
        The maximum duration of a connection.
    maxConnections : int, default is Undefined, optional
        Maximum number of HTTP1 /TCP connections to a destination host.
    tcpKeepalive : NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolTCPTCPKeepalive, default is Undefined, optional
        tcp keepalive
    """


    connectTimeout?: str

    idleTimeout?: str

    maxConnectionDuration?: str

    maxConnections?: int

    tcpKeepalive?: NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolTCPTCPKeepalive


schema NetworkingIstioIoV1SidecarSpecIngressItems0ConnectionPoolTCPTCPKeepalive:
    """
    If set then set SO_KEEPALIVE on the socket to enable TCP Keepalives.

    Attributes
    ----------
    interval : str, default is Undefined, optional
        The time duration between keep-alive probes.
    probes : int, default is Undefined, optional
        Maximum number of keepalive probes to send without response before deciding the connection is dead.
    time : str, default is Undefined, optional
        The time duration a connection needs to be idle before keep-alive probes start being sent.
    """


    interval?: str

    probes?: int

    time?: str


    check:
        probes <= 4.294967295e+09
        probes >= 0


schema NetworkingIstioIoV1SidecarSpecIngressItems0Port:
    """
    The port associated with the listener.

    Attributes
    ----------
    name : str, default is Undefined, optional
        Label assigned to the port.
    number : int, default is Undefined, optional
        A valid non-negative integer port number.
    $protocol : str, default is Undefined, optional
        The protocol exposed on the port.
    targetPort : int, default is Undefined, optional
        target port
    """


    name?: str

    number?: int

    $protocol?: str

    targetPort?: int


    check:
        number <= 4.294967295e+09
        number >= 0
        targetPort <= 4.294967295e+09
        targetPort >= 0


schema NetworkingIstioIoV1SidecarSpecIngressItems0TLS:
    """
    Set of TLS related options that will enable TLS termination on the sidecar for requests originating from outside the mesh.

    Attributes
    ----------
    caCertificates : str, default is Undefined, optional
        REQUIRED if mode is `MUTUAL` or `OPTIONAL_MUTUAL`.
    caCrl : str, default is Undefined, optional
        OPTIONAL: The path to the file containing the certificate revocation list (CRL) to use in verifying a presented client side certificate.
    cipherSuites : [str], default is Undefined, optional
        Optional: If specified, only support the specified cipher list.
    credentialName : str, default is Undefined, optional
        For gateways running on Kubernetes, the name of the secret that holds the TLS certs including the CA certificates.
    httpsRedirect : bool, default is Undefined, optional
        If set to true, the load balancer will send a 301 redirect for all http connections, asking the clients to use HTTPS.
    maxProtocolVersion : str, default is Undefined, optional
        Optional: Maximum TLS protocol version.

        Valid Options: TLS_AUTO, TLSV1_0, TLSV1_1, TLSV1_2, TLSV1_3
    minProtocolVersion : str, default is Undefined, optional
        Optional: Minimum TLS protocol version.

        Valid Options: TLS_AUTO, TLSV1_0, TLSV1_1, TLSV1_2, TLSV1_3
    mode : str, default is Undefined, optional
        Optional: Indicates whether connections to this port should be secured using TLS.

        Valid Options: PASSTHROUGH, SIMPLE, MUTUAL, AUTO_PASSTHROUGH, ISTIO_MUTUAL, OPTIONAL_MUTUAL
    privateKey : str, default is Undefined, optional
        REQUIRED if mode is `SIMPLE` or `MUTUAL`.
    serverCertificate : str, default is Undefined, optional
        REQUIRED if mode is `SIMPLE` or `MUTUAL`.
    subjectAltNames : [str], default is Undefined, optional
        A list of alternate names to verify the subject identity in the certificate presented by the client.
    verifyCertificateHash : [str], default is Undefined, optional
        An optional list of hex-encoded SHA-256 hashes of the authorized client certificates.
    verifyCertificateSpki : [str], default is Undefined, optional
        An optional list of base64-encoded SHA-256 hashes of the SPKIs of authorized client certificates.
    """


    caCertificates?: str

    caCrl?: str

    cipherSuites?: [str]

    credentialName?: str

    httpsRedirect?: bool

    maxProtocolVersion?: "TLS_AUTO" | "TLSV1_0" | "TLSV1_1" | "TLSV1_2" | "TLSV1_3"

    minProtocolVersion?: "TLS_AUTO" | "TLSV1_0" | "TLSV1_1" | "TLSV1_2" | "TLSV1_3"

    mode?: "PASSTHROUGH" | "SIMPLE" | "MUTUAL" | "AUTO_PASSTHROUGH" | "ISTIO_MUTUAL" | "OPTIONAL_MUTUAL"

    privateKey?: str

    serverCertificate?: str

    subjectAltNames?: [str]

    verifyCertificateHash?: [str]

    verifyCertificateSpki?: [str]


schema NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicy:
    """
    Configuration for the outbound traffic policy.

    Attributes
    ----------
    egressProxy : NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicyEgressProxy, default is Undefined, optional
        egress proxy
    mode : str, default is Undefined, optional


        Valid Options: REGISTRY_ONLY, ALLOW_ANY
    """


    egressProxy?: NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicyEgressProxy

    mode?: "REGISTRY_ONLY" | "ALLOW_ANY"


schema NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicyEgressProxy:
    """
    networking istio io v1 sidecar spec outbound traffic policy egress proxy

    Attributes
    ----------
    host : str, default is Undefined, required
        The name of a service from the service registry.
    port : NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicyEgressProxyPort, default is Undefined, optional
        port
    subset : str, default is Undefined, optional
        The name of a subset within the service.
    """


    host: str

    port?: NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicyEgressProxyPort

    subset?: str


schema NetworkingIstioIoV1SidecarSpecOutboundTrafficPolicyEgressProxyPort:
    """
    Specifies the port on the host that is being addressed.

    Attributes
    ----------
    number : int, default is Undefined, optional
        number
    """


    number?: int


    check:
        number <= 4.294967295e+09
        number >= 0


schema NetworkingIstioIoV1SidecarSpecWorkloadSelector:
    """
    Criteria used to select the specific set of pods/VMs on which this `Sidecar` configuration should be applied.

    Attributes
    ----------
    labels : {str:str}, default is Undefined, optional
        One or more labels that indicate a specific set of pods/VMs on which the configuration should be applied.
    """


    labels?: {str:str}


