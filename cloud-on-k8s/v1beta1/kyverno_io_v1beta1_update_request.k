"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema UpdateRequest:
    """
    UpdateRequestStatus is a request to process mutate and generate rules in background.

    Attributes
    ----------
    apiVersion : str, default is "kyverno.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "UpdateRequest", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : KyvernoIoV1beta1UpdateRequestSpec, default is Undefined, optional
        spec
    status : KyvernoIoV1beta1UpdateRequestStatus, default is Undefined, optional
        status
    """


    apiVersion: "kyverno.io/v1beta1" = "kyverno.io/v1beta1"

    kind: "UpdateRequest" = "UpdateRequest"

    metadata?: v1.ObjectMeta

    spec?: KyvernoIoV1beta1UpdateRequestSpec

    status?: KyvernoIoV1beta1UpdateRequestStatus


schema KyvernoIoV1beta1UpdateRequestSpec:
    """
    Spec is the information to identify the update request.

    Attributes
    ----------
    context : KyvernoIoV1beta1UpdateRequestSpecContext, default is Undefined, required
        context
    policy : str, default is Undefined, required
        Specifies the name of the policy.
    requestType : str, default is Undefined, optional
        Type represents request type for background processing
    resource : KyvernoIoV1beta1UpdateRequestSpecResource, default is Undefined, required
        resource
    """


    context: KyvernoIoV1beta1UpdateRequestSpecContext

    policy: str

    requestType?: "mutate" | "generate"

    resource: KyvernoIoV1beta1UpdateRequestSpecResource


schema KyvernoIoV1beta1UpdateRequestSpecContext:
    """
    Context ...

    Attributes
    ----------
    admissionRequestInfo : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfo, default is Undefined, optional
        admission request info
    userInfo : KyvernoIoV1beta1UpdateRequestSpecContextUserInfo, default is Undefined, optional
        user info
    """


    admissionRequestInfo?: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfo

    userInfo?: KyvernoIoV1beta1UpdateRequestSpecContextUserInfo


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfo:
    """
    AdmissionRequestInfoObject stores the admission request and operation details

    Attributes
    ----------
    admissionRequest : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequest, default is Undefined, optional
        admission request
    operation : str, default is Undefined, optional
        Operation is the type of resource operation being checked for admission control
    """


    admissionRequest?: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequest

    operation?: str


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequest:
    """
    AdmissionRequest describes the admission.Attributes for the admission request.

    Attributes
    ----------
    dryRun : bool, default is Undefined, optional
        DryRun indicates that modifications will definitely not be persisted for this request. Defaults to false.
    kind : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestKind, default is Undefined, required
        kind
    name : str, default is Undefined, optional
        Name is the name of the object as presented in the request.  On a CREATE operation, the client may omit name and rely on the server to generate the name.  If that is the case, this field will contain an empty string.
    namespace : str, default is Undefined, optional
        Namespace is the namespace associated with the request (if any).
    object : any, default is Undefined, optional
        Object is the object from the incoming request.
    oldObject : any, default is Undefined, optional
        OldObject is the existing object. Only populated for DELETE and UPDATE requests.
    operation : str, default is Undefined, required
        Operation is the operation being performed. This may be different than the operation requested. e.g. a patch can result in either a CREATE or UPDATE Operation.
    options : any, default is Undefined, optional
        Options is the operation option structure of the operation being performed. e.g. `meta.k8s.io/v1.DeleteOptions` or `meta.k8s.io/v1.CreateOptions`. This may be different than the options the caller provided. e.g. for a patch request the performed Operation might be a CREATE, in which case the Options will a `meta.k8s.io/v1.CreateOptions` even though the caller provided `meta.k8s.io/v1.PatchOptions`.
    requestKind : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestRequestKind, default is Undefined, optional
        request kind
    requestResource : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestRequestResource, default is Undefined, optional
        request resource
    requestSubResource : str, default is Undefined, optional
        RequestSubResource is the name of the subresource of the original API request, if any (for example, "status" or "scale") If this is specified and differs from the value in "subResource", an equivalent match and conversion was performed. See documentation for the "matchPolicy" field in the webhook configuration type.
    resource : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestResource, default is Undefined, required
        resource
    subResource : str, default is Undefined, optional
        SubResource is the subresource being requested, if any (for example, "status" or "scale")
    uid : str, default is Undefined, required
        UID is an identifier for the individual request/response. It allows us to distinguish instances of requests which are otherwise identical (parallel requests, requests when earlier requests did not modify etc) The UID is meant to track the round trip (request/response) between the KAS and the WebHook, not the user request. It is suitable for correlating log entries between the webhook and apiserver, for either auditing or debugging.
    userInfo : KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestUserInfo, default is Undefined, required
        user info
    """


    dryRun?: bool

    kind: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestKind

    name?: str

    namespace?: str

    object?: any

    oldObject?: any

    operation: str

    options?: any

    requestKind?: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestRequestKind

    requestResource?: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestRequestResource

    requestSubResource?: str

    resource: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestResource

    subResource?: str

    uid: str

    userInfo: KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestUserInfo


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestKind:
    """
    Kind is the fully-qualified type of object being submitted (for example, v1.Pod or autoscaling.v1.Scale)

    Attributes
    ----------
    group : str, default is Undefined, required
        group
    kind : str, default is Undefined, required
        kind
    version : str, default is Undefined, required
        version
    """


    group: str

    kind: str

    version: str


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestRequestKind:
    """
    RequestKind is the fully-qualified type of the original API request (for example, v1.Pod or autoscaling.v1.Scale). If this is specified and differs from the value in "kind", an equivalent match and conversion was performed. 
     For example, if deployments can be modified via apps/v1 and apps/v1beta1, and a webhook registered a rule of `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]` and `matchPolicy: Equivalent`, an API request to apps/v1beta1 deployments would be converted and sent to the webhook with `kind: {group:"apps", version:"v1", kind:"Deployment"}` (matching the rule the webhook registered for), and `requestKind: {group:"apps", version:"v1beta1", kind:"Deployment"}` (indicating the kind of the original API request). 
     See documentation for the "matchPolicy" field in the webhook configuration type for more details.

    Attributes
    ----------
    group : str, default is Undefined, required
        group
    kind : str, default is Undefined, required
        kind
    version : str, default is Undefined, required
        version
    """


    group: str

    kind: str

    version: str


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestRequestResource:
    """
    RequestResource is the fully-qualified resource of the original API request (for example, v1.pods). If this is specified and differs from the value in "resource", an equivalent match and conversion was performed. 
     For example, if deployments can be modified via apps/v1 and apps/v1beta1, and a webhook registered a rule of `apiGroups:["apps"], apiVersions:["v1"], resources: ["deployments"]` and `matchPolicy: Equivalent`, an API request to apps/v1beta1 deployments would be converted and sent to the webhook with `resource: {group:"apps", version:"v1", resource:"deployments"}` (matching the resource the webhook registered for), and `requestResource: {group:"apps", version:"v1beta1", resource:"deployments"}` (indicating the resource of the original API request). 
     See documentation for the "matchPolicy" field in the webhook configuration type.

    Attributes
    ----------
    group : str, default is Undefined, required
        group
    resource : str, default is Undefined, required
        resource
    version : str, default is Undefined, required
        version
    """


    group: str

    resource: str

    version: str


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestResource:
    """
    Resource is the fully-qualified resource being requested (for example, v1.pods)

    Attributes
    ----------
    group : str, default is Undefined, required
        group
    resource : str, default is Undefined, required
        resource
    version : str, default is Undefined, required
        version
    """


    group: str

    resource: str

    version: str


schema KyvernoIoV1beta1UpdateRequestSpecContextAdmissionRequestInfoAdmissionRequestUserInfo:
    """
    UserInfo is information about the requesting user

    Attributes
    ----------
    extra : {str:[str]}, default is Undefined, optional
        Any additional information provided by the authenticator.
    groups : [str], default is Undefined, optional
        The names of groups this user is a part of.
    uid : str, default is Undefined, optional
        A unique value that identifies this user across time. If this user is deleted and another user by the same name is added, they will have different UIDs.
    username : str, default is Undefined, optional
        The name that uniquely identifies this user among all active users.
    """


    extra?: {str:[str]}

    groups?: [str]

    uid?: str

    username?: str


schema KyvernoIoV1beta1UpdateRequestSpecContextUserInfo:
    """
    RequestInfo contains permission info carried in an admission request.

    Attributes
    ----------
    clusterRoles : [str], default is Undefined, optional
        ClusterRoles is a list of possible clusterRoles send the request.
    roles : [str], default is Undefined, optional
        Roles is a list of possible role send the request.
    userInfo : KyvernoIoV1beta1UpdateRequestSpecContextUserInfoUserInfo, default is Undefined, optional
        user info
    """


    clusterRoles?: [str]

    roles?: [str]

    userInfo?: KyvernoIoV1beta1UpdateRequestSpecContextUserInfoUserInfo


schema KyvernoIoV1beta1UpdateRequestSpecContextUserInfoUserInfo:
    """
    UserInfo is the userInfo carried in the admission request.

    Attributes
    ----------
    extra : {str:[str]}, default is Undefined, optional
        Any additional information provided by the authenticator.
    groups : [str], default is Undefined, optional
        The names of groups this user is a part of.
    uid : str, default is Undefined, optional
        A unique value that identifies this user across time. If this user is deleted and another user by the same name is added, they will have different UIDs.
    username : str, default is Undefined, optional
        The name that uniquely identifies this user among all active users.
    """


    extra?: {str:[str]}

    groups?: [str]

    uid?: str

    username?: str


schema KyvernoIoV1beta1UpdateRequestSpecResource:
    """
    ResourceSpec is the information to identify the update request.

    Attributes
    ----------
    apiVersion : str, default is Undefined, optional
        APIVersion specifies resource apiVersion.
    kind : str, default is Undefined, optional
        Kind specifies resource kind.
    name : str, default is Undefined, optional
        Name specifies the resource name.
    namespace : str, default is Undefined, optional
        Namespace specifies resource namespace.
    """


    apiVersion?: str

    kind?: str

    name?: str

    namespace?: str


schema KyvernoIoV1beta1UpdateRequestStatus:
    """
    Status contains statistics related to update request.

    Attributes
    ----------
    generatedResources : [KyvernoIoV1beta1UpdateRequestStatusGeneratedResourcesItems0], default is Undefined, optional
        This will track the resources that are updated by the generate Policy. Will be used during clean up resources.
    handler : str, default is Undefined, optional
        Handler represents the instance ID that handles the UR
    message : str, default is Undefined, optional
        Specifies request status message.
    state : str, default is Undefined, required
        State represents state of the update request.
    """


    generatedResources?: [KyvernoIoV1beta1UpdateRequestStatusGeneratedResourcesItems0]

    handler?: str

    message?: str

    state: str


schema KyvernoIoV1beta1UpdateRequestStatusGeneratedResourcesItems0:
    """
    kyverno io v1beta1 update request status generated resources items0

    Attributes
    ----------
    apiVersion : str, default is Undefined, optional
        APIVersion specifies resource apiVersion.
    kind : str, default is Undefined, optional
        Kind specifies resource kind.
    name : str, default is Undefined, optional
        Name specifies the resource name.
    namespace : str, default is Undefined, optional
        Namespace specifies resource namespace.
    """


    apiVersion?: str

    kind?: str

    name?: str

    namespace?: str


