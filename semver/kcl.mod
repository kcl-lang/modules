[package]
name = "semver"
version = "0.1.0"
description = "Module semver implements comparison of semantic version strings. In this module, semantic version strings must begin with a leading `v`, as in `v1.0.0`."
