"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema OrgPolicyPolicy:
    r"""
    OrgPolicyPolicy is the Schema for the OrgPolicyPolicy API

    Attributes
    ----------
    apiVersion : str, default is "orgpolicy.cnrm.cloud.google.com/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "OrgPolicyPolicy", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpec, default is Undefined, required
        spec
    status : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatus, default is Undefined, optional
        status
    """


    apiVersion: "orgpolicy.cnrm.cloud.google.com/v1alpha1" = "orgpolicy.cnrm.cloud.google.com/v1alpha1"

    kind: "OrgPolicyPolicy" = "OrgPolicyPolicy"

    metadata?: v1.ObjectMeta

    spec: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpec

    status?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatus


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpec:
    r"""
    OrgPolicyPolicySpec defines the desired state of OrgPolicyPolicy

    Attributes
    ----------
    dryRunSpec : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpec, default is Undefined, optional
        dry run spec
    folderRef : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecFolderRef, default is Undefined, optional
        folder ref
    organizationRef : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecOrganizationRef, default is Undefined, optional
        organization ref
    projectRef : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecProjectRef, default is Undefined, optional
        project ref
    resourceID : str, default is Undefined, optional
        The OrgPolicyPolicy name. If not given, the metadata.name will be used.
    spec : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpec, default is Undefined, optional
        spec
    """


    dryRunSpec?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpec

    folderRef?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecFolderRef

    organizationRef?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecOrganizationRef

    projectRef?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecProjectRef

    resourceID?: str

    spec?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpec


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpec:
    r"""
    Dry-run policy. Audit-only policy, can be used to monitor how the policy would have impacted the existing and future resources if it's enforced.

    Attributes
    ----------
    inheritFromParent : bool, default is Undefined, optional
        Determines the inheritance behavior for this policy.

         If `inherit_from_parent` is true, policy rules set higher up in the
         hierarchy (up to the closest root) are inherited and present in the
         effective policy. If it is false, then no rules are inherited, and this
         policy becomes the new root for evaluation.
         This field can be set only for policies which configure list constraints.
    reset : bool, default is Undefined, optional
        Ignores policies set above this resource and restores the `constraint_default` enforcement behavior of the specific constraint at this resource. This field can be set in policies for either list or boolean constraints. If set, `rules` must be empty and `inherit_from_parent` must be set to false.
    rules : [OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0], default is Undefined, optional
        In policies for boolean constraints, the following requirements apply:

           - There must be one and only one policy rule where condition is unset.
           - Boolean policy rules with conditions must set `enforced` to the
             opposite of the policy rule without a condition.
           - During policy evaluation, policy rules with conditions that are
             true for a target resource take precedence.
    """


    inheritFromParent?: bool

    reset?: bool

    rules?: [OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0]


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0:
    r"""
    orgpolicy cnrm cloud google com v1alpha1 org policy policy spec dry run spec rules items0

    Attributes
    ----------
    allowAll : bool, default is Undefined, optional
        Setting this to true means that all values are allowed. This field can be set only in policies for list constraints.
    condition : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0Condition, default is Undefined, optional
        condition
    denyAll : bool, default is Undefined, optional
        Setting this to true means that all values are denied. This field can be set only in policies for list constraints.
    enforce : bool, default is Undefined, optional
        If `true`, then the policy is enforced. If `false`, then any configuration is acceptable. This field can be set only in policies for boolean constraints.
    parameters : any, default is Undefined, optional
        Optional. Required for managed constraints if parameters are defined. Passes parameter values when policy enforcement is enabled. Ensure that parameter value types match those defined in the constraint definition. For example: { "allowedLocations" : ["us-east1", "us-west1"], "allowAll" : true }
    values : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0Values, default is Undefined, optional
        values
    """


    allowAll?: bool

    condition?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0Condition

    denyAll?: bool

    enforce?: bool

    parameters?: any

    values?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0Values


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0Condition:
    r"""
    A condition which determines whether this rule is used in the evaluation of the policy. When set, the `expression` field in the `Expr' must include from 1 to 10 subexpressions, joined by the "||" or "&&" operators. Each subexpression must be of the form "resource.matchTag('<ORG_ID>/tag_key_short_name, 'tag_value_short_name')". or "resource.matchTagId('tagKeys/key_id', 'tagValues/value_id')". where key_name and value_name are the resource names for Label Keys and Values. These names are available from the Tag Manager Service. An example expression is: "resource.matchTag('123456789/environment, 'prod')". or "resource.matchTagId('tagKeys/123', 'tagValues/456')".

    Attributes
    ----------
    description : str, default is Undefined, optional
        Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.
    expression : str, default is Undefined, optional
        Textual representation of an expression in Common Expression Language syntax.
    location : str, default is Undefined, optional
        Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.
    title : str, default is Undefined, optional
        Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.
    """


    description?: str

    expression?: str

    location?: str

    title?: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecDryRunSpecRulesItems0Values:
    r"""
    List of values to be used for this policy rule. This field can be set only in policies for list constraints.

    Attributes
    ----------
    allowedValues : [str], default is Undefined, optional
        List of values allowed at this resource.
    deniedValues : [str], default is Undefined, optional
        List of values denied at this resource.
    """


    allowedValues?: [str]

    deniedValues?: [str]


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecFolderRef:
    r"""
    Immutable. The Folder that this resource belongs to. One and only one of 'projectRef', 'folderRef', or 'organizationRef' must be set.

    Attributes
    ----------
    external : str, default is Undefined, optional
        The 'name' field of a folder, when not managed by Config Connector. This field must be set when 'name' field is not set.
    name : str, default is Undefined, optional
        The 'name' field of a 'Folder' resource. This field must be set when 'external' field is not set.
    namespace : str, default is Undefined, optional
        The 'namespace' field of a 'Folder' resource. If unset, the namespace is defaulted to the namespace of the referencer resource.
    """


    external?: str

    name?: str

    namespace?: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecOrganizationRef:
    r"""
    Immutable. The Organization that this resource belongs to. One and only one of 'projectRef', 'folderRef', or 'organizationRef' must be set.

    Attributes
    ----------
    external : str, default is Undefined, required
        The 'name' field of an organization, when not managed by Config Connector.
    """


    external: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecProjectRef:
    r"""
    Immutable. The Project that this resource belongs to. One and only one of 'projectRef', 'folderRef', or 'organizationRef' must be set.

    Attributes
    ----------
    external : str, default is Undefined, optional
        The `projectID` field of a project, when not managed by Config Connector.
    kind : str, default is Undefined, optional
        The kind of the Project resource; optional but must be `Project` if provided.
    name : str, default is Undefined, optional
        The `name` field of a `Project` resource.
    namespace : str, default is Undefined, optional
        The `namespace` field of a `Project` resource.
    """


    external?: str

    kind?: str

    name?: str

    namespace?: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpec:
    r"""
    Basic information about the Organization Policy.

    Attributes
    ----------
    inheritFromParent : bool, default is Undefined, optional
        Determines the inheritance behavior for this policy.

         If `inherit_from_parent` is true, policy rules set higher up in the
         hierarchy (up to the closest root) are inherited and present in the
         effective policy. If it is false, then no rules are inherited, and this
         policy becomes the new root for evaluation.
         This field can be set only for policies which configure list constraints.
    reset : bool, default is Undefined, optional
        Ignores policies set above this resource and restores the `constraint_default` enforcement behavior of the specific constraint at this resource. This field can be set in policies for either list or boolean constraints. If set, `rules` must be empty and `inherit_from_parent` must be set to false.
    rules : [OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0], default is Undefined, optional
        In policies for boolean constraints, the following requirements apply:

           - There must be one and only one policy rule where condition is unset.
           - Boolean policy rules with conditions must set `enforced` to the
             opposite of the policy rule without a condition.
           - During policy evaluation, policy rules with conditions that are
             true for a target resource take precedence.
    """


    inheritFromParent?: bool

    reset?: bool

    rules?: [OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0]


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0:
    r"""
    orgpolicy cnrm cloud google com v1alpha1 org policy policy spec spec rules items0

    Attributes
    ----------
    allowAll : bool, default is Undefined, optional
        Setting this to true means that all values are allowed. This field can be set only in policies for list constraints.
    condition : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0Condition, default is Undefined, optional
        condition
    denyAll : bool, default is Undefined, optional
        Setting this to true means that all values are denied. This field can be set only in policies for list constraints.
    enforce : bool, default is Undefined, optional
        If `true`, then the policy is enforced. If `false`, then any configuration is acceptable. This field can be set only in policies for boolean constraints.
    parameters : any, default is Undefined, optional
        Optional. Required for managed constraints if parameters are defined. Passes parameter values when policy enforcement is enabled. Ensure that parameter value types match those defined in the constraint definition. For example: { "allowedLocations" : ["us-east1", "us-west1"], "allowAll" : true }
    values : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0Values, default is Undefined, optional
        values
    """


    allowAll?: bool

    condition?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0Condition

    denyAll?: bool

    enforce?: bool

    parameters?: any

    values?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0Values


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0Condition:
    r"""
    A condition which determines whether this rule is used in the evaluation of the policy. When set, the `expression` field in the `Expr' must include from 1 to 10 subexpressions, joined by the "||" or "&&" operators. Each subexpression must be of the form "resource.matchTag('<ORG_ID>/tag_key_short_name, 'tag_value_short_name')". or "resource.matchTagId('tagKeys/key_id', 'tagValues/value_id')". where key_name and value_name are the resource names for Label Keys and Values. These names are available from the Tag Manager Service. An example expression is: "resource.matchTag('123456789/environment, 'prod')". or "resource.matchTagId('tagKeys/123', 'tagValues/456')".

    Attributes
    ----------
    description : str, default is Undefined, optional
        Optional. Description of the expression. This is a longer text which describes the expression, e.g. when hovered over it in a UI.
    expression : str, default is Undefined, optional
        Textual representation of an expression in Common Expression Language syntax.
    location : str, default is Undefined, optional
        Optional. String indicating the location of the expression for error reporting, e.g. a file name and a position in the file.
    title : str, default is Undefined, optional
        Optional. Title for the expression, i.e. a short string describing its purpose. This can be used e.g. in UIs which allow to enter the expression.
    """


    description?: str

    expression?: str

    location?: str

    title?: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicySpecSpecRulesItems0Values:
    r"""
    List of values to be used for this policy rule. This field can be set only in policies for list constraints.

    Attributes
    ----------
    allowedValues : [str], default is Undefined, optional
        List of values allowed at this resource.
    deniedValues : [str], default is Undefined, optional
        List of values denied at this resource.
    """


    allowedValues?: [str]

    deniedValues?: [str]


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatus:
    r"""
    OrgPolicyPolicyStatus defines the config connector machine state of OrgPolicyPolicy

    Attributes
    ----------
    conditions : [OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusConditionsItems0], default is Undefined, optional
        Conditions represent the latest available observations of the object's current state.
    externalRef : str, default is Undefined, optional
        A unique specifier for the OrgPolicyPolicy resource in GCP.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the generation of the resource that was most recently observed by the Config Connector controller. If this is equal to metadata.generation, then that means that the current reported status reflects the most recent desired state of the resource.
    observedState : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedState, default is Undefined, optional
        observed state
    """


    conditions?: [OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusConditionsItems0]

    externalRef?: str

    observedGeneration?: int

    observedState?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedState


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusConditionsItems0:
    r"""
    orgpolicy cnrm cloud google com v1alpha1 org policy policy status conditions items0

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        Last time the condition transitioned from one status to another.
    message : str, default is Undefined, optional
        Human-readable message indicating details about last transition.
    reason : str, default is Undefined, optional
        Unique, one-word, CamelCase reason for the condition's last transition.
    status : str, default is Undefined, optional
        Status is the status of the condition. Can be True, False, Unknown.
    $type : str, default is Undefined, optional
        Type is the type of the condition.
    """


    lastTransitionTime?: str

    message?: str

    reason?: str

    status?: str

    $type?: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedState:
    r"""
    ObservedState is the state of the resource as most recently observed in GCP.

    Attributes
    ----------
    dryRunSpec : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedStateDryRunSpec, default is Undefined, optional
        dry run spec
    spec : OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedStateSpec, default is Undefined, optional
        spec
    """


    dryRunSpec?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedStateDryRunSpec

    spec?: OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedStateSpec


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedStateDryRunSpec:
    r"""
    Dry-run policy. Audit-only policy, can be used to monitor how the policy would have impacted the existing and future resources if it's enforced.

    Attributes
    ----------
    updateTime : str, default is Undefined, optional
        Output only. The time stamp this was previously updated. This represents the last time a call to `CreatePolicy` or `UpdatePolicy` was made for that policy.
    """


    updateTime?: str


schema OrgpolicyCnrmCloudGoogleComV1alpha1OrgPolicyPolicyStatusObservedStateSpec:
    r"""
    Basic information about the Organization Policy.

    Attributes
    ----------
    updateTime : str, default is Undefined, optional
        Output only. The time stamp this was previously updated. This represents the last time a call to `CreatePolicy` or `UpdatePolicy` was made for that policy.
    """


    updateTime?: str


