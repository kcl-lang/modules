"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1


schema ElasticsearchAutoscaler:
    """
    ElasticsearchAutoscaler represents an ElasticsearchAutoscaler resource in a Kubernetes cluster.

    Attributes
    ----------
    apiVersion : str, default is "autoscaling.k8s.elastic.co/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ElasticsearchAutoscaler", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpec, default is Undefined, optional
        spec
    status : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatus, default is Undefined, optional
        status
    """


    apiVersion: "autoscaling.k8s.elastic.co/v1alpha1" = "autoscaling.k8s.elastic.co/v1alpha1"

    kind: "ElasticsearchAutoscaler" = "ElasticsearchAutoscaler"

    metadata?: v1.ObjectMeta

    spec?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpec

    status?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatus


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpec:
    """
    ElasticsearchAutoscalerSpec holds the specification of an Elasticsearch autoscaler resource.

    Attributes
    ----------
    elasticsearchRef : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecElasticsearchRef, default is Undefined, optional
        elasticsearch ref
    policies : [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0], default is Undefined, required
        policies
    pollingPeriod : str, default is Undefined, optional
        PollingPeriod is the period at which to synchronize with the Elasticsearch autoscaling API.
    """


    elasticsearchRef?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecElasticsearchRef

    policies: [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0]

    pollingPeriod?: str


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecElasticsearchRef:
    """
    ElasticsearchRef is a reference to an Elasticsearch cluster that exists in the same namespace.

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name is the name of the Elasticsearch resource to scale automatically.
    """


    name?: str


    check:
        len(name) >= 1


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0:
    """
    AutoscalingPolicySpec holds a named autoscaling policy and the associated resources limits (cpu, memory, storage).

    Attributes
    ----------
    deciders : {str:{str:str}}, default is Undefined, optional
        Deciders allow the user to override default settings for autoscaling deciders.
    name : str, default is Undefined, optional
        Name identifies the autoscaling policy in the autoscaling specification.
    resources : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0Resources, default is Undefined, required
        resources
    roles : [str], default is Undefined, optional
        An autoscaling policy must target a unique set of roles.
    """


    deciders?: {str:{str:str}}

    name?: str

    resources: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0Resources

    roles?: [str]


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0Resources:
    """
    AutoscalingResources model the limits, submitted by the user, for the supported resources in an autoscaling policy. Only the node count range is mandatory. For other resources, a limit range is required only if the Elasticsearch autoscaling capacity API returns a requirement for a given resource. For example, the memory limit range is only required if the autoscaling API response contains a memory requirement. If there is no limit range for a resource, and if that resource is not mandatory, then the resources in the NodeSets managed by the autoscaling policy are left untouched.

    Attributes
    ----------
    cpu : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesCPU, default is Undefined, optional
        cpu
    memory : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesMemory, default is Undefined, optional
        memory
    nodeCount : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesNodeCount, default is Undefined, required
        node count
    storage : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesStorage, default is Undefined, optional
        storage
    """


    cpu?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesCPU

    memory?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesMemory

    nodeCount: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesNodeCount

    storage?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesStorage


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesCPU:
    """
    QuantityRange models a resource limit range for resources which can be expressed with resource.Quantity.

    Attributes
    ----------
    max : int | str, default is Undefined, required
        Max represents the upper limit for the resources managed by the autoscaler.
    min : int | str, default is Undefined, required
        Min represents the lower limit for the resources managed by the autoscaler.
    requestsToLimitsRatio : int | str, default is Undefined, optional
        RequestsToLimitsRatio allows to customize Kubernetes resource Limit based on the Request.
    """


    max: int | str

    min: int | str

    requestsToLimitsRatio?: int | str


    check:
        regex.match(str(max), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$")
        regex.match(str(min), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$")
        regex.match(str(requestsToLimitsRatio), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$") if requestsToLimitsRatio


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesMemory:
    """
    QuantityRange models a resource limit range for resources which can be expressed with resource.Quantity.

    Attributes
    ----------
    max : int | str, default is Undefined, required
        Max represents the upper limit for the resources managed by the autoscaler.
    min : int | str, default is Undefined, required
        Min represents the lower limit for the resources managed by the autoscaler.
    requestsToLimitsRatio : int | str, default is Undefined, optional
        RequestsToLimitsRatio allows to customize Kubernetes resource Limit based on the Request.
    """


    max: int | str

    min: int | str

    requestsToLimitsRatio?: int | str


    check:
        regex.match(str(max), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$")
        regex.match(str(min), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$")
        regex.match(str(requestsToLimitsRatio), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$") if requestsToLimitsRatio


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesNodeCount:
    """
    NodeCountRange is used to model the minimum and the maximum number of nodes over all the NodeSets managed by the same autoscaling policy.

    Attributes
    ----------
    max : int, default is Undefined, required
        Max represents the maximum number of nodes in a tier.
    min : int, default is Undefined, required
        Min represents the minimum number of nodes in a tier.
    """


    max: int

    min: int


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerSpecPoliciesItems0ResourcesStorage:
    """
    QuantityRange models a resource limit range for resources which can be expressed with resource.Quantity.

    Attributes
    ----------
    max : int | str, default is Undefined, required
        Max represents the upper limit for the resources managed by the autoscaler.
    min : int | str, default is Undefined, required
        Min represents the lower limit for the resources managed by the autoscaler.
    requestsToLimitsRatio : int | str, default is Undefined, optional
        RequestsToLimitsRatio allows to customize Kubernetes resource Limit based on the Request.
    """


    max: int | str

    min: int | str

    requestsToLimitsRatio?: int | str


    check:
        regex.match(str(max), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$")
        regex.match(str(min), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$")
        regex.match(str(requestsToLimitsRatio), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$") if requestsToLimitsRatio


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatus:
    """
    autoscaling k8s elastic co v1alpha1 elasticsearch autoscaler status

    Attributes
    ----------
    conditions : [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusConditionsItems0], default is Undefined, optional
        Conditions holds the current service state of the autoscaling controller.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the last observed generation by the controller.
    policies : [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0], default is Undefined, optional
        AutoscalingPolicyStatuses is used to expose state messages to user or external system.
    """


    conditions?: [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusConditionsItems0]

    observedGeneration?: int

    policies?: [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0]


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusConditionsItems0:
    """
    Condition represents Elasticsearch resource's condition. **This API is in technical preview and may be changed or removed in a future release.**

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        last transition time
    message : str, default is Undefined, optional
        message
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        ConditionType defines the condition of an Elasticsearch resource.
    """


    lastTransitionTime?: str

    message?: str

    status: str

    $type: str


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0:
    """
    autoscaling k8s elastic co v1alpha1 elasticsearch autoscaler status policies items0

    Attributes
    ----------
    lastModificationTime : str, default is Undefined, optional
        LastModificationTime is the last time the resources have been updated, used by the cooldown algorithm.
    name : str, default is Undefined, required
        Name is the name of the autoscaling policy
    nodeSets : [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0NodeSetsItems0], default is Undefined, optional
        NodeSetNodeCount holds the number of nodes for each nodeSet.
    resources : AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0Resources, default is Undefined, optional
        resources
    state : [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0StateItems0], default is Undefined, optional
        PolicyStates may contain various messages regarding the current state of this autoscaling policy.
    """


    lastModificationTime?: str

    name: str

    nodeSets?: [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0NodeSetsItems0]

    resources?: AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0Resources

    state?: [AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0StateItems0]


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0NodeSetsItems0:
    """
    NodeSetNodeCount models the number of nodes expected in a given NodeSet.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the Nodeset.
    nodeCount : int, default is Undefined, required
        NodeCount is the number of nodes, as computed by the autoscaler, expected in this NodeSet.
    """


    name: str

    nodeCount: int


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0Resources:
    """
    ResourcesSpecification holds the resource values common to all the nodeSets managed by a same autoscaling policy. Only the resources managed by the autoscaling controller are saved in the Status.

    Attributes
    ----------
    limits : {str:int | str}, default is Undefined, optional
        ResourceList is a set of (resource name, quantity) pairs.
    requests : {str:int | str}, default is Undefined, optional
        ResourceList is a set of (resource name, quantity) pairs.
    """


    limits?: {str:int | str}

    requests?: {str:int | str}


    check:
        all _, n in limits { regex.match(str(n), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$") } if limits
        all _, n in requests { regex.match(str(n), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$") } if requests


schema AutoscalingK8sElasticCoV1alpha1ElasticsearchAutoscalerStatusPoliciesItems0StateItems0:
    """
    autoscaling k8s elastic co v1alpha1 elasticsearch autoscaler status policies items0 state items0

    Attributes
    ----------
    messages : [str], default is Undefined, required
        messages
    $type : str, default is Undefined, required
        type
    """


    messages: [str]

    $type: str


