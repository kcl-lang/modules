"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Document:
    r"""
    Document is the Schema for the Documents API. Provides an SSM Document resource

    Attributes
    ----------
    apiVersion : str, default is "ssm.aws.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Document", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SsmAwsUpboundIoV1beta1DocumentSpec, default is Undefined, required
        spec
    status : SsmAwsUpboundIoV1beta1DocumentStatus, default is Undefined, optional
        status
    """


    apiVersion: "ssm.aws.upbound.io/v1beta1" = "ssm.aws.upbound.io/v1beta1"

    kind: "Document" = "Document"

    metadata?: v1.ObjectMeta

    spec: SsmAwsUpboundIoV1beta1DocumentSpec

    status?: SsmAwsUpboundIoV1beta1DocumentStatus


schema SsmAwsUpboundIoV1beta1DocumentSpec:
    r"""
    DocumentSpec defines the desired state of Document

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : SsmAwsUpboundIoV1beta1DocumentSpecForProvider, default is Undefined, required
        for provider
    initProvider : SsmAwsUpboundIoV1beta1DocumentSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : SsmAwsUpboundIoV1beta1DocumentSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : SsmAwsUpboundIoV1beta1DocumentSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: SsmAwsUpboundIoV1beta1DocumentSpecForProvider

    initProvider?: SsmAwsUpboundIoV1beta1DocumentSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: SsmAwsUpboundIoV1beta1DocumentSpecProviderConfigRef

    publishConnectionDetailsTo?: SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: SsmAwsUpboundIoV1beta1DocumentSpecWriteConnectionSecretToRef


schema SsmAwsUpboundIoV1beta1DocumentSpecForProvider:
    r"""
    ssm aws upbound io v1beta1 document spec for provider

    Attributes
    ----------
    attachmentsSource : [SsmAwsUpboundIoV1beta1DocumentSpecForProviderAttachmentsSourceItems0], default is Undefined, optional
        One or more configuration blocks describing attachments sources to a version of a document. See attachments_source block below for details.
    content : str, default is Undefined, optional
        The content for the SSM document in JSON or YAML format. The content of the document must not exceed 64KB. This quota also includes the content specified for input parameters at runtime. We recommend storing the contents for your new document in an external JSON or YAML file and referencing the file in a command.
    documentFormat : str, default is Undefined, optional
        The format of the document. Valid values: JSON, TEXT, YAML.
    documentType : str, default is Undefined, optional
        The type of the document. For a list of valid values, see the API Reference.
    permissions : {str:str}, default is Undefined, optional
        Additional permissions to attach to the document. See Permissions below for details.
    region : str, default is Undefined, required
        Region is the region you'd like your resource to be created in.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    targetType : str, default is Undefined, optional
        The target type which defines the kinds of resources the document can run on. For example, /AWS::EC2::Instance. For a list of valid resource types, see AWS resource and property types reference.
    versionName : str, default is Undefined, optional
        The version of the artifact associated with the document. For example, 12.6. This value is unique across all versions of a document, and can't be changed.
    """


    attachmentsSource?: [SsmAwsUpboundIoV1beta1DocumentSpecForProviderAttachmentsSourceItems0]

    content?: str

    documentFormat?: str

    documentType?: str

    permissions?: {str:str}

    region: str

    tags?: {str:str}

    targetType?: str

    versionName?: str


schema SsmAwsUpboundIoV1beta1DocumentSpecForProviderAttachmentsSourceItems0:
    r"""
    ssm aws upbound io v1beta1 document spec for provider attachments source items0

    Attributes
    ----------
    key : str, default is Undefined, optional
        The key of a key-value pair that identifies the location of an attachment to the document. Valid values: SourceUrl, S3FileUrl, AttachmentReference.
    name : str, default is Undefined, optional
        The name of the document attachment file.
    values : [str], default is Undefined, optional
        The value of a key-value pair that identifies the location of an attachment to the document. The argument format is a list of a single string that depends on the type of key you specify - see the API Reference for details.
    """


    key?: str

    name?: str

    values?: [str]


schema SsmAwsUpboundIoV1beta1DocumentSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    attachmentsSource : [SsmAwsUpboundIoV1beta1DocumentSpecInitProviderAttachmentsSourceItems0], default is Undefined, optional
        One or more configuration blocks describing attachments sources to a version of a document. See attachments_source block below for details.
    content : str, default is Undefined, optional
        The content for the SSM document in JSON or YAML format. The content of the document must not exceed 64KB. This quota also includes the content specified for input parameters at runtime. We recommend storing the contents for your new document in an external JSON or YAML file and referencing the file in a command.
    documentFormat : str, default is Undefined, optional
        The format of the document. Valid values: JSON, TEXT, YAML.
    documentType : str, default is Undefined, optional
        The type of the document. For a list of valid values, see the API Reference.
    permissions : {str:str}, default is Undefined, optional
        Additional permissions to attach to the document. See Permissions below for details.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    targetType : str, default is Undefined, optional
        The target type which defines the kinds of resources the document can run on. For example, /AWS::EC2::Instance. For a list of valid resource types, see AWS resource and property types reference.
    versionName : str, default is Undefined, optional
        The version of the artifact associated with the document. For example, 12.6. This value is unique across all versions of a document, and can't be changed.
    """


    attachmentsSource?: [SsmAwsUpboundIoV1beta1DocumentSpecInitProviderAttachmentsSourceItems0]

    content?: str

    documentFormat?: str

    documentType?: str

    permissions?: {str:str}

    tags?: {str:str}

    targetType?: str

    versionName?: str


schema SsmAwsUpboundIoV1beta1DocumentSpecInitProviderAttachmentsSourceItems0:
    r"""
    ssm aws upbound io v1beta1 document spec init provider attachments source items0

    Attributes
    ----------
    key : str, default is Undefined, optional
        The key of a key-value pair that identifies the location of an attachment to the document. Valid values: SourceUrl, S3FileUrl, AttachmentReference.
    name : str, default is Undefined, optional
        The name of the document attachment file.
    values : [str], default is Undefined, optional
        The value of a key-value pair that identifies the location of an attachment to the document. The argument format is a list of a single string that depends on the type of key you specify - see the API Reference for details.
    """


    key?: str

    name?: str

    values?: [str]


schema SsmAwsUpboundIoV1beta1DocumentSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SsmAwsUpboundIoV1beta1DocumentSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SsmAwsUpboundIoV1beta1DocumentSpecProviderConfigRefPolicy


schema SsmAwsUpboundIoV1beta1DocumentSpecProviderConfigRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsTo:
    r"""
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToConfigRef

    metadata?: SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToMetadata

    name: str


schema SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToConfigRef:
    r"""
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToConfigRefPolicy


schema SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToConfigRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SsmAwsUpboundIoV1beta1DocumentSpecPublishConnectionDetailsToMetadata:
    r"""
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema SsmAwsUpboundIoV1beta1DocumentSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema SsmAwsUpboundIoV1beta1DocumentStatus:
    r"""
    DocumentStatus defines the observed state of Document.

    Attributes
    ----------
    atProvider : SsmAwsUpboundIoV1beta1DocumentStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [SsmAwsUpboundIoV1beta1DocumentStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: SsmAwsUpboundIoV1beta1DocumentStatusAtProvider

    conditions?: [SsmAwsUpboundIoV1beta1DocumentStatusConditionsItems0]

    observedGeneration?: int


schema SsmAwsUpboundIoV1beta1DocumentStatusAtProvider:
    r"""
    ssm aws upbound io v1beta1 document status at provider

    Attributes
    ----------
    arn : str, default is Undefined, optional
        The Amazon Resource Name (ARN) of the document.
    attachmentsSource : [SsmAwsUpboundIoV1beta1DocumentStatusAtProviderAttachmentsSourceItems0], default is Undefined, optional
        One or more configuration blocks describing attachments sources to a version of a document. See attachments_source block below for details.
    content : str, default is Undefined, optional
        The content for the SSM document in JSON or YAML format. The content of the document must not exceed 64KB. This quota also includes the content specified for input parameters at runtime. We recommend storing the contents for your new document in an external JSON or YAML file and referencing the file in a command.
    createdDate : str, default is Undefined, optional
        The date the document was created.
    defaultVersion : str, default is Undefined, optional
        The default version of the document.
    description : str, default is Undefined, optional
        The description of the document.
    documentFormat : str, default is Undefined, optional
        The format of the document. Valid values: JSON, TEXT, YAML.
    documentType : str, default is Undefined, optional
        The type of the document. For a list of valid values, see the API Reference.
    documentVersion : str, default is Undefined, optional
        The document version.
    hash : str, default is Undefined, optional
        The Sha256 or Sha1 hash created by the system when the document was created.
    hashType : str, default is Undefined, optional
        The hash type of the document. Valid values: Sha256, Sha1.
    id : str, default is Undefined, optional
        The name of the document.
    latestVersion : str, default is Undefined, optional
        The latest version of the document.
    owner : str, default is Undefined, optional
        The Amazon Web Services user that created the document.
    parameter : [SsmAwsUpboundIoV1beta1DocumentStatusAtProviderParameterItems0], default is Undefined, optional
        One or more configuration blocks describing the parameters for the document. See parameter block below for details.
    permissions : {str:str}, default is Undefined, optional
        Additional permissions to attach to the document. See Permissions below for details.
    platformTypes : [str], default is Undefined, optional
        The list of operating system (OS) platforms compatible with this SSM document. Valid values: Windows, Linux, MacOS.
    schemaVersion : str, default is Undefined, optional
        The schema version of the document.
    status : str, default is Undefined, optional
        The status of the SSM document. Valid values: Creating, Active, Updating, Deleting, Failed.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    tagsAll : {str:str}, default is Undefined, optional
        A map of tags assigned to the resource, including those inherited from the provider default_tags configuration block.
    targetType : str, default is Undefined, optional
        The target type which defines the kinds of resources the document can run on. For example, /AWS::EC2::Instance. For a list of valid resource types, see AWS resource and property types reference.
    versionName : str, default is Undefined, optional
        The version of the artifact associated with the document. For example, 12.6. This value is unique across all versions of a document, and can't be changed.
    """


    arn?: str

    attachmentsSource?: [SsmAwsUpboundIoV1beta1DocumentStatusAtProviderAttachmentsSourceItems0]

    content?: str

    createdDate?: str

    defaultVersion?: str

    description?: str

    documentFormat?: str

    documentType?: str

    documentVersion?: str

    hash?: str

    hashType?: str

    id?: str

    latestVersion?: str

    owner?: str

    parameter?: [SsmAwsUpboundIoV1beta1DocumentStatusAtProviderParameterItems0]

    permissions?: {str:str}

    platformTypes?: [str]

    schemaVersion?: str

    status?: str

    tags?: {str:str}

    tagsAll?: {str:str}

    targetType?: str

    versionName?: str


schema SsmAwsUpboundIoV1beta1DocumentStatusAtProviderAttachmentsSourceItems0:
    r"""
    ssm aws upbound io v1beta1 document status at provider attachments source items0

    Attributes
    ----------
    key : str, default is Undefined, optional
        The key of a key-value pair that identifies the location of an attachment to the document. Valid values: SourceUrl, S3FileUrl, AttachmentReference.
    name : str, default is Undefined, optional
        The name of the document attachment file.
    values : [str], default is Undefined, optional
        The value of a key-value pair that identifies the location of an attachment to the document. The argument format is a list of a single string that depends on the type of key you specify - see the API Reference for details.
    """


    key?: str

    name?: str

    values?: [str]


schema SsmAwsUpboundIoV1beta1DocumentStatusAtProviderParameterItems0:
    r"""
    ssm aws upbound io v1beta1 document status at provider parameter items0

    Attributes
    ----------
    defaultValue : str, default is Undefined, optional
        If specified, the default values for the parameters. Parameters without a default value are required. Parameters with a default value are optional.
    description : str, default is Undefined, optional
        A description of what the parameter does, how to use it, the default value, and whether or not the parameter is optional.
    name : str, default is Undefined, optional
        The name of the parameter.
    $type : str, default is Undefined, optional
        The type of parameter. Valid values: String, StringList.
    """


    defaultValue?: str

    description?: str

    name?: str

    $type?: str


schema SsmAwsUpboundIoV1beta1DocumentStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


