"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema DatabaseInstance:
    """
    DatabaseInstance is the Schema for the DatabaseInstances API. Creates a new SQL database instance in Google Cloud SQL.

    Attributes
    ----------
    apiVersion : str, default is "sql.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "DatabaseInstance", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SQLGcpUpboundIoV1beta1DatabaseInstanceSpec, default is Undefined, required
        spec
    status : SQLGcpUpboundIoV1beta1DatabaseInstanceStatus, default is Undefined, optional
        status
    """


    apiVersion: "sql.gcp.upbound.io/v1beta1" = "sql.gcp.upbound.io/v1beta1"

    kind: "DatabaseInstance" = "DatabaseInstance"

    metadata?: v1.ObjectMeta

    spec: SQLGcpUpboundIoV1beta1DatabaseInstanceSpec

    status?: SQLGcpUpboundIoV1beta1DatabaseInstanceStatus


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpec:
    """
    DatabaseInstanceSpec defines the desired state of DatabaseInstance

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProvider, default is Undefined, required
        for provider
    initProvider : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProvider

    initProvider?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecProviderConfigRef

    publishConnectionDetailsTo?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecWriteConnectionSecretToRef


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProvider:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider

    Attributes
    ----------
    clone : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderCloneItems0], default is Undefined, optional
        The context needed to create this instance as a clone of another instance. The
        configuration is detailed below.
    databaseVersion : str, default is Undefined, optional
        The MySQL, PostgreSQL or
        SQL Server version to use. Supported values include MYSQL_5_6,
        MYSQL_5_7, MYSQL_8_0, POSTGRES_9_6,POSTGRES_10, POSTGRES_11,
        POSTGRES_12, POSTGRES_13, POSTGRES_14, POSTGRES_15, SQLSERVER_2017_STANDARD,
        SQLSERVER_2017_ENTERPRISE, SQLSERVER_2017_EXPRESS, SQLSERVER_2017_WEB.
        SQLSERVER_2019_STANDARD, SQLSERVER_2019_ENTERPRISE, SQLSERVER_2019_EXPRESS,
        SQLSERVER_2019_WEB.
        Database Version Policies
        includes an up-to-date reference of supported versions.
    deletionProtection : bool, default is Undefined, optional
        Defaults to true.
    encryptionKeyName : str, default is Undefined, optional
        The full path to the encryption key used for the CMEK disk encryption.
        The provided key must be in the same region as the SQL instance.  In order
        to use this feature, a special kind of service account must be created and
        granted permission on this key.  This step can currently only be done
        manually, please see this step.
        That service account needs the Cloud KMS > Cloud KMS CryptoKey Encrypter/Decrypter role on your
        key - please see this step.
    maintenanceVersion : str, default is Undefined, optional
        The current software version on the instance. This attribute can not be set during creation. Refer to available_maintenance_versions attribute to see what maintenance_version are available for upgrade. When this attribute gets updated, it will cause an instance restart. Setting a maintenance_version value that is older than the current one on the instance will be ignored.
    masterInstanceName : str, default is Undefined, optional
        The name of the existing instance that will
        act as the master in the replication setup. Note, this requires the master to
        have binary_log_enabled set, as well as existing backups.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs. If it
        is not provided, the provider project is used.
    region : str, default is Undefined, optional
        The region the instance will sit in. If a region is not provided in the resource definition,
        the provider region will be used instead.
    replicaConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderReplicaConfigurationItems0], default is Undefined, optional
        The configuration for replication. The
        configuration is detailed below. Valid only for MySQL instances.
    restoreBackupContext : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderRestoreBackupContextItems0], default is Undefined, optional
        The context needed to restore the database to a backup run. The configuration is detailed below. Adding or modifying this
        block during resource creation/update will trigger the restore action after the resource is created/updated.
    rootPasswordSecretRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderRootPasswordSecretRef, default is Undefined, optional
        root password secret ref
    settings : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0], default is Undefined, optional
        The settings to use for the database. The
        configuration is detailed below. Required if clone is not set.
    """


    clone?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderCloneItems0]

    databaseVersion?: str

    deletionProtection?: bool

    encryptionKeyName?: str

    maintenanceVersion?: str

    masterInstanceName?: str

    project?: str

    region?: str

    replicaConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderReplicaConfigurationItems0]

    restoreBackupContext?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderRestoreBackupContextItems0]

    rootPasswordSecretRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderRootPasswordSecretRef

    settings?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0]


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderCloneItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider clone items0

    Attributes
    ----------
    allocatedIpRange : str, default is Undefined, optional
        The name of the allocated ip range for the private ip CloudSQL instance. For example: "google-managed-services-default". If set, the cloned instance ip will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression a-z?.
    databaseNames : [str], default is Undefined, optional
        (SQL Server only, use with point_in_time) Clone only the specified databases from the source instance. Clone all databases if empty.
    pointInTime : str, default is Undefined, optional
        The timestamp of the point in time that should be restored.
    preferredZone : str, default is Undefined, optional
        (Point-in-time recovery for PostgreSQL only) Clone to an instance in the specified zone. If no zone is specified, clone to the same zone as the source instance. clone-unavailable-instance
    sourceInstanceName : str, default is Undefined, optional
        Name of the source instance which will be cloned.
    """


    allocatedIpRange?: str

    databaseNames?: [str]

    pointInTime?: str

    preferredZone?: str

    sourceInstanceName?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderReplicaConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider replica configuration items0

    Attributes
    ----------
    caCertificate : str, default is Undefined, optional
        PEM representation of the trusted CA's x509
        certificate.
    clientCertificate : str, default is Undefined, optional
        PEM representation of the replica's x509
        certificate.
    clientKey : str, default is Undefined, optional
        PEM representation of the replica's private key. The
        corresponding public key in encoded in the client_certificate.
    connectRetryInterval : float, default is Undefined, optional
        The number of seconds
        between connect retries. MySQL's default is 60 seconds.
    dumpFilePath : str, default is Undefined, optional
        Path to a SQL file in GCS from which replica
        instances are created. Format is gs://bucket/filename.
    failoverTarget : bool, default is Undefined, optional
        Specifies if the replica is the failover target.
        If the field is set to true the replica will be designated as a failover replica.
        If the master instance fails, the replica instance will be promoted as
        the new master instance.
        ~> NOTE: Not supported for Postgres database.
    masterHeartbeatPeriod : float, default is Undefined, optional
        Time in ms between replication
        heartbeats.
    passwordSecretRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderReplicaConfigurationItems0PasswordSecretRef, default is Undefined, optional
        password secret ref
    sslCipher : str, default is Undefined, optional
        Permissible ciphers for use in SSL encryption.
    username : str, default is Undefined, optional
        Username for replication connection.
    verifyServerCertificate : bool, default is Undefined, optional
        True if the master's common name
        value is checked during the SSL handshake.
    """


    caCertificate?: str

    clientCertificate?: str

    clientKey?: str

    connectRetryInterval?: float

    dumpFilePath?: str

    failoverTarget?: bool

    masterHeartbeatPeriod?: float

    passwordSecretRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderReplicaConfigurationItems0PasswordSecretRef

    sslCipher?: str

    username?: str

    verifyServerCertificate?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderReplicaConfigurationItems0PasswordSecretRef:
    """
    Password for the replication connection.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    key: str

    name: str

    namespace: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderRestoreBackupContextItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider restore backup context items0

    Attributes
    ----------
    backupRunId : float, default is Undefined, optional
        The ID of the backup run to restore from.
    instanceId : str, default is Undefined, optional
        The ID of the instance that the backup was taken from. If left empty,
        this instance's ID will be used.
    project : str, default is Undefined, optional
        The full project ID of the source instance.`
    """


    backupRunId?: float

    instanceId?: str

    project?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderRootPasswordSecretRef:
    """
    Initial root password. Can be updated. Required for MS SQL Server.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    key: str

    name: str

    namespace: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0

    Attributes
    ----------
    activationPolicy : str, default is Undefined, optional
        This specifies when the instance should be
        active. Can be either ALWAYS, NEVER or ON_DEMAND.
    activeDirectoryConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0ActiveDirectoryConfigItems0], default is Undefined, optional
        active directory config
    advancedMachineFeatures : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0AdvancedMachineFeaturesItems0], default is Undefined, optional
        advanced machine features
    availabilityType : str, default is Undefined, optional
        The availability type of the Cloud SQL
        instance, high availability (REGIONAL) or single zone (ZONAL).' For all instances, ensure that
        settings.backup_configuration.enabled is set to true.
        For MySQL instances, ensure that settings.backup_configuration.binary_log_enabled is set to true.
        For Postgres and SQL Server instances, ensure that settings.backup_configuration.point_in_time_recovery_enabled
        is set to true. Defaults to ZONAL.
    backupConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0BackupConfigurationItems0], default is Undefined, optional
        backup configuration
    collation : str, default is Undefined, optional
        The name of server instance collation.
    connectorEnforcement : str, default is Undefined, optional
        Specifies if connections must use Cloud SQL connectors.
    dataCacheConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DataCacheConfigItems0], default is Undefined, optional
        data cache config
    databaseFlags : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DatabaseFlagsItems0], default is Undefined, optional
        database flags
    deletionProtectionEnabled : bool, default is Undefined, optional
        .
    denyMaintenancePeriod : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DenyMaintenancePeriodItems0], default is Undefined, optional
        deny maintenance period
    diskAutoresize : bool, default is Undefined, optional
        Enables auto-resizing of the storage size. Defaults to true.
    diskAutoresizeLimit : float, default is Undefined, optional
        The maximum size to which storage capacity can be automatically increased. The default value is 0, which specifies that there is no limit.
    diskSize : float, default is Undefined, optional
        The size of data disk, in GB. Size of a running instance cannot be reduced but can be increased. The minimum value is 10GB.
    diskType : str, default is Undefined, optional
        The type of data disk: PD_SSD or PD_HDD. Defaults to PD_SSD.
    edition : str, default is Undefined, optional
        The edition of the instance, can be ENTERPRISE or ENTERPRISE_PLUS.
    insightsConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0InsightsConfigItems0], default is Undefined, optional
        insights config
    ipConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0], default is Undefined, optional
        ip configuration
    locationPreference : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0LocationPreferenceItems0], default is Undefined, optional
        location preference
    maintenanceWindow : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0MaintenanceWindowItems0], default is Undefined, optional
        maintenance window
    passwordValidationPolicy : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0PasswordValidationPolicyItems0], default is Undefined, optional
        password validation policy
    pricingPlan : str, default is Undefined, optional
        Pricing plan for this instance, can only be PER_USE.
    sqlServerAuditConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0SQLServerAuditConfigItems0], default is Undefined, optional
        sql server audit config
    tier : str, default is Undefined, optional
        The machine type to use. See tiers
        for more details and supported versions. Postgres supports only shared-core machine types,
        and custom machine types such as db-custom-2-13312. See the Custom Machine Type Documentation to learn about specifying custom machine types.
    timeZone : str, default is Undefined, optional
        The time_zone to be used by the database engine (supported only for SQL Server), in SQL Server timezone format.
    userLabels : {str:str}, default is Undefined, optional
        A set of key/value user label pairs to assign to the instance.
    """


    activationPolicy?: str

    activeDirectoryConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0ActiveDirectoryConfigItems0]

    advancedMachineFeatures?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0AdvancedMachineFeaturesItems0]

    availabilityType?: str

    backupConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0BackupConfigurationItems0]

    collation?: str

    connectorEnforcement?: str

    dataCacheConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DataCacheConfigItems0]

    databaseFlags?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DatabaseFlagsItems0]

    deletionProtectionEnabled?: bool

    denyMaintenancePeriod?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DenyMaintenancePeriodItems0]

    diskAutoresize?: bool

    diskAutoresizeLimit?: float

    diskSize?: float

    diskType?: str

    edition?: str

    insightsConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0InsightsConfigItems0]

    ipConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0]

    locationPreference?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0LocationPreferenceItems0]

    maintenanceWindow?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0MaintenanceWindowItems0]

    passwordValidationPolicy?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0PasswordValidationPolicyItems0]

    pricingPlan?: str

    sqlServerAuditConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0SQLServerAuditConfigItems0]

    tier?: str

    timeZone?: str

    userLabels?: {str:str}


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0ActiveDirectoryConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 active directory config items0

    Attributes
    ----------
    domain : str, default is Undefined, optional
        The domain name for the active directory (e.g., mydomain.com).
        Can only be used with SQL Server.
    """


    domain?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0AdvancedMachineFeaturesItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 advanced machine features items0

    Attributes
    ----------
    threadsPerCore : float, default is Undefined, optional
        The number of threads per core. The value of this flag can be 1 or 2. To disable SMT, set this flag to 1. Only available in Cloud SQL for SQL Server instances. See smt for more details.
    """


    threadsPerCore?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0BackupConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 backup configuration items0

    Attributes
    ----------
    backupRetentionSettings : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0], default is Undefined, optional
        Backup retention settings. The configuration is detailed below.
    binaryLogEnabled : bool, default is Undefined, optional
        True if binary logging is enabled.
        Can only be used with MySQL.
    enabled : bool, default is Undefined, optional
        True if backup configuration is enabled.
    location : str, default is Undefined, optional
        The region where the backup will be stored
    pointInTimeRecoveryEnabled : bool, default is Undefined, optional
        True if Point-in-time recovery is enabled. Will restart database if enabled after instance creation. Valid only for PostgreSQL and SQL Server instances.
    startTime : str, default is Undefined, optional
        HH:MM format time indicating when backup
        configuration starts.
    transactionLogRetentionDays : float, default is Undefined, optional
        The number of days of transaction logs we retain for point in time restore, from 1-7. For PostgreSQL Enterprise Plus instances, the number of days of retained transaction logs can be set from 1 to 35.
    """


    backupRetentionSettings?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0]

    binaryLogEnabled?: bool

    enabled?: bool

    location?: str

    pointInTimeRecoveryEnabled?: bool

    startTime?: str

    transactionLogRetentionDays?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 backup configuration items0 backup retention settings items0

    Attributes
    ----------
    retainedBackups : float, default is Undefined, optional
        Depending on the value of retention_unit, this is used to determine if a backup needs to be deleted. If retention_unit
        is 'COUNT', we will retain this many backups.
    retentionUnit : str, default is Undefined, optional
        The unit that 'retained_backups' represents. Defaults to COUNT.
    """


    retainedBackups?: float

    retentionUnit?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DataCacheConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 data cache config items0

    Attributes
    ----------
    dataCacheEnabled : bool, default is Undefined, optional
        Whether data cache is enabled for the instance. Defaults to false. Can be used with MYSQL and PostgreSQL only.
    """


    dataCacheEnabled?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DatabaseFlagsItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 database flags items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        A name for this whitelist entry.
    value : str, default is Undefined, optional
        A CIDR notation IPv4 or IPv6 address that is allowed to
        access this instance. Must be set even if other two attributes are not for
        the whitelist to become active.
    """


    name?: str

    value?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0DenyMaintenancePeriodItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 deny maintenance period items0

    Attributes
    ----------
    endDate : str, default is Undefined, optional
        "deny maintenance period" end date. If the year of the end date is empty, the year of the start date also must be empty. In this case, it means the no maintenance interval recurs every year. The date is in format yyyy-mm-dd i.e., 2020-11-01, or mm-dd, i.e., 11-01
    startDate : str, default is Undefined, optional
        "deny maintenance period" start date. If the year of the start date is empty, the year of the end date also must be empty. In this case, it means the deny maintenance period recurs every year. The date is in format yyyy-mm-dd i.e., 2020-11-01, or mm-dd, i.e., 11-01
    time : str, default is Undefined, optional
        Time in UTC when the "deny maintenance period" starts on startDate and ends on endDate. The time is in format: HH:mm:SS, i.e., 00:00:00
    """


    endDate?: str

    startDate?: str

    time?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 IP configuration items0

    Attributes
    ----------
    allocatedIpRange : str, default is Undefined, optional
        The name of the allocated ip range for the private ip CloudSQL instance. For example: "google-managed-services-default". If set, the instance ip will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression a-z?.
    authorizedNetworks : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0], default is Undefined, optional
        authorized networks
    enablePrivatePathForGoogleCloudServices : bool, default is Undefined, optional
        Whether Google Cloud services such as BigQuery are allowed to access data in this Cloud SQL instance over a private IP connection. SQLSERVER database type is not supported.
    ipv4Enabled : bool, default is Undefined, optional
        Whether this Cloud SQL instance should be assigned
        a public IPV4 address. At least ipv4_enabled must be enabled or a
        private_network must be configured.
    privateNetwork : str, default is Undefined, optional
        The VPC network from which the Cloud SQL
        instance is accessible for private IP. For example, projects/myProject/global/networks/default.
        Specifying a network enables private IP.
        At least ipv4_enabled must be enabled or a private_network must be configured.
        This setting can be updated, but it cannot be removed after it is set.
    privateNetworkRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkRef, default is Undefined, optional
        private network ref
    privateNetworkSelector : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelector, default is Undefined, optional
        private network selector
    pscConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PscConfigItems0], default is Undefined, optional
        psc config
    requireSsl : bool, default is Undefined, optional
        Whether SSL connections over IP are enforced or not. To change this field, also set the corresponding value in ssl_mode.
    sslMode : str, default is Undefined, optional
        Specify how SSL connection should be enforced in DB connections. This field provides more SSL enforcment options compared to require_ssl. To change this field, also set the correspoding value in require_ssl.
    """


    allocatedIpRange?: str

    authorizedNetworks?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0]

    enablePrivatePathForGoogleCloudServices?: bool

    ipv4Enabled?: bool

    privateNetwork?: str

    privateNetworkRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkRef

    privateNetworkSelector?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelector

    pscConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PscConfigItems0]

    requireSsl?: bool

    sslMode?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 IP configuration items0 authorized networks items0

    Attributes
    ----------
    expirationTime : str, default is Undefined, optional
        The RFC 3339
        formatted date time string indicating when this whitelist expires.
    name : str, default is Undefined, optional
        A name for this whitelist entry.
    value : str, default is Undefined, optional
        A CIDR notation IPv4 or IPv6 address that is allowed to
        access this instance. Must be set even if other two attributes are not for
        the whitelist to become active.
    """


    expirationTime?: str

    name?: str

    value?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkRef:
    """
    Reference to a Network in compute to populate privateNetwork.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkRefPolicy


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelector:
    """
    Selector for a Network in compute to populate privateNetwork.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelectorPolicy


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0IPConfigurationItems0PscConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 IP configuration items0 psc config items0

    Attributes
    ----------
    allowedConsumerProjects : [str], default is Undefined, optional
        List of consumer projects that are allow-listed for PSC connections to this instance. This instance can be connected to with PSC from any network in these projects. Each consumer project in this list may be represented by a project number (numeric) or by a project id (alphanumeric).
    pscEnabled : bool, default is Undefined, optional
        Whether PSC connectivity is enabled for this instance.
    """


    allowedConsumerProjects?: [str]

    pscEnabled?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0InsightsConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 insights config items0

    Attributes
    ----------
    queryInsightsEnabled : bool, default is Undefined, optional
        True if Query Insights feature is enabled.
    queryPlansPerMinute : float, default is Undefined, optional
        Number of query execution plans captured by Insights per minute for all queries combined. Between 0 and 20. Default to 5.
    queryStringLength : float, default is Undefined, optional
        Maximum query length stored in bytes. Between 256 and 4500. Default to 1024. Higher query lengths are more useful for analytical queries, but they also require more memory. Changing the query length requires you to restart the instance. You can still add tags to queries that exceed the length limit.
    recordApplicationTags : bool, default is Undefined, optional
        True if Query Insights will record application tags from query when enabled.
    recordClientAddress : bool, default is Undefined, optional
        True if Query Insights will record client address when enabled.
    """


    queryInsightsEnabled?: bool

    queryPlansPerMinute?: float

    queryStringLength?: float

    recordApplicationTags?: bool

    recordClientAddress?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0LocationPreferenceItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 location preference items0

    Attributes
    ----------
    followGaeApplication : str, default is Undefined, optional
        A GAE application whose zone to remain
        in. Must be in the same region as this instance.
    secondaryZone : str, default is Undefined, optional
        The preferred Compute Engine zone for the secondary/failover.
    zone : str, default is Undefined, optional
        The preferred compute engine
        zone.
    """


    followGaeApplication?: str

    secondaryZone?: str

    zone?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0MaintenanceWindowItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 maintenance window items0

    Attributes
    ----------
    day : float, default is Undefined, optional
        Day of week (1-7), starting on Monday
    hour : float, default is Undefined, optional
        Hour of day (0-23), ignored if day not set
    updateTrack : str, default is Undefined, optional
        Receive updates earlier (canary) or later
        (stable)
    """


    day?: float

    hour?: float

    updateTrack?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0PasswordValidationPolicyItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 password validation policy items0

    Attributes
    ----------
    complexity : str, default is Undefined, optional
        Checks if the password is a combination of lowercase, uppercase, numeric, and non-alphanumeric characters.
    disallowUsernameSubstring : bool, default is Undefined, optional
        Prevents the use of the username in the password.
    enablePasswordPolicy : bool, default is Undefined, optional
        Enables or disable the password validation policy.
    minLength : float, default is Undefined, optional
        Specifies the minimum number of characters that the password must have.
    passwordChangeInterval : str, default is Undefined, optional
        Specifies the minimum duration after which you can change the password.
    reuseInterval : float, default is Undefined, optional
        Specifies the number of previous passwords that you can't reuse.
    """


    complexity?: str

    disallowUsernameSubstring?: bool

    enablePasswordPolicy?: bool

    minLength?: float

    passwordChangeInterval?: str

    reuseInterval?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecForProviderSettingsItems0SQLServerAuditConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec for provider settings items0 SQL server audit config items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        The name of the destination bucket (e.g., gs://mybucket).
    retentionInterval : str, default is Undefined, optional
        How long to keep generated audit files. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    uploadInterval : str, default is Undefined, optional
        How often to upload generated audit files. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    """


    bucket?: str

    retentionInterval?: str

    uploadInterval?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    clone : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderCloneItems0], default is Undefined, optional
        The context needed to create this instance as a clone of another instance. The
        configuration is detailed below.
    databaseVersion : str, default is Undefined, optional
        The MySQL, PostgreSQL or
        SQL Server version to use. Supported values include MYSQL_5_6,
        MYSQL_5_7, MYSQL_8_0, POSTGRES_9_6,POSTGRES_10, POSTGRES_11,
        POSTGRES_12, POSTGRES_13, POSTGRES_14, POSTGRES_15, SQLSERVER_2017_STANDARD,
        SQLSERVER_2017_ENTERPRISE, SQLSERVER_2017_EXPRESS, SQLSERVER_2017_WEB.
        SQLSERVER_2019_STANDARD, SQLSERVER_2019_ENTERPRISE, SQLSERVER_2019_EXPRESS,
        SQLSERVER_2019_WEB.
        Database Version Policies
        includes an up-to-date reference of supported versions.
    deletionProtection : bool, default is Undefined, optional
        Defaults to true.
    encryptionKeyName : str, default is Undefined, optional
        The full path to the encryption key used for the CMEK disk encryption.
        The provided key must be in the same region as the SQL instance.  In order
        to use this feature, a special kind of service account must be created and
        granted permission on this key.  This step can currently only be done
        manually, please see this step.
        That service account needs the Cloud KMS > Cloud KMS CryptoKey Encrypter/Decrypter role on your
        key - please see this step.
    maintenanceVersion : str, default is Undefined, optional
        The current software version on the instance. This attribute can not be set during creation. Refer to available_maintenance_versions attribute to see what maintenance_version are available for upgrade. When this attribute gets updated, it will cause an instance restart. Setting a maintenance_version value that is older than the current one on the instance will be ignored.
    masterInstanceName : str, default is Undefined, optional
        The name of the existing instance that will
        act as the master in the replication setup. Note, this requires the master to
        have binary_log_enabled set, as well as existing backups.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs. If it
        is not provided, the provider project is used.
    region : str, default is Undefined, optional
        The region the instance will sit in. If a region is not provided in the resource definition,
        the provider region will be used instead.
    replicaConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderReplicaConfigurationItems0], default is Undefined, optional
        The configuration for replication. The
        configuration is detailed below. Valid only for MySQL instances.
    restoreBackupContext : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderRestoreBackupContextItems0], default is Undefined, optional
        The context needed to restore the database to a backup run. The configuration is detailed below. Adding or modifying this
        block during resource creation/update will trigger the restore action after the resource is created/updated.
    settings : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0], default is Undefined, optional
        The settings to use for the database. The
        configuration is detailed below. Required if clone is not set.
    """


    clone?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderCloneItems0]

    databaseVersion?: str

    deletionProtection?: bool

    encryptionKeyName?: str

    maintenanceVersion?: str

    masterInstanceName?: str

    project?: str

    region?: str

    replicaConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderReplicaConfigurationItems0]

    restoreBackupContext?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderRestoreBackupContextItems0]

    settings?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0]


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderCloneItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider clone items0

    Attributes
    ----------
    allocatedIpRange : str, default is Undefined, optional
        The name of the allocated ip range for the private ip CloudSQL instance. For example: "google-managed-services-default". If set, the cloned instance ip will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression a-z?.
    databaseNames : [str], default is Undefined, optional
        (SQL Server only, use with point_in_time) Clone only the specified databases from the source instance. Clone all databases if empty.
    pointInTime : str, default is Undefined, optional
        The timestamp of the point in time that should be restored.
    preferredZone : str, default is Undefined, optional
        (Point-in-time recovery for PostgreSQL only) Clone to an instance in the specified zone. If no zone is specified, clone to the same zone as the source instance. clone-unavailable-instance
    sourceInstanceName : str, default is Undefined, optional
        Name of the source instance which will be cloned.
    """


    allocatedIpRange?: str

    databaseNames?: [str]

    pointInTime?: str

    preferredZone?: str

    sourceInstanceName?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderReplicaConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider replica configuration items0

    Attributes
    ----------
    caCertificate : str, default is Undefined, optional
        PEM representation of the trusted CA's x509
        certificate.
    clientCertificate : str, default is Undefined, optional
        PEM representation of the replica's x509
        certificate.
    clientKey : str, default is Undefined, optional
        PEM representation of the replica's private key. The
        corresponding public key in encoded in the client_certificate.
    connectRetryInterval : float, default is Undefined, optional
        The number of seconds
        between connect retries. MySQL's default is 60 seconds.
    dumpFilePath : str, default is Undefined, optional
        Path to a SQL file in GCS from which replica
        instances are created. Format is gs://bucket/filename.
    failoverTarget : bool, default is Undefined, optional
        Specifies if the replica is the failover target.
        If the field is set to true the replica will be designated as a failover replica.
        If the master instance fails, the replica instance will be promoted as
        the new master instance.
        ~> NOTE: Not supported for Postgres database.
    masterHeartbeatPeriod : float, default is Undefined, optional
        Time in ms between replication
        heartbeats.
    sslCipher : str, default is Undefined, optional
        Permissible ciphers for use in SSL encryption.
    username : str, default is Undefined, optional
        Username for replication connection.
    verifyServerCertificate : bool, default is Undefined, optional
        True if the master's common name
        value is checked during the SSL handshake.
    """


    caCertificate?: str

    clientCertificate?: str

    clientKey?: str

    connectRetryInterval?: float

    dumpFilePath?: str

    failoverTarget?: bool

    masterHeartbeatPeriod?: float

    sslCipher?: str

    username?: str

    verifyServerCertificate?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderRestoreBackupContextItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider restore backup context items0

    Attributes
    ----------
    backupRunId : float, default is Undefined, optional
        The ID of the backup run to restore from.
    instanceId : str, default is Undefined, optional
        The ID of the instance that the backup was taken from. If left empty,
        this instance's ID will be used.
    project : str, default is Undefined, optional
        The full project ID of the source instance.`
    """


    backupRunId?: float

    instanceId?: str

    project?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0

    Attributes
    ----------
    activationPolicy : str, default is Undefined, optional
        This specifies when the instance should be
        active. Can be either ALWAYS, NEVER or ON_DEMAND.
    activeDirectoryConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0ActiveDirectoryConfigItems0], default is Undefined, optional
        active directory config
    advancedMachineFeatures : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0AdvancedMachineFeaturesItems0], default is Undefined, optional
        advanced machine features
    availabilityType : str, default is Undefined, optional
        The availability type of the Cloud SQL
        instance, high availability (REGIONAL) or single zone (ZONAL).' For all instances, ensure that
        settings.backup_configuration.enabled is set to true.
        For MySQL instances, ensure that settings.backup_configuration.binary_log_enabled is set to true.
        For Postgres and SQL Server instances, ensure that settings.backup_configuration.point_in_time_recovery_enabled
        is set to true. Defaults to ZONAL.
    backupConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0BackupConfigurationItems0], default is Undefined, optional
        backup configuration
    collation : str, default is Undefined, optional
        The name of server instance collation.
    connectorEnforcement : str, default is Undefined, optional
        Specifies if connections must use Cloud SQL connectors.
    dataCacheConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DataCacheConfigItems0], default is Undefined, optional
        data cache config
    databaseFlags : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DatabaseFlagsItems0], default is Undefined, optional
        database flags
    deletionProtectionEnabled : bool, default is Undefined, optional
        .
    denyMaintenancePeriod : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DenyMaintenancePeriodItems0], default is Undefined, optional
        deny maintenance period
    diskAutoresize : bool, default is Undefined, optional
        Enables auto-resizing of the storage size. Defaults to true.
    diskAutoresizeLimit : float, default is Undefined, optional
        The maximum size to which storage capacity can be automatically increased. The default value is 0, which specifies that there is no limit.
    diskSize : float, default is Undefined, optional
        The size of data disk, in GB. Size of a running instance cannot be reduced but can be increased. The minimum value is 10GB.
    diskType : str, default is Undefined, optional
        The type of data disk: PD_SSD or PD_HDD. Defaults to PD_SSD.
    edition : str, default is Undefined, optional
        The edition of the instance, can be ENTERPRISE or ENTERPRISE_PLUS.
    insightsConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0InsightsConfigItems0], default is Undefined, optional
        insights config
    ipConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0], default is Undefined, optional
        ip configuration
    locationPreference : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0LocationPreferenceItems0], default is Undefined, optional
        location preference
    maintenanceWindow : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0MaintenanceWindowItems0], default is Undefined, optional
        maintenance window
    passwordValidationPolicy : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0PasswordValidationPolicyItems0], default is Undefined, optional
        password validation policy
    pricingPlan : str, default is Undefined, optional
        Pricing plan for this instance, can only be PER_USE.
    sqlServerAuditConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0SQLServerAuditConfigItems0], default is Undefined, optional
        sql server audit config
    tier : str, default is Undefined, optional
        The machine type to use. See tiers
        for more details and supported versions. Postgres supports only shared-core machine types,
        and custom machine types such as db-custom-2-13312. See the Custom Machine Type Documentation to learn about specifying custom machine types.
    timeZone : str, default is Undefined, optional
        The time_zone to be used by the database engine (supported only for SQL Server), in SQL Server timezone format.
    userLabels : {str:str}, default is Undefined, optional
        A set of key/value user label pairs to assign to the instance.
    """


    activationPolicy?: str

    activeDirectoryConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0ActiveDirectoryConfigItems0]

    advancedMachineFeatures?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0AdvancedMachineFeaturesItems0]

    availabilityType?: str

    backupConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0BackupConfigurationItems0]

    collation?: str

    connectorEnforcement?: str

    dataCacheConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DataCacheConfigItems0]

    databaseFlags?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DatabaseFlagsItems0]

    deletionProtectionEnabled?: bool

    denyMaintenancePeriod?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DenyMaintenancePeriodItems0]

    diskAutoresize?: bool

    diskAutoresizeLimit?: float

    diskSize?: float

    diskType?: str

    edition?: str

    insightsConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0InsightsConfigItems0]

    ipConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0]

    locationPreference?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0LocationPreferenceItems0]

    maintenanceWindow?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0MaintenanceWindowItems0]

    passwordValidationPolicy?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0PasswordValidationPolicyItems0]

    pricingPlan?: str

    sqlServerAuditConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0SQLServerAuditConfigItems0]

    tier?: str

    timeZone?: str

    userLabels?: {str:str}


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0ActiveDirectoryConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 active directory config items0

    Attributes
    ----------
    domain : str, default is Undefined, optional
        The domain name for the active directory (e.g., mydomain.com).
        Can only be used with SQL Server.
    """


    domain?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0AdvancedMachineFeaturesItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 advanced machine features items0

    Attributes
    ----------
    threadsPerCore : float, default is Undefined, optional
        The number of threads per core. The value of this flag can be 1 or 2. To disable SMT, set this flag to 1. Only available in Cloud SQL for SQL Server instances. See smt for more details.
    """


    threadsPerCore?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0BackupConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 backup configuration items0

    Attributes
    ----------
    backupRetentionSettings : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0], default is Undefined, optional
        Backup retention settings. The configuration is detailed below.
    binaryLogEnabled : bool, default is Undefined, optional
        True if binary logging is enabled.
        Can only be used with MySQL.
    enabled : bool, default is Undefined, optional
        True if backup configuration is enabled.
    location : str, default is Undefined, optional
        The region where the backup will be stored
    pointInTimeRecoveryEnabled : bool, default is Undefined, optional
        True if Point-in-time recovery is enabled. Will restart database if enabled after instance creation. Valid only for PostgreSQL and SQL Server instances.
    startTime : str, default is Undefined, optional
        HH:MM format time indicating when backup
        configuration starts.
    transactionLogRetentionDays : float, default is Undefined, optional
        The number of days of transaction logs we retain for point in time restore, from 1-7. For PostgreSQL Enterprise Plus instances, the number of days of retained transaction logs can be set from 1 to 35.
    """


    backupRetentionSettings?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0]

    binaryLogEnabled?: bool

    enabled?: bool

    location?: str

    pointInTimeRecoveryEnabled?: bool

    startTime?: str

    transactionLogRetentionDays?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 backup configuration items0 backup retention settings items0

    Attributes
    ----------
    retainedBackups : float, default is Undefined, optional
        Depending on the value of retention_unit, this is used to determine if a backup needs to be deleted. If retention_unit
        is 'COUNT', we will retain this many backups.
    retentionUnit : str, default is Undefined, optional
        The unit that 'retained_backups' represents. Defaults to COUNT.
    """


    retainedBackups?: float

    retentionUnit?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DataCacheConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 data cache config items0

    Attributes
    ----------
    dataCacheEnabled : bool, default is Undefined, optional
        Whether data cache is enabled for the instance. Defaults to false. Can be used with MYSQL and PostgreSQL only.
    """


    dataCacheEnabled?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DatabaseFlagsItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 database flags items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        A name for this whitelist entry.
    value : str, default is Undefined, optional
        A CIDR notation IPv4 or IPv6 address that is allowed to
        access this instance. Must be set even if other two attributes are not for
        the whitelist to become active.
    """


    name?: str

    value?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0DenyMaintenancePeriodItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 deny maintenance period items0

    Attributes
    ----------
    endDate : str, default is Undefined, optional
        "deny maintenance period" end date. If the year of the end date is empty, the year of the start date also must be empty. In this case, it means the no maintenance interval recurs every year. The date is in format yyyy-mm-dd i.e., 2020-11-01, or mm-dd, i.e., 11-01
    startDate : str, default is Undefined, optional
        "deny maintenance period" start date. If the year of the start date is empty, the year of the end date also must be empty. In this case, it means the deny maintenance period recurs every year. The date is in format yyyy-mm-dd i.e., 2020-11-01, or mm-dd, i.e., 11-01
    time : str, default is Undefined, optional
        Time in UTC when the "deny maintenance period" starts on startDate and ends on endDate. The time is in format: HH:mm:SS, i.e., 00:00:00
    """


    endDate?: str

    startDate?: str

    time?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 IP configuration items0

    Attributes
    ----------
    allocatedIpRange : str, default is Undefined, optional
        The name of the allocated ip range for the private ip CloudSQL instance. For example: "google-managed-services-default". If set, the instance ip will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression a-z?.
    authorizedNetworks : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0], default is Undefined, optional
        authorized networks
    enablePrivatePathForGoogleCloudServices : bool, default is Undefined, optional
        Whether Google Cloud services such as BigQuery are allowed to access data in this Cloud SQL instance over a private IP connection. SQLSERVER database type is not supported.
    ipv4Enabled : bool, default is Undefined, optional
        Whether this Cloud SQL instance should be assigned
        a public IPV4 address. At least ipv4_enabled must be enabled or a
        private_network must be configured.
    privateNetwork : str, default is Undefined, optional
        The VPC network from which the Cloud SQL
        instance is accessible for private IP. For example, projects/myProject/global/networks/default.
        Specifying a network enables private IP.
        At least ipv4_enabled must be enabled or a private_network must be configured.
        This setting can be updated, but it cannot be removed after it is set.
    privateNetworkRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkRef, default is Undefined, optional
        private network ref
    privateNetworkSelector : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelector, default is Undefined, optional
        private network selector
    pscConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PscConfigItems0], default is Undefined, optional
        psc config
    requireSsl : bool, default is Undefined, optional
        Whether SSL connections over IP are enforced or not. To change this field, also set the corresponding value in ssl_mode.
    sslMode : str, default is Undefined, optional
        Specify how SSL connection should be enforced in DB connections. This field provides more SSL enforcment options compared to require_ssl. To change this field, also set the correspoding value in require_ssl.
    """


    allocatedIpRange?: str

    authorizedNetworks?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0]

    enablePrivatePathForGoogleCloudServices?: bool

    ipv4Enabled?: bool

    privateNetwork?: str

    privateNetworkRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkRef

    privateNetworkSelector?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelector

    pscConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PscConfigItems0]

    requireSsl?: bool

    sslMode?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 IP configuration items0 authorized networks items0

    Attributes
    ----------
    expirationTime : str, default is Undefined, optional
        The RFC 3339
        formatted date time string indicating when this whitelist expires.
    name : str, default is Undefined, optional
        A name for this whitelist entry.
    value : str, default is Undefined, optional
        A CIDR notation IPv4 or IPv6 address that is allowed to
        access this instance. Must be set even if other two attributes are not for
        the whitelist to become active.
    """


    expirationTime?: str

    name?: str

    value?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkRef:
    """
    Reference to a Network in compute to populate privateNetwork.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkRefPolicy


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelector:
    """
    Selector for a Network in compute to populate privateNetwork.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelectorPolicy


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PrivateNetworkSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0IPConfigurationItems0PscConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 IP configuration items0 psc config items0

    Attributes
    ----------
    allowedConsumerProjects : [str], default is Undefined, optional
        List of consumer projects that are allow-listed for PSC connections to this instance. This instance can be connected to with PSC from any network in these projects. Each consumer project in this list may be represented by a project number (numeric) or by a project id (alphanumeric).
    pscEnabled : bool, default is Undefined, optional
        Whether PSC connectivity is enabled for this instance.
    """


    allowedConsumerProjects?: [str]

    pscEnabled?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0InsightsConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 insights config items0

    Attributes
    ----------
    queryInsightsEnabled : bool, default is Undefined, optional
        True if Query Insights feature is enabled.
    queryPlansPerMinute : float, default is Undefined, optional
        Number of query execution plans captured by Insights per minute for all queries combined. Between 0 and 20. Default to 5.
    queryStringLength : float, default is Undefined, optional
        Maximum query length stored in bytes. Between 256 and 4500. Default to 1024. Higher query lengths are more useful for analytical queries, but they also require more memory. Changing the query length requires you to restart the instance. You can still add tags to queries that exceed the length limit.
    recordApplicationTags : bool, default is Undefined, optional
        True if Query Insights will record application tags from query when enabled.
    recordClientAddress : bool, default is Undefined, optional
        True if Query Insights will record client address when enabled.
    """


    queryInsightsEnabled?: bool

    queryPlansPerMinute?: float

    queryStringLength?: float

    recordApplicationTags?: bool

    recordClientAddress?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0LocationPreferenceItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 location preference items0

    Attributes
    ----------
    followGaeApplication : str, default is Undefined, optional
        A GAE application whose zone to remain
        in. Must be in the same region as this instance.
    secondaryZone : str, default is Undefined, optional
        The preferred Compute Engine zone for the secondary/failover.
    zone : str, default is Undefined, optional
        The preferred compute engine
        zone.
    """


    followGaeApplication?: str

    secondaryZone?: str

    zone?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0MaintenanceWindowItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 maintenance window items0

    Attributes
    ----------
    day : float, default is Undefined, optional
        Day of week (1-7), starting on Monday
    hour : float, default is Undefined, optional
        Hour of day (0-23), ignored if day not set
    updateTrack : str, default is Undefined, optional
        Receive updates earlier (canary) or later
        (stable)
    """


    day?: float

    hour?: float

    updateTrack?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0PasswordValidationPolicyItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 password validation policy items0

    Attributes
    ----------
    complexity : str, default is Undefined, optional
        Checks if the password is a combination of lowercase, uppercase, numeric, and non-alphanumeric characters.
    disallowUsernameSubstring : bool, default is Undefined, optional
        Prevents the use of the username in the password.
    enablePasswordPolicy : bool, default is Undefined, optional
        Enables or disable the password validation policy.
    minLength : float, default is Undefined, optional
        Specifies the minimum number of characters that the password must have.
    passwordChangeInterval : str, default is Undefined, optional
        Specifies the minimum duration after which you can change the password.
    reuseInterval : float, default is Undefined, optional
        Specifies the number of previous passwords that you can't reuse.
    """


    complexity?: str

    disallowUsernameSubstring?: bool

    enablePasswordPolicy?: bool

    minLength?: float

    passwordChangeInterval?: str

    reuseInterval?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecInitProviderSettingsItems0SQLServerAuditConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance spec init provider settings items0 SQL server audit config items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        The name of the destination bucket (e.g., gs://mybucket).
    retentionInterval : str, default is Undefined, optional
        How long to keep generated audit files. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    uploadInterval : str, default is Undefined, optional
        How often to upload generated audit files. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    """


    bucket?: str

    retentionInterval?: str

    uploadInterval?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecProviderConfigRefPolicy


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToConfigRef

    metadata?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToMetadata

    name: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToConfigRefPolicy


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatus:
    """
    DatabaseInstanceStatus defines the observed state of DatabaseInstance.

    Attributes
    ----------
    atProvider : SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProvider

    conditions?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusConditionsItems0]


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProvider:
    """
    SQL gcp upbound io v1beta1 database instance status at provider

    Attributes
    ----------
    availableMaintenanceVersions : [str], default is Undefined, optional
        The list of all maintenance versions applicable on the instance.
    clone : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderCloneItems0], default is Undefined, optional
        The context needed to create this instance as a clone of another instance. The
        configuration is detailed below.
    connectionName : str, default is Undefined, optional
        The connection name of the instance to be used in
        connection strings. For example, when connecting with Cloud SQL Proxy.
    databaseVersion : str, default is Undefined, optional
        The MySQL, PostgreSQL or
        SQL Server version to use. Supported values include MYSQL_5_6,
        MYSQL_5_7, MYSQL_8_0, POSTGRES_9_6,POSTGRES_10, POSTGRES_11,
        POSTGRES_12, POSTGRES_13, POSTGRES_14, POSTGRES_15, SQLSERVER_2017_STANDARD,
        SQLSERVER_2017_ENTERPRISE, SQLSERVER_2017_EXPRESS, SQLSERVER_2017_WEB.
        SQLSERVER_2019_STANDARD, SQLSERVER_2019_ENTERPRISE, SQLSERVER_2019_EXPRESS,
        SQLSERVER_2019_WEB.
        Database Version Policies
        includes an up-to-date reference of supported versions.
    deletionProtection : bool, default is Undefined, optional
        Defaults to true.
    dnsName : str, default is Undefined, optional
        The name of the instance. This is done because after a name is used, it cannot be reused for
        up to one week.
    encryptionKeyName : str, default is Undefined, optional
        The full path to the encryption key used for the CMEK disk encryption.
        The provided key must be in the same region as the SQL instance.  In order
        to use this feature, a special kind of service account must be created and
        granted permission on this key.  This step can currently only be done
        manually, please see this step.
        That service account needs the Cloud KMS > Cloud KMS CryptoKey Encrypter/Decrypter role on your
        key - please see this step.
    firstIpAddress : str, default is Undefined, optional
        The first IPv4 address of any type assigned.
    id : str, default is Undefined, optional
        id
    instanceType : str, default is Undefined, optional
        The type of the instance. The supported values are SQL_INSTANCE_TYPE_UNSPECIFIED, CLOUD_SQL_INSTANCE, ON_PREMISES_INSTANCE and READ_REPLICA_INSTANCE.
    ipAddress : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderIPAddressItems0], default is Undefined, optional
        The IPv4 address assigned.
    maintenanceVersion : str, default is Undefined, optional
        The current software version on the instance. This attribute can not be set during creation. Refer to available_maintenance_versions attribute to see what maintenance_version are available for upgrade. When this attribute gets updated, it will cause an instance restart. Setting a maintenance_version value that is older than the current one on the instance will be ignored.
    masterInstanceName : str, default is Undefined, optional
        The name of the existing instance that will
        act as the master in the replication setup. Note, this requires the master to
        have binary_log_enabled set, as well as existing backups.
    privateIpAddress : str, default is Undefined, optional
        The first private (PRIVATE) IPv4 address assigned.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs. If it
        is not provided, the provider project is used.
    pscServiceAttachmentLink : str, default is Undefined, optional
        the URI that points to the service attachment of the instance.
    publicIpAddress : str, default is Undefined, optional
        The first public (PRIMARY) IPv4 address assigned.
    region : str, default is Undefined, optional
        The region the instance will sit in. If a region is not provided in the resource definition,
        the provider region will be used instead.
    replicaConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderReplicaConfigurationItems0], default is Undefined, optional
        The configuration for replication. The
        configuration is detailed below. Valid only for MySQL instances.
    restoreBackupContext : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderRestoreBackupContextItems0], default is Undefined, optional
        The context needed to restore the database to a backup run. The configuration is detailed below. Adding or modifying this
        block during resource creation/update will trigger the restore action after the resource is created/updated.
    selfLink : str, default is Undefined, optional
        The URI of the created resource.
    serviceAccountEmailAddress : str, default is Undefined, optional
        The service account email address assigned to the
        instance.
    settings : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0], default is Undefined, optional
        The settings to use for the database. The
        configuration is detailed below. Required if clone is not set.
    """


    availableMaintenanceVersions?: [str]

    clone?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderCloneItems0]

    connectionName?: str

    databaseVersion?: str

    deletionProtection?: bool

    dnsName?: str

    encryptionKeyName?: str

    firstIpAddress?: str

    id?: str

    instanceType?: str

    ipAddress?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderIPAddressItems0]

    maintenanceVersion?: str

    masterInstanceName?: str

    privateIpAddress?: str

    project?: str

    pscServiceAttachmentLink?: str

    publicIpAddress?: str

    region?: str

    replicaConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderReplicaConfigurationItems0]

    restoreBackupContext?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderRestoreBackupContextItems0]

    selfLink?: str

    serviceAccountEmailAddress?: str

    settings?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0]


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderCloneItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider clone items0

    Attributes
    ----------
    allocatedIpRange : str, default is Undefined, optional
        The name of the allocated ip range for the private ip CloudSQL instance. For example: "google-managed-services-default". If set, the cloned instance ip will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression a-z?.
    databaseNames : [str], default is Undefined, optional
        (SQL Server only, use with point_in_time) Clone only the specified databases from the source instance. Clone all databases if empty.
    pointInTime : str, default is Undefined, optional
        The timestamp of the point in time that should be restored.
    preferredZone : str, default is Undefined, optional
        (Point-in-time recovery for PostgreSQL only) Clone to an instance in the specified zone. If no zone is specified, clone to the same zone as the source instance. clone-unavailable-instance
    sourceInstanceName : str, default is Undefined, optional
        Name of the source instance which will be cloned.
    """


    allocatedIpRange?: str

    databaseNames?: [str]

    pointInTime?: str

    preferredZone?: str

    sourceInstanceName?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderIPAddressItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider IP address items0

    Attributes
    ----------
    ipAddress : str, default is Undefined, optional
        The IPv4 address assigned.
    timeToRetire : str, default is Undefined, optional
        The time this IP address will be retired, in RFC
        3339 format.
    $type : str, default is Undefined, optional
        The type of this IP address.
    """


    ipAddress?: str

    timeToRetire?: str

    $type?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderReplicaConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider replica configuration items0

    Attributes
    ----------
    caCertificate : str, default is Undefined, optional
        PEM representation of the trusted CA's x509
        certificate.
    clientCertificate : str, default is Undefined, optional
        PEM representation of the replica's x509
        certificate.
    clientKey : str, default is Undefined, optional
        PEM representation of the replica's private key. The
        corresponding public key in encoded in the client_certificate.
    connectRetryInterval : float, default is Undefined, optional
        The number of seconds
        between connect retries. MySQL's default is 60 seconds.
    dumpFilePath : str, default is Undefined, optional
        Path to a SQL file in GCS from which replica
        instances are created. Format is gs://bucket/filename.
    failoverTarget : bool, default is Undefined, optional
        Specifies if the replica is the failover target.
        If the field is set to true the replica will be designated as a failover replica.
        If the master instance fails, the replica instance will be promoted as
        the new master instance.
        ~> NOTE: Not supported for Postgres database.
    masterHeartbeatPeriod : float, default is Undefined, optional
        Time in ms between replication
        heartbeats.
    sslCipher : str, default is Undefined, optional
        Permissible ciphers for use in SSL encryption.
    username : str, default is Undefined, optional
        Username for replication connection.
    verifyServerCertificate : bool, default is Undefined, optional
        True if the master's common name
        value is checked during the SSL handshake.
    """


    caCertificate?: str

    clientCertificate?: str

    clientKey?: str

    connectRetryInterval?: float

    dumpFilePath?: str

    failoverTarget?: bool

    masterHeartbeatPeriod?: float

    sslCipher?: str

    username?: str

    verifyServerCertificate?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderRestoreBackupContextItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider restore backup context items0

    Attributes
    ----------
    backupRunId : float, default is Undefined, optional
        The ID of the backup run to restore from.
    instanceId : str, default is Undefined, optional
        The ID of the instance that the backup was taken from. If left empty,
        this instance's ID will be used.
    project : str, default is Undefined, optional
        The full project ID of the source instance.`
    """


    backupRunId?: float

    instanceId?: str

    project?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0

    Attributes
    ----------
    activationPolicy : str, default is Undefined, optional
        This specifies when the instance should be
        active. Can be either ALWAYS, NEVER or ON_DEMAND.
    activeDirectoryConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0ActiveDirectoryConfigItems0], default is Undefined, optional
        active directory config
    advancedMachineFeatures : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0AdvancedMachineFeaturesItems0], default is Undefined, optional
        advanced machine features
    availabilityType : str, default is Undefined, optional
        The availability type of the Cloud SQL
        instance, high availability (REGIONAL) or single zone (ZONAL).' For all instances, ensure that
        settings.backup_configuration.enabled is set to true.
        For MySQL instances, ensure that settings.backup_configuration.binary_log_enabled is set to true.
        For Postgres and SQL Server instances, ensure that settings.backup_configuration.point_in_time_recovery_enabled
        is set to true. Defaults to ZONAL.
    backupConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0BackupConfigurationItems0], default is Undefined, optional
        backup configuration
    collation : str, default is Undefined, optional
        The name of server instance collation.
    connectorEnforcement : str, default is Undefined, optional
        Specifies if connections must use Cloud SQL connectors.
    dataCacheConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DataCacheConfigItems0], default is Undefined, optional
        data cache config
    databaseFlags : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DatabaseFlagsItems0], default is Undefined, optional
        database flags
    deletionProtectionEnabled : bool, default is Undefined, optional
        .
    denyMaintenancePeriod : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DenyMaintenancePeriodItems0], default is Undefined, optional
        deny maintenance period
    diskAutoresize : bool, default is Undefined, optional
        Enables auto-resizing of the storage size. Defaults to true.
    diskAutoresizeLimit : float, default is Undefined, optional
        The maximum size to which storage capacity can be automatically increased. The default value is 0, which specifies that there is no limit.
    diskSize : float, default is Undefined, optional
        The size of data disk, in GB. Size of a running instance cannot be reduced but can be increased. The minimum value is 10GB.
    diskType : str, default is Undefined, optional
        The type of data disk: PD_SSD or PD_HDD. Defaults to PD_SSD.
    edition : str, default is Undefined, optional
        The edition of the instance, can be ENTERPRISE or ENTERPRISE_PLUS.
    insightsConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0InsightsConfigItems0], default is Undefined, optional
        insights config
    ipConfiguration : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0], default is Undefined, optional
        ip configuration
    locationPreference : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0LocationPreferenceItems0], default is Undefined, optional
        location preference
    maintenanceWindow : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0MaintenanceWindowItems0], default is Undefined, optional
        maintenance window
    passwordValidationPolicy : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0PasswordValidationPolicyItems0], default is Undefined, optional
        password validation policy
    pricingPlan : str, default is Undefined, optional
        Pricing plan for this instance, can only be PER_USE.
    sqlServerAuditConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0SQLServerAuditConfigItems0], default is Undefined, optional
        sql server audit config
    tier : str, default is Undefined, optional
        The machine type to use. See tiers
        for more details and supported versions. Postgres supports only shared-core machine types,
        and custom machine types such as db-custom-2-13312. See the Custom Machine Type Documentation to learn about specifying custom machine types.
    timeZone : str, default is Undefined, optional
        The time_zone to be used by the database engine (supported only for SQL Server), in SQL Server timezone format.
    userLabels : {str:str}, default is Undefined, optional
        A set of key/value user label pairs to assign to the instance.
    version : float, default is Undefined, optional
        Used to make sure changes to the settings block are
        atomic.
    """


    activationPolicy?: str

    activeDirectoryConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0ActiveDirectoryConfigItems0]

    advancedMachineFeatures?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0AdvancedMachineFeaturesItems0]

    availabilityType?: str

    backupConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0BackupConfigurationItems0]

    collation?: str

    connectorEnforcement?: str

    dataCacheConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DataCacheConfigItems0]

    databaseFlags?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DatabaseFlagsItems0]

    deletionProtectionEnabled?: bool

    denyMaintenancePeriod?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DenyMaintenancePeriodItems0]

    diskAutoresize?: bool

    diskAutoresizeLimit?: float

    diskSize?: float

    diskType?: str

    edition?: str

    insightsConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0InsightsConfigItems0]

    ipConfiguration?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0]

    locationPreference?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0LocationPreferenceItems0]

    maintenanceWindow?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0MaintenanceWindowItems0]

    passwordValidationPolicy?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0PasswordValidationPolicyItems0]

    pricingPlan?: str

    sqlServerAuditConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0SQLServerAuditConfigItems0]

    tier?: str

    timeZone?: str

    userLabels?: {str:str}

    version?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0ActiveDirectoryConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 active directory config items0

    Attributes
    ----------
    domain : str, default is Undefined, optional
        The domain name for the active directory (e.g., mydomain.com).
        Can only be used with SQL Server.
    """


    domain?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0AdvancedMachineFeaturesItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 advanced machine features items0

    Attributes
    ----------
    threadsPerCore : float, default is Undefined, optional
        The number of threads per core. The value of this flag can be 1 or 2. To disable SMT, set this flag to 1. Only available in Cloud SQL for SQL Server instances. See smt for more details.
    """


    threadsPerCore?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0BackupConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 backup configuration items0

    Attributes
    ----------
    backupRetentionSettings : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0], default is Undefined, optional
        Backup retention settings. The configuration is detailed below.
    binaryLogEnabled : bool, default is Undefined, optional
        True if binary logging is enabled.
        Can only be used with MySQL.
    enabled : bool, default is Undefined, optional
        True if backup configuration is enabled.
    location : str, default is Undefined, optional
        The region where the backup will be stored
    pointInTimeRecoveryEnabled : bool, default is Undefined, optional
        True if Point-in-time recovery is enabled. Will restart database if enabled after instance creation. Valid only for PostgreSQL and SQL Server instances.
    startTime : str, default is Undefined, optional
        HH:MM format time indicating when backup
        configuration starts.
    transactionLogRetentionDays : float, default is Undefined, optional
        The number of days of transaction logs we retain for point in time restore, from 1-7. For PostgreSQL Enterprise Plus instances, the number of days of retained transaction logs can be set from 1 to 35.
    """


    backupRetentionSettings?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0]

    binaryLogEnabled?: bool

    enabled?: bool

    location?: str

    pointInTimeRecoveryEnabled?: bool

    startTime?: str

    transactionLogRetentionDays?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0BackupConfigurationItems0BackupRetentionSettingsItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 backup configuration items0 backup retention settings items0

    Attributes
    ----------
    retainedBackups : float, default is Undefined, optional
        Depending on the value of retention_unit, this is used to determine if a backup needs to be deleted. If retention_unit
        is 'COUNT', we will retain this many backups.
    retentionUnit : str, default is Undefined, optional
        The unit that 'retained_backups' represents. Defaults to COUNT.
    """


    retainedBackups?: float

    retentionUnit?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DataCacheConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 data cache config items0

    Attributes
    ----------
    dataCacheEnabled : bool, default is Undefined, optional
        Whether data cache is enabled for the instance. Defaults to false. Can be used with MYSQL and PostgreSQL only.
    """


    dataCacheEnabled?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DatabaseFlagsItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 database flags items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        A name for this whitelist entry.
    value : str, default is Undefined, optional
        A CIDR notation IPv4 or IPv6 address that is allowed to
        access this instance. Must be set even if other two attributes are not for
        the whitelist to become active.
    """


    name?: str

    value?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0DenyMaintenancePeriodItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 deny maintenance period items0

    Attributes
    ----------
    endDate : str, default is Undefined, optional
        "deny maintenance period" end date. If the year of the end date is empty, the year of the start date also must be empty. In this case, it means the no maintenance interval recurs every year. The date is in format yyyy-mm-dd i.e., 2020-11-01, or mm-dd, i.e., 11-01
    startDate : str, default is Undefined, optional
        "deny maintenance period" start date. If the year of the start date is empty, the year of the end date also must be empty. In this case, it means the deny maintenance period recurs every year. The date is in format yyyy-mm-dd i.e., 2020-11-01, or mm-dd, i.e., 11-01
    time : str, default is Undefined, optional
        Time in UTC when the "deny maintenance period" starts on startDate and ends on endDate. The time is in format: HH:mm:SS, i.e., 00:00:00
    """


    endDate?: str

    startDate?: str

    time?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 IP configuration items0

    Attributes
    ----------
    allocatedIpRange : str, default is Undefined, optional
        The name of the allocated ip range for the private ip CloudSQL instance. For example: "google-managed-services-default". If set, the instance ip will be created in the allocated range. The range name must comply with RFC 1035. Specifically, the name must be 1-63 characters long and match the regular expression a-z?.
    authorizedNetworks : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0], default is Undefined, optional
        authorized networks
    enablePrivatePathForGoogleCloudServices : bool, default is Undefined, optional
        Whether Google Cloud services such as BigQuery are allowed to access data in this Cloud SQL instance over a private IP connection. SQLSERVER database type is not supported.
    ipv4Enabled : bool, default is Undefined, optional
        Whether this Cloud SQL instance should be assigned
        a public IPV4 address. At least ipv4_enabled must be enabled or a
        private_network must be configured.
    privateNetwork : str, default is Undefined, optional
        The VPC network from which the Cloud SQL
        instance is accessible for private IP. For example, projects/myProject/global/networks/default.
        Specifying a network enables private IP.
        At least ipv4_enabled must be enabled or a private_network must be configured.
        This setting can be updated, but it cannot be removed after it is set.
    pscConfig : [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0PscConfigItems0], default is Undefined, optional
        psc config
    requireSsl : bool, default is Undefined, optional
        Whether SSL connections over IP are enforced or not. To change this field, also set the corresponding value in ssl_mode.
    sslMode : str, default is Undefined, optional
        Specify how SSL connection should be enforced in DB connections. This field provides more SSL enforcment options compared to require_ssl. To change this field, also set the correspoding value in require_ssl.
    """


    allocatedIpRange?: str

    authorizedNetworks?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0]

    enablePrivatePathForGoogleCloudServices?: bool

    ipv4Enabled?: bool

    privateNetwork?: str

    pscConfig?: [SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0PscConfigItems0]

    requireSsl?: bool

    sslMode?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0AuthorizedNetworksItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 IP configuration items0 authorized networks items0

    Attributes
    ----------
    expirationTime : str, default is Undefined, optional
        The RFC 3339
        formatted date time string indicating when this whitelist expires.
    name : str, default is Undefined, optional
        A name for this whitelist entry.
    value : str, default is Undefined, optional
        A CIDR notation IPv4 or IPv6 address that is allowed to
        access this instance. Must be set even if other two attributes are not for
        the whitelist to become active.
    """


    expirationTime?: str

    name?: str

    value?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0IPConfigurationItems0PscConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 IP configuration items0 psc config items0

    Attributes
    ----------
    allowedConsumerProjects : [str], default is Undefined, optional
        List of consumer projects that are allow-listed for PSC connections to this instance. This instance can be connected to with PSC from any network in these projects. Each consumer project in this list may be represented by a project number (numeric) or by a project id (alphanumeric).
    pscEnabled : bool, default is Undefined, optional
        Whether PSC connectivity is enabled for this instance.
    """


    allowedConsumerProjects?: [str]

    pscEnabled?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0InsightsConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 insights config items0

    Attributes
    ----------
    queryInsightsEnabled : bool, default is Undefined, optional
        True if Query Insights feature is enabled.
    queryPlansPerMinute : float, default is Undefined, optional
        Number of query execution plans captured by Insights per minute for all queries combined. Between 0 and 20. Default to 5.
    queryStringLength : float, default is Undefined, optional
        Maximum query length stored in bytes. Between 256 and 4500. Default to 1024. Higher query lengths are more useful for analytical queries, but they also require more memory. Changing the query length requires you to restart the instance. You can still add tags to queries that exceed the length limit.
    recordApplicationTags : bool, default is Undefined, optional
        True if Query Insights will record application tags from query when enabled.
    recordClientAddress : bool, default is Undefined, optional
        True if Query Insights will record client address when enabled.
    """


    queryInsightsEnabled?: bool

    queryPlansPerMinute?: float

    queryStringLength?: float

    recordApplicationTags?: bool

    recordClientAddress?: bool


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0LocationPreferenceItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 location preference items0

    Attributes
    ----------
    followGaeApplication : str, default is Undefined, optional
        A GAE application whose zone to remain
        in. Must be in the same region as this instance.
    secondaryZone : str, default is Undefined, optional
        The preferred Compute Engine zone for the secondary/failover.
    zone : str, default is Undefined, optional
        The preferred compute engine
        zone.
    """


    followGaeApplication?: str

    secondaryZone?: str

    zone?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0MaintenanceWindowItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 maintenance window items0

    Attributes
    ----------
    day : float, default is Undefined, optional
        Day of week (1-7), starting on Monday
    hour : float, default is Undefined, optional
        Hour of day (0-23), ignored if day not set
    updateTrack : str, default is Undefined, optional
        Receive updates earlier (canary) or later
        (stable)
    """


    day?: float

    hour?: float

    updateTrack?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0PasswordValidationPolicyItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 password validation policy items0

    Attributes
    ----------
    complexity : str, default is Undefined, optional
        Checks if the password is a combination of lowercase, uppercase, numeric, and non-alphanumeric characters.
    disallowUsernameSubstring : bool, default is Undefined, optional
        Prevents the use of the username in the password.
    enablePasswordPolicy : bool, default is Undefined, optional
        Enables or disable the password validation policy.
    minLength : float, default is Undefined, optional
        Specifies the minimum number of characters that the password must have.
    passwordChangeInterval : str, default is Undefined, optional
        Specifies the minimum duration after which you can change the password.
    reuseInterval : float, default is Undefined, optional
        Specifies the number of previous passwords that you can't reuse.
    """


    complexity?: str

    disallowUsernameSubstring?: bool

    enablePasswordPolicy?: bool

    minLength?: float

    passwordChangeInterval?: str

    reuseInterval?: float


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusAtProviderSettingsItems0SQLServerAuditConfigItems0:
    """
    SQL gcp upbound io v1beta1 database instance status at provider settings items0 SQL server audit config items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        The name of the destination bucket (e.g., gs://mybucket).
    retentionInterval : str, default is Undefined, optional
        How long to keep generated audit files. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    uploadInterval : str, default is Undefined, optional
        How often to upload generated audit files. A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    """


    bucket?: str

    retentionInterval?: str

    uploadInterval?: str


schema SQLGcpUpboundIoV1beta1DatabaseInstanceStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


