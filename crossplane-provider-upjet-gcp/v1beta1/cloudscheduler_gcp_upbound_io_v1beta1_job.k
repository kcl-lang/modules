"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Job:
    """
    Job is the Schema for the Jobs API. A scheduled job that can publish a PubSub message or an HTTP request every X interval of time, using a crontab format string.

    Attributes
    ----------
    apiVersion : str, default is "cloudscheduler.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Job", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : CloudschedulerGcpUpboundIoV1beta1JobSpec, default is Undefined, required
        spec
    status : CloudschedulerGcpUpboundIoV1beta1JobStatus, default is Undefined, optional
        status
    """


    apiVersion: "cloudscheduler.gcp.upbound.io/v1beta1" = "cloudscheduler.gcp.upbound.io/v1beta1"

    kind: "Job" = "Job"

    metadata?: v1.ObjectMeta

    spec: CloudschedulerGcpUpboundIoV1beta1JobSpec

    status?: CloudschedulerGcpUpboundIoV1beta1JobStatus


schema CloudschedulerGcpUpboundIoV1beta1JobSpec:
    """
    JobSpec defines the desired state of Job

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : CloudschedulerGcpUpboundIoV1beta1JobSpecForProvider, default is Undefined, required
        for provider
    initProvider : CloudschedulerGcpUpboundIoV1beta1JobSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : CloudschedulerGcpUpboundIoV1beta1JobSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : CloudschedulerGcpUpboundIoV1beta1JobSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: CloudschedulerGcpUpboundIoV1beta1JobSpecForProvider

    initProvider?: CloudschedulerGcpUpboundIoV1beta1JobSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: CloudschedulerGcpUpboundIoV1beta1JobSpecProviderConfigRef

    publishConnectionDetailsTo?: CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: CloudschedulerGcpUpboundIoV1beta1JobSpecWriteConnectionSecretToRef


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProvider:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider

    Attributes
    ----------
    appEngineHttpTarget : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderAppEngineHTTPTargetItems0], default is Undefined, optional
        App Engine HTTP target.
        If the job providers a App Engine HTTP target the cron will
        send a request to the service instance
        Structure is documented below.
    attemptDeadline : str, default is Undefined, optional
        The deadline for job attempts. If the request handler does not respond by this deadline then the request is
        cancelled and the attempt is marked as a DEADLINE_EXCEEDED failure. The failed attempt can be viewed in
        execution logs. Cloud Scheduler will retry the job according to the RetryConfig.
        The allowed duration for this deadline is:
    description : str, default is Undefined, optional
        A human-readable description for the job.
        This string must not contain more than 500 characters.
    httpTarget : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0], default is Undefined, optional
        HTTP target.
        If the job providers a http_target the cron will
        send a request to the targeted url
        Structure is documented below.
    paused : bool, default is Undefined, optional
        Sets the job to a paused state. Jobs default to being enabled when this property is not set.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    pubsubTarget : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0], default is Undefined, optional
        Pub/Sub target
        If the job providers a Pub/Sub target the cron will publish
        a message to the provided topic
        Structure is documented below.
    region : str, default is Undefined, required
        Region where the scheduler job resides.
    retryConfig : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderRetryConfigItems0], default is Undefined, optional
        By default, if a job does not complete successfully,
        meaning that an acknowledgement is not received from the handler,
        then it will be retried with exponential backoff according to the settings
        Structure is documented below.
    schedule : str, default is Undefined, optional
        Describes the schedule on which the job will be executed.
    timeZone : str, default is Undefined, optional
        Specifies the time zone to be used in interpreting schedule.
        The value of this field must be a time zone name from the tz database.
    """


    appEngineHttpTarget?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderAppEngineHTTPTargetItems0]

    attemptDeadline?: str

    description?: str

    httpTarget?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0]

    paused?: bool

    project?: str

    pubsubTarget?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0]

    region: str

    retryConfig?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderRetryConfigItems0]

    schedule?: str

    timeZone?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderAppEngineHTTPTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider app engine HTTP target items0

    Attributes
    ----------
    appEngineRouting : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0], default is Undefined, optional
        App Engine Routing setting for the job.
        Structure is documented below.
    body : str, default is Undefined, optional
        HTTP request body.
        A request body is allowed only if the HTTP method is POST or PUT.
        It will result in invalid argument error to set a body on a job with an incompatible HttpMethod.
        A base64-encoded string.
    headers : {str:str}, default is Undefined, optional
        HTTP request headers.
        This map contains the header field names and values.
        Headers can be set when the job is created.
    httpMethod : str, default is Undefined, optional
        Which HTTP method to use for the request.
    relativeUri : str, default is Undefined, optional
        The relative URI.
        The relative URL must begin with "/" and must be a valid HTTP relative URL.
        It can contain a path, query string arguments, and # fragments.
        If the relative URL is empty, then the root path "/" will be used.
        No spaces are allowed, and the maximum length allowed is 2083 characters
    """


    appEngineRouting?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0]

    body?: str

    headers?: {str:str}

    httpMethod?: str

    relativeUri?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider app engine HTTP target items0 app engine routing items0

    Attributes
    ----------
    instance : str, default is Undefined, optional
        App instance.
        By default, the job is sent to an instance which is available when the job is attempted.
    service : str, default is Undefined, optional
        App service.
        By default, the job is sent to the service which is the default service when the job is attempted.
    version : str, default is Undefined, optional
        App version.
        By default, the job is sent to the version which is the default version when the job is attempted.
    """


    instance?: str

    service?: str

    version?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider HTTP target items0

    Attributes
    ----------
    body : str, default is Undefined, optional
        HTTP request body.
        A request body is allowed only if the HTTP method is POST, PUT, or PATCH.
        It is an error to set body on a job with an incompatible HttpMethod.
        A base64-encoded string.
    headers : {str:str}, default is Undefined, optional
        This map contains the header field names and values.
        Repeated headers are not supported, but a header value can contain commas.
    httpMethod : str, default is Undefined, optional
        Which HTTP method to use for the request.
    oauthToken : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0OauthTokenItems0], default is Undefined, optional
        Contains information needed for generating an OAuth token.
        This type of authorization should be used when sending requests to a GCP endpoint.
        Structure is documented below.
    oidcToken : [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0OidcTokenItems0], default is Undefined, optional
        Contains information needed for generating an OpenID Connect token.
        This type of authorization should be used when sending requests to third party endpoints or Cloud Run.
        Structure is documented below.
    uri : str, default is Undefined, optional
        The full URI path that the request will be sent to.
    """


    body?: str

    headers?: {str:str}

    httpMethod?: str

    oauthToken?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0OauthTokenItems0]

    oidcToken?: [CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0OidcTokenItems0]

    uri?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0OauthTokenItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider HTTP target items0 oauth token items0

    Attributes
    ----------
    scope : str, default is Undefined, optional
        OAuth scope to be used for generating OAuth access token. If not specified,
        "https://www.googleapis.com/auth/cloud-platform" will be used.
    serviceAccountEmail : str, default is Undefined, optional
        Service account email to be used for generating OAuth token.
        The service account must be within the same project as the job.
    """


    scope?: str

    serviceAccountEmail?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderHTTPTargetItems0OidcTokenItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider HTTP target items0 oidc token items0

    Attributes
    ----------
    audience : str, default is Undefined, optional
        Audience to be used when generating OIDC token. If not specified,
        the URI specified in target will be used.
    serviceAccountEmail : str, default is Undefined, optional
        Service account email to be used for generating OAuth token.
        The service account must be within the same project as the job.
    """


    audience?: str

    serviceAccountEmail?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider pubsub target items0

    Attributes
    ----------
    attributes : {str:str}, default is Undefined, optional
        Attributes for PubsubMessage.
        Pubsub message must contain either non-empty data, or at least one attribute.
    data : str, default is Undefined, optional
        The message payload for PubsubMessage.
        Pubsub message must contain either non-empty data, or at least one attribute.
        A base64-encoded string.
    topicName : str, default is Undefined, optional
        The full resource name for the Cloud Pub/Sub topic to which
        messages will be published when a job is delivered. ~>NOTE:
        The topic name must be in the same format as required by PubSub's
        PublishRequest.name, e.g. projects/my-project/topics/my-topic.
    topicNameRef : CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameRef, default is Undefined, optional
        topic name ref
    topicNameSelector : CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameSelector, default is Undefined, optional
        topic name selector
    """


    attributes?: {str:str}

    data?: str

    topicName?: str

    topicNameRef?: CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameRef

    topicNameSelector?: CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameSelector


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameRef:
    """
    Reference to a Topic in pubsub to populate topicName.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameRefPolicy


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameSelector:
    """
    Selector for a Topic in pubsub to populate topicName.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameSelectorPolicy


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderPubsubTargetItems0TopicNameSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudschedulerGcpUpboundIoV1beta1JobSpecForProviderRetryConfigItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec for provider retry config items0

    Attributes
    ----------
    maxBackoffDuration : str, default is Undefined, optional
        The maximum amount of time to wait before retrying a job after it fails.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    maxDoublings : float, default is Undefined, optional
        The time between retries will double maxDoublings times.
        A job's retry interval starts at minBackoffDuration,
        then doubles maxDoublings times, then increases linearly,
        and finally retries retries at intervals of maxBackoffDuration up to retryCount times.
    maxRetryDuration : str, default is Undefined, optional
        The time limit for retrying a failed job, measured from time when an execution was first attempted.
        If specified with retryCount, the job will be retried until both limits are reached.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    minBackoffDuration : str, default is Undefined, optional
        The minimum amount of time to wait before retrying a job after it fails.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    retryCount : float, default is Undefined, optional
        The number of attempts that the system will make to run a
        job using the exponential backoff procedure described by maxDoublings.
        Values greater than 5 and negative values are not allowed.
    """


    maxBackoffDuration?: str

    maxDoublings?: float

    maxRetryDuration?: str

    minBackoffDuration?: str

    retryCount?: float


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    appEngineHttpTarget : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderAppEngineHTTPTargetItems0], default is Undefined, optional
        App Engine HTTP target.
        If the job providers a App Engine HTTP target the cron will
        send a request to the service instance
        Structure is documented below.
    attemptDeadline : str, default is Undefined, optional
        The deadline for job attempts. If the request handler does not respond by this deadline then the request is
        cancelled and the attempt is marked as a DEADLINE_EXCEEDED failure. The failed attempt can be viewed in
        execution logs. Cloud Scheduler will retry the job according to the RetryConfig.
        The allowed duration for this deadline is:
    description : str, default is Undefined, optional
        A human-readable description for the job.
        This string must not contain more than 500 characters.
    httpTarget : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0], default is Undefined, optional
        HTTP target.
        If the job providers a http_target the cron will
        send a request to the targeted url
        Structure is documented below.
    paused : bool, default is Undefined, optional
        Sets the job to a paused state. Jobs default to being enabled when this property is not set.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    pubsubTarget : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0], default is Undefined, optional
        Pub/Sub target
        If the job providers a Pub/Sub target the cron will publish
        a message to the provided topic
        Structure is documented below.
    retryConfig : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderRetryConfigItems0], default is Undefined, optional
        By default, if a job does not complete successfully,
        meaning that an acknowledgement is not received from the handler,
        then it will be retried with exponential backoff according to the settings
        Structure is documented below.
    schedule : str, default is Undefined, optional
        Describes the schedule on which the job will be executed.
    timeZone : str, default is Undefined, optional
        Specifies the time zone to be used in interpreting schedule.
        The value of this field must be a time zone name from the tz database.
    """


    appEngineHttpTarget?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderAppEngineHTTPTargetItems0]

    attemptDeadline?: str

    description?: str

    httpTarget?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0]

    paused?: bool

    project?: str

    pubsubTarget?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0]

    retryConfig?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderRetryConfigItems0]

    schedule?: str

    timeZone?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderAppEngineHTTPTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider app engine HTTP target items0

    Attributes
    ----------
    appEngineRouting : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0], default is Undefined, optional
        App Engine Routing setting for the job.
        Structure is documented below.
    body : str, default is Undefined, optional
        HTTP request body.
        A request body is allowed only if the HTTP method is POST or PUT.
        It will result in invalid argument error to set a body on a job with an incompatible HttpMethod.
        A base64-encoded string.
    headers : {str:str}, default is Undefined, optional
        HTTP request headers.
        This map contains the header field names and values.
        Headers can be set when the job is created.
    httpMethod : str, default is Undefined, optional
        Which HTTP method to use for the request.
    relativeUri : str, default is Undefined, optional
        The relative URI.
        The relative URL must begin with "/" and must be a valid HTTP relative URL.
        It can contain a path, query string arguments, and # fragments.
        If the relative URL is empty, then the root path "/" will be used.
        No spaces are allowed, and the maximum length allowed is 2083 characters
    """


    appEngineRouting?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0]

    body?: str

    headers?: {str:str}

    httpMethod?: str

    relativeUri?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider app engine HTTP target items0 app engine routing items0

    Attributes
    ----------
    instance : str, default is Undefined, optional
        App instance.
        By default, the job is sent to an instance which is available when the job is attempted.
    service : str, default is Undefined, optional
        App service.
        By default, the job is sent to the service which is the default service when the job is attempted.
    version : str, default is Undefined, optional
        App version.
        By default, the job is sent to the version which is the default version when the job is attempted.
    """


    instance?: str

    service?: str

    version?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider HTTP target items0

    Attributes
    ----------
    body : str, default is Undefined, optional
        HTTP request body.
        A request body is allowed only if the HTTP method is POST, PUT, or PATCH.
        It is an error to set body on a job with an incompatible HttpMethod.
        A base64-encoded string.
    headers : {str:str}, default is Undefined, optional
        This map contains the header field names and values.
        Repeated headers are not supported, but a header value can contain commas.
    httpMethod : str, default is Undefined, optional
        Which HTTP method to use for the request.
    oauthToken : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0OauthTokenItems0], default is Undefined, optional
        Contains information needed for generating an OAuth token.
        This type of authorization should be used when sending requests to a GCP endpoint.
        Structure is documented below.
    oidcToken : [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0OidcTokenItems0], default is Undefined, optional
        Contains information needed for generating an OpenID Connect token.
        This type of authorization should be used when sending requests to third party endpoints or Cloud Run.
        Structure is documented below.
    uri : str, default is Undefined, optional
        The full URI path that the request will be sent to.
    """


    body?: str

    headers?: {str:str}

    httpMethod?: str

    oauthToken?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0OauthTokenItems0]

    oidcToken?: [CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0OidcTokenItems0]

    uri?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0OauthTokenItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider HTTP target items0 oauth token items0

    Attributes
    ----------
    scope : str, default is Undefined, optional
        OAuth scope to be used for generating OAuth access token. If not specified,
        "https://www.googleapis.com/auth/cloud-platform" will be used.
    serviceAccountEmail : str, default is Undefined, optional
        Service account email to be used for generating OAuth token.
        The service account must be within the same project as the job.
    """


    scope?: str

    serviceAccountEmail?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderHTTPTargetItems0OidcTokenItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider HTTP target items0 oidc token items0

    Attributes
    ----------
    audience : str, default is Undefined, optional
        Audience to be used when generating OIDC token. If not specified,
        the URI specified in target will be used.
    serviceAccountEmail : str, default is Undefined, optional
        Service account email to be used for generating OAuth token.
        The service account must be within the same project as the job.
    """


    audience?: str

    serviceAccountEmail?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider pubsub target items0

    Attributes
    ----------
    attributes : {str:str}, default is Undefined, optional
        Attributes for PubsubMessage.
        Pubsub message must contain either non-empty data, or at least one attribute.
    data : str, default is Undefined, optional
        The message payload for PubsubMessage.
        Pubsub message must contain either non-empty data, or at least one attribute.
        A base64-encoded string.
    topicName : str, default is Undefined, optional
        The full resource name for the Cloud Pub/Sub topic to which
        messages will be published when a job is delivered. ~>NOTE:
        The topic name must be in the same format as required by PubSub's
        PublishRequest.name, e.g. projects/my-project/topics/my-topic.
    topicNameRef : CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameRef, default is Undefined, optional
        topic name ref
    topicNameSelector : CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameSelector, default is Undefined, optional
        topic name selector
    """


    attributes?: {str:str}

    data?: str

    topicName?: str

    topicNameRef?: CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameRef

    topicNameSelector?: CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameSelector


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameRef:
    """
    Reference to a Topic in pubsub to populate topicName.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameRefPolicy


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameSelector:
    """
    Selector for a Topic in pubsub to populate topicName.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    policy : CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    policy?: CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameSelectorPolicy


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderPubsubTargetItems0TopicNameSelectorPolicy:
    """
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudschedulerGcpUpboundIoV1beta1JobSpecInitProviderRetryConfigItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job spec init provider retry config items0

    Attributes
    ----------
    maxBackoffDuration : str, default is Undefined, optional
        The maximum amount of time to wait before retrying a job after it fails.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    maxDoublings : float, default is Undefined, optional
        The time between retries will double maxDoublings times.
        A job's retry interval starts at minBackoffDuration,
        then doubles maxDoublings times, then increases linearly,
        and finally retries retries at intervals of maxBackoffDuration up to retryCount times.
    maxRetryDuration : str, default is Undefined, optional
        The time limit for retrying a failed job, measured from time when an execution was first attempted.
        If specified with retryCount, the job will be retried until both limits are reached.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    minBackoffDuration : str, default is Undefined, optional
        The minimum amount of time to wait before retrying a job after it fails.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    retryCount : float, default is Undefined, optional
        The number of attempts that the system will make to run a
        job using the exponential backoff procedure described by maxDoublings.
        Values greater than 5 and negative values are not allowed.
    """


    maxBackoffDuration?: str

    maxDoublings?: float

    maxRetryDuration?: str

    minBackoffDuration?: str

    retryCount?: float


schema CloudschedulerGcpUpboundIoV1beta1JobSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : CloudschedulerGcpUpboundIoV1beta1JobSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: CloudschedulerGcpUpboundIoV1beta1JobSpecProviderConfigRefPolicy


schema CloudschedulerGcpUpboundIoV1beta1JobSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRef

    metadata?: CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToMetadata

    name: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRefPolicy


schema CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudschedulerGcpUpboundIoV1beta1JobSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema CloudschedulerGcpUpboundIoV1beta1JobSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatus:
    """
    JobStatus defines the observed state of Job.

    Attributes
    ----------
    atProvider : CloudschedulerGcpUpboundIoV1beta1JobStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [CloudschedulerGcpUpboundIoV1beta1JobStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: CloudschedulerGcpUpboundIoV1beta1JobStatusAtProvider

    conditions?: [CloudschedulerGcpUpboundIoV1beta1JobStatusConditionsItems0]


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProvider:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider

    Attributes
    ----------
    appEngineHttpTarget : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderAppEngineHTTPTargetItems0], default is Undefined, optional
        App Engine HTTP target.
        If the job providers a App Engine HTTP target the cron will
        send a request to the service instance
        Structure is documented below.
    attemptDeadline : str, default is Undefined, optional
        The deadline for job attempts. If the request handler does not respond by this deadline then the request is
        cancelled and the attempt is marked as a DEADLINE_EXCEEDED failure. The failed attempt can be viewed in
        execution logs. Cloud Scheduler will retry the job according to the RetryConfig.
        The allowed duration for this deadline is:
    description : str, default is Undefined, optional
        A human-readable description for the job.
        This string must not contain more than 500 characters.
    httpTarget : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0], default is Undefined, optional
        HTTP target.
        If the job providers a http_target the cron will
        send a request to the targeted url
        Structure is documented below.
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/locations/{{region}}/jobs/{{name}}
    paused : bool, default is Undefined, optional
        Sets the job to a paused state. Jobs default to being enabled when this property is not set.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    pubsubTarget : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderPubsubTargetItems0], default is Undefined, optional
        Pub/Sub target
        If the job providers a Pub/Sub target the cron will publish
        a message to the provided topic
        Structure is documented below.
    region : str, default is Undefined, optional
        Region where the scheduler job resides.
    retryConfig : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderRetryConfigItems0], default is Undefined, optional
        By default, if a job does not complete successfully,
        meaning that an acknowledgement is not received from the handler,
        then it will be retried with exponential backoff according to the settings
        Structure is documented below.
    schedule : str, default is Undefined, optional
        Describes the schedule on which the job will be executed.
    state : str, default is Undefined, optional
        State of the job.
    timeZone : str, default is Undefined, optional
        Specifies the time zone to be used in interpreting schedule.
        The value of this field must be a time zone name from the tz database.
    """


    appEngineHttpTarget?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderAppEngineHTTPTargetItems0]

    attemptDeadline?: str

    description?: str

    httpTarget?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0]

    id?: str

    paused?: bool

    project?: str

    pubsubTarget?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderPubsubTargetItems0]

    region?: str

    retryConfig?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderRetryConfigItems0]

    schedule?: str

    state?: str

    timeZone?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderAppEngineHTTPTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider app engine HTTP target items0

    Attributes
    ----------
    appEngineRouting : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0], default is Undefined, optional
        App Engine Routing setting for the job.
        Structure is documented below.
    body : str, default is Undefined, optional
        HTTP request body.
        A request body is allowed only if the HTTP method is POST or PUT.
        It will result in invalid argument error to set a body on a job with an incompatible HttpMethod.
        A base64-encoded string.
    headers : {str:str}, default is Undefined, optional
        HTTP request headers.
        This map contains the header field names and values.
        Headers can be set when the job is created.
    httpMethod : str, default is Undefined, optional
        Which HTTP method to use for the request.
    relativeUri : str, default is Undefined, optional
        The relative URI.
        The relative URL must begin with "/" and must be a valid HTTP relative URL.
        It can contain a path, query string arguments, and # fragments.
        If the relative URL is empty, then the root path "/" will be used.
        No spaces are allowed, and the maximum length allowed is 2083 characters
    """


    appEngineRouting?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0]

    body?: str

    headers?: {str:str}

    httpMethod?: str

    relativeUri?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderAppEngineHTTPTargetItems0AppEngineRoutingItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider app engine HTTP target items0 app engine routing items0

    Attributes
    ----------
    instance : str, default is Undefined, optional
        App instance.
        By default, the job is sent to an instance which is available when the job is attempted.
    service : str, default is Undefined, optional
        App service.
        By default, the job is sent to the service which is the default service when the job is attempted.
    version : str, default is Undefined, optional
        App version.
        By default, the job is sent to the version which is the default version when the job is attempted.
    """


    instance?: str

    service?: str

    version?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider HTTP target items0

    Attributes
    ----------
    body : str, default is Undefined, optional
        HTTP request body.
        A request body is allowed only if the HTTP method is POST, PUT, or PATCH.
        It is an error to set body on a job with an incompatible HttpMethod.
        A base64-encoded string.
    headers : {str:str}, default is Undefined, optional
        This map contains the header field names and values.
        Repeated headers are not supported, but a header value can contain commas.
    httpMethod : str, default is Undefined, optional
        Which HTTP method to use for the request.
    oauthToken : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0OauthTokenItems0], default is Undefined, optional
        Contains information needed for generating an OAuth token.
        This type of authorization should be used when sending requests to a GCP endpoint.
        Structure is documented below.
    oidcToken : [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0OidcTokenItems0], default is Undefined, optional
        Contains information needed for generating an OpenID Connect token.
        This type of authorization should be used when sending requests to third party endpoints or Cloud Run.
        Structure is documented below.
    uri : str, default is Undefined, optional
        The full URI path that the request will be sent to.
    """


    body?: str

    headers?: {str:str}

    httpMethod?: str

    oauthToken?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0OauthTokenItems0]

    oidcToken?: [CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0OidcTokenItems0]

    uri?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0OauthTokenItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider HTTP target items0 oauth token items0

    Attributes
    ----------
    scope : str, default is Undefined, optional
        OAuth scope to be used for generating OAuth access token. If not specified,
        "https://www.googleapis.com/auth/cloud-platform" will be used.
    serviceAccountEmail : str, default is Undefined, optional
        Service account email to be used for generating OAuth token.
        The service account must be within the same project as the job.
    """


    scope?: str

    serviceAccountEmail?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderHTTPTargetItems0OidcTokenItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider HTTP target items0 oidc token items0

    Attributes
    ----------
    audience : str, default is Undefined, optional
        Audience to be used when generating OIDC token. If not specified,
        the URI specified in target will be used.
    serviceAccountEmail : str, default is Undefined, optional
        Service account email to be used for generating OAuth token.
        The service account must be within the same project as the job.
    """


    audience?: str

    serviceAccountEmail?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderPubsubTargetItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider pubsub target items0

    Attributes
    ----------
    attributes : {str:str}, default is Undefined, optional
        Attributes for PubsubMessage.
        Pubsub message must contain either non-empty data, or at least one attribute.
    data : str, default is Undefined, optional
        The message payload for PubsubMessage.
        Pubsub message must contain either non-empty data, or at least one attribute.
        A base64-encoded string.
    topicName : str, default is Undefined, optional
        The full resource name for the Cloud Pub/Sub topic to which
        messages will be published when a job is delivered. ~>NOTE:
        The topic name must be in the same format as required by PubSub's
        PublishRequest.name, e.g. projects/my-project/topics/my-topic.
    """


    attributes?: {str:str}

    data?: str

    topicName?: str


schema CloudschedulerGcpUpboundIoV1beta1JobStatusAtProviderRetryConfigItems0:
    """
    cloudscheduler gcp upbound io v1beta1 job status at provider retry config items0

    Attributes
    ----------
    maxBackoffDuration : str, default is Undefined, optional
        The maximum amount of time to wait before retrying a job after it fails.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    maxDoublings : float, default is Undefined, optional
        The time between retries will double maxDoublings times.
        A job's retry interval starts at minBackoffDuration,
        then doubles maxDoublings times, then increases linearly,
        and finally retries retries at intervals of maxBackoffDuration up to retryCount times.
    maxRetryDuration : str, default is Undefined, optional
        The time limit for retrying a failed job, measured from time when an execution was first attempted.
        If specified with retryCount, the job will be retried until both limits are reached.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    minBackoffDuration : str, default is Undefined, optional
        The minimum amount of time to wait before retrying a job after it fails.
        A duration in seconds with up to nine fractional digits, terminated by 's'.
    retryCount : float, default is Undefined, optional
        The number of attempts that the system will make to run a
        job using the exponential backoff procedure described by maxDoublings.
        Values greater than 5 and negative values are not allowed.
    """


    maxBackoffDuration?: str

    maxDoublings?: float

    maxRetryDuration?: str

    minBackoffDuration?: str

    retryCount?: float


schema CloudschedulerGcpUpboundIoV1beta1JobStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


