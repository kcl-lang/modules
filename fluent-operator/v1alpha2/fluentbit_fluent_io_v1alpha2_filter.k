"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1


schema Filter:
    """
    Filter is the Schema for namespace level filter API

    Attributes
    ----------
    apiVersion : str, default is "fluentbit.fluent.io/v1alpha2", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Filter", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : FluentbitFluentIoV1alpha2FilterSpec, default is Undefined, optional
        spec
    """


    apiVersion: "fluentbit.fluent.io/v1alpha2" = "fluentbit.fluent.io/v1alpha2"

    kind: "Filter" = "Filter"

    metadata?: v1.ObjectMeta

    spec?: FluentbitFluentIoV1alpha2FilterSpec


schema FluentbitFluentIoV1alpha2FilterSpec:
    """
    FilterSpec defines the desired state of ClusterFilter

    Attributes
    ----------
    filters : [FluentbitFluentIoV1alpha2FilterSpecFiltersItems0], default is Undefined, optional
        A set of filter plugins in order.
    logLevel : str, default is Undefined, optional
        log level
    match : str, default is Undefined, optional
        A pattern to match against the tags of incoming records. It's case-sensitive and support the star (*) character as a wildcard.
    matchRegex : str, default is Undefined, optional
        A regular expression to match against the tags of incoming records. Use this option if you want to use the full regex syntax.
    """


    filters?: [FluentbitFluentIoV1alpha2FilterSpecFiltersItems0]

    logLevel?: "off" | "error" | "warning" | "info" | "debug" | "trace"

    match?: str

    matchRegex?: str


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0:
    """
    fluentbit fluent io v1alpha2 filter spec filters items0

    Attributes
    ----------
    aws : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Aws, default is Undefined, optional
        aws
    customPlugin : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0CustomPlugin, default is Undefined, optional
        custom plugin
    grep : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Grep, default is Undefined, optional
        grep
    kubernetes : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Kubernetes, default is Undefined, optional
        kubernetes
    lua : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Lua, default is Undefined, optional
        lua
    modify : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Modify, default is Undefined, optional
        modify
    multiline : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Multiline, default is Undefined, optional
        multiline
    nest : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Nest, default is Undefined, optional
        nest
    parser : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Parser, default is Undefined, optional
        parser
    recordModifier : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0RecordModifier, default is Undefined, optional
        record modifier
    rewriteTag : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0RewriteTag, default is Undefined, optional
        rewrite tag
    throttle : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Throttle, default is Undefined, optional
        throttle
    """


    aws?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Aws

    customPlugin?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0CustomPlugin

    grep?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Grep

    kubernetes?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Kubernetes

    lua?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Lua

    modify?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Modify

    multiline?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Multiline

    nest?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Nest

    parser?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Parser

    recordModifier?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0RecordModifier

    rewriteTag?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0RewriteTag

    throttle?: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Throttle


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Aws:
    """
    Aws defines a Aws configuration.

    Attributes
    ----------
    accountID : bool, default is Undefined, optional
        The account ID for current EC2 instance.Default is false.
    alias : str, default is Undefined, optional
        Alias for the plugin
    amiID : bool, default is Undefined, optional
        The EC2 instance image id.Default is false.
    az : bool, default is Undefined, optional
        The availability zone; for example, "us-east-1a". Default is true.
    ec2InstanceID : bool, default is Undefined, optional
        The EC2 instance ID.Default is true.
    ec2InstanceType : bool, default is Undefined, optional
        The EC2 instance type.Default is false.
    hostName : bool, default is Undefined, optional
        The hostname for current EC2 instance.Default is false.
    imdsVersion : str, default is Undefined, optional
        Specify which version of the instance metadata service to use. Valid values are 'v1' or 'v2'.
    privateIP : bool, default is Undefined, optional
        The EC2 instance private ip.Default is false.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    vpcID : bool, default is Undefined, optional
        The VPC ID for current EC2 instance.Default is false.
    """


    accountID?: bool

    alias?: str

    amiID?: bool

    az?: bool

    ec2InstanceID?: bool

    ec2InstanceType?: bool

    hostName?: bool

    imdsVersion?: "v1" | "v2"

    privateIP?: bool

    retryLimit?: str

    vpcID?: bool


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0CustomPlugin:
    """
    CustomPlugin defines a Custom plugin configuration.

    Attributes
    ----------
    config : str, default is Undefined, optional
        config
    """


    config?: str


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Grep:
    """
    Grep defines Grep Filter configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    exclude : str, default is Undefined, optional
        Exclude records which field matches the regular expression. Value Format: FIELD REGEX
    regex : str, default is Undefined, optional
        Keep records which field matches the regular expression. Value Format: FIELD REGEX
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    """


    alias?: str

    exclude?: str

    regex?: str

    retryLimit?: str


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Kubernetes:
    """
    Kubernetes defines Kubernetes Filter configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    annotations : bool, default is Undefined, optional
        Include Kubernetes resource annotations in the extra metadata.
    bufferSize : str, default is Undefined, optional
        Set the buffer size for HTTP client when reading responses from Kubernetes API server.
    cacheUseDockerId : bool, default is Undefined, optional
        When enabled, metadata will be fetched from K8s when docker_id is changed.
    dnsRetries : int, default is Undefined, optional
        DNS lookup retries N times until the network start working
    dnsWaitTime : int, default is Undefined, optional
        DNS lookup interval between network status checks
    dummyMeta : bool, default is Undefined, optional
        If set, use dummy-meta data (for test/dev purposes)
    k8sLoggingExclude : bool, default is Undefined, optional
        Allow Kubernetes Pods to exclude their logs from the log processor (read more about it in Kubernetes Annotations section).
    k8sLoggingParser : bool, default is Undefined, optional
        Allow Kubernetes Pods to suggest a pre-defined Parser (read more about it in Kubernetes Annotations section)
    keepLog : bool, default is Undefined, optional
        When Keep_Log is disabled, the log field is removed from the incoming message once it has been successfully merged (Merge_Log must be enabled as well).
    kubeCAFile : str, default is Undefined, optional
        CA certificate file
    kubeCAPath : str, default is Undefined, optional
        Absolute path to scan for certificate files
    kubeMetaCacheTTL : str, default is Undefined, optional
        configurable TTL for K8s cached metadata. By default, it is set to 0 which means TTL for cache entries is disabled and cache entries are evicted at random when capacity is reached. In order to enable this option, you should set the number to a time interval. For example, set this value to 60 or 60s and cache entries which have been created more than 60s will be evicted.
    kubeMetaPreloadCacheDir : str, default is Undefined, optional
        If set, Kubernetes meta-data can be cached/pre-loaded from files in JSON format in this directory, named as namespace-pod.meta
    kubeTagPrefix : str, default is Undefined, optional
        When the source records comes from Tail input plugin, this option allows to specify what's the prefix used in Tail configuration.
    kubeTokenFile : str, default is Undefined, optional
        Token file
    kubeTokenTTL : str, default is Undefined, optional
        configurable 'time to live' for the K8s token. By default, it is set to 600 seconds. After this time, the token is reloaded from Kube_Token_File or the Kube_Token_Command.
    kubeURL : str, default is Undefined, optional
        API Server end-point
    kubeletHost : str, default is Undefined, optional
        kubelet host using for HTTP request, this only works when Use_Kubelet set to On.
    kubeletPort : int, default is Undefined, optional
        kubelet port using for HTTP request, this only works when useKubelet is set to On.
    labels : bool, default is Undefined, optional
        Include Kubernetes resource labels in the extra metadata.
    mergeLog : bool, default is Undefined, optional
        When enabled, it checks if the log field content is a JSON string map, if so, it append the map fields as part of the log structure.
    mergeLogKey : str, default is Undefined, optional
        When Merge_Log is enabled, the filter tries to assume the log field from the incoming message is a JSON string message and make a structured representation of it at the same level of the log field in the map. Now if Merge_Log_Key is set (a string name), all the new structured fields taken from the original log content are inserted under the new key.
    mergeLogTrim : bool, default is Undefined, optional
        When Merge_Log is enabled, trim (remove possible \n or \r) field values.
    mergeParser : str, default is Undefined, optional
        Optional parser name to specify how to parse the data contained in the log key. Recommended use is for developers or testing only.
    regexParser : str, default is Undefined, optional
        Set an alternative Parser to process record Tag and extract pod_name, namespace_name, container_name and docker_id. The parser must be registered in a parsers file (refer to parser filter-kube-test as an example).
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    tlsDebug : int, default is Undefined, optional
        Debug level between 0 (nothing) and 4 (every detail).
    tlsVerify : bool, default is Undefined, optional
        When enabled, turns on certificate validation when connecting to the Kubernetes API server.
    useJournal : bool, default is Undefined, optional
        When enabled, the filter reads logs coming in Journald format.
    useKubelet : bool, default is Undefined, optional
        This is an optional feature flag to get metadata information from kubelet instead of calling Kube Server API to enhance the log. This could mitigate the Kube API heavy traffic issue for large cluster.
    """


    alias?: str

    annotations?: bool

    bufferSize?: str

    cacheUseDockerId?: bool

    dnsRetries?: int

    dnsWaitTime?: int

    dummyMeta?: bool

    k8sLoggingExclude?: bool

    k8sLoggingParser?: bool

    keepLog?: bool

    kubeCAFile?: str

    kubeCAPath?: str

    kubeMetaCacheTTL?: str

    kubeMetaPreloadCacheDir?: str

    kubeTagPrefix?: str

    kubeTokenFile?: str

    kubeTokenTTL?: str

    kubeURL?: str

    kubeletHost?: str

    kubeletPort?: int

    labels?: bool

    mergeLog?: bool

    mergeLogKey?: str

    mergeLogTrim?: bool

    mergeParser?: str

    regexParser?: str

    retryLimit?: str

    tlsDebug?: int

    tlsVerify?: bool

    useJournal?: bool

    useKubelet?: bool


    check:
        regex.match(str(bufferSize), r"^\d+(k|K|KB|kb|m|M|MB|mb|g|G|GB|gb)?$") if bufferSize
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Lua:
    """
    Lua defines Lua Filter configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    call : str, default is Undefined, required
        Lua function name that will be triggered to do filtering. It's assumed that the function is declared inside the Script defined above.
    protectedMode : bool, default is Undefined, optional
        If enabled, Lua script will be executed in protected mode. It prevents to crash when invalid Lua script is executed. Default is true.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    script : FluentbitFluentIoV1alpha2FilterSpecFiltersItems0LuaScript, default is Undefined, required
        script
    timeAsTable : bool, default is Undefined, optional
        By default when the Lua script is invoked, the record timestamp is passed as a Floating number which might lead to loss precision when the data is converted back. If you desire timestamp precision enabling this option will pass the timestamp as a Lua table with keys sec for seconds since epoch and nsec for nanoseconds.
    typeIntKey : [str], default is Undefined, optional
        If these keys are matched, the fields are converted to integer. If more than one key, delimit by space. Note that starting from Fluent Bit v1.6 integer data types are preserved and not converted to double as in previous versions.
    """


    alias?: str

    call: str

    protectedMode?: bool

    retryLimit?: str

    script: FluentbitFluentIoV1alpha2FilterSpecFiltersItems0LuaScript

    timeAsTable?: bool

    typeIntKey?: [str]


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0LuaScript:
    """
    Path to the Lua script that will be used.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Modify:
    """
    Modify defines Modify Filter configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    conditions : [FluentbitFluentIoV1alpha2FilterSpecFiltersItems0ModifyConditionsItems0], default is Undefined, optional
        All conditions have to be true for the rules to be applied.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    rules : [FluentbitFluentIoV1alpha2FilterSpecFiltersItems0ModifyRulesItems0], default is Undefined, optional
        Rules are applied in the order they appear, with each rule operating on the result of the previous rule.
    """


    alias?: str

    conditions?: [FluentbitFluentIoV1alpha2FilterSpecFiltersItems0ModifyConditionsItems0]

    retryLimit?: str

    rules?: [FluentbitFluentIoV1alpha2FilterSpecFiltersItems0ModifyRulesItems0]


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0ModifyConditionsItems0:
    """
    The plugin supports the following conditions

    Attributes
    ----------
    aKeyMatches : str, default is Undefined, optional
        Is true if a key matches regex KEY
    keyDoesNotExist : {str:str}, default is Undefined, optional
        Is true if KEY does not exist
    keyExists : str, default is Undefined, optional
        Is true if KEY exists
    keyValueDoesNotEqual : {str:str}, default is Undefined, optional
        Is true if KEY exists and its value is not VALUE
    keyValueDoesNotMatch : {str:str}, default is Undefined, optional
        Is true if key KEY exists and its value does not match VALUE
    keyValueEquals : {str:str}, default is Undefined, optional
        Is true if KEY exists and its value is VALUE
    keyValueMatches : {str:str}, default is Undefined, optional
        Is true if key KEY exists and its value matches VALUE
    matchingKeysDoNotHaveMatchingValues : {str:str}, default is Undefined, optional
        Is true if all keys matching KEY have values that do not match VALUE
    matchingKeysHaveMatchingValues : {str:str}, default is Undefined, optional
        Is true if all keys matching KEY have values that match VALUE
    noKeyMatches : str, default is Undefined, optional
        Is true if no key matches regex KEY
    """


    aKeyMatches?: str

    keyDoesNotExist?: {str:str}

    keyExists?: str

    keyValueDoesNotEqual?: {str:str}

    keyValueDoesNotMatch?: {str:str}

    keyValueEquals?: {str:str}

    keyValueMatches?: {str:str}

    matchingKeysDoNotHaveMatchingValues?: {str:str}

    matchingKeysHaveMatchingValues?: {str:str}

    noKeyMatches?: str


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0ModifyRulesItems0:
    """
    The plugin supports the following rules

    Attributes
    ----------
    add : {str:str}, default is Undefined, optional
        Add a key/value pair with key KEY and value VALUE if KEY does not exist
    copy : {str:str}, default is Undefined, optional
        Copy a key/value pair with key KEY to COPIED_KEY if KEY exists AND COPIED_KEY does not exist
    hardCopy : {str:str}, default is Undefined, optional
        Copy a key/value pair with key KEY to COPIED_KEY if KEY exists. If COPIED_KEY already exists, this field is overwritten
    hardRename : {str:str}, default is Undefined, optional
        Rename a key/value pair with key KEY to RENAMED_KEY if KEY exists. If RENAMED_KEY already exists, this field is overwritten
    remove : str, default is Undefined, optional
        Remove a key/value pair with key KEY if it exists
    removeRegex : str, default is Undefined, optional
        Remove all key/value pairs with key matching regexp KEY
    removeWildcard : str, default is Undefined, optional
        Remove all key/value pairs with key matching wildcard KEY
    rename : {str:str}, default is Undefined, optional
        Rename a key/value pair with key KEY to RENAMED_KEY if KEY exists AND RENAMED_KEY does not exist
    set : {str:str}, default is Undefined, optional
        Add a key/value pair with key KEY and value VALUE. If KEY already exists, this field is overwritten
    """


    add?: {str:str}

    copy?: {str:str}

    hardCopy?: {str:str}

    hardRename?: {str:str}

    remove?: str

    removeRegex?: str

    removeWildcard?: str

    rename?: {str:str}

    set?: {str:str}


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Multiline:
    """
    Multiline defines a Multiline configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    keyContent : str, default is Undefined, optional
        Key name that holds the content to process. Note that a Multiline Parser definition can already specify the key_content to use, but this option allows to overwrite that value for the purpose of the filter.
    parser : str, default is Undefined, required
        Specify one or multiple Multiline Parsing definitions to apply to the content. You can specify multiple multiline parsers to detect different formats by separating them with a comma.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    """


    alias?: str

    keyContent?: str

    parser: str

    retryLimit?: str


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Nest:
    """
    Nest defines Nest Filter configuration.

    Attributes
    ----------
    addPrefix : str, default is Undefined, optional
        Prefix affected keys with this string
    alias : str, default is Undefined, optional
        Alias for the plugin
    nestUnder : str, default is Undefined, optional
        Nest records matching the Wildcard under this key
    nestedUnder : str, default is Undefined, optional
        Lift records nested under the Nested_under key
    operation : str, default is Undefined, optional
        Select the operation nest or lift
    removePrefix : str, default is Undefined, optional
        Remove prefix from affected keys if it matches this string
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    wildcard : [str], default is Undefined, optional
        Nest records which field matches the wildcard
    """


    addPrefix?: str

    alias?: str

    nestUnder?: str

    nestedUnder?: str

    operation?: "nest" | "lift"

    removePrefix?: str

    retryLimit?: str

    wildcard?: [str]


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Parser:
    """
    Parser defines Parser Filter configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    keyName : str, default is Undefined, optional
        Specify field name in record to parse.
    parser : str, default is Undefined, optional
        Specify the parser name to interpret the field. Multiple Parser entries are allowed (split by comma).
    preserveKey : bool, default is Undefined, optional
        Keep original Key_Name field in the parsed result. If false, the field will be removed.
    reserveData : bool, default is Undefined, optional
        Keep all other original fields in the parsed result. If false, all other original fields will be removed.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    unescapeKey : bool, default is Undefined, optional
        If the key is a escaped string (e.g: stringify JSON), unescape the string before to apply the parser.
    """


    alias?: str

    keyName?: str

    parser?: str

    preserveKey?: bool

    reserveData?: bool

    retryLimit?: str

    unescapeKey?: bool


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0RecordModifier:
    """
    RecordModifier defines Record Modifier Filter configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    allowlistKeys : [str], default is Undefined, optional
        If the key is not matched, that field is removed.
    records : [str], default is Undefined, optional
        Append fields. This parameter needs key and value pair.
    removeKeys : [str], default is Undefined, optional
        If the key is matched, that field is removed.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    uuidKeys : [str], default is Undefined, optional
        If set, the plugin appends uuid to each record. The value assigned becomes the key in the map.
    whitelistKeys : [str], default is Undefined, optional
        An alias of allowlistKeys for backwards compatibility.
    """


    alias?: str

    allowlistKeys?: [str]

    records?: [str]

    removeKeys?: [str]

    retryLimit?: str

    uuidKeys?: [str]

    whitelistKeys?: [str]


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0RewriteTag:
    """
    RewriteTag defines a RewriteTag configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    emitterName : str, default is Undefined, optional
        When the filter emits a record under the new Tag, there is an internal emitter plugin that takes care of the job. Since this emitter expose metrics as any other component of the pipeline, you can use this property to configure an optional name for it.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    rules : [str], default is Undefined, optional
        Defines the matching criteria and the format of the Tag for the matching record. The Rule format have four components: KEY REGEX NEW_TAG KEEP.
    """


    alias?: str

    emitterName?: str

    retryLimit?: str

    rules?: [str]


    check:
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


schema FluentbitFluentIoV1alpha2FilterSpecFiltersItems0Throttle:
    """
    Throttle defines a Throttle configuration.

    Attributes
    ----------
    alias : str, default is Undefined, optional
        Alias for the plugin
    interval : str, default is Undefined, optional
        Interval is the time interval expressed in "sleep" format. e.g. 3s, 1.5m, 0.5h, etc.
    printStatus : bool, default is Undefined, optional
        PrintStatus represents whether to print status messages with current rate and the limits to information logs.
    rate : int, default is Undefined, optional
        Rate is the amount of messages for the time.
    retryLimit : str, default is Undefined, optional
        RetryLimit describes how many times fluent-bit should retry to send data to a specific output. If set to false fluent-bit will try indefinetly. If set to any integer N>0 it will try at most N+1 times. Leading zeros are not allowed (values such as 007, 0150, 01 do not work). If this property is not defined fluent-bit will use the default value: 1.
    window : int, default is Undefined, optional
        Window is the amount of intervals to calculate average over.
    """


    alias?: str

    interval?: str

    printStatus?: bool

    rate?: int

    retryLimit?: str

    window?: int


    check:
        regex.match(str(interval), r"^\d+(\.[0-9]{0,2})?(s|m|h|d)?$") if interval
        regex.match(str(retryLimit), r"^(((f|F)alse)|(no_limits)|(no_retries)|([1-9]+[0-9]*))$") if retryLimit


