"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema ChangeTransferPolicy:
    r"""
    ChangeTransferPolicy is the Schema for the changetransferpolicies API

    Attributes
    ----------
    apiVersion : str, default is "promoter.argoproj.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ChangeTransferPolicy", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : PromoterArgoprojIoV1alpha1ChangeTransferPolicySpec, default is Undefined, optional
        spec
    status : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatus, default is Undefined, optional
        status
    """


    apiVersion: "promoter.argoproj.io/v1alpha1" = "promoter.argoproj.io/v1alpha1"

    kind: "ChangeTransferPolicy" = "ChangeTransferPolicy"

    metadata?: v1.ObjectMeta

    spec?: PromoterArgoprojIoV1alpha1ChangeTransferPolicySpec

    status?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatus


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicySpec:
    r"""
    ChangeTransferPolicySpec defines the desired state of ChangeTransferPolicy

    Attributes
    ----------
    activeBranch : str, default is Undefined, required
        ActiveBranch staging hydrated branch
    activeCommitStatuses : [PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecActiveCommitStatusesItems0], default is Undefined, optional
        ActiveCommitStatuses lists the statuses to be monitored on the active branch
    autoMerge : bool, default is True, optional
        auto merge
    gitRepositoryRef : PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecGitRepositoryRef, default is Undefined, required
        git repository ref
    proposedBranch : str, default is Undefined, required
        ProposedBranch staging hydrated branch
    proposedCommitStatuses : [PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecProposedCommitStatusesItems0], default is Undefined, optional
        ProposedCommitStatuses lists the statuses to be monitored on the proposed branch
    """


    activeBranch: str

    activeCommitStatuses?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecActiveCommitStatusesItems0]

    autoMerge?: bool = True

    gitRepositoryRef: PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecGitRepositoryRef

    proposedBranch: str

    proposedCommitStatuses?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecProposedCommitStatusesItems0]


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecActiveCommitStatusesItems0:
    r"""
    CommitStatusSelector is used to select commit statuses by their key.

    Attributes
    ----------
    key : str, default is Undefined, required
        key
    """


    key: str


    check:
        _regex_match(str(key), r"(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?")


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecGitRepositoryRef:
    r"""
    RepositoryReference what repository to open the PR on.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name is the name of the object to refer to.
    """


    name: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicySpecProposedCommitStatusesItems0:
    r"""
    CommitStatusSelector is used to select commit statuses by their key.

    Attributes
    ----------
    key : str, default is Undefined, required
        key
    """


    key: str


    check:
        _regex_match(str(key), r"(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?")


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatus:
    r"""
    ChangeTransferPolicyStatus defines the observed state of ChangeTransferPolicy

    Attributes
    ----------
    active : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActive, default is Undefined, optional
        active
    conditions : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusConditionsItems0], default is Undefined, optional
        Conditions Represents the observations of the current state.
    proposed : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposed, default is Undefined, optional
        proposed
    pullRequest : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusPullRequest, default is Undefined, optional
        pull request
    """


    active?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActive

    conditions?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusConditionsItems0]

    proposed?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposed

    pullRequest?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusPullRequest


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActive:
    r"""
    Active is the state of the active branch.

    Attributes
    ----------
    commitStatuses : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveCommitStatusesItems0], default is Undefined, optional
        CommitStatuses is a list of commit statuses that are being monitored for this branch.
    dry : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDry, default is Undefined, optional
        dry
    hydrated : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydrated, default is Undefined, optional
        hydrated
    """


    commitStatuses?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveCommitStatusesItems0]

    dry?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDry

    hydrated?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydrated


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveCommitStatusesItems0:
    r"""
    ChangeRequestPolicyCommitStatusPhase defines the phase of a commit status in a ChangeTransferPolicy.

    Attributes
    ----------
    key : str, default is Undefined, required
        Key staging hydrated branch
    phase : str, default is Undefined, required
        Phase what phase is the status in
    url : str, default is Undefined, optional
        Url is the URL of the commit status
    """


    key: str

    phase: "pending" | "success" | "failure"

    url?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDry:
    r"""
    Dry is the dry state of the branch, which is the commit that is being proposed.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit
    body : str, default is Undefined, optional
        Body is the body of the commit message without the subject line
    commitTime : str, default is Undefined, optional
        CommitTime is the time the commit was made
    references : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDryReferencesItems0], default is Undefined, optional
        References are the references to other commits, that went into the hydration of the branch
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located
    sha : str, default is Undefined, optional
        Sha is the SHA of the commit in the branch
    subject : str, default is Undefined, optional
        Subject is the subject line of the commit message
    """


    author?: str

    body?: str

    commitTime?: str

    references?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDryReferencesItems0]

    repoURL?: str

    sha?: str

    subject?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDryReferencesItems0:
    r"""
    RevisionReference contains a reference to a some information that is related in some way to another commit. For now,
    it supports only references to a commit. In the future, it may support other types of references.

    Attributes
    ----------
    commit : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDryReferencesItems0Commit, default is Undefined, optional
        commit
    """


    commit?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDryReferencesItems0Commit


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveDryReferencesItems0Commit:
    r"""
    Commit contains metadata about the commit that is related in some way to another commit.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit.
    body : str, default is Undefined, optional
        Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`.
    date : str, default is Undefined, optional
        Date is the date of the commit, formatted as by `git show -s --format=%aI`.
    message : str, default is Undefined, optional
        Subject is the subject line of the commit message, i.e. `git show --format=%s`.
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located.
    sha : str, default is Undefined, optional
        Sha is the commit hash.
    """


    author?: str

    body?: str

    date?: str

    message?: str

    repoURL?: str

    sha?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydrated:
    r"""
    Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit
    body : str, default is Undefined, optional
        Body is the body of the commit message without the subject line
    commitTime : str, default is Undefined, optional
        CommitTime is the time the commit was made
    references : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydratedReferencesItems0], default is Undefined, optional
        References are the references to other commits, that went into the hydration of the branch
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located
    sha : str, default is Undefined, optional
        Sha is the SHA of the commit in the branch
    subject : str, default is Undefined, optional
        Subject is the subject line of the commit message
    """


    author?: str

    body?: str

    commitTime?: str

    references?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydratedReferencesItems0]

    repoURL?: str

    sha?: str

    subject?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydratedReferencesItems0:
    r"""
    RevisionReference contains a reference to a some information that is related in some way to another commit. For now,
    it supports only references to a commit. In the future, it may support other types of references.

    Attributes
    ----------
    commit : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydratedReferencesItems0Commit, default is Undefined, optional
        commit
    """


    commit?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydratedReferencesItems0Commit


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusActiveHydratedReferencesItems0Commit:
    r"""
    Commit contains metadata about the commit that is related in some way to another commit.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit.
    body : str, default is Undefined, optional
        Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`.
    date : str, default is Undefined, optional
        Date is the date of the commit, formatted as by `git show -s --format=%aI`.
    message : str, default is Undefined, optional
        Subject is the subject line of the commit message, i.e. `git show --format=%s`.
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located.
    sha : str, default is Undefined, optional
        Sha is the commit hash.
    """


    author?: str

    body?: str

    date?: str

    message?: str

    repoURL?: str

    sha?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
    """


    lastTransitionTime: str

    message: str

    observedGeneration?: int

    reason: str

    status: "True" | "False" | "Unknown"

    $type: str


    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposed:
    r"""
    Proposed is the state of the proposed branch.

    Attributes
    ----------
    commitStatuses : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedCommitStatusesItems0], default is Undefined, optional
        CommitStatuses is a list of commit statuses that are being monitored for this branch.
    dry : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDry, default is Undefined, optional
        dry
    hydrated : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydrated, default is Undefined, optional
        hydrated
    """


    commitStatuses?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedCommitStatusesItems0]

    dry?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDry

    hydrated?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydrated


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedCommitStatusesItems0:
    r"""
    ChangeRequestPolicyCommitStatusPhase defines the phase of a commit status in a ChangeTransferPolicy.

    Attributes
    ----------
    key : str, default is Undefined, required
        Key staging hydrated branch
    phase : str, default is Undefined, required
        Phase what phase is the status in
    url : str, default is Undefined, optional
        Url is the URL of the commit status
    """


    key: str

    phase: "pending" | "success" | "failure"

    url?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDry:
    r"""
    Dry is the dry state of the branch, which is the commit that is being proposed.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit
    body : str, default is Undefined, optional
        Body is the body of the commit message without the subject line
    commitTime : str, default is Undefined, optional
        CommitTime is the time the commit was made
    references : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDryReferencesItems0], default is Undefined, optional
        References are the references to other commits, that went into the hydration of the branch
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located
    sha : str, default is Undefined, optional
        Sha is the SHA of the commit in the branch
    subject : str, default is Undefined, optional
        Subject is the subject line of the commit message
    """


    author?: str

    body?: str

    commitTime?: str

    references?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDryReferencesItems0]

    repoURL?: str

    sha?: str

    subject?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDryReferencesItems0:
    r"""
    RevisionReference contains a reference to a some information that is related in some way to another commit. For now,
    it supports only references to a commit. In the future, it may support other types of references.

    Attributes
    ----------
    commit : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDryReferencesItems0Commit, default is Undefined, optional
        commit
    """


    commit?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDryReferencesItems0Commit


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedDryReferencesItems0Commit:
    r"""
    Commit contains metadata about the commit that is related in some way to another commit.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit.
    body : str, default is Undefined, optional
        Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`.
    date : str, default is Undefined, optional
        Date is the date of the commit, formatted as by `git show -s --format=%aI`.
    message : str, default is Undefined, optional
        Subject is the subject line of the commit message, i.e. `git show --format=%s`.
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located.
    sha : str, default is Undefined, optional
        Sha is the commit hash.
    """


    author?: str

    body?: str

    date?: str

    message?: str

    repoURL?: str

    sha?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydrated:
    r"""
    Hydrated is the hydrated state of the branch, which is the commit that is currently being worked on.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit
    body : str, default is Undefined, optional
        Body is the body of the commit message without the subject line
    commitTime : str, default is Undefined, optional
        CommitTime is the time the commit was made
    references : [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydratedReferencesItems0], default is Undefined, optional
        References are the references to other commits, that went into the hydration of the branch
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located
    sha : str, default is Undefined, optional
        Sha is the SHA of the commit in the branch
    subject : str, default is Undefined, optional
        Subject is the subject line of the commit message
    """


    author?: str

    body?: str

    commitTime?: str

    references?: [PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydratedReferencesItems0]

    repoURL?: str

    sha?: str

    subject?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydratedReferencesItems0:
    r"""
    RevisionReference contains a reference to a some information that is related in some way to another commit. For now,
    it supports only references to a commit. In the future, it may support other types of references.

    Attributes
    ----------
    commit : PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydratedReferencesItems0Commit, default is Undefined, optional
        commit
    """


    commit?: PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydratedReferencesItems0Commit


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusProposedHydratedReferencesItems0Commit:
    r"""
    Commit contains metadata about the commit that is related in some way to another commit.

    Attributes
    ----------
    author : str, default is Undefined, optional
        Author is the author of the commit.
    body : str, default is Undefined, optional
        Body is the body of the commit message, excluding the subject line, i.e. `git show --format=%b`.
    date : str, default is Undefined, optional
        Date is the date of the commit, formatted as by `git show -s --format=%aI`.
    message : str, default is Undefined, optional
        Subject is the subject line of the commit message, i.e. `git show --format=%s`.
    repoURL : str, default is Undefined, optional
        RepoURL is the URL of the repository where the commit is located.
    sha : str, default is Undefined, optional
        Sha is the commit hash.
    """


    author?: str

    body?: str

    date?: str

    message?: str

    repoURL?: str

    sha?: str


schema PromoterArgoprojIoV1alpha1ChangeTransferPolicyStatusPullRequest:
    r"""
    PullRequest is the state of the pull request that was created for this ChangeTransferPolicy.

    Attributes
    ----------
    id : str, default is Undefined, optional
        ID is the unique identifier of the pull request, set by the SCM.
    prCreationTime : str, default is Undefined, optional
        PRCreationTime is the time when the pull request was created.
    state : str, default is Undefined, optional
        State is the state of the pull request.
    url : str, default is Undefined, optional
        Url is the URL of the pull request.
    """


    id?: str

    prCreationTime?: str

    state?: "closed" | "merged" | "open"

    url?: str


