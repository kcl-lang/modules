"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Instance:
    """
    Instance is the Schema for the Instances API. A Cloud AI Platform Notebook instance.

    Attributes
    ----------
    apiVersion : str, default is "notebooks.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Instance", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NotebooksGcpUpboundIoV1beta1InstanceSpec, default is Undefined, required
        spec
    status : NotebooksGcpUpboundIoV1beta1InstanceStatus, default is Undefined, optional
        status
    """


    apiVersion: "notebooks.gcp.upbound.io/v1beta1" = "notebooks.gcp.upbound.io/v1beta1"

    kind: "Instance" = "Instance"

    metadata?: v1.ObjectMeta

    spec: NotebooksGcpUpboundIoV1beta1InstanceSpec

    status?: NotebooksGcpUpboundIoV1beta1InstanceStatus


schema NotebooksGcpUpboundIoV1beta1InstanceSpec:
    """
    InstanceSpec defines the desired state of Instance

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : NotebooksGcpUpboundIoV1beta1InstanceSpecForProvider, default is Undefined, required
        for provider
    initProvider : NotebooksGcpUpboundIoV1beta1InstanceSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : NotebooksGcpUpboundIoV1beta1InstanceSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : NotebooksGcpUpboundIoV1beta1InstanceSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: NotebooksGcpUpboundIoV1beta1InstanceSpecForProvider

    initProvider?: NotebooksGcpUpboundIoV1beta1InstanceSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: NotebooksGcpUpboundIoV1beta1InstanceSpecProviderConfigRef

    publishConnectionDetailsTo?: NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: NotebooksGcpUpboundIoV1beta1InstanceSpecWriteConnectionSecretToRef


schema NotebooksGcpUpboundIoV1beta1InstanceSpecForProvider:
    """
    notebooks gcp upbound io v1beta1 instance spec for provider

    Attributes
    ----------
    acceleratorConfig : [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderAcceleratorConfigItems0], default is Undefined, optional
        The hardware accelerator used on this instance. If you use accelerators,
        make sure that your configuration has enough vCPUs and memory to support the
        machineType you have selected.
        Structure is documented below.
    bootDiskSizeGb : float, default is Undefined, optional
        The size of the boot disk in GB attached to this instance,
        up to a maximum of 64000 GB (64 TB). The minimum recommended value is 100 GB.
        If not specified, this defaults to 100.
    bootDiskType : str, default is Undefined, optional
        Possible disk types for notebook instances.
        Possible values are: DISK_TYPE_UNSPECIFIED, PD_STANDARD, PD_SSD, PD_BALANCED, PD_EXTREME.
    containerImage : [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderContainerImageItems0], default is Undefined, optional
        Use a container image to start the notebook instance.
        Structure is documented below.
    customGpuDriverPath : str, default is Undefined, optional
        Specify a custom Cloud Storage path where the GPU driver is stored.
        If not specified, we'll automatically choose from official GPU drivers.
    dataDiskSizeGb : float, default is Undefined, optional
        The size of the data disk in GB attached to this instance,
        up to a maximum of 64000 GB (64 TB).
        You can choose the size of the data disk based on how big your notebooks and data are.
        If not specified, this defaults to 100.
    dataDiskType : str, default is Undefined, optional
        Possible disk types for notebook instances.
        Possible values are: DISK_TYPE_UNSPECIFIED, PD_STANDARD, PD_SSD, PD_BALANCED, PD_EXTREME.
    desiredState : str, default is Undefined, optional
        Desired state of the Notebook Instance. Set this field to ACTIVE to start the Instance, and STOPPED to stop the Instance.
    diskEncryption : str, default is Undefined, optional
        Disk encryption method used on the boot and data disks, defaults to GMEK.
        Possible values are: DISK_ENCRYPTION_UNSPECIFIED, GMEK, CMEK.
    installGpuDriver : bool, default is Undefined, optional
        Whether the end user authorizes Google Cloud to install GPU driver
        on this instance. If this field is empty or set to false, the GPU driver
        won't be installed. Only applicable to instances with GPUs.
    instanceOwners : [str], default is Undefined, optional
        The list of owners of this instance after creation.
        Format: alias@example.com.
        Currently supports one owner only.
        If not specified, all of the service account users of
        your VM instance's service account can use the instance.
    kmsKey : str, default is Undefined, optional
        The KMS key used to encrypt the disks, only applicable if diskEncryption is CMEK.
        Format: projects/{project_id}/locations/{location}/keyRings/{key_ring_id}/cryptoKeys/{key_id}
    labels : {str:str}, default is Undefined, optional
        Labels to apply to this instance. These can be later modified by the setLabels method.
        An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    location : str, default is Undefined, required
        A reference to the zone where the machine resides.
    machineType : str, default is Undefined, optional
        A reference to a machine type which defines VM kind.
    metadata : {str:str}, default is Undefined, optional
        Custom metadata to apply to this instance.
        An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    network : str, default is Undefined, optional
        The name of the VPC that this instance is in.
        Format: projects/{project_id}/global/networks/{network_id}
    nicType : str, default is Undefined, optional
        The type of vNIC driver.
        Possible values are: UNSPECIFIED_NIC_TYPE, VIRTIO_NET, GVNIC.
    noProxyAccess : bool, default is Undefined, optional
        The notebook instance will not register with the proxy..
    noPublicIp : bool, default is Undefined, optional
        No public IP will be assigned to this instance.
    noRemoveDataDisk : bool, default is Undefined, optional
        If true, the data disk will not be auto deleted when deleting the instance.
    postStartupScript : str, default is Undefined, optional
        Path to a Bash script that automatically runs after a
        notebook instance fully boots up. The path must be a URL
        or Cloud Storage path (gs://path-to-file/file-name).
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    reservationAffinity : [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderReservationAffinityItems0], default is Undefined, optional
        Reservation Affinity for consuming Zonal reservation.
        Structure is documented below.
    serviceAccount : str, default is Undefined, optional
        The service account on this instance, giving access to other
        Google Cloud services. You can use any service account within
        the same project, but you must have the service account user
        permission to use the instance. If not specified,
        the Compute Engine default service account is used.
    serviceAccountScopes : [str], default is Undefined, optional
        Optional. The URIs of service account scopes to be included in Compute Engine instances.
        If not specified, the following scopes are defined:
    shieldedInstanceConfig : [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderShieldedInstanceConfigItems0], default is Undefined, optional
        A set of Shielded Instance options. Check [Images using supported Shielded VM features]
        Not all combinations are valid
        Structure is documented below.
    subnet : str, default is Undefined, optional
        The name of the subnet that this instance is in.
        Format: projects/{project_id}/regions/{region}/subnetworks/{subnetwork_id}
    tags : [str], default is Undefined, optional
        The Compute Engine tags to add to instance.
    vmImage : [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderVMImageItems0], default is Undefined, optional
        Use a Compute Engine VM image to start the notebook instance.
        Structure is documented below.
    """


    acceleratorConfig?: [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderAcceleratorConfigItems0]

    bootDiskSizeGb?: float

    bootDiskType?: str

    containerImage?: [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderContainerImageItems0]

    customGpuDriverPath?: str

    dataDiskSizeGb?: float

    dataDiskType?: str

    desiredState?: str

    diskEncryption?: str

    installGpuDriver?: bool

    instanceOwners?: [str]

    kmsKey?: str

    labels?: {str:str}

    location: str

    machineType?: str

    metadata?: {str:str}

    network?: str

    nicType?: str

    noProxyAccess?: bool

    noPublicIp?: bool

    noRemoveDataDisk?: bool

    postStartupScript?: str

    project?: str

    reservationAffinity?: [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderReservationAffinityItems0]

    serviceAccount?: str

    serviceAccountScopes?: [str]

    shieldedInstanceConfig?: [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderShieldedInstanceConfigItems0]

    subnet?: str

    tags?: [str]

    vmImage?: [NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderVMImageItems0]


schema NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderAcceleratorConfigItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec for provider accelerator config items0

    Attributes
    ----------
    coreCount : float, default is Undefined, optional
        Count of cores of this accelerator.
    $type : str, default is Undefined, optional
        Type of this accelerator.
        Possible values are: ACCELERATOR_TYPE_UNSPECIFIED, NVIDIA_TESLA_K80, NVIDIA_TESLA_P100, NVIDIA_TESLA_V100, NVIDIA_TESLA_P4, NVIDIA_TESLA_T4, NVIDIA_TESLA_T4_VWS, NVIDIA_TESLA_P100_VWS, NVIDIA_TESLA_P4_VWS, NVIDIA_TESLA_A100, TPU_V2, TPU_V3.
    """


    coreCount?: float

    $type?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderContainerImageItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec for provider container image items0

    Attributes
    ----------
    repository : str, default is Undefined, optional
        The path to the container image repository.
        For example: gcr.io/{project_id}/{imageName}
    tag : str, default is Undefined, optional
        The tag of the container image. If not specified, this defaults to the latest tag.
    """


    repository?: str

    tag?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderReservationAffinityItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec for provider reservation affinity items0

    Attributes
    ----------
    consumeReservationType : str, default is Undefined, optional
        The type of Compute Reservation.
        Possible values are: NO_RESERVATION, ANY_RESERVATION, SPECIFIC_RESERVATION.
    key : str, default is Undefined, optional
        Corresponds to the label key of reservation resource.
    values : [str], default is Undefined, optional
        Corresponds to the label values of reservation resource.
    """


    consumeReservationType?: str

    key?: str

    values?: [str]


schema NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderShieldedInstanceConfigItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec for provider shielded instance config items0

    Attributes
    ----------
    enableIntegrityMonitoring : bool, default is Undefined, optional
        Defines whether the instance has integrity monitoring enabled. Enables monitoring and attestation of the
        boot integrity of the instance. The attestation is performed against the integrity policy baseline.
        This baseline is initially derived from the implicitly trusted boot image when the instance is created.
        Enabled by default.
    enableSecureBoot : bool, default is Undefined, optional
        Defines whether the instance has Secure Boot enabled. Secure Boot helps ensure that the system only runs
        authentic software by verifying the digital signature of all boot components, and halting the boot process
        if signature verification fails.
        Disabled by default.
    enableVtpm : bool, default is Undefined, optional
        Defines whether the instance has the vTPM enabled.
        Enabled by default.
    """


    enableIntegrityMonitoring?: bool

    enableSecureBoot?: bool

    enableVtpm?: bool


schema NotebooksGcpUpboundIoV1beta1InstanceSpecForProviderVMImageItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec for provider VM image items0

    Attributes
    ----------
    imageFamily : str, default is Undefined, optional
        Use this VM image family to find the image; the newest image in this family will be used.
    imageName : str, default is Undefined, optional
        Use VM image name to find the image.
    project : str, default is Undefined, optional
        The name of the Google Cloud project that this VM image belongs to.
        Format: projects/{project_id}
    """


    imageFamily?: str

    imageName?: str

    project?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    acceleratorConfig : [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderAcceleratorConfigItems0], default is Undefined, optional
        The hardware accelerator used on this instance. If you use accelerators,
        make sure that your configuration has enough vCPUs and memory to support the
        machineType you have selected.
        Structure is documented below.
    bootDiskSizeGb : float, default is Undefined, optional
        The size of the boot disk in GB attached to this instance,
        up to a maximum of 64000 GB (64 TB). The minimum recommended value is 100 GB.
        If not specified, this defaults to 100.
    bootDiskType : str, default is Undefined, optional
        Possible disk types for notebook instances.
        Possible values are: DISK_TYPE_UNSPECIFIED, PD_STANDARD, PD_SSD, PD_BALANCED, PD_EXTREME.
    containerImage : [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderContainerImageItems0], default is Undefined, optional
        Use a container image to start the notebook instance.
        Structure is documented below.
    customGpuDriverPath : str, default is Undefined, optional
        Specify a custom Cloud Storage path where the GPU driver is stored.
        If not specified, we'll automatically choose from official GPU drivers.
    dataDiskSizeGb : float, default is Undefined, optional
        The size of the data disk in GB attached to this instance,
        up to a maximum of 64000 GB (64 TB).
        You can choose the size of the data disk based on how big your notebooks and data are.
        If not specified, this defaults to 100.
    dataDiskType : str, default is Undefined, optional
        Possible disk types for notebook instances.
        Possible values are: DISK_TYPE_UNSPECIFIED, PD_STANDARD, PD_SSD, PD_BALANCED, PD_EXTREME.
    desiredState : str, default is Undefined, optional
        Desired state of the Notebook Instance. Set this field to ACTIVE to start the Instance, and STOPPED to stop the Instance.
    diskEncryption : str, default is Undefined, optional
        Disk encryption method used on the boot and data disks, defaults to GMEK.
        Possible values are: DISK_ENCRYPTION_UNSPECIFIED, GMEK, CMEK.
    installGpuDriver : bool, default is Undefined, optional
        Whether the end user authorizes Google Cloud to install GPU driver
        on this instance. If this field is empty or set to false, the GPU driver
        won't be installed. Only applicable to instances with GPUs.
    instanceOwners : [str], default is Undefined, optional
        The list of owners of this instance after creation.
        Format: alias@example.com.
        Currently supports one owner only.
        If not specified, all of the service account users of
        your VM instance's service account can use the instance.
    kmsKey : str, default is Undefined, optional
        The KMS key used to encrypt the disks, only applicable if diskEncryption is CMEK.
        Format: projects/{project_id}/locations/{location}/keyRings/{key_ring_id}/cryptoKeys/{key_id}
    labels : {str:str}, default is Undefined, optional
        Labels to apply to this instance. These can be later modified by the setLabels method.
        An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    machineType : str, default is Undefined, optional
        A reference to a machine type which defines VM kind.
    metadata : {str:str}, default is Undefined, optional
        Custom metadata to apply to this instance.
        An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    network : str, default is Undefined, optional
        The name of the VPC that this instance is in.
        Format: projects/{project_id}/global/networks/{network_id}
    nicType : str, default is Undefined, optional
        The type of vNIC driver.
        Possible values are: UNSPECIFIED_NIC_TYPE, VIRTIO_NET, GVNIC.
    noProxyAccess : bool, default is Undefined, optional
        The notebook instance will not register with the proxy..
    noPublicIp : bool, default is Undefined, optional
        No public IP will be assigned to this instance.
    noRemoveDataDisk : bool, default is Undefined, optional
        If true, the data disk will not be auto deleted when deleting the instance.
    postStartupScript : str, default is Undefined, optional
        Path to a Bash script that automatically runs after a
        notebook instance fully boots up. The path must be a URL
        or Cloud Storage path (gs://path-to-file/file-name).
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    reservationAffinity : [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderReservationAffinityItems0], default is Undefined, optional
        Reservation Affinity for consuming Zonal reservation.
        Structure is documented below.
    serviceAccount : str, default is Undefined, optional
        The service account on this instance, giving access to other
        Google Cloud services. You can use any service account within
        the same project, but you must have the service account user
        permission to use the instance. If not specified,
        the Compute Engine default service account is used.
    serviceAccountScopes : [str], default is Undefined, optional
        Optional. The URIs of service account scopes to be included in Compute Engine instances.
        If not specified, the following scopes are defined:
    shieldedInstanceConfig : [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderShieldedInstanceConfigItems0], default is Undefined, optional
        A set of Shielded Instance options. Check [Images using supported Shielded VM features]
        Not all combinations are valid
        Structure is documented below.
    subnet : str, default is Undefined, optional
        The name of the subnet that this instance is in.
        Format: projects/{project_id}/regions/{region}/subnetworks/{subnetwork_id}
    tags : [str], default is Undefined, optional
        The Compute Engine tags to add to instance.
    vmImage : [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderVMImageItems0], default is Undefined, optional
        Use a Compute Engine VM image to start the notebook instance.
        Structure is documented below.
    """


    acceleratorConfig?: [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderAcceleratorConfigItems0]

    bootDiskSizeGb?: float

    bootDiskType?: str

    containerImage?: [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderContainerImageItems0]

    customGpuDriverPath?: str

    dataDiskSizeGb?: float

    dataDiskType?: str

    desiredState?: str

    diskEncryption?: str

    installGpuDriver?: bool

    instanceOwners?: [str]

    kmsKey?: str

    labels?: {str:str}

    machineType?: str

    metadata?: {str:str}

    network?: str

    nicType?: str

    noProxyAccess?: bool

    noPublicIp?: bool

    noRemoveDataDisk?: bool

    postStartupScript?: str

    project?: str

    reservationAffinity?: [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderReservationAffinityItems0]

    serviceAccount?: str

    serviceAccountScopes?: [str]

    shieldedInstanceConfig?: [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderShieldedInstanceConfigItems0]

    subnet?: str

    tags?: [str]

    vmImage?: [NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderVMImageItems0]


schema NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderAcceleratorConfigItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec init provider accelerator config items0

    Attributes
    ----------
    coreCount : float, default is Undefined, optional
        Count of cores of this accelerator.
    $type : str, default is Undefined, optional
        Type of this accelerator.
        Possible values are: ACCELERATOR_TYPE_UNSPECIFIED, NVIDIA_TESLA_K80, NVIDIA_TESLA_P100, NVIDIA_TESLA_V100, NVIDIA_TESLA_P4, NVIDIA_TESLA_T4, NVIDIA_TESLA_T4_VWS, NVIDIA_TESLA_P100_VWS, NVIDIA_TESLA_P4_VWS, NVIDIA_TESLA_A100, TPU_V2, TPU_V3.
    """


    coreCount?: float

    $type?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderContainerImageItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec init provider container image items0

    Attributes
    ----------
    repository : str, default is Undefined, optional
        The path to the container image repository.
        For example: gcr.io/{project_id}/{imageName}
    tag : str, default is Undefined, optional
        The tag of the container image. If not specified, this defaults to the latest tag.
    """


    repository?: str

    tag?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderReservationAffinityItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec init provider reservation affinity items0

    Attributes
    ----------
    consumeReservationType : str, default is Undefined, optional
        The type of Compute Reservation.
        Possible values are: NO_RESERVATION, ANY_RESERVATION, SPECIFIC_RESERVATION.
    key : str, default is Undefined, optional
        Corresponds to the label key of reservation resource.
    values : [str], default is Undefined, optional
        Corresponds to the label values of reservation resource.
    """


    consumeReservationType?: str

    key?: str

    values?: [str]


schema NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderShieldedInstanceConfigItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec init provider shielded instance config items0

    Attributes
    ----------
    enableIntegrityMonitoring : bool, default is Undefined, optional
        Defines whether the instance has integrity monitoring enabled. Enables monitoring and attestation of the
        boot integrity of the instance. The attestation is performed against the integrity policy baseline.
        This baseline is initially derived from the implicitly trusted boot image when the instance is created.
        Enabled by default.
    enableSecureBoot : bool, default is Undefined, optional
        Defines whether the instance has Secure Boot enabled. Secure Boot helps ensure that the system only runs
        authentic software by verifying the digital signature of all boot components, and halting the boot process
        if signature verification fails.
        Disabled by default.
    enableVtpm : bool, default is Undefined, optional
        Defines whether the instance has the vTPM enabled.
        Enabled by default.
    """


    enableIntegrityMonitoring?: bool

    enableSecureBoot?: bool

    enableVtpm?: bool


schema NotebooksGcpUpboundIoV1beta1InstanceSpecInitProviderVMImageItems0:
    """
    notebooks gcp upbound io v1beta1 instance spec init provider VM image items0

    Attributes
    ----------
    imageFamily : str, default is Undefined, optional
        Use this VM image family to find the image; the newest image in this family will be used.
    imageName : str, default is Undefined, optional
        Use VM image name to find the image.
    project : str, default is Undefined, optional
        The name of the Google Cloud project that this VM image belongs to.
        Format: projects/{project_id}
    """


    imageFamily?: str

    imageName?: str

    project?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : NotebooksGcpUpboundIoV1beta1InstanceSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: NotebooksGcpUpboundIoV1beta1InstanceSpecProviderConfigRefPolicy


schema NotebooksGcpUpboundIoV1beta1InstanceSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToConfigRef

    metadata?: NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToMetadata

    name: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToConfigRefPolicy


schema NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema NotebooksGcpUpboundIoV1beta1InstanceSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema NotebooksGcpUpboundIoV1beta1InstanceSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema NotebooksGcpUpboundIoV1beta1InstanceStatus:
    """
    InstanceStatus defines the observed state of Instance.

    Attributes
    ----------
    atProvider : NotebooksGcpUpboundIoV1beta1InstanceStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [NotebooksGcpUpboundIoV1beta1InstanceStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: NotebooksGcpUpboundIoV1beta1InstanceStatusAtProvider

    conditions?: [NotebooksGcpUpboundIoV1beta1InstanceStatusConditionsItems0]


schema NotebooksGcpUpboundIoV1beta1InstanceStatusAtProvider:
    """
    notebooks gcp upbound io v1beta1 instance status at provider

    Attributes
    ----------
    acceleratorConfig : [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderAcceleratorConfigItems0], default is Undefined, optional
        The hardware accelerator used on this instance. If you use accelerators,
        make sure that your configuration has enough vCPUs and memory to support the
        machineType you have selected.
        Structure is documented below.
    bootDiskSizeGb : float, default is Undefined, optional
        The size of the boot disk in GB attached to this instance,
        up to a maximum of 64000 GB (64 TB). The minimum recommended value is 100 GB.
        If not specified, this defaults to 100.
    bootDiskType : str, default is Undefined, optional
        Possible disk types for notebook instances.
        Possible values are: DISK_TYPE_UNSPECIFIED, PD_STANDARD, PD_SSD, PD_BALANCED, PD_EXTREME.
    containerImage : [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderContainerImageItems0], default is Undefined, optional
        Use a container image to start the notebook instance.
        Structure is documented below.
    createTime : str, default is Undefined, optional
        Instance creation time
    customGpuDriverPath : str, default is Undefined, optional
        Specify a custom Cloud Storage path where the GPU driver is stored.
        If not specified, we'll automatically choose from official GPU drivers.
    dataDiskSizeGb : float, default is Undefined, optional
        The size of the data disk in GB attached to this instance,
        up to a maximum of 64000 GB (64 TB).
        You can choose the size of the data disk based on how big your notebooks and data are.
        If not specified, this defaults to 100.
    dataDiskType : str, default is Undefined, optional
        Possible disk types for notebook instances.
        Possible values are: DISK_TYPE_UNSPECIFIED, PD_STANDARD, PD_SSD, PD_BALANCED, PD_EXTREME.
    desiredState : str, default is Undefined, optional
        Desired state of the Notebook Instance. Set this field to ACTIVE to start the Instance, and STOPPED to stop the Instance.
    diskEncryption : str, default is Undefined, optional
        Disk encryption method used on the boot and data disks, defaults to GMEK.
        Possible values are: DISK_ENCRYPTION_UNSPECIFIED, GMEK, CMEK.
    effectiveLabels : {str:str}, default is Undefined, optional
        for all of the labels present on the resource.
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/locations/{{location}}/instances/{{name}}
    installGpuDriver : bool, default is Undefined, optional
        Whether the end user authorizes Google Cloud to install GPU driver
        on this instance. If this field is empty or set to false, the GPU driver
        won't be installed. Only applicable to instances with GPUs.
    instanceOwners : [str], default is Undefined, optional
        The list of owners of this instance after creation.
        Format: alias@example.com.
        Currently supports one owner only.
        If not specified, all of the service account users of
        your VM instance's service account can use the instance.
    kmsKey : str, default is Undefined, optional
        The KMS key used to encrypt the disks, only applicable if diskEncryption is CMEK.
        Format: projects/{project_id}/locations/{location}/keyRings/{key_ring_id}/cryptoKeys/{key_id}
    labels : {str:str}, default is Undefined, optional
        Labels to apply to this instance. These can be later modified by the setLabels method.
        An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    location : str, default is Undefined, optional
        A reference to the zone where the machine resides.
    machineType : str, default is Undefined, optional
        A reference to a machine type which defines VM kind.
    metadata : {str:str}, default is Undefined, optional
        Custom metadata to apply to this instance.
        An object containing a list of "key": value pairs. Example: { "name": "wrench", "mass": "1.3kg", "count": "3" }.
    network : str, default is Undefined, optional
        The name of the VPC that this instance is in.
        Format: projects/{project_id}/global/networks/{network_id}
    nicType : str, default is Undefined, optional
        The type of vNIC driver.
        Possible values are: UNSPECIFIED_NIC_TYPE, VIRTIO_NET, GVNIC.
    noProxyAccess : bool, default is Undefined, optional
        The notebook instance will not register with the proxy..
    noPublicIp : bool, default is Undefined, optional
        No public IP will be assigned to this instance.
    noRemoveDataDisk : bool, default is Undefined, optional
        If true, the data disk will not be auto deleted when deleting the instance.
    postStartupScript : str, default is Undefined, optional
        Path to a Bash script that automatically runs after a
        notebook instance fully boots up. The path must be a URL
        or Cloud Storage path (gs://path-to-file/file-name).
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    proxyUri : str, default is Undefined, optional
        The proxy endpoint that is used to access the Jupyter notebook.
        Only returned when the resource is in a PROVISIONED state.
    reservationAffinity : [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderReservationAffinityItems0], default is Undefined, optional
        Reservation Affinity for consuming Zonal reservation.
        Structure is documented below.
    serviceAccount : str, default is Undefined, optional
        The service account on this instance, giving access to other
        Google Cloud services. You can use any service account within
        the same project, but you must have the service account user
        permission to use the instance. If not specified,
        the Compute Engine default service account is used.
    serviceAccountScopes : [str], default is Undefined, optional
        Optional. The URIs of service account scopes to be included in Compute Engine instances.
        If not specified, the following scopes are defined:
    shieldedInstanceConfig : [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderShieldedInstanceConfigItems0], default is Undefined, optional
        A set of Shielded Instance options. Check [Images using supported Shielded VM features]
        Not all combinations are valid
        Structure is documented below.
    state : str, default is Undefined, optional
        The state of this instance.
    subnet : str, default is Undefined, optional
        The name of the subnet that this instance is in.
        Format: projects/{project_id}/regions/{region}/subnetworks/{subnetwork_id}
    tags : [str], default is Undefined, optional
        The Compute Engine tags to add to instance.
    terraformLabels : {str:str}, default is Undefined, optional
        The combination of labels configured directly on the resource
        and default labels configured on the provider.
    updateTime : str, default is Undefined, optional
        Instance update time.
    vmImage : [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderVMImageItems0], default is Undefined, optional
        Use a Compute Engine VM image to start the notebook instance.
        Structure is documented below.
    """


    acceleratorConfig?: [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderAcceleratorConfigItems0]

    bootDiskSizeGb?: float

    bootDiskType?: str

    containerImage?: [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderContainerImageItems0]

    createTime?: str

    customGpuDriverPath?: str

    dataDiskSizeGb?: float

    dataDiskType?: str

    desiredState?: str

    diskEncryption?: str

    effectiveLabels?: {str:str}

    id?: str

    installGpuDriver?: bool

    instanceOwners?: [str]

    kmsKey?: str

    labels?: {str:str}

    location?: str

    machineType?: str

    metadata?: {str:str}

    network?: str

    nicType?: str

    noProxyAccess?: bool

    noPublicIp?: bool

    noRemoveDataDisk?: bool

    postStartupScript?: str

    project?: str

    proxyUri?: str

    reservationAffinity?: [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderReservationAffinityItems0]

    serviceAccount?: str

    serviceAccountScopes?: [str]

    shieldedInstanceConfig?: [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderShieldedInstanceConfigItems0]

    state?: str

    subnet?: str

    tags?: [str]

    terraformLabels?: {str:str}

    updateTime?: str

    vmImage?: [NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderVMImageItems0]


schema NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderAcceleratorConfigItems0:
    """
    notebooks gcp upbound io v1beta1 instance status at provider accelerator config items0

    Attributes
    ----------
    coreCount : float, default is Undefined, optional
        Count of cores of this accelerator.
    $type : str, default is Undefined, optional
        Type of this accelerator.
        Possible values are: ACCELERATOR_TYPE_UNSPECIFIED, NVIDIA_TESLA_K80, NVIDIA_TESLA_P100, NVIDIA_TESLA_V100, NVIDIA_TESLA_P4, NVIDIA_TESLA_T4, NVIDIA_TESLA_T4_VWS, NVIDIA_TESLA_P100_VWS, NVIDIA_TESLA_P4_VWS, NVIDIA_TESLA_A100, TPU_V2, TPU_V3.
    """


    coreCount?: float

    $type?: str


schema NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderContainerImageItems0:
    """
    notebooks gcp upbound io v1beta1 instance status at provider container image items0

    Attributes
    ----------
    repository : str, default is Undefined, optional
        The path to the container image repository.
        For example: gcr.io/{project_id}/{imageName}
    tag : str, default is Undefined, optional
        The tag of the container image. If not specified, this defaults to the latest tag.
    """


    repository?: str

    tag?: str


schema NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderReservationAffinityItems0:
    """
    notebooks gcp upbound io v1beta1 instance status at provider reservation affinity items0

    Attributes
    ----------
    consumeReservationType : str, default is Undefined, optional
        The type of Compute Reservation.
        Possible values are: NO_RESERVATION, ANY_RESERVATION, SPECIFIC_RESERVATION.
    key : str, default is Undefined, optional
        Corresponds to the label key of reservation resource.
    values : [str], default is Undefined, optional
        Corresponds to the label values of reservation resource.
    """


    consumeReservationType?: str

    key?: str

    values?: [str]


schema NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderShieldedInstanceConfigItems0:
    """
    notebooks gcp upbound io v1beta1 instance status at provider shielded instance config items0

    Attributes
    ----------
    enableIntegrityMonitoring : bool, default is Undefined, optional
        Defines whether the instance has integrity monitoring enabled. Enables monitoring and attestation of the
        boot integrity of the instance. The attestation is performed against the integrity policy baseline.
        This baseline is initially derived from the implicitly trusted boot image when the instance is created.
        Enabled by default.
    enableSecureBoot : bool, default is Undefined, optional
        Defines whether the instance has Secure Boot enabled. Secure Boot helps ensure that the system only runs
        authentic software by verifying the digital signature of all boot components, and halting the boot process
        if signature verification fails.
        Disabled by default.
    enableVtpm : bool, default is Undefined, optional
        Defines whether the instance has the vTPM enabled.
        Enabled by default.
    """


    enableIntegrityMonitoring?: bool

    enableSecureBoot?: bool

    enableVtpm?: bool


schema NotebooksGcpUpboundIoV1beta1InstanceStatusAtProviderVMImageItems0:
    """
    notebooks gcp upbound io v1beta1 instance status at provider VM image items0

    Attributes
    ----------
    imageFamily : str, default is Undefined, optional
        Use this VM image family to find the image; the newest image in this family will be used.
    imageName : str, default is Undefined, optional
        Use VM image name to find the image.
    project : str, default is Undefined, optional
        The name of the Google Cloud project that this VM image belongs to.
        Format: projects/{project_id}
    """


    imageFamily?: str

    imageName?: str

    project?: str


schema NotebooksGcpUpboundIoV1beta1InstanceStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


