"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1


schema ServiceIntegration:
    """
    ServiceIntegration is the Schema for the serviceintegrations API

    Attributes
    ----------
    apiVersion : str, default is "aiven.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ServiceIntegration", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : AivenIoV1alpha1ServiceIntegrationSpec, default is Undefined, optional
        spec
    status : AivenIoV1alpha1ServiceIntegrationStatus, default is Undefined, optional
        status
    """


    apiVersion: "aiven.io/v1alpha1" = "aiven.io/v1alpha1"

    kind: "ServiceIntegration" = "ServiceIntegration"

    metadata?: v1.ObjectMeta

    spec?: AivenIoV1alpha1ServiceIntegrationSpec

    status?: AivenIoV1alpha1ServiceIntegrationStatus


schema AivenIoV1alpha1ServiceIntegrationSpec:
    """
    ServiceIntegrationSpec defines the desired state of ServiceIntegration

    Attributes
    ----------
    authSecretRef : AivenIoV1alpha1ServiceIntegrationSpecAuthSecretRef, default is Undefined, optional
        auth secret ref
    clickhouseKafka : AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafka, default is Undefined, optional
        clickhouse kafka
    clickhousePostgresql : AivenIoV1alpha1ServiceIntegrationSpecClickhousePostgresql, default is Undefined, optional
        clickhouse postgresql
    datadog : AivenIoV1alpha1ServiceIntegrationSpecDatadog, default is Undefined, optional
        datadog
    destinationEndpointId : str, default is Undefined, optional
        Destination endpoint for the integration (if any)
    destinationProjectName : str, default is Undefined, optional
        Destination project for the integration (if any)
    destinationServiceName : str, default is Undefined, optional
        Destination service for the integration (if any)
    externalAWSCloudwatchMetrics : AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetrics, default is Undefined, optional
        external a w s cloudwatch metrics
    integrationType : str, default is Undefined, required
        Type of the service integration accepted by Aiven API. Some values may not be supported by the operator
    kafkaConnect : AivenIoV1alpha1ServiceIntegrationSpecKafkaConnect, default is Undefined, optional
        kafka connect
    kafkaLogs : AivenIoV1alpha1ServiceIntegrationSpecKafkaLogs, default is Undefined, optional
        kafka logs
    kafkaMirrormaker : AivenIoV1alpha1ServiceIntegrationSpecKafkaMirrormaker, default is Undefined, optional
        kafka mirrormaker
    logs : AivenIoV1alpha1ServiceIntegrationSpecLogs, default is Undefined, optional
        logs
    metrics : AivenIoV1alpha1ServiceIntegrationSpecMetrics, default is Undefined, optional
        metrics
    project : str, default is Undefined, required
        Project the integration belongs to
    sourceEndpointID : str, default is Undefined, optional
        Source endpoint for the integration (if any)
    sourceProjectName : str, default is Undefined, optional
        Source project for the integration (if any)
    sourceServiceName : str, default is Undefined, optional
        Source service for the integration (if any)
    """


    authSecretRef?: AivenIoV1alpha1ServiceIntegrationSpecAuthSecretRef

    clickhouseKafka?: AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafka

    clickhousePostgresql?: AivenIoV1alpha1ServiceIntegrationSpecClickhousePostgresql

    datadog?: AivenIoV1alpha1ServiceIntegrationSpecDatadog

    destinationEndpointId?: str

    destinationProjectName?: str

    destinationServiceName?: str

    externalAWSCloudwatchMetrics?: AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetrics

    integrationType: "alertmanager" | "autoscaler" | "caching" | "cassandra_cross_service_cluster" | "clickhouse_kafka" | "clickhouse_postgresql" | "dashboard" | "datadog" | "datasource" | "external_aws_cloudwatch_logs" | "external_aws_cloudwatch_metrics" | "external_elasticsearch_logs" | "external_google_cloud_logging" | "external_opensearch_logs" | "flink" | "flink_external_kafka" | "internal_connectivity" | "jolokia" | "kafka_connect" | "kafka_logs" | "kafka_mirrormaker" | "logs" | "m3aggregator" | "m3coordinator" | "metrics" | "opensearch_cross_cluster_replication" | "opensearch_cross_cluster_search" | "prometheus" | "read_replica" | "rsyslog" | "schema_registry_proxy" | "stresstester" | "thanosquery" | "thanosstore" | "vmalert"

    kafkaConnect?: AivenIoV1alpha1ServiceIntegrationSpecKafkaConnect

    kafkaLogs?: AivenIoV1alpha1ServiceIntegrationSpecKafkaLogs

    kafkaMirrormaker?: AivenIoV1alpha1ServiceIntegrationSpecKafkaMirrormaker

    logs?: AivenIoV1alpha1ServiceIntegrationSpecLogs

    metrics?: AivenIoV1alpha1ServiceIntegrationSpecMetrics

    project: str

    sourceEndpointID?: str

    sourceProjectName?: str

    sourceServiceName?: str


    check:
        len(destinationEndpointId) <= 36
        len(destinationProjectName) <= 63
        len(destinationServiceName) <= 64
        len(project) <= 63
        len(sourceEndpointID) <= 36
        len(sourceProjectName) <= 63
        len(sourceServiceName) <= 64


schema AivenIoV1alpha1ServiceIntegrationSpecAuthSecretRef:
    """
    Authentication reference to Aiven token in a secret

    Attributes
    ----------
    key : str, default is Undefined, required
        key
    name : str, default is Undefined, required
        name
    """


    key: str

    name: str


    check:
        len(key) >= 1
        len(name) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafka:
    """
    Clickhouse Kafka configuration values

    Attributes
    ----------
    tables : [AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0], default is Undefined, optional
        Tables to create
    """


    tables?: [AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0]


    check:
        len(tables) <= 100


schema AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0:
    """
    Table to create

    Attributes
    ----------
    columns : [AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0ColumnsItems0], default is Undefined, required
        Table columns
    data_format : str, default is Undefined, required
        Message data format
    group_name : str, default is Undefined, required
        Kafka consumers group
    name : str, default is Undefined, required
        Name of the table
    topics : [AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0TopicsItems0], default is Undefined, required
        Kafka topics
    """


    columns: [AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0ColumnsItems0]

    data_format: "Avro" | "CSV" | "JSONAsString" | "JSONCompactEachRow" | "JSONCompactStringsEachRow" | "JSONEachRow" | "JSONStringsEachRow" | "MsgPack" | "TSKV" | "TSV" | "TabSeparated" | "RawBLOB"

    group_name: str

    name: str

    topics: [AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0TopicsItems0]


    check:
        len(columns) <= 100
        len(group_name) <= 249
        len(group_name) >= 1
        len(name) <= 40
        len(name) >= 1
        len(topics) <= 100


schema AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0ColumnsItems0:
    """
    Table column

    Attributes
    ----------
    name : str, default is Undefined, required
        Column name
    $type : str, default is Undefined, required
        Column type
    """


    name: str

    $type: str


    check:
        len(name) <= 40
        len(name) >= 1
        len($type) <= 1000
        len($type) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecClickhouseKafkaTablesItems0TopicsItems0:
    """
    Kafka topic

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the topic
    """


    name: str


    check:
        len(name) <= 249
        len(name) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecClickhousePostgresql:
    """
    Clickhouse PostgreSQL configuration values

    Attributes
    ----------
    databases : [AivenIoV1alpha1ServiceIntegrationSpecClickhousePostgresqlDatabasesItems0], default is Undefined, optional
        Databases to expose
    """


    databases?: [AivenIoV1alpha1ServiceIntegrationSpecClickhousePostgresqlDatabasesItems0]


    check:
        len(databases) <= 10


schema AivenIoV1alpha1ServiceIntegrationSpecClickhousePostgresqlDatabasesItems0:
    """
    Database to expose

    Attributes
    ----------
    database : str, default is Undefined, optional
        PostgreSQL database to expose
    $schema : str, default is Undefined, optional
        PostgreSQL schema to expose
    """


    database?: str

    $schema?: str


    check:
        len(database) <= 63
        len(database) >= 1
        len($schema) <= 63
        len($schema) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecDatadog:
    """
    Datadog specific user configuration options

    Attributes
    ----------
    datadog_dbm_enabled : bool, default is Undefined, optional
        Enable Datadog Database Monitoring
    datadog_tags : [AivenIoV1alpha1ServiceIntegrationSpecDatadogDatadogTagsItems0], default is Undefined, optional
        Custom tags provided by user
    exclude_consumer_groups : [str], default is Undefined, optional
        List of custom metrics
    exclude_topics : [str], default is Undefined, optional
        List of topics to exclude
    include_consumer_groups : [str], default is Undefined, optional
        List of custom metrics
    include_topics : [str], default is Undefined, optional
        List of topics to include
    kafka_custom_metrics : [str], default is Undefined, optional
        List of custom metrics
    max_jmx_metrics : int, default is Undefined, optional
        Maximum number of JMX metrics to send
    opensearch : AivenIoV1alpha1ServiceIntegrationSpecDatadogOpensearch, default is Undefined, optional
        opensearch
    """


    datadog_dbm_enabled?: bool

    datadog_tags?: [AivenIoV1alpha1ServiceIntegrationSpecDatadogDatadogTagsItems0]

    exclude_consumer_groups?: [str]

    exclude_topics?: [str]

    include_consumer_groups?: [str]

    include_topics?: [str]

    kafka_custom_metrics?: [str]

    max_jmx_metrics?: int

    opensearch?: AivenIoV1alpha1ServiceIntegrationSpecDatadogOpensearch


    check:
        len(datadog_tags) <= 32
        len(exclude_consumer_groups) <= 1024
        len(exclude_topics) <= 1024
        len(include_consumer_groups) <= 1024
        len(include_topics) <= 1024
        len(kafka_custom_metrics) <= 1024
        max_jmx_metrics <= 100000
        max_jmx_metrics >= 10


schema AivenIoV1alpha1ServiceIntegrationSpecDatadogDatadogTagsItems0:
    """
    Datadog tag defined by user

    Attributes
    ----------
    comment : str, default is Undefined, optional
        Optional tag explanation
    tag : str, default is Undefined, required
        Tag format and usage are described here: https://docs.datadoghq.com/getting_started/tagging. Tags with prefix 'aiven-' are reserved for Aiven.
    """


    comment?: str

    tag: str


    check:
        len(comment) <= 1024
        len(tag) <= 200
        len(tag) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecDatadogOpensearch:
    """
    Datadog Opensearch Options

    Attributes
    ----------
    index_stats_enabled : bool, default is Undefined, optional
        Enable Datadog Opensearch Index Monitoring
    pending_task_stats_enabled : bool, default is Undefined, optional
        Enable Datadog Opensearch Pending Task Monitoring
    pshard_stats_enabled : bool, default is Undefined, optional
        Enable Datadog Opensearch Primary Shard Monitoring
    """


    index_stats_enabled?: bool

    pending_task_stats_enabled?: bool

    pshard_stats_enabled?: bool


schema AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetrics:
    """
    External AWS CloudWatch Metrics integration Logs configuration values

    Attributes
    ----------
    dropped_metrics : [AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetricsDroppedMetricsItems0], default is Undefined, optional
        Metrics to not send to AWS CloudWatch (takes precedence over extra_metrics)
    extra_metrics : [AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetricsExtraMetricsItems0], default is Undefined, optional
        Metrics to allow through to AWS CloudWatch (in addition to default metrics)
    """


    dropped_metrics?: [AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetricsDroppedMetricsItems0]

    extra_metrics?: [AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetricsExtraMetricsItems0]


    check:
        len(dropped_metrics) <= 1024
        len(extra_metrics) <= 1024


schema AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetricsDroppedMetricsItems0:
    """
    Metric name and subfield

    Attributes
    ----------
    field : str, default is Undefined, required
        Identifier of a value in the metric
    metric : str, default is Undefined, required
        Identifier of the metric
    """


    field: str

    metric: str


    check:
        len(field) <= 1000
        len(metric) <= 1000


schema AivenIoV1alpha1ServiceIntegrationSpecExternalAWSCloudwatchMetricsExtraMetricsItems0:
    """
    Metric name and subfield

    Attributes
    ----------
    field : str, default is Undefined, required
        Identifier of a value in the metric
    metric : str, default is Undefined, required
        Identifier of the metric
    """


    field: str

    metric: str


    check:
        len(field) <= 1000
        len(metric) <= 1000


schema AivenIoV1alpha1ServiceIntegrationSpecKafkaConnect:
    """
    Kafka Connect service configuration values

    Attributes
    ----------
    kafka_connect : AivenIoV1alpha1ServiceIntegrationSpecKafkaConnectKafkaConnect, default is Undefined, optional
        kafka connect
    """


    kafka_connect?: AivenIoV1alpha1ServiceIntegrationSpecKafkaConnectKafkaConnect


schema AivenIoV1alpha1ServiceIntegrationSpecKafkaConnectKafkaConnect:
    """
    Kafka Connect service configuration values

    Attributes
    ----------
    config_storage_topic : str, default is Undefined, optional
        The name of the topic where connector and task configuration data are stored.This must be the same for all workers with the same group_id.
    group_id : str, default is Undefined, optional
        A unique string that identifies the Connect cluster group this worker belongs to.
    offset_storage_topic : str, default is Undefined, optional
        The name of the topic where connector and task configuration offsets are stored.This must be the same for all workers with the same group_id.
    status_storage_topic : str, default is Undefined, optional
        The name of the topic where connector and task configuration status updates are stored.This must be the same for all workers with the same group_id.
    """


    config_storage_topic?: str

    group_id?: str

    offset_storage_topic?: str

    status_storage_topic?: str


    check:
        len(config_storage_topic) <= 249
        len(group_id) <= 249
        len(offset_storage_topic) <= 249
        len(status_storage_topic) <= 249


schema AivenIoV1alpha1ServiceIntegrationSpecKafkaLogs:
    """
    Kafka logs configuration values

    Attributes
    ----------
    kafka_topic : str, default is Undefined, required
        Topic name
    """


    kafka_topic: str


    check:
        len(kafka_topic) <= 249
        len(kafka_topic) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecKafkaMirrormaker:
    """
    Kafka MirrorMaker configuration values

    Attributes
    ----------
    cluster_alias : str, default is Undefined, optional
        The alias under which the Kafka cluster is known to MirrorMaker. Can contain the following symbols: ASCII alphanumerics, '.', '_', and '-'.
    kafka_mirrormaker : AivenIoV1alpha1ServiceIntegrationSpecKafkaMirrormakerKafkaMirrormaker, default is Undefined, optional
        kafka mirrormaker
    """


    cluster_alias?: str

    kafka_mirrormaker?: AivenIoV1alpha1ServiceIntegrationSpecKafkaMirrormakerKafkaMirrormaker


    check:
        len(cluster_alias) <= 128
        regex.match(str(cluster_alias), r"^[a-zA-Z0-9_.-]+$") if cluster_alias


schema AivenIoV1alpha1ServiceIntegrationSpecKafkaMirrormakerKafkaMirrormaker:
    """
    Kafka MirrorMaker configuration values

    Attributes
    ----------
    consumer_fetch_min_bytes : int, default is Undefined, optional
        The minimum amount of data the server should return for a fetch request
    producer_batch_size : int, default is Undefined, optional
        The batch size in bytes producer will attempt to collect before publishing to broker.
    producer_buffer_memory : int, default is Undefined, optional
        The amount of bytes producer can use for buffering data before publishing to broker.
    producer_linger_ms : int, default is Undefined, optional
        The linger time (ms) for waiting new data to arrive for publishing.
    producer_max_request_size : int, default is Undefined, optional
        The maximum request size in bytes.
    """


    consumer_fetch_min_bytes?: int

    producer_batch_size?: int

    producer_buffer_memory?: int

    producer_linger_ms?: int

    producer_max_request_size?: int


    check:
        consumer_fetch_min_bytes <= 5.24288e+06
        consumer_fetch_min_bytes >= 1
        producer_batch_size <= 5.24288e+06
        producer_batch_size >= 0
        producer_buffer_memory <= 1.34217728e+08
        producer_buffer_memory >= 5.24288e+06
        producer_linger_ms <= 5000
        producer_linger_ms >= 0
        producer_max_request_size <= 6.7108864e+07
        producer_max_request_size >= 0


schema AivenIoV1alpha1ServiceIntegrationSpecLogs:
    """
    Logs configuration values

    Attributes
    ----------
    elasticsearch_index_days_max : int, default is Undefined, optional
        Elasticsearch index retention limit
    elasticsearch_index_prefix : str, default is Undefined, optional
        Elasticsearch index prefix
    """


    elasticsearch_index_days_max?: int

    elasticsearch_index_prefix?: str


    check:
        elasticsearch_index_days_max <= 10000
        elasticsearch_index_days_max >= 1
        len(elasticsearch_index_prefix) <= 1024
        len(elasticsearch_index_prefix) >= 1


schema AivenIoV1alpha1ServiceIntegrationSpecMetrics:
    """
    Metrics configuration values

    Attributes
    ----------
    database : str, default is Undefined, optional
        Name of the database where to store metric datapoints. Only affects PostgreSQL destinations. Defaults to 'metrics'. Note that this must be the same for all metrics integrations that write data to the same PostgreSQL service.
    retention_days : int, default is Undefined, optional
        Number of days to keep old metrics. Only affects PostgreSQL destinations. Set to 0 for no automatic cleanup. Defaults to 30 days.
    ro_username : str, default is Undefined, optional
        Name of a user that can be used to read metrics. This will be used for Grafana integration (if enabled) to prevent Grafana users from making undesired changes. Only affects PostgreSQL destinations. Defaults to 'metrics_reader'. Note that this must be the same for all metrics integrations that write data to the same PostgreSQL service.
    source_mysql : AivenIoV1alpha1ServiceIntegrationSpecMetricsSourceMysql, default is Undefined, optional
        source mysql
    username : str, default is Undefined, optional
        Name of the user used to write metrics. Only affects PostgreSQL destinations. Defaults to 'metrics_writer'. Note that this must be the same for all metrics integrations that write data to the same PostgreSQL service.
    """


    database?: str

    retention_days?: int

    ro_username?: str

    source_mysql?: AivenIoV1alpha1ServiceIntegrationSpecMetricsSourceMysql

    username?: str


    check:
        len(database) <= 40
        regex.match(str(database), r"^[_A-Za-z0-9][-_A-Za-z0-9]{0,39}$") if database
        retention_days <= 10000
        retention_days >= 0
        len(ro_username) <= 40
        regex.match(str(ro_username), r"^[_A-Za-z0-9][-._A-Za-z0-9]{0,39}$") if ro_username
        len(username) <= 40
        regex.match(str(username), r"^[_A-Za-z0-9][-._A-Za-z0-9]{0,39}$") if username


schema AivenIoV1alpha1ServiceIntegrationSpecMetricsSourceMysql:
    """
    Configuration options for metrics where source service is MySQL

    Attributes
    ----------
    telegraf : AivenIoV1alpha1ServiceIntegrationSpecMetricsSourceMysqlTelegraf, default is Undefined, optional
        telegraf
    """


    telegraf?: AivenIoV1alpha1ServiceIntegrationSpecMetricsSourceMysqlTelegraf


schema AivenIoV1alpha1ServiceIntegrationSpecMetricsSourceMysqlTelegraf:
    """
    Configuration options for Telegraf MySQL input plugin

    Attributes
    ----------
    gather_event_waits : bool, default is Undefined, optional
        Gather metrics from PERFORMANCE_SCHEMA.EVENT_WAITS
    gather_file_events_stats : bool, default is Undefined, optional
        gather metrics from PERFORMANCE_SCHEMA.FILE_SUMMARY_BY_EVENT_NAME
    gather_index_io_waits : bool, default is Undefined, optional
        Gather metrics from PERFORMANCE_SCHEMA.TABLE_IO_WAITS_SUMMARY_BY_INDEX_USAGE
    gather_info_schema_auto_inc : bool, default is Undefined, optional
        Gather auto_increment columns and max values from information schema
    gather_innodb_metrics : bool, default is Undefined, optional
        Gather metrics from INFORMATION_SCHEMA.INNODB_METRICS
    gather_perf_events_statements : bool, default is Undefined, optional
        Gather metrics from PERFORMANCE_SCHEMA.EVENTS_STATEMENTS_SUMMARY_BY_DIGEST
    gather_process_list : bool, default is Undefined, optional
        Gather thread state counts from INFORMATION_SCHEMA.PROCESSLIST
    gather_slave_status : bool, default is Undefined, optional
        Gather metrics from SHOW SLAVE STATUS command output
    gather_table_io_waits : bool, default is Undefined, optional
        Gather metrics from PERFORMANCE_SCHEMA.TABLE_IO_WAITS_SUMMARY_BY_TABLE
    gather_table_lock_waits : bool, default is Undefined, optional
        Gather metrics from PERFORMANCE_SCHEMA.TABLE_LOCK_WAITS
    gather_table_schema : bool, default is Undefined, optional
        Gather metrics from INFORMATION_SCHEMA.TABLES
    perf_events_statements_digest_text_limit : int, default is Undefined, optional
        Truncates digest text from perf_events_statements into this many characters
    perf_events_statements_limit : int, default is Undefined, optional
        Limits metrics from perf_events_statements
    perf_events_statements_time_limit : int, default is Undefined, optional
        Only include perf_events_statements whose last seen is less than this many seconds
    """


    gather_event_waits?: bool

    gather_file_events_stats?: bool

    gather_index_io_waits?: bool

    gather_info_schema_auto_inc?: bool

    gather_innodb_metrics?: bool

    gather_perf_events_statements?: bool

    gather_process_list?: bool

    gather_slave_status?: bool

    gather_table_io_waits?: bool

    gather_table_lock_waits?: bool

    gather_table_schema?: bool

    perf_events_statements_digest_text_limit?: int

    perf_events_statements_limit?: int

    perf_events_statements_time_limit?: int


    check:
        perf_events_statements_digest_text_limit <= 2048
        perf_events_statements_digest_text_limit >= 1
        perf_events_statements_limit <= 4000
        perf_events_statements_limit >= 1
        perf_events_statements_time_limit <= 2.592e+06
        perf_events_statements_time_limit >= 1


schema AivenIoV1alpha1ServiceIntegrationStatus:
    """
    ServiceIntegrationStatus defines the observed state of ServiceIntegration

    Attributes
    ----------
    conditions : [AivenIoV1alpha1ServiceIntegrationStatusConditionsItems0], default is Undefined, required
        Conditions represent the latest available observations of an ServiceIntegration state
    id : str, default is Undefined, required
        Service integration ID
    """


    conditions: [AivenIoV1alpha1ServiceIntegrationStatusConditionsItems0]

    id: str


schema AivenIoV1alpha1ServiceIntegrationStatusConditionsItems0:
    """
    Condition contains details for one aspect of the current state of this API Resource. --- This struct is intended for direct use as an array at the field path .status.conditions.  For example, 
     type FooStatus struct{ // Represents the observations of a foo's current state. // Known .status.conditions.type are: "Available", "Progressing", and "Degraded" // +patchMergeKey=type // +patchStrategy=merge // +listType=map // +listMapKey=type Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type" protobuf:"bytes,1,rep,name=conditions"` 
     // other fields }

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another. This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition. This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon. For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition. Producers of specific condition types may define expected values and meanings for this field, and whether the values are considered a guaranteed API. The value should be a CamelCase string. This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase. --- Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be useful (see .node.status.conditions), the ability to deconflict is important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
    """


    lastTransitionTime: str

    message: str

    observedGeneration?: int

    reason: str

    status: "True" | "False" | "Unknown"

    $type: str


    check:
        len(message) <= 32768
        observedGeneration >= 0
        len(reason) <= 1024
        len(reason) >= 1
        regex.match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        regex.match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


