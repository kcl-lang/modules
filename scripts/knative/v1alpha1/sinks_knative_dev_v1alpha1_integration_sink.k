"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema IntegrationSink:
    r"""
    IntegrationSink sends events to generic event sink

    Attributes
    ----------
    apiVersion : str, default is "sinks.knative.dev/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "IntegrationSink", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SinksKnativeDevV1alpha1IntegrationSinkSpec, default is Undefined, optional
        spec
    status : SinksKnativeDevV1alpha1IntegrationSinkStatus, default is Undefined, optional
        status
    """


    apiVersion: "sinks.knative.dev/v1alpha1" = "sinks.knative.dev/v1alpha1"

    kind: "IntegrationSink" = "IntegrationSink"

    metadata?: v1.ObjectMeta

    spec?: SinksKnativeDevV1alpha1IntegrationSinkSpec

    status?: SinksKnativeDevV1alpha1IntegrationSinkStatus


schema SinksKnativeDevV1alpha1IntegrationSinkSpec:
    r"""
    Spec defines the desired state of the IntegrationSink.

    Attributes
    ----------
    aws : SinksKnativeDevV1alpha1IntegrationSinkSpecAws, default is Undefined, optional
        aws
    log : SinksKnativeDevV1alpha1IntegrationSinkSpecLog, default is Undefined, optional
        log
    """


    aws?: SinksKnativeDevV1alpha1IntegrationSinkSpecAws

    log?: SinksKnativeDevV1alpha1IntegrationSinkSpecLog


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAws:
    r"""
    sinks knative dev v1alpha1 integration sink spec aws

    Attributes
    ----------
    auth : SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuth, default is Undefined, optional
        auth
    s3 : SinksKnativeDevV1alpha1IntegrationSinkSpecAwsS3, default is Undefined, optional
        s3
    sns : SinksKnativeDevV1alpha1IntegrationSinkSpecAwsSns, default is Undefined, optional
        sns
    sqs : SinksKnativeDevV1alpha1IntegrationSinkSpecAwsSqs, default is Undefined, optional
        sqs
    """


    auth?: SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuth

    s3?: SinksKnativeDevV1alpha1IntegrationSinkSpecAwsS3

    sns?: SinksKnativeDevV1alpha1IntegrationSinkSpecAwsSns

    sqs?: SinksKnativeDevV1alpha1IntegrationSinkSpecAwsSqs


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuth:
    r"""
    Auth configurations

    Attributes
    ----------
    secret : SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuthSecret, default is Undefined, optional
        secret
    """


    secret?: SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuthSecret


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuthSecret:
    r"""
    Auth secret

    Attributes
    ----------
    ref : SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuthSecretRef, default is Undefined, optional
        ref
    """


    ref?: SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuthSecretRef


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAwsAuthSecretRef:
    r"""
    Secret reference.


    Attributes
    ----------
    name : str, default is Undefined, required
        Secret name
    """


    name: str


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAwsS3:
    r"""
    sinks knative dev v1alpha1 integration sink spec aws s3

    Attributes
    ----------
    arn : str, default is Undefined, optional
        Bucket Name        The S3 Bucket name or Amazon Resource Name (ARN).
    autoCreateBucket : bool, default is Undefined, optional
        Autocreate Bucket        Specifies to automatically create the S3 bucket.
    delay : int, default is 500, optional
        Delay        The number of milliseconds before the next poll of the selected bucket.
    deleteAfterRead : bool, default is True, optional
        Auto-delete Objects        Specifies to delete objects after consuming them.
    destinationBucket : str, default is Undefined, optional
        Destination Bucket        Define the destination bucket where an object must be moved when moveAfterRead is set to true.
    destinationBucketPrefix : str, default is Undefined, optional
        Destination Bucket Prefix        Define the destination bucket prefix to use when an object must be moved, and moveAfterRead is set to true.
    destinationBucketSuffix : str, default is Undefined, optional
        Destination Bucket Suffix        Define the destination bucket suffix to use when an object must be moved, and moveAfterRead is set to true.
    forcePathStyle : bool, default is Undefined, optional
        Force Path Style        Forces path style when accessing AWS S3 buckets.
    ignoreBody : bool, default is Undefined, optional
        Ignore Body        If true, the S3 Object body is ignored. Setting this to true overrides any behavior defined by the `includeBody` option. If false, the S3 object is put in the body.
    maxMessagesPerPoll : int, default is 10, optional
        Max Messages Per Poll        Gets the maximum number of messages as a limit to poll at each polling. Gets the maximum number of messages as a limit to poll at each polling. The default value is 10. Use 0 or a negative number to set it as unlimited.
    moveAfterRead : bool, default is Undefined, optional
        Move Objects After Delete        Move objects from S3 bucket to a different bucket after they have been retrieved.
    overrideEndpoint : bool, default is Undefined, optional
        Endpoint Overwrite        Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
    prefix : str, default is Undefined, optional
        Prefix        The AWS S3 bucket prefix to consider while searching.
    region : str, default is Undefined, optional
        AWS Region        The AWS region to access.
    uriEndpointOverride : str, default is Undefined, optional
        Overwrite Endpoint URI        The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
    """


    arn?: str

    autoCreateBucket?: bool = False

    delay?: int = 500

    deleteAfterRead?: bool = True

    destinationBucket?: str

    destinationBucketPrefix?: str

    destinationBucketSuffix?: str

    forcePathStyle?: bool = False

    ignoreBody?: bool = False

    maxMessagesPerPoll?: int = 10

    moveAfterRead?: bool = False

    overrideEndpoint?: bool = False

    prefix?: str

    region?: str

    uriEndpointOverride?: str


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAwsSns:
    r"""
    sinks knative dev v1alpha1 integration sink spec aws sns

    Attributes
    ----------
    arn : str, default is Undefined, optional
        Topic Name        The SNS topic name name or Amazon Resource Name (ARN).
    autoCreateTopic : bool, default is Undefined, optional
        Autocreate Topic        Setting the autocreation of the SNS topic.
    overrideEndpoint : bool, default is Undefined, optional
        Endpoint Overwrite        Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
    region : str, default is Undefined, optional
        AWS Region        The AWS region to access.
    uriEndpointOverride : str, default is Undefined, optional
        Overwrite Endpoint URI        The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
    """


    arn?: str

    autoCreateTopic?: bool = False

    overrideEndpoint?: bool = False

    region?: str

    uriEndpointOverride?: str


schema SinksKnativeDevV1alpha1IntegrationSinkSpecAwsSqs:
    r"""
    sinks knative dev v1alpha1 integration sink spec aws sqs

    Attributes
    ----------
    arn : str, default is Undefined, optional
        Queue Name        The SQS Queue Name or ARN
    autoCreateQueue : bool, default is Undefined, optional
        Autocreate Queue        Setting the autocreation of the SQS queue.
    delay : int, default is 500, optional
        Delay        The number of milliseconds before the next poll of the selected stream
    deleteAfterRead : bool, default is True, optional
        Auto-delete Messages        Delete messages after consuming them
    greedy : bool, default is Undefined, optional
        Greedy Scheduler        If greedy is enabled, then the polling will happen immediately again, if the previous run polled 1 or more messages.
    host : str, default is "amazonaws.com", optional
        AWS Host        The hostname of the Amazon AWS cloud.
    maxMessagesPerPoll : int, default is 1, optional
        Max Messages Per Poll        The maximum number of messages to return. Amazon SQS never returns more messages than this value (however, fewer messages might be returned). Valid values 1 to 10. Default 1.
    overrideEndpoint : bool, default is Undefined, optional
        Endpoint Overwrite        Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
    $protocol : str, default is "https", optional
        Protocol        The underlying protocol used to communicate with SQS
    queueURL : str, default is Undefined, optional
        Queue URL        The full SQS Queue URL (required if using KEDA)
    region : str, default is Undefined, optional
        AWS Region        The AWS region to access.
    uriEndpointOverride : str, default is Undefined, optional
        Overwrite Endpoint URI        The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
    visibilityTimeout : int, default is Undefined, optional
        Visibility Timeout        The duration (in seconds) that the received messages are hidden from subsequent retrieve requests after being retrieved by a ReceiveMessage request.
    waitTimeSeconds : int, default is Undefined, optional
        Wait Time Seconds        The duration (in seconds) for which the call waits for a message to arrive in the queue before returning. If a message is available, the call returns sooner than WaitTimeSeconds. If no messages are available and the wait time expires, the call does not return a message list.
    """


    arn?: str

    autoCreateQueue?: bool = False

    delay?: int = 500

    deleteAfterRead?: bool = True

    greedy?: bool = False

    host?: str = "amazonaws.com"

    maxMessagesPerPoll?: int = 1

    overrideEndpoint?: bool = False

    $protocol?: str = "https"

    queueURL?: str

    region?: str

    uriEndpointOverride?: str

    visibilityTimeout?: int

    waitTimeSeconds?: int


schema SinksKnativeDevV1alpha1IntegrationSinkSpecLog:
    r"""
    sinks knative dev v1alpha1 integration sink spec log

    Attributes
    ----------
    level : str, default is "INFO", optional
        Log Level        Logging level to use
    logMask : bool, default is Undefined, optional
        Log Mask        Mask sensitive information like password or passphrase in the log
    loggerName : str, default is "log-sink", optional
        Logger Name        Name of the logging category to use
    marker : str, default is Undefined, optional
        Marker        An optional Marker name to use
    multiline : bool, default is Undefined, optional
        Multiline        If enabled then each information is outputted on a newline
    showAllProperties : bool, default is Undefined, optional
        Show All Properties        Show all of the exchange properties (both internal and custom)
    showBody : bool, default is True, optional
        Show Body        Show the message body
    showBodyType : bool, default is True, optional
        Show Body Type        Show the body Java type
    showCachedStreams : bool, default is True, optional
        Show Cached Streams        Whether Camel should show cached stream bodies or not.
    showExchangePattern : bool, default is True, optional
        Show Exchange Pattern        Shows the Message Exchange Pattern (or MEP for short)
    showHeaders : bool, default is Undefined, optional
        Show Headers        Show the headers received
    showProperties : bool, default is Undefined, optional
        Show Properties        Show the exchange properties (only custom). Use showAllProperties to show both internal and custom properties.
    showStreams : bool, default is Undefined, optional
        Show Streams        Show the stream bodies (they may not be available in following steps)
    """


    level?: str = "INFO"

    logMask?: bool = False

    loggerName?: str = "log-sink"

    marker?: str

    multiline?: bool = False

    showAllProperties?: bool = False

    showBody?: bool = True

    showBodyType?: bool = True

    showCachedStreams?: bool = True

    showExchangePattern?: bool = True

    showHeaders?: bool = False

    showProperties?: bool = False

    showStreams?: bool = False


schema SinksKnativeDevV1alpha1IntegrationSinkStatus:
    r"""
    Status represents the current state of the IntegrationSink. This data may be out of date.

    Attributes
    ----------
    address : SinksKnativeDevV1alpha1IntegrationSinkStatusAddress, default is Undefined, optional
        address
    addresses : [SinksKnativeDevV1alpha1IntegrationSinkStatusAddressesItems0], default is Undefined, optional
        IntegrationSink is Addressable. It exposes the endpoints as URIs to get events delivered into the used Kamelet.
    annotations : any, default is Undefined, optional
        Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
    conditions : [SinksKnativeDevV1alpha1IntegrationSinkStatusConditionsItems0], default is Undefined, optional
        Conditions the latest available observations of a resource's current state.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
    policies : [SinksKnativeDevV1alpha1IntegrationSinkStatusPoliciesItems0], default is Undefined, optional
        List of applied EventPolicies
    """


    address?: SinksKnativeDevV1alpha1IntegrationSinkStatusAddress

    addresses?: [SinksKnativeDevV1alpha1IntegrationSinkStatusAddressesItems0]

    annotations?: any

    conditions?: [SinksKnativeDevV1alpha1IntegrationSinkStatusConditionsItems0]

    observedGeneration?: int

    policies?: [SinksKnativeDevV1alpha1IntegrationSinkStatusPoliciesItems0]


schema SinksKnativeDevV1alpha1IntegrationSinkStatusAddress:
    r"""
    IntegrationSink is Addressable. It exposes the endpoints as URIs to get events delivered into the used Kamelet.

    Attributes
    ----------
    CACerts : str, default is Undefined, optional
        c a certs
    audience : str, default is Undefined, optional
        audience
    name : str, default is Undefined, optional
        name
    url : str, default is Undefined, optional
        url
    """


    CACerts?: str

    audience?: str

    name?: str

    url?: str


schema SinksKnativeDevV1alpha1IntegrationSinkStatusAddressesItems0:
    r"""
    sinks knative dev v1alpha1 integration sink status addresses items0

    Attributes
    ----------
    CACerts : str, default is Undefined, optional
        c a certs
    audience : str, default is Undefined, optional
        audience
    name : str, default is Undefined, optional
        name
    url : str, default is Undefined, optional
        url
    """


    CACerts?: str

    audience?: str

    name?: str

    url?: str


schema SinksKnativeDevV1alpha1IntegrationSinkStatusConditionsItems0:
    r"""
    sinks knative dev v1alpha1 integration sink status conditions items0

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
    message : str, default is Undefined, optional
        A human readable message indicating details about the transition.
    reason : str, default is Undefined, optional
        The reason for the condition's last transition.
    severity : str, default is Undefined, optional
        Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
    status : str, default is Undefined, required
        Status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        Type of condition.
    """


    lastTransitionTime?: str

    message?: str

    reason?: str

    severity?: str

    status: str

    $type: str


schema SinksKnativeDevV1alpha1IntegrationSinkStatusPoliciesItems0:
    r"""
    sinks knative dev v1alpha1 integration sink status policies items0

    Attributes
    ----------
    apiVersion : str, default is Undefined, optional
        The API version of the applied EventPolicy. This indicates, which version of EventPolicy is supported by the resource.
    name : str, default is Undefined, optional
        The name of the applied EventPolicy
    """


    apiVersion?: str

    name?: str


