"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Gateway:
    """
    networking istio io v1 gateway

    Attributes
    ----------
    apiVersion : str, default is "networking.istio.io/v1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Gateway", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : NetworkingIstioIoV1GatewaySpec, default is Undefined, optional
        spec
    status : any, default is Undefined, optional
        status
    """


    apiVersion: "networking.istio.io/v1" = "networking.istio.io/v1"

    kind: "Gateway" = "Gateway"

    metadata?: v1.ObjectMeta

    spec?: NetworkingIstioIoV1GatewaySpec

    status?: any


schema NetworkingIstioIoV1GatewaySpec:
    """
    Configuration affecting edge load balancer. See more details at: https://istio.io/docs/reference/config/networking/gateway.html

    Attributes
    ----------
    selector : {str:str}, default is Undefined, optional
        One or more labels that indicate a specific set of pods/VMs on which this gateway configuration should be applied.
    servers : [NetworkingIstioIoV1GatewaySpecServersItems0], default is Undefined, optional
        A list of server specifications.
    """


    selector?: {str:str}

    servers?: [NetworkingIstioIoV1GatewaySpecServersItems0]


schema NetworkingIstioIoV1GatewaySpecServersItems0:
    """
    networking istio io v1 gateway spec servers items0

    Attributes
    ----------
    bind : str, default is Undefined, optional
        The ip or the Unix domain socket to which the listener should be bound to.
    defaultEndpoint : str, default is Undefined, optional
        default endpoint
    hosts : [str], default is Undefined, required
        One or more hosts exposed by this gateway.
    name : str, default is Undefined, optional
        An optional name of the server, when set must be unique across all servers.
    port : NetworkingIstioIoV1GatewaySpecServersItems0Port, default is Undefined, required
        port
    tls : NetworkingIstioIoV1GatewaySpecServersItems0TLS, default is Undefined, optional
        tls
    """


    bind?: str

    defaultEndpoint?: str

    hosts: [str]

    name?: str

    port: NetworkingIstioIoV1GatewaySpecServersItems0Port

    tls?: NetworkingIstioIoV1GatewaySpecServersItems0TLS


schema NetworkingIstioIoV1GatewaySpecServersItems0Port:
    """
    The Port on which the proxy should listen for incoming connections.

    Attributes
    ----------
    name : str, default is Undefined, required
        Label assigned to the port.
    number : int, default is Undefined, required
        A valid non-negative integer port number.
    $protocol : str, default is Undefined, required
        The protocol exposed on the port.
    targetPort : int, default is Undefined, optional
        target port
    """


    name: str

    number: int

    $protocol: str

    targetPort?: int


    check:
        number <= 4.294967295e+09
        number >= 0
        targetPort <= 4.294967295e+09
        targetPort >= 0


schema NetworkingIstioIoV1GatewaySpecServersItems0TLS:
    """
    Set of TLS related options that govern the server's behavior.

    Attributes
    ----------
    caCertificates : str, default is Undefined, optional
        REQUIRED if mode is `MUTUAL` or `OPTIONAL_MUTUAL`.
    caCrl : str, default is Undefined, optional
        OPTIONAL: The path to the file containing the certificate revocation list (CRL) to use in verifying a presented client side certificate.
    cipherSuites : [str], default is Undefined, optional
        Optional: If specified, only support the specified cipher list.
    credentialName : str, default is Undefined, optional
        For gateways running on Kubernetes, the name of the secret that holds the TLS certs including the CA certificates.
    httpsRedirect : bool, default is Undefined, optional
        If set to true, the load balancer will send a 301 redirect for all http connections, asking the clients to use HTTPS.
    maxProtocolVersion : str, default is Undefined, optional
        Optional: Maximum TLS protocol version.

        Valid Options: TLS_AUTO, TLSV1_0, TLSV1_1, TLSV1_2, TLSV1_3
    minProtocolVersion : str, default is Undefined, optional
        Optional: Minimum TLS protocol version.

        Valid Options: TLS_AUTO, TLSV1_0, TLSV1_1, TLSV1_2, TLSV1_3
    mode : str, default is Undefined, optional
        Optional: Indicates whether connections to this port should be secured using TLS.

        Valid Options: PASSTHROUGH, SIMPLE, MUTUAL, AUTO_PASSTHROUGH, ISTIO_MUTUAL, OPTIONAL_MUTUAL
    privateKey : str, default is Undefined, optional
        REQUIRED if mode is `SIMPLE` or `MUTUAL`.
    serverCertificate : str, default is Undefined, optional
        REQUIRED if mode is `SIMPLE` or `MUTUAL`.
    subjectAltNames : [str], default is Undefined, optional
        A list of alternate names to verify the subject identity in the certificate presented by the client.
    verifyCertificateHash : [str], default is Undefined, optional
        An optional list of hex-encoded SHA-256 hashes of the authorized client certificates.
    verifyCertificateSpki : [str], default is Undefined, optional
        An optional list of base64-encoded SHA-256 hashes of the SPKIs of authorized client certificates.
    """


    caCertificates?: str

    caCrl?: str

    cipherSuites?: [str]

    credentialName?: str

    httpsRedirect?: bool

    maxProtocolVersion?: "TLS_AUTO" | "TLSV1_0" | "TLSV1_1" | "TLSV1_2" | "TLSV1_3"

    minProtocolVersion?: "TLS_AUTO" | "TLSV1_0" | "TLSV1_1" | "TLSV1_2" | "TLSV1_3"

    mode?: "PASSTHROUGH" | "SIMPLE" | "MUTUAL" | "AUTO_PASSTHROUGH" | "ISTIO_MUTUAL" | "OPTIONAL_MUTUAL"

    privateKey?: str

    serverCertificate?: str

    subjectAltNames?: [str]

    verifyCertificateHash?: [str]

    verifyCertificateSpki?: [str]


