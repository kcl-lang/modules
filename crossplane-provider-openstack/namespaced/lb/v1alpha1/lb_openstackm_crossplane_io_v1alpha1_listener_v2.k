"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema ListenerV2:
    r"""
    ListenerV2 is the Schema for the ListenerV2s API. Manages a V2 listener resource within OpenStack.

    Attributes
    ----------
    apiVersion : str, default is "lb.openstack.m.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ListenerV2", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : LbOpenstackmCrossplaneIoV1alpha1ListenerV2Spec, default is Undefined, required
        spec
    status : LbOpenstackmCrossplaneIoV1alpha1ListenerV2Status, default is Undefined, optional
        status
    """


    apiVersion: "lb.openstack.m.crossplane.io/v1alpha1" = "lb.openstack.m.crossplane.io/v1alpha1"

    kind: "ListenerV2" = "ListenerV2"

    metadata?: v1.ObjectMeta

    spec: LbOpenstackmCrossplaneIoV1alpha1ListenerV2Spec

    status?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2Status


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2Spec:
    r"""
    ListenerV2Spec defines the desired state of ListenerV2

    Attributes
    ----------
    forProvider : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProvider, default is Undefined, required
        for provider
    initProvider : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProvider

    initProvider?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecProviderConfigRef

    writeConnectionSecretToRef?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecWriteConnectionSecretToRef


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProvider:
    r"""
    lb openstackm crossplane io v1alpha1 listener v2 spec for provider

    Attributes
    ----------
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the Listener. A
        valid value is true (UP) or false (DOWN).
    allowedCidrs : [str], default is Undefined, optional
        A list of CIDR blocks that are permitted to
        connect to this listener, denying all other source addresses. If not present,
        defaults to allow all.
    alpnProtocols : [str], default is Undefined, optional
        A list of ALPN protocols. Available protocols:
        http/1.0, http/1.1, h2. Supported only in Octavia minor version >=
        2.20.
    clientAuthentication : str, default is Undefined, optional
        The TLS client authentication mode.
        Available options: NONE, OPTIONAL or MANDATORY. Requires
        TERMINATED_HTTPS listener protocol and the client_ca_tls_container_ref.
        Supported only in Octavia minor version >= 2.8.
    clientCaTlsContainerRef : str, default is Undefined, optional
        The ref of the key manager service
        secret containing a PEM format client CA certificate bundle for
        TERMINATED_HTTPS listeners. Required if client_authentication is
        OPTIONAL or MANDATORY. Supported only in Octavia minor version >=
        2.8.
    clientCaTlsContainerRefRef : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefRef, default is Undefined, optional
        client ca Tls container ref ref
    clientCaTlsContainerRefSelector : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefSelector, default is Undefined, optional
        client ca Tls container ref selector
    clientCrlContainerRef : str, default is Undefined, optional
        The URI of the key manager service
        secret containing a PEM format CA revocation list file for TERMINATED_HTTPS
        listeners. Supported only in Octavia minor version >= 2.8.
    connectionLimit : float, default is Undefined, optional
        The maximum number of connections allowed for
        the Listener.
    defaultPoolId : str, default is Undefined, optional
        The ID of the default pool with which the
        Listener is associated.
    defaultTlsContainerRef : str, default is Undefined, optional
        A reference to a Barbican Secrets
        container which stores TLS information. This is required if the protocol is
        TERMINATED_HTTPS. See
        here
        for more information.
    description : str, default is Undefined, optional
        Human-readable description for the Listener.
    hstsIncludeSubdomains : bool, default is Undefined, optional
        Defines whether the
        includeSubDomains directive should be added to the
        Strict-Transport-Security HTTP response header. This requires setting the
        hsts_max_age option as well in order to become effective. Requires
        TERMINATED_HTTPS listener protocol. Supported only in Octavia minor
        version >= 2.27.
    hstsMaxAge : float, default is Undefined, optional
        The value of the max_age directive for the
        Strict-Transport-Security HTTP response header. Setting this enables HTTP
        Strict Transport Security (HSTS) for the TLS-terminated listener. Requires
        TERMINATED_HTTPS listener protocol. Supported only in Octavia minor
        version >= 2.27.
    hstsPreload : bool, default is Undefined, optional
        Defines whether the preload directive should
        be added to the Strict-Transport-Security HTTP response header. This requires
        setting the hsts_max_age option as well in order to become effective.
        Requires TERMINATED_HTTPS listener protocol. Supported only in Octavia
        minor version >= 2.27.
    insertHeaders : {str:str}, default is Undefined, optional
        The list of key value pairs representing
        headers to insert into the request before it is sent to the backend members.
        Changing this updates the headers of the existing listener.
    loadbalancerId : str, default is Undefined, optional
        The load balancer on which to provision this
        Listener. Changing this creates a new Listener.
    loadbalancerIdRef : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDRef, default is Undefined, optional
        loadbalancer Id ref
    loadbalancerIdSelector : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDSelector, default is Undefined, optional
        loadbalancer Id selector
    name : str, default is Undefined, optional
        Human-readable name for the Listener. Does not have to be
        unique.
    $protocol : str, default is Undefined, optional
        The protocol can be either TCP, HTTP, HTTPS,
        TERMINATED_HTTPS, UDP, SCTP (supported only in Octavia minor version
        >= 2.23), or PROMETHEUS (supported only in Octavia minor version >=
        2.25). Changing this creates a new Listener.
    protocolPort : float, default is Undefined, optional
        The port on which to listen for client traffic.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create a listener. If omitted, the region
        argument of the provider is used. Changing this creates a new Listener.
    sniContainerRefs : [str], default is Undefined, optional
        A list of references to Barbican Secrets
        containers which store SNI information. See
        here
        for more information.
    tags : [str], default is Undefined, optional
        A list of simple strings assigned to the pool. Available
        for Octavia minor version 2.5 or later.
    tenantId : str, default is Undefined, optional
        Required for admins. The UUID of the tenant who owns
        the Listener.  Only administrative users can specify a tenant UUID other than
        their own. Changing this creates a new Listener.
    timeoutClientData : float, default is Undefined, optional
        The client inactivity timeout in
        milliseconds.
    timeoutMemberConnect : float, default is Undefined, optional
        The member connection timeout in
        milliseconds.
    timeoutMemberData : float, default is Undefined, optional
        The member inactivity timeout in
        milliseconds.
    timeoutTcpInspect : float, default is Undefined, optional
        The time in milliseconds, to wait for
        additional TCP packets for content inspection.
    tlsCiphers : str, default is Undefined, optional
        List of ciphers in OpenSSL format
        (colon-separated). See
        https://www.openssl.org/docs/man1.1.1/man1/ciphers.html for more information.
        Supported only in Octavia minor version >= 2.15.
    tlsVersions : [str], default is Undefined, optional
        A list of TLS protocol versions. Available
        versions: TLSv1, TLSv1.1, TLSv1.2, TLSv1.3. Supported only in
        Octavia minor version >= 2.17.
    """


    adminStateUp?: bool

    allowedCidrs?: [str]

    alpnProtocols?: [str]

    clientAuthentication?: str

    clientCaTlsContainerRef?: str

    clientCaTlsContainerRefRef?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefRef

    clientCaTlsContainerRefSelector?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefSelector

    clientCrlContainerRef?: str

    connectionLimit?: float

    defaultPoolId?: str

    defaultTlsContainerRef?: str

    description?: str

    hstsIncludeSubdomains?: bool

    hstsMaxAge?: float

    hstsPreload?: bool

    insertHeaders?: {str:str}

    loadbalancerId?: str

    loadbalancerIdRef?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDRef

    loadbalancerIdSelector?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDSelector

    name?: str

    $protocol?: str

    protocolPort?: float

    region?: str

    sniContainerRefs?: [str]

    tags?: [str]

    tenantId?: str

    timeoutClientData?: float

    timeoutMemberConnect?: float

    timeoutMemberData?: float

    timeoutTcpInspect?: float

    tlsCiphers?: str

    tlsVersions?: [str]


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefRef:
    r"""
    Reference to a SecretV1 in keymanager to populate clientCaTlsContainerRef.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefRefPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefSelector:
    r"""
    Selector for a SecretV1 in keymanager to populate clientCaTlsContainerRef.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefSelectorPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderClientCaTLSContainerRefSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDRef:
    r"""
    Reference to a LoadbalancerV2 in lb to populate loadbalancerId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDRefPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDSelector:
    r"""
    Selector for a LoadbalancerV2 in lb to populate loadbalancerId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDSelectorPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecForProviderLoadbalancerIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the Listener. A
        valid value is true (UP) or false (DOWN).
    allowedCidrs : [str], default is Undefined, optional
        A list of CIDR blocks that are permitted to
        connect to this listener, denying all other source addresses. If not present,
        defaults to allow all.
    alpnProtocols : [str], default is Undefined, optional
        A list of ALPN protocols. Available protocols:
        http/1.0, http/1.1, h2. Supported only in Octavia minor version >=
        2.20.
    clientAuthentication : str, default is Undefined, optional
        The TLS client authentication mode.
        Available options: NONE, OPTIONAL or MANDATORY. Requires
        TERMINATED_HTTPS listener protocol and the client_ca_tls_container_ref.
        Supported only in Octavia minor version >= 2.8.
    clientCaTlsContainerRef : str, default is Undefined, optional
        The ref of the key manager service
        secret containing a PEM format client CA certificate bundle for
        TERMINATED_HTTPS listeners. Required if client_authentication is
        OPTIONAL or MANDATORY. Supported only in Octavia minor version >=
        2.8.
    clientCaTlsContainerRefRef : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefRef, default is Undefined, optional
        client ca Tls container ref ref
    clientCaTlsContainerRefSelector : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefSelector, default is Undefined, optional
        client ca Tls container ref selector
    clientCrlContainerRef : str, default is Undefined, optional
        The URI of the key manager service
        secret containing a PEM format CA revocation list file for TERMINATED_HTTPS
        listeners. Supported only in Octavia minor version >= 2.8.
    connectionLimit : float, default is Undefined, optional
        The maximum number of connections allowed for
        the Listener.
    defaultPoolId : str, default is Undefined, optional
        The ID of the default pool with which the
        Listener is associated.
    defaultTlsContainerRef : str, default is Undefined, optional
        A reference to a Barbican Secrets
        container which stores TLS information. This is required if the protocol is
        TERMINATED_HTTPS. See
        here
        for more information.
    description : str, default is Undefined, optional
        Human-readable description for the Listener.
    hstsIncludeSubdomains : bool, default is Undefined, optional
        Defines whether the
        includeSubDomains directive should be added to the
        Strict-Transport-Security HTTP response header. This requires setting the
        hsts_max_age option as well in order to become effective. Requires
        TERMINATED_HTTPS listener protocol. Supported only in Octavia minor
        version >= 2.27.
    hstsMaxAge : float, default is Undefined, optional
        The value of the max_age directive for the
        Strict-Transport-Security HTTP response header. Setting this enables HTTP
        Strict Transport Security (HSTS) for the TLS-terminated listener. Requires
        TERMINATED_HTTPS listener protocol. Supported only in Octavia minor
        version >= 2.27.
    hstsPreload : bool, default is Undefined, optional
        Defines whether the preload directive should
        be added to the Strict-Transport-Security HTTP response header. This requires
        setting the hsts_max_age option as well in order to become effective.
        Requires TERMINATED_HTTPS listener protocol. Supported only in Octavia
        minor version >= 2.27.
    insertHeaders : {str:str}, default is Undefined, optional
        The list of key value pairs representing
        headers to insert into the request before it is sent to the backend members.
        Changing this updates the headers of the existing listener.
    loadbalancerId : str, default is Undefined, optional
        The load balancer on which to provision this
        Listener. Changing this creates a new Listener.
    loadbalancerIdRef : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDRef, default is Undefined, optional
        loadbalancer Id ref
    loadbalancerIdSelector : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDSelector, default is Undefined, optional
        loadbalancer Id selector
    name : str, default is Undefined, optional
        Human-readable name for the Listener. Does not have to be
        unique.
    $protocol : str, default is Undefined, optional
        The protocol can be either TCP, HTTP, HTTPS,
        TERMINATED_HTTPS, UDP, SCTP (supported only in Octavia minor version
        >= 2.23), or PROMETHEUS (supported only in Octavia minor version >=
        2.25). Changing this creates a new Listener.
    protocolPort : float, default is Undefined, optional
        The port on which to listen for client traffic.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create a listener. If omitted, the region
        argument of the provider is used. Changing this creates a new Listener.
    sniContainerRefs : [str], default is Undefined, optional
        A list of references to Barbican Secrets
        containers which store SNI information. See
        here
        for more information.
    tags : [str], default is Undefined, optional
        A list of simple strings assigned to the pool. Available
        for Octavia minor version 2.5 or later.
    tenantId : str, default is Undefined, optional
        Required for admins. The UUID of the tenant who owns
        the Listener.  Only administrative users can specify a tenant UUID other than
        their own. Changing this creates a new Listener.
    timeoutClientData : float, default is Undefined, optional
        The client inactivity timeout in
        milliseconds.
    timeoutMemberConnect : float, default is Undefined, optional
        The member connection timeout in
        milliseconds.
    timeoutMemberData : float, default is Undefined, optional
        The member inactivity timeout in
        milliseconds.
    timeoutTcpInspect : float, default is Undefined, optional
        The time in milliseconds, to wait for
        additional TCP packets for content inspection.
    tlsCiphers : str, default is Undefined, optional
        List of ciphers in OpenSSL format
        (colon-separated). See
        https://www.openssl.org/docs/man1.1.1/man1/ciphers.html for more information.
        Supported only in Octavia minor version >= 2.15.
    tlsVersions : [str], default is Undefined, optional
        A list of TLS protocol versions. Available
        versions: TLSv1, TLSv1.1, TLSv1.2, TLSv1.3. Supported only in
        Octavia minor version >= 2.17.
    """


    adminStateUp?: bool

    allowedCidrs?: [str]

    alpnProtocols?: [str]

    clientAuthentication?: str

    clientCaTlsContainerRef?: str

    clientCaTlsContainerRefRef?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefRef

    clientCaTlsContainerRefSelector?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefSelector

    clientCrlContainerRef?: str

    connectionLimit?: float

    defaultPoolId?: str

    defaultTlsContainerRef?: str

    description?: str

    hstsIncludeSubdomains?: bool

    hstsMaxAge?: float

    hstsPreload?: bool

    insertHeaders?: {str:str}

    loadbalancerId?: str

    loadbalancerIdRef?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDRef

    loadbalancerIdSelector?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDSelector

    name?: str

    $protocol?: str

    protocolPort?: float

    region?: str

    sniContainerRefs?: [str]

    tags?: [str]

    tenantId?: str

    timeoutClientData?: float

    timeoutMemberConnect?: float

    timeoutMemberData?: float

    timeoutTcpInspect?: float

    tlsCiphers?: str

    tlsVersions?: [str]


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefRef:
    r"""
    Reference to a SecretV1 in keymanager to populate clientCaTlsContainerRef.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefRefPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefSelector:
    r"""
    Selector for a SecretV1 in keymanager to populate clientCaTlsContainerRef.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefSelectorPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderClientCaTLSContainerRefSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDRef:
    r"""
    Reference to a LoadbalancerV2 in lb to populate loadbalancerId.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDRefPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDSelector:
    r"""
    Selector for a LoadbalancerV2 in lb to populate loadbalancerId.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDSelectorPolicy


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecInitProviderLoadbalancerIDSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2SpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2Status:
    r"""
    ListenerV2Status defines the observed state of ListenerV2.

    Attributes
    ----------
    atProvider : LbOpenstackmCrossplaneIoV1alpha1ListenerV2StatusAtProvider, default is Undefined, optional
        at provider
    conditions : [LbOpenstackmCrossplaneIoV1alpha1ListenerV2StatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: LbOpenstackmCrossplaneIoV1alpha1ListenerV2StatusAtProvider

    conditions?: [LbOpenstackmCrossplaneIoV1alpha1ListenerV2StatusConditionsItems0]

    observedGeneration?: int


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2StatusAtProvider:
    r"""
    lb openstackm crossplane io v1alpha1 listener v2 status at provider

    Attributes
    ----------
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the Listener. A
        valid value is true (UP) or false (DOWN).
    allowedCidrs : [str], default is Undefined, optional
        A list of CIDR blocks that are permitted to
        connect to this listener, denying all other source addresses. If not present,
        defaults to allow all.
    alpnProtocols : [str], default is Undefined, optional
        A list of ALPN protocols. Available protocols:
        http/1.0, http/1.1, h2. Supported only in Octavia minor version >=
        2.20.
    clientAuthentication : str, default is Undefined, optional
        The TLS client authentication mode.
        Available options: NONE, OPTIONAL or MANDATORY. Requires
        TERMINATED_HTTPS listener protocol and the client_ca_tls_container_ref.
        Supported only in Octavia minor version >= 2.8.
    clientCaTlsContainerRef : str, default is Undefined, optional
        The ref of the key manager service
        secret containing a PEM format client CA certificate bundle for
        TERMINATED_HTTPS listeners. Required if client_authentication is
        OPTIONAL or MANDATORY. Supported only in Octavia minor version >=
        2.8.
    clientCrlContainerRef : str, default is Undefined, optional
        The URI of the key manager service
        secret containing a PEM format CA revocation list file for TERMINATED_HTTPS
        listeners. Supported only in Octavia minor version >= 2.8.
    connectionLimit : float, default is Undefined, optional
        The maximum number of connections allowed for
        the Listener.
    defaultPoolId : str, default is Undefined, optional
        The ID of the default pool with which the
        Listener is associated.
    defaultTlsContainerRef : str, default is Undefined, optional
        A reference to a Barbican Secrets
        container which stores TLS information. This is required if the protocol is
        TERMINATED_HTTPS. See
        here
        for more information.
    description : str, default is Undefined, optional
        Human-readable description for the Listener.
    hstsIncludeSubdomains : bool, default is Undefined, optional
        Defines whether the
        includeSubDomains directive should be added to the
        Strict-Transport-Security HTTP response header. This requires setting the
        hsts_max_age option as well in order to become effective. Requires
        TERMINATED_HTTPS listener protocol. Supported only in Octavia minor
        version >= 2.27.
    hstsMaxAge : float, default is Undefined, optional
        The value of the max_age directive for the
        Strict-Transport-Security HTTP response header. Setting this enables HTTP
        Strict Transport Security (HSTS) for the TLS-terminated listener. Requires
        TERMINATED_HTTPS listener protocol. Supported only in Octavia minor
        version >= 2.27.
    hstsPreload : bool, default is Undefined, optional
        Defines whether the preload directive should
        be added to the Strict-Transport-Security HTTP response header. This requires
        setting the hsts_max_age option as well in order to become effective.
        Requires TERMINATED_HTTPS listener protocol. Supported only in Octavia
        minor version >= 2.27.
    id : str, default is Undefined, optional
        The unique ID for the Listener.
    insertHeaders : {str:str}, default is Undefined, optional
        The list of key value pairs representing
        headers to insert into the request before it is sent to the backend members.
        Changing this updates the headers of the existing listener.
    loadbalancerId : str, default is Undefined, optional
        The load balancer on which to provision this
        Listener. Changing this creates a new Listener.
    name : str, default is Undefined, optional
        Human-readable name for the Listener. Does not have to be
        unique.
    $protocol : str, default is Undefined, optional
        The protocol can be either TCP, HTTP, HTTPS,
        TERMINATED_HTTPS, UDP, SCTP (supported only in Octavia minor version
        >= 2.23), or PROMETHEUS (supported only in Octavia minor version >=
        2.25). Changing this creates a new Listener.
    protocolPort : float, default is Undefined, optional
        The port on which to listen for client traffic.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create a listener. If omitted, the region
        argument of the provider is used. Changing this creates a new Listener.
    sniContainerRefs : [str], default is Undefined, optional
        A list of references to Barbican Secrets
        containers which store SNI information. See
        here
        for more information.
    tags : [str], default is Undefined, optional
        A list of simple strings assigned to the pool. Available
        for Octavia minor version 2.5 or later.
    tenantId : str, default is Undefined, optional
        Required for admins. The UUID of the tenant who owns
        the Listener.  Only administrative users can specify a tenant UUID other than
        their own. Changing this creates a new Listener.
    timeoutClientData : float, default is Undefined, optional
        The client inactivity timeout in
        milliseconds.
    timeoutMemberConnect : float, default is Undefined, optional
        The member connection timeout in
        milliseconds.
    timeoutMemberData : float, default is Undefined, optional
        The member inactivity timeout in
        milliseconds.
    timeoutTcpInspect : float, default is Undefined, optional
        The time in milliseconds, to wait for
        additional TCP packets for content inspection.
    tlsCiphers : str, default is Undefined, optional
        List of ciphers in OpenSSL format
        (colon-separated). See
        https://www.openssl.org/docs/man1.1.1/man1/ciphers.html for more information.
        Supported only in Octavia minor version >= 2.15.
    tlsVersions : [str], default is Undefined, optional
        A list of TLS protocol versions. Available
        versions: TLSv1, TLSv1.1, TLSv1.2, TLSv1.3. Supported only in
        Octavia minor version >= 2.17.
    """


    adminStateUp?: bool

    allowedCidrs?: [str]

    alpnProtocols?: [str]

    clientAuthentication?: str

    clientCaTlsContainerRef?: str

    clientCrlContainerRef?: str

    connectionLimit?: float

    defaultPoolId?: str

    defaultTlsContainerRef?: str

    description?: str

    hstsIncludeSubdomains?: bool

    hstsMaxAge?: float

    hstsPreload?: bool

    id?: str

    insertHeaders?: {str:str}

    loadbalancerId?: str

    name?: str

    $protocol?: str

    protocolPort?: float

    region?: str

    sniContainerRefs?: [str]

    tags?: [str]

    tenantId?: str

    timeoutClientData?: float

    timeoutMemberConnect?: float

    timeoutMemberData?: float

    timeoutTcpInspect?: float

    tlsCiphers?: str

    tlsVersions?: [str]


schema LbOpenstackmCrossplaneIoV1alpha1ListenerV2StatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


