"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema ClusterResourceUsage:
    """
    ClusterResourceUsage provides cluster-wide resource usage.

    Attributes
    ----------
    apiVersion : str, default is "kwok.x-k8s.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ClusterResourceUsage", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, required
        metadata
    spec : KwokxK8sIoV1alpha1ClusterResourceUsageSpec, default is Undefined, required
        spec
    status : KwokxK8sIoV1alpha1ClusterResourceUsageStatus, default is Undefined, optional
        status
    """


    apiVersion: "kwok.x-k8s.io/v1alpha1" = "kwok.x-k8s.io/v1alpha1"

    kind: "ClusterResourceUsage" = "ClusterResourceUsage"

    metadata: v1.ObjectMeta

    spec: KwokxK8sIoV1alpha1ClusterResourceUsageSpec

    status?: KwokxK8sIoV1alpha1ClusterResourceUsageStatus


schema KwokxK8sIoV1alpha1ClusterResourceUsageSpec:
    """
    Spec holds spec for cluster resource usage.

    Attributes
    ----------
    usages : [KwokxK8sIoV1alpha1ClusterResourceUsageSpecUsagesItems0], default is Undefined, optional
        Usages is a list of resource usage for the pod.
    selector : KwokxK8sIoV1alpha1ClusterResourceUsageSpecSelector, default is Undefined, optional
        selector
    """


    usages?: [KwokxK8sIoV1alpha1ClusterResourceUsageSpecUsagesItems0]

    selector?: KwokxK8sIoV1alpha1ClusterResourceUsageSpecSelector


schema KwokxK8sIoV1alpha1ClusterResourceUsageSpecSelector:
    """
    Selector is a selector to filter pods to configure.

    Attributes
    ----------
    matchNames : [str], default is Undefined, optional
        MatchNames is a list of names to match.
        if not set, all names will be matched.
    matchNamespaces : [str], default is Undefined, optional
        MatchNamespaces is a list of namespaces to match.
        if not set, all namespaces will be matched.
    """


    matchNames?: [str]

    matchNamespaces?: [str]


schema KwokxK8sIoV1alpha1ClusterResourceUsageSpecUsagesItems0:
    """
    ResourceUsageContainer holds spec for resource usage container.

    Attributes
    ----------
    containers : [str], default is Undefined, optional
        Containers is list of container names.
    usage : {str:KwokxK8sIoV1alpha1ClusterResourceUsageSpecUsagesItems0UsageAnon}, default is Undefined, optional
        Usage is a list of resource usage for the container.
    """


    containers?: [str]

    usage?: {str:KwokxK8sIoV1alpha1ClusterResourceUsageSpecUsagesItems0UsageAnon}


    check:
        all _, usage in usage { } if usage


schema KwokxK8sIoV1alpha1ClusterResourceUsageSpecUsagesItems0UsageAnon:
    """
    ResourceUsageValue holds value for resource usage.

    Attributes
    ----------
    expression : str, default is Undefined, optional
        Expression is the expression for resource usage.
    value : int | str, default is Undefined, optional
        Value is the value for resource usage.
    """


    expression?: str

    value?: int | str


    check:
        _regex_match(str(value), r"^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$") if value


schema KwokxK8sIoV1alpha1ClusterResourceUsageStatus:
    """
    Status holds status for cluster resource usage

    Attributes
    ----------
    conditions : [KwokxK8sIoV1alpha1ClusterResourceUsageStatusConditionsItems0], default is Undefined, optional
        Conditions holds conditions for cluster resource usage
    """


    conditions?: [KwokxK8sIoV1alpha1ClusterResourceUsageStatusConditionsItems0]


schema KwokxK8sIoV1alpha1ClusterResourceUsageStatusConditionsItems0:
    """
    Condition contains details for one aspect of the current state of this API Resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        Message is a human readable message indicating details about the transition.
        This may be an empty string.
    reason : str, default is Undefined, required
        Reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        Status of the condition
    $type : str, default is Undefined, required
        Type of condition in CamelCase or in foo.example.com/CamelCase.
        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
        useful (see .node.status.conditions), the ability to deconflict is important.
        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
    """


    lastTransitionTime: str

    message: str

    reason: str

    status: str

    $type: str


    check:
        len(message) <= 32768
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


