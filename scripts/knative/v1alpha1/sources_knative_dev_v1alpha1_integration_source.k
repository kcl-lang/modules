"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema IntegrationSource:
    r"""
    IntegrationSource is an event source that starts a container image which generates events under certain situations and sends messages to a sink URI

    Attributes
    ----------
    apiVersion : str, default is "sources.knative.dev/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "IntegrationSource", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SourcesKnativeDevV1alpha1IntegrationSourceSpec, default is Undefined, optional
        spec
    status : SourcesKnativeDevV1alpha1IntegrationSourceStatus, default is Undefined, optional
        status
    """


    apiVersion: "sources.knative.dev/v1alpha1" = "sources.knative.dev/v1alpha1"

    kind: "IntegrationSource" = "IntegrationSource"

    metadata?: v1.ObjectMeta

    spec?: SourcesKnativeDevV1alpha1IntegrationSourceSpec

    status?: SourcesKnativeDevV1alpha1IntegrationSourceStatus


schema SourcesKnativeDevV1alpha1IntegrationSourceSpec:
    r"""
    sources knative dev v1alpha1 integration source spec

    Attributes
    ----------
    aws : SourcesKnativeDevV1alpha1IntegrationSourceSpecAws, default is Undefined, optional
        aws
    ceOverrides : SourcesKnativeDevV1alpha1IntegrationSourceSpecCeOverrides, default is Undefined, optional
        ce overrides
    sink : SourcesKnativeDevV1alpha1IntegrationSourceSpecSink, default is Undefined, optional
        sink
    timer : SourcesKnativeDevV1alpha1IntegrationSourceSpecTimer, default is Undefined, optional
        timer
    """


    aws?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAws

    ceOverrides?: SourcesKnativeDevV1alpha1IntegrationSourceSpecCeOverrides

    sink?: SourcesKnativeDevV1alpha1IntegrationSourceSpecSink

    timer?: SourcesKnativeDevV1alpha1IntegrationSourceSpecTimer


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAws:
    r"""
    sources knative dev v1alpha1 integration source spec aws

    Attributes
    ----------
    auth : SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuth, default is Undefined, optional
        auth
    ddbStreams : SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsDdbStreams, default is Undefined, optional
        ddb streams
    s3 : SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsS3, default is Undefined, optional
        s3
    sqs : SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsSqs, default is Undefined, optional
        sqs
    """


    auth?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuth

    ddbStreams?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsDdbStreams

    s3?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsS3

    sqs?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsSqs


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuth:
    r"""
    Auth configurations

    Attributes
    ----------
    secret : SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuthSecret, default is Undefined, optional
        secret
    """


    secret?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuthSecret


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuthSecret:
    r"""
    Auth secret

    Attributes
    ----------
    ref : SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuthSecretRef, default is Undefined, optional
        ref
    """


    ref?: SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuthSecretRef


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsAuthSecretRef:
    r"""
    Secret reference.


    Attributes
    ----------
    name : str, default is Undefined, required
        Secret name
    """


    name: str


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsDdbStreams:
    r"""
    sources knative dev v1alpha1 integration source spec aws ddb streams

    Attributes
    ----------
    delay : int, default is 500, optional
        Delay        The number of milliseconds before the next poll from the database.
    overrideEndpoint : bool, default is Undefined, optional
        Endpoint Overwrite        Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
    region : str, default is Undefined, optional
        AWS Region        The AWS region to access.
    streamIteratorType : str, default is "FROM_LATEST", optional
        Stream Iterator Type        Defines where in the DynamoDB stream to start getting records. There are two enums and the value can be one of FROM_LATEST and FROM_START. Note that using FROM_START can cause a significant delay before the stream has caught up to real-time.
    table : str, default is Undefined, optional
        Table        The name of the DynamoDB table.
    uriEndpointOverride : str, default is Undefined, optional
        Overwrite Endpoint URI        The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
    """


    delay?: int = 500

    overrideEndpoint?: bool = False

    region?: str

    streamIteratorType?: str = "FROM_LATEST"

    table?: str

    uriEndpointOverride?: str


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsS3:
    r"""
    sources knative dev v1alpha1 integration source spec aws s3

    Attributes
    ----------
    arn : str, default is Undefined, optional
        Bucket Name        The S3 Bucket name or Amazon Resource Name (ARN).
    autoCreateBucket : bool, default is Undefined, optional
        Autocreate Bucket        Specifies to automatically create the S3 bucket.
    delay : int, default is 500, optional
        Delay        The number of milliseconds before the next poll of the selected bucket.
    deleteAfterRead : bool, default is True, optional
        Auto-delete Objects        Specifies to delete objects after consuming them.
    destinationBucket : str, default is Undefined, optional
        Destination Bucket        Define the destination bucket where an object must be moved when moveAfterRead is set to true.
    destinationBucketPrefix : str, default is Undefined, optional
        Destination Bucket Prefix        Define the destination bucket prefix to use when an object must be moved, and moveAfterRead is set to true.
    destinationBucketSuffix : str, default is Undefined, optional
        Destination Bucket Suffix        Define the destination bucket suffix to use when an object must be moved, and moveAfterRead is set to true.
    forcePathStyle : bool, default is Undefined, optional
        Force Path Style        Forces path style when accessing AWS S3 buckets.
    ignoreBody : bool, default is Undefined, optional
        Ignore Body        If true, the S3 Object body is ignored. Setting this to true overrides any behavior defined by the `includeBody` option. If false, the S3 object is put in the body.
    maxMessagesPerPoll : int, default is 10, optional
        Max Messages Per Poll        Gets the maximum number of messages as a limit to poll at each polling. Gets the maximum number of messages as a limit to poll at each polling. The default value is 10. Use 0 or a negative number to set it as unlimited.
    moveAfterRead : bool, default is Undefined, optional
        Move Objects After Delete        Move objects from S3 bucket to a different bucket after they have been retrieved.
    overrideEndpoint : bool, default is Undefined, optional
        Endpoint Overwrite        Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
    prefix : str, default is Undefined, optional
        Prefix        The AWS S3 bucket prefix to consider while searching.
    region : str, default is Undefined, optional
        AWS Region        The AWS region to access.
    uriEndpointOverride : str, default is Undefined, optional
        Overwrite Endpoint URI        The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
    """


    arn?: str

    autoCreateBucket?: bool = False

    delay?: int = 500

    deleteAfterRead?: bool = True

    destinationBucket?: str

    destinationBucketPrefix?: str

    destinationBucketSuffix?: str

    forcePathStyle?: bool = False

    ignoreBody?: bool = False

    maxMessagesPerPoll?: int = 10

    moveAfterRead?: bool = False

    overrideEndpoint?: bool = False

    prefix?: str

    region?: str

    uriEndpointOverride?: str


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecAwsSqs:
    r"""
    sources knative dev v1alpha1 integration source spec aws sqs

    Attributes
    ----------
    arn : str, default is Undefined, optional
        Queue Name        The SQS Queue Name or ARN
    autoCreateQueue : bool, default is Undefined, optional
        Autocreate Queue        Setting the autocreation of the SQS queue.
    delay : int, default is 500, optional
        Delay        The number of milliseconds before the next poll of the selected stream
    deleteAfterRead : bool, default is True, optional
        Auto-delete Messages        Delete messages after consuming them
    greedy : bool, default is Undefined, optional
        Greedy Scheduler        If greedy is enabled, then the polling will happen immediately again, if the previous run polled 1 or more messages.
    host : str, default is "amazonaws.com", optional
        AWS Host        The hostname of the Amazon AWS cloud.
    maxMessagesPerPoll : int, default is 1, optional
        Max Messages Per Poll        The maximum number of messages to return. Amazon SQS never returns more messages than this value (however, fewer messages might be returned). Valid values 1 to 10. Default 1.
    overrideEndpoint : bool, default is Undefined, optional
        Endpoint Overwrite        Select this option to override the endpoint URI. To use this option, you must also provide a URI for the `uriEndpointOverride` option.
    $protocol : str, default is "https", optional
        Protocol        The underlying protocol used to communicate with SQS
    queueURL : str, default is Undefined, optional
        Queue URL        The full SQS Queue URL (required if using KEDA)
    region : str, default is Undefined, optional
        AWS Region        The AWS region to access.
    uriEndpointOverride : str, default is Undefined, optional
        Overwrite Endpoint URI        The overriding endpoint URI. To use this option, you must also select the `overrideEndpoint` option.
    visibilityTimeout : int, default is Undefined, optional
        Visibility Timeout        The duration (in seconds) that the received messages are hidden from subsequent retrieve requests after being retrieved by a ReceiveMessage request.
    waitTimeSeconds : int, default is Undefined, optional
        Wait Time Seconds        The duration (in seconds) for which the call waits for a message to arrive in the queue before returning. If a message is available, the call returns sooner than WaitTimeSeconds. If no messages are available and the wait time expires, the call does not return a message list.
    """


    arn?: str

    autoCreateQueue?: bool = False

    delay?: int = 500

    deleteAfterRead?: bool = True

    greedy?: bool = False

    host?: str = "amazonaws.com"

    maxMessagesPerPoll?: int = 1

    overrideEndpoint?: bool = False

    $protocol?: str = "https"

    queueURL?: str

    region?: str

    uriEndpointOverride?: str

    visibilityTimeout?: int

    waitTimeSeconds?: int


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecCeOverrides:
    r"""
    CloudEventOverrides defines overrides to control the output format and modifications of the event sent to the sink.

    Attributes
    ----------
    extensions : any, default is Undefined, optional
        Extensions specify what attribute are added or overridden on the outbound event. Each `Extensions` key-value pair are set on the event as an attribute extension independently.
    """


    extensions?: any


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecSink:
    r"""
    Sink is a reference to an object that will resolve to a uri to use as the sink.

    Attributes
    ----------
    CACerts : str, default is Undefined, optional
        CACerts is the Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink.
    audience : str, default is Undefined, optional
        Audience is the OIDC audience. This only needs to be set if the target is not an Addressable and thus the Audience can't be received from the target itself. If specified, it takes precedence over the target's Audience.
    ref : SourcesKnativeDevV1alpha1IntegrationSourceSpecSinkRef, default is Undefined, optional
        ref
    uri : str, default is Undefined, optional
        URI can be an absolute URL(non-empty scheme and non-empty host) pointing to the target or a relative URI. Relative URIs will be resolved using the base URI retrieved from Ref.
    """


    CACerts?: str

    audience?: str

    ref?: SourcesKnativeDevV1alpha1IntegrationSourceSpecSinkRef

    uri?: str


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecSinkRef:
    r"""
    Ref points to an Addressable.

    Attributes
    ----------
    apiVersion : str, default is Undefined, optional
        API version of the referent.
    kind : str, default is Undefined, optional
        Kind of the referent. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
    namespace : str, default is Undefined, optional
        Namespace of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/ This is optional field, it gets defaulted to the object holding it if left out.
    """


    apiVersion?: str

    kind?: str

    name?: str

    namespace?: str


schema SourcesKnativeDevV1alpha1IntegrationSourceSpecTimer:
    r"""
    sources knative dev v1alpha1 integration source spec timer

    Attributes
    ----------
    contentType : str, default is "text/plain", optional
        Content Type        The content type of the generated message.
    message : str, default is Undefined, optional
        Message        The message to generate.
    period : int, default is 1000, optional
        Period        The interval (in milliseconds) to wait between producing the next message.
    repeatCount : int, default is Undefined, optional
        Repeat Count        Specifies a maximum limit of number of fires
    """


    contentType?: str = "text/plain"

    message?: str

    period?: int = 1000

    repeatCount?: int


schema SourcesKnativeDevV1alpha1IntegrationSourceStatus:
    r"""
    sources knative dev v1alpha1 integration source status

    Attributes
    ----------
    annotations : any, default is Undefined, optional
        Annotations is additional Status fields for the Resource to save some additional State as well as convey more information to the user. This is roughly akin to Annotations on any k8s resource, just the reconciler conveying richer information outwards.
    auth : SourcesKnativeDevV1alpha1IntegrationSourceStatusAuth, default is Undefined, optional
        auth
    ceAttributes : [SourcesKnativeDevV1alpha1IntegrationSourceStatusCeAttributesItems0], default is Undefined, optional
        CloudEventAttributes are the specific attributes that the Source uses as part of its CloudEvents.
    conditions : [SourcesKnativeDevV1alpha1IntegrationSourceStatusConditionsItems0], default is Undefined, optional
        Conditions the latest available observations of a resource's current state.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the 'Generation' of the Service that was last processed by the controller.
    sinkAudience : str, default is Undefined, optional
        Audience is the OIDC audience of the sink.
    sinkCACerts : str, default is Undefined, optional
        CACerts is the Certification Authority (CA) certificates in PEM format that the source trusts when sending events to the sink.
    sinkUri : str, default is Undefined, optional
        SinkURI is the current active sink URI that has been configured for the Source.
    """


    annotations?: any

    auth?: SourcesKnativeDevV1alpha1IntegrationSourceStatusAuth

    ceAttributes?: [SourcesKnativeDevV1alpha1IntegrationSourceStatusCeAttributesItems0]

    conditions?: [SourcesKnativeDevV1alpha1IntegrationSourceStatusConditionsItems0]

    observedGeneration?: int

    sinkAudience?: str

    sinkCACerts?: str

    sinkUri?: str


schema SourcesKnativeDevV1alpha1IntegrationSourceStatusAuth:
    r"""
    Auth provides the relevant information for OIDC authentication.

    Attributes
    ----------
    serviceAccountName : str, default is Undefined, optional
        ServiceAccountName is the name of the generated service account used for this components OIDC authentication.
    serviceAccountNames : [str], default is Undefined, optional
        ServiceAccountNames is the list of names of the generated service accounts used for this components OIDC authentication.
    """


    serviceAccountName?: str

    serviceAccountNames?: [str]


schema SourcesKnativeDevV1alpha1IntegrationSourceStatusCeAttributesItems0:
    r"""
    sources knative dev v1alpha1 integration source status ce attributes items0

    Attributes
    ----------
    source : str, default is Undefined, optional
        Source is the CloudEvents source attribute.
    $type : str, default is Undefined, optional
        Type refers to the CloudEvent type attribute.
    """


    source?: str

    $type?: str


schema SourcesKnativeDevV1alpha1IntegrationSourceStatusConditionsItems0:
    r"""
    sources knative dev v1alpha1 integration source status conditions items0

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        LastTransitionTime is the last time the condition transitioned from one status to another. We use VolatileTime in place of metav1.Time to exclude this from creating equality.Semantic differences (all other things held constant).
    message : str, default is Undefined, optional
        A human readable message indicating details about the transition.
    reason : str, default is Undefined, optional
        The reason for the condition's last transition.
    severity : str, default is Undefined, optional
        Severity with which to treat failures of this type of condition. When this is not specified, it defaults to Error.
    status : str, default is Undefined, required
        Status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        Type of condition.
    """


    lastTransitionTime?: str

    message?: str

    reason?: str

    severity?: str

    status: str

    $type: str


