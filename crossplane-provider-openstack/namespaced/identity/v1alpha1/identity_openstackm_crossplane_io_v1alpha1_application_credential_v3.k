"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema ApplicationCredentialV3:
    r"""
    ApplicationCredentialV3 is the Schema for the ApplicationCredentialV3s API. Manages a V3 Application Credential resource within OpenStack Keystone.

    Attributes
    ----------
    apiVersion : str, default is "identity.openstack.m.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ApplicationCredentialV3", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3Spec, default is Undefined, required
        spec
    status : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3Status, default is Undefined, optional
        status
    """


    apiVersion: "identity.openstack.m.crossplane.io/v1alpha1" = "identity.openstack.m.crossplane.io/v1alpha1"

    kind: "ApplicationCredentialV3" = "ApplicationCredentialV3"

    metadata?: v1.ObjectMeta

    spec: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3Spec

    status?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3Status


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3Spec:
    r"""
    ApplicationCredentialV3Spec defines the desired state of ApplicationCredentialV3

    Attributes
    ----------
    forProvider : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProvider, default is Undefined, required
        for provider
    initProvider : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProvider

    initProvider?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecProviderConfigRef

    writeConnectionSecretToRef?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecWriteConnectionSecretToRef


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProvider:
    r"""
    identity openstackm crossplane io v1alpha1 application credential v3 spec for provider

    Attributes
    ----------
    accessRules : [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProviderAccessRulesItems0], default is Undefined, optional
        A collection of one or more access rules, which
        this application credential allows to follow. The structure is described
        below. Changing this creates a new application credential.
    description : str, default is Undefined, optional
        A description of the application credential.
        Changing this creates a new application credential.
    expiresAt : str, default is Undefined, optional
        The expiration time of the application credential
        in the RFC3339 timestamp format (e.g. 2019-03-09T12:58:49Z). If omitted,
        an application credential will never expire. Changing this creates a new
        application credential.
    name : str, default is Undefined, optional
        A name of the application credential. Changing this
        creates a new application credential.
    region : str, default is Undefined, optional
        The region in which to obtain the V3 Keystone client.
        If omitted, the region argument of the provider is used. Changing this
        creates a new application credential.
    roles : [str], default is Undefined, optional
        A collection of one or more role names, which this
        application credential has to be associated with its project. If omitted,
        all the current user's roles within the scoped project will be inherited by
        a new application credential. Changing this creates a new application
        credential.
    secretSecretRef : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProviderSecretSecretRef, default is Undefined, optional
        secret secret ref
    unrestricted : bool, default is Undefined, optional
        A flag indicating whether the application
        credential may be used for creation or destruction of other application
        credentials or trusts. Changing this creates a new application credential.
    """


    accessRules?: [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProviderAccessRulesItems0]

    description?: str

    expiresAt?: str

    name?: str

    region?: str

    roles?: [str]

    secretSecretRef?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProviderSecretSecretRef

    unrestricted?: bool


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProviderAccessRulesItems0:
    r"""
    identity openstackm crossplane io v1alpha1 application credential v3 spec for provider access rules items0

    Attributes
    ----------
    method : str, default is Undefined, optional
        The request method that the application credential is
        permitted to use for a given API endpoint. Allowed values: POST, GET,
        HEAD, PATCH, PUT and DELETE.
    path : str, default is Undefined, optional
        The API path that the application credential is permitted
        to access. May use named wildcards such as {tag} or the unnamed wildcard
        * to match against any string in the path up to a /, or the recursive
        wildcard ** to include / in the matched path.
    service : str, default is Undefined, optional
        The service type identifier for the service that the
        application credential is granted to access. Must be a service type that is
        listed in the service catalog and not a code name for a service. E.g.
        identity, compute, volumev3, image, network,
        object-store, sharev2, dns, key-manager, monitoring, etc.
    """


    method?: str

    path?: str

    service?: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecForProviderSecretSecretRef:
    r"""
    The secret for the application credential. If omitted,
    it will be generated by the server. Changing this creates a new application
    credential.

    Attributes
    ----------
    key : str, default is Undefined, required
        key
    name : str, default is Undefined, required
        Name of the secret.
    """


    key: str

    name: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    accessRules : [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProviderAccessRulesItems0], default is Undefined, optional
        A collection of one or more access rules, which
        this application credential allows to follow. The structure is described
        below. Changing this creates a new application credential.
    description : str, default is Undefined, optional
        A description of the application credential.
        Changing this creates a new application credential.
    expiresAt : str, default is Undefined, optional
        The expiration time of the application credential
        in the RFC3339 timestamp format (e.g. 2019-03-09T12:58:49Z). If omitted,
        an application credential will never expire. Changing this creates a new
        application credential.
    name : str, default is Undefined, optional
        A name of the application credential. Changing this
        creates a new application credential.
    region : str, default is Undefined, optional
        The region in which to obtain the V3 Keystone client.
        If omitted, the region argument of the provider is used. Changing this
        creates a new application credential.
    roles : [str], default is Undefined, optional
        A collection of one or more role names, which this
        application credential has to be associated with its project. If omitted,
        all the current user's roles within the scoped project will be inherited by
        a new application credential. Changing this creates a new application
        credential.
    secretSecretRef : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProviderSecretSecretRef, default is Undefined, optional
        secret secret ref
    unrestricted : bool, default is Undefined, optional
        A flag indicating whether the application
        credential may be used for creation or destruction of other application
        credentials or trusts. Changing this creates a new application credential.
    """


    accessRules?: [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProviderAccessRulesItems0]

    description?: str

    expiresAt?: str

    name?: str

    region?: str

    roles?: [str]

    secretSecretRef?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProviderSecretSecretRef

    unrestricted?: bool


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProviderAccessRulesItems0:
    r"""
    identity openstackm crossplane io v1alpha1 application credential v3 spec init provider access rules items0

    Attributes
    ----------
    method : str, default is Undefined, optional
        The request method that the application credential is
        permitted to use for a given API endpoint. Allowed values: POST, GET,
        HEAD, PATCH, PUT and DELETE.
    path : str, default is Undefined, optional
        The API path that the application credential is permitted
        to access. May use named wildcards such as {tag} or the unnamed wildcard
        * to match against any string in the path up to a /, or the recursive
        wildcard ** to include / in the matched path.
    service : str, default is Undefined, optional
        The service type identifier for the service that the
        application credential is granted to access. Must be a service type that is
        listed in the service catalog and not a code name for a service. E.g.
        identity, compute, volumev3, image, network,
        object-store, sharev2, dns, key-manager, monitoring, etc.
    """


    method?: str

    path?: str

    service?: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecInitProviderSecretSecretRef:
    r"""
    The secret for the application credential. If omitted,
    it will be generated by the server. Changing this creates a new application
    credential.

    Attributes
    ----------
    key : str, default is Undefined, required
        key
    name : str, default is Undefined, required
        Name of the secret.
    """


    key: str

    name: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3SpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3Status:
    r"""
    ApplicationCredentialV3Status defines the observed state of ApplicationCredentialV3.

    Attributes
    ----------
    atProvider : IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusAtProvider, default is Undefined, optional
        at provider
    conditions : [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusAtProvider

    conditions?: [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusConditionsItems0]

    observedGeneration?: int


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusAtProvider:
    r"""
    identity openstackm crossplane io v1alpha1 application credential v3 status at provider

    Attributes
    ----------
    accessRules : [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusAtProviderAccessRulesItems0], default is Undefined, optional
        A collection of one or more access rules, which
        this application credential allows to follow. The structure is described
        below. Changing this creates a new application credential.
    description : str, default is Undefined, optional
        A description of the application credential.
        Changing this creates a new application credential.
    expiresAt : str, default is Undefined, optional
        The expiration time of the application credential
        in the RFC3339 timestamp format (e.g. 2019-03-09T12:58:49Z). If omitted,
        an application credential will never expire. Changing this creates a new
        application credential.
    id : str, default is Undefined, optional
        (Computed) The ID of the existing access rule. The access rule ID of
        another application credential can be provided.
    name : str, default is Undefined, optional
        A name of the application credential. Changing this
        creates a new application credential.
    projectId : str, default is Undefined, optional
        The ID of the project the application credential was created
        for and that authentication requests using this application credential will
        be scoped to.
    region : str, default is Undefined, optional
        The region in which to obtain the V3 Keystone client.
        If omitted, the region argument of the provider is used. Changing this
        creates a new application credential.
    roles : [str], default is Undefined, optional
        A collection of one or more role names, which this
        application credential has to be associated with its project. If omitted,
        all the current user's roles within the scoped project will be inherited by
        a new application credential. Changing this creates a new application
        credential.
    unrestricted : bool, default is Undefined, optional
        A flag indicating whether the application
        credential may be used for creation or destruction of other application
        credentials or trusts. Changing this creates a new application credential.
    """


    accessRules?: [IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusAtProviderAccessRulesItems0]

    description?: str

    expiresAt?: str

    id?: str

    name?: str

    projectId?: str

    region?: str

    roles?: [str]

    unrestricted?: bool


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusAtProviderAccessRulesItems0:
    r"""
    identity openstackm crossplane io v1alpha1 application credential v3 status at provider access rules items0

    Attributes
    ----------
    id : str, default is Undefined, optional
        (Computed) The ID of the existing access rule. The access rule ID of
        another application credential can be provided.
    method : str, default is Undefined, optional
        The request method that the application credential is
        permitted to use for a given API endpoint. Allowed values: POST, GET,
        HEAD, PATCH, PUT and DELETE.
    path : str, default is Undefined, optional
        The API path that the application credential is permitted
        to access. May use named wildcards such as {tag} or the unnamed wildcard
        * to match against any string in the path up to a /, or the recursive
        wildcard ** to include / in the matched path.
    service : str, default is Undefined, optional
        The service type identifier for the service that the
        application credential is granted to access. Must be a service type that is
        listed in the service catalog and not a code name for a service. E.g.
        identity, compute, volumev3, image, network,
        object-store, sharev2, dns, key-manager, monitoring, etc.
    """


    id?: str

    method?: str

    path?: str

    service?: str


schema IdentityOpenstackmCrossplaneIoV1alpha1ApplicationCredentialV3StatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


