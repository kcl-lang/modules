"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema PatchDeployment:
    """
    PatchDeployment is the Schema for the PatchDeployments API. Patch deployments are configurations that individual patch jobs use to complete a patch.

    Attributes
    ----------
    apiVersion : str, default is "osconfig.gcp.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "PatchDeployment", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpec, default is Undefined, required
        spec
    status : OsconfigGcpUpboundIoV1beta1PatchDeploymentStatus, default is Undefined, optional
        status
    """


    apiVersion: "osconfig.gcp.upbound.io/v1beta1" = "osconfig.gcp.upbound.io/v1beta1"

    kind: "PatchDeployment" = "PatchDeployment"

    metadata?: v1.ObjectMeta

    spec: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpec

    status?: OsconfigGcpUpboundIoV1beta1PatchDeploymentStatus


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpec:
    """
    PatchDeploymentSpec defines the desired state of PatchDeployment

    Attributes
    ----------
    deletionPolicy : str, default is "Delete", optional
        DeletionPolicy specifies what will happen to the underlying external
        when this managed resource is deleted - either "Delete" or "Orphan" the
        external resource.
        This field is planned to be deprecated in favor of the ManagementPolicies
        field in a future release. Currently, both could be set independently and
        non-default values would be honored if the feature flag is enabled.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
    forProvider : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProvider, default is Undefined, required
        for provider
    initProvider : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        This field is planned to replace the DeletionPolicy field in a future
        release. Currently, both could be set independently and non-default
        values would be honored if the feature flag is enabled. If both are
        custom, the DeletionPolicy field will be ignored.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    publishConnectionDetailsTo : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsTo, default is Undefined, optional
        publish connection details to
    writeConnectionSecretToRef : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    deletionPolicy?: "Orphan" | "Delete" = "Delete"

    forProvider: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProvider

    initProvider?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecProviderConfigRef

    publishConnectionDetailsTo?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsTo

    writeConnectionSecretToRef?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecWriteConnectionSecretToRef


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProvider:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider

    Attributes
    ----------
    description : str, default is Undefined, optional
        Description of the patch deployment. Length of the description is limited to 1024 characters.
    duration : str, default is Undefined, optional
        Duration of the patch. After the duration ends, the patch times out.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s"
    instanceFilter : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderInstanceFilterItems0], default is Undefined, optional
        VM instances to patch.
        Structure is documented below.
    oneTimeSchedule : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderOneTimeScheduleItems0], default is Undefined, optional
        Schedule a one-time execution.
        Structure is documented below.
    patchConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0], default is Undefined, optional
        Patch configuration that is applied.
        Structure is documented below.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    recurringSchedule : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0], default is Undefined, optional
        Schedule recurring executions.
        Structure is documented below.
    rollout : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRolloutItems0], default is Undefined, optional
        Rollout strategy of the patch job.
        Structure is documented below.
    """


    description?: str

    duration?: str

    instanceFilter?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderInstanceFilterItems0]

    oneTimeSchedule?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderOneTimeScheduleItems0]

    patchConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0]

    project?: str

    recurringSchedule?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0]

    rollout?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRolloutItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderInstanceFilterItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider instance filter items0

    Attributes
    ----------
    $all : bool, default is Undefined, optional
        Target all VM instances in the project. If true, no other criteria is permitted.
    groupLabels : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderInstanceFilterItems0GroupLabelsItems0], default is Undefined, optional
        Targets VM instances matching ANY of these GroupLabels. This allows targeting of disparate groups of VM instances.
        Structure is documented below.
    instanceNamePrefixes : [str], default is Undefined, optional
        Targets VMs whose name starts with one of these prefixes. Similar to labels, this is another way to group
        VMs when targeting configs, for example prefix="prod-".
    instances : [str], default is Undefined, optional
        Targets any of the VM instances specified. Instances are specified by their URI in the form zones/{{zone}}/instances/{{instance_name}},
        projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}, or
        https://www.googleapis.com/compute/v1/projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}
    zones : [str], default is Undefined, optional
        Targets VM instances in ANY of these zones. Leave empty to target VM instances in any zone.
    """


    $all?: bool

    groupLabels?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderInstanceFilterItems0GroupLabelsItems0]

    instanceNamePrefixes?: [str]

    instances?: [str]

    zones?: [str]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderInstanceFilterItems0GroupLabelsItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider instance filter items0 group labels items0

    Attributes
    ----------
    labels : {str:str}, default is Undefined, optional
        Compute Engine instance labels that must be present for a VM instance to be targeted by this filter
    """


    labels?: {str:str}


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderOneTimeScheduleItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider one time schedule items0

    Attributes
    ----------
    executeTime : str, default is Undefined, optional
        The desired patch job execution time. A timestamp in RFC3339 UTC "Zulu" format,
        accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    """


    executeTime?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0

    Attributes
    ----------
    apt : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0AptItems0], default is Undefined, optional
        Apt update settings. Use this setting to override the default apt patch rules.
        Structure is documented below.
    goo : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0GooItems0], default is Undefined, optional
        goo update settings. Use this setting to override the default goo patch rules.
        Structure is documented below.
    migInstancesAllowed : bool, default is Undefined, optional
        Allows the patch job to run on Managed instance groups (MIGs).
    postStep : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0], default is Undefined, optional
        The ExecStep to run after the patch update.
        Structure is documented below.
    preStep : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0], default is Undefined, optional
        The ExecStep to run before the patch update.
        Structure is documented below.
    rebootConfig : str, default is Undefined, optional
        Post-patch reboot settings.
        Possible values are: DEFAULT, ALWAYS, NEVER.
    windowsUpdate : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0WindowsUpdateItems0], default is Undefined, optional
        Windows update settings. Use this setting to override the default Windows patch rules.
        Structure is documented below.
    yum : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0YumItems0], default is Undefined, optional
        Yum update settings. Use this setting to override the default yum patch rules.
        Structure is documented below.
    zypper : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0ZypperItems0], default is Undefined, optional
        zypper update settings. Use this setting to override the default zypper patch rules.
        Structure is documented below.
    """


    apt?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0AptItems0]

    goo?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0GooItems0]

    migInstancesAllowed?: bool

    postStep?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0]

    preStep?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0]

    rebootConfig?: str

    windowsUpdate?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0WindowsUpdateItems0]

    yum?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0YumItems0]

    zypper?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0ZypperItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0AptItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 apt items0

    Attributes
    ----------
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePackages : [str], default is Undefined, optional
        An exclusive list of packages to be updated. These are the only packages that will be updated.
        If these packages are not installed, they will be ignored. This field cannot be specified with
        any other patch configuration fields.
    $type : str, default is Undefined, optional
        By changing the type to DIST, the patching is performed using apt-get dist-upgrade instead.
        Possible values are: DIST, UPGRADE.
    """


    excludes?: [str]

    exclusivePackages?: [str]

    $type?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0GooItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 goo items0

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        goo update settings. Use this setting to override the default goo patch rules.
    """


    enabled?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 post step items0

    Attributes
    ----------
    linuxExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Linux VMs targeted by the PatchJob.
        Structure is documented below.
    windowsExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Windows VMs targeted by the PatchJob.
        Structure is documented below.
    """


    linuxExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0]

    windowsExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 post step items0 linux exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 post step items0 linux exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 post step items0 windows exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 post step items0 windows exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 pre step items0

    Attributes
    ----------
    linuxExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Linux VMs targeted by the PatchJob.
        Structure is documented below.
    windowsExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Windows VMs targeted by the PatchJob.
        Structure is documented below.
    """


    linuxExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0]

    windowsExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 pre step items0 linux exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 pre step items0 linux exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 pre step items0 windows exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 pre step items0 windows exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0WindowsUpdateItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 windows update items0

    Attributes
    ----------
    classifications : [str], default is Undefined, optional
        Only apply updates of these windows update classifications. If empty, all updates are applied.
        Each value may be one of: CRITICAL, SECURITY, DEFINITION, DRIVER, FEATURE_PACK, SERVICE_PACK, TOOL, UPDATE_ROLLUP, UPDATE.
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePatches : [str], default is Undefined, optional
        An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command.
        This field must not be used with any other patch configuration fields.
    """


    classifications?: [str]

    excludes?: [str]

    exclusivePatches?: [str]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0YumItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 yum items0

    Attributes
    ----------
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePackages : [str], default is Undefined, optional
        An exclusive list of packages to be updated. These are the only packages that will be updated.
        If these packages are not installed, they will be ignored. This field cannot be specified with
        any other patch configuration fields.
    minimal : bool, default is Undefined, optional
        Will cause patch to run yum update-minimal instead.
    security : bool, default is Undefined, optional
        Adds the --security flag to yum update. Not supported on all platforms.
    """


    excludes?: [str]

    exclusivePackages?: [str]

    minimal?: bool

    security?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderPatchConfigItems0ZypperItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider patch config items0 zypper items0

    Attributes
    ----------
    categories : [str], default is Undefined, optional
        Install only patches with these categories. Common categories include security, recommended, and feature.
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePatches : [str], default is Undefined, optional
        An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command.
        This field must not be used with any other patch configuration fields.
    severities : [str], default is Undefined, optional
        Install only patches with these severities. Common severities include critical, important, moderate, and low.
    withOptional : bool, default is Undefined, optional
        Adds the --with-optional flag to zypper patch.
    withUpdate : bool, default is Undefined, optional
        Adds the --with-update flag, to zypper patch.
    """


    categories?: [str]

    excludes?: [str]

    exclusivePatches?: [str]

    severities?: [str]

    withOptional?: bool

    withUpdate?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider recurring schedule items0

    Attributes
    ----------
    endTime : str, default is Undefined, optional
        The end time at which a recurring patch deployment schedule is no longer active.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    monthly : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0MonthlyItems0], default is Undefined, optional
        Schedule with monthly executions.
        Structure is documented below.
    startTime : str, default is Undefined, optional
        The time that the recurring schedule becomes effective. Defaults to createTime of the patch deployment.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    timeOfDay : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0TimeOfDayItems0], default is Undefined, optional
        Time of the day to run a recurring deployment.
        Structure is documented below.
    timeZone : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0TimeZoneItems0], default is Undefined, optional
        Defines the time zone that timeOfDay is relative to. The rules for daylight saving time are
        determined by the chosen time zone.
        Structure is documented below.
    weekly : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0WeeklyItems0], default is Undefined, optional
        Schedule with weekly executions.
        Structure is documented below.
    """


    endTime?: str

    monthly?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0MonthlyItems0]

    startTime?: str

    timeOfDay?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0TimeOfDayItems0]

    timeZone?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0TimeZoneItems0]

    weekly?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0WeeklyItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0MonthlyItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider recurring schedule items0 monthly items0

    Attributes
    ----------
    monthDay : float, default is Undefined, optional
        One day of the month. 1-31 indicates the 1st to the 31st day. -1 indicates the last day of the month.
        Months without the target day will be skipped. For example, a schedule to run "every month on the 31st"
        will not run in February, April, June, etc.
    weekDayOfMonth : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0], default is Undefined, optional
        Week day in a month.
        Structure is documented below.
    """


    monthDay?: float

    weekDayOfMonth?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider recurring schedule items0 monthly items0 week day of month items0

    Attributes
    ----------
    dayOfWeek : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
        Possible values are: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
    dayOffset : float, default is Undefined, optional
        Represents the number of days before or after the given week day of month that the patch deployment is scheduled for.
    weekOrdinal : float, default is Undefined, optional
        Week number in a month. 1-4 indicates the 1st to 4th week of the month. -1 indicates the last week of the month.
    """


    dayOfWeek?: str

    dayOffset?: float

    weekOrdinal?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0TimeOfDayItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider recurring schedule items0 time of day items0

    Attributes
    ----------
    hours : float, default is Undefined, optional
        Hours of day in 24 hour format. Should be from 0 to 23.
        An API may choose to allow the value "24:00:00" for scenarios like business closing time.
    minutes : float, default is Undefined, optional
        Minutes of hour of day. Must be from 0 to 59.
    nanos : float, default is Undefined, optional
        Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999.
    seconds : float, default is Undefined, optional
        Seconds of minutes of the time. Must normally be from 0 to 59. An API may allow the value 60 if it allows leap-seconds.
    """


    hours?: float

    minutes?: float

    nanos?: float

    seconds?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0TimeZoneItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider recurring schedule items0 time zone items0

    Attributes
    ----------
    id : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
    version : str, default is Undefined, optional
        IANA Time Zone Database version number, e.g. "2019a".
    """


    id?: str

    version?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRecurringScheduleItems0WeeklyItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider recurring schedule items0 weekly items0

    Attributes
    ----------
    dayOfWeek : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
        Possible values are: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
    """


    dayOfWeek?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRolloutItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider rollout items0

    Attributes
    ----------
    disruptionBudget : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRolloutItems0DisruptionBudgetItems0], default is Undefined, optional
        The maximum number (or percentage) of VMs per zone to disrupt at any given moment. The number of VMs calculated from multiplying the percentage by the total number of VMs in a zone is rounded up.
        During patching, a VM is considered disrupted from the time the agent is notified to begin until patching has completed. This disruption time includes the time to complete reboot and any post-patch steps.
        A VM contributes to the disruption budget if its patching operation fails either when applying the patches, running pre or post patch steps, or if it fails to respond with a success notification before timing out. VMs that are not running or do not have an active agent do not count toward this disruption budget.
        For zone-by-zone rollouts, if the disruption budget in a zone is exceeded, the patch job stops, because continuing to the next zone requires completion of the patch process in the previous zone.
        For example, if the disruption budget has a fixed value of 10, and 8 VMs fail to patch in the current zone, the patch job continues to patch 2 VMs at a time until the zone is completed. When that zone is completed successfully, patching begins with 10 VMs at a time in the next zone. If 10 VMs in the next zone fail to patch, the patch job stops.
        Structure is documented below.
    mode : str, default is Undefined, optional
        Mode of the patch rollout.
        Possible values are: ZONE_BY_ZONE, CONCURRENT_ZONES.
    """


    disruptionBudget?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRolloutItems0DisruptionBudgetItems0]

    mode?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecForProviderRolloutItems0DisruptionBudgetItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec for provider rollout items0 disruption budget items0

    Attributes
    ----------
    fixed : float, default is Undefined, optional
        Specifies a fixed value.
    percentage : float, default is Undefined, optional
        Specifies the relative value defined as a percentage, which will be multiplied by a reference value.
    """


    fixed?: float

    percentage?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProvider:
    """
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    description : str, default is Undefined, optional
        Description of the patch deployment. Length of the description is limited to 1024 characters.
    duration : str, default is Undefined, optional
        Duration of the patch. After the duration ends, the patch times out.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s"
    instanceFilter : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderInstanceFilterItems0], default is Undefined, optional
        VM instances to patch.
        Structure is documented below.
    oneTimeSchedule : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderOneTimeScheduleItems0], default is Undefined, optional
        Schedule a one-time execution.
        Structure is documented below.
    patchConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0], default is Undefined, optional
        Patch configuration that is applied.
        Structure is documented below.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    recurringSchedule : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0], default is Undefined, optional
        Schedule recurring executions.
        Structure is documented below.
    rollout : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRolloutItems0], default is Undefined, optional
        Rollout strategy of the patch job.
        Structure is documented below.
    """


    description?: str

    duration?: str

    instanceFilter?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderInstanceFilterItems0]

    oneTimeSchedule?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderOneTimeScheduleItems0]

    patchConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0]

    project?: str

    recurringSchedule?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0]

    rollout?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRolloutItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderInstanceFilterItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider instance filter items0

    Attributes
    ----------
    $all : bool, default is Undefined, optional
        Target all VM instances in the project. If true, no other criteria is permitted.
    groupLabels : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderInstanceFilterItems0GroupLabelsItems0], default is Undefined, optional
        Targets VM instances matching ANY of these GroupLabels. This allows targeting of disparate groups of VM instances.
        Structure is documented below.
    instanceNamePrefixes : [str], default is Undefined, optional
        Targets VMs whose name starts with one of these prefixes. Similar to labels, this is another way to group
        VMs when targeting configs, for example prefix="prod-".
    instances : [str], default is Undefined, optional
        Targets any of the VM instances specified. Instances are specified by their URI in the form zones/{{zone}}/instances/{{instance_name}},
        projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}, or
        https://www.googleapis.com/compute/v1/projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}
    zones : [str], default is Undefined, optional
        Targets VM instances in ANY of these zones. Leave empty to target VM instances in any zone.
    """


    $all?: bool

    groupLabels?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderInstanceFilterItems0GroupLabelsItems0]

    instanceNamePrefixes?: [str]

    instances?: [str]

    zones?: [str]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderInstanceFilterItems0GroupLabelsItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider instance filter items0 group labels items0

    Attributes
    ----------
    labels : {str:str}, default is Undefined, optional
        Compute Engine instance labels that must be present for a VM instance to be targeted by this filter
    """


    labels?: {str:str}


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderOneTimeScheduleItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider one time schedule items0

    Attributes
    ----------
    executeTime : str, default is Undefined, optional
        The desired patch job execution time. A timestamp in RFC3339 UTC "Zulu" format,
        accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    """


    executeTime?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0

    Attributes
    ----------
    apt : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0AptItems0], default is Undefined, optional
        Apt update settings. Use this setting to override the default apt patch rules.
        Structure is documented below.
    goo : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0GooItems0], default is Undefined, optional
        goo update settings. Use this setting to override the default goo patch rules.
        Structure is documented below.
    migInstancesAllowed : bool, default is Undefined, optional
        Allows the patch job to run on Managed instance groups (MIGs).
    postStep : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0], default is Undefined, optional
        The ExecStep to run after the patch update.
        Structure is documented below.
    preStep : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0], default is Undefined, optional
        The ExecStep to run before the patch update.
        Structure is documented below.
    rebootConfig : str, default is Undefined, optional
        Post-patch reboot settings.
        Possible values are: DEFAULT, ALWAYS, NEVER.
    windowsUpdate : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0WindowsUpdateItems0], default is Undefined, optional
        Windows update settings. Use this setting to override the default Windows patch rules.
        Structure is documented below.
    yum : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0YumItems0], default is Undefined, optional
        Yum update settings. Use this setting to override the default yum patch rules.
        Structure is documented below.
    zypper : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0ZypperItems0], default is Undefined, optional
        zypper update settings. Use this setting to override the default zypper patch rules.
        Structure is documented below.
    """


    apt?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0AptItems0]

    goo?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0GooItems0]

    migInstancesAllowed?: bool

    postStep?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0]

    preStep?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0]

    rebootConfig?: str

    windowsUpdate?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0WindowsUpdateItems0]

    yum?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0YumItems0]

    zypper?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0ZypperItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0AptItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 apt items0

    Attributes
    ----------
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePackages : [str], default is Undefined, optional
        An exclusive list of packages to be updated. These are the only packages that will be updated.
        If these packages are not installed, they will be ignored. This field cannot be specified with
        any other patch configuration fields.
    $type : str, default is Undefined, optional
        By changing the type to DIST, the patching is performed using apt-get dist-upgrade instead.
        Possible values are: DIST, UPGRADE.
    """


    excludes?: [str]

    exclusivePackages?: [str]

    $type?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0GooItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 goo items0

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        goo update settings. Use this setting to override the default goo patch rules.
    """


    enabled?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 post step items0

    Attributes
    ----------
    linuxExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Linux VMs targeted by the PatchJob.
        Structure is documented below.
    windowsExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Windows VMs targeted by the PatchJob.
        Structure is documented below.
    """


    linuxExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0]

    windowsExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 post step items0 linux exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 post step items0 linux exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 post step items0 windows exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 post step items0 windows exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 pre step items0

    Attributes
    ----------
    linuxExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Linux VMs targeted by the PatchJob.
        Structure is documented below.
    windowsExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Windows VMs targeted by the PatchJob.
        Structure is documented below.
    """


    linuxExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0]

    windowsExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 pre step items0 linux exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 pre step items0 linux exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 pre step items0 windows exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 pre step items0 windows exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0WindowsUpdateItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 windows update items0

    Attributes
    ----------
    classifications : [str], default is Undefined, optional
        Only apply updates of these windows update classifications. If empty, all updates are applied.
        Each value may be one of: CRITICAL, SECURITY, DEFINITION, DRIVER, FEATURE_PACK, SERVICE_PACK, TOOL, UPDATE_ROLLUP, UPDATE.
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePatches : [str], default is Undefined, optional
        An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command.
        This field must not be used with any other patch configuration fields.
    """


    classifications?: [str]

    excludes?: [str]

    exclusivePatches?: [str]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0YumItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 yum items0

    Attributes
    ----------
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePackages : [str], default is Undefined, optional
        An exclusive list of packages to be updated. These are the only packages that will be updated.
        If these packages are not installed, they will be ignored. This field cannot be specified with
        any other patch configuration fields.
    minimal : bool, default is Undefined, optional
        Will cause patch to run yum update-minimal instead.
    security : bool, default is Undefined, optional
        Adds the --security flag to yum update. Not supported on all platforms.
    """


    excludes?: [str]

    exclusivePackages?: [str]

    minimal?: bool

    security?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderPatchConfigItems0ZypperItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider patch config items0 zypper items0

    Attributes
    ----------
    categories : [str], default is Undefined, optional
        Install only patches with these categories. Common categories include security, recommended, and feature.
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePatches : [str], default is Undefined, optional
        An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command.
        This field must not be used with any other patch configuration fields.
    severities : [str], default is Undefined, optional
        Install only patches with these severities. Common severities include critical, important, moderate, and low.
    withOptional : bool, default is Undefined, optional
        Adds the --with-optional flag to zypper patch.
    withUpdate : bool, default is Undefined, optional
        Adds the --with-update flag, to zypper patch.
    """


    categories?: [str]

    excludes?: [str]

    exclusivePatches?: [str]

    severities?: [str]

    withOptional?: bool

    withUpdate?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider recurring schedule items0

    Attributes
    ----------
    endTime : str, default is Undefined, optional
        The end time at which a recurring patch deployment schedule is no longer active.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    monthly : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0MonthlyItems0], default is Undefined, optional
        Schedule with monthly executions.
        Structure is documented below.
    startTime : str, default is Undefined, optional
        The time that the recurring schedule becomes effective. Defaults to createTime of the patch deployment.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    timeOfDay : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0TimeOfDayItems0], default is Undefined, optional
        Time of the day to run a recurring deployment.
        Structure is documented below.
    timeZone : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0TimeZoneItems0], default is Undefined, optional
        Defines the time zone that timeOfDay is relative to. The rules for daylight saving time are
        determined by the chosen time zone.
        Structure is documented below.
    weekly : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0WeeklyItems0], default is Undefined, optional
        Schedule with weekly executions.
        Structure is documented below.
    """


    endTime?: str

    monthly?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0MonthlyItems0]

    startTime?: str

    timeOfDay?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0TimeOfDayItems0]

    timeZone?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0TimeZoneItems0]

    weekly?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0WeeklyItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0MonthlyItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider recurring schedule items0 monthly items0

    Attributes
    ----------
    monthDay : float, default is Undefined, optional
        One day of the month. 1-31 indicates the 1st to the 31st day. -1 indicates the last day of the month.
        Months without the target day will be skipped. For example, a schedule to run "every month on the 31st"
        will not run in February, April, June, etc.
    weekDayOfMonth : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0], default is Undefined, optional
        Week day in a month.
        Structure is documented below.
    """


    monthDay?: float

    weekDayOfMonth?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider recurring schedule items0 monthly items0 week day of month items0

    Attributes
    ----------
    dayOfWeek : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
        Possible values are: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
    dayOffset : float, default is Undefined, optional
        Represents the number of days before or after the given week day of month that the patch deployment is scheduled for.
    weekOrdinal : float, default is Undefined, optional
        Week number in a month. 1-4 indicates the 1st to 4th week of the month. -1 indicates the last week of the month.
    """


    dayOfWeek?: str

    dayOffset?: float

    weekOrdinal?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0TimeOfDayItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider recurring schedule items0 time of day items0

    Attributes
    ----------
    hours : float, default is Undefined, optional
        Hours of day in 24 hour format. Should be from 0 to 23.
        An API may choose to allow the value "24:00:00" for scenarios like business closing time.
    minutes : float, default is Undefined, optional
        Minutes of hour of day. Must be from 0 to 59.
    nanos : float, default is Undefined, optional
        Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999.
    seconds : float, default is Undefined, optional
        Seconds of minutes of the time. Must normally be from 0 to 59. An API may allow the value 60 if it allows leap-seconds.
    """


    hours?: float

    minutes?: float

    nanos?: float

    seconds?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0TimeZoneItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider recurring schedule items0 time zone items0

    Attributes
    ----------
    id : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
    version : str, default is Undefined, optional
        IANA Time Zone Database version number, e.g. "2019a".
    """


    id?: str

    version?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRecurringScheduleItems0WeeklyItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider recurring schedule items0 weekly items0

    Attributes
    ----------
    dayOfWeek : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
        Possible values are: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
    """


    dayOfWeek?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRolloutItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider rollout items0

    Attributes
    ----------
    disruptionBudget : [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRolloutItems0DisruptionBudgetItems0], default is Undefined, optional
        The maximum number (or percentage) of VMs per zone to disrupt at any given moment. The number of VMs calculated from multiplying the percentage by the total number of VMs in a zone is rounded up.
        During patching, a VM is considered disrupted from the time the agent is notified to begin until patching has completed. This disruption time includes the time to complete reboot and any post-patch steps.
        A VM contributes to the disruption budget if its patching operation fails either when applying the patches, running pre or post patch steps, or if it fails to respond with a success notification before timing out. VMs that are not running or do not have an active agent do not count toward this disruption budget.
        For zone-by-zone rollouts, if the disruption budget in a zone is exceeded, the patch job stops, because continuing to the next zone requires completion of the patch process in the previous zone.
        For example, if the disruption budget has a fixed value of 10, and 8 VMs fail to patch in the current zone, the patch job continues to patch 2 VMs at a time until the zone is completed. When that zone is completed successfully, patching begins with 10 VMs at a time in the next zone. If 10 VMs in the next zone fail to patch, the patch job stops.
        Structure is documented below.
    mode : str, default is Undefined, optional
        Mode of the patch rollout.
        Possible values are: ZONE_BY_ZONE, CONCURRENT_ZONES.
    """


    disruptionBudget?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRolloutItems0DisruptionBudgetItems0]

    mode?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecInitProviderRolloutItems0DisruptionBudgetItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment spec init provider rollout items0 disruption budget items0

    Attributes
    ----------
    fixed : float, default is Undefined, optional
        Specifies a fixed value.
    percentage : float, default is Undefined, optional
        Specifies the relative value defined as a percentage, which will be multiplied by a reference value.
    """


    fixed?: float

    percentage?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecProviderConfigRef:
    """
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecProviderConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecProviderConfigRefPolicy


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecProviderConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsTo:
    """
    PublishConnectionDetailsTo specifies the connection secret config which
    contains a name, metadata and a reference to secret store config to
    which any connection details for this managed resource should be written.
    Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    configRef : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToConfigRef, default is Undefined, optional
        config ref
    metadata : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToMetadata, default is Undefined, optional
        metadata
    name : str, default is Undefined, required
        Name is the name of the connection secret.
    """


    configRef?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToConfigRef

    metadata?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToMetadata

    name: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToConfigRef:
    """
    SecretStoreConfigRef specifies which secret store config should be used
    for this ConnectionSecret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    policy : OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToConfigRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    policy?: OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToConfigRefPolicy


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToConfigRefPolicy:
    """
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecPublishConnectionDetailsToMetadata:
    """
    Metadata is the metadata for connection secret.

    Attributes
    ----------
    annotations : {str:str}, default is Undefined, optional
        Annotations are the annotations to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.annotations".
        - It is up to Secret Store implementation for others store types.
    labels : {str:str}, default is Undefined, optional
        Labels are the labels/tags to be added to connection secret.
        - For Kubernetes secrets, this will be used as "metadata.labels".
        - It is up to Secret Store implementation for others store types.
    $type : str, default is Undefined, optional
        Type is the SecretType for the connection secret.
        - Only valid for Kubernetes Secret Stores.
    """


    annotations?: {str:str}

    labels?: {str:str}

    $type?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentSpecWriteConnectionSecretToRef:
    """
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.
    This field is planned to be replaced in a future release in favor of
    PublishConnectionDetailsTo. Currently, both could be set independently
    and connection details would be published to both without affecting
    each other.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    namespace : str, default is Undefined, required
        Namespace of the secret.
    """


    name: str

    namespace: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatus:
    """
    PatchDeploymentStatus defines the observed state of PatchDeployment.

    Attributes
    ----------
    atProvider : OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    """


    atProvider?: OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProvider

    conditions?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusConditionsItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProvider:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider

    Attributes
    ----------
    createTime : str, default is Undefined, optional
        Time the patch deployment was created. Timestamp is in RFC3339 text format.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    description : str, default is Undefined, optional
        Description of the patch deployment. Length of the description is limited to 1024 characters.
    duration : str, default is Undefined, optional
        Duration of the patch. After the duration ends, the patch times out.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s"
    id : str, default is Undefined, optional
        an identifier for the resource with format {{name}}
    instanceFilter : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderInstanceFilterItems0], default is Undefined, optional
        VM instances to patch.
        Structure is documented below.
    lastExecuteTime : str, default is Undefined, optional
        The last time a patch job was started by this deployment. Timestamp is in RFC3339 text format.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    name : str, default is Undefined, optional
        Unique name for the patch deployment resource in a project.
        The patch deployment name is in the form: projects/{project_id}/patchDeployments/{patchDeploymentId}.
    oneTimeSchedule : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderOneTimeScheduleItems0], default is Undefined, optional
        Schedule a one-time execution.
        Structure is documented below.
    patchConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0], default is Undefined, optional
        Patch configuration that is applied.
        Structure is documented below.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    recurringSchedule : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0], default is Undefined, optional
        Schedule recurring executions.
        Structure is documented below.
    rollout : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRolloutItems0], default is Undefined, optional
        Rollout strategy of the patch job.
        Structure is documented below.
    updateTime : str, default is Undefined, optional
        Time the patch deployment was last updated. Timestamp is in RFC3339 text format.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    """


    createTime?: str

    description?: str

    duration?: str

    id?: str

    instanceFilter?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderInstanceFilterItems0]

    lastExecuteTime?: str

    name?: str

    oneTimeSchedule?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderOneTimeScheduleItems0]

    patchConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0]

    project?: str

    recurringSchedule?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0]

    rollout?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRolloutItems0]

    updateTime?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderInstanceFilterItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider instance filter items0

    Attributes
    ----------
    $all : bool, default is Undefined, optional
        Target all VM instances in the project. If true, no other criteria is permitted.
    groupLabels : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderInstanceFilterItems0GroupLabelsItems0], default is Undefined, optional
        Targets VM instances matching ANY of these GroupLabels. This allows targeting of disparate groups of VM instances.
        Structure is documented below.
    instanceNamePrefixes : [str], default is Undefined, optional
        Targets VMs whose name starts with one of these prefixes. Similar to labels, this is another way to group
        VMs when targeting configs, for example prefix="prod-".
    instances : [str], default is Undefined, optional
        Targets any of the VM instances specified. Instances are specified by their URI in the form zones/{{zone}}/instances/{{instance_name}},
        projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}, or
        https://www.googleapis.com/compute/v1/projects/{{project_id}}/zones/{{zone}}/instances/{{instance_name}}
    zones : [str], default is Undefined, optional
        Targets VM instances in ANY of these zones. Leave empty to target VM instances in any zone.
    """


    $all?: bool

    groupLabels?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderInstanceFilterItems0GroupLabelsItems0]

    instanceNamePrefixes?: [str]

    instances?: [str]

    zones?: [str]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderInstanceFilterItems0GroupLabelsItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider instance filter items0 group labels items0

    Attributes
    ----------
    labels : {str:str}, default is Undefined, optional
        Compute Engine instance labels that must be present for a VM instance to be targeted by this filter
    """


    labels?: {str:str}


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderOneTimeScheduleItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider one time schedule items0

    Attributes
    ----------
    executeTime : str, default is Undefined, optional
        The desired patch job execution time. A timestamp in RFC3339 UTC "Zulu" format,
        accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    """


    executeTime?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0

    Attributes
    ----------
    apt : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0AptItems0], default is Undefined, optional
        Apt update settings. Use this setting to override the default apt patch rules.
        Structure is documented below.
    goo : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0GooItems0], default is Undefined, optional
        goo update settings. Use this setting to override the default goo patch rules.
        Structure is documented below.
    migInstancesAllowed : bool, default is Undefined, optional
        Allows the patch job to run on Managed instance groups (MIGs).
    postStep : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0], default is Undefined, optional
        The ExecStep to run after the patch update.
        Structure is documented below.
    preStep : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0], default is Undefined, optional
        The ExecStep to run before the patch update.
        Structure is documented below.
    rebootConfig : str, default is Undefined, optional
        Post-patch reboot settings.
        Possible values are: DEFAULT, ALWAYS, NEVER.
    windowsUpdate : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0WindowsUpdateItems0], default is Undefined, optional
        Windows update settings. Use this setting to override the default Windows patch rules.
        Structure is documented below.
    yum : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0YumItems0], default is Undefined, optional
        Yum update settings. Use this setting to override the default yum patch rules.
        Structure is documented below.
    zypper : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0ZypperItems0], default is Undefined, optional
        zypper update settings. Use this setting to override the default zypper patch rules.
        Structure is documented below.
    """


    apt?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0AptItems0]

    goo?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0GooItems0]

    migInstancesAllowed?: bool

    postStep?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0]

    preStep?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0]

    rebootConfig?: str

    windowsUpdate?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0WindowsUpdateItems0]

    yum?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0YumItems0]

    zypper?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0ZypperItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0AptItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 apt items0

    Attributes
    ----------
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePackages : [str], default is Undefined, optional
        An exclusive list of packages to be updated. These are the only packages that will be updated.
        If these packages are not installed, they will be ignored. This field cannot be specified with
        any other patch configuration fields.
    $type : str, default is Undefined, optional
        By changing the type to DIST, the patching is performed using apt-get dist-upgrade instead.
        Possible values are: DIST, UPGRADE.
    """


    excludes?: [str]

    exclusivePackages?: [str]

    $type?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0GooItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 goo items0

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        goo update settings. Use this setting to override the default goo patch rules.
    """


    enabled?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 post step items0

    Attributes
    ----------
    linuxExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Linux VMs targeted by the PatchJob.
        Structure is documented below.
    windowsExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Windows VMs targeted by the PatchJob.
        Structure is documented below.
    """


    linuxExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0]

    windowsExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 post step items0 linux exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0LinuxExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 post step items0 linux exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 post step items0 windows exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PostStepItems0WindowsExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 post step items0 windows exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 pre step items0

    Attributes
    ----------
    linuxExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Linux VMs targeted by the PatchJob.
        Structure is documented below.
    windowsExecStepConfig : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0], default is Undefined, optional
        The ExecStepConfig for all Windows VMs targeted by the PatchJob.
        Structure is documented below.
    """


    linuxExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0]

    windowsExecStepConfig?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 pre step items0 linux exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0LinuxExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 pre step items0 linux exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 pre step items0 windows exec step config items0

    Attributes
    ----------
    allowedSuccessCodes : [float], default is Undefined, optional
        Defaults to [0]. A list of possible return values that the execution can return to indicate a success.
    gcsObject : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0], default is Undefined, optional
        A Cloud Storage object containing the executable.
        Structure is documented below.
    interpreter : str, default is Undefined, optional
        The script interpreter to use to run the script. If no interpreter is specified the script will
        be executed directly, which will likely only succeed for scripts with shebang lines.
        Possible values are: SHELL, POWERSHELL.
    localPath : str, default is Undefined, optional
        An absolute path to the executable on the VM.
    """


    allowedSuccessCodes?: [float]

    gcsObject?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0]

    interpreter?: str

    localPath?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0PreStepItems0WindowsExecStepConfigItems0GcsObjectItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 pre step items0 windows exec step config items0 gcs object items0

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Bucket of the Cloud Storage object.
    generationNumber : str, default is Undefined, optional
        Generation number of the Cloud Storage object. This is used to ensure that the ExecStep specified by this PatchJob does not change.
    object : str, default is Undefined, optional
        Name of the Cloud Storage object.
    """


    bucket?: str

    generationNumber?: str

    object?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0WindowsUpdateItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 windows update items0

    Attributes
    ----------
    classifications : [str], default is Undefined, optional
        Only apply updates of these windows update classifications. If empty, all updates are applied.
        Each value may be one of: CRITICAL, SECURITY, DEFINITION, DRIVER, FEATURE_PACK, SERVICE_PACK, TOOL, UPDATE_ROLLUP, UPDATE.
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePatches : [str], default is Undefined, optional
        An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command.
        This field must not be used with any other patch configuration fields.
    """


    classifications?: [str]

    excludes?: [str]

    exclusivePatches?: [str]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0YumItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 yum items0

    Attributes
    ----------
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePackages : [str], default is Undefined, optional
        An exclusive list of packages to be updated. These are the only packages that will be updated.
        If these packages are not installed, they will be ignored. This field cannot be specified with
        any other patch configuration fields.
    minimal : bool, default is Undefined, optional
        Will cause patch to run yum update-minimal instead.
    security : bool, default is Undefined, optional
        Adds the --security flag to yum update. Not supported on all platforms.
    """


    excludes?: [str]

    exclusivePackages?: [str]

    minimal?: bool

    security?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderPatchConfigItems0ZypperItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider patch config items0 zypper items0

    Attributes
    ----------
    categories : [str], default is Undefined, optional
        Install only patches with these categories. Common categories include security, recommended, and feature.
    excludes : [str], default is Undefined, optional
        List of packages to exclude from update.
    exclusivePatches : [str], default is Undefined, optional
        An exclusive list of patches to be updated. These are the only patches that will be installed using 'zypper patch patch:' command.
        This field must not be used with any other patch configuration fields.
    severities : [str], default is Undefined, optional
        Install only patches with these severities. Common severities include critical, important, moderate, and low.
    withOptional : bool, default is Undefined, optional
        Adds the --with-optional flag to zypper patch.
    withUpdate : bool, default is Undefined, optional
        Adds the --with-update flag, to zypper patch.
    """


    categories?: [str]

    excludes?: [str]

    exclusivePatches?: [str]

    severities?: [str]

    withOptional?: bool

    withUpdate?: bool


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider recurring schedule items0

    Attributes
    ----------
    endTime : str, default is Undefined, optional
        The end time at which a recurring patch deployment schedule is no longer active.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    lastExecuteTime : str, default is Undefined, optional
        (Output)
        The time the last patch job ran successfully.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    monthly : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0MonthlyItems0], default is Undefined, optional
        Schedule with monthly executions.
        Structure is documented below.
    nextExecuteTime : str, default is Undefined, optional
        (Output)
        The time the next patch job is scheduled to run.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    startTime : str, default is Undefined, optional
        The time that the recurring schedule becomes effective. Defaults to createTime of the patch deployment.
        A timestamp in RFC3339 UTC "Zulu" format, accurate to nanoseconds. Example: "2014-10-02T15:01:23.045123456Z".
    timeOfDay : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0TimeOfDayItems0], default is Undefined, optional
        Time of the day to run a recurring deployment.
        Structure is documented below.
    timeZone : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0TimeZoneItems0], default is Undefined, optional
        Defines the time zone that timeOfDay is relative to. The rules for daylight saving time are
        determined by the chosen time zone.
        Structure is documented below.
    weekly : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0WeeklyItems0], default is Undefined, optional
        Schedule with weekly executions.
        Structure is documented below.
    """


    endTime?: str

    lastExecuteTime?: str

    monthly?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0MonthlyItems0]

    nextExecuteTime?: str

    startTime?: str

    timeOfDay?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0TimeOfDayItems0]

    timeZone?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0TimeZoneItems0]

    weekly?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0WeeklyItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0MonthlyItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider recurring schedule items0 monthly items0

    Attributes
    ----------
    monthDay : float, default is Undefined, optional
        One day of the month. 1-31 indicates the 1st to the 31st day. -1 indicates the last day of the month.
        Months without the target day will be skipped. For example, a schedule to run "every month on the 31st"
        will not run in February, April, June, etc.
    weekDayOfMonth : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0], default is Undefined, optional
        Week day in a month.
        Structure is documented below.
    """


    monthDay?: float

    weekDayOfMonth?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0]


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0MonthlyItems0WeekDayOfMonthItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider recurring schedule items0 monthly items0 week day of month items0

    Attributes
    ----------
    dayOfWeek : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
        Possible values are: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
    dayOffset : float, default is Undefined, optional
        Represents the number of days before or after the given week day of month that the patch deployment is scheduled for.
    weekOrdinal : float, default is Undefined, optional
        Week number in a month. 1-4 indicates the 1st to 4th week of the month. -1 indicates the last week of the month.
    """


    dayOfWeek?: str

    dayOffset?: float

    weekOrdinal?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0TimeOfDayItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider recurring schedule items0 time of day items0

    Attributes
    ----------
    hours : float, default is Undefined, optional
        Hours of day in 24 hour format. Should be from 0 to 23.
        An API may choose to allow the value "24:00:00" for scenarios like business closing time.
    minutes : float, default is Undefined, optional
        Minutes of hour of day. Must be from 0 to 59.
    nanos : float, default is Undefined, optional
        Fractions of seconds in nanoseconds. Must be from 0 to 999,999,999.
    seconds : float, default is Undefined, optional
        Seconds of minutes of the time. Must normally be from 0 to 59. An API may allow the value 60 if it allows leap-seconds.
    """


    hours?: float

    minutes?: float

    nanos?: float

    seconds?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0TimeZoneItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider recurring schedule items0 time zone items0

    Attributes
    ----------
    id : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
    version : str, default is Undefined, optional
        IANA Time Zone Database version number, e.g. "2019a".
    """


    id?: str

    version?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRecurringScheduleItems0WeeklyItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider recurring schedule items0 weekly items0

    Attributes
    ----------
    dayOfWeek : str, default is Undefined, optional
        IANA Time Zone Database time zone, e.g. "America/New_York".
        Possible values are: MONDAY, TUESDAY, WEDNESDAY, THURSDAY, FRIDAY, SATURDAY, SUNDAY.
    """


    dayOfWeek?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRolloutItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider rollout items0

    Attributes
    ----------
    disruptionBudget : [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRolloutItems0DisruptionBudgetItems0], default is Undefined, optional
        The maximum number (or percentage) of VMs per zone to disrupt at any given moment. The number of VMs calculated from multiplying the percentage by the total number of VMs in a zone is rounded up.
        During patching, a VM is considered disrupted from the time the agent is notified to begin until patching has completed. This disruption time includes the time to complete reboot and any post-patch steps.
        A VM contributes to the disruption budget if its patching operation fails either when applying the patches, running pre or post patch steps, or if it fails to respond with a success notification before timing out. VMs that are not running or do not have an active agent do not count toward this disruption budget.
        For zone-by-zone rollouts, if the disruption budget in a zone is exceeded, the patch job stops, because continuing to the next zone requires completion of the patch process in the previous zone.
        For example, if the disruption budget has a fixed value of 10, and 8 VMs fail to patch in the current zone, the patch job continues to patch 2 VMs at a time until the zone is completed. When that zone is completed successfully, patching begins with 10 VMs at a time in the next zone. If 10 VMs in the next zone fail to patch, the patch job stops.
        Structure is documented below.
    mode : str, default is Undefined, optional
        Mode of the patch rollout.
        Possible values are: ZONE_BY_ZONE, CONCURRENT_ZONES.
    """


    disruptionBudget?: [OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRolloutItems0DisruptionBudgetItems0]

    mode?: str


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusAtProviderRolloutItems0DisruptionBudgetItems0:
    """
    osconfig gcp upbound io v1beta1 patch deployment status at provider rollout items0 disruption budget items0

    Attributes
    ----------
    fixed : float, default is Undefined, optional
        Specifies a fixed value.
    percentage : float, default is Undefined, optional
        Specifies the relative value defined as a percentage, which will be multiplied by a reference value.
    """


    fixed?: float

    percentage?: float


schema OsconfigGcpUpboundIoV1beta1PatchDeploymentStatusConditionsItems0:
    """
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    reason: str

    status: str

    $type: str


