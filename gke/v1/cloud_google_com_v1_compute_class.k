"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema ComputeClass:
    r"""
    ComputeClass is a way to impact Cluster Autoscaler scaling
    decisions based on user preferences. It gives control over preference of
    hardware to be selected by Cluster Autoscaler.
    Given ComputeClass affects only workloads using workload separation
    label equal to CCs name, except ComputeClass with name default
    which will be used for workloads not specifying any preferences.

    Attributes
    ----------
    apiVersion : str, default is "cloud.google.com/v1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ComputeClass", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : CloudGoogleComV1ComputeClassSpec, default is Undefined, required
        spec
    status : CloudGoogleComV1ComputeClassStatus, default is Undefined, optional
        status
    """


    apiVersion: "cloud.google.com/v1" = "cloud.google.com/v1"

    kind: "ComputeClass" = "ComputeClass"

    metadata?: v1.ObjectMeta

    spec: CloudGoogleComV1ComputeClassSpec

    status?: CloudGoogleComV1ComputeClassStatus


schema CloudGoogleComV1ComputeClassSpec:
    r"""
    Specification of the ComputeClass object.
    More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#spec-and-status.

    Attributes
    ----------
    activeMigration : CloudGoogleComV1ComputeClassSpecActiveMigration, default is Undefined, optional
        active migration
    autoscalingPolicy : CloudGoogleComV1ComputeClassSpecAutoscalingPolicy, default is Undefined, optional
        autoscaling policy
    nodePoolAutoCreation : CloudGoogleComV1ComputeClassSpecNodePoolAutoCreation, default is Undefined, optional
        node pool auto creation
    nodePoolConfig : CloudGoogleComV1ComputeClassSpecNodePoolConfig, default is Undefined, optional
        node pool config
    priorities : [CloudGoogleComV1ComputeClassSpecPrioritiesItems0], default is Undefined, optional
        Priorities is a description of user preferences to be
        used by a given ComputeClass.
    priorityDefaults : CloudGoogleComV1ComputeClassSpecPriorityDefaults, default is Undefined, optional
        priority defaults
    whenUnsatisfiable : str, default is "ScaleUpAnyway", required
        WhenUnsatisfiable describes autoscaler behaviour in case none
        of the provided priorities is satisfiable.
        Currently supported values:
        * ScaleUpAnyway
        * DoNotScaleUp
    """


    activeMigration?: CloudGoogleComV1ComputeClassSpecActiveMigration

    autoscalingPolicy?: CloudGoogleComV1ComputeClassSpecAutoscalingPolicy

    nodePoolAutoCreation?: CloudGoogleComV1ComputeClassSpecNodePoolAutoCreation

    nodePoolConfig?: CloudGoogleComV1ComputeClassSpecNodePoolConfig

    priorities?: [CloudGoogleComV1ComputeClassSpecPrioritiesItems0] = []

    priorityDefaults?: CloudGoogleComV1ComputeClassSpecPriorityDefaults

    whenUnsatisfiable: "ScaleUpAnyway" | "DoNotScaleUp" = "ScaleUpAnyway"


    check:
        len(priorities) >= 0 if priorities


schema CloudGoogleComV1ComputeClassSpecActiveMigration:
    r"""
    ActiveMigration describes settings related to active reconciliation of
    a given ComputeClass.

    Attributes
    ----------
    ensureAllDaemonSetPodsRunning : bool, default is Undefined, optional
        EnsureAllDaemonSetPodsRunning defines whether node pools should be migrated
        to larger ones to ensure that all daemon sets are schedulable.
    optimizeRulePriority : bool, default is Undefined, required
        OptimizeRulePriority defines whether workloads affected by given
        ComputeClass should be migrated to nodepool defined by higher priority rule, if possible.
    """


    ensureAllDaemonSetPodsRunning?: bool

    optimizeRulePriority: bool = False


schema CloudGoogleComV1ComputeClassSpecAutoscalingPolicy:
    r"""
    AutoscalingPolicy describes settings related to active reconciliation of
    a given ComputeClass.

    Attributes
    ----------
    consolidationDelayMinutes : int, default is Undefined, optional
        ConsolidationDelayMinutes determines how long a node should be unneeded before it is eligible for scale down.
        Minimum duration is 1 minute, maximum is 24 hours or 1440 minutes
    consolidationThreshold : int, default is Undefined, optional
        ConsolidationThreshold determines resource utilization threshold below which a node can be considered for scale down.
    gpuConsolidationThreshold : int, default is Undefined, optional
        GPUConsolidationThreshold determines GPU resource utilization threshold below which a node can be considered for scale down.
        Utilization calculation only cares about GPU resource for accelerator node, CPU and memory utilization will be ignored.
    """


    consolidationDelayMinutes?: int

    consolidationThreshold?: int

    gpuConsolidationThreshold?: int


    check:
        consolidationDelayMinutes <= 1440 if consolidationDelayMinutes not in [None, Undefined]
        consolidationDelayMinutes >= 1 if consolidationDelayMinutes not in [None, Undefined]
        consolidationThreshold <= 100 if consolidationThreshold not in [None, Undefined]
        consolidationThreshold >= 0 if consolidationThreshold not in [None, Undefined]
        gpuConsolidationThreshold <= 100 if gpuConsolidationThreshold not in [None, Undefined]
        gpuConsolidationThreshold >= 0 if gpuConsolidationThreshold not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecNodePoolAutoCreation:
    r"""
    NodePoolAutoCreation describes the auto provisioning settings for a given
    ComputeClass.

    Attributes
    ----------
    enabled : bool, default is Undefined, required
        Enabled indicates whether NodePoolAutoCreation is enabled for a given ComputeClass.
    """


    enabled: bool = False


schema CloudGoogleComV1ComputeClassSpecNodePoolConfig:
    r"""
    NodePoolConfig defines required node pool configuration. Existing node pools will be matched with the ComputeClass
    only if their configuration match this field. Auto-provisioned node pools will be created with this configuration.

    Attributes
    ----------
    serviceAccount : str, default is Undefined, optional
        ServiceAccount used by the node pool.
    """


    serviceAccount?: str


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0:
    r"""
    Priority is a specification of preferred machine characteristics.

    Attributes
    ----------
    gpu : CloudGoogleComV1ComputeClassSpecPrioritiesItems0Gpu, default is Undefined, optional
        gpu
    machineFamily : str, default is Undefined, optional
        Machine family describes preferred instance family for a node. If none is specified,
        the default autoprovisioning machine family is used.
    machineType : str, default is Undefined, optional
        MachineType defines preferred machine type for a node.
    maxPodsPerNode : int, default is Undefined, optional
        MaxPodsPerNode describes the maximum number of pods a node can accommodate.
    maxRunDurationSeconds : int, default is Undefined, optional
        MaxRunDurationSeconds defines the maximum duration for the nodes to exist. If unspecified, the nodes can exist indefinitely.
    minCores : int, default is Undefined, optional
        MinCores describes a minimum number of CPU cores of a node.
    minMemoryGb : int, default is Undefined, optional
        MinMemoryGb describes a minimum GBs of memory of a node.
    nodeSystemConfig : CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfig, default is Undefined, optional
        node system config
    nodepools : [str], default is Undefined, optional
        Nodepools describes preference of specific, preexisting nodepools.
    reservations : CloudGoogleComV1ComputeClassSpecPrioritiesItems0Reservations, default is Undefined, optional
        reservations
    spot : bool, default is Undefined, optional
        Spot if set to true specifies that a node should be a spot instance, on-demand otherwise.
    storage : CloudGoogleComV1ComputeClassSpecPrioritiesItems0Storage, default is Undefined, optional
        storage
    tpu : CloudGoogleComV1ComputeClassSpecPrioritiesItems0Tpu, default is Undefined, optional
        tpu
    """


    gpu?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0Gpu

    machineFamily?: str

    machineType?: str

    maxPodsPerNode?: int

    maxRunDurationSeconds?: int

    minCores?: int

    minMemoryGb?: int

    nodeSystemConfig?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfig

    nodepools?: [str]

    reservations?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0Reservations

    spot?: bool

    storage?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0Storage

    tpu?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0Tpu


    check:
        maxPodsPerNode <= 256 if maxPodsPerNode not in [None, Undefined]
        maxPodsPerNode >= 8 if maxPodsPerNode not in [None, Undefined]
        minCores >= 0 if minCores not in [None, Undefined]
        minMemoryGb >= 0 if minMemoryGb not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0Gpu:
    r"""
    Gpu defines preferred GPU config for a node.

    Attributes
    ----------
    count : int, default is Undefined, optional
        Count describes preferred count of GPUs for a node.
    driverVersion : str, default is "default", optional
        DriverVersion describes version of GPU driver for a node.
    $type : str, default is Undefined, optional
        Type describes preferred GPU accelerator type for a node.
    """


    count?: int

    driverVersion?: "default" | "latest" = "default"

    $type?: str


    check:
        count >= 1 if count not in [None, Undefined]
        len($type) >= 1 if $type


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfig:
    r"""
    NodeSystemConfig defines node system config for a node.

    Attributes
    ----------
    kubeletConfig : CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigKubeletConfig, default is Undefined, optional
        kubelet config
    linuxNodeConfig : CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfig, default is Undefined, optional
        linux node config
    """


    kubeletConfig?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigKubeletConfig

    linuxNodeConfig?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfig


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigKubeletConfig:
    r"""
    KubeletConfig defines kubelet config for a node.

    Attributes
    ----------
    cpuCfsQuota : bool, default is Undefined, optional
        This setting enforces the Pod's CPU limit. Setting this value to false means that the CPU limits for Pods are ignored.
        Ignoring CPU limits might be desirable in certain scenarios where Pods are sensitive to CPU limits.
        The risk of disabling cpuCFSQuota is that a rogue Pod can consume more CPU resources than intended.
    cpuCfsQuotaPeriod : str, default is Undefined, optional
        This setting sets the CPU CFS quota period value, cpu.cfs_period_us, which specifies the period of how often a cgroup's access to CPU resources should be reallocated.
        This option lets you tune the CPU throttling behavior. Value must be 1ms <= period <= 1s.
    cpuManagerPolicy : str, default is Undefined, optional
        This setting controls the kubelet's CPU Manager Policy. The default value is none which is the default CPU affinity scheme, providing no affinity beyond what the OS scheduler does automatically.
        Setting this value to static allows Pods in the Guaranteed QoS class with integer CPU requests to be assigned exclusive use of CPUs.
    podPidsLimit : int, default is Undefined, optional
        This setting sets the maximum number of process IDs (PIDs) that each Pod can use.
    """


    cpuCfsQuota?: bool

    cpuCfsQuotaPeriod?: str

    cpuManagerPolicy?: "none" | "static"

    podPidsLimit?: int


    check:
        _regex_match(str(cpuCfsQuotaPeriod), r"^([1-9][0-9]*)m?s$") if cpuCfsQuotaPeriod
        podPidsLimit <= 4.194304e+06 if podPidsLimit not in [None, Undefined]
        podPidsLimit >= 1024 if podPidsLimit not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfig:
    r"""
    LinuxNodeConfig defines linux node config for a node.

    Attributes
    ----------
    hugepageConfig : CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfigHugepageConfig, default is Undefined, optional
        hugepage config
    sysctls : CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfigSysctls, default is Undefined, optional
        sysctls
    """


    hugepageConfig?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfigHugepageConfig

    sysctls?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfigSysctls


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfigHugepageConfig:
    r"""
    HugepagesConfig defines hugepages config for a node.

    Attributes
    ----------
    hugepage_size1g : int, default is Undefined, optional
        Number of 1-gigabyte-sized huge pages to allocate.
    hugepage_size2m : int, default is Undefined, optional
        Number of 2-megabyte-sized huge pages to allocate.
    """


    hugepage_size1g?: int

    hugepage_size2m?: int


    check:
        hugepage_size1g >= 1 if hugepage_size1g not in [None, Undefined]
        hugepage_size2m >= 1 if hugepage_size2m not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0NodeSystemConfigLinuxNodeConfigSysctls:
    r"""
    SysctlsConfig defines sysctls config for a node.

    Attributes
    ----------
    "net.core.busy_poll" : int, default is Undefined, optional
        Low latency busy poll timeout for poll and select. (needs CONFIG_NET_RX_BUSY_POLL) Approximate time in us to busy loop waiting for events.
    "net.core.busy_read" : int, default is Undefined, optional
        Low latency busy poll timeout for socket reads. (needs CONFIG_NET_RX_BUSY_POLL) Approximate time in us to busy loop waiting for packets on the device queue.
    "net.core.netdev_max_backlog" : int, default is Undefined, optional
        Maximum number of packets, queued on the INPUT side, when the interface receives packets faster than kernel can process them.
    "net.core.optmem_max" : int, default is Undefined, optional
        Maximum ancillary buffer size allowed per socket. Ancillary data is a sequence of struct cmsghdr structures with appended data.
    "net.core.rmem_max" : int, default is Undefined, optional
        The maximum receive socket buffer size in bytes.
    "net.core.somaxconn" : int, default is Undefined, optional
        Limit of socket listen() backlog, known in userspace as SOMAXCONN. Defaults to 128. See also tcp_max_syn_backlog for additional tuning for TCP sockets.
    "net.core.wmem_default" : int, default is Undefined, optional
        The default setting (in bytes) of the socket send buffer.
    "net.core.wmem_max" : int, default is Undefined, optional
        The maximum send socket buffer size in bytes.
    "net.ipv4.tcp_rmem" : str, default is Undefined, optional
        Minimal size of receive buffer used by UDP sockets in moderation. Each UDP socket is able to use the size for receiving data, even if total pages of UDP sockets exceed udp_mem pressure. The unit is byte. Default: 1 page. The three values are: min, default, max. Eg. '4096 87380 6291456'.
    "net.ipv4.tcp_tw_reuse" : bool, default is Undefined, optional
        Allow to reuse TIME-WAIT sockets for new connections when it is safe from protocol viewpoint. Default value is 0. It should not be changed without advice/request of technical experts.
    "net.ipv4.tcp_wmem" : str, default is Undefined, optional
        Minimal size of send buffer used by UDP sockets in moderation. Each UDP socket is able to use the size for sending data, even if total pages of UDP sockets exceed udp_mem pressure. The unit is byte. Default: 1 page. The three values are: min, default, max. Eg. '4096 87380 6291456'.
    "net.ipv6.conf.all.disable_ipv6" : bool, default is Undefined, optional
        Changing this value is same as changing conf/default/disable_ipv6 setting and also all per-interface disable_ipv6 settings to the same value.
    "net.ipv6.conf.default.disable_ipv6" : bool, default is Undefined, optional
        Disable IPv6 operation.
    "vm.max_map_count" : int, default is Undefined, optional
        Maximum number of memory map areas a process may have.
    """


    "net.core.busy_poll"?: int

    "net.core.busy_read"?: int

    "net.core.netdev_max_backlog"?: int

    "net.core.optmem_max"?: int

    "net.core.rmem_max"?: int

    "net.core.somaxconn"?: int

    "net.core.wmem_default"?: int

    "net.core.wmem_max"?: int

    "net.ipv4.tcp_rmem"?: str

    "net.ipv4.tcp_tw_reuse"?: bool

    "net.ipv4.tcp_wmem"?: str

    "net.ipv6.conf.all.disable_ipv6"?: bool

    "net.ipv6.conf.default.disable_ipv6"?: bool

    "vm.max_map_count"?: int


    check:
        True  # Skip validations for "net.core.busy_poll"
        True  # Skip validations for "net.core.busy_read"
        True  # Skip validations for "net.core.netdev_max_backlog"
        True  # Skip validations for "net.core.optmem_max"
        True  # Skip validations for "net.core.rmem_max"
        True  # Skip validations for "net.core.somaxconn"
        True  # Skip validations for "net.core.wmem_default"
        True  # Skip validations for "net.core.wmem_max"
        True  # Skip validations for "vm.max_map_count"


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0Reservations:
    r"""
    Reservations defines reservations config for a node.

    Attributes
    ----------
    affinity : str, default is Undefined, required
        ReservationAffinity affects reservations considered and the way how they are consumed.
        "Specific" means that only specific reservations are considered with no fallback possible.
        "AnyBestEffort" affinity would consider any non-specific reservation available
        to be claimed with a fallback to on-demand nodes in case of none claimable.
        "None" affinity would prevent reservations from being used
    specific : [CloudGoogleComV1ComputeClassSpecPrioritiesItems0ReservationsSpecificItems0], default is Undefined, optional
        Specific is a non prioritized list of specific reservations to be considered by the priority rule.
    """


    affinity: "Specific" | "AnyBestEffort" | "None"

    specific?: [CloudGoogleComV1ComputeClassSpecPrioritiesItems0ReservationsSpecificItems0]


    check:
        len(specific) >= 0 if specific


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0ReservationsSpecificItems0:
    r"""
    SpecificReservation defines a single specific reservation to be consumed by the created node.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the reservation to be used.
    project : str, default is Undefined, optional
        Project is the project where the specific reservation lives.
    reservationBlock : CloudGoogleComV1ComputeClassSpecPrioritiesItems0ReservationsSpecificItems0ReservationBlock, default is Undefined, optional
        reservation block
    """


    name: str

    project?: str

    reservationBlock?: CloudGoogleComV1ComputeClassSpecPrioritiesItems0ReservationsSpecificItems0ReservationBlock


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0ReservationsSpecificItems0ReservationBlock:
    r"""
    ReservationBlock is the block of the reservation.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name is the name of the block.
    """


    name: str


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0Storage:
    r"""
    Storage describes storage config of a node.

    Attributes
    ----------
    bootDiskKMSKey : str, default is Undefined, optional
        BootDiskKMSKey defines a key used to encrypt the boot disk attached.
    bootDiskSize : int, default is Undefined, optional
        BootDiskSize defines the size of a disk attached to node, specified in GB.
    bootDiskType : str, default is Undefined, optional
        BootDiskType defines type of the disk attached to the node.
        Note that available boot disk types depend on the machine family / machine type selected.
        Currently supported types:
        * pd-balanced
        * pd-standard
        * pd-ssd
        * hyperdisk-balanced
    localSSDCount : int, default is Undefined, optional
        LocalSSDCount defines a number of local SSDs attached to node.
    secondaryBootDisks : [CloudGoogleComV1ComputeClassSpecPrioritiesItems0StorageSecondaryBootDisksItems0], default is Undefined, optional
        SecondaryBootDisks represent persistent disks attached to a node with special configurations based on their modes.
    """


    bootDiskKMSKey?: str

    bootDiskSize?: int

    bootDiskType?: "pd-balanced" | "pd-standard" | "pd-ssd" | "hyperdisk-balanced"

    localSSDCount?: int

    secondaryBootDisks?: [CloudGoogleComV1ComputeClassSpecPrioritiesItems0StorageSecondaryBootDisksItems0]


    check:
        _regex_match(str(bootDiskKMSKey), r"projects/[^/]+/locations/[^/]+/keyRings/[^/]+/cryptoKeys/[^/]+") if bootDiskKMSKey
        bootDiskSize >= 10 if bootDiskSize not in [None, Undefined]
        localSSDCount >= 1 if localSSDCount not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0StorageSecondaryBootDisksItems0:
    r"""
    SecondaryBootDisk represents a persistent disk attached to a node with special configurations based on its mode.

    Attributes
    ----------
    diskImageName : str, default is Undefined, required
        The name of the disk image.
    mode : str, default is Undefined, optional
        Currently supported modes:
        * MODE_UNSPECIFIED - MODE_UNSPECIFIED is when mode is not set.
        * CONTAINER_IMAGE_CACHE - it is for using the secondary boot disk as a container image cache.
    project : str, default is Undefined, optional
        The name of the project that the disk image belongs to.
    """


    diskImageName: str

    mode?: "MODE_UNSPECIFIED" | "CONTAINER_IMAGE_CACHE"

    project?: str


schema CloudGoogleComV1ComputeClassSpecPrioritiesItems0Tpu:
    r"""
    Tpu defines preferred TPU config for a node.

    Attributes
    ----------
    count : int, default is Undefined, optional
        Count describes preferred count of TPU chips for a node.
    topology : str, default is Undefined, optional
        Topology describes preferred TPU topology of a node.
    $type : str, default is Undefined, optional
        Type describes preferred TPU type for a node.
    """


    count?: int

    topology?: str

    $type?: str


schema CloudGoogleComV1ComputeClassSpecPriorityDefaultsLocation:
    r"""
    Location defines the location of the compute class.

    Attributes
    ----------
    zones: [str], A list of Google Cloud zones to use. In order for a node to be used, it must belong to a node pool that spans exactly the same set of zones.
    locationPolicy?: str, Location policy specifies the strategy for selecting zones when scaling up a node pool managed by this Compute Class. It can be either ANY or BALANCED. This setting controls the distribution of new nodes across zones in the node pool's region and corresponds to the node pool setting of the same name.
    """

    zones?: [str]
    locationPolicy?: str

schema CloudGoogleComV1ComputeClassSpecPriorityDefaults:
    r"""
    PriorityDefaults define the default rules for all priorities if the rule doesn't exist in some priority.
    Note: PriorityDefaults doesn't apply to priorities with only Nodepools.

    Attributes
    ----------
    nodeSystemConfig : CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfig, default is Undefined, optional
        node system config
    
    location: CloudGoogleComV1ComputeClassSpecPriorityDefaultsLocation, default is Undefined, optional
    """


    nodeSystemConfig?: CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfig

    location?: CloudGoogleComV1ComputeClassSpecPriorityDefaultsLocation


schema CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfig:
    r"""
    NodeSystemConfig defines node system config for a node.

    Attributes
    ----------
    kubeletConfig : CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigKubeletConfig, default is Undefined, optional
        kubelet config
    linuxNodeConfig : CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfig, default is Undefined, optional
        linux node config
    """


    kubeletConfig?: CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigKubeletConfig

    linuxNodeConfig?: CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfig


schema CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigKubeletConfig:
    r"""
    KubeletConfig defines kubelet config for a node.

    Attributes
    ----------
    cpuCfsQuota : bool, default is Undefined, optional
        This setting enforces the Pod's CPU limit. Setting this value to false means that the CPU limits for Pods are ignored.
        Ignoring CPU limits might be desirable in certain scenarios where Pods are sensitive to CPU limits.
        The risk of disabling cpuCFSQuota is that a rogue Pod can consume more CPU resources than intended.
    cpuCfsQuotaPeriod : str, default is Undefined, optional
        This setting sets the CPU CFS quota period value, cpu.cfs_period_us, which specifies the period of how often a cgroup's access to CPU resources should be reallocated.
        This option lets you tune the CPU throttling behavior. Value must be 1ms <= period <= 1s.
    cpuManagerPolicy : str, default is Undefined, optional
        This setting controls the kubelet's CPU Manager Policy. The default value is none which is the default CPU affinity scheme, providing no affinity beyond what the OS scheduler does automatically.
        Setting this value to static allows Pods in the Guaranteed QoS class with integer CPU requests to be assigned exclusive use of CPUs.
    podPidsLimit : int, default is Undefined, optional
        This setting sets the maximum number of process IDs (PIDs) that each Pod can use.
    """


    cpuCfsQuota?: bool

    cpuCfsQuotaPeriod?: str

    cpuManagerPolicy?: "none" | "static"

    podPidsLimit?: int


    check:
        _regex_match(str(cpuCfsQuotaPeriod), r"^([1-9][0-9]*)m?s$") if cpuCfsQuotaPeriod
        podPidsLimit <= 4.194304e+06 if podPidsLimit not in [None, Undefined]
        podPidsLimit >= 1024 if podPidsLimit not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfig:
    r"""
    LinuxNodeConfig defines linux node config for a node.

    Attributes
    ----------
    hugepageConfig : CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfigHugepageConfig, default is Undefined, optional
        hugepage config
    sysctls : CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfigSysctls, default is Undefined, optional
        sysctls
    """


    hugepageConfig?: CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfigHugepageConfig

    sysctls?: CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfigSysctls


schema CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfigHugepageConfig:
    r"""
    HugepagesConfig defines hugepages config for a node.

    Attributes
    ----------
    hugepage_size1g : int, default is Undefined, optional
        Number of 1-gigabyte-sized huge pages to allocate.
    hugepage_size2m : int, default is Undefined, optional
        Number of 2-megabyte-sized huge pages to allocate.
    """


    hugepage_size1g?: int

    hugepage_size2m?: int


    check:
        hugepage_size1g >= 1 if hugepage_size1g not in [None, Undefined]
        hugepage_size2m >= 1 if hugepage_size2m not in [None, Undefined]


schema CloudGoogleComV1ComputeClassSpecPriorityDefaultsNodeSystemConfigLinuxNodeConfigSysctls:
    r"""
    SysctlsConfig defines sysctls config for a node.

    Attributes
    ----------
    "net.core.busy_poll" : int, default is Undefined, optional
        Low latency busy poll timeout for poll and select. (needs CONFIG_NET_RX_BUSY_POLL) Approximate time in us to busy loop waiting for events.
    "net.core.busy_read" : int, default is Undefined, optional
        Low latency busy poll timeout for socket reads. (needs CONFIG_NET_RX_BUSY_POLL) Approximate time in us to busy loop waiting for packets on the device queue.
    "net.core.netdev_max_backlog" : int, default is Undefined, optional
        Maximum number of packets, queued on the INPUT side, when the interface receives packets faster than kernel can process them.
    "net.core.optmem_max" : int, default is Undefined, optional
        Maximum ancillary buffer size allowed per socket. Ancillary data is a sequence of struct cmsghdr structures with appended data.
    "net.core.rmem_max" : int, default is Undefined, optional
        The maximum receive socket buffer size in bytes.
    "net.core.somaxconn" : int, default is Undefined, optional
        Limit of socket listen() backlog, known in userspace as SOMAXCONN. Defaults to 128. See also tcp_max_syn_backlog for additional tuning for TCP sockets.
    "net.core.wmem_default" : int, default is Undefined, optional
        The default setting (in bytes) of the socket send buffer.
    "net.core.wmem_max" : int, default is Undefined, optional
        The maximum send socket buffer size in bytes.
    "net.ipv4.tcp_rmem" : str, default is Undefined, optional
        Minimal size of receive buffer used by UDP sockets in moderation. Each UDP socket is able to use the size for receiving data, even if total pages of UDP sockets exceed udp_mem pressure. The unit is byte. Default: 1 page. The three values are: min, default, max. Eg. '4096 87380 6291456'.
    "net.ipv4.tcp_tw_reuse" : bool, default is Undefined, optional
        Allow to reuse TIME-WAIT sockets for new connections when it is safe from protocol viewpoint. Default value is 0. It should not be changed without advice/request of technical experts.
    "net.ipv4.tcp_wmem" : str, default is Undefined, optional
        Minimal size of send buffer used by UDP sockets in moderation. Each UDP socket is able to use the size for sending data, even if total pages of UDP sockets exceed udp_mem pressure. The unit is byte. Default: 1 page. The three values are: min, default, max. Eg. '4096 87380 6291456'.
    "net.ipv6.conf.all.disable_ipv6" : bool, default is Undefined, optional
        Changing this value is same as changing conf/default/disable_ipv6 setting and also all per-interface disable_ipv6 settings to the same value.
    "net.ipv6.conf.default.disable_ipv6" : bool, default is Undefined, optional
        Disable IPv6 operation.
    "vm.max_map_count" : int, default is Undefined, optional
        Maximum number of memory map areas a process may have.
    """


    "net.core.busy_poll"?: int

    "net.core.busy_read"?: int

    "net.core.netdev_max_backlog"?: int

    "net.core.optmem_max"?: int

    "net.core.rmem_max"?: int

    "net.core.somaxconn"?: int

    "net.core.wmem_default"?: int

    "net.core.wmem_max"?: int

    "net.ipv4.tcp_rmem"?: str

    "net.ipv4.tcp_tw_reuse"?: bool

    "net.ipv4.tcp_wmem"?: str

    "net.ipv6.conf.all.disable_ipv6"?: bool

    "net.ipv6.conf.default.disable_ipv6"?: bool

    "vm.max_map_count"?: int


    check:
        True  # Skip validations for "net.core.busy_poll"
        True  # Skip validations for "net.core.busy_read"
        True  # Skip validations for "net.core.netdev_max_backlog"
        True  # Skip validations for "net.core.optmem_max"
        True  # Skip validations for "net.core.rmem_max"
        True  # Skip validations for "net.core.somaxconn"
        True  # Skip validations for "net.core.wmem_default"
        True  # Skip validations for "net.core.wmem_max"
        True  # Skip validations for "vm.max_map_count"


schema CloudGoogleComV1ComputeClassStatus:
    r"""
    Status of the ComputeClass.

    Attributes
    ----------
    conditions : [CloudGoogleComV1ComputeClassStatusConditionsItems0], default is Undefined, optional
        Conditions represent the observations of a ComputeClass's current state.
    """


    conditions?: [CloudGoogleComV1ComputeClassStatusConditionsItems0]


schema CloudGoogleComV1ComputeClassStatusConditionsItems0:
    r"""
    Condition contains details for one aspect of the current state of this API Resource.
    ---
    This struct is intended for direct use as an array at the field path .status.conditions.  For example,


    	type FooStatus struct{
    	    // Represents the observations of a foo's current state.
    	    // Known .status.conditions.type are: "Available", "Progressing", and "Degraded"
    	    // +patchMergeKey=type
    	    // +patchStrategy=merge
    	    // +listType=map
    	    // +listMapKey=type
    	    Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type" protobuf:"bytes,1,rep,name=conditions"`


    	    // other fields
    	}

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
    message : str, default is Undefined, required
        message is a human readable message indicating details about the transition.
        This may be an empty string.
    observedGeneration : int, default is Undefined, optional
        observedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        reason contains a programmatic identifier indicating the reason for the condition's last transition.
        Producers of specific condition types may define expected values and meanings for this field,
        and whether the values are considered a guaranteed API.
        The value should be a CamelCase string.
        This field may not be empty.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
        ---
        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
        useful (see .node.status.conditions), the ability to deconflict is important.
        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
    """


    lastTransitionTime: str

    message: str

    observedGeneration?: int

    reason: str

    status: "True" | "False" | "Unknown"

    $type: str


    check:
        len(message) <= 32768
        observedGeneration >= 0 if observedGeneration not in [None, Undefined]
        len(reason) <= 1024
        len(reason) >= 1
        _regex_match(str(reason), r"^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$")
        len($type) <= 316
        _regex_match(str($type), r"^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$")


