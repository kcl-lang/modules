"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Build:
    """
    Build is the Schema for the builds API.

    Attributes
    ----------
    apiVersion : str, default is "camel.apache.org/v1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Build", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : CamelApacheOrgV1BuildSpec, default is Undefined, optional
        spec
    status : CamelApacheOrgV1BuildStatus, default is Undefined, optional
        status
    """


    apiVersion: "camel.apache.org/v1" = "camel.apache.org/v1"

    kind: "Build" = "Build"

    metadata?: v1.ObjectMeta

    spec?: CamelApacheOrgV1BuildSpec

    status?: CamelApacheOrgV1BuildStatus


schema CamelApacheOrgV1BuildSpec:
    """
    BuildSpec defines the list of tasks to be execute for a Build. From Camel K version 2, it would be more appropriate to think it as pipeline.

    Attributes
    ----------
    configuration : CamelApacheOrgV1BuildSpecConfiguration, default is Undefined, optional
        configuration
    maxRunningBuilds : int, default is Undefined, optional
        the maximum amount of parallel running builds started by this operator instance Deprecated: no longer in use in Camel K 2 - maintained for backward compatibility
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation). Deprecated: no longer in use in Camel K 2 - maintained for backward compatibility
    tasks : [CamelApacheOrgV1BuildSpecTasksItems0], default is Undefined, optional
        The sequence of tasks (pipeline) to be performed.
    timeout : str, default is Undefined, optional
        Timeout defines the Build maximum execution duration. The Build deadline is set to the Build start time plus the Timeout duration. If the Build deadline is exceeded, the Build context is canceled, and its phase set to BuildPhaseFailed.
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build. Deprecated: no longer in use in Camel K 2 - maintained for backward compatibility
    """


    configuration?: CamelApacheOrgV1BuildSpecConfiguration

    maxRunningBuilds?: int

    operatorNamespace?: str

    tasks?: [CamelApacheOrgV1BuildSpecTasksItems0]

    timeout?: str

    toolImage?: str


schema CamelApacheOrgV1BuildSpecConfiguration:
    """
    The configuration that should be used to perform the Build. Deprecated: no longer in use in Camel K 2 - maintained for backward compatibility

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0:
    """
    Task represents the abstract task. Only one of the task should be configured to represent the specific task chosen.

    Attributes
    ----------
    buildah : CamelApacheOrgV1BuildSpecTasksItems0Buildah, default is Undefined, optional
        buildah
    builder : CamelApacheOrgV1BuildSpecTasksItems0Builder, default is Undefined, optional
        builder
    custom : CamelApacheOrgV1BuildSpecTasksItems0Custom, default is Undefined, optional
        custom
    jib : CamelApacheOrgV1BuildSpecTasksItems0Jib, default is Undefined, optional
        jib
    kaniko : CamelApacheOrgV1BuildSpecTasksItems0Kaniko, default is Undefined, optional
        kaniko
    package : CamelApacheOrgV1BuildSpecTasksItems0Package, default is Undefined, optional
        package
    s2i : CamelApacheOrgV1BuildSpecTasksItems0S2i, default is Undefined, optional
        s2i
    spectrum : CamelApacheOrgV1BuildSpecTasksItems0Spectrum, default is Undefined, optional
        spectrum
    """


    buildah?: CamelApacheOrgV1BuildSpecTasksItems0Buildah

    builder?: CamelApacheOrgV1BuildSpecTasksItems0Builder

    custom?: CamelApacheOrgV1BuildSpecTasksItems0Custom

    jib?: CamelApacheOrgV1BuildSpecTasksItems0Jib

    kaniko?: CamelApacheOrgV1BuildSpecTasksItems0Kaniko

    package?: CamelApacheOrgV1BuildSpecTasksItems0Package

    s2i?: CamelApacheOrgV1BuildSpecTasksItems0S2i

    spectrum?: CamelApacheOrgV1BuildSpecTasksItems0Spectrum


schema CamelApacheOrgV1BuildSpecTasksItems0Buildah:
    """
    a BuildahTask, for Buildah strategy Deprecated: use spectrum, jib or s2i instead

    Attributes
    ----------
    baseImage : str, default is Undefined, optional
        base image layer
    configuration : CamelApacheOrgV1BuildSpecTasksItems0BuildahConfiguration, default is Undefined, optional
        configuration
    contextDir : str, default is Undefined, optional
        can be useful to share info with other tasks
    executorImage : str, default is Undefined, optional
        docker image to use
    image : str, default is Undefined, optional
        final image name
    name : str, default is Undefined, optional
        name of the task
    platform : str, default is Undefined, optional
        The platform of build image
    registry : CamelApacheOrgV1BuildSpecTasksItems0BuildahRegistry, default is Undefined, optional
        registry
    verbose : bool, default is Undefined, optional
        log more information
    """


    baseImage?: str

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0BuildahConfiguration

    contextDir?: str

    executorImage?: str

    image?: str

    name?: str

    platform?: str

    registry?: CamelApacheOrgV1BuildSpecTasksItems0BuildahRegistry

    verbose?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuildahConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuildahRegistry:
    """
    where to publish the final image

    Attributes
    ----------
    address : str, default is Undefined, optional
        the URI to access
    ca : str, default is Undefined, optional
        the configmap which stores the Certificate Authority
    insecure : bool, default is Undefined, optional
        if the container registry is insecure (ie, http only)
    organization : str, default is Undefined, optional
        the registry organization
    secret : str, default is Undefined, optional
        the secret where credentials are stored
    """


    address?: str

    ca?: str

    insecure?: bool

    organization?: str

    secret?: str


schema CamelApacheOrgV1BuildSpecTasksItems0Builder:
    """
    a BuilderTask, used to generate and build the project

    Attributes
    ----------
    baseImage : str, default is Undefined, optional
        the base image layer
    buildDir : str, default is Undefined, optional
        workspace directory to use
    configuration : CamelApacheOrgV1BuildSpecTasksItems0BuilderConfiguration, default is Undefined, optional
        configuration
    dependencies : [str], default is Undefined, optional
        the list of dependencies to use for this build
    maven : CamelApacheOrgV1BuildSpecTasksItems0BuilderMaven, default is Undefined, optional
        maven
    name : str, default is Undefined, optional
        name of the task
    runtime : CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntime, default is Undefined, optional
        runtime
    sources : [CamelApacheOrgV1BuildSpecTasksItems0BuilderSourcesItems0], default is Undefined, optional
        the sources to add at build time
    steps : [str], default is Undefined, optional
        the list of steps to execute (see pkg/builder/)
    """


    baseImage?: str

    buildDir?: str

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0BuilderConfiguration

    dependencies?: [str]

    maven?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMaven

    name?: str

    runtime?: CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntime

    sources?: [CamelApacheOrgV1BuildSpecTasksItems0BuilderSourcesItems0]

    steps?: [str]


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMaven:
    """
    the configuration required by Maven for the application build phase

    Attributes
    ----------
    caSecrets : [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenCaSecretsItems0], default is Undefined, optional
        The Secrets name and key, containing the CA certificate(s) used to connect to remote Maven repositories. It can contain X.509 certificates, and PKCS#7 formatted certificate chains. A JKS formatted keystore is automatically created to store the CA certificate(s), and configured to be used as a trusted certificate(s) by the Maven commands. Note that the root CA certificates are also imported into the created keystore.
    cliOptions : [str], default is Undefined, optional
        The CLI options that are appended to the list of arguments for Maven commands, e.g., `-V,--no-transfer-progress,-Dstyle.color=never`. See https://maven.apache.org/ref/3.8.4/maven-embedder/cli.html.
    extension : [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenExtensionItems0], default is Undefined, optional
        The Maven build extensions. See https://maven.apache.org/guides/mini/guide-using-extensions.html.
    localRepository : str, default is Undefined, optional
        The path of the local Maven repository.
    profiles : [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0], default is Undefined, optional
        A reference to the ConfigMap or Secret key that contains the Maven profile.
    properties : {str:str}, default is Undefined, optional
        The Maven properties.
    repositories : [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0], default is Undefined, optional
        additional repositories
    servers : [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenServersItems0], default is Undefined, optional
        Servers (auth)
    settings : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettings, default is Undefined, optional
        settings
    settingsSecurity : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecurity, default is Undefined, optional
        settings security
    """


    caSecrets?: [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenCaSecretsItems0]

    cliOptions?: [str]

    extension?: [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenExtensionItems0]

    localRepository?: str

    profiles?: [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0]

    properties?: {str:str}

    repositories?: [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0]

    servers?: [CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenServersItems0]

    settings?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettings

    settingsSecurity?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecurity


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenCaSecretsItems0:
    """
    SecretKeySelector selects a key of a Secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenExtensionItems0:
    """
    MavenArtifact defines a GAV (Group:Artifact:Version) Maven artifact.

    Attributes
    ----------
    artifactId : str, default is Undefined, required
        Maven Artifact
    groupId : str, default is Undefined, required
        Maven Group
    version : str, default is Undefined, optional
        Maven Version
    """


    artifactId: str

    groupId: str

    version?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0:
    """
    ValueSource --.

    Attributes
    ----------
    configMapKeyRef : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0ConfigMapKeyRef, default is Undefined, optional
        config map key ref
    secretKeyRef : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0SecretKeyRef, default is Undefined, optional
        secret key ref
    """


    configMapKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0ConfigMapKeyRef

    secretKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0SecretKeyRef


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0ConfigMapKeyRef:
    """
    Selects a key of a ConfigMap.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenProfilesItems0SecretKeyRef:
    """
    Selects a key of a secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0:
    """
    Repository defines a Maven repository.

    Attributes
    ----------
    id : str, default is Undefined, required
        identifies the repository
    name : str, default is Undefined, optional
        name of the repository
    releases : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0Releases, default is Undefined, optional
        releases
    snapshots : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0Snapshots, default is Undefined, optional
        snapshots
    url : str, default is Undefined, required
        location of the repository
    """


    id: str

    name?: str

    releases?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0Releases

    snapshots?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0Snapshots

    url: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0Releases:
    """
    can use stable releases

    Attributes
    ----------
    checksumPolicy : str, default is Undefined, optional
        When Maven deploys files to the repository, it also deploys corresponding checksum files. Your options are to `ignore`, `fail`, or `warn` on missing or incorrect checksums.
    enabled : bool, default is Undefined, required
        is the policy activated or not
    updatePolicy : str, default is Undefined, optional
        This element specifies how often updates should attempt to occur. Maven will compare the local POM's timestamp (stored in a repository's maven-metadata file) to the remote. The choices are: `always`, `daily` (default), `interval:X` (where X is an integer in minutes) or `never`
    """


    checksumPolicy?: str

    enabled: bool

    updatePolicy?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenRepositoriesItems0Snapshots:
    """
    can use snapshot

    Attributes
    ----------
    checksumPolicy : str, default is Undefined, optional
        When Maven deploys files to the repository, it also deploys corresponding checksum files. Your options are to `ignore`, `fail`, or `warn` on missing or incorrect checksums.
    enabled : bool, default is Undefined, required
        is the policy activated or not
    updatePolicy : str, default is Undefined, optional
        This element specifies how often updates should attempt to occur. Maven will compare the local POM's timestamp (stored in a repository's maven-metadata file) to the remote. The choices are: `always`, `daily` (default), `interval:X` (where X is an integer in minutes) or `never`
    """


    checksumPolicy?: str

    enabled: bool

    updatePolicy?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenServersItems0:
    """
    Server see link:https://maven.apache.org/settings.html[Maven settings].

    Attributes
    ----------
    configuration : {str:str}, default is Undefined, optional
        Properties -- .
    id : str, default is Undefined, optional
        id
    password : str, default is Undefined, optional
        password
    username : str, default is Undefined, optional
        username
    """


    configuration?: {str:str}

    id?: str

    password?: str

    username?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettings:
    """
    A reference to the ConfigMap or Secret key that contains the Maven settings.

    Attributes
    ----------
    configMapKeyRef : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsConfigMapKeyRef, default is Undefined, optional
        config map key ref
    secretKeyRef : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecretKeyRef, default is Undefined, optional
        secret key ref
    """


    configMapKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsConfigMapKeyRef

    secretKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecretKeyRef


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsConfigMapKeyRef:
    """
    Selects a key of a ConfigMap.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecretKeyRef:
    """
    Selects a key of a secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecurity:
    """
    A reference to the ConfigMap or Secret key that contains the security of the Maven settings.

    Attributes
    ----------
    configMapKeyRef : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecurityConfigMapKeyRef, default is Undefined, optional
        config map key ref
    secretKeyRef : CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecuritySecretKeyRef, default is Undefined, optional
        secret key ref
    """


    configMapKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecurityConfigMapKeyRef

    secretKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecuritySecretKeyRef


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecurityConfigMapKeyRef:
    """
    Selects a key of a ConfigMap.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderMavenSettingsSecuritySecretKeyRef:
    """
    Selects a key of a secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntime:
    """
    the configuration required for the runtime application

    Attributes
    ----------
    applicationClass : str, default is Undefined, required
        application entry point (main) to be executed
    capabilities : {str:CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeCapabilitiesAnon}, default is Undefined, optional
        features offered by this runtime
    dependencies : [CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeDependenciesItems0], default is Undefined, required
        list of dependencies needed to run the application
    metadata : {str:str}, default is Undefined, optional
        set of metadata
    provider : str, default is Undefined, required
        Camel main application provider, ie, Camel Quarkus
    version : str, default is Undefined, required
        Camel K Runtime version
    """


    applicationClass: str

    capabilities?: {str:CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeCapabilitiesAnon}

    dependencies: [CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeDependenciesItems0]

    metadata?: {str:str}

    provider: str

    version: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeCapabilitiesAnon:
    """
    Capability is a particular feature which requires a well known set of dependencies which are specified in the runtime catalog.

    Attributes
    ----------
    dependencies : [CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeCapabilitiesAnonDependenciesItems0], default is Undefined, required
        dependencies
    """


    dependencies: [CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeCapabilitiesAnonDependenciesItems0]


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeCapabilitiesAnonDependenciesItems0:
    """
    MavenArtifact defines a GAV (Group:Artifact:Version) Maven artifact.

    Attributes
    ----------
    artifactId : str, default is Undefined, required
        Maven Artifact
    groupId : str, default is Undefined, required
        Maven Group
    version : str, default is Undefined, optional
        Maven Version
    """


    artifactId: str

    groupId: str

    version?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderRuntimeDependenciesItems0:
    """
    MavenArtifact defines a GAV (Group:Artifact:Version) Maven artifact.

    Attributes
    ----------
    artifactId : str, default is Undefined, required
        Maven Artifact
    groupId : str, default is Undefined, required
        Maven Group
    version : str, default is Undefined, optional
        Maven Version
    """


    artifactId: str

    groupId: str

    version?: str


schema CamelApacheOrgV1BuildSpecTasksItems0BuilderSourcesItems0:
    """
    SourceSpec defines the configuration for one or more routes to be executed in a certain Camel DSL language.

    Attributes
    ----------
    compression : bool, default is Undefined, optional
        if the content is compressed (base64 encrypted)
    content : str, default is Undefined, optional
        the source code (plain text)
    contentKey : str, default is Undefined, optional
        the confimap key holding the source content
    contentRef : str, default is Undefined, optional
        the confimap reference holding the source content
    contentType : str, default is Undefined, optional
        the content type (tipically text or binary)
    from_kamelet : bool, default is Undefined, optional
        True if the spec is generated from a Kamelet
    interceptors : [str], default is Undefined, optional
        Interceptors are optional identifiers the org.apache.camel.k.RoutesLoader uses to pre/post process sources
    language : str, default is Undefined, optional
        specify which is the language (Camel DSL) used to interpret this source code
    loader : str, default is Undefined, optional
        Loader is an optional id of the org.apache.camel.k.RoutesLoader that will interpret this source at runtime
    name : str, default is Undefined, optional
        the name of the specification
    path : str, default is Undefined, optional
        the path where the file is stored
    property_names : [str], default is Undefined, optional
        List of property names defined in the source (e.g. if type is "template")
    rawContent : str, default is Undefined, optional
        the source code (binary)
    $type : str, default is Undefined, optional
        Type defines the kind of source described by this object
    """


    compression?: bool

    content?: str

    contentKey?: str

    contentRef?: str

    contentType?: str

    from_kamelet?: bool

    interceptors?: [str]

    language?: str

    loader?: str

    name?: str

    path?: str

    property_names?: [str]

    rawContent?: str

    $type?: str


schema CamelApacheOrgV1BuildSpecTasksItems0Custom:
    """
    User customizable task execution. These are executed after the build and before the package task.

    Attributes
    ----------
    command : str, default is Undefined, optional
        the command to execute Deprecated: use ContainerCommands
    commands : [str], default is Undefined, optional
        the command to execute
    configuration : CamelApacheOrgV1BuildSpecTasksItems0CustomConfiguration, default is Undefined, optional
        configuration
    image : str, default is Undefined, optional
        the container image to use
    name : str, default is Undefined, optional
        name of the task
    """


    command?: str

    commands?: [str]

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0CustomConfiguration

    image?: str

    name?: str


schema CamelApacheOrgV1BuildSpecTasksItems0CustomConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0Jib:
    """
    a JibTask, for Jib strategy

    Attributes
    ----------
    baseImage : str, default is Undefined, optional
        base image layer
    configuration : CamelApacheOrgV1BuildSpecTasksItems0JibConfiguration, default is Undefined, optional
        configuration
    contextDir : str, default is Undefined, optional
        can be useful to share info with other tasks
    image : str, default is Undefined, optional
        final image name
    name : str, default is Undefined, optional
        name of the task
    registry : CamelApacheOrgV1BuildSpecTasksItems0JibRegistry, default is Undefined, optional
        registry
    """


    baseImage?: str

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0JibConfiguration

    contextDir?: str

    image?: str

    name?: str

    registry?: CamelApacheOrgV1BuildSpecTasksItems0JibRegistry


schema CamelApacheOrgV1BuildSpecTasksItems0JibConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0JibRegistry:
    """
    where to publish the final image

    Attributes
    ----------
    address : str, default is Undefined, optional
        the URI to access
    ca : str, default is Undefined, optional
        the configmap which stores the Certificate Authority
    insecure : bool, default is Undefined, optional
        if the container registry is insecure (ie, http only)
    organization : str, default is Undefined, optional
        the registry organization
    secret : str, default is Undefined, optional
        the secret where credentials are stored
    """


    address?: str

    ca?: str

    insecure?: bool

    organization?: str

    secret?: str


schema CamelApacheOrgV1BuildSpecTasksItems0Kaniko:
    """
    a KanikoTask, for Kaniko strategy Deprecated: use spectrum, jib or s2i instead

    Attributes
    ----------
    baseImage : str, default is Undefined, optional
        base image layer
    cache : CamelApacheOrgV1BuildSpecTasksItems0KanikoCache, default is Undefined, optional
        cache
    configuration : CamelApacheOrgV1BuildSpecTasksItems0KanikoConfiguration, default is Undefined, optional
        configuration
    contextDir : str, default is Undefined, optional
        can be useful to share info with other tasks
    executorImage : str, default is Undefined, optional
        docker image to use
    image : str, default is Undefined, optional
        final image name
    name : str, default is Undefined, optional
        name of the task
    registry : CamelApacheOrgV1BuildSpecTasksItems0KanikoRegistry, default is Undefined, optional
        registry
    verbose : bool, default is Undefined, optional
        log more information
    """


    baseImage?: str

    cache?: CamelApacheOrgV1BuildSpecTasksItems0KanikoCache

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0KanikoConfiguration

    contextDir?: str

    executorImage?: str

    image?: str

    name?: str

    registry?: CamelApacheOrgV1BuildSpecTasksItems0KanikoRegistry

    verbose?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0KanikoCache:
    """
    use a cache

    Attributes
    ----------
    enabled : bool, default is Undefined, optional
        true if a cache is enabled
    persistentVolumeClaim : str, default is Undefined, optional
        the PVC used to store the cache
    """


    enabled?: bool

    persistentVolumeClaim?: str


schema CamelApacheOrgV1BuildSpecTasksItems0KanikoConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0KanikoRegistry:
    """
    where to publish the final image

    Attributes
    ----------
    address : str, default is Undefined, optional
        the URI to access
    ca : str, default is Undefined, optional
        the configmap which stores the Certificate Authority
    insecure : bool, default is Undefined, optional
        if the container registry is insecure (ie, http only)
    organization : str, default is Undefined, optional
        the registry organization
    secret : str, default is Undefined, optional
        the secret where credentials are stored
    """


    address?: str

    ca?: str

    insecure?: bool

    organization?: str

    secret?: str


schema CamelApacheOrgV1BuildSpecTasksItems0Package:
    """
    Application pre publishing a PackageTask, used to package the project

    Attributes
    ----------
    baseImage : str, default is Undefined, optional
        the base image layer
    buildDir : str, default is Undefined, optional
        workspace directory to use
    configuration : CamelApacheOrgV1BuildSpecTasksItems0PackageConfiguration, default is Undefined, optional
        configuration
    dependencies : [str], default is Undefined, optional
        the list of dependencies to use for this build
    maven : CamelApacheOrgV1BuildSpecTasksItems0PackageMaven, default is Undefined, optional
        maven
    name : str, default is Undefined, optional
        name of the task
    runtime : CamelApacheOrgV1BuildSpecTasksItems0PackageRuntime, default is Undefined, optional
        runtime
    sources : [CamelApacheOrgV1BuildSpecTasksItems0PackageSourcesItems0], default is Undefined, optional
        the sources to add at build time
    steps : [str], default is Undefined, optional
        the list of steps to execute (see pkg/builder/)
    """


    baseImage?: str

    buildDir?: str

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0PackageConfiguration

    dependencies?: [str]

    maven?: CamelApacheOrgV1BuildSpecTasksItems0PackageMaven

    name?: str

    runtime?: CamelApacheOrgV1BuildSpecTasksItems0PackageRuntime

    sources?: [CamelApacheOrgV1BuildSpecTasksItems0PackageSourcesItems0]

    steps?: [str]


schema CamelApacheOrgV1BuildSpecTasksItems0PackageConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMaven:
    """
    the configuration required by Maven for the application build phase

    Attributes
    ----------
    caSecrets : [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenCaSecretsItems0], default is Undefined, optional
        The Secrets name and key, containing the CA certificate(s) used to connect to remote Maven repositories. It can contain X.509 certificates, and PKCS#7 formatted certificate chains. A JKS formatted keystore is automatically created to store the CA certificate(s), and configured to be used as a trusted certificate(s) by the Maven commands. Note that the root CA certificates are also imported into the created keystore.
    cliOptions : [str], default is Undefined, optional
        The CLI options that are appended to the list of arguments for Maven commands, e.g., `-V,--no-transfer-progress,-Dstyle.color=never`. See https://maven.apache.org/ref/3.8.4/maven-embedder/cli.html.
    extension : [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenExtensionItems0], default is Undefined, optional
        The Maven build extensions. See https://maven.apache.org/guides/mini/guide-using-extensions.html.
    localRepository : str, default is Undefined, optional
        The path of the local Maven repository.
    profiles : [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0], default is Undefined, optional
        A reference to the ConfigMap or Secret key that contains the Maven profile.
    properties : {str:str}, default is Undefined, optional
        The Maven properties.
    repositories : [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0], default is Undefined, optional
        additional repositories
    servers : [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenServersItems0], default is Undefined, optional
        Servers (auth)
    settings : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettings, default is Undefined, optional
        settings
    settingsSecurity : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecurity, default is Undefined, optional
        settings security
    """


    caSecrets?: [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenCaSecretsItems0]

    cliOptions?: [str]

    extension?: [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenExtensionItems0]

    localRepository?: str

    profiles?: [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0]

    properties?: {str:str}

    repositories?: [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0]

    servers?: [CamelApacheOrgV1BuildSpecTasksItems0PackageMavenServersItems0]

    settings?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettings

    settingsSecurity?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecurity


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenCaSecretsItems0:
    """
    SecretKeySelector selects a key of a Secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenExtensionItems0:
    """
    MavenArtifact defines a GAV (Group:Artifact:Version) Maven artifact.

    Attributes
    ----------
    artifactId : str, default is Undefined, required
        Maven Artifact
    groupId : str, default is Undefined, required
        Maven Group
    version : str, default is Undefined, optional
        Maven Version
    """


    artifactId: str

    groupId: str

    version?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0:
    """
    ValueSource --.

    Attributes
    ----------
    configMapKeyRef : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0ConfigMapKeyRef, default is Undefined, optional
        config map key ref
    secretKeyRef : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0SecretKeyRef, default is Undefined, optional
        secret key ref
    """


    configMapKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0ConfigMapKeyRef

    secretKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0SecretKeyRef


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0ConfigMapKeyRef:
    """
    Selects a key of a ConfigMap.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenProfilesItems0SecretKeyRef:
    """
    Selects a key of a secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0:
    """
    Repository defines a Maven repository.

    Attributes
    ----------
    id : str, default is Undefined, required
        identifies the repository
    name : str, default is Undefined, optional
        name of the repository
    releases : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0Releases, default is Undefined, optional
        releases
    snapshots : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0Snapshots, default is Undefined, optional
        snapshots
    url : str, default is Undefined, required
        location of the repository
    """


    id: str

    name?: str

    releases?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0Releases

    snapshots?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0Snapshots

    url: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0Releases:
    """
    can use stable releases

    Attributes
    ----------
    checksumPolicy : str, default is Undefined, optional
        When Maven deploys files to the repository, it also deploys corresponding checksum files. Your options are to `ignore`, `fail`, or `warn` on missing or incorrect checksums.
    enabled : bool, default is Undefined, required
        is the policy activated or not
    updatePolicy : str, default is Undefined, optional
        This element specifies how often updates should attempt to occur. Maven will compare the local POM's timestamp (stored in a repository's maven-metadata file) to the remote. The choices are: `always`, `daily` (default), `interval:X` (where X is an integer in minutes) or `never`
    """


    checksumPolicy?: str

    enabled: bool

    updatePolicy?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenRepositoriesItems0Snapshots:
    """
    can use snapshot

    Attributes
    ----------
    checksumPolicy : str, default is Undefined, optional
        When Maven deploys files to the repository, it also deploys corresponding checksum files. Your options are to `ignore`, `fail`, or `warn` on missing or incorrect checksums.
    enabled : bool, default is Undefined, required
        is the policy activated or not
    updatePolicy : str, default is Undefined, optional
        This element specifies how often updates should attempt to occur. Maven will compare the local POM's timestamp (stored in a repository's maven-metadata file) to the remote. The choices are: `always`, `daily` (default), `interval:X` (where X is an integer in minutes) or `never`
    """


    checksumPolicy?: str

    enabled: bool

    updatePolicy?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenServersItems0:
    """
    Server see link:https://maven.apache.org/settings.html[Maven settings].

    Attributes
    ----------
    configuration : {str:str}, default is Undefined, optional
        Properties -- .
    id : str, default is Undefined, optional
        id
    password : str, default is Undefined, optional
        password
    username : str, default is Undefined, optional
        username
    """


    configuration?: {str:str}

    id?: str

    password?: str

    username?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettings:
    """
    A reference to the ConfigMap or Secret key that contains the Maven settings.

    Attributes
    ----------
    configMapKeyRef : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsConfigMapKeyRef, default is Undefined, optional
        config map key ref
    secretKeyRef : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecretKeyRef, default is Undefined, optional
        secret key ref
    """


    configMapKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsConfigMapKeyRef

    secretKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecretKeyRef


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsConfigMapKeyRef:
    """
    Selects a key of a ConfigMap.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecretKeyRef:
    """
    Selects a key of a secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecurity:
    """
    A reference to the ConfigMap or Secret key that contains the security of the Maven settings.

    Attributes
    ----------
    configMapKeyRef : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecurityConfigMapKeyRef, default is Undefined, optional
        config map key ref
    secretKeyRef : CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecuritySecretKeyRef, default is Undefined, optional
        secret key ref
    """


    configMapKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecurityConfigMapKeyRef

    secretKeyRef?: CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecuritySecretKeyRef


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecurityConfigMapKeyRef:
    """
    Selects a key of a ConfigMap.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key to select.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the ConfigMap or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageMavenSettingsSecuritySecretKeyRef:
    """
    Selects a key of a secret.

    Attributes
    ----------
    key : str, default is Undefined, required
        The key of the secret to select from.  Must be a valid secret key.
    name : str, default is Undefined, optional
        Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names TODO: Add other useful fields. apiVersion, kind, uid?
    optional : bool, default is Undefined, optional
        Specify whether the Secret or its key must be defined
    """


    key: str

    name?: str

    optional?: bool


schema CamelApacheOrgV1BuildSpecTasksItems0PackageRuntime:
    """
    the configuration required for the runtime application

    Attributes
    ----------
    applicationClass : str, default is Undefined, required
        application entry point (main) to be executed
    capabilities : {str:CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeCapabilitiesAnon}, default is Undefined, optional
        features offered by this runtime
    dependencies : [CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeDependenciesItems0], default is Undefined, required
        list of dependencies needed to run the application
    metadata : {str:str}, default is Undefined, optional
        set of metadata
    provider : str, default is Undefined, required
        Camel main application provider, ie, Camel Quarkus
    version : str, default is Undefined, required
        Camel K Runtime version
    """


    applicationClass: str

    capabilities?: {str:CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeCapabilitiesAnon}

    dependencies: [CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeDependenciesItems0]

    metadata?: {str:str}

    provider: str

    version: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeCapabilitiesAnon:
    """
    Capability is a particular feature which requires a well known set of dependencies which are specified in the runtime catalog.

    Attributes
    ----------
    dependencies : [CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeCapabilitiesAnonDependenciesItems0], default is Undefined, required
        dependencies
    """


    dependencies: [CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeCapabilitiesAnonDependenciesItems0]


schema CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeCapabilitiesAnonDependenciesItems0:
    """
    MavenArtifact defines a GAV (Group:Artifact:Version) Maven artifact.

    Attributes
    ----------
    artifactId : str, default is Undefined, required
        Maven Artifact
    groupId : str, default is Undefined, required
        Maven Group
    version : str, default is Undefined, optional
        Maven Version
    """


    artifactId: str

    groupId: str

    version?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageRuntimeDependenciesItems0:
    """
    MavenArtifact defines a GAV (Group:Artifact:Version) Maven artifact.

    Attributes
    ----------
    artifactId : str, default is Undefined, required
        Maven Artifact
    groupId : str, default is Undefined, required
        Maven Group
    version : str, default is Undefined, optional
        Maven Version
    """


    artifactId: str

    groupId: str

    version?: str


schema CamelApacheOrgV1BuildSpecTasksItems0PackageSourcesItems0:
    """
    SourceSpec defines the configuration for one or more routes to be executed in a certain Camel DSL language.

    Attributes
    ----------
    compression : bool, default is Undefined, optional
        if the content is compressed (base64 encrypted)
    content : str, default is Undefined, optional
        the source code (plain text)
    contentKey : str, default is Undefined, optional
        the confimap key holding the source content
    contentRef : str, default is Undefined, optional
        the confimap reference holding the source content
    contentType : str, default is Undefined, optional
        the content type (tipically text or binary)
    from_kamelet : bool, default is Undefined, optional
        True if the spec is generated from a Kamelet
    interceptors : [str], default is Undefined, optional
        Interceptors are optional identifiers the org.apache.camel.k.RoutesLoader uses to pre/post process sources
    language : str, default is Undefined, optional
        specify which is the language (Camel DSL) used to interpret this source code
    loader : str, default is Undefined, optional
        Loader is an optional id of the org.apache.camel.k.RoutesLoader that will interpret this source at runtime
    name : str, default is Undefined, optional
        the name of the specification
    path : str, default is Undefined, optional
        the path where the file is stored
    property_names : [str], default is Undefined, optional
        List of property names defined in the source (e.g. if type is "template")
    rawContent : str, default is Undefined, optional
        the source code (binary)
    $type : str, default is Undefined, optional
        Type defines the kind of source described by this object
    """


    compression?: bool

    content?: str

    contentKey?: str

    contentRef?: str

    contentType?: str

    from_kamelet?: bool

    interceptors?: [str]

    language?: str

    loader?: str

    name?: str

    path?: str

    property_names?: [str]

    rawContent?: str

    $type?: str


schema CamelApacheOrgV1BuildSpecTasksItems0S2i:
    """
    a S2iTask, for S2I strategy

    Attributes
    ----------
    configuration : CamelApacheOrgV1BuildSpecTasksItems0S2iConfiguration, default is Undefined, optional
        configuration
    contextDir : str, default is Undefined, optional
        can be useful to share info with other tasks
    name : str, default is Undefined, optional
        name of the task
    tag : str, default is Undefined, optional
        used by the ImageStream
    """


    configuration?: CamelApacheOrgV1BuildSpecTasksItems0S2iConfiguration

    contextDir?: str

    name?: str

    tag?: str


schema CamelApacheOrgV1BuildSpecTasksItems0S2iConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0Spectrum:
    """
    a SpectrumTask, for Spectrum strategy

    Attributes
    ----------
    baseImage : str, default is Undefined, optional
        base image layer
    configuration : CamelApacheOrgV1BuildSpecTasksItems0SpectrumConfiguration, default is Undefined, optional
        configuration
    contextDir : str, default is Undefined, optional
        can be useful to share info with other tasks
    image : str, default is Undefined, optional
        final image name
    name : str, default is Undefined, optional
        name of the task
    registry : CamelApacheOrgV1BuildSpecTasksItems0SpectrumRegistry, default is Undefined, optional
        registry
    """


    baseImage?: str

    configuration?: CamelApacheOrgV1BuildSpecTasksItems0SpectrumConfiguration

    contextDir?: str

    image?: str

    name?: str

    registry?: CamelApacheOrgV1BuildSpecTasksItems0SpectrumRegistry


schema CamelApacheOrgV1BuildSpecTasksItems0SpectrumConfiguration:
    """
    The configuration that should be used to perform the Build.

    Attributes
    ----------
    limitCPU : str, default is Undefined, optional
        The maximum amount of CPU required. Only used for `pod` strategy
    limitMemory : str, default is Undefined, optional
        The maximum amount of memory required. Only used for `pod` strategy
    operatorNamespace : str, default is Undefined, optional
        The namespace where to run the builder Pod (must be the same of the operator in charge of this Build reconciliation).
    orderStrategy : str, default is Undefined, optional
        the build order strategy to adopt
    requestCPU : str, default is Undefined, optional
        The minimum amount of CPU required. Only used for `pod` strategy
    requestMemory : str, default is Undefined, optional
        The minimum amount of memory required. Only used for `pod` strategy
    strategy : str, default is Undefined, optional
        the strategy to adopt
    toolImage : str, default is Undefined, optional
        The container image to be used to run the build.
    """


    limitCPU?: str

    limitMemory?: str

    operatorNamespace?: str

    orderStrategy?: "dependencies" | "fifo" | "sequential"

    requestCPU?: str

    requestMemory?: str

    strategy?: "routine" | "pod"

    toolImage?: str


schema CamelApacheOrgV1BuildSpecTasksItems0SpectrumRegistry:
    """
    where to publish the final image

    Attributes
    ----------
    address : str, default is Undefined, optional
        the URI to access
    ca : str, default is Undefined, optional
        the configmap which stores the Certificate Authority
    insecure : bool, default is Undefined, optional
        if the container registry is insecure (ie, http only)
    organization : str, default is Undefined, optional
        the registry organization
    secret : str, default is Undefined, optional
        the secret where credentials are stored
    """


    address?: str

    ca?: str

    insecure?: bool

    organization?: str

    secret?: str


schema CamelApacheOrgV1BuildStatus:
    """
    BuildStatus defines the observed state of Build.

    Attributes
    ----------
    artifacts : [CamelApacheOrgV1BuildStatusArtifactsItems0], default is Undefined, optional
        a list of artifacts contained in the build
    baseImage : str, default is Undefined, optional
        the base image used for this build
    conditions : [CamelApacheOrgV1BuildStatusConditionsItems0], default is Undefined, optional
        a list of conditions occurred during the build
    digest : str, default is Undefined, optional
        the digest from image
    duration : str, default is Undefined, optional
        how long it took for the build Change to Duration / ISO 8601 when CRD uses OpenAPI spec v3 https://github.com/OAI/OpenAPI-Specification/issues/845
    error : str, default is Undefined, optional
        the error description (if any)
    failure : CamelApacheOrgV1BuildStatusFailure, default is Undefined, optional
        failure
    image : str, default is Undefined, optional
        the image name built
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the most recent generation observed for this Build.
    phase : str, default is Undefined, optional
        describes the phase
    startedAt : str, default is Undefined, optional
        the time when it started
    """


    artifacts?: [CamelApacheOrgV1BuildStatusArtifactsItems0]

    baseImage?: str

    conditions?: [CamelApacheOrgV1BuildStatusConditionsItems0]

    digest?: str

    duration?: str

    error?: str

    failure?: CamelApacheOrgV1BuildStatusFailure

    image?: str

    observedGeneration?: int

    phase?: str

    startedAt?: str


schema CamelApacheOrgV1BuildStatusArtifactsItems0:
    """
    Artifact represents a materialized artifact (a jar dependency or in general a file used by the build).

    Attributes
    ----------
    checksum : str, default is Undefined, optional
        a checksum (SHA1) of the content
    id : str, default is Undefined, required
        the identification (GAV for maven dependencies or file name for other file types)
    location : str, default is Undefined, optional
        where it is located in the builder `Pod`
    target : str, default is Undefined, optional
        the expected location in the runtime
    """


    checksum?: str

    id: str

    location?: str

    target?: str


schema CamelApacheOrgV1BuildStatusConditionsItems0:
    """
    BuildCondition describes the state of a resource at a certain point.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, optional
        Last time the condition transitioned from one status to another.
    lastUpdateTime : str, default is Undefined, optional
        The last time this condition was updated.
    message : str, default is Undefined, optional
        A human-readable message indicating details about the transition.
    reason : str, default is Undefined, optional
        The reason for the condition's last transition.
    status : str, default is Undefined, required
        Status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        Type of integration condition.
    """


    lastTransitionTime?: str

    lastUpdateTime?: str

    message?: str

    reason?: str

    status: str

    $type: str


schema CamelApacheOrgV1BuildStatusFailure:
    """
    the reason of the failure (if any)

    Attributes
    ----------
    reason : str, default is Undefined, required
        a short text specifying the reason
    recovery : CamelApacheOrgV1BuildStatusFailureRecovery, default is Undefined, required
        recovery
    time : str, default is Undefined, required
        the time when the failure has happened
    """


    reason: str

    recovery: CamelApacheOrgV1BuildStatusFailureRecovery

    time: str


schema CamelApacheOrgV1BuildStatusFailureRecovery:
    """
    the recovery attempted for this failure

    Attributes
    ----------
    attempt : int, default is Undefined, required
        attempt number
    attemptMax : int, default is Undefined, required
        maximum number of attempts
    attemptTime : str, default is Undefined, optional
        time of the attempt execution
    """


    attempt: int

    attemptMax: int

    attemptTime?: str


