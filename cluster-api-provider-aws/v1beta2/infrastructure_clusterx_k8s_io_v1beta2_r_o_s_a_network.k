"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema ROSANetwork:
    r"""
    ROSANetwork is the schema for the rosanetworks API

    Attributes
    ----------
    apiVersion : str, default is "infrastructure.cluster.x-k8s.io/v1beta2", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ROSANetwork", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : InfrastructureClusterxK8sIoV1beta2ROSANetworkSpec, default is Undefined, optional
        spec
    status : InfrastructureClusterxK8sIoV1beta2ROSANetworkStatus, default is Undefined, optional
        status
    """


    apiVersion: "infrastructure.cluster.x-k8s.io/v1beta2" = "infrastructure.cluster.x-k8s.io/v1beta2"

    kind: "ROSANetwork" = "ROSANetwork"

    metadata?: v1.ObjectMeta

    spec?: InfrastructureClusterxK8sIoV1beta2ROSANetworkSpec

    status?: InfrastructureClusterxK8sIoV1beta2ROSANetworkStatus


schema InfrastructureClusterxK8sIoV1beta2ROSANetworkSpec:
    r"""
    ROSANetworkSpec defines the desired state of ROSANetwork

    Attributes
    ----------
    availabilityZoneCount : int, default is Undefined, optional
        The number of availability zones to be used for creation of the network infrastructure.
        You can specify anything between one and four, depending on the chosen AWS region.
        Either AvailabilityZoneCount OR AvailabilityZones must be set.
    availabilityZones : [str], default is Undefined, optional
        The list of availability zones to be used for creation of the network infrastructure.
        You can specify anything between one and four valid availability zones from a given region.
        Either AvailabilityZones OR AvailabilityZoneCount must be set.
    cidrBlock : str, default is Undefined, required
        CIDR block to be used for the VPC
    identityRef : InfrastructureClusterxK8sIoV1beta2ROSANetworkSpecIdentityRef, default is Undefined, optional
        identity ref
    region : str, default is Undefined, required
        The AWS region in which the components of ROSA network infrastruture are to be crated
    stackName : str, default is Undefined, required
        The name of the cloudformation stack under which the network infrastructure would be created
    stackTags : {str:str}, default is Undefined, optional
        StackTags is an optional set of tags to add to the created cloudformation stack.
        The stack tags will then be automatically applied to the supported AWS resources (VPC, subnets, ...).
    """


    availabilityZoneCount?: int

    availabilityZones?: [str]

    cidrBlock: str

    identityRef?: InfrastructureClusterxK8sIoV1beta2ROSANetworkSpecIdentityRef

    region: str

    stackName: str

    stackTags?: {str:str}


    check:
        availabilityZoneCount >= 1 if availabilityZoneCount not in [None, Undefined]


schema InfrastructureClusterxK8sIoV1beta2ROSANetworkSpecIdentityRef:
    r"""
    IdentityRef is a reference to an identity to be used when reconciling rosa network.
    If no identity is specified, the default identity for this controller will be used.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the identity.
    name : str, default is Undefined, required
        Name of the identity.
    """


    kind: "AWSClusterControllerIdentity" | "AWSClusterRoleIdentity" | "AWSClusterStaticIdentity"

    name: str


    check:
        len(name) >= 1


schema InfrastructureClusterxK8sIoV1beta2ROSANetworkStatus:
    r"""
    ROSANetworkStatus defines the observed state of ROSANetwork

    Attributes
    ----------
    conditions : [InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusConditionsItems0], default is Undefined, optional
        Conditions specifies the conditions for ROSANetwork
    resources : [InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusResourcesItems0], default is Undefined, optional
        Resources created in the cloudformation stack
    subnets : [InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusSubnetsItems0], default is Undefined, optional
        Array of created private, public subnets and availability zones, grouped by availability zones
    """


    conditions?: [InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusConditionsItems0]

    resources?: [InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusResourcesItems0]

    subnets?: [InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusSubnetsItems0]


schema InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusConditionsItems0:
    r"""
    Condition defines an observation of a Cluster API resource operational state.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        lastTransitionTime is the last time the condition transitioned from one status to another.
        This should be when the underlying condition changed. If that is not known, then using the time when
        the API field changed is acceptable.
    message : str, default is Undefined, optional
        message is a human readable message indicating details about the transition.
        This field may be empty.
    reason : str, default is Undefined, optional
        reason is the reason for the condition's last transition in CamelCase.
        The specific API may choose whether or not this field is considered a guaranteed API.
        This field may be empty.
    severity : str, default is Undefined, optional
        severity provides an explicit classification of Reason code, so the users or machines can immediately
        understand the current situation and act accordingly.
        The Severity field MUST be set only when Status=False.
    status : str, default is Undefined, required
        status of the condition, one of True, False, Unknown.
    $type : str, default is Undefined, required
        type of condition in CamelCase or in foo.example.com/CamelCase.
        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
        can be useful (see .node.status.conditions), the ability to deconflict is important.
    """


    lastTransitionTime: str

    message?: str

    reason?: str

    severity?: str

    status: str

    $type: str


    check:
        len(message) <= 10240 if message
        len(message) >= 1 if message
        len(reason) <= 256 if reason
        len(reason) >= 1 if reason
        len(severity) <= 32 if severity
        len($type) <= 256
        len($type) >= 1


schema InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusResourcesItems0:
    r"""
    CFResource groups information pertaining to a resource created as a part of a cloudformation stack

    Attributes
    ----------
    logicalId : str, default is Undefined, required
        LogicalResourceID of the created resource.
    physicalId : str, default is Undefined, required
        PhysicalResourceID of the created resource.
    reason : str, default is Undefined, required
        Message pertaining to the status of the resource
    resource : str, default is Undefined, required
        Type of the created resource: AWS::EC2::VPC, AWS::EC2::Subnet, ...
    status : str, default is Undefined, required
        Status of the resource: CREATE_IN_PROGRESS, CREATE_COMPLETE, ...
    """


    logicalId: str

    physicalId: str

    reason: str

    resource: str

    status: str


schema InfrastructureClusterxK8sIoV1beta2ROSANetworkStatusSubnetsItems0:
    r"""
    ROSANetworkSubnet groups public and private subnet and the availability zone in which the two subnets got created

    Attributes
    ----------
    availabilityZone : str, default is Undefined, required
        Availability zone of the subnet pair, for example us-west-2a
    privateSubnet : str, default is Undefined, required
        ID of the private subnet, for example subnet-07a20d6c41af2b725
    publicSubnet : str, default is Undefined, required
        ID of the public subnet, for example subnet-0f7e49a3ce68ff338
    """


    availabilityZone: str

    privateSubnet: str

    publicSubnet: str


