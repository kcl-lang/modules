"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema ClustertemplateV1:
    r"""
    ClustertemplateV1 is the Schema for the ClustertemplateV1s API. Manages a V1 Magnum cluster template resource within OpenStack.

    Attributes
    ----------
    apiVersion : str, default is "containerinfra.openstack.m.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ClustertemplateV1", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1Spec, default is Undefined, required
        spec
    status : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1Status, default is Undefined, optional
        status
    """


    apiVersion: "containerinfra.openstack.m.crossplane.io/v1alpha1" = "containerinfra.openstack.m.crossplane.io/v1alpha1"

    kind: "ClustertemplateV1" = "ClustertemplateV1"

    metadata?: v1.ObjectMeta

    spec: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1Spec

    status?: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1Status


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1Spec:
    r"""
    ClustertemplateV1Spec defines the desired state of ClustertemplateV1

    Attributes
    ----------
    forProvider : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecForProvider, default is Undefined, required
        for provider
    initProvider : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecForProvider

    initProvider?: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecProviderConfigRef

    writeConnectionSecretToRef?: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecWriteConnectionSecretToRef


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecForProvider:
    r"""
    containerinfra openstackm crossplane io v1alpha1 clustertemplate v1 spec for provider

    Attributes
    ----------
    apiserverPort : float, default is Undefined, optional
        The API server port for the Container
        Orchestration Engine for this cluster template. Changing this updates the
        API server port of the existing cluster template.
    clusterDistro : str, default is Undefined, optional
        The distro for the cluster (fedora-atomic,
        coreos, etc.). Changing this updates the cluster distro of the existing
        cluster template.
    coe : str, default is Undefined, optional
        The Container Orchestration Engine for this cluster
        template. Changing this updates the engine of the existing cluster
        template.
    dnsNameserver : str, default is Undefined, optional
        Address of the DNS nameserver that is used in
        nodes of the cluster. Changing this updates the DNS nameserver of the
        existing cluster template.
    dockerStorageDriver : str, default is Undefined, optional
        Docker storage driver. Changing this
        updates the Docker storage driver of the existing cluster template.
    dockerVolumeSize : float, default is Undefined, optional
        The size (in GB) of the Docker volume.
        Changing this updates the Docker volume size of the existing cluster
        template.
    externalNetworkId : str, default is Undefined, optional
        The ID of the external network that will
        be used for the cluster. Changing this updates the external network ID of
        the existing cluster template.
    fixedNetwork : str, default is Undefined, optional
        The fixed network that will be attached to the
        cluster. Changing this updates the fixed network of the existing cluster
        template.
    fixedSubnet : str, default is Undefined, optional
        The fixed subnet that will be attached to the
        cluster. Changing this updates the fixed subnet of the existing cluster
        template.
    flavor : str, default is Undefined, optional
        The flavor for the nodes of the cluster. Can be set via
        the OS_MAGNUM_FLAVOR environment variable. Changing this updates the
        flavor of the existing cluster template.
    floatingIpEnabled : bool, default is Undefined, optional
        Indicates whether created cluster should
        create floating IP for every node or not. Changing this updates the
        floating IP enabled attribute of the existing cluster template.
    hidden : bool, default is Undefined, optional
        Indicates whether the ClusterTemplate is hidden or not.
        Changing this updates the hidden attribute of the existing cluster
        template.
    httpProxy : str, default is Undefined, optional
        The address of a proxy for receiving all HTTP
        requests and relay them. Changing this updates the HTTP proxy address of
        the existing cluster template.
    httpsProxy : str, default is Undefined, optional
        The address of a proxy for receiving all HTTPS
        requests and relay them. Changing this updates the HTTPS proxy address of
        the existing cluster template.
    image : str, default is Undefined, optional
        The reference to an image that is used for nodes of the
        cluster. Can be set via the OS_MAGNUM_IMAGE environment variable.
        Changing this updates the image attribute of the existing cluster template.
    insecureRegistry : str, default is Undefined, optional
        The insecure registry URL for the cluster
        template. Changing this updates the insecure registry attribute of the
        existing cluster template.
    keypairId : str, default is Undefined, optional
        The name of the Compute service SSH keypair.
        Changing this updates the keypair of the existing cluster template.
    labels : {str:str}, default is Undefined, optional
        The list of key value pairs representing additional
        properties of the cluster template. Changing this updates the labels of the
        existing cluster template.
    masterFlavor : str, default is Undefined, optional
        The flavor for the master nodes. Can be set via
        the OS_MAGNUM_MASTER_FLAVOR environment variable. Changing this updates
        the master flavor of the existing cluster template.
    masterLbEnabled : bool, default is Undefined, optional
        Indicates whether created cluster should
        has a loadbalancer for master nodes or not. Changing this updates the
        attribute of the existing cluster template.
    name : str, default is Undefined, optional
        The name of the cluster template. Changing this updates
        the name of the existing cluster template.
    networkDriver : str, default is Undefined, optional
        The name of the driver for the container
        network. Changing this updates the network driver of the existing cluster
        template.
    noProxy : str, default is Undefined, optional
        A comma-separated list of IP addresses that shouldn't
        be used in the cluster. Changing this updates the no proxy list of the
        existing cluster template.
    public : bool, default is Undefined, optional
        Indicates whether cluster template should be public.
        Changing this updates the public attribute of the existing cluster
        template.
    region : str, default is Undefined, optional
        The region in which to obtain the V1 Container Infra
        client. A Container Infra client is needed to create a cluster template. If
        omitted,the region argument of the provider is used. Changing this
        creates a new cluster template.
    registryEnabled : bool, default is Undefined, optional
        Indicates whether Docker registry is enabled
        in the cluster. Changing this updates the registry enabled attribute of the
        existing cluster template.
    serverType : str, default is Undefined, optional
        The server type for the cluster template. Changing
        this updates the server type of the existing cluster template.
    tlsDisabled : bool, default is Undefined, optional
        Indicates whether the TLS should be disabled in
        the cluster. Changing this updates the attribute of the existing cluster.
    volumeDriver : str, default is Undefined, optional
        The name of the driver that is used for the
        volumes of the cluster nodes. Changing this updates the volume driver of
        the existing cluster template.
    """


    apiserverPort?: float

    clusterDistro?: str

    coe?: str

    dnsNameserver?: str

    dockerStorageDriver?: str

    dockerVolumeSize?: float

    externalNetworkId?: str

    fixedNetwork?: str

    fixedSubnet?: str

    flavor?: str

    floatingIpEnabled?: bool

    hidden?: bool

    httpProxy?: str

    httpsProxy?: str

    image?: str

    insecureRegistry?: str

    keypairId?: str

    labels?: {str:str}

    masterFlavor?: str

    masterLbEnabled?: bool

    name?: str

    networkDriver?: str

    noProxy?: str

    public?: bool

    region?: str

    registryEnabled?: bool

    serverType?: str

    tlsDisabled?: bool

    volumeDriver?: str


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    apiserverPort : float, default is Undefined, optional
        The API server port for the Container
        Orchestration Engine for this cluster template. Changing this updates the
        API server port of the existing cluster template.
    clusterDistro : str, default is Undefined, optional
        The distro for the cluster (fedora-atomic,
        coreos, etc.). Changing this updates the cluster distro of the existing
        cluster template.
    coe : str, default is Undefined, optional
        The Container Orchestration Engine for this cluster
        template. Changing this updates the engine of the existing cluster
        template.
    dnsNameserver : str, default is Undefined, optional
        Address of the DNS nameserver that is used in
        nodes of the cluster. Changing this updates the DNS nameserver of the
        existing cluster template.
    dockerStorageDriver : str, default is Undefined, optional
        Docker storage driver. Changing this
        updates the Docker storage driver of the existing cluster template.
    dockerVolumeSize : float, default is Undefined, optional
        The size (in GB) of the Docker volume.
        Changing this updates the Docker volume size of the existing cluster
        template.
    externalNetworkId : str, default is Undefined, optional
        The ID of the external network that will
        be used for the cluster. Changing this updates the external network ID of
        the existing cluster template.
    fixedNetwork : str, default is Undefined, optional
        The fixed network that will be attached to the
        cluster. Changing this updates the fixed network of the existing cluster
        template.
    fixedSubnet : str, default is Undefined, optional
        The fixed subnet that will be attached to the
        cluster. Changing this updates the fixed subnet of the existing cluster
        template.
    flavor : str, default is Undefined, optional
        The flavor for the nodes of the cluster. Can be set via
        the OS_MAGNUM_FLAVOR environment variable. Changing this updates the
        flavor of the existing cluster template.
    floatingIpEnabled : bool, default is Undefined, optional
        Indicates whether created cluster should
        create floating IP for every node or not. Changing this updates the
        floating IP enabled attribute of the existing cluster template.
    hidden : bool, default is Undefined, optional
        Indicates whether the ClusterTemplate is hidden or not.
        Changing this updates the hidden attribute of the existing cluster
        template.
    httpProxy : str, default is Undefined, optional
        The address of a proxy for receiving all HTTP
        requests and relay them. Changing this updates the HTTP proxy address of
        the existing cluster template.
    httpsProxy : str, default is Undefined, optional
        The address of a proxy for receiving all HTTPS
        requests and relay them. Changing this updates the HTTPS proxy address of
        the existing cluster template.
    image : str, default is Undefined, optional
        The reference to an image that is used for nodes of the
        cluster. Can be set via the OS_MAGNUM_IMAGE environment variable.
        Changing this updates the image attribute of the existing cluster template.
    insecureRegistry : str, default is Undefined, optional
        The insecure registry URL for the cluster
        template. Changing this updates the insecure registry attribute of the
        existing cluster template.
    keypairId : str, default is Undefined, optional
        The name of the Compute service SSH keypair.
        Changing this updates the keypair of the existing cluster template.
    labels : {str:str}, default is Undefined, optional
        The list of key value pairs representing additional
        properties of the cluster template. Changing this updates the labels of the
        existing cluster template.
    masterFlavor : str, default is Undefined, optional
        The flavor for the master nodes. Can be set via
        the OS_MAGNUM_MASTER_FLAVOR environment variable. Changing this updates
        the master flavor of the existing cluster template.
    masterLbEnabled : bool, default is Undefined, optional
        Indicates whether created cluster should
        has a loadbalancer for master nodes or not. Changing this updates the
        attribute of the existing cluster template.
    name : str, default is Undefined, optional
        The name of the cluster template. Changing this updates
        the name of the existing cluster template.
    networkDriver : str, default is Undefined, optional
        The name of the driver for the container
        network. Changing this updates the network driver of the existing cluster
        template.
    noProxy : str, default is Undefined, optional
        A comma-separated list of IP addresses that shouldn't
        be used in the cluster. Changing this updates the no proxy list of the
        existing cluster template.
    public : bool, default is Undefined, optional
        Indicates whether cluster template should be public.
        Changing this updates the public attribute of the existing cluster
        template.
    region : str, default is Undefined, optional
        The region in which to obtain the V1 Container Infra
        client. A Container Infra client is needed to create a cluster template. If
        omitted,the region argument of the provider is used. Changing this
        creates a new cluster template.
    registryEnabled : bool, default is Undefined, optional
        Indicates whether Docker registry is enabled
        in the cluster. Changing this updates the registry enabled attribute of the
        existing cluster template.
    serverType : str, default is Undefined, optional
        The server type for the cluster template. Changing
        this updates the server type of the existing cluster template.
    tlsDisabled : bool, default is Undefined, optional
        Indicates whether the TLS should be disabled in
        the cluster. Changing this updates the attribute of the existing cluster.
    volumeDriver : str, default is Undefined, optional
        The name of the driver that is used for the
        volumes of the cluster nodes. Changing this updates the volume driver of
        the existing cluster template.
    """


    apiserverPort?: float

    clusterDistro?: str

    coe?: str

    dnsNameserver?: str

    dockerStorageDriver?: str

    dockerVolumeSize?: float

    externalNetworkId?: str

    fixedNetwork?: str

    fixedSubnet?: str

    flavor?: str

    floatingIpEnabled?: bool

    hidden?: bool

    httpProxy?: str

    httpsProxy?: str

    image?: str

    insecureRegistry?: str

    keypairId?: str

    labels?: {str:str}

    masterFlavor?: str

    masterLbEnabled?: bool

    name?: str

    networkDriver?: str

    noProxy?: str

    public?: bool

    region?: str

    registryEnabled?: bool

    serverType?: str

    tlsDisabled?: bool

    volumeDriver?: str


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1SpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1Status:
    r"""
    ClustertemplateV1Status defines the observed state of ClustertemplateV1.

    Attributes
    ----------
    atProvider : ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1StatusAtProvider, default is Undefined, optional
        at provider
    conditions : [ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1StatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1StatusAtProvider

    conditions?: [ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1StatusConditionsItems0]

    observedGeneration?: int


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1StatusAtProvider:
    r"""
    containerinfra openstackm crossplane io v1alpha1 clustertemplate v1 status at provider

    Attributes
    ----------
    apiserverPort : float, default is Undefined, optional
        The API server port for the Container
        Orchestration Engine for this cluster template. Changing this updates the
        API server port of the existing cluster template.
    clusterDistro : str, default is Undefined, optional
        The distro for the cluster (fedora-atomic,
        coreos, etc.). Changing this updates the cluster distro of the existing
        cluster template.
    coe : str, default is Undefined, optional
        The Container Orchestration Engine for this cluster
        template. Changing this updates the engine of the existing cluster
        template.
    createdAt : str, default is Undefined, optional
        The time at which cluster template was created.
    dnsNameserver : str, default is Undefined, optional
        Address of the DNS nameserver that is used in
        nodes of the cluster. Changing this updates the DNS nameserver of the
        existing cluster template.
    dockerStorageDriver : str, default is Undefined, optional
        Docker storage driver. Changing this
        updates the Docker storage driver of the existing cluster template.
    dockerVolumeSize : float, default is Undefined, optional
        The size (in GB) of the Docker volume.
        Changing this updates the Docker volume size of the existing cluster
        template.
    externalNetworkId : str, default is Undefined, optional
        The ID of the external network that will
        be used for the cluster. Changing this updates the external network ID of
        the existing cluster template.
    fixedNetwork : str, default is Undefined, optional
        The fixed network that will be attached to the
        cluster. Changing this updates the fixed network of the existing cluster
        template.
    fixedSubnet : str, default is Undefined, optional
        The fixed subnet that will be attached to the
        cluster. Changing this updates the fixed subnet of the existing cluster
        template.
    flavor : str, default is Undefined, optional
        The flavor for the nodes of the cluster. Can be set via
        the OS_MAGNUM_FLAVOR environment variable. Changing this updates the
        flavor of the existing cluster template.
    floatingIpEnabled : bool, default is Undefined, optional
        Indicates whether created cluster should
        create floating IP for every node or not. Changing this updates the
        floating IP enabled attribute of the existing cluster template.
    hidden : bool, default is Undefined, optional
        Indicates whether the ClusterTemplate is hidden or not.
        Changing this updates the hidden attribute of the existing cluster
        template.
    httpProxy : str, default is Undefined, optional
        The address of a proxy for receiving all HTTP
        requests and relay them. Changing this updates the HTTP proxy address of
        the existing cluster template.
    httpsProxy : str, default is Undefined, optional
        The address of a proxy for receiving all HTTPS
        requests and relay them. Changing this updates the HTTPS proxy address of
        the existing cluster template.
    id : str, default is Undefined, optional
        id
    image : str, default is Undefined, optional
        The reference to an image that is used for nodes of the
        cluster. Can be set via the OS_MAGNUM_IMAGE environment variable.
        Changing this updates the image attribute of the existing cluster template.
    insecureRegistry : str, default is Undefined, optional
        The insecure registry URL for the cluster
        template. Changing this updates the insecure registry attribute of the
        existing cluster template.
    keypairId : str, default is Undefined, optional
        The name of the Compute service SSH keypair.
        Changing this updates the keypair of the existing cluster template.
    labels : {str:str}, default is Undefined, optional
        The list of key value pairs representing additional
        properties of the cluster template. Changing this updates the labels of the
        existing cluster template.
    masterFlavor : str, default is Undefined, optional
        The flavor for the master nodes. Can be set via
        the OS_MAGNUM_MASTER_FLAVOR environment variable. Changing this updates
        the master flavor of the existing cluster template.
    masterLbEnabled : bool, default is Undefined, optional
        Indicates whether created cluster should
        has a loadbalancer for master nodes or not. Changing this updates the
        attribute of the existing cluster template.
    name : str, default is Undefined, optional
        The name of the cluster template. Changing this updates
        the name of the existing cluster template.
    networkDriver : str, default is Undefined, optional
        The name of the driver for the container
        network. Changing this updates the network driver of the existing cluster
        template.
    noProxy : str, default is Undefined, optional
        A comma-separated list of IP addresses that shouldn't
        be used in the cluster. Changing this updates the no proxy list of the
        existing cluster template.
    projectId : str, default is Undefined, optional
        The project of the cluster template. Required if
        admin wants to create a cluster template in another project. Changing this
        creates a new cluster template.
    public : bool, default is Undefined, optional
        Indicates whether cluster template should be public.
        Changing this updates the public attribute of the existing cluster
        template.
    region : str, default is Undefined, optional
        The region in which to obtain the V1 Container Infra
        client. A Container Infra client is needed to create a cluster template. If
        omitted,the region argument of the provider is used. Changing this
        creates a new cluster template.
    registryEnabled : bool, default is Undefined, optional
        Indicates whether Docker registry is enabled
        in the cluster. Changing this updates the registry enabled attribute of the
        existing cluster template.
    serverType : str, default is Undefined, optional
        The server type for the cluster template. Changing
        this updates the server type of the existing cluster template.
    tlsDisabled : bool, default is Undefined, optional
        Indicates whether the TLS should be disabled in
        the cluster. Changing this updates the attribute of the existing cluster.
    updatedAt : str, default is Undefined, optional
        The time at which cluster template was created.
    userId : str, default is Undefined, optional
        The user of the cluster template. Required if admin
        wants to create a cluster template for another user. Changing this creates
        a new cluster template.
    volumeDriver : str, default is Undefined, optional
        The name of the driver that is used for the
        volumes of the cluster nodes. Changing this updates the volume driver of
        the existing cluster template.
    """


    apiserverPort?: float

    clusterDistro?: str

    coe?: str

    createdAt?: str

    dnsNameserver?: str

    dockerStorageDriver?: str

    dockerVolumeSize?: float

    externalNetworkId?: str

    fixedNetwork?: str

    fixedSubnet?: str

    flavor?: str

    floatingIpEnabled?: bool

    hidden?: bool

    httpProxy?: str

    httpsProxy?: str

    id?: str

    image?: str

    insecureRegistry?: str

    keypairId?: str

    labels?: {str:str}

    masterFlavor?: str

    masterLbEnabled?: bool

    name?: str

    networkDriver?: str

    noProxy?: str

    projectId?: str

    public?: bool

    region?: str

    registryEnabled?: bool

    serverType?: str

    tlsDisabled?: bool

    updatedAt?: str

    userId?: str

    volumeDriver?: str


schema ContainerinfraOpenstackmCrossplaneIoV1alpha1ClustertemplateV1StatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


