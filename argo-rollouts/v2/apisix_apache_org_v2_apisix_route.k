"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema ApisixRoute:
    """
    apisix apache org v2 apisix route

    Attributes
    ----------
    apiVersion : str, default is "apisix.apache.org/v2", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "ApisixRoute", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ApisixApacheOrgV2ApisixRouteSpec, default is Undefined, optional
        spec
    status : ApisixApacheOrgV2ApisixRouteStatus, default is Undefined, optional
        status
    """


    apiVersion: "apisix.apache.org/v2" = "apisix.apache.org/v2"

    kind: "ApisixRoute" = "ApisixRoute"

    metadata?: v1.ObjectMeta

    spec?: ApisixApacheOrgV2ApisixRouteSpec

    status?: ApisixApacheOrgV2ApisixRouteStatus


schema ApisixApacheOrgV2ApisixRouteSpec:
    """
    apisix apache org v2 apisix route spec

    Attributes
    ----------
    http : [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0], default is Undefined, optional
        http
    stream : [ApisixApacheOrgV2ApisixRouteSpecStreamItems0], default is Undefined, optional
        stream
    """


    http?: [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0]

    stream?: [ApisixApacheOrgV2ApisixRouteSpecStreamItems0]


    check:
        len(http) >= 1
        len(stream) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0:
    """
    apisix apache org v2 apisix route spec HTTP items0

    Attributes
    ----------
    authentication : ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Authentication, default is Undefined, optional
        authentication
    backends : [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0BackendsItems0], default is Undefined, optional
        backends
    match : ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Match, default is Undefined, optional
        match
    name : str, default is Undefined, optional
        name
    plugin_config_name : str, default is Undefined, optional
        plugin config name
    plugins : [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0PluginsItems0], default is Undefined, optional
        plugins
    priority : int, default is Undefined, optional
        priority
    timeout : ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Timeout, default is Undefined, optional
        timeout
    upstreams : [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0UpstreamsItems0], default is Undefined, optional
        Upstreams refer to ApisixUpstream CRD
    websocket : bool, default is Undefined, optional
        websocket
    """


    authentication?: ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Authentication

    backends?: [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0BackendsItems0]

    match?: ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Match

    name?: str

    plugin_config_name?: str

    plugins?: [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0PluginsItems0]

    priority?: int

    timeout?: ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Timeout

    upstreams?: [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0UpstreamsItems0]

    websocket?: bool


    check:
        len(backends) >= 1
        len(name) >= 1
        len(plugin_config_name) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Authentication:
    """
    apisix apache org v2 apisix route spec HTTP items0 authentication

    Attributes
    ----------
    enable : bool, default is Undefined, required
        enable
    jwtAuth : ApisixApacheOrgV2ApisixRouteSpecHTTPItems0AuthenticationJwtAuth, default is Undefined, optional
        jwt auth
    keyAuth : ApisixApacheOrgV2ApisixRouteSpecHTTPItems0AuthenticationKeyAuth, default is Undefined, optional
        key auth
    $type : str, default is Undefined, optional
        type
    """


    enable: bool

    jwtAuth?: ApisixApacheOrgV2ApisixRouteSpecHTTPItems0AuthenticationJwtAuth

    keyAuth?: ApisixApacheOrgV2ApisixRouteSpecHTTPItems0AuthenticationKeyAuth

    $type?: "basicAuth" | "keyAuth" | "jwtAuth" | "wolfRBAC" | "hmacAuth"


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0AuthenticationJwtAuth:
    """
    apisix apache org v2 apisix route spec HTTP items0 authentication jwt auth

    Attributes
    ----------
    cookie : str, default is Undefined, optional
        cookie
    header : str, default is Undefined, optional
        header
    query : str, default is Undefined, optional
        query
    """


    cookie?: str

    header?: str

    query?: str


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0AuthenticationKeyAuth:
    """
    apisix apache org v2 apisix route spec HTTP items0 authentication key auth

    Attributes
    ----------
    header : str, default is Undefined, optional
        header
    """


    header?: str


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0BackendsItems0:
    """
    apisix apache org v2 apisix route spec HTTP items0 backends items0

    Attributes
    ----------
    resolveGranularity : str, default is Undefined, optional
        resolve granularity
    serviceName : str, default is Undefined, optional
        service name
    servicePort : int | str, default is Undefined, optional
        service port
    subset : str, default is Undefined, optional
        subset
    weight : int, default is Undefined, optional
        weight
    """


    resolveGranularity?: "endpoint" | "service"

    serviceName?: str

    servicePort?: int | str

    subset?: str

    weight?: int


    check:
        len(serviceName) >= 1
        weight >= 0


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Match:
    """
    apisix apache org v2 apisix route spec HTTP items0 match

    Attributes
    ----------
    exprs : [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0MatchExprsItems0], default is Undefined, optional
        exprs
    hosts : [str], default is Undefined, optional
        hosts
    methods : [str], default is Undefined, optional
        methods
    paths : [str], default is Undefined, required
        paths
    remoteAddrs : [str], default is Undefined, optional
        remote addrs
    """


    exprs?: [ApisixApacheOrgV2ApisixRouteSpecHTTPItems0MatchExprsItems0]

    hosts?: [str]

    methods?: [str]

    paths: [str]

    remoteAddrs?: [str]


    check:
        len(exprs) >= 1
        len(hosts) >= 1
        len(methods) >= 1
        len(paths) >= 1
        len(remoteAddrs) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0MatchExprsItems0:
    """
    apisix apache org v2 apisix route spec HTTP items0 match exprs items0

    Attributes
    ----------
    op : str, default is Undefined, optional
        op
    set : [str], default is Undefined, optional
        set
    subject : ApisixApacheOrgV2ApisixRouteSpecHTTPItems0MatchExprsItems0Subject, default is Undefined, optional
        subject
    value : str, default is Undefined, optional
        value
    """


    op?: "Equal" | "NotEqual" | "GreaterThan" | "GreaterThanEqual" | "LessThan" | "LessThanEqual" | "In" | "NotIn" | "RegexMatch" | "RegexNotMatch" | "RegexMatchCaseInsensitive" | "RegexNotMatchCaseInsensitive"

    set?: [str]

    subject?: ApisixApacheOrgV2ApisixRouteSpecHTTPItems0MatchExprsItems0Subject

    value?: str


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0MatchExprsItems0Subject:
    """
    apisix apache org v2 apisix route spec HTTP items0 match exprs items0 subject

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    scope : str, default is Undefined, required
        scope
    """


    name?: str

    scope: "Cookie" | "Header" | "Path" | "Query" | "Variable"


    check:
        len(name) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0PluginsItems0:
    """
    apisix apache org v2 apisix route spec HTTP items0 plugins items0

    Attributes
    ----------
    config : any, default is Undefined, optional
        config
    enable : bool, default is Undefined, optional
        enable
    name : str, default is Undefined, optional
        name
    """


    config?: any

    enable?: bool

    name?: str


    check:
        len(name) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0Timeout:
    """
    apisix apache org v2 apisix route spec HTTP items0 timeout

    Attributes
    ----------
    connect : str, default is Undefined, optional
        connect
    read : str, default is Undefined, optional
        read
    send : str, default is Undefined, optional
        send
    """


    connect?: str

    read?: str

    send?: str


schema ApisixApacheOrgV2ApisixRouteSpecHTTPItems0UpstreamsItems0:
    """
    ApisixRouteUpstreamReference contains a ApisixUpstream CRD reference

    Attributes
    ----------
    name : str, default is Undefined, optional
        name
    weight : int, default is Undefined, optional
        weight
    """


    name?: str

    weight?: int


schema ApisixApacheOrgV2ApisixRouteSpecStreamItems0:
    """
    apisix apache org v2 apisix route spec stream items0

    Attributes
    ----------
    backend : ApisixApacheOrgV2ApisixRouteSpecStreamItems0Backend, default is Undefined, required
        backend
    match : ApisixApacheOrgV2ApisixRouteSpecStreamItems0Match, default is Undefined, required
        match
    name : str, default is Undefined, required
        name
    plugins : [ApisixApacheOrgV2ApisixRouteSpecStreamItems0PluginsItems0], default is Undefined, optional
        plugins
    $protocol : str, default is Undefined, required
        protocol
    """


    backend: ApisixApacheOrgV2ApisixRouteSpecStreamItems0Backend

    match: ApisixApacheOrgV2ApisixRouteSpecStreamItems0Match

    name: str

    plugins?: [ApisixApacheOrgV2ApisixRouteSpecStreamItems0PluginsItems0]

    $protocol: "TCP" | "UDP"


    check:
        len(name) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecStreamItems0Backend:
    """
    apisix apache org v2 apisix route spec stream items0 backend

    Attributes
    ----------
    resolveGranularity : str, default is Undefined, optional
        resolve granularity
    serviceName : str, default is Undefined, required
        service name
    servicePort : int | str, default is Undefined, required
        service port
    subset : str, default is Undefined, optional
        subset
    """


    resolveGranularity?: "endpoint" | "service"

    serviceName: str

    servicePort: int | str

    subset?: str


    check:
        len(serviceName) >= 1


schema ApisixApacheOrgV2ApisixRouteSpecStreamItems0Match:
    """
    apisix apache org v2 apisix route spec stream items0 match

    Attributes
    ----------
    host : str, default is Undefined, optional
        host
    ingressPort : int, default is Undefined, required
        ingress port
    """


    host?: str

    ingressPort: int


    check:
        ingressPort <= 65535
        ingressPort >= 1


schema ApisixApacheOrgV2ApisixRouteSpecStreamItems0PluginsItems0:
    """
    apisix apache org v2 apisix route spec stream items0 plugins items0

    Attributes
    ----------
    config : any, default is Undefined, optional
        config
    enable : bool, default is Undefined, optional
        enable
    name : str, default is Undefined, optional
        name
    """


    config?: any

    enable?: bool

    name?: str


    check:
        len(name) >= 1


schema ApisixApacheOrgV2ApisixRouteStatus:
    """
    apisix apache org v2 apisix route status

    Attributes
    ----------
    conditions : [ApisixApacheOrgV2ApisixRouteStatusConditionsItems0], default is Undefined, optional
        conditions
    """


    conditions?: [ApisixApacheOrgV2ApisixRouteStatusConditionsItems0]


schema ApisixApacheOrgV2ApisixRouteStatusConditionsItems0:
    """
    apisix apache org v2 apisix route status conditions items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        message
    observedGeneration : int, default is Undefined, optional
        observed generation
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, optional
        status
    $type : str, default is Undefined, optional
        type
    """


    message?: str

    observedGeneration?: int

    reason?: str

    status?: str

    $type?: str


