"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema IPAddress:
    r"""
    IPAddress is the Schema for the ipaddress API.

    Attributes
    ----------
    apiVersion : str, default is "ipam.cluster.x-k8s.io/v1beta2", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "IPAddress", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : IpamClusterxK8sIoV1beta2IPAddressSpec, default is Undefined, required
        spec
    """


    apiVersion: "ipam.cluster.x-k8s.io/v1beta2" = "ipam.cluster.x-k8s.io/v1beta2"

    kind: "IPAddress" = "IPAddress"

    metadata?: v1.ObjectMeta

    spec: IpamClusterxK8sIoV1beta2IPAddressSpec


schema IpamClusterxK8sIoV1beta2IPAddressSpec:
    r"""
    spec is the desired state of IPAddress.

    Attributes
    ----------
    address : str, default is Undefined, required
        address is the IP address.
    claimRef : IpamClusterxK8sIoV1beta2IPAddressSpecClaimRef, default is Undefined, required
        claim ref
    gateway : str, default is Undefined, optional
        gateway is the network gateway of the network the address is from.
    poolRef : IpamClusterxK8sIoV1beta2IPAddressSpecPoolRef, default is Undefined, required
        pool ref
    prefix : int, default is Undefined, required
        prefix is the prefix of the address.
    """


    address: str

    claimRef: IpamClusterxK8sIoV1beta2IPAddressSpecClaimRef

    gateway?: str

    poolRef: IpamClusterxK8sIoV1beta2IPAddressSpecPoolRef

    prefix: int


    check:
        len(address) <= 39
        len(address) >= 1
        len(gateway) <= 39 if gateway
        len(gateway) >= 1 if gateway
        prefix <= 128
        prefix >= 0


schema IpamClusterxK8sIoV1beta2IPAddressSpecClaimRef:
    r"""
    claimRef is a reference to the claim this IPAddress was created for.

    Attributes
    ----------
    name : str, default is Undefined, required
        name of the IPAddressClaim.
        name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
    """


    name: str


    check:
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")


schema IpamClusterxK8sIoV1beta2IPAddressSpecPoolRef:
    r"""
    poolRef is a reference to the pool that this IPAddress was created from.

    Attributes
    ----------
    apiGroup : str, default is Undefined, required
        apiGroup of the IPPool.
        apiGroup must be fully qualified domain name.
    kind : str, default is Undefined, required
        kind of the IPPool.
        kind must consist of alphanumeric characters or '-', start with an alphabetic character, and end with an alphanumeric character.
    name : str, default is Undefined, required
        name of the IPPool.
        name must consist of lower case alphanumeric characters, '-' or '.', and must start and end with an alphanumeric character.
    """


    apiGroup: str

    kind: str

    name: str


    check:
        len(apiGroup) <= 253
        len(apiGroup) >= 1
        _regex_match(str(apiGroup), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")
        len(kind) <= 63
        len(kind) >= 1
        _regex_match(str(kind), r"^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$")
        len(name) <= 253
        len(name) >= 1
        _regex_match(str(name), r"^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$")


