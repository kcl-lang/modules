"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema LoadbalancerV2:
    r"""
    LoadbalancerV2 is the Schema for the LoadbalancerV2s API. Manages a V2 loadbalancer resource within OpenStack.

    Attributes
    ----------
    apiVersion : str, default is "lb.openstack.m.crossplane.io/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "LoadbalancerV2", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2Spec, default is Undefined, required
        spec
    status : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2Status, default is Undefined, optional
        status
    """


    apiVersion: "lb.openstack.m.crossplane.io/v1alpha1" = "lb.openstack.m.crossplane.io/v1alpha1"

    kind: "LoadbalancerV2" = "LoadbalancerV2"

    metadata?: v1.ObjectMeta

    spec: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2Spec

    status?: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2Status


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2Spec:
    r"""
    LoadbalancerV2Spec defines the desired state of LoadbalancerV2

    Attributes
    ----------
    forProvider : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecForProvider, default is Undefined, required
        for provider
    initProvider : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecForProvider

    initProvider?: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecProviderConfigRef

    writeConnectionSecretToRef?: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecWriteConnectionSecretToRef


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecForProvider:
    r"""
    lb openstackm crossplane io v1alpha1 loadbalancer v2 spec for provider

    Attributes
    ----------
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the Loadbalancer.
        A valid value is true (UP) or false (DOWN).
    availabilityZone : str, default is Undefined, optional
        The availability zone of the Loadbalancer.
        Changing this creates a new loadbalancer. Available only for Octavia
        minor version 2.14 or later.
    description : str, default is Undefined, optional
        Human-readable description for the Loadbalancer.
    flavorId : str, default is Undefined, optional
        The UUID of a flavor. Changing this creates a new
        loadbalancer.
    loadbalancerProvider : str, default is Undefined, optional
        The name of the provider. Changing this
        creates a new loadbalancer.
    name : str, default is Undefined, optional
        Human-readable name for the Loadbalancer. Does not have
        to be unique.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create an LB member. If omitted, the
        region argument of the provider is used. Changing this creates a new
        LB member.
    securityGroupIds : [str], default is Undefined, optional
        A list of security group IDs to apply to the
        loadbalancer. The security groups must be specified by ID and not name (as
        opposed to how they are configured with the Compute Instance).
    tags : [str], default is Undefined, optional
        A list of simple strings assigned to the loadbalancer.
        Available only for Octavia minor version 2.5 or later.
    tenantId : str, default is Undefined, optional
        Required for admins. The UUID of the tenant who owns
        the Loadbalancer.  Only administrative users can specify a tenant UUID
        other than their own.  Changing this creates a new loadbalancer.
    vipAddress : str, default is Undefined, optional
        The ip address of the load balancer.
        Changing this creates a new loadbalancer.
    vipNetworkId : str, default is Undefined, optional
        The network on which to allocate the
        Loadbalancer's address. A tenant can only create Loadbalancers on networks
        authorized by policy (e.g. networks that belong to them or networks that
        are shared).  Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    vipPortId : str, default is Undefined, optional
        The port UUID that the loadbalancer will use.
        Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    vipQosPolicyId : str, default is Undefined, optional
        The ID of the QoS Policy which will
        be applied to the Virtual IP (VIP).
    vipSubnetId : str, default is Undefined, optional
        The subnet on which to allocate the
        Loadbalancer's address. A tenant can only create Loadbalancers on networks
        authorized by policy (e.g. networks that belong to them or networks that
        are shared).  Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    """


    adminStateUp?: bool

    availabilityZone?: str

    description?: str

    flavorId?: str

    loadbalancerProvider?: str

    name?: str

    region?: str

    securityGroupIds?: [str]

    tags?: [str]

    tenantId?: str

    vipAddress?: str

    vipNetworkId?: str

    vipPortId?: str

    vipQosPolicyId?: str

    vipSubnetId?: str


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the Loadbalancer.
        A valid value is true (UP) or false (DOWN).
    availabilityZone : str, default is Undefined, optional
        The availability zone of the Loadbalancer.
        Changing this creates a new loadbalancer. Available only for Octavia
        minor version 2.14 or later.
    description : str, default is Undefined, optional
        Human-readable description for the Loadbalancer.
    flavorId : str, default is Undefined, optional
        The UUID of a flavor. Changing this creates a new
        loadbalancer.
    loadbalancerProvider : str, default is Undefined, optional
        The name of the provider. Changing this
        creates a new loadbalancer.
    name : str, default is Undefined, optional
        Human-readable name for the Loadbalancer. Does not have
        to be unique.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create an LB member. If omitted, the
        region argument of the provider is used. Changing this creates a new
        LB member.
    securityGroupIds : [str], default is Undefined, optional
        A list of security group IDs to apply to the
        loadbalancer. The security groups must be specified by ID and not name (as
        opposed to how they are configured with the Compute Instance).
    tags : [str], default is Undefined, optional
        A list of simple strings assigned to the loadbalancer.
        Available only for Octavia minor version 2.5 or later.
    tenantId : str, default is Undefined, optional
        Required for admins. The UUID of the tenant who owns
        the Loadbalancer.  Only administrative users can specify a tenant UUID
        other than their own.  Changing this creates a new loadbalancer.
    vipAddress : str, default is Undefined, optional
        The ip address of the load balancer.
        Changing this creates a new loadbalancer.
    vipNetworkId : str, default is Undefined, optional
        The network on which to allocate the
        Loadbalancer's address. A tenant can only create Loadbalancers on networks
        authorized by policy (e.g. networks that belong to them or networks that
        are shared).  Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    vipPortId : str, default is Undefined, optional
        The port UUID that the loadbalancer will use.
        Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    vipQosPolicyId : str, default is Undefined, optional
        The ID of the QoS Policy which will
        be applied to the Virtual IP (VIP).
    vipSubnetId : str, default is Undefined, optional
        The subnet on which to allocate the
        Loadbalancer's address. A tenant can only create Loadbalancers on networks
        authorized by policy (e.g. networks that belong to them or networks that
        are shared).  Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    """


    adminStateUp?: bool

    availabilityZone?: str

    description?: str

    flavorId?: str

    loadbalancerProvider?: str

    name?: str

    region?: str

    securityGroupIds?: [str]

    tags?: [str]

    tenantId?: str

    vipAddress?: str

    vipNetworkId?: str

    vipPortId?: str

    vipQosPolicyId?: str

    vipSubnetId?: str


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2SpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2Status:
    r"""
    LoadbalancerV2Status defines the observed state of LoadbalancerV2.

    Attributes
    ----------
    atProvider : LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2StatusAtProvider, default is Undefined, optional
        at provider
    conditions : [LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2StatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2StatusAtProvider

    conditions?: [LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2StatusConditionsItems0]

    observedGeneration?: int


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2StatusAtProvider:
    r"""
    lb openstackm crossplane io v1alpha1 loadbalancer v2 status at provider

    Attributes
    ----------
    adminStateUp : bool, default is Undefined, optional
        The administrative state of the Loadbalancer.
        A valid value is true (UP) or false (DOWN).
    availabilityZone : str, default is Undefined, optional
        The availability zone of the Loadbalancer.
        Changing this creates a new loadbalancer. Available only for Octavia
        minor version 2.14 or later.
    description : str, default is Undefined, optional
        Human-readable description for the Loadbalancer.
    flavorId : str, default is Undefined, optional
        The UUID of a flavor. Changing this creates a new
        loadbalancer.
    id : str, default is Undefined, optional
        id
    loadbalancerProvider : str, default is Undefined, optional
        The name of the provider. Changing this
        creates a new loadbalancer.
    name : str, default is Undefined, optional
        Human-readable name for the Loadbalancer. Does not have
        to be unique.
    region : str, default is Undefined, optional
        The region in which to obtain the V2 Networking client.
        A Networking client is needed to create an LB member. If omitted, the
        region argument of the provider is used. Changing this creates a new
        LB member.
    securityGroupIds : [str], default is Undefined, optional
        A list of security group IDs to apply to the
        loadbalancer. The security groups must be specified by ID and not name (as
        opposed to how they are configured with the Compute Instance).
    tags : [str], default is Undefined, optional
        A list of simple strings assigned to the loadbalancer.
        Available only for Octavia minor version 2.5 or later.
    tenantId : str, default is Undefined, optional
        Required for admins. The UUID of the tenant who owns
        the Loadbalancer.  Only administrative users can specify a tenant UUID
        other than their own.  Changing this creates a new loadbalancer.
    vipAddress : str, default is Undefined, optional
        The ip address of the load balancer.
        Changing this creates a new loadbalancer.
    vipNetworkId : str, default is Undefined, optional
        The network on which to allocate the
        Loadbalancer's address. A tenant can only create Loadbalancers on networks
        authorized by policy (e.g. networks that belong to them or networks that
        are shared).  Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    vipPortId : str, default is Undefined, optional
        The port UUID that the loadbalancer will use.
        Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    vipQosPolicyId : str, default is Undefined, optional
        The ID of the QoS Policy which will
        be applied to the Virtual IP (VIP).
    vipSubnetId : str, default is Undefined, optional
        The subnet on which to allocate the
        Loadbalancer's address. A tenant can only create Loadbalancers on networks
        authorized by policy (e.g. networks that belong to them or networks that
        are shared).  Changing this creates a new loadbalancer. Exactly one of
        vip_subnet_id, vip_network_id or vip_port_id has to be defined.
    """


    adminStateUp?: bool

    availabilityZone?: str

    description?: str

    flavorId?: str

    id?: str

    loadbalancerProvider?: str

    name?: str

    region?: str

    securityGroupIds?: [str]

    tags?: [str]

    tenantId?: str

    vipAddress?: str

    vipNetworkId?: str

    vipPortId?: str

    vipQosPolicyId?: str

    vipSubnetId?: str


schema LbOpenstackmCrossplaneIoV1alpha1LoadbalancerV2StatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


